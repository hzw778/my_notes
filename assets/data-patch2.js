window.TAB_PATCH = window.TAB_PATCH || {};
window.TAB_PATCH["java"] = window.TAB_PATCH["java"] || {};
window.TAB_PATCH["java"]["ConcurrentHashMap 底层原理是什么"] = "<p><strong>核心回答：</strong>JDK 1.8 的 ConcurrentHashMap = <code>Node 数组 + 链表/红黑树 + CAS + synchronized</code>，锁粒度细化到<strong>单个桶头节点</strong>，读操作无锁（volatile 保证可见性）。</p><ol><li>put 流程：计算 hash → CAS 尝试插入空桶（无锁快速路径）→ 桶非空则 synchronized 锁住头节点再插入/树化</li><li>get 无锁：Node 的 val 和 next 用 volatile 修饰，保证读可见性</li><li>扩容：多线程协助扩容（helpTransfer），每个线程负责一段桶区间（stride），迁移完的桶放 ForwardingNode 指引后续请求去新表</li><li>size 统计：baseCount + CounterCell 分散计数（类似 LongAdder），避免热点</li><li>与 1.7 的区别：1.7 是 Segment 分段锁（默认 16 段，锁粒度是段），1.8 取消 Segment，直接锁桶头，并发度更高</li></ol><p><strong>面试背诵版：</strong>1.8 的 ConcurrentHashMap 用 CAS + synchronized 实现细粒度锁：桶为空用 CAS 插入，桶非空只 synchronized 头节点，不同桶之间完全并行；读操作完全无锁，靠 volatile 保证可见性；扩容支持多线程协助迁移。相比 1.7 的 Segment 分段锁，锁粒度更细、内存占用更小。</p>";
window.TAB_PATCH["java"]["ReentrantLock 和 synchronized 有什么区别"] = "<p><strong>核心回答：</strong>两者都是可重入互斥锁，核心区别是 synchronized 是 JVM 内置的<strong>隐式锁</strong>，ReentrantLock 是 JDK 层实现的<strong>显式锁</strong>（基于 AQS），功能更丰富。</p><ol><li>用法：synchronized 自动加锁释放（进出代码块），ReentrantLock 必须手动 lock()/unlock()，且 unlock 要放 finally</li><li>功能：ReentrantLock 支持<strong>公平锁</strong>、<strong>可中断锁</strong> lockInterruptibly()、<strong>超时获取</strong> tryLock(timeout)、<strong>多条件变量</strong> Condition；synchronized 都不支持</li><li>性能：JDK 1.6 后 synchronized 有锁升级优化（偏向→轻量级→重量级），常规场景性能已接近</li><li>底层：synchronized 依赖对象头 Mark Word 和 monitor；ReentrantLock 依赖 AQS 的 state + CLH 队列</li></ol><p><strong>选择：</strong>简单同步用 synchronized；需要公平、可中断、超时、多 Condition 时用 ReentrantLock。</p><p><strong>面试背诵版：</strong>synchronized 是 JVM 关键字，自动释放、有锁升级优化，够用且简单；ReentrantLock 是基于 AQS 的 API 级锁，要手动释放，但提供公平锁、tryLock 超时、可中断、Condition 多条件队列等高级能力。一般场景优先 synchronized，有高级需求才用 ReentrantLock。</p>";
window.TAB_PATCH["java"]["Spring MVC 请求流程是什么"] = "<p><strong>核心回答：</strong>请求进入 <strong>DispatcherServlet</strong>（前端控制器）统一分发：查 HandlerMapping 找到 Handler → 通过 HandlerAdapter 执行 Controller → 返回 ModelAndView → ViewResolver 解析视图 → 渲染返回。</p><ol><li>请求到达 DispatcherServlet（web.xml / SpringBoot 自动注册）</li><li>HandlerMapping 根据 URL 解析出 Handler（Controller 方法）及其拦截器链</li><li>HandlerAdapter 适配执行 Controller 方法（参数解析、数据绑定、@ResponseBody 处理）</li><li>Controller 返回 ModelAndView（@ResponseBody/@RestController 则直接由 HttpMessageConverter 写 JSON，跳过视图）</li><li>ViewResolver 解析出 View，渲染后写回响应</li></ol><p><strong>面试背诵版：</strong>所有请求先到 DispatcherServlet，它查 HandlerMapping 定位到具体 Controller 方法，由 HandlerAdapter 执行并处理参数绑定，返回结果如果是视图就经 ViewResolver 渲染，如果是 @ResponseBody 就由消息转换器直接序列化成 JSON 返回。DispatcherServlet 是核心，解耦了请求与具体处理逻辑。</p>";

window.TAB_PATCH["agent"] = window.TAB_PATCH["agent"] || {};
window.TAB_PATCH["agent"]["Agent 如何和环境交互"] = "<p><strong>核心回答：</strong>Agent 与环境的交互遵循<strong>感知 → 决策 → 行动 → 观察反馈</strong>的闭环：通过工具调用（Tool/Action）改变环境，通过工具返回结果和上下文感知环境状态，循环直到任务完成。</p><ol><li>感知：读取用户输入、系统消息、工具返回结果、文件/数据库等环境状态</li><li>决策：LLM 基于当前上下文（历史 + 观察）推理下一步动作（ReAct 模式：Thought → Action）</li><li>行动：调用工具执行（执行命令、读写文件、调 API、搜索）</li><li>观察：把执行结果（Observation）拼回上下文，进入下一轮循环</li><li>终止：模型输出最终答案或达到停止条件（步数/时间/token 上限）</li></ol><p><strong>面试背诵版：</strong>Agent 通过 Agent Loop 与环境交互：每轮把目标、历史动作和观察结果放进上下文，让 LLM 决策下一个工具调用，工具在真实环境执行后把结果返回给模型作为观察，模型据此调整策略继续循环，直到任务完成。本质是用工具调用作为 Agent 作用环境的唯一动作接口，用观察反馈闭环修正。</p>";
window.TAB_PATCH["agent"]["为什么 Agent 需要 Sandbox"] = "<p><strong>核心回答：</strong>因为 Agent 会<strong>真实执行不可逆的副作用操作</strong>（删文件、发请求、改数据库），而 LLM 输出有幻觉和被注入攻击的风险，沙箱保证即使模型犯错或被诱导，破坏也被限制在隔离环境内。</p><ol><li>安全隔离：防止误删宿主文件、泄漏密钥、攻击内网（Agent 被提示词注入后可能执行恶意命令）</li><li>可重复：沙箱内环境可快照、回滚，方便重试和评测</li><li>权限收敛：默认无网络、受限文件系统，按需白名单放开</li><li>资源限制：限制 CPU/内存/时长，防止死循环拖垮宿主机</li></ol><p><strong>典型实现：</strong>Docker 容器、microVM（Firecracker）、云端临时虚拟机（如 Manus 的沙箱）、WebAssembly 隔离。</p><p><strong>面试背诵版：</strong>Agent 直接执行代码和命令，输出又不可 100% 信任（幻觉 + 提示词注入风险），一旦执行 rm -rf 或外发敏感数据就是生产事故。沙箱把执行环境与宿主隔离，限制文件、网络、资源权限，并提供快照回滚能力，让 Agent 可以放心试错，是 Coding Agent 和自动化 Agent 上生产的必备前提。</p>";
window.TAB_PATCH["agent"]["Coding Agent 的 Environment 包含什么"] = "<p><strong>核心回答：</strong>Coding Agent 的 Environment 指模型工作所处的<strong>完整执行上下文</strong>，通常包括：代码仓库（文件系统）、Shell 执行器、依赖运行时、工具集和权限边界。</p><ol><li>文件系统：项目代码、可读写的工作目录（本地或沙箱内）</li><li>Shell / 终端：执行构建、测试、git 等命令的通道</li><li>运行时与依赖：语言运行时（JDK/Python/Node）、包管理器、测试框架</li><li>工具集：读文件、写文件、搜索代码（grep）、运行测试、浏览器预览等</li><li>权限与环境变量：网络访问开关、密钥注入、目录白名单</li><li>反馈机制：编译报错、测试结果、lint 输出——这些 Observation 驱动 Agent 自我修正</li></ol><p><strong>面试背诵版：</strong>Coding Agent 的 Environment = 沙箱化的文件系统 + Shell + 语言运行时 + 工具集 + 权限控制。Agent 通过工具读写代码、执行命令，把编译和测试输出作为观察反馈来迭代修复。环境的好坏直接决定 Agent 能不能跑通验证闭环。</p>";
window.TAB_PATCH["agent"]["如何隔离 Agent 执行环境"] = "<p><strong>核心回答：</strong>按隔离强度递增：进程级隔离 → 容器隔离 → microVM/虚拟机隔离 → 云端临时工作区，按任务敏感度选择。</p><ol><li>容器（Docker）：文件系统 + 进程命名空间隔离，启动快，最常用；配合只读挂载、网络禁用、资源限额</li><li>microVM（Firecracker/gVisor）：内核级隔离，防止容器逃逸，多租户生产环境首选</li><li>云沙箱：每次任务分配临时虚拟机，任务结束销毁（Manus 模式），天然一次性</li><li>WebAssembly：轻量进程内隔离，适合跑不可信代码片段</li><li>辅助手段：非 root 用户运行、seccomp 限制系统调用、网络白名单、密钥走临时注入用完即焚</li></ol><p><strong>面试背诵版：</strong>常见方案是 Docker 容器做基础隔离（限制网络、文件、资源），敏感场景升级到 Firecracker microVM 或云端一次性沙箱，配合非 root 运行、系统调用过滤和临时密钥注入。核心原则：每次任务独立环境、最小权限、用完销毁。</p>";
window.TAB_PATCH["agent"]["如何限制 Agent 权限"] = "<p><strong>核心回答：</strong>权限控制 = <strong>最小权限原则 + 分级授权 + 白名单</strong>：默认全部拒绝，按任务需要逐项放行，高危操作要求人工确认。</p><ol><li>工具级：只注册任务必需的工具，没注册就调用不到（能力裁剪）</li><li>文件级：目录白名单（只能读写工作区），敏感路径只读或不可见</li><li>网络级：域名/IP 白名单，禁内网地址段（防 SSRF）</li><li>操作分级：只读操作自动执行；写操作需确认；删除/支付/外发等高危操作强制人工审批（Human-in-the-loop）</li><li>资源级：token 上限、步数上限、超时、花费预算</li><li>凭证级：短期临时凭证，作用域限定到单个任务，用后吊销</li></ol><p><strong>面试背诵版：</strong>按最小权限原则：能力上只给必需工具，范围上限定目录和域名白名单，操作上分只读/可写/需确认三级，高危动作人工审批，再用 token、步数、预算做资源上限，凭证用短期临时的。权限默认拒绝、显式放行，任何不可逆操作都要有人工确认兜底。</p>";
window.TAB_PATCH["agent"]["Agent 如何获取环境状态"] = "<p><strong>核心回答：</strong>Agent 通过<strong>观察（Observation）</strong>获取环境状态，来源包括工具返回值、命令执行输出、文件内容、事件回调和系统探测命令。</p><ol><li>工具返回：ls/read_file 返回目录和文件内容，search 返回匹配结果</li><li>Shell 输出：执行 <code>git status</code>、<code>ps</code>、<code>df -h</code> 等命令读取系统状态</li><li>API/传感器：监控指标、数据库查询、日历日程等结构化数据</li><li>事件驱动：订阅环境事件（文件变更、消息到达、CI 完成）触发 Agent 感知</li><li>注意：观察结果要控制长度（截断、摘要），避免撑爆上下文</li></ol><p><strong>面试背诵版：</strong>Agent 感知环境靠观察机制：每轮循环中工具调用的返回值、Shell 命令的标准输出、文件读取结果和环境事件回调都会作为 Observation 写入上下文，模型据此更新对环境状态的理解。实践中要对观察做截断和摘要，防止长输出污染上下文。</p>";
window.TAB_PATCH["agent"]["Claude Agent SDK 架构"] = "<p><strong>核心回答：</strong>Claude Agent SDK（原 Claude Agent SDK，前身 Claude Code SDK）是 Anthropic 官方的 Agent 构建框架，核心架构 = <strong>Query 会话循环 + 工具系统 + MCP + 子代理 + 权限网关</strong>。</p><ol><li>会话层：query() 发起对话，维护消息历史和 Agent Loop（模型 → 工具调用 → 观察 → 继续）</li><li>工具层：内置 Bash、文件读写、搜索等 Coding 工具，可自定义工具注册</li><li>MCP 集成：作为 MCP Client 接入外部 MCP Server，动态获得第三方能力</li><li>子代理：Task 工具派生 SubAgent，独立上下文处理子任务，只回传摘要，保护主上下文</li><li>权限层：PermissionMode 控制（如默认拒绝/需确认），Hooks 在动作前后插入校验逻辑</li><li>可编程性：支持 systemPrompt、模型切换、流式消息回调、会话恢复（resume）</li></ol><p><strong>面试背诵版：</strong>Claude Agent SDK 以 query 会话为入口跑 Agent Loop，内置文件和 Shell 工具，通过 MCP 协议扩展外部能力，用 Task 子代理隔离上下文做并行子任务，权限模式和 Hooks 提供动作级管控。定位是构建类 Claude Code 的长任务、强工具 Agent。</p>";
window.TAB_PATCH["agent"]["Skill 如何使用"] = "<p><strong>核心回答：</strong>Skill（以 Agent Skills 为代表）是把<strong>流程性知识打包成模型可发现的技能包</strong>：一个文件夹包含 SKILL.md（说明 + 触发条件）+ 可选脚本和资源，运行时按需加载进上下文。</p><ol><li>结构：SKILL.md 写清技能用途、何时使用、操作步骤；scripts/ 放确定性脚本；references/ 放详细资料</li><li>发现：把 Skill 目录放到约定位置（如 <code>./skills/</code>、<code>~/.claude/skills/</code>），系统启动时扫描生成技能索引（名称 + 描述）注入系统提示</li><li>触发：用户请求匹配时，模型自主决定调用该 Skill，先读 SKILL.md 获取流程，再按需执行其中脚本</li><li>渐进加载：索引常驻（省 token），正文和资源按需读取——这是和一次性塞 prompt 的最大区别</li><li>自建：写一个目录 + SKILL.md 描述做什么、怎么做，即可让 Agent 学会你的工作流</li></ol><p><strong>面试背诵版：</strong>Skill 是文件夹形式的可插拔知识包：SKILL.md 定义用途和步骤，配脚本和参考资料。使用时把目录放进 skills 目录，系统扫描后把技能摘要注入提示词；模型判断任务匹配就加载完整 SKILL.md 按流程执行。价值在于把领域 SOP 从提示词硬编码变成按需动态加载，省 token 且易维护。</p>";
window.TAB_PATCH["agent"]["Tool 如何接入"] = "<p><strong>核心回答：</strong>给 Agent 接工具的标准做法：<strong>定义工具 schema（名称、描述、参数 JSON Schema）→ 注册到运行时 → 模型输出工具调用 → 运行时执行并回填结果</strong>。</p><ol><li>原生 Function Calling：在请求的 tools 字段声明工具定义，模型返回 tool_call（含参数），你执行后以 tool 角色消息回传</li><li>SDK 方式：如 OpenAI Agents SDK 用 @function_tool 装饰器自动从函数签名生成 schema；LangChain 用 @tool</li><li>MCP 方式：启动 MCP Server（stdio/SSE），Client 连接后自动发现其工具列表，跨应用复用</li><li>关键点：<strong>工具描述要写清楚什么时候用、参数含义</strong>（模型靠描述决策）；参数校验防注入；执行加超时和错误处理，错误信息要能帮模型自我修正</li></ol><p><strong>面试背诵版：</strong>接工具四步：用 JSON Schema 声明工具名、描述和参数；注册到 Agent 运行时；模型在循环中产出结构化工具调用；运行时真实执行并把结果作为观察回填。工程上重点是写好工具描述、做参数校验和超时控制，需要跨系统复用就封装成 MCP Server。</p>";

window.TAB_PATCH["concurrent"] = window.TAB_PATCH["concurrent"] || {};
window.TAB_PATCH["concurrent"]["notify 和 notifyAll 的区别"] = "<p><strong>核心回答：</strong>notify() 随机唤醒<strong>一个</strong>等待在该对象监视器上的线程，notifyAll() 唤醒<strong>全部</strong>等待线程，让它们竞争锁。</p><ol><li>notify：只唤醒一个，但如果被唤醒的线程条件不满足（如唤醒了消费者而实际需要生产者），可能<strong>信号丢失导致所有线程永久等待</strong></li><li>notifyAll：全部唤醒后逐个拿锁，未抢到的继续 wait，逻辑安全但有唤醒开销</li><li>经验法则：多类线程共享同一把锁（生产者/消费者混合等待）时用 notifyAll；只有一类等待线程且条件同质时 notify 才安全</li><li>更优实践：用 java.util.concurrent 的 Condition.await()/signalAll() 或 BlockingQueue，语义更清晰</li></ol><p><strong>面试背诵版：</strong>notify 唤醒一个随机等待线程，开销小但可能唤醒错的对象造成死等；notifyAll 唤醒所有等待线程再竞争，安全但有调度开销。生产者-消费者这类多种角色共用一把锁的场景必须 notifyAll，或者直接用 Condition / BlockingQueue 替代。</p>";

window.TAB_PATCH["llm"] = window.TAB_PATCH["llm"] || {};
window.TAB_PATCH["llm"]["CNN为什么适合图像"] = "<p><strong>核心回答：</strong>CNN 靠<strong>局部感受野、权值共享、平移不变性</strong>三大特性高效提取图像的空间特征，参数量远小于全连接网络。</p><ol><li>局部感受野：图像特征是局部的（边缘、纹理），卷积核只看局部区域，符合图像的局部相关性</li><li>权值共享：同一个卷积核滑过整张图，一个核只学一种特征，参数量从 O(H×W×C) 降到核大小级别</li><li>平移不变性：目标出现在图像任何位置都能被同一组核检测到</li><li>层次化特征：浅层学边缘纹理，深层组合成部件和物体，天然匹配图像语义结构</li><li>池化进一步压缩尺寸、增强鲁棒性</li></ol><p><strong>面试背诵版：</strong>图像数据具有局部相关和平移不变的特点，CNN 用小卷积核做局部感知、用权值共享大幅减少参数、用池化获得尺度鲁棒性，层层堆叠后从边缘到语义逐级抽象，所以既高效又贴合图像本质。这也是 ViT 出现前 CV 领域几乎全用 CNN 的原因。</p>";
window.TAB_PATCH["llm"]["RNN为什么存在长距离依赖问题"] = "<p><strong>核心回答：</strong>标准 RNN 沿时间步<strong>递归传递隐状态</strong>，长序列上梯度要经过连乘传播，容易<strong>梯度消失/爆炸</strong>，远距离信息难以保留。</p><ol><li>数学原因：BPTT 中梯度含 W 的 t 次连乘，W 最大奇异值小于 1 则梯度指数级消失，大于 1 则爆炸</li><li>信息原因：每个时间步的隐状态都被新输入重写，早期 token 信息不断被稀释覆盖</li><li>表现：记住开头、忘记结尾，长句主谓一致、指代消解等任务表现差</li><li>缓解：LSTM/GRU 的门控机制、梯度裁剪；根治：注意力机制（Transformer 可一步直达任意位置）</li></ol><p><strong>面试背诵版：</strong>RNN 按时间步线性传递隐状态，反向传播时梯度是权重矩阵的多次连乘，序列一长就梯度消失或爆炸，远处的信息传不到当前步；同时隐状态容量有限，旧信息不断被覆盖。所以需要 LSTM 门控缓解，最终被能直接建模任意距离依赖的自注意力取代。</p>";
window.TAB_PATCH["llm"]["LSTM如何解决梯度问题"] = "<p><strong>核心回答：</strong>LSTM 引入<strong>细胞状态 ct 的加性传送带 + 三个门控</strong>，让梯度沿时间有一条<strong>近似无衰减的通道</strong>。</p><ol><li>细胞状态：ct = ft⊙ct-1 + it⊙gt，是加法更新而非连乘，梯度可以近似原样回传</li><li>遗忘门 ft：决定保留多少历史信息</li><li>输入门 it：决定写入多少新信息</li><li>输出门 ot：决定隐状态暴露什么</li><li>效果：需要长记忆时 ft 接近 1，梯度畅通；不需要时可关门截断，梯度按需流动</li></ol><p><strong>面试背诵版：</strong>LSTM 用细胞状态做信息高速公路：通过遗忘门和输入门做加性更新（ct = ft⊙ct-1 + it⊙gt），反向传播时梯度走加法路径而不是反复乘权重，避免了指数衰减；三个门控让网络自己学习什么该记长、什么该遗忘，从而缓解（不是完全解决）长距离梯度消失。</p>";
window.TAB_PATCH["llm"]["GRU和LSTM有什么区别"] = "<p><strong>核心回答：</strong>GRU 是 LSTM 的简化版：<strong>三个门变两个门（更新门 + 重置门），去掉独立细胞状态</strong>，参数少约 1/4，训练更快，多数任务效果相当。</p><ol><li>结构：LSTM 有遗忘/输入/输出三门 + 独立 ct；GRU 只有更新门 zt 和重置门 rt，隐状态兼任记忆</li><li>参数量：GRU 是 3×h×(h+x)，LSTM 是 4×h×(h+x)，GRU 更少</li><li>表达力：LSTM 记忆与输出分离，理论表达力更强，适合更长依赖</li><li>选择：数据少/要快选 GRU；长序列/要精度选 LSTM；现在两者大多被 Transformer 取代</li></ol><p><strong>面试背诵版：</strong>GRU 把 LSTM 的三门简化为更新门和重置门，取消了独立细胞状态，用隐状态直接当记忆。参数更少、收敛更快，一般任务和 LSTM 打平；LSTM 在超长依赖上略强。工程上按资源和序列长度取舍，主流 LLM 场景则都已转向 Transformer。</p>";
window.TAB_PATCH["llm"]["数据质量为什么重要"] = "<p><strong>核心回答：</strong>大模型能力上限由数据决定——<strong>垃圾进垃圾出</strong>：模型是在模仿训练分布，数据的准确性、多样性、去重程度直接决定模型的知识、推理风格和泛化能力。</p><ol><li>知识来源：模型参数里的事实全部来自训练数据，错误数据 = 错误知识（幻觉源头之一）</li><li>多样性：覆盖面不足会导致分布偏窄，长尾问题表现差</li><li>去重：重复数据浪费算力，还会放大记忆和过拟合</li><li>配比：不同来源（网页/代码/书籍/对话）的比例影响能力平衡（如代码数据提升推理）</li><li>清洗成本收益：研究表明小而精的数据可以超过大而脏的数据（如 phi 系列用教科书级数据）</li></ol><p><strong>面试背诵版：</strong>模型是训练分布的压缩快照，数据错了模型就学错。数据质量影响知识正确性（脏数据直接变成幻觉）、能力上限（phi 系列证明高质量小数据可打败低质量大数据）、以及 token 效率。所以预训练前要做过滤、去重、毒性清洗和配比实验，数据工程往往比模型结构更影响最终效果。</p>";
window.TAB_PATCH["llm"]["数据污染是什么"] = "<p><strong>核心回答：</strong>数据污染指<strong>测试/评测集的内容（或其变体）混入了训练数据</strong>，导致评测分数虚高，不能反映真实泛化能力。</p><ol><li>典型形式：基准题目原文泄露、答案模板泄露、同源改写（翻译/复述）</li><li>危害：榜单分数高但实际能力差，误导模型选型和科研结论</li><li>检测：n-gram 重叠检测、embedding 相似度检索、成员推断（canary 插入探针）</li><li>防范：动态新题基准（LiveBench）、私有测试集、按时间切分（训练截止晚于评测集发布即污染）</li></ol><p><strong>面试背诵版：</strong>数据污染是评测集内容提前进了训练语料，模型是在背答案而不是解题，分数虚高。常见于爬取的网页含解题讨论。可靠的做法是用 n-gram 和语义相似度做去污染、用时间切分或动态更新的基准（如 LiveBench）评测。面试时可补充：这是当前榜单可信度争议的核心原因之一。</p>";
window.TAB_PATCH["llm"]["Loss如何计算"] = "<p><strong>核心回答：</strong>LLM 预训练用<strong>自回归交叉熵损失</strong>：对每个位置预测下一个 token 的概率分布，与真实 token 的 one-hot 求负对数，再对序列取平均。</p><ol><li>公式：L = -(1/T) Σ log P(xt | x&lt;xt; θ)，即最大化下一个 token 的对数似然</li><li>实现：logits 经 softmax 得分布，取真实 token 位置的 -log 概率</li><li>监控：Loss 随训练下降并趋于平缓；异常尖峰可能指示数据问题或学习率过大</li><li>SFT 同样用交叉熵（通常只对回答部分计算）；RLHF 阶段则换成策略梯度目标（PPO/GRPO）加 KL 约束</li></ol><p><strong>面试背诵版：</strong>预训练是 next token prediction，损失就是每个位置真实下一个 token 的交叉熵：softmax 后取 -logP(真实token)，全序列平均。SFT 沿用交叉熵但只算响应段；RLHF 改用带 KL 惩罚的奖励目标。看训练曲线主要看 loss 是否平稳下降、有无尖峰。</p>";
window.TAB_PATCH["llm"]["Perplexity是什么"] = "<p><strong>核心回答：</strong>困惑度 = 模型对文本的<strong>交叉熵损失的指数</strong>，直观含义是“模型在每个位置平均在多少个词里犹豫”，越低越好。</p><ol><li>公式：PPL = exp(平均交叉熵) = exp(L)</li><li>含义：PPL=10 表示模型每步相当于在 10 个等概率候选中做选择</li><li>用途：衡量语言建模质量、比较基座模型、检测数据分布差异（领域 PPL 高说明不熟）</li><li>局限：只衡量“像不像训练分布”，不等于回答质量；不同 tokenizer 的 PPL 不可直接比较</li></ol><p><strong>面试背诵版：</strong>困惑度是交叉熵损失的指数形式，衡量模型预测下个 token 的不确定度，越小说明建模越准。常用于评测基座和检测 OOD 文本。注意它评的是概率建模能力而非对话能力，且跨 tokenizer 不可比，所以下游任务还要靠 benchmark。</p>";
window.TAB_PATCH["llm"]["如何判断模型训练是否收敛"] = "<p><strong>核心回答：</strong>综合<strong>训练 loss 曲线、验证 loss、梯度范数和下游指标</strong>判断：训练 loss 趋平、验证 loss 不再下降（或开始回升即过拟合信号）。</p><ol><li>训练 loss：平滑下降并进入平台期，波动小</li><li>验证 loss：关键指标，停止下降或回升说明到头了</li><li>梯度范数：稳定在小区间，突然飙升说明学习率过大或脏数据</li><li>下游评测：MMLU/代码等基准分数趋于稳定</li><li>lr schedule 影响：cosine 衰减末期 loss 自然变平，要区分“调度导致的平”和“真收敛”</li></ol><p><strong>面试背诵版：</strong>看四件事：训练 loss 进入平台且无尖峰、验证 loss 不再改善、梯度范数稳定、下游 benchmark 分数稳定。若验证 loss 回升是过拟合，训练 loss 不降可能是学习率或数据问题。大模型常用固定 token 预算 + cosine 调度，所谓收敛更多是预算耗尽前的稳定下降。</p>";
window.TAB_PATCH["llm"]["为什么大模型训练需要大量GPU"] = "<p><strong>核心回答：</strong>三个因素相乘：<strong>参数规模 × 数据规模 × 计算密度</strong>——训练要做万亿次前向反向，单卡显存和算力远远不够，必须大规模并行。</p><ol><li>算力需求：训练 FLOPs ≈ 6 × 参数量 × token 数。70B 模型训 15T token ≈ 6.3e24 FLOPs，单张 A100（约 312 TFLOPS）要不间断跑几十年</li><li>显存需求：参数 + 优化器状态 + 梯度 + 激活值。70B 模型 FP16 权重就 140GB，训练态需 TB 级显存，单卡（80GB）放不下，必须切分</li><li>时间约束：商业竞争要求数周内完成，只能横向扩展成千上万卡</li><li>通信开销：并行引入卡间通信，需要 NVLink/InfiniBand 高速互联，进一步推高成本</li></ol><p><strong>面试背诵版：</strong>训练总计算量约等于 6ND（N 参数量、D token 数），千亿模型就是 10 的 24 次方量级 FLOPs；同时训练态显存（权重+优化器+梯度+激活）远超单卡容量。所以既要数据/张量/流水线并行拆显存，又要上万卡堆算力把数年的计算压到几周，还需要高速互联支撑通信。</p>";
window.TAB_PATCH["llm"]["LoRA和Adapter有什么区别"] = "<p><strong>核心回答：</strong>两者都是<strong>冻结主干、只训小模块</strong>的参数高效微调（PEFT），区别在于插入方式：Adapter 是<strong>串行插入</strong>新模块（增加推理延迟），LoRA 是<strong>并行低秩旁路</strong>（可合并进权重，零额外延迟）。</p><ol><li>Adapter：在 Transformer 层间串联一个小瓶颈网络（降维-非线性-升维），训练只更新它；缺点是串在主干上，推理延迟增加</li><li>LoRA：W' = W + BA（B×A 低秩分解，秩 r 很小），只训 A、B；部署时可把 BA 合并进 W，结构不变、无延迟</li><li>效果：LoRA 通常不低于 Adapter，且更省显存（无新增激活串联）</li><li>变体：QLoRA = 4bit 量化基座 + LoRA，单卡可调大模型</li></ol><p><strong>面试背诵版：</strong>Adapter 在层间串接瓶颈模块，参数少但推理多一跳延迟；LoRA 用低秩矩阵 BA 并行旁路原权重，训练只动旁路，部署时合并回权重，推理零开销。因此工业界 LoRA 基本取代了 Adapter，配合 QLoRA 还能在单卡微调几十 B 模型。</p>";
window.TAB_PATCH["llm"]["Reward Model如何训练"] = "<p><strong>核心回答：</strong>用<strong>人类偏好对数据</strong>（同一 prompt 的好/坏回答对）训练一个打分模型：输入 prompt + 回答，输出标量分数，用 Bradley-Terry 成对排序损失让好回答分数更高。</p><ol><li>数据收集：同一 prompt 采样多个回答，人工标注偏好（A&gt;B 或平局），覆盖多样性任务</li><li>损失：L = -log σ(r(x, y_win) - r(x, y_lose))，即拉大好坏分差</li><li>结构：通常是同尺寸或稍小的 SFT 模型，把 LM 头换成标量输出头</li><li>工程点：去偏（长度偏置、位置偏置）、多 RM（有用性/无害性分开）、清洗标注噪声</li><li>产出：作为 RLHF 中 PPO 的奖励信号源，或直接做 best-of-n 重排</li></ol><p><strong>面试背诵版：</strong>RM 训练三步：对同一问题采样多个回答并人工排序；把回答对喂给带标量头的模型，用 Bradley-Terry 损失最大化好回答与坏回答的分差；训练好后给 RL 策略提供每步奖励。难点在标注质量、长度偏好校正和分布外泛化（reward hacking 的根源）。</p>";
window.TAB_PATCH["llm"]["PPO为什么用于RLHF"] = "<p><strong>核心回答：</strong>RLHF 的优化目标 = <strong>最大化奖励 − KL 惩罚</strong>，PPO 是能带约束地做策略梯度且训练稳定的成熟算法：截断概率比防止策略突变。</p><ol><li>目标：max E[r(x,y)] − β·KL(π‖π_SFT)，奖励高但不能偏离 SFT 太远（防 reward hacking 和语言崩坏）</li><li>PPO 机制：截断替代目标 clip(ratio) 限制单步更新幅度，加 KL 惩罚项，训练稳定不易崩</li><li>适配 LLM：on-policy 采样 → RM 打分 → 优势估计（GAE）→ 小步更新，四阶段循环</li><li>对比：DPO 直接从偏好对监督学习，绕开 RM 和采样，更简单，但在线探索能力弱于 PPO；GRPO 去掉 value 网络用组内相对优势，被 DeepSeek 等采用</li></ol><p><strong>面试背诵版：</strong>RLHF 要在“奖励最大化”和“别跑偏 SFT 分布”之间权衡，PPO 的截断目标 + KL 惩罚正好提供这种受约束的稳定策略更新，且是经过验证的成熟实现（Actor-Critic + GAE）。缺点是工程复杂（要同时跑 4 个模型）；因此出现 DPO 绕开在线 RL、GRPO 简化价值估计等替代路线。</p>";
window.TAB_PATCH["llm"]["LLM推理过程是什么"] = "<p><strong>核心回答：</strong>推理 = <strong>prefill（并行处理全部 prompt）+ decode（逐 token 自回归生成）</strong>两个阶段，直到输出 EOS 或达到长度上限。</p><ol><li>Prefill：一次性并行计算整个输入的 KV Cache，算力密集（compute-bound），首 token 延迟（TTFT）主要由它决定</li><li>Decode：每步用上一步输出 + KV Cache 预测下一个 token，一步一个，显存带宽密集（memory-bound）</li><li>采样：logits 经温度/top-p/top-k 采样选出 token</li><li>终止：遇到 EOS/stop 序列或 max_tokens</li><li>优化方向：KV Cache 管理（PagedAttention）、连续批处理、投机解码、量化</li></ol><p><strong>面试背诵版：</strong>推理分两阶段：prefill 阶段并行吃下全部提示词并生成 KV Cache，决定首字延迟；decode 阶段基于缓存逐 token 生成，每步只算最新位置，是访存瓶颈。生成由采样策略控制随机性，遇到终止符停止。工程优化主要围绕 KV Cache、批调度和投机解码展开。</p>";
window.TAB_PATCH["llm"]["为什么大模型需要分布式训练"] = "<p><strong>核心回答：</strong>单卡在<strong>显存和算力</strong>两方面都装不下、算不完大模型训练：必须把模型、数据、优化器状态切分到多卡，并用并行把计算时间压缩到可接受范围。</p><ol><li>显存墙：训练态显存 ≈ 16 字节/参数（权重+梯度+Adam 状态）+ 激活值，7B 模型就要 100GB+，70B 需 TB 级</li><li>算力墙：6ND 的计算量单卡要数年，必须数据并行扩展</li><li>手段：数据并行（DP/ZeRO）、张量并行（TP）、流水线并行（PP）、专家并行（EP，MoE）组合成 3D/4D 并行</li><li>配套：梯度同步通信（AllReduce）、高速互联（NVLink/IB）、容错（checkpoint）</li></ol><p><strong>面试背诵版：</strong>因为单卡显存放不下训练态模型（权重+优化器+激活是参数量的十几倍字节数），单卡算力也算不完 6ND 的总量。分布式训练用 DP 扩算力、TP/PP 切模型、ZeRO 切状态，把训练压到数周内完成。代价是通信开销和故障率上升，需要 checkpoint 和容错调度。</p>";
window.TAB_PATCH["llm"]["Tensor Parallel是什么"] = "<p><strong>核心回答：</strong>张量并行（TP）把<strong>单层权重矩阵按行列切开放到不同卡</strong>，一次矩阵乘法由多卡协作完成，通信量大但延迟低，适合同机高速互联。</p><ol><li>切法：Attention 的多头天然可切（每卡若干头）；FFN 的权重按列/行切分，配合 AllReduce/AllGather 拼结果</li><li>通信：每层前向反向都要同步（2 次 AllReduce/层），必须 NVLink 级带宽</li><li>适用：单机 8 卡内，卡数越多通信占比越高；跨机 TP 会拖垮吞吐</li><li>效果：解决单层放不下的显存问题，是 Megatron-LM 的核心并行方式</li></ol><p><strong>面试背诵版：</strong>TP 把一层的大矩阵切到多张卡上，每卡算自己那块再通信拼起来，像多人合力抬一根柱子。因为每层都要同步，通信极频繁，只适合 NVLink 互联的机内场景（一般 ≤8 卡）。它解决“层太大单卡放不下”，和数据并行（切样本）、流水线并行（切层）正交，可组合成 3D 并行。</p>";
window.TAB_PATCH["llm"]["Pipeline Parallel是什么"] = "<p><strong>核心回答：</strong>流水线并行（PP）把模型<strong>按层切成多段，每段放一张卡</strong>，数据像流水过车间一样依次经过各段，用 micro-batch 填满空泡。</p><ol><li>切法：64 层模型切成 4 段，每卡 16 层；前向按段传递激活，反向按逆序回传梯度</li><li>流水线气泡：段间等待造成的空闲，用 micro-batch 切小、1F1B/interleaved 调度压缩</li><li>通信量小（只传段边界激活），适合跨机低带宽场景</li><li>局限：负载均衡要精心设计（按 FLOPs 均分段）；气泡无法完全消除</li></ol><p><strong>面试背诵版：</strong>PP 按层分段，每段一张卡，样本分段流动，像工厂流水线。优点是通信少、能跨机扩展；缺点是存在流水线气泡（前面的段在等后面的段），靠 micro-batch 和 1F1B 调度缓解。实践中 PP 切层、TP 切层内矩阵、DP 复制整机，组成 3D 并行。</p>";
window.TAB_PATCH["llm"]["三种并行有什么区别"] = "<p><strong>核心回答：</strong>按<strong>切分对象</strong>区分：DP 切<strong>数据</strong>（模型每卡一份）、TP 切<strong>层内矩阵</strong>（多卡合算一层）、PP 切<strong>层</strong>（多卡接力一段）。</p><ol><li>数据并行 DP：各卡拿不同 batch，算完梯度 AllReduce 同步；扩展性最好，但每卡仍存全量模型（ZeRO 可切状态）</li><li>张量并行 TP：一层内部切开协作计算；通信最频繁，限机内 NVLink</li><li>流水线并行 PP：按层分段接力；通信最少，可跨机，但有气泡</li><li>组合：大模型训练 = DP × TP × PP（3D 并行），MoE 再加专家并行 EP</li></ol><p><strong>面试背诵版：</strong>DP 切样本、模型复制，通信是梯度同步，扩展简单；TP 把单层矩阵切到多卡，通信密集只能机内；PP 把层切成段流水执行，通信少但有空泡。生产训练通常三者叠加：机内 TP、跨机 PP、最外层 DP/ZeRO，MoE 模型再加 EP 切专家。</p>";
window.TAB_PATCH["llm"]["ZeRO是什么"] = "<p><strong>核心回答：</strong>ZeRO（Zero Redundancy Optimizer）是<strong>对数据并行的冗余状态做切分</strong>的显存优化技术：把优化器状态、梯度、参数按卡切分存储，用少量通信换大幅显存节省。</p><ol><li>ZeRO-1：切优化器状态（Adam 的一二阶矩），显存约降到 1/4，通信不变</li><li>ZeRO-2：再切梯度，适合训练</li><li>ZeRO-3：再切参数，前向反向时按需 AllGather 拼回，可训超大模型，通信增加约 50%</li><li>效果示例：400 张 V100 用 ZeRO-3 可训 1T 参数模型（论文数据）</li><li>延伸：ZeRO-Offload 卸载到 CPU 内存，ZeRO-Infinity 进一步用 NVMe</li></ol><p><strong>面试背诵版：</strong>普通 DP 每卡冗余存完整优化器状态、梯度和参数，ZeRO 把这些按卡切分、用时再聚合：三级递进切优化器→梯度→参数。以约 50% 的额外通信为代价，让数据并行也能跑下超大模型。DeepSpeed 的核心就是 ZeRO，配合 Offload 还能拿 CPU/NVMe 当显存池。</p>";
window.TAB_PATCH["llm"]["DeepSpeed是什么"] = "<p><strong>核心回答：</strong>DeepSpeed 是微软开源的<strong>大模型训练/推理加速框架</strong>，核心是 ZeRO 系列显存优化，配合流水线、offload、推理加速能力，让有限资源训大模型成为可能。</p><ol><li>ZeRO 1/2/3：切分优化器/梯度/参数，最著名的能力</li><li>3D 并行封装：集成 TP/PP/DP 调度</li><li>Offload：CPU Offload / ZeRO-Infinity 把状态卸载到内存和 NVMe</li><li>推理侧：DeepSpeed-Inference 提供高吞吐推理（kernel 融合、并行）</li><li>用法：json 配置文件声明 zero_stage、offload 等，对 PyTorch 代码侵入小</li></ol><p><strong>面试背诵版：</strong>DeepSpeed 是微软的大模型训练框架，招牌是 ZeRO 显存切分（三级递进），加上 3D 并行和 CPU/NVMe 卸载，使百亿千亿模型能在有限 GPU 上训练；也有推理加速组件。与 Megatron-LM（强在 TP/PP 工程极致）常被对比，实践中两者可结合：Megatron 切模型 + DeepSpeed ZeRO 切状态。</p>";
window.TAB_PATCH["llm"]["Megatron是什么"] = "<p><strong>核心回答：</strong>Megatron-LM 是 NVIDIA 开源的<strong>高效大模型并行训练框架</strong>，以张量并行 + 流水线并行的极致工程实现著称，是许多主流大模型训练底座（GPT-NeoX、多个国产模型）。</p><ol><li>核心：高效的 TP 实现（Attention 按头切、FFN 按行列切，通信与计算重叠）</li><li>PP：interleaved 交错式流水线调度，减少气泡</li><li>3D 并行：TP × PP × DP 的组合编排</li><li>生态：和 NVIDIA 硬件深度协同（NCCL、Tensor Core、FP8/BF16）</li><li>衍生：Megatron-DeepSpeed（结合 ZeRO）、NeoX、Nemotron 等都基于它</li></ol><p><strong>面试背诵版：</strong>Megatron-LM 是 NVIDIA 的大模型训练框架，把张量并行做到极致（按头切注意力、按行列切 FFN），配合交错流水线和 3D 并行编排，充分利用 NCCL 和 GPU 互联。特点是重工程、性能天花板高，适合追求训练吞吐的超大规模集群；轻量场景用 DeepSpeed 更省心。</p>";
window.TAB_PATCH["llm"]["如何训练百亿参数模型"] = "<p><strong>核心回答：</strong>按<strong>数据 → 并行策略 → 训练配置 → 监控容错</strong>四步走：高质量万亿级 token、3D 并行 + ZeRO、混合精度 + 合理学习率、全程监控和 checkpoint。</p><ol><li>数据：1-2T+ token，严格清洗去重去污染，做好领域配比</li><li>并行：机内 TP（8 卡）、跨机 PP、全局 ZeRO-DP；显存不够加 Offload</li><li>配置：BF16 混合精度、AdamW（β=0.9/0.95，wd=0.1）、lr 约 3e-4（10B 级）、warmup + cosine 衰减、梯度裁剪 1.0</li><li>稳定训练：监控 loss 尖峰（回滚或降 lr）、定期 checkpoint、loss scale 监控</li><li>续训练：SFT 指令数据 → RLHF/DPO 对齐</li></ol><p><strong>面试背诵版：</strong>百亿模型训练要点：万级高质量 token 数据和配比实验；机内张量并行 + 跨机流水线 + ZeRO 数据并行的组合；BF16 + AdamW + warmup/cosine 调度 + 梯度裁剪保稳定；全程盯 loss 曲线和梯度范数，异常回滚。资源有限时可用 QLoRA/LoRA 做领域续训，成本降一个数量级。</p>";
window.TAB_PATCH["llm"]["如何计算训练显存"] = "<p><strong>核心回答：</strong>混合精度训练显存 ≈ <strong>参数 16 字节 + 激活值</strong>：权重 2 + 梯度 2 + Adam 状态（FP32 副本 + 一二阶矩）12 字节，激活值随 batch/序列长度线性增长。</p><ol><li>静态部分（每参数）：权重 BF16 2B + 梯度 BF16 2B + FP32 主权重 4B + Adam 一阶矩 4B + 二阶矩 4B = <strong>16 字节/参数</strong></li><li>示例：7B 模型静态 ≈ 112GB → 单卡 80GB 放不下，需要 ZeRO/并行切分</li><li>激活值：与 batch × seq × hidden × 层数成正比，长上下文是大头（可用 FlashAttention、激活重计算压缩）</li><li>推理显存 ≈ 2 字节/参数（仅权重）+ KV Cache（随并发和长度增长）</li><li>优化：ZeRO 切分、Offload、量化（QLoRA 4bit ≈ 0.5 字节/参数）</li></ol><p><strong>面试背诵版：</strong>训练态按每参数 16 字节估：权重 2 + 梯度 2 + Adam 优化器 12（FP32 主权重和两个矩）；7B 模型光静态就要 112GB。再加与 batch、序列长成正比的激活值，用 FlashAttention 和重计算可压。推理只需权重 2 字节/参数加 KV Cache。记住 16B/参数和 2B/参数两个数，面试口算显存够用。</p>";
window.TAB_PATCH["llm"]["如何评价一个大模型"] = "<p><strong>核心回答：</strong>从<strong>能力维度评测 + 对比基线 + 实际任务表现</strong>三层评价：通用 benchmark 看能力，同级别模型对比看相对水平，自家业务集上看最终效果。</p><ol><li>通用能力：MMLU-Pro（知识）、GSM8K/MATH（数学）、HumanEval（代码）、IFEval（指令遵循）</li><li>中文与长文本：C-Eval/CMMLU、LongBench、大海捞针（needle in haystack）</li><li>对齐安全：MT-Bench、Arena 人工偏好、红队测试</li><li>工程指标：吞吐（tokens/s）、TTFT、并发能力、成本</li><li>业务验证：私有测试集 + A/B，警惕数据污染导致的虚高</li></ol><p><strong>面试背诵版：</strong>评大模型要分四层：公开 benchmark 测知识、推理、代码等硬能力；Arena/人工评测评对话偏好和对齐；吞吐延迟成本等工程指标；最后必须落在自己业务的私有评测集上——因为榜单有污染风险，能力不等于业务效果。选型时同量级对比才有意义。</p>";
window.TAB_PATCH["llm"]["MMLU是什么"] = "<p><strong>核心回答：</strong>MMLU（Massive Multitask Language Understanding）是覆盖<strong>57 个学科的四选一知识问答基准</strong>，衡量模型的世界知识和问题理解广度，是 LLM 评测事实标准之一。</p><ol><li>形式：约 1.4 万道选择题，覆盖 STEM、人文、社科、法律医学等 57 个领域</li><li>评分：准确率；GPT-4 级模型 86+，随机基线 25%</li><li>局限：题目泄露污染严重（有更难的 MMLU-Pro 抗污染版本）、只测选择不测生成</li><li>用法：常作为预训练/SFT 后的综合能力体检项</li></ol><p><strong>面试背诵版：</strong>MMLU 是 57 学科、1.4 万题的选择题知识基准，测模型的学科知识广度，是模型发布必报指标之一。因为老题泄露严重，现在更多用 MMLU-Pro（题目更难、选项更多、做了去污染）。答题时可补充：它测的是知识而非推理，要配合 GSM8K 等推理基准看。</p>";
window.TAB_PATCH["llm"]["C-Eval是什么"] = "<p><strong>核心回答：</strong>C-Eval 是<strong>中文多学科评测基准</strong>：覆盖 52 个学科、5 万余道中文学科选择题，从中学生到职业资格难度，衡量中文知识与推理能力。</p><ol><li>结构：52 学科（理工/文史/医学/法律/职业资格）四选一，分 val/test/dev 集</li><li>配套：C-Eval Hard 子集（高数、物理等难题）区分度更高</li><li>用途：中文模型（Qwen、GLM、Baichuan 等）发布必测项</li><li>同类：CMMLU（67 科目）、SuperCLUE、FlagEval 等</li></ol><p><strong>面试背诵版：</strong>C-Eval 是最常用的中文学科基准，52 个学科 5 万道选择题，覆盖中学到职业资格考试难度，还提供 Hard 难题子集拉开区分度。评测中文模型时和 CMMLU 一起看，避免只用英文基准造成偏差。同样要注意老题污染问题。</p>";
window.TAB_PATCH["llm"]["GSM8K是什么"] = "<p><strong>核心回答：</strong>GSM8K（Grade School Math 8K）是<strong>小学数学应用题推理基准</strong>：8.5K 道需要多步计算的英文数学题，答案是一个数字，衡量模型的逐步推理能力。</p><ol><li>形式：自然语言应用题 + 数字答案，正确率计分</li><li>意义：最早广泛使用的推理试金石，CoT 提示显著提升该榜</li><li>现状：头部模型已 90%+ 接近饱和，区分度下降</li><li>替代：MATH（竞赛级）、AIME、GPQA 等更难基准</li></ol><p><strong>面试背诵版：</strong>GSM8K 是 8.5K 道小学数学应用题，靠最终数字判对错，考察多步推理，是 CoT（思维链）效果的经典验证场。现在强模型都打满了，前沿评测转向 MATH、AIME、GPQA 这类更难基准，但 GSM8K 仍是快速体检推理能力的低成本指标。</p>";
window.TAB_PATCH["llm"]["HumanEval是什么"] = "<p><strong>核心回答：</strong>HumanEval 是 OpenAI 发布的<strong>代码生成基准</strong>：164 道手写 Python 编程题，用 pass@k（单元测试通过率）衡量模型从 docstring 生成正确函数的能力。</p><ol><li>形式：函数签名 + docstring → 生成代码 → 跑测试用例，pass@1 最常报</li><li>意义：第一个广泛使用的函数级代码合成基准</li><li>局限：题少且简单（已饱和）、只测 Python 单函数、不反映工程级能力</li><li>替代：MBPP、HumanEval+（加强测试）、SWE-bench（真实 GitHub issue 修复）</li></ol><p><strong>面试背诵版：</strong>HumanEval 是 164 道 Python 题的代码生成基准，按单元测试通过率算 pass@k，是最经典的代码能力指标。因为题目简单已趋饱和，现在评价编码能力更多看 SWE-bench（修真实 issue）、LiveCodeBench（动态新题防污染）。面试提到它的局限是加分项。</p>";
window.TAB_PATCH["llm"]["大模型人工评测如何设计"] = "<p><strong>核心回答：</strong>人工评测核心是<strong>偏好对比 + 盲评 + 标注质量控制</strong>：双模型匿名对比打分，多人交叉标注，统计一致性，持续校准标注员。</p><ol><li>任务设计：真实场景任务集（多样、分层难度），定义清晰评分维度（有用性/准确性/安全性/格式）</li><li>盲评对比：A/B 匿名呈现 + 顺序随机，避免位置偏置；比绝对打分更可靠</li><li>标注规范：写 rubric 和示例答案，小规模试标校准后再放量</li><li>质量控制：多人标注算一致性（Cohen Kappa），分歧仲裁，抽检 + 金标题混入</li><li>结果分析：按维度统计胜率、按任务类型下钻，报告置信区间（如 Chatbot Arena 的 Elo）</li></ol><p><strong>面试背诵版：</strong>设计四要点：任务集覆盖真实分布；采用匿名成对比较（盲评 + 随机顺序）拿偏好而不是绝对分；用 rubric 校准标注员并用一致性和金标控制质量；结果用胜率/Elo 加置信区间呈现。人工评测贵但可靠，常与自动评测互为校验，Arena 模式就是规模化的人工偏好评测。</p>";
