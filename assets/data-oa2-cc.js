window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["claudecode设计一个企业级 Claude Code 平台。"] = "<p><strong>核心回答：</strong>企业级 Claude Code 平台 = <strong>多租户接入层 + Agent Runtime 集群 + 权限与安全体系 + 上下文/知识管理 + 可观测与成本治理</strong>五大块。设计的关键不是模型本身，而是让 hundreds of Agent 在企业内<strong>可控、可审计、可复用</strong>地跑起来。</p><ol><li><strong>整体架构分层</strong><pre><code>接入层：Web IDE / CLI / CI Webhook / API Gateway\n执行层：Agent Runtime 集群（K8s Pod 池，每任务一沙箱）\n模型层：LLM 网关（多模型路由、配额、缓存）\n能力层：Tool Registry / MCP Server / 代码索引服务\n数据层：任务库 / 会话存储 / 知识库（RAG）/ 审计日志</code></pre></li><li><strong>多租户与隔离</strong>：按团队划分 workspace，代码仓库权限与 Agent 权限联动（Agent 只能读用户有权限的 repo）；执行沙箱按租户隔离网络（egress 白名单）与文件系统。</li><li><strong>权限体系</strong>：三级审批——只读工具自动放行、写文件/跑测试需策略放行、删除/push/生产命令必须人工确认；所有敏感操作走 Policy Engine（如 OPA）统一裁决并留痕。</li><li><strong>上下文管理</strong>：仓库级代码索引（AST + embedding）+ 项目规范文档 RAG，任务开始时只检索相关文件注入上下文，控制 token 成本。</li><li><strong>可观测性</strong>：每个任务生成 traceId，串联每次 LLM 调用、工具执行、文件变更；指标包括任务成功率、平均轮次、token 成本、人工干预率。</li><li><strong>面试收口：</strong>企业级平台的核心矛盾是<strong>能力开放 vs 风险可控</strong>，用沙箱隔离执行、权限分级审批、全链路审计三个机制平衡。</li></ol>";

  A["claudecode如何设计支持多人、多仓库的 Coding Agent 平台？"] = "<p><strong>核心回答：</strong>关键设计点是<strong>仓库-会话-任务三级模型 + 并发冲突控制 + 共享知识层</strong>。</p><ol><li><strong>数据模型</strong>：Workspace（团队）→ Repository（接入的仓库，绑定分支策略）→ Session（一个任务会话）→ Task Run（一次 Agent Loop 执行）。多人可在同一仓库开不同 Session。</li><li><strong>并发与冲突</strong>：Agent 修改统一走<strong>工作分支 + PR</strong>，永远不直接写主分支；同仓库并发任务用文件级锁或 rebase 重试解决冲突；任务开始前快照 base commit，结束后生成结构化 diff。</li><li><strong>多仓库支持</strong>：代码索引服务按仓库建立独立索引；跨仓库任务（如改接口+改调用方）由 Orchestrator 拆成子任务分别在不同仓库沙箱执行，最后汇总。</li><li><strong>协作模式</strong>：人可以在 Session 中随时插入指令（steer）；任务产出 PR 后进入人工 review 流程，Agent 根据 review 意见继续迭代。</li><li><strong>成本与配额</strong>：按团队设置 token/任务数配额，大任务自动降级到便宜模型做简单步骤（如格式化、跑测试）。</li></ol>";

  A["claudecode如何设计一个 Agent Runtime？"] = "<p><strong>核心回答：</strong>Agent Runtime 是<strong>Agent 的操作系统</strong>：负责任务生命周期管理、上下文维护、工具调度、沙箱执行、容错恢复。核心模块：</p><ol><li><strong>任务生命周期</strong>：Task 状态机 pending → running → waiting_approval → done/failed/cancelled；支持暂停恢复（持久化 state）。</li><li><strong>会话与上下文管理</strong>：维护 message history + 工具结果 + 文件变更记录；上下文超限时自动压缩（compaction：摘要旧轮次，保留关键决策和最近 N 轮）。</li><li><strong>工具执行引擎</strong>：统一 Tool 接口（name/schema/execute/timeout），内置工具 + MCP 动态注册；执行带超时、重试、结果截断（防止超大输出撑爆上下文）。</li><li><strong>沙箱</strong>：每个任务在独立容器/microVM 中执行，限制 CPU/内存/网络白名单，任务结束销毁。</li><li><strong>持久化与恢复</strong>：每隔 N 轮 checkpoint（消息 + 工具状态）到存储；进程崩溃后从 checkpoint 恢复继续执行，实现长任务不间断。</li><li><strong>接口层</strong>：对上暴露 start/cancel/steer/get_result API，对下调用 LLM 网关。</li></ol>";

  A["claudecode如何设计 Agent 调度系统？"] = "<p><strong>核心回答：</strong>Agent 调度系统解决<strong>“哪个任务在哪个资源上跑、什么时候跑”</strong>，核心是任务队列 + 优先级 + 资源池 + 公平性。</p><ol><li><strong>队列模型</strong>：按租户/团队分队列，防止大团队占满资源；队列内按优先级（交互任务 &gt; CI 自动任务 &gt; 后台批量）排序。</li><li><strong>资源匹配</strong>：任务声明资源需求（内存、是否需要 GPU、仓库缓存亲和性），调度器从沙箱池匹配；预热池（warm pool）减少冷启动。</li><li><strong>并发控制</strong>：全局并发上限 + 每用户并发上限（token 限流 + 任务数限流）；交互式会话优先抢占低优先级任务。</li><li><strong>长任务处理</strong>：Agent Loop 是长连接任务，调度器要支持任务迁移（checkpoint 后迁移到其他节点）和超时熔断（如 2 小时未完成自动暂停并通知）。</li><li><strong>可观测</strong>：队列深度、排队时长、任务成功率按队列维度监控，容量不足时自动扩容沙箱池。</li></ol>";

  A["claudecode如何设计 Agent 权限控制系统？"] = "<p><strong>核心回答：</strong>权限控制的核心是<strong>“默认最小权限 + 分级审批 + 集中策略 + 全量审计”</strong>。</p><ol><li><strong>工具分级</strong>：<pre><code>Level 0 只读：list/read/grep —— 自动放行\nLevel 1 低风险写：编辑工作区文件、跑测试 —— 策略放行\nLevel 2 敏感操作：git push、安装依赖、网络请求 —— 人工确认\nLevel 3 危险操作：删除、改生产配置、外发数据 —— 禁止或双人审批</code></pre></li><li><strong>策略引擎</strong>：权限规则集中管理（RBAC + ABAC），如“junior 角色的 Agent 不能执行 Level 2 以上操作”“生产仓库只读”；决策逻辑与业务代码解耦（OPA/Cedar 风格）。</li><li><strong>路径级控制</strong>：文件访问按 allowlist/denylist 控制（如 .env、密钥目录永久 deny）；命令执行用正则规则拦截危险模式（rm -rf、curl 外发）。</li><li><strong>审批流</strong>：敏感操作挂起任务，推送审批卡片（IM/Web IDE），人批准后继续；审批记录与任务 trace 关联。</li><li><strong>审计</strong>：所有工具调用记录 who（哪个用户/任务）when what（参数+结果摘要）decision（放行原因），不可篡改存储。</li></ol>";

  A["claudecode如何设计 Agent 日志和 Trace 系统？"] = "<p><strong>核心回答：</strong>Agent 日志系统的挑战是<strong>一次任务跨多轮 LLM 调用和工具执行，传统单请求日志无法串联</strong>，需要以任务为中心的 trace 模型。</p><ol><li><strong>Trace 模型</strong>：Trace = 一次任务；Span = 一次 LLM 调用 / 工具执行 / 子 Agent；Span 记录输入摘要、输出摘要、耗时、token 数、成本、错误。<pre><code>Trace(task_123)\n ├─ Span(llm_call)  model=Claude tokens=8.2k 1.2s\n ├─ Span(tool:read_file) path=src/main.java 0.1s\n ├─ Span(tool:run_tests) exit=1 45s\n └─ Span(subagent:fix_bug) ...\n</code></pre></li><li><strong>分级记录</strong>：摘要索引（ES，可搜索）+ 全量明细（对象存储，按需展开）；LLM prompt/completion 可能含敏感代码，落盘前脱敏并可配置保留期。</li><li><strong>关键用途</strong>：a) 排查任务失败（哪一步工具报错导致 Agent 走偏）；b) 成本分析（哪些任务类型烧 token）；c) 质量评估（人工干预率、返工率）；d) 审计合规。</li><li><strong>实时观察</strong>：任务进行中支持 live tail（像看 CI 日志一样看 Agent 每步在做什么），异常时人可及时介入。</li><li><strong>关联用户侧</strong>：traceId 贯穿 IDE 展示、PR 描述、审批记录，出问题能一键回放整个执行过程。</li></ol>";

  A["claudecode如何设计一个类似 Claude Code 的自动代码修复系统？"] = "<p><strong>核心回答：</strong>自动修复系统 = <strong>失败信号接入 → 诊断 Agent → 修复 Agent → 验证闭环 → PR 产出</strong>的流水线。</p><ol><li><strong>信号接入</strong>：CI 失败 webhook、监控告警（Sentry/ErrorStack）、安全扫描（Snyk）统一转成修复任务，带上下文（报错栈、失败日志、相关代码仓）。</li><li><strong>诊断阶段</strong>：Agent 读报错栈 + 相关代码 + git blame（谁最近改的、哪个 commit 引入），定位根因；输出结构化诊断（根因假设、涉及文件、修复方案）。</li><li><strong>修复阶段</strong>：在干净沙箱 checkout 分支，最小化修改（明确禁止顺手重构），生成 diff。</li><li><strong>验证闭环</strong>：跑单测 → 失败则读新报错继续修（最多 N 轮）→ 跑集成测试 → 全绿才产出 PR；PR 描述含根因分析、修改说明、测试证据，标记 auto-fix 等待人工 review。</li><li><strong>护栏</strong>：修改行数超阈值、涉及公共接口、测试无法全绿时降级为“建议 PR + 诊断报告”，不自动合并；每个 auto-fix PR 必须人工批准。</li><li><strong>反馈学习</strong>：人工 review 拒绝的修复回流为评估数据，用于优化 prompt 和工具策略。</li></ol>";

  A["claudecode为什么 Agent 应用需要基础设施层？"] = "<p><strong>核心回答：</strong>因为 Agent 应用与普通 Web 应用最大的不同是<strong>“LLM 输出不可靠 + 执行链路长 + 状态复杂 + 成本高”</strong>，这些复杂度不该每个应用自己扛，需要统一的基础设施层（Agent Infra）解决。</p><ol><li><strong>可靠性</strong>：LLM 会幻觉、格式会错、工具会挂，Infra 层提供重试、schema 校验、降级、兜底，让上层应用拿到稳定结果。</li><li><strong>执行环境</strong>：Agent 要跑命令改文件，需要沙箱（安全隔离）、网络策略、资源限制——这是平台能力不是业务逻辑。</li><li><strong>状态与恢复</strong>：Agent 任务一跑几十分钟、几百轮，必须有序列化/恢复/checkpoint 能力，否则一崩全丢。</li><li><strong>可观测</strong>：Agent 是黑盒决策，没有 trace/eval 体系就无法调试和改进。</li><li><strong>成本控制</strong>：token 是真金白银，模型路由、缓存、prompt 压缩都需要统一网关来做。</li><li><strong>类比收口</strong>：就像 Web 时代大家不自己写 TCP 栈和负载均衡，Agent 时代也不该每个团队自己造沙箱、调度、trace——这就是 Agent Infra 的价值（面试高频：Kubernetes for Agents 的说法）。</li></ol>";

  A["claudecodeClaude Code 和普通 ChatBot 架构最大的区别是什么？"] = "<p><strong>核心回答：</strong>三个本质区别：<strong>有无 Agent Loop、有无工具执行环境、状态生命周期</strong>。</p><ol><li><strong>ChatBot 架构</strong>：一问一答，请求-响应模型，无状态（或只带历史消息）；输出即终点，不接触真实环境。<pre><code>用户 → LLM → 文本回复 → 结束</code></pre></li><li><strong>Claude Code 架构</strong>：目标驱动，多轮循环，有状态会话；输出是动作（工具调用），执行后观察结果再决策，直到任务完成。<pre><code>用户目标 → 规划 → [LLM思考 → 调工具 → 观察] × N → 交付结果</code></pre></li><li><strong>执行环境</strong>：ChatBot 不需要沙箱（只生成文本）；Claude Code 必须有文件系统、终端、网络隔离的执行环境，这是 infra 上的分水岭。</li><li><strong>可靠性设计</strong>：ChatBot 错了用户重问即可；Coding Agent 错了会改坏代码，所以必须有权限控制、diff review、测试验证、回滚能力。</li><li><strong>上下文管理</strong>：ChatBot 上下文=对话历史；Claude Code 上下文=对话+代码库+工具结果+任务状态，必须做压缩和管理策略。</li></ol>";

  A["claudecodeAgent Runtime 如何支撑多个 Agent 并发执行？"] = "<p><strong>核心回答：</strong>核心是<strong>“执行环境隔离 + 会话状态独立 + 资源池化调度”</strong>三件套。</p><ol><li><strong>环境隔离</strong>：每个 Agent 任务跑在独立沙箱（容器/microVM），文件系统、网络、进程互相不可见，天然支持并发且互不干扰。</li><li><strong>状态独立</strong>：每个 Agent 有自己的 Session Store（消息历史、工具状态、checkpoint），Runtime 用 sessionId 隔离，多 Agent 并发读写各自状态无锁冲突。</li><li><strong>并发模型</strong>：单个 Runtime 进程可用协程/事件循环管理多个轻量会话（等待 LLM 响应时让出）；重执行（跑构建）下沉到沙箱不占 Runtime。多 Agent 协作时由 Orchestrator 并发派发子任务，收集结果汇总。</li><li><strong>资源治理</strong>：全局并发上限（保护 LLM 配额）、每租户配额、排队机制；沙箱池预热复用降低启动开销。</li><li><strong>故障隔离</strong>：单 Agent 崩溃不影响其他（沙箱级隔离），调度器自动重派或从 checkpoint 恢复。</li></ol>";

  A["claudecode如何降低 Agent 推理成本？"] = "<p><strong>核心回答：</strong>五个抓手：<strong>模型分级路由、上下文瘦身、缓存复用、结构化输出控制、批量与并行</strong>。</p><ol><li><strong>模型路由</strong>：简单步骤（格式检查、文件读取总结）用小模型/便宜模型，复杂决策用大模型；Claude Opus 做规划、Haiku 做执行类小任务。</li><li><strong>上下文瘦身</strong>：这是最大头。只注入相关文件（检索而非全量）、工具输出截断（测试日志只留报错段）、历史压缩（旧轮次摘要成一段）、系统提示词精简。</li><li><strong>缓存复用</strong>：prompt prefix 缓存（system prompt + 项目上下文不变部分命中缓存，价格约 1/10）；相同问题的工具结果短缓存；embedding 缓存避免重复索引。</li><li><strong>减少无效轮次</strong>：清晰的停止条件、先规划再执行（避免盲目试错）、失败快速止损而不是无限重试。</li><li><strong>评估 ROI</strong>：监控每任务成本分布，找出烧钱任务类型针对性优化（如某类任务 80% token 花在重复读大文件 → 加代码索引）。</li></ol>";

  A["claudecode如何降低 Agent 延迟？"] = "<p><strong>核心回答：</strong>延迟来自<strong>LLM 推理、工具执行、串行等待</strong>三部分，分别优化。</p><ol><li><strong>LLM 推理延迟</strong>：a) 流式输出（首 token 快，用户感知立刻改善）；b) 小模型做简单任务；c) prompt 更短解析更快；d) 选择低延迟端点/区域就近部署。</li><li><strong>工具执行延迟</strong>：a) 增量构建缓存（只跑受影响的测试）；b) 文件索引预建（grep 秒回）；c) 命令并行执行（无依赖的工具调用并发跑）。</li><li><strong>消除串行等待</strong>：预判下一步可能读的文件提前 prefetch；多文件编辑并行下发；子 Agent 并行探索。</li><li><strong>交互层优化</strong>：长任务拆分进度反馈（每步展示在做什么），用户感知比绝对延迟更重要；可中断可插话（不必等任务全完成）。</li><li><strong>预算换延迟</strong>：speculative 执行（预测用户可能要的两个方向并行探索，取对的那个）。</li></ol>";

  A["claudecode如何保证 Agent 输出稳定性？"] = "<p><strong>核心回答：</strong>LLM 天然不确定，稳定性靠<strong>工程手段收敛</strong>：结构化约束 + 验证闭环 + 失败兜底 + 评估回归。</p><ol><li><strong>结构化约束</strong>：工具调用用 JSON Schema 强校验，格式错误自动重试（带错误反馈）；输出模板化（PR 描述固定字段）。</li><li><strong>验证闭环</strong>：代码改动必须过编译+测试才算成功，不信任 LLM 自称完成；测试失败自动进入修复循环，N 轮后降级人工。</li><li><strong>确定性外壳</strong>：能确定的不交给 LLM——文件路径用工具返回值不用模型生成、随机性 temperature 调低、关键操作幂等设计（重复执行结果一致）。</li><li><strong>失败兜底</strong>：每个 Agent 任务有超时、最大轮次、预算上限三重熔断；失败时保留现场 + 诊断报告转人工，不能静默失败。</li><li><strong>评估回归</strong>：建立任务测试集（真实仓库+预期结果），模型或 prompt 升级前跑回归（成功率和成本不劣化才上线），这是长期稳定性的根本保障。</li></ol>";

  A["claudecode如何实现 Agent 版本管理和回滚？"] = "<p><strong>核心回答：</strong>Agent 的“版本”包括<strong>提示词/配置版本、模型版本、工具实现版本</strong>，以及<strong>会话状态的快照</strong>，四者都要可追溯可回滚。</p><ol><li><strong>配置版本化</strong>：system prompt、工具描述、模型路由规则放进 git 管理，发布走 CI（先灰度一小部分流量），每次任务记录所用配置版本号。</li><li><strong>模型版本固定</strong>：生产任务绑定模型 snapshot 版本（如 claude-xxx-20260801），模型商升级不自动跟随；升级前用评估集跑 A/B。</li><li><strong>会话回放</strong>：任务 trace 完整记录（消息+工具调用+参数+结果），可按 trace 重放调试；支持从任意 checkpoint 恢复会话继续（换 prompt 或模型重跑）。</li><li><strong>回滚机制</strong>：a) 配置回滚——git revert + 重新发布，分钟级；b) 任务级回滚——Agent 的文件改动都走 git 分支，任务失败直接丢弃分支；c) 数据回滚——Agent 写入的外部系统（如数据库）用事务/补偿模式。</li><li><strong>灰度发布</strong>：新版本 Agent 先跑影子流量（同任务新旧双跑比对结果），稳定后逐步放量。</li></ol>";

  A["claudecode如果让你重新设计 Claude Code，你会怎么改进？"] = "<p><strong>核心回答：</strong>可以从<strong>记忆、协作、可控性、成本</strong>四个方向提出改进（面试考察批判性思维，言之有理即可）。</p><ol><li><strong>长期记忆</strong>：现在的会话记忆任务结束即失散。加项目级持久记忆——团队规范、常见坑、代码库结构摘要沉淀下来跨会话复用（类似 CLAUDE.md 的增强版，自动维护）。</li><li><strong>多人协作</strong>：目前是单人工具。加共享会话（多人围观/接管一个 Agent 任务）、任务交接（A 的会话移交给 B 继续续跑）、团队任务看板。</li><li><strong>更细的可控性</strong>：执行计划先展示（plan mode 强化），人可编辑计划再放行；中途插入约束（“不许改这个文件”）即时生效。</li><li><strong>成本透明</strong>：实时显示每个任务花了多少 token/钱，预算阈值自动暂停；提供“省钱模式”（更激进的上下文压缩+小模型）。</li><li><strong>确定性增强</strong>：内置代码索引服务替代大量 grep 探索（减少轮次）；测试驱动任务（先写测试再让 Agent 实现到绿）。</li><li><strong>本地化优先</strong>：小任务用本地小模型（隐私+低延迟），复杂任务才上云大模型，混合路由。</li></ol>";
})(window.OTHERS_AI);
