window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["linuxLinux 如何定位 CPU 高？"] = "<p><strong>核心回答：</strong>定位 CPU 高是<strong>先找进程、再找线程、再抓热点栈</strong>的三步走：<code>top</code> 找高 CPU 进程 → <code>top -H -p</code>（或 pidstat）定位到具体线程 → <code>jstack/pidstat+gdb/perf</code> 抓栈定位到具体代码行。</p><ol><li><strong>top 找进程</strong>：<code>top</code> 按 %CPU 排序（P 键），锁定占用高的 PID。</li><li><strong>定位线程</strong>：<pre><code>top -H -p &lt;PID&gt;      # 列出该进程所有线程的 CPU\npidstat -t -p &lt;PID&gt; 1  # 线程级 CPU 采样</code></pre>记下 TID（线程号）。</li><li><strong>把 TID 换算成 16 进制</strong>：<pre><code>printf &#39;%x\n&#39; &lt;TID&gt;   # nid 是十六进制</code></pre></li><li><strong>抓线程栈（Java）</strong>：<pre><code>jstack &lt;PID&gt; | grep -A 20 &lt;hexTID&gt;</code></pre>看线程在跑什么方法。若栈在 GC，说明 GC 频繁导致 CPU 高。</li><li><strong>抓热点（原生/通用）</strong>：<pre><code>perf top -p &lt;PID&gt;           # 实时热点函数\nperf record -p &lt;PID&gt; -g sleep 10 &amp;&amp; perf report</code></pre></li><li><strong>常见原因</strong>：死循环/忙等、正则回溯、频繁 GC、锁自旋、IO 变成忙轮询、惊群。结合 <code>vmstat 1</code> 看是 us（用户态）还是 sy（内核态）高：us 高多为业务计算/GC，sy 高多为系统调用过多、上下文切换、锁。</li></ol>";

  A["linuxLinux 如何定位内存泄漏？"] = "<p><strong>核心回答：</strong>内存泄漏就是<strong>程序不再使用但未释放的内存持续增长</strong>。定位核心是「抓两个时间点的内存快照做 diff」，找出持续增长的对象/缓冲区。</p><ol><li><strong>先看进程内存趋势</strong>：<pre><code>ps -o rss,vsz -p &lt;PID&gt;   # RSS 持续涨 = 泄漏嫌疑\ntop / htop 观察 RES</code></pre></li><li><strong>malloc 类程序（C/C++）</strong>：<pre><code>valgrind --leak-check=full ./prog   # 最直接\nvalgrind --tool=massif ./prog        # 画内存曲线</code></pre></li><li><strong>Java</strong>：<pre><code>jmap -heap &lt;PID&gt;                 # 各代使用\njmap -dump:format=b,file=heap.hprof &lt;PID&gt;\njstat -gcutil &lt;PID&gt; 1000        # 看 FGC 是否频繁（老年代涨）</code></pre>再用 MAT / Eclipse Memory Analyzer 打开 hprof，看 dominator tree + leak suspects。</li><li><strong>Go</strong>：<pre><code>pprof：net/http/pprof，或 go tool pprof http://.../debug/pprof/heap\ncurl .../heap &gt; h1; 等待后抓 h2; go tool pprof -base h1 h2</code></pre></li><li><strong>通用观测</strong>：<code>smem</code>、<code>pmap -x &lt;PID&gt;</code> 看匿名内存映射、<code>/proc/&lt;PID&gt;/smaps</code> 看具体段；<code>slabtop</code> 看内核 slab 对象泄漏。</li><li><strong>判断泄漏 vs 正常</strong>：<code>free -m</code> 看 system 的 used + cache；进程 RSS 持续单调上涨且不被回收就是泄漏。</li></ol>";

  A["linuxLinux 如何定位磁盘 IO 高？"] = "<p><strong>核心回答：</strong>磁盘 IO 高定位用<code>iostat</code>看设备压力 → <code>iotop/pidstat -d</code>找高 IO 进程 → <code>lsof/strace</code>确认在读写什么文件。</p><ol><li><strong>看整体设备压力</strong>：<pre><code>iostat -x 1   # 关注 %util、await、svctm、r/s、w/s、rMB/s、wMB/s</code></pre><code>%util</code> 长期接近 100% 说明磁盘饱和；<code>await</code> 高说明排队或盘慢。</li><li><strong>找进程</strong>：<pre><code>iotop -oP        # 只看有 IO 的进程\npidstat -d 1     # 进程级读写量</code></pre></li><li><strong>找文件</strong>：<pre><code>lsof -p &lt;PID&gt; | grep -E &#39;(REG|DIR)&#39;   # 打开的文件\nstrace -p &lt;PID&gt; -e trace=open,read,write -f 2&gt;&amp;1 | grep -oE &#39;\\&quot;[^\\&quot;]+\\.(log|db|dat)[^\\&quot;]*\\&quot;&#39;</code></pre></li><li><strong>区分来源</strong>：日志狂打、数据库刷盘、大文件拷贝、swap 换页（vmstat 里 si/so 非零）、page cache 抖动。</li><li><strong>磁盘类型判断</strong>：机械盘随机 IO 弱、SSD 强；用 <code>fio</code> 压测对比基线。若是网络存储（云盘），还要看网络和配额。</li><li><strong>缓解</strong>：慢查询优化、开启 Buffered IO/批写、日志异步化、加大内存做 cache、上 SSD、分区隔离。</li></ol>";

  A["linuxLinux 如何定位网络问题？"] = "<p><strong>核心回答：</strong>网络问题定位沿<strong>「应用层 → 传输层 → 链路层」分层排查</strong>：先 ping/连通性，再看端口/连接状态，再抓包分析。</p><ol><li><strong>连通性</strong>：<pre><code>ping &lt;ip&gt;          # ICMP 通不通（可能被禁）\ntelnet ip port     # 端口通不通\nnc -vz ip port     # 快速测端口</code></pre></li><li><strong>连接状态</strong>：<pre><code>ss -s              # 汇总\nss -tanp           # 所有 TCP 连接 + 进程\nss -tan state time-wait | wc -l   # TIME_WAIT 数量\nnetstat -s         # 协议统计（重传、丢包、reset）</code></pre>关注：大量 TIME_WAIT、SYN_RECV（可能 SYN flood）、CLOSE_WAIT（对方关闭我没关，代码 bug）、重传率。</li><li><strong>路由/网关排查</strong>：<pre><code>traceroute &lt;ip&gt;     # 跳点延迟\nip route           # 本机路由\nip addr / ifconfig # 网卡、IP、丢包计数</code></pre></li><li><strong>抓包</strong>：<pre><code>tcpdump -i eth0 -nn host 1.2.3.4 and port 80 -w a.pcap\n# 用 wireshark 打开看握手、重传、RST、TCP window</code></pre></li><li><strong>性能</strong>：<code>iftop</code> 看实时流量、<code>nethogs</code> 看每进程带宽、<code>mtr</code> 看持续性丢包。</li><li><strong>常见根因</strong>：DNS 解析慢、防火墙、半连接队列溢出、TIME_WAIT 过多、内核参数（somaxconn、tcp_tw_reuse）、DNS/宽带瓶颈。</li></ol>";

  A["linuxload average 高说明什么？"] = "<p><strong>核心回答：</strong>load average 是<strong>一段时间内处于「运行（R）+ 不可中断睡眠（D）」状态的进程平均数量</strong>，它衡量的是系统整体繁忙和排队程度，不是单纯的 CPU 使用率。</p><ol><li><strong>三个数字</strong>：<code>uptime</code> 里三个值分别是 1 分钟、5 分钟、15 分钟平均 load。1 分钟 > 15 分钟说明负载在上升。</li><li><strong>如何判断是否过高</strong>：与<strong>CPU 核心数</strong>比——单核 load=1 即满载，8 核 load=8 即满载。经验值：load / 核数 &gt; 1 开始排队，&gt; 1.5 需要关注，&gt; 3 明显过载。</li><li><strong>load 高但 CPU 不高</strong>：很可能大量进程处于 D 状态（不可中断睡眠，等磁盘/网络 IO），即 IO 瓶颈，而非 CPU 瓶颈。用 <code>ps -eo stat,pid,cmd | grep ^D</code> 或 <code>vmstat</code> 看 b 列（阻塞队列）+ wa 列（IO 等待）。</li><li><strong>排查</strong>：<code>vmstat 1</code> 看 r（运行队列）、b（阻塞）、cs（上下文切换）、us/sy/id/wa；<code>mpstat -P ALL 1</code> 看每核；<code>pidstat -w 1</code> 看切换多的进程。</li><li><strong>误区</strong>：load 高 ≠ 一定有问题，若业务量确实大且吞吐正常可接受；关键看是否影响延迟和可用性。</li></ol>";

  A["linuxtop、vmstat、iostat、sar 分别用于什么场景？"] = "<p><strong>核心回答：</strong>四者都是性能观测工具，但侧重不同：<strong>top 看进程视角</strong>、<strong>vmstat 看系统整体（CPU/内存/上下文）</strong>、<strong>iostat 看磁盘 IO</strong>、<strong>sar 看历史趋势采样</strong>。</p><table><tr><th>工具</th><th>关注点</th><th>关键指标</th><th>典型命令</th></tr><tr><td>top/htop</td><td>进程级 CPU、内存、负载</td><td>%CPU、RES、load、进程排序</td><td><code>top</code>、<code>top -H -p PID</code></td></tr><tr><td>vmstat</td><td>系统全局：CPU、内存、IO、上下文切换</td><td>r/b、si/so、cs、us/sy/id/wa</td><td><code>vmstat 1 5</code></td></tr><tr><td>iostat</td><td>块设备/磁盘 IO</td><td>%util、await、r/s、w/s、吞吐</td><td><code>iostat -x 1</code></td></tr><tr><td>sar</td><td>历史采样与趋势（CPU/内存/网络/磁盘）</td><td>可按-t指定时间回放</td><td><code>sar -u 1 5</code>、<code>sar -n DEV</code></td></tr></table><ol><li><strong>top</strong>：实时看哪些进程吃资源，最常用入口。</li><li><strong>vmstat</strong>：判断系统是否整体过载、是否频繁上下文切换（cs 高）、是否 swap（si/so）。</li><li><strong>iostat</strong>：专门定位磁盘慢、IO 饱和。</li><li><strong>sar</strong>：周期采集存日志（/var/log/sa/saXX），适合事后回溯（如昨晚 3 点负载高的原因），无需实时盯着。</li></ol>";

  A["linuxperf 工具是什么？"] = "<p><strong>核心回答：</strong>perf 是 Linux 内核自带的<strong>性能剖析（profiling）工具</strong>，通过 PMU 硬件计数器和 tracepoint 采样/追踪 CPU 指令、函数调用热点、缓存命中、分支预测等，是定位 CPU 性能底层的利器。</p><ol><li><strong>采样热点</strong>：<pre><code>perf top                # 实时热点函数\nperf record -g ./prog   # 记录 + 调用栈\nperf report             # 查看报告</code></pre></li><li><strong>统计计数器</strong>：<pre><code>perf stat ./prog   # 指令数、cache-misses、cache-references、cpu-clock、context-switches</code></pre></li><li><strong>火焰图</strong>：<code>perf record -g</code> 生成的数据配合 <code>FlameGraph</code> 脚本画火焰图，直观看热点路径。</li><li><strong>内核态</strong>：<code>perf list</code> 看支持的事件；tracepoint 追踪内核函数（如 sched、syscall）。</li><li><strong>前提</strong>：需要 root 或有 perf_event_paranoid 权限（<code>/proc/sys/kernel/perf_event_paranoid</code> ≤ 1）；容器里需要额外开启权限。</li></ol>";

  A["linux火焰图是什么？如何分析？"] = "<p><strong>核心回答：</strong>火焰图（Flame Graph）是<strong>把采样到的函数调用栈按出现频率堆叠可视化的图</strong>，X 轴宽度=采样占比（越宽越热），Y 轴从下到上=调用栈从根到叶，颜色仅用于区分。</p><ol><li><strong>形态</strong>：每个函数一个矩形条，下宽上窄；底部是调用者，顶部是被调用者（叶子）。</li><li><strong>分析看宽度不看高度</strong>：顶部「平台」（宽而平的顶）代表 CPU 长时间停留在某叶子函数，就是热点；宽度从左到右累加是总采样。</li><li><strong>生成流程</strong>：<pre><code>perf record -F 99 -p &lt;PID&gt; -g -- sleep 30   # 99Hz 采样\nperf script &gt; out.perf\nFlameGraph/stackcollapse-perf.pl out.perf &gt; out.folded\nFlameGraph/flamegraph.pl out.folded &gt; flame.svg</code></pre></li><li><strong>常见变体</strong>：CPU 火焰图（CPU 热点）、内存火焰图（malloc 分配量）、off-CPU 火焰图（阻塞/等待时间，用 bcc offcputime）。</li><li><strong>技巧</strong>：先看最宽的叶子；对比优化前后两张图看哪个函数相对变窄了；放大用 SVG 交互（点击缩放）。</li></ol>";

  A["linux为什么 AI 推理服务需要 Linux？"] = "<p><strong>核心回答：</strong>AI 推理服务需要 Linux 是因为它<strong>对 GPU 驱动、高性能 IO、进程调度、容器化编排、内存和网络调优有硬性要求</strong>，而这些生态（CUDA、Docker/K8s、vLLM/TensorRT、NUMA、io_uring 等）几乎都建立在 Linux 之上。</p><ol><li><strong>GPU 生态</strong>：NVIDIA CUDA 驱动、cuda-toolkit、NVML 库首选 Linux（数据中心版）；Windows 的 CUDA 支持落后且调度/共享能力弱。</li><li><strong>容器编排</strong>：K8s + NVIDIA Device Plugin、GPU 共享/隔离、多租户调度都依赖 Linux cgroup/namespace。</li><li><strong>高性能 IO/网络</strong>：推理服务要并发处理海量请求，依赖 epoll、io_uring、zero-copy、RDMA、DPDK 等 Linux 内核能力。</li><li><strong>内存/性能调优</strong>：NUMA 亲和、hugepage、CPU 绑核、内核参数（net.core.somaxconn 等）、perf/eBPF 可观测性。</li><li><strong>生态与标准</strong>：AI 推理框架（vLLM、TensorRT-LLM、Triton、SGLang）和部署工具都默认 Linux；生产环境的 systemd、日志、监控、安全加固都以 Linux 为基准。</li></ol>";

  A["linuxGPU 服务为什么需要 Linux 环境？"] = "<p><strong>核心回答：</strong>GPU 服务需要 Linux 环境，核心在于<strong>CUDA 工具链、驱动、NVIDIA 容器运行时、以及大规模 GPU 调度的成熟度都在 Linux 上，Windows 在多卡、多租户、高并发 GPU 场景下能力受限</strong>。</p><ol><li><strong>驱动与工具链</strong>：NVIDIA 数据中心 GPU 的驱动（Data Center Driver）、CUDA、cuDNN、TensorRT 都以 Linux 为第一支持平台，稳定性/性能最好。</li><li><strong>容器运行时</strong>：nvidia-container-toolkit / nvidia-docker 让容器透明访问 GPU，配合 Device Plugin 在 K8s 实现 GPU 发现与分配；这些都是 Linux 原生。</li><li><strong>多卡通信</strong>：NCCL/NVLink、RDMA、多进程 GPU 通信对内核网络栈和驱动协作要求高，Linux 支持最完善。</li><li><strong>资源管理</strong>：cgroup 限制 GPU、NUMA 绑核靠近 GPU、hugepage、CPU 亲和，都基于 Linux 内核特性。</li><li><strong>生产运维</strong>：GPU 监控（DCGM/nvidia-smi -> Prometheus）、日志、自动扩缩容、故障自愈在 Linux + K8s 生态成熟。</li></ol>";

  A["linuxCUDA 程序运行时 Linux 做了什么？"] = "<p><strong>核心回答：</strong>CUDA 程序运行时，Linux 作为宿主操作系统负责<strong>进程创建与调度、驱动交互、显存管理、PCIe 设备访问、内存拷贝与同步</strong>，把 GPU 当作一块可通过驱动访问的 PCIe 设备来协调 CPU 与 GPU 的协作。</p><ol><li><strong>驱动加载</strong>：<code>nvidia.ko</code>/<code>nvidia-uvm.ko</code> 内核模块注册为一个字符设备（/dev/nvidia*），用户态 CUDA Runtime 通过 ioctl 与驱动通信。</li><li><strong>进程与上下文</strong>：每个 CUDA 进程在驱动里有一个 CUDA context，负责维护该进程的模块（代码）、显存分配、流（stream）。Linux 负责该进程的调度和生命周期。</li><li><strong>内存管理</strong>：<code>cudaMalloc</code> 分配设备显存 → 驱动管理；统一内存（UVM）通过 nvidia-uvm 驱动在 CPU 内存和 GPU 显存间按需迁移页、处理缺页。</li><li><strong>数据搬运</strong>：<code>cudaMemcpy</code>（Host2Device/Device2Host）走 PCIe DMA，Linux DMA 框架负责；页锁定内存（pinned memory）避免分页保证 DMA 效率。</li><li><strong>执行与同步</strong>：kernel launch 提交到流；cudaDeviceSynchronize / 事件依赖驱动完成；CPU 侧通过 Linux 的等待机制（futex/event）同步。</li><li><strong>资源限制</strong>：cgroup 限制 CPU/内存，隔离 GPU 资源的 Device Plugin 也跑在 Linux 之上。</li></ol>";

  A["linuxvLLM / TensorRT 服务为什么依赖 Linux 高性能 IO？"] = "<p><strong>核心回答：</strong>vLLM / TensorRT 这类推理服务要处理<strong>海量并发请求和 GB 级模型权重/ KV Cache</strong>，依赖 Linux 的 epoll 高并发网络、zero-copy、hugepage、io_uring 等高性能 IO 能力来降低延迟、提升吞吐。</p><ol><li><strong>高并发网络</strong>：epoll 事件驱动 + 多 worker 进程/线程，支撑成千上万的并发连接，是 FastAPI/uvicorn 的底层。</li><li><strong>zero-copy / sendfile</strong>：把 token 流式回传时减少内核态-用户态拷贝，降低 CPU 和内存带宽占用。</li><li><strong>hugepage（大页）</strong>：权重和 KV Cache 分配用 hugepage 降低 TLB miss，稳定高吞吐。</li><li><strong>连续 batching 的内存带宽</strong>：推理是内存带宽密集型，Linux 的 NUMA、内存分配器（jemalloc/tcmalloc）、page cache 调优直接影响 decode 吞吐。</li><li><strong>io_uring</strong>：异步磁盘 IO 用于模型加载、Checkpoint、日志，降低阻塞。</li><li><strong>内核参数与网卡</strong>：somaxconn、tcp 缓冲区、NIC 多队列、软中断绑核（RPS/RFS、irqbalance）都属 Linux 调优。</li></ol>";

  A["linux大模型推理服务如何利用 Linux 多线程和网络能力？"] = "<p><strong>核心回答：</strong>大模型推理服务（如 vLLM）通过 Linux 的<strong>多进程/多线程 + NUMA + epoll 高并发网络 + 异步 IO</strong>，把「请求接入 → 调度 → prefill/decode 计算 → 流式回传」这条链路在高并发下榨干 GPU 和网络。</p><ol><li><strong>多进程/多线程</strong>：调度进程 + 推理 worker 进程/线程池；decode 阶段并发处理多个 sequence（continuous batching），线程绑核减少上下文切换和缓存抖动。</li><li><strong>NUMA 亲和</strong>：把 worker 绑到靠近 GPU 的 CPU 核/NUMA node，减少 PCIe 跨节点访问延迟，权重和 KV Cache 尽量本地。</li><li><strong>epoll 并发网络</strong>：FastAPI/uvicorn + asyncio 基于 epoll 同时维持海量 HTTP/SSE 长连接，流式 token（Server-Sent Events）逐字返回。</li><li><strong>异步与零拷贝</strong>：io_uring 做异步加载模型/checkpoint，sendfile/零拷贝加速大响应。</li><li><strong>调度与亲和</strong>：task 队列按 GPU 可用显存/算力调度，用 Linux 的 cgroup、CPU sets 隔离不同的服务，避免互相抢占。</li><li><strong>网卡多队列</strong>：多 RSS 队列 + 软中断亲和，把网络包处理分散到多核，避免单核瓶颈。</li></ol>";
})(window.OTHERS_AI);