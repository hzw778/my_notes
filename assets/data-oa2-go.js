window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["gosync.Mutex 底层实现是什么？"] = "<p><strong>核心回答：</strong>sync.Mutex 基于.runtime 内部的<strong>信号量（semaphore）+ 自旋 + 休眠等待队列</strong>实现：快路径原子 CAS 抢锁，抢不到进入慢路径自旋几次后挂起，由 runtime 调度唤醒。</p><ol><li><strong>结构</strong>：state（int32，编码了锁标志、唤醒标志、饥饿模式、等待者数量）+ sema（uint32 信号量）。</li><li><strong>加锁流程</strong>：a) 快路径：CAS 把 state 的 locked 位从 0 改 1，成功即持有；b) 慢路径：先自旋（多核 CPU 下期待锁马上释放，空转几次避免上下文切换）；c) 自旋失败，通过 sema 信号量挂起 goroutine，加入等待队列（FIFO）。</li><li><strong>解锁流程</strong>：清 locked 位；若有等待者，释放信号量唤醒队头 goroutine。</li><li><strong>饥饿模式</strong>（Go 1.9+）：正常模式下被唤醒的 waiter 要和新来的抢锁（可能一直抢不到）；某 waiter 等待超 1ms 切换饥饿模式——锁直接交接给队头，新来的不自旋直接排队；队列清空后回到正常模式。解决了尾延迟问题。</li><li><strong>要点</strong>：不可重入（重复 Lock 死锁）；Unlock 未加锁的 mutex 会 fatal error；零值可用。</li></ol>";

  A["gosync.Once 如何保证只执行一次？"] = "<p><strong>核心回答：</strong>sync.Once 用<strong>原子标志位 done + 互斥锁双检</strong>：先原子读 done（快路径，已完成直接返回），未完成加锁后再检查一次，仍为 0 则执行 f 并在 defer 中原子置 1。</p><ol><li><strong>源码骨架</strong>：<pre><code>type Once struct { done uint32; m Mutex }\nfunc (o *Once) Do(f func()) {\n  if atomic.LoadUint32(&amp;o.done) == 0 { // 快路径\n    o.doSlow(f)\n  }\n}\nfunc (o *Once) doSlow(f func()) {\n  o.m.Lock(); defer o.m.Unlock()\n  if o.done == 0 {            // 双检\n    defer atomic.StoreUint32(&amp;o.done, 1)\n    f()\n  }\n}</code></pre></li><li><strong>细节</strong>：done 置 1 在 defer 中（f 执行完才置位）；f panic 时 done 不置位，下次 Do 会再执行（Once 不保证 panic 后永久跳过——Go 1.21 前行为如此）。</li><li><strong>为什么 done 放第一个字段</strong>：内存对齐，让原子操作更高效（hot path 优化）。</li><li><strong>使用场景</strong>：单例初始化、配置加载、连接建立；并发调用 Do(f) 时所有 goroutine 都会阻塞到 f 首次执行完。</li><li><strong>Go 1.21+ OnceFunc/OnceValue</strong>：包装成只执行一次的函数/带返回值缓存。</li></ol>";

  A["gosync.Pool 有什么作用？"] = "<p><strong>核心回答：</strong>sync.Pool 是<strong>临时对象缓存池</strong>，复用已分配对象减轻 GC 压力，适合高频创建销毁的大对象（如 buffer、临时切片）。</p><ol><li><strong>API</strong>：New 字段（工厂函数）、Get()（取对象，空则调 New 新建）、Put()（还回池）。</li><li><strong>实现</strong>：每个 P（处理器）一个本地池（poolLocal，数组+链表），Get/Put 优先操作本 P 私有槽和共享链表，无锁快；本 P 空了偷其他 P 的共享链表；全空走 New。</li><li><strong>GC 行为</strong>：每次 GC 会清空池中对象（两轮 GC 后彻底丢弃）——所以 Pool 不是持久缓存，只是复用窗口。</li><li><strong>典型用法</strong>：<pre><code>var bufPool = sync.Pool{New: func() any { return new(bytes.Buffer) }}\nfunc handle() {\n  b := bufPool.Get().(*bytes.Buffer)\n  defer func() { b.Reset(); bufPool.Put(b) }() // 复用前重置\n  // 使用 b ...\n}</code></pre>标准库 fmt、encoding/json、http 都大量使用。</li><li><strong>注意</strong>：Put 前要 Reset 清理状态；不要存有状态/大生命周期对象；池化收益要在 benchmark 验证。</li></ol>";

  A["goatomic 原子操作原理是什么？"] = "<p><strong>核心回答：</strong>Go 的 atomic 操作底层是<strong>CPU 单指令原子指令</strong>（x86 的 LOCK 前缀指令如 LOCK CMPXCHG、ARM 的 LL/SC），在硬件层面保证读-改-写不可分割，比互斥锁轻量得多。</p><ol><li><strong>指令层</strong>：LOCK 前缀让指令执行期间锁住缓存行（现代 CPU 是缓存锁而非总线锁），其他核不能同时修改该地址；CAS（Compare-And-Swap）循环构成各种原子操作。</li><li><strong>Go 层 API</strong>：atomic.AddInt32/64（原子加减）、CompareAndSwap（CAS）、Load/Store（原子读写，保证可见性和顺序）、Swap、Go 1.19+ 的 atomic.Pointer/Int64/Uint64/Bool 类型封装。</li><li><strong>与锁的区别</strong>：原子操作无 goroutine 切换、无等待队列（失败就返回/循环），纳秒级；Mutex 慢路径要挂起唤醒，微秒级。但原子操作只保护单个变量，保护临界区多变量还得锁。</li><li><strong>内存序</strong>：Go 内存模型保证 atomic 操作是顺序一致的（sequentially consistent），可以作为 goroutine 间同步点（happens-before）。</li><li><strong>典型应用</strong>：计数器、状态标志位、无锁队列节点指针、配置热更新的原子替换（atomic.Pointer 存整个配置对象）。</li></ol>";

  A["goGo scheduler 如何实现 work stealing？"] = "<p><strong>核心回答：</strong>当某个 P 的本地队列空了，它会<strong>尝试从其他 P 的本地队列尾部偷取一半 goroutine</strong>（work stealing），配合全局队列轮询和网络轮询器，保证所有 P 都有活干。</p><ol><li><strong>调度对象</strong>：G（goroutine）、M（OS 线程）、P（逻辑处理器，持有本地运行队列，数量=GOMAXPROCS）。</li><li><strong>找工作的顺序</strong>（findrunnable）：a) 每 61 次调度先看全局队列（防饿死）；b) 本 P 本地队列；c) 全局队列；d) 网络轮询器（netpoller，就绪的网络 goroutine）；e) <strong>随机选一个 P 偷其本地队列的一半</strong>（从队列尾部偷，偷的任务是较旧的，减少被频繁偷）；f) 实在没有就休眠 M。</li><li><strong>为什么偷一半</strong>：摊平负载，一次偷走多个减少偷窃频率。</li><li><strong>spinlock 防冲突</strong>：偷取与被偷 P 的操作通过本地队列的锁/无锁环形数组协调。</li><li><strong>收益</strong>：负载均衡（忙的 P 的任务被闲 P 分走）、减少 M 休眠唤醒、提高 CPU 利用率——这是 Go 高并发吞吐的关键设计之一。</li></ol>";

  A["goGo runtime 包有哪些核心能力？"] = "<p><strong>核心回答：</strong>runtime 是 Go 程序的<strong>运行时内核</strong>：调度器（GMP）、内存管理（分配+GC）、goroutine 生命周期、cgo、网络轮询、反射支持，以及暴露给程序员的控制/观测 API。</p><ol><li><strong>goroutine 控制</strong>：Go（创建）、Gosched（让出）、GOMAXPROCS（P 数量控制）。</li><li><strong>调度观测</strong>：NumGoroutine、NumCPU、GODEBUG=schedtrace=... 打印调度状态；pprof 的 goroutine profile。</li><li><strong>内存管理</strong>：内存分配器（mcache/mcentral/mheap 三级）、逃逸分析配合、ReadMemStats/MemProfile 观测；GC：三色标记+混合写屏障，GC()/SetGCPercent/调试 GODEBUG=gctrace=1。</li><li><strong>系统调用封装</strong>：os 里的文件/时间底层都走 runtime（如 epoll 的 netpoller，让阻塞 IO 变成 goroutine 挂起）。</li><li><strong>崩溃与退出</strong>：panic/recover 机制、Goexit（终止当前 goroutine）、Goexit 配 defer 清理。</li><li><strong>常用入口</strong>：runtime/pprof（性能剖析 CPU/heap/goroutine）、debug.SetMaxStack、Callers（调用栈，zap 等日志库用）、AllThreads 相关调试钩子。</li><li><strong>面试角度</strong>：runtime 决定了 Go 的并发模型（用户态调度）和低延迟 GC（sub-ms STW），是语言生产力的底层支撑。</li></ol>";
})(window.OTHERS_AI);
