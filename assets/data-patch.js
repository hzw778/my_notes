window.TAB_PATCH = {
  "java": {
    "为什么重写 equals() 必须重写 hashCode()": "<p><strong>核心回答：</strong>这是 Java 的通用约定：equals 相等的两个对象，hashCode 必须相等。如果只重写 equals 不重写 hashCode，对象在 HashMap、HashSet 等哈希容器中会出现逻辑错误。</p><ol><li>哈希容器先按 hashCode 定位桶，再用 equals 判断相等</li><li>只重写 equals：两个“内容相等”的对象 hashCode 不同 → 落入不同桶 → HashSet 去重失效、HashMap 取不到值</li><li>约定要求：equals 相等 → hashCode 必须相等；hashCode 相等 → equals 不一定相等</li></ol><p><strong>面试背诵版：</strong>因为 HashMap/HashSet 依赖 hashCode 分桶、equals 判等。不重写 hashCode 时，两个逻辑相等的对象哈希值不同，会被分到不同的桶里，导致去重失败、get 查不到，破坏容器语义，所以必须同时重写，保证 equals 相等则 hashCode 相等。</p>",
    "String、StringBuilder、StringBuffer 有什么区别": "<p><strong>核心回答：</strong>String 不可变；StringBuilder 可变、非线程安全、性能最好；StringBuffer 可变、方法加 synchronized、线程安全。</p><ol><li>String：final char[]（JDK9 后是 byte[]）存储，任何修改都产生新对象</li><li>StringBuilder：原地修改字符数组，预分配容量，单线程拼接首选</li><li>StringBuffer：API 与 StringBuilder 几乎相同，方法加了 synchronized，多线程才用</li></ol><p>循环拼接字符串必须用 StringBuilder，用 + 每轮都会创建新对象，产生大量临时垃圾。</p>",
    "final 关键字有什么作用": "<p><strong>核心回答：</strong>final 修饰类、方法、变量三种场景，语义是“不可变/不可覆盖/不可继承”。</p><ol><li>修饰类：类不可被继承（如 String）</li><li>修饰方法：方法不可被子类重写（可重载）</li><li>修饰变量：基本类型值不可变；引用类型引用不可变（指向的对象内容仍可改）</li><li>final 字段有特殊语义：构造器安全发布，避免指令重排序引发的可见性问题</li></ol>",
    "static 关键字有什么作用": "<p><strong>核心回答：</strong>static 表示“属于类而非实例”，生命周期与类相同，随类加载初始化。</p><ol><li>静态变量：所有实例共享一份，存于方法区/元空间（JDK8+）</li><li>静态方法：可直接用类名调用，不能访问实例成员，不能被 abstract 修饰</li><li>静态代码块：类加载时执行一次，常用于初始化静态资源</li><li>静态内部类：不依赖外部实例，不持有外部类引用</li></ol><p>注意：静态方法不能同时是抽象方法；静态成员访问用类名，避免被实例“误导”。</p>",
    "Object 类有哪些常用方法": "<p><strong>核心回答：</strong>Object 是所有类的根类，常用方法可分三类：</p><ol><li>判等相关：equals()、hashCode()</li><li>字符串表示：toString()</li><li>并发/生命周期相关：getClass()、clone()、finalize()（已废弃）、wait()/wait(timeout)/notify()/notifyAll()</li></ol><p>高频追问点：wait/notify 属于 Object 而不是 Thread，因为锁对象可以是任意对象，锁对象自己负责协调等待队列；线程唤醒必须持有该对象的 monitor 锁。</p>",
    "Java 创建对象有哪些方式": "<p><strong>核心回答：</strong>五种：new、反射、clone、反序列化、Unsafe.allocateInstance。</p><ol><li>new：最常见，调用构造器</li><li>反射：Class.newInstance()（已废弃）或 Constructor.newInstance()，框架常用</li><li>clone()：native 方法复制，不调用构造器，需要实现 Cloneable</li><li>反序列化：ObjectInputStream 从字节流还原，不调用构造器</li><li>Unsafe.allocateInstance：直接分配内存，绕过构造器</li></ol><p>只有 new 和反射会执行构造器，其余三种不会。</p>",
    "Java 多态是如何实现的": "<p><strong>核心回答：</strong>多态三要素：继承/实现 + 重写 + 父类引用指向子类对象。运行时基于动态绑定实现。</p><ol><li>编译期：按静态类型检查方法是否存在（静态分派，重载在此决定）</li><li>运行期：JVM 通过对象实际类型（动态类型）查虚方法表（vtable）找到真正实现（动态绑定，重写在此决定）</li><li>invokevirtual 指令：先找实际类的方法表，逐级向上查找父类实现</li></ol><p>注意：private、static、final 方法是静态绑定（invokestatic/invokespecial），不体现多态；成员变量没有多态，按引用类型访问。</p>",
    "为什么 Java 不支持多继承": "<p><strong>核心回答：</strong>为了避免菱形继承（钻石问题）带来的复杂性和歧义。</p><ol><li>菱形问题：A 同时继承 B、C，B/C 都重写了 D 的方法，A 调用时无法确定用哪个</li><li>C++ 用虚继承解决，实现复杂、内存布局开销大</li><li>Java 用单继承 + 多实现接口替代：接口 JDK8 前没有方法体，不存在冲突；JDK8 引入 default 方法后冲突时编译器强制重写解决</li></ol><p>本质是“组合灵活性”与“语言简单性”的权衡，Java 选择了简单、明确。</p>",
    "接口中的 default 方法是什么": "<p><strong>核心回答：</strong>JDK8 引入，允许接口携带默认实现的方法，主要目的是接口演化兼容。</p><ol><li>语法：default 修饰，有方法体，实现类可直接继承或覆盖</li><li>动机：给 Collection 加 stream() 等方法时，不破坏已有实现类</li><li>冲突规则：实现两个接口有相同 default 方法 → 编译报错，必须显式重写；可用 InterfaceName.super.method() 指定调用</li></ol><p>default 方法让接口向“抽象类化”演进，但仍不能有状态（无实例字段）。</p>",
    "为什么推荐组合优于继承": "<p><strong>核心回答：</strong>继承是强耦合的白盒复用，组合是松耦合的黑盒复用。</p><ol><li>继承破坏封装：子类依赖父类实现细节，父类改动可能直接破坏子类（脆弱基类问题）</li><li>继承在编译期固定（is-a），组合可在运行期替换（has-a），更灵活</li><li>继承层次深难以维护；组合能按需装配能力</li></ol><p>判断标准：严格的 is-a 关系且子类不破坏父类约定时才用继承；否则用组合 + 接口委托。</p>",
    "ArrayList 扩容机制是什么": "<p><strong>核心回答：</strong>添加元素前检查容量，不足则扩容为原来的 1.5 倍。</p><ol><li>无参构造：初始为空数组，首次 add 时分配 10</li><li>扩容：newCapacity = oldCapacity + (oldCapacity >> 1)，即 1.5 倍</li><li>若所需最小容量大于 1.5 倍，直接用最小需求量</li><li>Arrays.copyOf 复制到新数组（Arrays.copyOf → System.arraycopy，native 复制）</li><li>扩容是 O(n)，预估大小用 ensureCapacity 或带初始容量的构造器可避免反复扩容</li></ol>",
    "ArrayList 为什么查询快，插入慢": "<p><strong>核心回答：</strong>底层数组支持 O(1) 随机访问；插入需移动元素，是 O(n)。</p><ol><li>查询：数组内存连续，根据 index 偏移直接定位（RandomAccess 接口标记）</li><li>中间插入/删除：后续元素整体前移或后移，平均移动 n/2 个元素</li><li>尾部插入：均摊 O(1)（扩容那一次是 O(n)，均摊后常数）</li></ol><p>对比 LinkedList：随机访问 O(n)，但头尾插入 O(1)；不过 LinkedList 节点内存不连续、缓存不友好，实际性能通常仍不如 ArrayList。</p>",
    "LinkedList 为什么现在很少使用": "<p><strong>核心回答：</strong>理论优势在现实中很难兑现，劣势却真实存在。</p><ol><li>随机访问 O(n)：只能从头/尾遍历</li><li>每个节点额外 24 字节（前后指针 + 对象头），内存开销大</li><li>内存不连续，CPU 缓存命中率低，节点分配零散、GC 压力大</li><li>增删 O(1) 的前提是已定位到节点；按索引增删仍是 O(n)</li></ol><p>现代 CPU 下连续内存的 ArrayList 性能经常反超；需要频繁头尾操作用 ArrayDeque（也基于数组），它同时是官方推荐的栈和队列实现。</p>",
    "HashMap JDK1.7 和 JDK1.8 有什么区别": "<p><strong>核心回答：</strong>1.8 做了三大改造：数组+链表 → 数组+链表+红黑树，头插 → 尾插，扩容重算 → 高低位拆分。</p><ol><li>数据结构：1.7 数组+链表，链表过长退化 O(n)；1.8 链表长度 ≥8 且数组 ≥64 时转红黑树，查询 O(log n)</li><li>插入方式：1.7 头插法在并发扩容时会成环导致 CPU 100%；1.8 改尾插，消除成环（但仍非线程安全）</li><li>扩容：1.7 重新计算每个 key 的 hash；1.8 只看 hash 新增位是 0/1，拆成原位置/原位置+oldCap 两条链</li><li>节点：1.7 Entry，1.8 Node（树节点 TreeNode）</li></ol>",
    "HashMap 为什么使用红黑树": "<p><strong>核心回答：</strong>防止哈希冲突严重时链表过长导致查询退化。</p><ol><li>纯链表查询 O(n)，恶意构造 hash 攻击（哈希碰撞 DoS）可让服务瘫痪</li><li>红黑树是近似平衡二叉搜索树，查找 O(log n)：链长 8 → 最多 3 次比较</li><li>选红黑树而不是 AVL：红黑树插入/删除旋转次数少（最多 2/3 次），综合读写性能更好</li><li>树化的阈值 8 源于泊松分布：正常 hash 下链长到 8 的概率约千万分之六，树化是兜底而非常态</li></ol>",
    "HashMap 为什么要进行 hash 扰动": "<p><strong>核心回答：</strong>让高位也参与运算，减少低位相同造成的碰撞。</p><ol><li>定位公式：index = (n-1) &amp; hash，n 是 2 的幂，掩码只取低位</li><li>hashCode 的信息集中在高位，直接用低位会丢失区分度</li><li>扰动函数：(h = key.hashCode()) ^ (h >>> 16)：高 16 位与低 16 位异或，混合高低位信息</li><li>效果：分布更均匀，碰撞更少，同时只增加一次移位一次异或的微小开销</li></ol>",
    "HashTable 和 ConcurrentHashMap 有什么区别": "<p><strong>核心回答：</strong>两者都线程安全，但并发性能差距很大。</p><ol><li>锁粒度：Hashtable 全表一把锁，读写全部串行；ConcurrentHashMap 1.7 分段锁（Segment），1.8 CAS + synchronized 锁单个桶头节点</li><li>并发度：Hashtable 同一时刻只有一个线程操作；ConcurrentHashMap 不同桶可并发操作</li><li>null：Hashtable 不允许 null 键值；ConcurrentHashMap 也不允许；HashMap 允许</li><li>性能：高并发下 ConcurrentHashMap 吞吐量高一个数量级</li></ol><p>Hashtable 已被淘汰，多线程场景一律用 ConcurrentHashMap。</p>",
    "Java 对象创建过程是什么": "<p><strong>核心回答：</strong>五步：类加载检查 → 分配内存 → 初始化零值 → 设置对象头 → init 构造。</p><ol><li>类加载检查：new 指令检查常量池符号引用，类未加载则先执行加载/解析/初始化</li><li>分配内存：指针碰撞（内存规整）或空闲列表（内存不规整）；TLAB 优先在 Eden 区线程私有缓冲分配，避免并发竞争</li><li>初始化零值：字段置默认值（int=0、ref=null），保证不初始化也能访问</li><li>设置对象头：Mark Word（哈希码、GC 分代年龄、锁状态）+ 类型指针（ Klass Pointer）</li><li>执行 &lt;init&gt;：构造方法按父类到子类顺序执行显式初始化和构造体</li></ol>",
    "对象在 JVM 中如何分配": "<p><strong>核心回答：</strong>优先栈上分配，其次 Eden，大对象直接进老年代，长期存活晋升老年代。</p><ol><li>栈上分配（逃逸分析）：对象不逃逸出方法且可拆分 → 标量替换，直接在栈上，方法结束即回收</li><li>TLAB：Eden 内线程私有缓冲，分配只需指针移动，无锁</li><li>大对象：超过 -XX:PretenureSizeThreshold 直接进老年代，避免 Eden 和 Survivor 来回复制</li><li>动态年龄判断：Survivor 中同龄对象总和超过 Survivor 一半，该年龄及以上直接晋升</li><li>GC 年龄默认 15（4 bit），达到 -XX:MaxTenuringThreshold 晋升老年代</li></ol>",
    "什么情况下对象会进入老年代": "<p><strong>核心回答：</strong>四种情况：年龄达标、大对象、动态年龄判断、Minor GC 后 Survivor 放不下。</p><ol><li>年龄达到 MaxTenuringThreshold（默认 15），每熬过一次 Minor GC 年龄 +1</li><li>大对象直接分配：超过 PretenureSizeThreshold（串行/ParNew 有效）</li><li>动态年龄判断：Survivor 中某年龄对象总大小 &gt; Survivor/2，该年龄以上全部晋升</li><li>Minor GC 后存活对象太多，Survivor 装不下，通过担保机制直接进老年代</li></ol>",
    "新生代为什么使用复制算法": "<p><strong>核心回答：</strong>新生代对象朝生夕死（存活率极低），复制算法只需复制少量存活对象，效率最高。</p><ol><li>复制算法开销与存活对象数量成正比，与堆大小无关</li><li>新生代 98% 对象活不过第一次 Minor GC，复制量极小</li><li>Eden:S0:S1 = 8:1:1，每次只用 10% 的 Survivor 空间做交换，内存浪费可控</li><li>对比：标记-清除会产生碎片，标记-整理要移动全部对象，都不适合高死亡率场景</li><li>老年代存活率高，才用标记-清除/标记-整理</li></ol>",
    "CMS 和 G1 有什么区别": "<p><strong>核心回答：</strong>CMS 追求最低停顿但不整理碎片；G1 面向大堆、可预测停顿、整体上整理碎片。</p><ol><li>内存布局：CMS 物理分代（连续新生代/老年代）；G1 逻辑分代、物理不分（Region 分区）</li><li>算法：CMS 标记-清除，产生浮动碎片，碎片过多退化为 Serial Old 整理（长停顿）；G1 整体标记-整理 + Region 间复制，不产生碎片</li><li>停顿可控：G1 可设 -XX:MaxGCPauseMillis 目标，按回收价值排序 Region 组成回收集（停顿预测模型）</li><li>并发漏标处理：CMS 增量更新；G1 原始快照（SATB）</li><li>适合场景：CMS 适合小堆低延迟（JDK14 已移除）；G1 适合大堆（4G+）大内存服务器</li></ol>",
    "Minor GC、Major GC、Full GC 有什么区别": "<p><strong>核心回答：</strong>按回收区域划分：新生代 / 老年代 / 整堆 + 方法区。</p><ol><li>Minor GC：回收新生代（Eden + Survivor），频繁、速度快（存活对象少），由 Eden 满触发</li><li>Major GC：回收老年代，常伴随至少一次 Minor GC（老年代空间不足/晋升失败触发），速度慢</li><li>Full GC：回收整堆 + 方法区（元空间），停顿最长；触发条件：老年代满、元空间满、System.gc()、担保失败、堆外内存分配失败</li></ol><p>调优目标：减少 Full GC 频率。常见手段：增大新生代/ Survivor、调大晋升年龄、避免内存泄漏和大对象滥用。</p>",
    "JVM 如何进行性能调优": "<p><strong>核心回答：</strong>先定位问题再调参，顺序：监控 → 定位 → 调优 → 验证。</p><ol><li>监控指标：GC 频率/耗时（jstat -gcutil）、停顿时间（GC 日志）、内存趋势、CPU、RT</li><li>定位工具：jmap（堆转储）、MAT/JProfiler（内存分析）、arthas（线上诊断）、jstack（线程）</li><li>常见问题与参数：频繁 Minor GC → 增大新生代 -Xmn；Full GC 频繁 → 排查内存泄漏/老年代过小；停顿长 → 换 G1/ZGC、设 MaxGCPauseMillis</li><li>典型初始配置：-Xms = -Xmx 避免动态扩容抖动，堆内合理分配，开启 GC 日志（-Xlog:gc*）</li></ol><p>原则：多数问题靠“代码修复”而非调参；调参是最后手段。</p>",
    "线程和进程有什么区别": "<p><strong>核心回答：</strong>进程是资源分配的基本单位，线程是 CPU 调度的基本单位。</p><ol><li>资源：进程拥有独立地址空间、文件描述符；线程共享所属进程的堆和方法区，仅私有栈、程序计数器、局部变量</li><li>开销：进程创建/切换成本高（页表、上下文大）；线程切换只需保存寄存器和栈，开销小</li><li>通信：进程间需 IPC（管道、消息队列、共享内存、Socket）；线程直接读写共享变量（需同步）</li><li>隔离性：进程崩溃不影响其他进程；线程崩溃可能带走整个进程</li></ol><p>一句话：线程是轻量级进程，共享资源换来了低开销，但也引入了同步问题。</p>",
    "Runnable 和 Callable 有什么区别": "<p><strong>核心回答：</strong>Runnable 无返回值不抛检查异常；Callable 有返回值可抛异常。</p><ol><li>方法签名：Runnable.run() 无返回；Callable.call() 返回 V，可 throws</li><li>配合：Callable 需配合 Future/FutureTask 获取结果（get() 阻塞）</li><li>线程池：execute(Runnable) 无结果；submit(两者皆可) 返回 Future</li><li>JDK8 后推荐 CompletableFuture，支持回调、编排、异常传播，不再阻塞 get</li></ol>",
    "Thread 生命周期有哪些状态": "<p><strong>核心回答：</strong>Thread.State 枚举六种状态。</p><ol><li>NEW：创建未 start</li><li>RUNNABLE：就绪/运行（Java 将两者合一，含 RUNNING 和 READY，还包括等待 IO 等 OS 层阻塞）</li><li>BLOCKED：竞争 synchronized 锁失败，等待进入 monitor</li><li>WAITING：无限期等待，需 notify/notifyAll 唤醒——wait()、join()、LockSupport.park()</li><li>TIMED_WAITING：限期等待——sleep(ms)、wait(ms)、join(ms)、parkNanos</li><li>TERMINATED：run 执行完或异常退出</li></ol><p>易错点：调用 wait 的线程释放锁进入 WAITING；sleep 不释放锁进入 TIMED_WAITING；等锁是 BLOCKED。</p>",
    "notify() 和 notifyAll() 有什么区别": "<p><strong>核心回答：</strong>notify 随机唤醒一个等待线程；notifyAll 唤醒所有等待线程，让它们竞争锁。</p><ol><li>notify：只唤醒一个，若该线程的条件不满足（多条件共享一把锁时）可能“丢失唤醒”，其余线程永久等待</li><li>notifyAll：全部唤醒后竞争锁，不满足条件的继续 wait，安全但有调度开销</li><li>必须在持有对象锁时调用（synchronized 块内），否则 IllegalMonitorStateException</li><li>唤醒不等于立刻执行：被唤醒线程要从 WAITING 先竞争回锁</li></ol><p>工程实践：默认用 notifyAll 更安全；多条件场景应使用 Condition（await/signal）拆分等待队列。</p>",
    "synchronized 锁升级过程是什么": "<p><strong>核心回答：</strong>JDK6 引入偏向锁 → 轻量级锁 → 重量级锁的膨胀过程，只升不降。</p><ol><li>无锁 → 偏向锁：只有一个线程反复获取，Mark Word 记录线程 ID，之后该线程进入无需同步操作（CAS 都省了）</li><li>偏向锁 → 轻量级锁：第二个线程竞争，撤销偏向，线程栈创建 Lock Record，CAS 把 Mark Word 指向锁记录；失败自旋</li><li>轻量级锁 → 重量级锁：自旋超过阈值或第三线程竞争，膨胀为 monitor（互斥量），未抢到锁的线程阻塞（内核态）</li><li>锁状态存于对象头 Mark Word（2 bit 锁标志 + 1 bit 偏向位）</li></ol><p>JDK15 起默认废弃偏向锁（维护成本高）。另可答：锁消除、锁粗化优化。</p>",
    "volatile 为什么不能保证原子性": "<p><strong>核心回答：</strong>volatile 只保证可见性和有序性，不保证互斥。</p><ol><li>可见性原理：写 volatile 变量强制刷回主存并使其他 CPU 缓存行失效（MESI + lock 前缀指令）</li><li>原子性问题：i++ 是“读-改-写”三步，两个线程可同时读到相同旧值，各自加 1 后写回，丢失一次更新</li><li>volatile 没有锁语义：多个线程可同时进入临界区，无法让复合操作串行化</li><li>解决：AtomicInteger（CAS）、synchronized、LongAdder（高并发计数）</li></ol><p>一句话：volatile 管“看得见、排得序”，不管“别插队”。</p>",
    "CAS 是什么": "<p><strong>核心回答：</strong>CAS（Compare And Swap）是比较并交换的无锁原子操作，是并发包的基石。</p><ol><li>语义：内存值 V、预期值 E、新值 N；仅当 V == E 时把 V 更新为 N，否则失败重试（自旋）</li><li>硬件支撑：CPU 的 cmpxchg 指令 + lock 前缀保证原子性，锁的是缓存行/总线而非线程阻塞</li><li>Java 实现：Unsafe.compareAndSwapInt 等，Atomic 类、AQS、ConcurrentHashMap 都基于它</li><li>优点：无阻塞、无上下文切换开销；缺点：自旋耗 CPU、只能保证单变量原子、ABA 问题（用版本号 AtomicStampedReference 解决）</li></ol>",
    "ABA 问题是什么？如何解决": "<p><strong>核心回答：</strong>值从 A 改成 B 又改回 A，CAS 检测不到中间变化，误认为没被动过。</p><ol><li>场景：线程1读到 A；线程2把 A→B→A；线程1 CAS(A→C) 成功，但对象已“不是原来的 A”（如引用被重用、链表节点被回收复用）</li><li>危害场景：无锁栈/队列中节点被回收又复用，指针虽然相同但链结构已变，导致数据损坏</li><li>解决：加版本号/时间戳——AtomicStampedReference（引用+版本戳）、AtomicMarkableReference（布尔标记）</li><li>JVM 层缓解：AQS 队列等用简单自旋+局部变量快照，多数业务场景 ABA 影响可忽略</li></ol>",
    "AQS 原理是什么": "<p><strong>核心回答：</strong>AQS（AbstractQueuedSynchronizer）= 一个 volatile int state + 一个 CLH 变体的 FIFO 双向等待队列，用 CAS + LockSupport 实现“获取-排队-阻塞-唤醒”。</p><ol><li>核心字段：state（同步状态，不同实现语义不同：ReentrantLock 的重入次数、Semaphore 的许可数、CountDownLatch 的计数）+ head/tail 指向等待队列</li><li>获取流程：子类 tryAcquire 尝试（CAS 改 state）→ 失败则包装成 Node 入队（enq 自旋 CAS）→ parkContingOn 挂起线程（LockSupport.park）</li><li>释放流程：tryRelease 改 state → unparkSuccessor 唤醒队列后继节点（从 tail 向前找第一个有效节点，应对取消状态）</li><li>模式：独占（锁）与共享（读锁、Semaphore、CountDownLatch），模板方法模式开放 tryAcquire/tryRelease 给子类</li><li>应用：ReentrantLock、Semaphore、CountDownLatch、ReentrantReadWriteLock、线程池 Worker 都基于 AQS</li></ol>",
    "ThreadLocal 原理是什么": "<p><strong>核心回答：</strong>每个 Thread 内部有一个 ThreadLocalMap，以 ThreadLocal 对象（弱引用）为 key，实现“线程私有副本”。</p><ol><li>set：取当前线程的 ThreadLocalMap，key=ThreadLocal 实例（弱引用包装），value=值</li><li>get：同理从当前线程的 map 里查，查询不到执行 setInitialValue 初始化</li><li>数据存在 Thread 上而不是 ThreadLocal 上：多线程访问各自的 map，天然无竞争，空间换时间</li><li>哈希：threadLocalHashCode 线性探测（开放寻址），冲突向后找空位</li><li>InheritableThreadLocal 可让子线程继承父线程的值（线程池场景会失灵，需阿里 TransmittableThreadLocal）</li></ol>",
    "ThreadLocal 为什么会内存泄漏": "<p><strong>核心回答：</strong>key 是弱引用会被回收变 null，但 value 是强引用链 Thread→Map→Entry→value 一直存在。</p><ol><li>引用链：Thread 活着 → ThreadLocalMap 活着 → Entry 的 value 活着；只要线程不死，value 无法回收</li><li>key 弱引用：ThreadLocal 外部无强引用时，GC 回收 key → Entry 变 (null, value) 的脏条目</li><li>线程池场景：核心线程长期存活，脏 value 持续累积 → 内存泄漏 + 脏数据串线程（业务事故）</li><li>自救机制：get/set 时会清理部分脏条目，但触发不确定</li><li>正确做法：用完必须 finally 中 remove()（try-with-resources 风格）；避免把大对象塞 ThreadLocal</li></ol>",
    "ThreadPoolExecutor 核心参数有哪些": "<p><strong>核心回答：</strong>七大参数（5+2）。</p><ol><li>corePoolSize：核心线程数，默认不回收（allowCoreThreadTimeOut 可改）</li><li>maximumPoolSize：最大线程数 = 线程池容量上限</li><li>keepAliveTime + unit：非核心线程空闲存活时间</li><li>workQueue：任务队列（SynchronousQueue 直接交接、LinkedBlockingQueue 无界默认 Integer.MAX、ArrayBlockingQueue 有界、DelayedWorkQueue 延时）</li><li>threadFactory：线程工厂，命名（便于排查）</li><li>handler：拒绝策略（Abort/CallerRuns/Discard/DiscardOldest）</li></ol>",
    "线程池有哪些拒绝策略": "<p><strong>核心回答：</strong>四种内置 + 自定义。</p><ol><li>AbortPolicy（默认）：抛 RejectedExecutionException，快速失败，多数场景最安全</li><li>CallerRunsPolicy：由提交任务的线程自己执行，天然限流降速，但会阻塞提交线程（IO 线程被占）</li><li>DiscardPolicy：静默丢弃新任务，不抛异常不通知，易吞掉关键任务</li><li>DiscardOldestPolicy：丢弃队列最老任务再重试提交（优先级场景慎用）</li><li>自定义：实现 RejectedExecutionException 钩子，常见做法：落 MQ/持久化重试、告警、降级</li></ol>",
    "为什么不推荐使用 Executors 创建线程池": "<p><strong>核心回答：</strong>两类工厂方法要么无界队列导致 OOM，要么无线程上限导致 OOM，且线程不可命名。</p><ol><li>newFixedThreadPool/newSingleThreadExecutor：LinkedBlockingQueue 无界（Integer.MAX_VALUE），任务堆积 → 内存溢出</li><li>newCachedThreadPool/newScheduledThreadPool：最大线程数 Integer.MAX_VALUE，瞬时打爆系统资源</li><li>阿里规约：必须手动 new ThreadPoolExecutor，明确七参数 + 有界队列 + 命名 ThreadFactory + 明确拒绝策略</li><li>显式参数让容量可评估、可监控、问题可定位</li></ol>",
    "如何合理设置线程池大小": "<p><strong>核心回答：</strong>按任务类型套公式起步，再压测调优。</p><ol><li>CPU 密集型：N + 1（多 1 防缺页/偶发停顿时的空转）</li><li>IO 密集型：N × (1 + 等待时间/计算时间)，或 2N 起步；纯等待型可更高</li><li>混合型：拆成两个线程池分别设置</li><li>其他考量：下游承受能力（DB 连接池、下游接口限流）、内存（每线程栈 1MB）、容器 CPU 配额（K8s limit 要换算）</li><li>动态化：可结合 setCorePoolSize 动态调整 + 监控（活跃线程数、队列长度、拒绝数）用数据迭代</li></ol>",
    "什么是 IO 多路复用": "<p><strong>核心回答：</strong>一个线程通过 select/poll/epoll 同时监听多个 fd 的就绪事件，谁就绪处理谁，避免一连接一线程的阻塞模型。</p><ol><li>解决痛点：BIO 每个连接占一个线程阻塞等待，连接数 = 线程数，万级连接即崩</li><li>核心思想：把“等待”交给内核（注册 fd 集合 + 就绪事件），线程只在 fd 就绪后才读写</li><li>三阶段对比：select 轮询全量 fd（O(n)，1024 上限）→ poll 链表无上限仍 O(n) → epoll 事件回调（O(1)，红黑树 + 就绪链表）</li><li>Java 对应：NIO 的 Selector 底层在 Linux 即 epoll</li><li>注意：多路复用只解决“等待连接”的并发，就绪后的读写仍需处理（可配合非阻塞 + Reactor 线程模型）</li></ol>",
    "Selector 原理是什么": "<p><strong>核心回答：</strong>Selector 是 Java NIO 的多路复用器，统一管理多个 Channel 的注册、选择键和就绪事件分发。</p><ol><li>组件：Selector（选择器）、SelectableChannel（ServerSocketChannel/SocketChannel）、SelectionKey（注册关系+兴趣事件集）</li><li>流程：channel.configureBlocking(false) → register(selector, OP_ACCEPT/OP_CONNECT/OP_READ/OP_WRITE) → select() 阻塞到有事件 → selectedKeys() 遍历处理 → key 移除</li><li>底层：Linux 上由 EPollSelectorImpl 实现（epoll_create/ctl/wait），macOS 用 kqueue</li><li>经典模式：主线程 select 循环（Reactor），读写在单线程或 Worker 池处理（Netty 主从 Reactor 即此思路）</li></ol>",
    "Java NIO 为什么性能更高": "<p><strong>核心回答：</strong>NIO = 非阻塞 IO + 多路复用 + Buffer + 面向块，用少量线程支撑海量连接。</p><ol><li>线程模型：BIO 一连接一线程（阻塞、内存和调度开销爆炸）；NIO 一个 Selector 线程管理上万 Channel（非阻塞，读写就绪才处理）</li><li>Buffer：面向 Buffer 的读写可批量操作，支持 position/limit/capacity 精细控制与零拷贝拼接</li><li>内核优化：配合 epoll 事件驱动，无空轮询的系统调用浪费；传输可直接对接零拷贝</li><li>生态：Netty 在 NIO 上补齐了内存池、无锁串行化、FastThreadLocal 等，成为高性能网络标配</li></ol><p>注意：连接少且每连接大流量时 BIO+线程池可能更简单；NIO 的优势在“海量连接”。</p>",
    "零拷贝是什么": "<p><strong>核心回答：</strong>零拷贝指减少（甚至消除）数据在内核态与用户态之间的冗余 CPU 拷贝。</p><ol><li>传统读文件发网络：4 次拷贝（磁盘→内核页缓存→用户缓冲→Socket 缓冲→网卡）+ 4 次上下文切换</li><li>mmap + write：文件映射到用户地址空间，省去“内核→用户”那次拷贝，剩 3 次拷贝</li><li>sendfile（Linux 2.1+）：数据全程在内核态从页缓存直达网卡（2 次拷贝 2 次切换）；带 Scatter/Gather 的 sendfile 仅拷贝描述符，由网卡 DMA 完成实际搬运（真·零 CPU 拷贝）</li><li>Java 支持：FileChannel.transferTo/transferFrom（Netty、Kafka、RocketMQ 的性能基石）</li></ol><p>收益：CPU 占用降低、内存带宽节省、延迟下降；Kafka 高吞吐的核心手段之一。</p>",
    "JDK 动态代理和 CGLIB 有什么区别": "<p><strong>核心回答：</strong>接口 vs 子类：JDK 代理基于接口（反射）；CGLIB 基于继承生成子类（字节码）。</p><ol><li>JDK 动态代理：Proxy.newProxyInstance + InvocationHandler，要求目标实现接口；代理类实现同一接口，方法调用转发到 invoke()</li><li>CGLIB：字节码框架（ASM）生成目标类的子类，重写非 final 方法，拦截在 MethodInterceptor；不能代理 final 类/方法（private/static 天然不可覆写）</li><li>性能：JDK 代理旧版本反射调用慢，JDK8+ 已大幅优化；CGLIB 生成快但首次生成耗时</li><li>Spring 选择：目标有接口默认 JDK 代理（Boot 2.x 默认改 CGLIB）；事务/异步注解加在实现类上时 CGLIB 更省心</li></ol>",
    "Spring Boot 自动配置原理是什么": "<p><strong>核心回答：</strong>@EnableAutoConfiguration → spring.factories/AutoConfiguration.imports 加载配置类 → 条件注解按需装配 Bean。</p><ol><li>@SpringBootApplication 组合了 @EnableAutoConfiguration，其 @Import(AutoConfigurationImportSelector) 导入候选配置</li><li>加载清单：Boot 2.7 前读 META-INF/spring.factories；之后读 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</li><li>条件装配：@ConditionalOnClass（类路径有类才生效）、@ConditionalOnMissingBean（用户自定义优先）、@ConditionalOnProperty 等</li><li>属性绑定：@EnableConfigurationProperties + @ConfigurationProperties(prefix=xxx) 绑定 yml 配置</li><li>约定优于配置：引入 starter 依赖即触发对应自动配置，开箱即用</li></ol>",
    "Spring Bean 默认单例安全吗": "<p><strong>核心回答：</strong>不安全。单例 Bean 被所有请求共享，有可变成员变量时存在并发问题。</p><ol><li>风险：Controller/Service 中定义可变实例字段（如计数器、SimpleDateFormat、非线程安全集合），并发请求会互相覆盖</li><li>无状态则安全：方法内局部变量、参数、注入的无状态 Bean 都没有共享可变状态</li><li>解决方案：改为局部变量；用 ThreadLocal 隔离；改 prototype/request 作用域；使用线程安全类型（DateTimeFormatter、AtomicInteger、ConcurrentHashMap）</li><li>排查特征：偶现数据串号/错乱，压测可复现</li></ol>",
    "MyBatis 工作流程是什么": "<p><strong>核心回答：</strong>接口代理 → SqlSession → Executor → StatementHandler → JDBC，结果经 ResultSetHandler 映射返回。</p><ol><li>启动：解析 mybatis-config 与 Mapper XML（或注解），注册 MappedStatement（SQL+参数映射+结果映射）到 Configuration</li><li>调用 getMapper：MapperProxy（JDK 动态代理）拦截接口方法，按全限定名定位 MappedStatement</li><li>执行：DefaultSqlSession 委派 Executor（先查缓存 → queryFromDatabase）；Executor 创建 StatementHandler，经 ParameterHandler 设参、执行 JDBC</li><li>结果：ResultSetHandler 按 resultMap/typeHandler 映射为对象返回</li><li>集成 Spring：SqlSessionTemplate 线程安全代理事务，MapperScannerConfigurer 扫描注册 Bean</li></ol>",
    "MyBatis 一级缓存和二级缓存有什么区别": "<p><strong>核心回答：</strong>一级缓存 SqlSession 级默认开启；二级缓存 Mapper(namespace) 级需显式开启、跨 Session 共享。</p><ol><li>一级缓存：PerpetualCache 存 BaseExecutor 本地 HashMap；同一 SqlSession 相同查询（同 SQL 同参数同分页）直接命中；执行 update/insert/delete 或 commit/close 后清空。Spring 集成下每次请求通常新 SqlSession，一级缓存几乎失效（防止脏读）</li><li>二级缓存：namespace 级，多个 SqlSession 共享；需 &lt;cache/&gt; 或 @CacheNamespace 开启，可配 TTL/FIFO/LRU/自定义；事务提交后才写入</li><li>坑：多表联查的 namespace 缓存无法感知其他表更新，易脏读；分布式多节点本地缓存不一致</li><li>结论：生产环境通常关闭二级缓存，缓存统一交给 Redis</li></ol>",
    "MyBatis 如何防止 SQL 注入": "<p><strong>核心回答：</strong>用 #{} 预编译占位符（PreparedStatement），绝不用 ${} 拼接用户输入。</p><ol><li>原理：#{} 转成 ?，参数通过 setString/setInt 传入，SQL 结构与数据分离，恶意输入只是“值”不可能改变语义</li><li>风险写法：${} 直接字符串替换进 SQL（表名、排序字段、like 拼接用户值）→ 注入点</li><li>必须用 ${} 的场景（动态表名/order by 列）：白名单校验 + 枚举映射，绝不透传用户输入</li><li>like 正确姿势：name like concat('%', #{keyword}, '%')</li><li>补充：同时开启数据库最小权限、日志审计做纵深防御</li></ol>",
    "MyBatis 中 #{} 和 ${} 有什么区别": "<p><strong>核心回答：</strong>#{} 预编译参数占位（防注入）；${} 字符串直接拼接（有注入风险）。</p><ol><li>底层：#{} → JDBC PreparedStatement 的 ? + 参数绑定；${} → StringFormat 式替换后执行</li><li>时机：#{} 在执行前已完成 SQL 骨架；${} 在解析 SQL 时替换，可用于动态表名/列名等结构性位置</li><li>安全：#{} 天然防注入；${} 若拼接用户输入即注入漏洞</li><li>实践：值一律 #{}；${} 仅用于数据库标识符（表/列/排序字段）且必须白名单校验</li></ol>",
    "MyBatis 为什么需要 Mapper 接口": "<p><strong>核心回答：</strong>接口是“SQL 的 Java 映射”，让开发者以面向对象方式调用 SQL，由动态代理完成真实执行。</p><ol><li>无实现类：MapperProxy（JDK 动态代理）拦截接口调用，按“接口全限定名.方法名”找到 MappedStatement 执行 SQL</li><li>好处：类型安全、IDE 可导航、免手写 JDBC 模板代码、SQL 与代码解耦（XML）</li><li>约束：方法名与 XML 的 id 一致、参数与 parameterType 对应、返回类型匹配 resultMap</li><li>本质：面向接口编程 + 配置驱动，是“半自动 ORM”灵活性的来源</li></ol>",
    "单例模式有哪些实现方式": "<p><strong>核心回答：</strong>主流五种：饿汉、双重检查锁（DCL）、静态内部类、枚举、ThreadLocal（少见）。</p><ol><li>饿汉式：类加载即创建，天然线程安全，可能浪费（未用到也创建）</li><li>懒汉式 + synchronized：线程安全但每次获取都加锁，性能差（基本弃用）</li><li>DCL：双重检查 + volatile（防止指令重排导致拿到半初始化对象），懒加载 + 高性能</li><li>静态内部类：利用类加载机制（外部类初始化不加载内部类），懒加载 + 线程安全，无锁，推荐</li><li>枚举：《Effective Java》推荐，天然防反射攻击和反序列化破坏</li></ol><p>追问点：反射可破坏前四种（AccessibleObject.setAccessible 调私有构造），可通过构造器内抛异常防御；反序列化破坏需 readResolve。</p>",
    "双重检查锁为什么需要 volatile": "<p><strong>核心回答：</strong>防止“分配内存 → 初始化 → 赋值引用”被指令重排，导致其他线程拿到未初始化完的对象。</p><ol><li>new 实际三步：①分配对象内存 ②执行构造初始化 ③instance 指向内存地址</li><li>②③可能重排为 ①③②：线程 A 执行到 ③（未②），线程 B 第一重检查发现 instance != null 直接返回，拿到“空壳”对象使用 → NPE/数据错乱</li><li>volatile 禁止该重排（写 volatile 前的操作不能重排到其后），并保证写入后对其他线程立即可见</li><li>第一重检查的意义：已创建后直接返回，避免每次都进入同步块，无竞争开销</li></ol>",
    "工厂模式解决什么问题": "<p><strong>核心回答：</strong>把“对象的创建”和“使用”解耦，让新增类型不动调用方代码。</p><ol><li>问题：调用方 new 具体类导致强耦合；类型增多时到处 if-else，改动散落各处（违反开闭原则）</li><li>三种形态：简单工厂（一个工厂 + switch，最常用）、工厂方法（每产品一个工厂类，符合开闭）、抽象工厂（产品族，如“一套 UI 组件”）</li><li>收益：统一创建逻辑（缓存/单例/日志/参数校验）、隐藏实现细节、便于测试（注入 mock 工厂）</li><li>框架应用：Spring BeanFactory/FactoryBean、JDK 的 Calendar.getInstance、连接池 DataSource</li></ol>",
    "代理模式有哪些应用场景": "<p><strong>核心回答：</strong>不改变目标类的前提下增强或控制访问，静态代理和动态代理两大类。</p><ol><li>横切增强：AOP 事务、日志、监控、权限（Spring AOP = JDK/CGLIB 动态代理）</li><li>远程代理：RPC 桩（Stub）、Feign/Dubbo 接口远程调用伪装成本地接口</li><li>延迟加载：Hibernate 懒加载实体、MyBatis 延迟关联查询</li><li>保护代理：权限校验、限流、熔断（Sentinel/Resilience4j 的资源代理）</li><li>池化与缓存：数据库连接代理（close 归还而非关闭）、缓存命中代理</li></ol>",
    "Spring 使用了哪些设计模式": "<p><strong>核心回答：</strong>至少八个，按框架核心到外围：</p><ol><li>工厂模式：BeanFactory/ApplicationContext 创建管理 Bean</li><li>单例模式：Bean 默认作用域（容器级单例，注册表实现）</li><li>代理模式：AOP（JDK 动态代理/CGLIB）</li><li>模板方法：JdbcTemplate/RedisTemplate/RestTemplate，固定流程开放扩展点</li><li>观察者模式：ApplicationEvent + Listener 事件机制</li><li>策略模式：Resource 加载策略、实例化策略（SimpleInstantiationStrategy）</li><li>适配器模式：HandlerAdapter（MVC 中适配不同 Controller）；AdvisorAdapter</li><li>装饰器模式：BeanWrapper、IO 相关的 Resource 包装</li><li>责任链模式：MVC 拦截器链、AOP 拦截器链（MethodInterceptor 链）</li></ol>",
    "Lambda 表达式原理是什么": "<p><strong>核心回答：</strong>Lambda 是编译期语法糖：调用点生成 invokedynamic 指令 + 运行时用 LambdaMetafactory 动态生成实现类。</p><ol><li>编译期：Lambda 体被编译成类中的一个私有静态/实例合成方法 lambda$xxx$0；调用点留 invokedynamic + BootstrapMethods 引导</li><li>首次执行：Bootstrap 调用 LambdaMetafactory.metafactory，用 ASM 动态生成一个实现函数式接口的内部类（非内部类文件，运行时生成），其方法体转调合成方法</li><li>后续执行：invokedynamic 常量绑定 CallSite，之后直接调用，无反射开销</li><li>this 语义：Lambda 中的 this 指向外围类实例（与匿名内部类不同，匿名类 this 指自身）</li><li>函数式接口：单一抽象方法（@FunctionalInterface 校验），Lambda 类型即该接口</li></ol>",
    "Stream 流是什么": "<p><strong>核心回答：</strong>声明式数据处理管道：数据源 → 中间操作（惰性）→ 终端操作（触发执行）。</p><ol><li>三类操作：源（集合/数组/IO/generate）；中间操作惰性求值（filter/map/flatMap/sorted/distinct/peek，返回新 Stream 可链式）；终端操作触发计算（collect/reduce/forEach/count/findFirst/anyMatch）</li><li>特性：不修改原数据源；一次消费（已消费的 Stream 抛 IllegalStateException）；惰性 + 短路（limit/findFirst 可提前结束）</li><li>并行流：parallelStream 基于 ForkJoinPool.commonPool 分治并行；有顺序依赖、IO 密集或小数据量时慎用（线程竞争反而慢）</li><li>注意：避免在循环外定义可变状态收集结果（破坏无副作用原则）；简单遍历用 for 性能更好，复杂转换才体现 Stream 表达力</li></ol>",
    "Optional 为什么出现": "<p><strong>核心回答：</strong>用返回值类型显式表达“可能没有值”，把 NPE 从运行期错误变成编译期可感知的问题。</p><ol><li>问题背景：null 的调用方无法知道“是否可能为 null”，防御式判空散落各处；null 语义模糊（无值/未初始化/未找到）</li><li>设计意图：Optional 只作为返回类型表达可能缺席，不是字段类型、不是参数类型（作者明确不建议）</li><li>正确用法：链式 orElse/orElseGet/orElseThrow（替代层层判空）、map/flatMap（ null 安全的转换）、ifPresent（消费）</li><li>反例：Optional.get() 不判 isPresent 直接取（比 null 更隐蔽）、orElse(昂贵计算)（无论是否为空都执行，应用 orElseGet）</li></ol>",
    "CompletableFuture 如何实现异步编程": "<p><strong>核心回答：</strong>基于观察者模式的异步回调 + 链式编排：注册回调，完成时按依赖关系触发，实现回调地狱到流水线的转变。</p><ol><li>创建：supplyAsync(有返回)/runAsync(无返回)，默认 ForkJoinPool.commonPool，建议显式传自定义线程池</li><li>串行：thenApply(转换)/thenAccept(消费)/thenRun(执行)，thenCompose 用于依赖另一个异步任务的串联</li><li>聚合：thenCombine(两者结果合并)/allOf(等全部)/anyOf(任一完成)；异常处理 exceptionally/whenComplete(可观察不改变)/handle(可转换)</li><li>原理：内部 Completion 栈（观察者链），任务完成时依次触发依赖阶段；结果传递无需阻塞 get</li><li>实践：自定义线程池隔离（避免 commonPool 被占满）、超时 orTimeout、与虚拟机/响应式框架选型对比（高吞吐网关可考虑 WebFlux）</li></ol>",
    "如何设计一个高并发 Java 服务": "<p><strong>核心回答：</strong>遵循总-分-总：先分层拆解流量路径（接入 → 应用 → 数据），每层给出“扩容 + 缓存 + 异步 + 降级”手段，最后强调度量与压测。</p><ol><li>接入层：无状态水平扩容 + 负载均衡（Nginx/LB），静态资源 CDN，网关层限流（令牌桶/漏桶、Sentinel 集群流控）</li><li>应用层：异步化（MQ 削峰、CompletableFuture 并行 RPC）、线程池隔离、无状态设计、JVM 调优与池化复用</li><li>数据层：缓存（Redis 多级缓存、缓存一致性策略：旁路 + 延迟双删/订阅 binlog）、读写分离、分库分表（ShardingSphere）、热点 key 本地缓存</li><li>存储兜底：MQ 削峰填谷、最终一致性对账、幂等设计</li><li>稳定性：限流-熔断-降级三板斧、容量规划压测（全链路压测）、监控告警（RT/QPS/错误率/资源水位）</li></ol><p>答题技巧：先问清量级（QPS、读写比、一致性要求）再给方案，展示“权衡”意识而非堆技术名词。</p>",
    "Java 服务 CPU 飙高如何排查": "<p><strong>核心回答：</strong>定位“线程 → 代码行”四步法：top 找进程 → top -Hp 找线程 → 线程号转 16 进制 → jstack 抓栈定位。</p><ol><li>top：找到 CPU 高的 Java 进程 PID</li><li>top -Hp PID：按线程查看，找到最耗 CPU 的线程 TID</li><li>printf '%x' TID：转十六进制（jstack 里 nid 是 16 进制）</li><li>jstack PID | grep -A 30 nid=0x..：定位线程栈，看是业务死循环、正则回溯、频繁 GC 还是锁自旋</li><li>补充手段：arthas thread -n 3 一条命令出 Top3 线程栈；持续高先看 GC（jstat -gcutil）区分“业务耗 CPU”还是“GC 耗 CPU”</li><li>常见根因：死循环/无限重试、大集合操作、正则灾难回溯、频繁 Full GC、序列化反射热点、死锁自旋</li></ol>",
    "Java 服务内存泄漏如何排查": "<p><strong>核心回答：</strong>先确认“泄漏”（内存持续增长且 Full GC 后不回落），再 dump 堆用 MAT 找支配树中的异常大对象。</p><ol><li>确认：jstat -gcutil 观察老年代/元空间趋势 + GC 后是否回落；监控堆内存锯齿是否“底部抬升”</li><li>导出：jmap -dump:live,format=b,file=heap.hprof PID（线上大堆注意停顿，可先用 jmap -histo 快速看对象直方图；或配置 -XX:+HeapDumpOnOutOfMemoryError）</li><li>分析：MAT 打开 hprof → Leak Suspects 报告 → Dominator Tree 找大对象 → 沿 GC Roots 引用链找谁持有</li><li>常见根因：静态集合只增不减、ThreadLocal 未 remove（线程池）、监听器/回调未注销、连接/流未关闭、缓存无界（无淘汰策略）、ClassLoader 泄漏（热部署）</li><li>修复后压测回归 + 增加内存水位告警</li></ol>",
    "线上接口 RT 突然升高如何定位": "<p><strong>核心回答：</strong>先确定影响面（全局 or 单接口），再沿调用链逐层测量，锁定“慢在哪一跳”。</p><ol><li>影响面判断：监控看是全服务 RT 升高（资源/依赖类问题）还是单接口（业务/数据类问题）</li><li>应用自身：CPU/GC/线程数（jstat、jstack）；线程池是否打满排队、锁竞争（BLOCKED 线程）</li><li>依赖排查：链路追踪（SkyWalking/Zipkin）看耗时分布——慢在哪个下游（DB/Redis/RPC/外部 HTTP）</li><li>DB 慢查：慢 SQL 日志、执行计划（explain）、锁等待、连接池耗尽（Druid/HikariCP 指标）</li><li>常见根因：慢 SQL/缺失索引、缓存失效击穿、下游抖动、大对象序列化、线程池排队、Full GC 停顿、流量突增触发限流排队</li><li>止血优先：降级非核心依赖、扩容、限流，再根治</li></ol>",
    "如何设计一个秒杀系统": "<p><strong>核心回答：</strong>核心矛盾是“瞬时洪峰 vs 库存少”，方案 = 层层削峰 + 库存不超卖 + 全链路防刷。</p><ol><li>流量漏斗：CDN/静态化（商品页）→ 网关限流（令牌桶按 QPS）→ MQ 排队（请求先入队异步扣减，用户轮询结果）→ 数据库兜底</li><li>库存不超卖：Redis 预扣（decr/Lua 原子判断+扣减）→ 扣减成功再落库；DB 层乐观锁（update stock = stock - 1 where id=? and stock &gt; 0）双保险</li><li>热点优化：库存分段（1 个商品拆 N 个 key）、本地缓存标记、热点探测（如京东 hotkey）</li><li>防刷：答题/验证码错峰、风控黑名单、接口隐藏（动态 URL）、下单限购（一人一单：唯一索引/Redis setnx）</li><li>一致性对账：异步落库失败进补偿队列；库存预热与回补；对账 Job 巡检</li><li>降级预案：库存售罄快速失败标记、非核心服务降级、静态兜底页</li></ol>",
    "如何保证接口幂等": "<p><strong>核心回答：</strong>幂等 = 同一请求执行 N 次与 1 次效果相同，按业务场景选：唯一索引、token 机制、状态机、分布式锁。</p><ol><li>insert 场景：唯一索引（业务键 order_no/biz_id 唯一约束），重复插入报错捕获返回成功——最可靠</li><li>更新场景：状态机（update ... where status = 待支付，条件更新天然幂等）；乐观锁版本号</li><li>防重复提交：token 机制——进页面先领 token（Redis setnx），提交带 token 删除成功才执行；或前端防抖 + 后端分布式锁（Redis setnx + 过期时间）锁业务键</li><li>删除场景：天然幂等</li><li>通用实现：唯一消息 ID + 消费去重表（MQ 场景）；支付回调等资金场景必须“查询-判断-操作”闭环 + 事务 + 对账兜底</li></ol>",
    "Java 服务如何优雅关闭": "<p><strong>核心回答：</strong>捕获“终止信号 → 停止接收新请求 → 等存量处理完 → 释放资源退出”，关键是注册 ShutdownHook + 配合外部探活摘流。</p><ol><li>注册钩子：Runtime.addShutdownHook，在 kill（SIGTERM）时触发（kill -9 SIGKILL 不触发，禁止用）</li><li>摘流：先从注册中心（Nacos）主动反注册/标记下线，等 LB/网关健康检查摘除节点（配合 sleep）</li><li>停止收新请求：Server（Tomcat/Netty）stop 停止接受新连接；Spring Boot 2.3+ server.shutdown=graceful + spring.lifecycle.timeout-per-shutdown-phase</li><li>存量处理：等待在途请求完成（超时上限 30s 左右）；关闭线程池 shutdown → awaitTermination → shutdownNow 兜底</li><li>资源释放：刷新缓冲、关闭数据源/连接池、MQ consumer 反订阅、提交未落盘数据、清理临时文件</li></ol><p>K8s 场景：preStop 摘流 + terminationGracePeriodSeconds 配合，覆盖滚动更新不丢流量。</p>"
  },
  "agent": {
    "OpenAI Agents SDK": "<p><strong>核心回答：</strong>OpenAI 官方的多 Agent 编排框架（Swarm 的生产化版本），核心抽象是 Agent、Handoff、Guardrail、Tracing。</p><ol><li>Agent：由模型 + 指令（instructions）+ 工具（function tools / hosted tools）+ 模型设置组成的单元</li><li>Handoff：Agent 间的任务移交机制，主 Agent 可把对话转给更专精的子 Agent（如客服转退款专员），交接时传递全部上下文</li><li>Guardrail：输入/输出校验器（并行运行），如用小模型做敏感内容检测</li><li>Runner：执行入口，支持单次、流式（streamed）和异步运行；Agent Loop 内置——模型输出工具调用就执行并把结果回灌，直到产出最终答复</li><li>Tracing：内建可视化轨迹（每步 LLM 调用、工具调用、handoff），用于调试与评估</li></ol><p>定位：官方、轻量、Python/JS 双语言，适合快速搭建“模型 + 工具 + 移交”模式的助手型多 Agent 系统。</p>",
    "Claude Agent SDK": "<p><strong>核心回答：</strong>Anthropic 官方 Agent 开发套件（即此前的 Claude Agent SDK），核心是把 Claude Code 的 Agent 能力（循环、工具、权限、上下文管理）产品化给开发者。</p><ol><li>架构：以 Claude 模型为核心的 Agent Loop（LLM ↔ 工具执行的反思循环），SDK 负责会话状态、上下文压缩、工具调度</li><li>能力组成：内置工具集（文件读写、Bash、搜索等）+ MCP 接入外部工具 + Skills（把流程性知识打包成可发现的技能）+ SubAgent（隔离上下文的并行子任务）</li><li>权限模型：分级权限控制（只读/可写/需确认），适合构建受控的 Coding Agent 和自动化办公 Agent</li><li>典型用法：query 接口发起会话，配置 tools/MCP server/systemPrompt，可接入文件系统与自有工具构建企业内部 Agent</li></ol><p>定位：面向“长任务、强工具、要沙箱”的重型 Agent 场景，与 Claude Code、MCP 生态深度协同。</p>"
  }
};
