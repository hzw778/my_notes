window.TAB_DATA = window.TAB_DATA || {};
window.TAB_DATA["concurrent"] = {"key":"concurrent","title":"2小时速通并发编程","url":"https://utxc8uqzfk.feishu.cn/wiki/XmklwlrvAiQ7cXkwpXpcOuQen6f","chapters":[{"no":"","title":"多线程","questions":[{"t":"Java里面的线程和操作系统的线程一样吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>不完全一样，但<strong>现在主流 JVM 里的 Java 线程基本是一对一映射到操作系统线程</strong>。</p><p><strong>1. Java 线程是什么</strong></p><p>Java 里你创建的是：</p><pre><code>new Thread(...)</code></pre><p>这是 JVM 提供的线程抽象。</p><p>它有 Java 层面的：</p><ul><li><code>Thread</code> 对象</li><li>线程状态</li><li>优先级</li><li>中断标记</li><li>线程栈</li><li><code>ThreadLocal</code></li></ul><p><strong>2. 操作系统线程是什么</strong></p><p>操作系统线程是内核真正调度的执行单位。</p><p>CPU 最终调度执行的是操作系统线程。</p><p><strong>3. 两者关系</strong></p><p>在现在常见的 HotSpot JVM 中：</p><pre><code>Java Thread  ≈  OS Thread</code></pre><p>也就是一个 Java 线程通常对应一个操作系统原生线程。</p><p>所以：</p><ul><li>Java 线程的创建最终会调用操作系统创建线程。</li><li>Java 线程调度依赖操作系统调度器。</li><li>Java 线程阻塞时，对应 OS 线程也可能阻塞。</li><li>多个 Java 线程可以在多核 CPU 上并行执行。</li></ul><p><strong>4. 不是完全一样的原因</strong></p><p>Java 线程是 JVM 层的抽象，操作系统线程是内核层的实体。</p><p>JVM 还会额外管理：</p><ul><li>Java 线程状态。</li><li>栈大小。</li><li>安全点。</li><li>GC 协作。</li><li><code>ThreadLocal</code>。</li><li>Java 中断机制。</li><li>监视器锁 <code>synchronized</code>。</li></ul><p>所以不能说它们完全等价。</p><p><strong>5. 补充：虚拟线程</strong></p><p>Java 21 之后有虚拟线程：</p><pre><code>Thread.startVirtualThread(...)</code></pre><p>虚拟线程不是一对一绑定 OS 线程。</p><p>它是 JVM 管理的轻量级线程，很多虚拟线程可以挂载到少量平台线程上执行。</p><p>所以更准确地说：</p><ul><li>传统 Java 平台线程：通常一对一对应 OS 线程。</li><li>Java 虚拟线程：不是一对一，对 OS 线程是多对少的关系。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 线程是 JVM 提供的抽象，操作系统线程是内核调度的执行单位。在主流 HotSpot JVM 中，传统 Java 线程通常是<strong>一对一映射到操作系统线程，所以 Java 线程的创建、调度和阻塞最终都依赖操作系统线程</strong>。但它们不完全一样，因为 JVM 还会在线程之上维护 Java 层状态、中断标记、ThreadLocal、GC 安全点和锁等机制。需要补充的是，Java 21 的虚拟线程不是一对一映射 OS 线程，而是大量虚拟线程复用少量平台线程。</p></blockquote>"},{"t":"使用多线程要注意哪些问题？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>使用多线程，面试可以从<strong>安全性、性能、资源、异常</strong>几个方面答：</p><ol><li><strong>线程安全问题</strong></li></ol><ul><li>多个线程共享变量时，可能出现数据竞争。</li><li>比如 <code>count++</code> 不是原子操作。</li><li>需要用：<ul><li><code>synchronized</code></li><li><code>Lock</code></li><li><code>volatile</code></li><li><code>AtomicInteger</code></li><li>线程安全集合</li></ul></li></ul><ol><li><strong>可见性问题</strong></li></ol><ul><li>一个线程修改了变量，另一个线程不一定马上看到。</li><li>可以用：<ul><li><code>volatile</code></li><li>加锁</li><li>并发工具类</li></ul></li></ul><ol><li><strong>原子性问题</strong></li></ol><ul><li>多步操作可能被线程切换打断。</li><li>比如读、改、写不是一个整体。</li><li>可以用：<ul><li>锁</li><li>CAS</li><li>原子类</li></ul></li></ul><ol><li><strong>有序性问题</strong></li></ol><ul><li>编译器和 CPU 可能指令重排序。</li><li>多线程下可能导致结果不符合预期。</li><li>可以通过 <code>volatile</code>、锁、内存屏障保证。</li></ul><ol><li><strong>死锁问题</strong></li></ol><ul><li>多个线程互相等待对方持有的锁，会死锁。</li><li>避免方式：<ul><li>固定加锁顺序</li><li>减少锁嵌套</li><li>使用 <code>tryLock</code></li><li>设置超时时间</li></ul></li></ul><ol><li><strong>线程数量控制</strong></li></ol><ul><li>线程不是越多越好。</li><li>太多会导致：<ul><li>上下文切换变多</li><li>内存占用增加</li><li>CPU 空转</li><li>打满数据库、Redis、下游服务</li></ul></li></ul><p>建议使用线程池，而不是无限创建线程。</p><ol><li><strong>线程池配置</strong></li></ol><p>使用线程池要注意：</p><ul><li>核心线程数</li><li>最大线程数</li><li>队列长度</li><li>拒绝策略</li><li>线程命名</li><li>任务超时时间</li></ul><p>不要随便用无界队列，否则可能导致内存堆积。</p><ol><li><strong>异常处理</strong></li></ol><ul><li>子线程异常不会自动抛到主线程。</li><li>要在线程任务内部捕获异常。</li><li>线程池中要注意任务异常日志，否则问题可能被吞掉。</li></ul><p><strong>面试背诵版</strong></p><p>使用多线程主要要注意线程安全、可见性、原子性和有序性问题。</p><p>多个线程共享变量时可能发生数据竞争，需要通过锁、volatile、CAS、原子类或线程安全集合来保证正确性。同时要避免死锁，比如固定加锁顺序、减少锁嵌套、使用 tryLock。线程数量也不能无限增加，线程太多会带来上下文切换、内存占用和资源打满的问题，所以通常使用线程池，并合理配置线程数、队列和拒绝策略。另外还要注意子线程异常处理，避免异常被吞掉。</p></blockquote>"},{"t":"保证数据的一致性有哪些方案呢？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>这里可以从<strong>单机多线程、数据库事务、分布式系统</strong>三个层面回答。</p><ol><li><strong>单机多线程内的数据一致性</strong></li></ol><p>如果是多个线程操作同一份数据，要用同步机制：</p><ul><li><code>synchronized</code></li><li><code>ReentrantLock</code></li><li><code>volatile</code></li><li><code>CAS</code></li><li>原子类</li><li>线程安全集合</li></ul><p>比如库存计数可以用锁或 <code>AtomicInteger</code>，避免并发修改丢失。</p><ol><li><strong>数据库层的一致性</strong></li></ol><p>数据库里主要靠事务和约束：</p><ul><li><strong>事务 ACID</strong><ul><li>原子性：要么都成功，要么都失败。</li><li>一致性：事务前后数据合法。</li><li>隔离性：并发事务互不干扰。</li><li>持久性：提交后不丢失。</li></ul></li><li><strong>隔离级别</strong><ul><li>防止脏读、不可重复读、幻读。</li></ul></li><li><strong>锁机制</strong><ul><li>行锁、表锁、间隙锁。</li><li><code>SELECT ... FOR UPDATE</code></li></ul></li><li><strong>唯一约束 / 外键 / 非空约束</strong><ul><li>从数据库层防止非法数据。</li></ul></li></ul><ol><li><strong>乐观锁 / 悲观锁</strong></li></ol><p>高并发更新时常用：</p><p><strong>乐观锁</strong></p><pre><code>UPDATE product<br/>SET stock = stock - 1,<br/>    version = version + 1<br/>WHERE id = 1<br/>  AND version = 10;</code></pre><p>适合冲突少的场景。</p><p><strong>悲观锁</strong></p><pre><code>SELECT *<br/>FROM product<br/>WHERE id = 1<br/>FOR UPDATE;</code></pre><p>适合冲突多、一致性要求高的场景。</p><ol><li><strong>分布式系统一致性</strong></li></ol><p>多个服务、多个数据库之间，一般用：</p><ul><li>分布式事务：<ul><li><code>2PC</code></li><li><code>TCC</code></li><li><code>Seata</code></li></ul></li><li>最终一致性：<ul><li>本地消息表</li><li>事务消息</li><li>MQ 重试</li><li>补偿任务</li></ul></li><li>幂等控制：<ul><li>唯一请求号</li><li>唯一索引</li><li>防重复消费</li></ul></li></ul><ol><li><strong>缓存和数据库一致性</strong></li></ol><p>如果用了 Redis 缓存，要考虑缓存一致性：</p><p>常见策略：</p><ul><li>先更新数据库，再删除缓存。</li><li>延迟双删。</li><li>通过 MQ 或 binlog 订阅同步缓存。</li><li>设置缓存过期时间兜底。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>保证数据一致性要看场景。单机多线程里，可以通过锁、CAS、volatile、原子类和线程安全集合保证共享数据一致。数据库层主要靠事务 ACID、隔离级别、行锁、唯一约束等机制。高并发更新场景可以使用乐观锁或悲观锁。分布式场景下可以用 2PC、TCC、Seata 这类分布式事务，也可以通过 MQ、本地消息表、事务消息、补偿任务实现最终一致性。同时还要做好幂等控制，比如唯一请求号和唯一索引。如果涉及缓存，还要考虑缓存和数据库的一致性。</p></blockquote>"},{"t":"线程的创建方式有哪些?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Java 面试里常说的线程创建方式有这几种：</p><ol><li><strong>继承 </strong><strong><code>Thread</code></strong><strong> 类</strong></li></ol><pre><code>class MyThread extends Thread {<br/>    public void run() {<br/>        System.out.println(\"执行任务\");<br/>    }<br/>}<br/><br/>new MyThread().start();</code></pre><p>特点：</p><ul><li>简单直接。</li><li>但 Java 单继承，继承了 <code>Thread</code> 就不能再继承其他类。</li></ul><ol><li><strong>实现 </strong><strong><code>Runnable</code></strong><strong> 接口</strong></li></ol><pre><code>class MyTask implements Runnable {<br/>    public void run() {<br/>        System.out.println(\"执行任务\");<br/>    }<br/>}<br/><br/>new Thread(new MyTask()).start();</code></pre><p>特点：</p><ul><li>更推荐。</li><li>任务和线程分离。</li><li>可以多个线程复用同一个任务对象。</li></ul><ol><li><strong>实现 </strong><strong><code>Callable</code></strong><strong> 接口</strong></li></ol><pre><code>Callable&lt;Integer&gt; task = () -&gt; 1 + 1;<br/><br/>FutureTask&lt;Integer&gt; futureTask = new FutureTask&lt;&gt;(task);<br/>new Thread(futureTask).start();<br/><br/>Integer result = futureTask.get();</code></pre><p>特点：</p><ul><li>可以有返回值。</li><li>可以抛异常。</li><li>需要配合 <code>FutureTask</code> 或线程池使用。</li></ul><ol><li><strong>使用线程池</strong></li></ol><pre><code>ExecutorService executor = Executors.newFixedThreadPool(10);<br/><br/>executor.submit(() -&gt; {<br/>    System.out.println(\"执行任务\");<br/>});</code></pre><p>特点：</p><ul><li>实际开发最常用。</li><li>可以复用线程。</li><li>避免频繁创建和销毁线程。</li><li>可以控制线程数量、队列和拒绝策略。</li></ul><ol><li><strong>面试补充：虚拟线程</strong></li></ol><p>Java 21 之后可以使用虚拟线程：</p><pre><code>Thread.startVirtualThread(() -&gt; {<br/>    System.out.println(\"虚拟线程执行任务\");<br/>});</code></pre><p>特点：</p><ul><li>更轻量。</li><li>适合大量 IO 密集型任务。</li><li>不等同于传统操作系统线程。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 创建线程常见方式有四种：继承 Thread 类、实现 Runnable 接口、实现 Callable 接口配合 FutureTask，以及使用线程池。Thread 方式简单但受单继承限制；Runnable 更常用，实现了任务和线程分离；Callable 可以有返回值和异常；实际开发中最推荐使用线程池，因为它可以复用线程、控制并发数量，避免频繁创建和销毁线程。Java 21 之后还可以补充虚拟线程，适合大量 IO 密集型任务。</p></blockquote>"},{"t":"怎么启动线程 ？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>启动线程用的是：</p><pre><code>start()</code></pre><p>不是直接调用 <code>run()</code>。</p><ol><li><strong>正确启动方式</strong></li></ol><pre><code>Thread t = new Thread(() -&gt; {<br/>    System.out.println(\"子线程执行\");<br/>});<br/><br/>t.start();</code></pre><p>调用 <code>start()</code> 后，JVM 会创建新的线程，并让这个新线程去执行 <code>run()</code> 方法里的代码。</p><p><strong>2. </strong><strong><code>start()</code></strong><strong> 和 </strong><strong><code>run()</code></strong><strong> 的区别</strong></p><pre><code>t.start();</code></pre><ul><li>真正启动一个新线程。</li><li>由新线程执行 <code>run()</code>。</li></ul><pre><code>t.run();</code></pre><ul><li>只是普通方法调用。</li><li>不会创建新线程。</li><li>还是当前线程自己执行。</li></ul><ol><li><strong>举个例子</strong></li></ol><pre><code>Thread t = new Thread(() -&gt; {<br/>    System.out.println(Thread.currentThread().getName());<br/>});<br/><br/>t.start();</code></pre><p>可能输出：</p><pre><code>Thread-0</code></pre><p>如果写成：</p><pre><code>t.run();</code></pre><p>输出可能是：</p><pre><code>main</code></pre><p>说明还是主线程在执行。</p><p><strong>面试背诵版</strong></p><blockquote><p>启动线程应该调用 <code>start()</code> 方法，而不是直接调用 <code>run()</code>。<code>start()</code> 会让 JVM 创建一个新的线程，并由新线程执行 <code>run()</code> 方法；而直接调用 <code>run()</code> 只是普通方法调用，不会创建新线程，代码仍然在当前线程中执行。另外，一个线程对象的 <code>start()</code> 只能调用一次，重复调用会抛出 <code>IllegalThreadStateException</code>。</p></blockquote>"},{"t":"如何停止一个线程的运行?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Java 里停止线程，<strong>推荐让线程自己“优雅退出”</strong>，不要强行杀死。</p><ol><li><strong>使用中断机制 </strong><strong><code>interrupt()</code></strong></li></ol><p>这是最推荐的方式。</p><pre><code>Thread thread = new Thread(() -&gt; {<br/>    while (!Thread.currentThread().isInterrupted()) {<br/>        // 执行业务逻辑<br/>    }<br/>});<br/><br/>thread.start();<br/><br/>// 请求线程停止<br/>thread.interrupt();</code></pre><p>含义是：</p><blockquote><p>外部发出停止信号，线程自己检查中断标记后退出。</p></blockquote><ol><li><strong>阻塞状态下的中断</strong></li></ol><p>如果线程在 <code>sleep</code>、<code>wait</code>、<code>join</code> 等阻塞方法中，调用 <code>interrupt()</code> 会抛出 <code>InterruptedException</code>。</p><pre><code>Thread thread = new Thread(() -&gt; {<br/>    try {<br/>        while (!Thread.currentThread().isInterrupted()) {<br/>            Thread.sleep(1000);<br/>        }<br/>    } catch (InterruptedException e) {<br/>        // 恢复中断标记<br/>        Thread.currentThread().interrupt();<br/>    }<br/>});</code></pre><p>注意：</p><ul><li>抛出 <code>InterruptedException</code> 后，中断标记会被清除。</li><li>通常需要重新设置中断标记，或者直接退出。</li></ul><ol><li><strong>使用 volatile 标志位</strong></li></ol><p>也可以用一个共享标志控制线程退出：</p><pre><code>class Worker implements Runnable {<br/>    private volatile boolean running = true;<br/><br/>    public void stop() {<br/>        running = false;<br/>    }<br/><br/>    public void run() {<br/>        while (running) {<br/>            // 执行业务逻辑<br/>        }<br/>    }<br/>}</code></pre><p>适合非阻塞循环任务。</p><p>但如果线程阻塞在 <code>sleep</code>、IO、队列等待里，单纯标志位可能不能马上生效。</p><ol><li><strong>线程池中停止任务</strong></li></ol><p>如果是线程池：</p><pre><code>executor.shutdown();</code></pre><p>表示不再接收新任务，等已有任务执行完。</p><pre><code>executor.shutdownNow();</code></pre><p>会尝试中断正在执行的任务，并返回等待队列中的任务。</p><ol><li><strong>不推荐使用 </strong><strong><code>stop()</code></strong></li></ol><pre><code>thread.stop();</code></pre><p>不推荐，已经废弃。</p><p>原因：</p><ul><li>会强制终止线程。</li><li>可能导致锁被突然释放。</li><li>共享数据处于不一致状态。</li><li>容易产生严重并发问题。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 中停止线程推荐使用优雅停止，而不是强制杀死。常见方式是调用 <code>interrupt()</code> 设置中断标记，线程在运行过程中定期检查 <code>isInterrupted()</code>，然后自己退出；如果线程阻塞在 <code>sleep</code>、wait、join<code>，会抛出 </code>InterruptedException<code>，可以在 catch 中处理退出或恢复中断标记。也可以使用 </code>volatile<code>标志位控制循环结束，但它对阻塞场景不够及时。线程池中可以用</code>shutdown<code>或</code>shutdownNow<code>。不推荐使用已经废弃的 </code>stop()`，因为它可能导致锁突然释放和数据不一致。</p></blockquote>"},{"t":"调用 interrupt 是如何让线程抛出异常的?","tag":"","p":"","html":"<p><code>interrupt()</code> 本身<strong>不是直接把异常扔进线程里</strong>，它主要做的是：<strong>设置线程的中断标记</strong>。只有线程正阻塞在某些“可中断方法”上时，这些方法检测到中断，才会抛出 <code>InterruptedException</code>。</p><ol><li><strong>普通运行中的线程</strong></li></ol><p>如果线程正在正常执行代码：</p><pre><code>while (true) {<br/>    // 业务逻辑<br/>}</code></pre><p>另一个线程调用：</p><pre><code>thread.interrupt();</code></pre><p>结果只是：</p><pre><code>该线程的中断标记被设置为 true</code></pre><p>不会立刻抛异常，也不会强制停止线程。</p><p>线程需要自己检查：</p><pre><code>Thread.currentThread().isInterrupted()</code></pre><ol><li><strong>阻塞中的线程</strong></li></ol><p>如果线程正在执行：</p><pre><code>Thread.sleep(10000);</code></pre><p>这时其他线程调用：</p><pre><code>thread.interrupt();</code></pre><p><code>sleep()</code> 会检测到中断标记，然后：</p><ul><li>清除中断标记。</li><li>抛出 <code>InterruptedException</code>。</li><li>线程从阻塞状态被唤醒。</li></ul><p>类似的可中断阻塞方法还有：</p><ul><li><code>Object.wait()</code></li><li><code>Thread.sleep()</code></li><li><code>Thread.join()</code></li><li><code>BlockingQueue.take()</code></li><li><code>Lock.lockInterruptibly()</code></li></ul><ol><li><strong>为什么会抛异常</strong></li></ol><p>因为这些方法内部会进入等待 / 阻塞状态。</p><p>JVM 或底层线程库发现线程被中断后，会让这个阻塞方法提前返回，并通过 <code>InterruptedException</code> 告诉你：</p><pre><code>我不是正常等完的，是被中断唤醒的</code></pre><ol><li><strong>注意中断标记会被清除</strong></li></ol><p>很多抛出 <code>InterruptedException</code> 的方法，在抛异常前会清除中断标记。</p><p>所以常见写法是：</p><pre><code>try {<br/>    Thread.sleep(10000);<br/>} catch (InterruptedException e) {<br/>    Thread.currentThread().interrupt(); // 恢复中断标记<br/>}</code></pre><p>或者直接退出线程。</p><p><strong>面试背诵版</strong></p><blockquote><p><code>interrupt()</code> 并不会直接杀死线程，也不是直接抛异常。它本质上是设置线程的中断标记。如果线程正在正常运行，只会把中断标记设为 true，需要线程自己通过 <code>isInterrupted()</code> 检查并退出。如果线程阻塞在 <code>sleep</code>、<code>wait</code>、<code>join</code>、<code>BlockingQueue.take</code> 等可中断方法上，这些方法检测到中断后会提前唤醒线程，清除中断标记，并抛出 <code>InterruptedException</code>。所以异常是阻塞方法响应中断时抛出的，不是 <code>interrupt()</code> 强行抛进去的。</p></blockquote>"},{"t":"Java线程的状态有哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Java 线程状态在 <code>Thread.State</code> 里一共有 <strong>6 种</strong>：</p><pre><code>NEW、RUNNABLE、BLOCKED、WAITING、TIMED_WAITING、TERMINATED</code></pre><ol><li><strong>NEW：新建状态</strong></li></ol><p>线程对象创建了，但还没有调用 <code>start()</code>。</p><pre><code>Thread t = new Thread();</code></pre><p>此时就是 <code>NEW</code>。</p><ol><li><strong>RUNNABLE：可运行状态</strong></li></ol><p>调用 <code>start()</code> 后，线程进入 <code>RUNNABLE</code>。</p><p>注意 Java 里的 <code>RUNNABLE</code> 包括两种情况：</p><ul><li>正在 CPU 上运行。</li><li>已经就绪，等待 CPU 调度。</li></ul><pre><code>t.start();</code></pre><ol><li><strong>BLOCKED：阻塞状态</strong></li></ol><p>线程在等待进入 <code>synchronized</code> 临界区时，会进入 <code>BLOCKED</code>。</p><span>[原文档此处有配图]</span><p>比如一个线程已经持有锁，另一个线程想进入同一个 <code>synchronized</code> 块，就会 <code>BLOCKED</code>。</p><pre><code>synchronized (lock) {<br/>    // 等待 monitor 锁<br/>}</code></pre><ol><li><strong>WAITING：无限期等待</strong></li></ol><p>线程在等待其他线程唤醒，没有超时时间。</p><p>常见方式：</p><pre><code>object.wait();<br/>thread.join();<br/>LockSupport.park();</code></pre><p>需要其他线程：</p><ul><li><code>notify</code></li><li><code>notifyAll</code></li><li>被等待线程结束</li><li><code>unpark</code></li></ul><p>才能继续。</p><ol><li><strong>TIMED_WAITING：限时等待</strong></li></ol><p>线程等待一段时间，时间到了自动返回。</p><p>常见方式：</p><pre><code>Thread.sleep(1000);<br/>object.wait(1000);<br/>thread.join(1000);<br/>LockSupport.parkNanos(...);</code></pre><ol><li><strong>TERMINATED：终止状态</strong></li></ol><p>线程执行完 <code>run()</code> 方法，或者因为异常退出，就进入终止状态。</p><p>线程终止后不能再次 <code>start()</code>。</p><p><strong>状态流转简单图</strong></p><pre><code>NEW<br/> ↓ start()<br/>RUNNABLE<br/> ↓ 等 synchronized 锁<br/>BLOCKED<br/> ↓ wait/join/park<br/>WAITING<br/> ↓ sleep/wait(timeout)/join(timeout)<br/>TIMED_WAITING<br/> ↓ run 结束<br/>TERMINATED</code></pre><p>注意：<code>BLOCKED</code>、<code>WAITING</code>、<code>TIMED_WAITING</code> 结束后，一般回到 <code>RUNNABLE</code>，等待 CPU 调度。</p><p><strong>面试背诵版</strong></p><blockquote><p>Java 线程状态有 6 种：NEW、RUNNABLE、BLOCKED、WAITING、TIMED_WAITING 和 TERMINATED。</p><ul><li>NEW 是线程创建但未 start；</li><li>RUNNABLE 表示可运行，包括正在运行和等待 CPU 调度；</li><li>BLOCKED 是等待 synchronized 监视器锁；</li><li>WAITING 是无限期等待，比如 wait、join、park；</li><li>TIMED_WAITING 是限时等待，比如 sleep、wait(timeout)、join(timeout)；</li><li>TERMINATED 是线程执行结束或异常退出。需要注意，Java 的 RUNNABLE 不区分操作系统里的就绪和运行。</li></ul></blockquote>"},{"t":"sleep 和 wait的区别是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>核心区别</strong></p><ul><li><code>sleep()</code> 是 <code>Thread</code> 类的方法，作用是让当前线程休眠一段时间。</li><li><code>wait()</code> 是 <code>Object</code> 类的方法，作用是让线程等待某个对象的通知。</li></ul><ol><li><strong>所属类不同</strong></li></ol><pre><code>Thread.sleep(1000);</code></pre><ul><li><code>sleep()</code> 属于 <code>Thread</code>。</li></ul><pre><code>lock.wait();</code></pre><ul><li><code>wait()</code> 属于 <code>Object</code>。</li></ul><p>因为 Java 中每个对象都可以作为锁，所以 <code>wait()</code> 放在 <code>Object</code> 里。</p><ol><li><strong>是否释放锁不同</strong></li></ol><p>这是最重要区别。</p><ul><li><code>sleep()</code> <strong>不会释放锁</strong>。</li><li><code>wait()</code> <strong>会释放当前对象锁</strong>。</li></ul><p>比如线程在 <code>synchronized</code> 里调用 <code>sleep()</code>：</p><pre><code>synchronized (lock) {<br/>    Thread.sleep(1000);<br/>}</code></pre><p>睡眠期间仍然持有 <code>lock</code>，其他线程进不来。</p><p>如果调用 <code>wait()</code>：</p><pre><code>synchronized (lock) {<br/>    lock.wait();<br/>}</code></pre><p>线程会释放 <code>lock</code>，其他线程可以进入同步块。</p><ol><li><strong>使用位置不同</strong></li></ol><ul><li><code>sleep()</code> 可以在任何地方调用。</li><li><code>wait()</code> 必须在 <code>synchronized</code> 代码块或同步方法里调用。</li></ul><p>否则会抛：</p><pre><code>IllegalMonitorStateException</code></pre><ol><li><strong>唤醒方式不同</strong></li></ol><p><code>sleep()</code>：</p><ul><li>时间到了自动醒。</li><li>或者被 <code>interrupt()</code> 中断。</li></ul><p><code>wait()</code>：</p><ul><li>需要其他线程调用：<ul><li><code>notify()</code></li><li><code>notifyAll()</code></li></ul></li><li>或者设置超时时间：<ul><li><code>wait(timeout)</code></li></ul></li><li>或者被 <code>interrupt()</code> 中断。</li></ul><ol><li><strong>用途不同</strong></li></ol><ul><li><code>sleep()</code>：让线程暂停执行一段时间。</li><li><code>wait()</code>：线程之间通信 / 协作。</li></ul><p>比如生产者消费者模型中，经常用：</p><pre><code>wait();<br/>notify();</code></pre><p><strong>面试背诵版</strong></p><blockquote><p>sleep 和 wait 的区别主要有四点。第一，sleep 是 Thread 类的方法，wait 是 Object 类的方法；第二，sleep 不会释放锁，而 wait 会释放当前对象锁，这是最核心区别；第三，sleep 可以在任意位置调用，wait 必须在 synchronized 中调用，否则会抛 IllegalMonitorStateException；第四，sleep 时间到了会自动恢复，wait 需要 notify、notifyAll、超时或 interrupt 唤醒。sleep 主要用于线程休眠，wait 主要用于线程间通信。</p></blockquote>"},{"t":"sleep会释放cpu吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>会，<code>sleep()</code> 会让出 CPU。</p><p><strong>准确说法</strong></p><ul><li>线程调用 <code>Thread.sleep()</code> 后，会进入 <code>TIMED_WAITING</code> 状态。</li><li>在睡眠时间内，它不会占用 CPU 执行。</li><li>CPU 可以去调度其他线程。</li></ul><p><strong>但是注意</strong></p><p><code>sleep()</code> <strong>不会释放锁</strong>。</p><p>比如：</p><pre><code>synchronized (lock) {<br/>    Thread.sleep(1000);<br/>}</code></pre><p>这个线程睡眠时：</p><ul><li>CPU 会让出来。</li><li>但 <code>lock</code> 这把锁还被它持有。</li><li>其他线程如果想进入同一个 <code>synchronized(lock)</code>，还是进不去。</li></ul><p><strong>一句话记忆</strong></p><blockquote><p><code>sleep()</code> 会释放 CPU，但不会释放锁。<code>wait()</code> 既会让出 CPU，也会释放对象锁。</p></blockquote><p><strong>面试回答版</strong></p><blockquote><p>sleep 会让当前线程暂停执行一段时间，进入 TIMED_WAITING 状态，所以它会让出 CPU，CPU 可以调度其他线程。但 sleep 不会释放已经持有的锁，如果在线程持有 synchronized 锁时调用 sleep，其他线程仍然拿不到这把锁。</p></blockquote>"},{"t":"blocked和waiting有啥区别","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>一句话区别</strong></p><ul><li><code>BLOCKED</code>：线程在<strong>等 synchronized 锁</strong>。</li><li><code>WAITING</code>：线程在<strong>等别人唤醒/通知</strong>。</li></ul><ol><li><strong>BLOCKED</strong></li></ol><p>线程想进入 <code>synchronized</code>，但锁被别人拿着。</p><pre><code>synchronized (lock) {<br/>    // 临界区<br/>}</code></pre><p>如果线程 A 持有 <code>lock</code>，线程 B 也想进入，就会进入 <code>BLOCKED</code>。</p><p>特点：</p><ul><li>被动等待锁。</li><li>等的是对象监视器锁。</li><li>锁释放后，重新竞争锁。</li><li>主要对应 <code>synchronized</code>。</li></ul><ol><li><strong>WAITING</strong></li></ol><p>线程主动调用等待方法，进入无限期等待。</p><p>常见：</p><pre><code>lock.wait();<br/>thread.join();<br/>LockSupport.park();</code></pre><p>特点：</p><ul><li>主动等待。</li><li>等通知、唤醒或其他线程结束。</li><li>没有超时时间。</li><li>被 <code>notify</code>、<code>notifyAll</code>、<code>unpark</code>、<code>interrupt</code> 等唤醒。</li></ul><ol><li><strong>关键区别表</strong></li></ol><sheet></sheet><ol><li><strong>注意</strong></li></ol><p>如果线程调用：</p><pre><code>lock.wait();</code></pre><p>它会：</p><ul><li>释放当前对象锁。</li><li>进入 <code>WAITING</code>。</li></ul><p>如果线程只是想进入 <code>synchronized</code> 但没抢到锁，它是：</p><pre><code>BLOCKED</code></pre><p><strong>面试背诵版</strong></p><blockquote><p>BLOCKED 和 WAITING 的区别在于等待的原因不同。BLOCKED 是线程想进入 synchronized 同步块，但拿不到对象监视器锁，所以被阻塞；WAITING 是线程主动调用 wait、join 或 LockSupport.park 后进入无限期等待，等待其他线程 notify、unpark 或被等待线程结束。BLOCKED 是等锁，WAITING 是等通知。wait 会释放锁，而 BLOCKED 线程本来就没有拿到锁。</p></blockquote><span>[原文档此处有配图]</span>"},{"t":"wait 状态下的线程如何进行恢复到 running 状态?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>WAITING</code> 状态的线程<strong>不是直接恢复到 running</strong>，而是先被唤醒，变成可运行状态，再等待 CPU 调度。</p><ol><li><strong>怎么被唤醒</strong></li></ol><p>线程进入等待：</p><pre><code>synchronized (lock) {<br/>    lock.wait();<br/>}</code></pre><p>可以通过这些方式恢复：</p><ul><li>其他线程调用：</li></ul><pre><code>lock.notify();</code></pre><ul><li>或者：</li></ul><pre><code>lock.notifyAll();</code></pre><ul><li>线程被中断：</li></ul><pre><code>thread.interrupt();</code></pre><ul><li>如果是 <code>wait(timeout)</code>，时间到了也会恢复。</li></ul><ol><li><strong>notify 后能不能马上执行</strong></li></ol><p>不能。</p><p>调用 <code>wait()</code> 时，线程会释放 <code>lock</code> 并进入等待队列。</p><p>当其他线程调用 <code>notify()</code> 后：</p><pre><code>等待线程从 Wait Set 进入 Entry Set</code></pre><p>也就是从等待队列进入锁竞争队列。</p><p>它还必须重新竞争到 <code>lock</code>，才能从 <code>wait()</code> 后面继续执行。</p><ol><li><strong>完整流程</strong></li></ol><pre><code>线程 A 调用 wait()<br/>  ↓<br/>释放锁，进入 WAITING<br/>  ↓<br/>线程 B 调用 notify/notifyAll<br/>  ↓<br/>线程 A 被唤醒，进入锁竞争队列<br/>  ↓<br/>线程 B 退出 synchronized，释放锁<br/>  ↓<br/>线程 A 重新竞争锁<br/>  ↓<br/>线程 A 拿到锁，从 wait 后继续执行<br/>  ↓<br/>进入 RUNNABLE，等待 CPU 调度运行</code></pre><p>Java 线程状态里没有单独的 <code>RUNNING</code>，正在运行和就绪都属于 <code>RUNNABLE</code>。</p><ol><li><strong>interrupt 的情况</strong></li></ol><p>如果等待中的线程被中断：</p><pre><code>thread.interrupt();</code></pre><p>会从 <code>wait()</code> 中醒来，并抛出：</p><pre><code>InterruptedException</code></pre><p><strong>面试背诵版</strong></p><blockquote><p>wait 状态下的线程可以通过 notify、notifyAll 或 interrupt 恢复，如果是 wait(timeout)，超时也会恢复。需要注意，notify 后线程不会马上运行，而是从等待队列进入锁竞争队列，必须等当前线程释放 synchronized 锁，并且自己重新获取到锁后，才能从 wait 方法后继续执行。之后线程进入 RUNNABLE 状态，等待 CPU 调度。Java 中没有单独的 RUNNING 状态，运行中和就绪都属于 RUNNABLE。</p></blockquote>"},{"t":"notify 和 notifyAll 的区别?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p></p></blockquote>"},{"t":"notify 选择哪个线程?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>WAITING</code> 状态的线程恢复，不是一步直接到 <code>RUNNING</code>，而是一般先回到 <code>RUNNABLE</code>，再由 CPU 调度运行。</p><ol><li><strong>wait 状态怎么被唤醒</strong></li></ol><p>线程调用：</p><pre><code>lock.wait();</code></pre><p>后会进入 <code>WAITING</code>，并释放 <code>lock</code> 对象锁。</p><p>它可以通过这些方式被唤醒：</p><ul><li>其他线程调用：</li></ul><pre><code>lock.notify();</code></pre><ul><li>其他线程调用：</li></ul><pre><code>lock.notifyAll();</code></pre><ul><li>线程被中断：</li></ul><pre><code>thread.interrupt();</code></pre><p>如果是 <code>wait(timeout)</code>，时间到了也会被唤醒，进入的是从 <code>TIMED_WAITING</code> 恢复。</p><ol><li><strong>notify 后能直接 running 吗？</strong></li></ol><p>不能。</p><p>被 <code>notify()</code> 唤醒后，它只是从：</p><pre><code>Wait Set 等待队列</code></pre><p>进入：</p><pre><code>Entry Set 锁竞争队列</code></pre><p>它必须重新竞争到 <code>lock</code> 这把锁，才能继续执行。</p><p>因为 <code>wait()</code> 后面的代码还在 <code>synchronized(lock)</code> 里面。</p><ol><li><strong>完整流程</strong></li></ol><p>假设线程 A 在等待：</p><pre><code>synchronized (lock) {<br/>    lock.wait();<br/>    // 被唤醒后从这里继续<br/>}</code></pre><p>线程 B 唤醒它：</p><pre><code>synchronized (lock) {<br/>    lock.notify();<br/>}</code></pre><p>流程是：</p><pre><code>1. 线程 A 调用 wait，释放 lock，进入 WAITING<br/>2. 线程 B 获取 lock，调用 notify<br/>3. 线程 A 被唤醒，进入锁竞争队列<br/>4. 线程 B 退出 synchronized，释放 lock<br/>5. 线程 A 重新竞争 lock<br/>6. 线程 A 拿到 lock 后，从 wait 后面继续执行<br/>7. 最终进入 RUNNABLE，等待 CPU 调度，真正运行时就是 RUNNING</code></pre><p>Java 线程状态里没有单独的 <code>RUNNING</code>，通常统一叫 <code>RUNNABLE</code>。</p><ol><li><strong>interrupt 情况</strong></li></ol><p>如果线程在 <code>wait()</code> 中被中断：</p><pre><code>thread.interrupt();</code></pre><p>会抛出：</p><pre><code>InterruptedException</code></pre><p>并从等待中返回。</p><p><strong>面试背诵版</strong></p><blockquote><p>wait 状态的线程可以通过 notify、notifyAll 或 interrupt 被唤醒，如果是 wait(timeout)，超时也会恢复。需要注意，notify 后线程不会立刻运行，而是从等待队列进入锁竞争队列，必须等通知线程释放 synchronized 锁，并且自己重新竞争到锁后，才能从 wait 方法后面继续执行。之后线程进入 RUNNABLE 状态，等待 CPU 调度执行。Java 线程状态中没有单独的 RUNNING，一般运行中和就绪都属于 RUNNABLE。</p></blockquote>"},{"t":"不同的线程之间如何通信？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>不同线程之间通信，核心是：<strong>同一进程内线程</strong><strong>共享内存</strong><strong>，所以可以通过共享变量 + 同步机制来通信。</strong></p><ol><li><strong>共享变量</strong></li></ol><p>多个线程可以访问同一个对象或变量。</p><pre><code>class Data {<br/>    volatile boolean flag = false;<br/>}</code></pre><p>一个线程修改，另一个线程读取。</p><p>但要注意线程安全，需要配合：</p><ul><li><code>volatile</code></li><li>锁</li><li>原子类</li></ul><ol><li><strong>wait / notify</strong></li></ol><p>用于线程等待和通知。</p><pre><code>synchronized (lock) {<br/>    while (!condition) {<br/>        lock.wait();<br/>    }<br/>}</code></pre><p>另一个线程：</p><pre><code>synchronized (lock) {<br/>    condition = true;<br/>    lock.notifyAll();<br/>}</code></pre><p>适合生产者消费者这类场景。</p><ol><li><strong>BlockingQueue</strong></li></ol><p>最常用、最推荐的线程通信方式之一。</p><pre><code>BlockingQueue&lt;String&gt; queue = new LinkedBlockingQueue&lt;&gt;();<br/><br/>// 生产者<br/>queue.put(\"task\");<br/><br/>// 消费者<br/>String task = queue.take();</code></pre><p>特点：</p><ul><li>队列为空时消费者阻塞。</li><li>队列满时生产者阻塞。</li><li>内部已经处理好了加锁和唤醒。</li></ul><ol><li><strong>volatile</strong></li></ol><p>适合传递简单状态。</p><pre><code>volatile boolean running = true;</code></pre><p>一个线程修改：</p><pre><code>running = false;</code></pre><p>另一个线程能及时看到。</p><p>注意：</p><ul><li><code>volatile</code> 保证可见性。</li><li>不保证复合操作原子性，比如 <code>count++</code>。</li></ul><ol><li><strong>Lock / Condition</strong></li></ol><p>比 <code>wait/notify</code> 更灵活。</p><pre><code>Condition notEmpty = lock.newCondition();<br/>Condition notFull = lock.newCondition();</code></pre><p>可以创建多个条件队列。</p><p>适合复杂的生产者消费者模型。</p><ol><li><strong>并发工具类</strong></li></ol><p>常见有：</p><ul><li><code>CountDownLatch</code><ul><li>一个线程等待多个线程完成。</li></ul></li><li><code>CyclicBarrier</code><ul><li>多个线程互相等待，到齐后继续。</li></ul></li><li><code>Semaphore</code><ul><li>控制并发访问数量。</li></ul></li><li><code>Exchanger</code><ul><li>两个线程交换数据。</li></ul></li></ul><ol><li><strong>ThreadLocal 不算通信</strong></li></ol><p><code>ThreadLocal</code> 是线程隔离。</p><p>每个线程拿到自己的变量副本。</p><p>它不是用来线程间通信的。</p><p><strong>面试背诵版</strong></p><blockquote><p>线程间通信主要依赖共享内存和同步机制。常见方式有共享变量配合 volatile、synchronized 或 Lock；wait/notify 用于线程等待和唤醒；BlockingQueue 常用于生产者消费者模型；Lock 配合 Condition 可以实现更灵活的条件通知；另外还有 CountDownLatch、CyclicBarrier、Semaphore、Exchanger 等并发工具类。实际开发中，简单状态通知可以用 volatile，生产者消费者优先用 BlockingQueue，复杂条件等待可以用 Condition。ThreadLocal 是线程隔离，不是线程通信。</p></blockquote>"},{"t":"线程间通信方式有哪些？","tag":"","p":"","html":"<p>线程间通信方式可以这样答：</p><ol><li><strong>共享变量</strong></li></ol><ul><li>线程共享同一个对象或变量。</li><li>需要配合同步机制保证安全。</li><li>比如 <code>volatile</code>、锁、原子类。</li></ul><pre><code>volatile boolean flag = false;</code></pre><p><strong>2. </strong><strong><code>wait / notify / notifyAll</code></strong></p><ul><li>基于对象监视器。</li><li>用于线程等待和唤醒。</li><li>必须在 <code>synchronized</code> 中使用。</li></ul><pre><code>synchronized (lock) {<br/>    while (!flag) {<br/>        lock.wait();<br/>    }<br/>}</code></pre><p><strong>3. </strong><strong><code>Lock / Condition</code></strong></p><span>[原文档此处有配图]</span><ul><li><code>Condition</code> 类似增强版 <code>wait/notify</code>。</li><li>可以有多个条件队列。</li><li>更适合复杂等待通知场景。</li></ul><pre><code>condition.await();<br/>condition.signal();</code></pre><ol><li><strong>阻塞队列 </strong><strong><code>BlockingQueue</code></strong></li></ol><ul><li>生产者消费者模型最常用。</li><li>队列为空时消费者阻塞。</li><li>队列满时生产者阻塞。</li></ul><pre><code>queue.put(data);<br/>queue.take();</code></pre><ol><li><strong>并发工具类</strong></li></ol><p>常见有：</p><ul><li><code>CountDownLatch</code>：等待多个线程完成。</li><li><code>CyclicBarrier</code>：多个线程互相等待。</li><li><code>Semaphore</code>：控制并发资源数量。</li><li><code>Exchanger</code>：两个线程交换数据。</li></ul><p><strong>6. </strong><strong><code>ThreadLocal</code></strong><strong> 注意</strong></p><ul><li><code>ThreadLocal</code> 是线程隔离，不是线程通信。</li><li>它让每个线程拥有自己的变量副本。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 线程间通信主要有共享变量、wait/notify、Lock/Condition、BlockingQueue 和并发工具类。共享变量需要配合 volatile、锁或原子类保证可见性和线程安全；wait/notify 用于线程等待和唤醒；Condition 提供更灵活的多个条件队列；BlockingQueue 常用于生产者消费者模型；CountDownLatch、CyclicBarrier、Semaphore、Exchanger 适合不同线程协作场景。ThreadLocal 是线程隔离，不属于线程通信。</p></blockquote>"},{"t":"如何停止一个线程？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>Java 里停止线程，推荐 <strong>优雅停止</strong>，不要强制杀死。</p><ol><li><strong>使用 </strong><strong><code>interrupt()</code></strong></li></ol><p>最推荐。</p><pre><code>Thread thread = new Thread(() -&gt; {<br/>    while (!Thread.currentThread().isInterrupted()) {<br/>        // 执行业务逻辑<br/>    }<br/>});<br/><br/>thread.start();<br/><br/>// 请求停止<br/>thread.interrupt();</code></pre><p><code>interrupt()</code> 不会直接杀死线程，只是设置中断标记。线程需要自己检查标记并退出。</p><ol><li><strong>阻塞状态下处理中断</strong></li></ol><p>如果线程在 <code>sleep()</code>、<code>wait()</code>、<code>join()</code>、<code>BlockingQueue.take()</code> 等方法中阻塞，调用 <code>interrupt()</code> 会让这些方法抛出 <code>InterruptedException</code>。</p><pre><code>Thread thread = new Thread(() -&gt; {<br/>    try {<br/>        while (!Thread.currentThread().isInterrupted()) {<br/>            Thread.sleep(1000);<br/>        }<br/>    } catch (InterruptedException e) {<br/>        Thread.currentThread().interrupt(); // 恢复中断标记<br/>    }<br/>});</code></pre><ol><li><strong>使用 </strong><strong><code>volatile</code></strong><strong> 标志位</strong></li></ol><p>适合普通循环任务：</p><pre><code>class Worker implements Runnable {<br/>    private volatile boolean running = true;<br/><br/>    public void stop() {<br/>        running = false;<br/>    }<br/><br/>    public void run() {<br/>        while (running) {<br/>            // 执行业务逻辑<br/>        }<br/>    }<br/>}</code></pre><p>但如果线程阻塞在 IO、sleep、队列等待里，单靠标志位可能不能及时停止。</p><ol><li><strong>线程池停止</strong></li></ol><pre><code>executor.shutdown();</code></pre><ul><li>不再接收新任务。</li><li>等待已有任务执行完。</li></ul><pre><code>executor.shutdownNow();</code></pre><ul><li>尝试中断正在执行的任务。</li><li>返回队列中未执行的任务。</li></ul><ol><li><strong>不推荐 </strong><strong><code>stop()</code></strong></li></ol><p><code>Thread.stop()</code> 已废弃，不推荐。</p><p>原因：</p><ul><li>会强制终止线程。</li><li>可能导致锁突然释放。</li><li>共享数据处于不一致状态。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 停止线程推荐使用优雅停止。常用方式是调用 <code>interrupt()</code> 设置中断标记，线程在运行中通过 <code>isInterrupted()</code> 检查后主动退出；如果线程阻塞在 <code>sleep</code>、<code>wait</code>、<code>join</code> 或 <code>BlockingQueue.take</code>，会抛出 <code>InterruptedException</code>，可以在 catch 中退出或恢复中断标记。也可以用 <code>volatile</code> 标志位控制循环结束，但对阻塞场景不够及时。线程池中用 <code>shutdown</code> 或 <code>shutdownNow</code>。不推荐使用 <code>stop()</code>，因为它会强制终止线程，可能破坏数据一致性。</p></blockquote><p></p><blockquote><p></p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"并发安全","questions":[{"t":"juc包下你常用的类？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>面试可以按类别说，不用全背：</p><ol><li><strong>原子类</strong></li></ol><p>用于无锁原子操作。</p><p>常用：</p><ul><li><code>AtomicInteger</code></li><li><code>AtomicLong</code></li><li><code>AtomicReference</code></li><li><code>LongAdder</code></li></ul><p>比如计数器场景：</p><pre><code>AtomicInteger count = new AtomicInteger();<br/>count.incrementAndGet();</code></pre><ol><li><strong>锁相关</strong></li></ol><p>比 <code>synchronized</code> 更灵活。</p><p>常用：</p><ul><li><code>ReentrantLock</code></li><li><code>ReadWriteLock</code></li><li><code>ReentrantReadWriteLock</code></li><li><code>StampedLock</code></li><li><code>Condition</code></li></ul><p>比如读多写少可以用读写锁。</p><ol><li><strong>线程池相关</strong></li></ol><p>实际开发最常用。</p><p>常用：</p><ul><li><code>ExecutorService</code></li><li><code>ThreadPoolExecutor</code></li><li><code>ScheduledThreadPoolExecutor</code></li><li><code>Future</code></li><li><code>Callable</code></li><li><code>CompletableFuture</code></li></ul><p>比如异步任务、定时任务、并行处理。</p><ol><li><strong>并发集合</strong></li></ol><p>线程安全集合。</p><p>常用：</p><ul><li><code>ConcurrentHashMap</code></li><li><code>CopyOnWriteArrayList</code></li><li><code>ConcurrentLinkedQueue</code></li><li><code>BlockingQueue</code></li><li><code>ArrayBlockingQueue</code></li><li><code>LinkedBlockingQueue</code></li></ul><p>比如缓存、任务队列、生产者消费者。</p><ol><li><strong>同步工具类</strong></li></ol><p>用于线程协作。</p><p>常用：</p><ul><li><code>CountDownLatch</code></li><li><code>CyclicBarrier</code></li><li><code>Semaphore</code></li><li><code>Exchanger</code></li></ul><p>场景：</p><ul><li><code>CountDownLatch</code>：主线程等待多个子任务完成。</li><li><code>CyclicBarrier</code>：多个线程互相等待。</li><li><code>Semaphore</code>：限制并发数量。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>JUC 包里我常用的类主要分几类：原子类，比如 AtomicInteger、LongAdder，用于无锁计数；锁相关，比如 ReentrantLock、ReadWriteLock、Condition；线程池相关，比如 ThreadPoolExecutor、ExecutorService、Future、CompletableFuture；并发集合，比如 ConcurrentHashMap、BlockingQueue、CopyOnWriteArrayList；还有同步工具类，比如 CountDownLatch、CyclicBarrier、Semaphore。实际项目中用得最多的是线程池、ConcurrentHashMap、BlockingQueue、CountDownLatch 和原子类。</p></blockquote>"},{"t":"怎么保证多线程安全？","tag":"","p":"","html":"<p>保证多线程安全，核心是处理好 <strong>原子性、可见性、有序性</strong>。</p><ol><li><strong>加锁保证临界区安全</strong></li></ol><p>多个线程修改共享资源时，可以用锁保证同一时间只有一个线程执行。</p><p>常见方式：</p><pre><code>synchronized<br/>ReentrantLock</code></pre><p>适合：</p><ul><li>多步复合操作。</li><li>修改共享对象。</li><li>对一致性要求高的场景。</li></ul><ol><li><strong>使用 </strong><strong><code>volatile</code></strong><strong> 保证可见性</strong></li></ol><p>如果只是一个线程修改状态，其他线程读取状态，可以用 <code>volatile</code>。</p><pre><code>private volatile boolean running = true;</code></pre><p>注意：</p><ul><li><code>volatile</code> 保证可见性和一定有序性。</li><li>不保证复合操作原子性。</li><li>比如 <code>count++</code> 不能只靠 <code>volatile</code>。</li></ul><ol><li><strong>使用原子类</strong></li></ol><p>对于计数、CAS 更新，可以用：</p><pre><code>AtomicInteger<br/>AtomicLong<br/>AtomicReference<br/>LongAdder</code></pre><p>例如：</p><pre><code>AtomicInteger count = new AtomicInteger();<br/>count.incrementAndGet();</code></pre><p>适合简单原子操作，减少锁竞争。</p><ol><li><strong>使用线程安全集合</strong></li></ol><p>不要在多线程下直接操作普通集合。</p><p>可以用：</p><pre><code>ConcurrentHashMap<br/>CopyOnWriteArrayList<br/>BlockingQueue<br/>ConcurrentLinkedQueue</code></pre><p>比如共享 Map 优先考虑 <code>ConcurrentHashMap</code>。</p><ol><li><strong>减少共享状态</strong></li></ol><p>最好的线程安全方式是少共享。</p><p>可以：</p><ul><li>使用局部变量。</li><li>使用不可变对象。</li><li>使用 <code>final</code>。</li><li>使用 <code>ThreadLocal</code> 做线程隔离。</li><li>拆分任务，避免多个线程修改同一份数据。</li></ul><ol><li><strong>使用并发工具类</strong></li></ol><p>比如：</p><ul><li><code>CountDownLatch</code></li><li><code>Semaphore</code></li><li><code>CyclicBarrier</code></li><li><code>BlockingQueue</code></li></ul><p>用于线程协作，避免自己手写复杂同步逻辑。</p><ol><li><strong>控制线程池</strong></li></ol><p>线程池也要合理配置：</p><ul><li>不要无限创建线程。</li><li>队列不要无限堆积。</li><li>处理好拒绝策略。</li><li>任务异常要捕获。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>保证多线程安全主要从原子性、可见性和有序性入手。对于共享资源的复合操作，可以用 synchronized 或 ReentrantLock 加锁；简单状态标记可以用 volatile 保证可见性；计数和 CAS 更新可以用 AtomicInteger、AtomicLong 等原子类；共享集合要使用 ConcurrentHashMap、BlockingQueue 等线程安全集合。另外要尽量减少共享状态，比如使用局部变量、不可变对象或 ThreadLocal。实际开发中还会配合线程池和 CountDownLatch、Semaphore 等并发工具类来控制并发和线程协作。</p></blockquote><p></p><blockquote><p></p></blockquote>"},{"t":"Java中有哪些常用的锁，在什么场景下使用？","tag":"","p":"","html":"<p>面试可以按<strong>锁类型 + 使用场景</strong>回答：</p><p><strong>1. </strong><strong><code>synchronized</code></strong></p><ul><li>Java 内置锁。</li><li>使用简单，自动加锁和释放锁。</li><li>适合普通同步场景。</li></ul><pre><code>synchronized (lock) {<br/>    // 临界区<br/>}</code></pre><p>适合：</p><ul><li>代码块或方法同步。</li><li>临界区较小。</li><li>不需要复杂锁功能。</li></ul><p><strong>2. </strong><strong><code>ReentrantLock</code></strong></p><ul><li>可重入锁。</li><li>功能比 <code>synchronized</code> 更灵活。</li><li>需要手动释放锁。</li></ul><pre><code>lock.lock();<br/>try {<br/>    // 临界区<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>适合：</p><ul><li>需要 <code>tryLock</code></li><li>需要可中断加锁</li><li>需要公平锁</li><li>需要多个 <code>Condition</code></li></ul><p><strong>3. </strong><strong><code>ReadWriteLock</code></strong></p><ul><li>读写锁。</li><li>读锁共享，写锁互斥。</li><li>适合读多写少场景。</li></ul><pre><code>ReentrantReadWriteLock</code></pre><p>适合：</p><ul><li>本地缓存。</li><li>配置信息。</li><li>字典数据。</li><li>读远多于写的共享数据。</li></ul><p><strong>4. </strong><strong><code>StampedLock</code></strong></p><ul><li>支持乐观读、悲观读、写锁。</li><li>性能在读多写少场景可能更好。</li><li>但使用更复杂，不可重入。</li></ul><p>适合：</p><ul><li>读多写少。</li><li>对性能要求高。</li><li>可以接受更复杂的锁控制。</li></ul><ol><li><strong>乐观锁</strong></li></ol><ul><li>不是真正的 Java 锁，而是一种思想。</li><li>常用 CAS 或版本号实现。</li></ul><p>Java 中：</p><pre><code>AtomicInteger<br/>AtomicReference</code></pre><p>数据库中：</p><pre><code>UPDATE table<br/>SET version = version + 1<br/>WHERE id = ?<br/>  AND version = ?</code></pre><p>适合：</p><ul><li>读多写少。</li><li>冲突不激烈。</li><li>不希望线程阻塞。</li></ul><ol><li><strong>偏向锁 / 轻量级锁 / 重量级锁</strong></li></ol><p>这是 <code>synchronized</code> 在 JVM 层面的锁优化概念。</p><ul><li>偏向锁：适合基本没有竞争的场景。</li><li>轻量级锁：适合短时间竞争。</li><li>重量级锁：竞争激烈时，线程阻塞挂起。</li></ul><p>面试可以提一句：</p><blockquote><p>这些是 JVM 对 synchronized 的优化，不是我们直接手动使用的锁。</p></blockquote><ol><li><strong>分布式锁</strong></li></ol><p>跨 JVM、跨机器控制并发。</p><p>常见实现：</p><ul><li>Redis 分布式锁</li><li>ZooKeeper 分布式锁</li><li>MySQL 唯一索引</li></ul><p>适合：</p><ul><li>分布式定时任务。</li><li>防重复提交。</li><li>秒杀库存。</li><li>多实例资源互斥。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 中常用锁有 synchronized、ReentrantLock、ReadWriteLock、StampedLock，以及基于 CAS 的乐观锁。synchronized 适合简单同步场景，使用方便；ReentrantLock 更灵活，支持 tryLock、可中断锁、公平锁和多个 Condition；ReadWriteLock 适合读多写少场景，比如缓存；StampedLock 支持乐观读，适合高性能读多写少场景，但使用复杂且不可重入；AtomicInteger 等原子类属于乐观锁思想，适合冲突少的简单原子更新。分布式场景下还会用 Redis、ZooKeeper 或 MySQL 实现分布式锁。</p></blockquote>"},{"t":"怎么在实践中用锁的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>实践中用锁，我一般会按<strong>锁的范围、锁的类型、锁的释放和异常处理</strong>来考虑。</p><ol><li><strong>先判断是否真的需要锁</strong></li></ol><ul><li>如果没有共享可变数据，就不需要锁。</li><li>优先用局部变量、不可变对象、无状态设计。</li><li>能用线程安全工具类，就少自己手写锁。</li></ul><p>比如：</p><ul><li>计数器用 <code>AtomicInteger</code> / <code>LongAdder</code></li><li>生产消费用 <code>BlockingQueue</code></li><li>共享 Map 用 <code>ConcurrentHashMap</code></li></ul><ol><li><strong>简单同步用 </strong><strong><code>synchronized</code></strong></li></ol><p>适合临界区简单、锁逻辑不复杂的场景。</p><pre><code>private final Object lock = new Object();<br/><br/>public void add() {<br/>    synchronized (lock) {<br/>        count++;<br/>    }<br/>}</code></pre><p>特点：</p><ul><li>简单。</li><li>自动释放锁。</li><li>不容易忘记 unlock。</li></ul><ol><li><strong>复杂场景用 </strong><strong><code>ReentrantLock</code></strong></li></ol><p>如果需要：</p><ul><li>尝试获取锁 <code>tryLock</code></li><li>超时获取锁</li><li>可中断等待</li><li>多个条件队列 <code>Condition</code></li><li>公平锁</li></ul><p>就用 <code>ReentrantLock</code>。</p><pre><code>lock.lock();<br/>try {<br/>    // 修改共享资源<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>注意必须在 <code>finally</code> 里释放锁。</p><ol><li><strong>读多写少用读写锁</strong></li></ol><p>比如本地缓存、配置中心：</p><pre><code>rwLock.readLock().lock();<br/>try {<br/>    // 读缓存<br/>} finally {<br/>    rwLock.readLock().unlock();<br/>}</code></pre><p>写入时用写锁：</p><pre><code>rwLock.writeLock().lock();<br/>try {<br/>    // 更新缓存<br/>} finally {<br/>    rwLock.writeLock().unlock();<br/>}</code></pre><p>多个读可以并发，写时互斥。</p><ol><li><strong>高并发简单更新用乐观锁</strong></li></ol><p>比如计数、状态更新：</p><pre><code>AtomicInteger count = new AtomicInteger();<br/>count.incrementAndGet();</code></pre><p>数据库更新可以用版本号：</p><pre><code>UPDATE product<br/>SET stock = stock - 1,<br/>    version = version + 1<br/>WHERE id = ?<br/>  AND version = ?;</code></pre><p>适合冲突不高的场景。</p><ol><li><strong>分布式场景用分布式锁</strong></li></ol><p>如果多个服务实例都可能操作同一资源，JVM 本地锁不够。</p><p>可以用：</p><ul><li>Redis 分布式锁</li><li>ZooKeeper 锁</li><li>MySQL 唯一索引</li></ul><p>比如防止多个实例同时执行同一个定时任务。</p><ol><li><strong>用锁的注意事项</strong></li></ol><ul><li>锁粒度尽量小。</li><li>锁内不要做耗时操作，比如 RPC、IO、数据库慢查询。</li><li>多把锁要固定加锁顺序，避免死锁。</li><li>不要锁字符串常量、公共对象。</li><li><code>ReentrantLock</code> 必须 <code>finally</code> 释放。</li><li>能用并发容器就不用手写锁。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>实践中我会先判断是否真的需要锁，能用无状态、不可变对象、线程安全集合或原子类就优先用这些。简单临界区用 synchronized，代码简单且自动释放锁；需要 tryLock、超时、公平锁或多个 Condition 时用 ReentrantLock；读多写少的缓存场景用 ReadWriteLock；简单计数或冲突较少的更新用 Atomic 类或版本号乐观锁；跨 JVM 的并发控制用 Redis、ZooKeeper 或 MySQL 实现分布式锁。使用锁时要控制锁粒度，避免锁内做耗时操作，多把锁固定顺序，并确保 finally 释放锁。</p></blockquote>"},{"t":"Java 并发工具你知道哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p></p><blockquote><p>Java 并发工具可以按类别回答：</p><ol><li><strong>线程池相关</strong></li></ol><ul><li><code>ExecutorService</code></li><li><code>ThreadPoolExecutor</code></li><li><code>ScheduledThreadPoolExecutor</code></li><li><code>Future</code></li><li><code>Callable</code></li><li><code>CompletableFuture</code></li></ul><p>用途：</p><ul><li>复用线程。</li><li>控制并发数量。</li><li>执行异步任务、定时任务。</li></ul><ol><li><strong>原子类</strong></li></ol><ul><li><code>AtomicInteger</code></li><li><code>AtomicLong</code></li><li><code>AtomicReference</code></li><li><code>LongAdder</code></li></ul><p>用途：</p><ul><li>无锁计数。</li><li>CAS 原子更新。</li><li>高并发统计。</li></ul><ol><li><strong>锁工具</strong></li></ol><ul><li><code>ReentrantLock</code></li><li><code>ReentrantReadWriteLock</code></li><li><code>StampedLock</code></li><li><code>Condition</code></li></ul><p>用途：</p><ul><li>控制共享资源访问。</li><li>实现复杂等待通知。</li><li>读多写少场景优化。</li></ul><ol><li><strong>并发集合</strong></li></ol><ul><li><code>ConcurrentHashMap</code></li><li><code>CopyOnWriteArrayList</code></li><li><code>ConcurrentLinkedQueue</code></li><li><code>BlockingQueue</code></li><li><code>ArrayBlockingQueue</code></li><li><code>LinkedBlockingQueue</code></li><li><code>PriorityBlockingQueue</code></li></ul><p>用途：</p><ul><li>线程安全的数据存储。</li><li>任务队列。</li><li>生产者消费者模型。</li></ul><ol><li><strong>线程协作工具</strong></li></ol><ul><li><code>CountDownLatch</code></li><li><code>CyclicBarrier</code></li><li><code>Semaphore</code></li><li><code>Exchanger</code></li><li><code>Phaser</code></li></ul><p>用途：</p><ul><li><code>CountDownLatch</code>：等待多个任务完成。</li><li><code>CyclicBarrier</code>：多个线程互相等待。</li><li><code>Semaphore</code>：限制并发数量。</li><li><code>Exchanger</code>：两个线程交换数据。</li><li><code>Phaser</code>：更灵活的阶段同步。</li></ul><ol><li><strong>其他常用</strong></li></ol><ul><li><code>ThreadLocal</code><ul><li>线程隔离变量。</li><li>常用于保存用户上下文、traceId。</li></ul></li><li><code>LockSupport</code><ul><li>线程阻塞和唤醒。</li><li>很多并发工具底层会用到。</li></ul></li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 并发工具主要在 JUC 包下，可以分为几类：线程池相关，比如 ThreadPoolExecutor、ExecutorService、Future、CompletableFuture；原子类，比如 AtomicInteger、AtomicLong、LongAdder；锁工具，比如 ReentrantLock、ReadWriteLock、StampedLock、Condition；并发集合，比如 ConcurrentHashMap、BlockingQueue、CopyOnWriteArrayList；线程协作工具，比如 CountDownLatch、CyclicBarrier、Semaphore、Exchanger、Phaser。实际项目里用得比较多的是线程池、ConcurrentHashMap、BlockingQueue、CountDownLatch、Semaphore 和原子类。</p></blockquote></blockquote>"},{"t":"CountDownLatch 是做什么的讲一讲？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>CountDownLatch</code> 是一个<strong>线程等待工具</strong>，作用是：<strong>让一个或多个线程等待其他线程完成后再继续执行。</strong></p><ol><li><strong>核心理解</strong></li></ol><p>它里面有一个计数器。</p><ul><li>初始化时设置一个数量。</li><li>每完成一个任务，就调用一次 <code>countDown()</code>，计数减一。</li><li>等计数变成 <code>0</code>，等待的线程就会被唤醒。</li></ul><ol><li><strong>常见场景</strong></li></ol><p>比如主线程要等 3 个子任务都完成后，再汇总结果：</p><pre><code>子任务 A 完成 → countDown<br/>子任务 B 完成 → countDown<br/>子任务 C 完成 → countDown<br/>计数变成 0 → 主线程继续执行</code></pre><ol><li><strong>示例代码</strong></li></ol><pre><code>CountDownLatch latch = new CountDownLatch(3);<br/><br/>for (int i = 0; i &lt; 3; i++) {<br/>    new Thread(() -&gt; {<br/>        try {<br/>            System.out.println(\"子任务执行\");<br/>        } finally {<br/>            latch.countDown();<br/>        }<br/>    }).start();<br/>}<br/><br/>latch.await();<br/><br/>System.out.println(\"所有子任务完成，主线程继续\");</code></pre><ol><li><strong>重要方法</strong></li></ol><ul><li><code>await()</code><ul><li>当前线程等待，直到计数器变成 <code>0</code>。</li></ul></li><li><code>countDown()</code><ul><li>计数器减一。</li></ul></li><li><code>await(timeout, unit)</code><ul><li>最多等待指定时间，避免一直阻塞。</li></ul></li></ul><ol><li><strong>注意点</strong></li></ol><ul><li><code>CountDownLatch</code> 是一次性的。</li><li>计数减到 <code>0</code> 后不能重置。</li><li>如果需要重复使用，可以考虑 <code>CyclicBarrier</code>。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>CountDownLatch 是 JUC 中的线程同步工具，用来让一个或多个线程等待其他线程完成。它内部有一个计数器，初始化时指定数量，每个任务完成后调用 countDown 让计数减一，等待线程调用 await 阻塞，直到计数变为 0 后继续执行。常见场景是主线程等待多个子任务执行完再汇总结果。它是一次性的，计数归零后不能复用，如果需要循环复用可以考虑 CyclicBarrier。</p></blockquote>"},{"t":"synchronized和reentrantlock及其应用场景？","tag":"","p":"","html":"<p>面试可以这样答：<strong>两者都能实现互斥，但 </strong><strong><code>synchronized</code></strong><strong> 更简单，</strong><strong><code>ReentrantLock</code></strong><strong> 更灵活。</strong></p><p><strong>1. </strong><strong><code>synchronized</code></strong></p><p><code> synchronized</code> 是 Java 内置锁，由 JVM 管理。</p><p>用法：</p><pre><code>synchronized (lock) {<br/>    // 临界区<br/>}</code></pre><p>特点：</p><ul><li>使用简单。</li><li>自动加锁和释放锁。</li><li>发生异常也会自动释放。</li><li>支持可重入。</li><li>适合普通同步场景。</li></ul><p>适用场景：</p><ul><li>临界区简单。</li><li>不需要尝试加锁、超时、中断、公平锁。</li><li>希望代码简洁、安全。</li></ul><p>比如：</p><pre><code>public synchronized void add() {<br/>    count++;<br/>}</code></pre><p><strong>2. </strong><strong><code>ReentrantLock</code></strong></p><p><code>ReentrantLock</code> 是 JUC 里的显示锁，需要手动加锁和释放锁。</p><p>用法：</p><pre><code>lock.lock();<br/>try {<br/>    // 临界区<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>特点：</p><ul><li>支持可重入。</li><li>可以设置公平锁 / 非公平锁。</li><li>支持 <code>tryLock()</code> 尝试获取锁。</li><li>支持超时获取锁。</li><li>支持可中断获取锁 <code>lockInterruptibly()</code>。</li><li>支持多个条件队列 <code>Condition</code>。</li><li>需要手动释放锁，必须写在 <code>finally</code> 里。</li></ul><p>适用场景：</p><ul><li>需要尝试获取锁，获取不到就放弃。</li><li>需要超时等待锁。</li><li>需要响应中断。</li><li>需要公平锁。</li><li>需要多个条件等待队列。</li><li>复杂线程协作场景。</li></ul><ol><li><strong>对比</strong></li></ol><sheet></sheet><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>synchronized 和 ReentrantLock 都是可重入的互斥锁。synchronized 是 JVM 内置锁，使用简单，进入同步块自动加锁，退出时自动释放锁，适合大多数简单同步场景。ReentrantLock 是 JUC 提供的显式锁，需要手动 lock 和 unlock，必须在 finally 中释放，但功能更灵活，支持 tryLock、超时获取锁、可中断锁、公平锁以及多个 Condition，所以适合复杂并发控制场景。一般简单同步优先用 synchronized，需要高级功能时用 ReentrantLock。</p></blockquote>"},{"t":"除了用synchronized，还有什么方法可以实现线程同步？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>除了 <code>synchronized</code>，Java 里实现线程同步还有这些方式：</p><p><strong>1. </strong><strong><code>ReentrantLock</code></strong></p><p>显式锁，功能比 <code>synchronized</code> 更灵活。</p><pre><code>lock.lock();<br/>try {<br/>    // 临界区<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>适合：</p><ul><li>需要 <code>tryLock</code></li><li>超时获取锁</li><li>可中断锁</li><li>公平锁</li><li>多个 <code>Condition</code></li></ul><p><strong>2. </strong><strong><code>volatile</code></strong></p><p>保证变量的可见性和一定的有序性。</p><pre><code>private volatile boolean running = true;</code></pre><p>适合：</p><ul><li>状态标记。</li><li>一个线程写，多个线程读。</li></ul><p>不适合：</p><ul><li><code>count++</code> 这种复合操作。</li><li>因为它不保证原子性。</li></ul><ol><li><strong>原子类</strong></li></ol><p>基于 CAS 实现无锁同步。</p><p>常见：</p><pre><code>AtomicInteger<br/>AtomicLong<br/>AtomicReference<br/>LongAdder</code></pre><p>适合：</p><ul><li>计数器。</li><li>简单原子更新。</li><li>高并发统计。</li></ul><p><strong>4. </strong><strong><code>ReadWriteLock</code></strong></p><p>读写锁。</p><pre><code>ReentrantReadWriteLock</code></pre><p>特点：</p><ul><li>多个读线程可以并发。</li><li>写线程独占。</li><li>适合读多写少场景，比如缓存。</li></ul><p><strong>5. </strong><strong><code>Condition</code></strong></p><p>配合 <code>ReentrantLock</code> 使用。</p><p>类似增强版 <code>wait/notify</code>。</p><p>适合：</p><ul><li>多个等待条件。</li><li>复杂生产者消费者模型。</li></ul><pre><code>condition.await();<br/>condition.signal();</code></pre><ol><li><strong>并发容器</strong></li></ol><p>使用线程安全集合，避免自己加锁。</p><p>常见：</p><pre><code>ConcurrentHashMap<br/>BlockingQueue<br/>CopyOnWriteArrayList<br/>ConcurrentLinkedQueue</code></pre><p>比如生产者消费者可以直接用 <code>BlockingQueue</code>。</p><ol><li><strong>并发工具类</strong></li></ol><p>常见：</p><ul><li><code>CountDownLatch</code></li><li><code>CyclicBarrier</code></li><li><code>Semaphore</code></li><li><code>Exchanger</code></li></ul><p>用于线程协作和控制并发。</p><p><strong>面试背诵版</strong></p><blockquote><p>除了 synchronized，还可以用 ReentrantLock 实现显式加锁，它支持 tryLock、超时、公平锁和 Condition；可以用 volatile 保证变量可见性，适合状态标记；可以用 AtomicInteger、AtomicLong 等原子类通过 CAS 实现无锁同步；读多写少场景可以用 ReadWriteLock；复杂等待通知可以用 Lock 配合 Condition；生产者消费者可以用 BlockingQueue；另外 CountDownLatch、CyclicBarrier、Semaphore 等并发工具类也可以实现线程协作。实际开发中会根据场景选择，而不是所有地方都手写锁。</p></blockquote>"},{"t":"synchronized锁静态方法和普通方法区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>核心区别：锁对象不同。</strong></p><p><strong>1. </strong><strong><code>synchronized</code></strong><strong> 普通方法</strong></p><p>锁的是<strong>当前实例对象</strong>，也就是：</p><pre><code>this</code></pre><p>例如：</p><pre><code>public synchronized void method() {<br/>    // ...<br/>}</code></pre><p>等价于：</p><pre><code>public void method() {<br/>    synchronized (this) {<br/>        // ...<br/>    }<br/>}</code></pre><p>所以：</p><ul><li>同一个对象调用同步普通方法，会互斥。</li><li>不同对象调用同步普通方法，不互斥。</li></ul><p><strong>2. </strong><strong><code>synchronized</code></strong><strong> 静态方法</strong></p><p>锁的是<strong>类对象</strong>，也就是：</p><pre><code>Class 对象</code></pre><p>例如：</p><pre><code>public static synchronized void staticMethod() {<br/>    // ...<br/>}</code></pre><p>等价于：</p><pre><code>public static void staticMethod() {<br/>    synchronized (当前类.class) {<br/>        // ...<br/>    }<br/>}</code></pre><p>所以：</p><ul><li>同一个类的静态同步方法之间会互斥。</li><li>即使是不同实例，只要调用的是同一个类的静态同步方法，也会互斥。</li></ul><ol><li><strong>两者会不会互斥</strong></li></ol><p>普通同步方法锁的是 <code>this</code>。</p><p>静态同步方法锁的是 <code>Class</code> 对象。</p><p>它们锁对象不同，所以<strong>默认不互斥</strong>。</p><p>例如：</p><pre><code>class UserService {<br/>    public synchronized void normalMethod() {}<br/><br/>    public static synchronized void staticMethod() {}<br/>}</code></pre><p>一个线程执行：</p><pre><code>userService.normalMethod();</code></pre><p>另一个线程执行：</p><pre><code>UserService.staticMethod();</code></pre><p>它们锁的不是同一个对象，所以可以并发执行。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><ul><li>synchronized 修饰普通方法时，锁的是当前实例对象 this，所以同一个对象的同步普通方法会互斥，不同对象之间不互斥。</li><li>synchronized 修饰静态方法时，锁的是当前类的 Class 对象，比如 UserService.class，所以同一个类的静态同步方法即使通过不同实例调用，也会互斥。</li><li>普通同步方法和静态同步方法锁对象不同，一个锁 this，一个锁 Class 对象，所以默认不会互斥。</li></ul></blockquote>"},{"t":"synchronized和reentrantlock区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p></p><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p><strong>核心区别</strong></p><ul><li><code>synchronized</code> 是 <strong>JVM 内置锁</strong>，使用简单，自动释放。</li><li><code>ReentrantLock</code> 是 <strong>JUC 显式锁</strong>，功能更灵活，但需要手动释放。</li></ul><ol><li><strong>使用方式不同</strong></li></ol><p><code>synchronized</code>：</p><pre><code>synchronized (lock) {<br/>    // 临界区<br/>}</code></pre><ul><li>进入代码块自动加锁。</li><li>退出代码块自动释放锁。</li></ul><p><code>ReentrantLock</code>：</p><pre><code>lock.lock();<br/>try {<br/>    // 临界区<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><ul><li>需要手动 <code>lock()</code> 和 <code>unlock()</code>。</li><li>必须在 <code>finally</code> 中释放锁。</li></ul><ol><li><strong>功能不同</strong></li></ol><p><code>ReentrantLock</code> 支持更多高级功能：</p><ul><li><code>tryLock()</code>：尝试获取锁，获取不到可以放弃。</li><li><code>tryLock(timeout)</code>：超时等待锁。</li><li><code>lockInterruptibly()</code>：等待锁时可以响应中断。</li><li>公平锁 / 非公平锁。</li><li>多个 <code>Condition</code> 条件队列。</li></ul><p><code>synchronized</code> 不支持这些灵活控制。</p><ol><li><strong>是否可重入</strong></li></ol><p>两者都是可重入锁。</p><p>也就是说，同一个线程拿到锁后，可以再次进入同一把锁保护的代码。</p><ol><li><strong>公平性不同</strong></li></ol><ul><li><code>synchronized</code> 不能指定公平锁。</li><li><code>ReentrantLock</code> 可以指定公平或非公平。</li></ul><pre><code>new ReentrantLock(true);  // 公平锁<br/>new ReentrantLock(false); // 非公平锁</code></pre><ol><li><strong>等待通知机制不同</strong></li></ol><p><code>synchronized</code> 配合：</p><pre><code>wait()<br/>notify()<br/>notifyAll()</code></pre><p><code>ReentrantLock</code> 配合：</p><pre><code>Condition.await()<br/>Condition.signal()<br/>Condition.signalAll()</code></pre><p><code>Condition</code> 可以有多个条件队列，更灵活。</p><ol><li><strong>性能区别</strong></li></ol><p>早期 <code>ReentrantLock</code> 性能更好。</p><p>但现在 JVM 对 <code>synchronized</code> 做了很多优化，比如：</p><ul><li>偏向锁</li><li>轻量级锁</li><li>锁膨胀</li><li>锁消除</li><li>锁粗化</li></ul><p>所以普通场景下性能差距不大。</p><p><strong>面试背诵版</strong></p><blockquote><p>synchronized 是 JVM 内置锁，使用简单，自动加锁和释放锁，适合普通同步场景；ReentrantLock 是 JUC 提供的显式锁，需要手动 lock 和 unlock，必须在 finally 中释放，但功能更灵活，支持 tryLock、超时获取锁、可中断锁、公平锁以及多个 Condition 条件队列。两者都是可重入锁。现在 JVM 对 synchronized 做了很多优化，所以普通场景优先用 synchronized；如果需要更灵活的锁控制，再使用 ReentrantLock。</p></blockquote>"},{"t":"怎么理解可重入锁？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>可重入锁</strong>就是：<strong>同一个线程已经拿到一把锁之后，可以再次获取这把锁，不会被自己阻塞。</strong></p><ol><li><strong>为什么需要可重入</strong></li></ol><p>假设一个同步方法里调用另一个同步方法：</p><pre><code>public synchronized void methodA() {<br/>    methodB();<br/>}<br/><br/>public synchronized void methodB() {<br/>    // ...<br/>}<br/></code></pre><p>线程进入 <code>methodA()</code> 时，已经拿到了当前对象的锁。</p><p>然后调用 <code>methodB()</code>，<code>methodB()</code> 也需要同一把锁。</p><p>如果锁不可重入，这个线程就会自己等自己释放锁，造成死锁。</p><p>可重入锁允许它再次进入。</p><ol><li><strong>synchronized 是可重入的</strong></li></ol><pre><code>public synchronized void methodA() {<br/>    methodB();<br/>}<br/><br/>public synchronized void methodB() {<br/>    System.out.println(\"methodB\");<br/>}</code></pre><p>同一个线程可以连续进入 <code>methodA</code> 和 <code>methodB</code>。</p><ol><li><strong>ReentrantLock 也是可重入的</strong></li></ol><pre><code>lock.lock();<br/>try {<br/>    lock.lock();<br/>    try {<br/>        // 同一个线程再次获取同一把锁<br/>    } finally {<br/>        lock.unlock();<br/>    }<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>注意：</p><blockquote><p>加锁几次，就要释放几次。</p></blockquote><p>否则锁不会真正释放。</p><ol><li><strong>底层怎么理解</strong></li></ol><p>可重入锁内部会记录：</p><ul><li>当前持有锁的线程。</li><li>重入次数。</li></ul><p>当同一个线程再次加锁：</p><pre><code>重入次数 +1</code></pre><p>每释放一次：</p><pre><code>重入次数 -1</code></pre><p>直到重入次数变成 <code>0</code>，锁才真正释放，其他线程才能获取。</p><p><strong>面试背诵版</strong></p><blockquote><p>可重入锁指的是同一个线程获取到某把锁后，可以再次获取这把锁，不会被自己阻塞。它主要解决同步方法嵌套调用时线程自己等待自己的问题。比如 synchronized 和 ReentrantLock 都是可重入锁。底层会记录当前持锁线程和重入次数，同一个线程重复加锁时计数加一，释放锁时计数减一，直到计数为零才真正释放锁。需要注意 ReentrantLock 加锁几次就必须 unlock 几次。</p></blockquote>"},{"t":"synchronized 支持重入吗？如何实现的?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>支持，<code>synchronized</code> 是<strong>可重入锁</strong>。</p><ol><li><strong>什么是支持重入</strong></li></ol><p>同一个线程拿到某个对象锁后，可以再次进入这把锁保护的代码，不会被自己阻塞。</p><p>比如：</p><pre><code>public synchronized void methodA() {<br/>    methodB();<br/>}<br/><br/>public synchronized void methodB() {<br/>    System.out.println(\"methodB\");<br/>}</code></pre><p>线程进入 <code>methodA()</code> 时已经拿到了 <code>this</code> 锁。</p><p><code>methodA()</code> 里调用 <code>methodB()</code>，<code>methodB()</code> 也需要 <code>this</code> 锁。</p><p>因为 <code>synchronized</code> 支持重入，所以同一个线程可以继续进入 <code>methodB()</code>。</p><ol><li><strong>如果不支持重入会怎样</strong></li></ol><p>如果锁不可重入：</p><pre><code>线程已经持有 this 锁<br/>又想再次获取 this 锁<br/>结果自己等自己释放锁</code></pre><p>就会死锁。</p><ol><li><strong>synchronized 如何实现重入</strong></li></ol><p><code>synchronized</code> 底层依赖对象监视器，也就是 <code>Monitor</code>。</p><p>Monitor 里会记录两个关键信息：</p><ul><li>当前持有锁的线程。</li><li>重入计数器。</li></ul><p>当线程第一次进入 <code>synchronized</code>：</p><pre><code>owner = 当前线程<br/>count = 1</code></pre><p>同一个线程再次进入同一把锁：</p><pre><code>发现 owner 还是当前线程<br/>count + 1</code></pre><p>每退出一次同步代码块：</p><pre><code>count - 1</code></pre><p>当计数器减到 <code>0</code>：</p><pre><code>owner = null<br/>锁真正释放<br/>其他线程可以竞争</code></pre><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>synchronized 支持重入。它的底层对象监视器 Monitor 会记录当前持锁线程和重入次数。当一个线程第一次进入 synchronized 时，会成为锁的 owner，计数加一；如果同一个线程再次进入同一把锁，发现 owner 是自己，就允许进入，并把计数继续加一；每退出一次 synchronized，计数减一，直到计数为 0 才真正释放锁。这样可以避免同步方法之间相互调用时线程自己把自己阻塞。</p></blockquote><span>[原文档此处有配图]</span>"},{"t":"syncronized锁升级的过程讲一下","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>先纠正一下拼写：是 <code>synchronized</code>。</p><p>面试可以这样答：<code>synchronized</code> 锁升级主要是 JVM 为了减少加锁开销做的优化，锁状态大致会经历：</p><pre><code>无锁 → 偏向锁 → 轻量级锁 → 重量级锁</code></pre><p>不过注意：<strong>JDK 15 之后偏向锁已经被废弃并默认关闭</strong>，但面试里仍然常问这个过程。</p><ol><li><strong>无锁状态</strong></li></ol><p>对象刚创建时，如果还没有线程竞争，处于无锁状态。</p><p>对象头里的 <code>Mark Word</code> 会记录对象的锁状态、哈希码、GC 年龄等信息。</p><ol><li><strong>偏向锁</strong></li></ol><p>适合场景：</p><blockquote><p>只有一个线程反复进入同步块，没有竞争。</p></blockquote><p>当线程第一次获取锁时，JVM 会把当前线程 ID 记录到对象头 <code>Mark Word</code> 中。</p><p>之后这个线程再次进入同步块：</p><ul><li>不需要 CAS。</li><li>只要判断对象头里记录的是自己。</li><li>直接进入。</li></ul><p>这样可以减少无竞争场景下的加锁成本。</p><ol><li><strong>轻量级锁</strong></li></ol><p>当另一个线程也来竞争这把锁时，偏向锁会被撤销，升级为轻量级锁。</p><p>轻量级锁适合：</p><blockquote><p>多个线程交替执行同步块，但竞争不激烈，锁持有时间短。</p></blockquote><p>实现大致是：</p><ul><li>线程在自己的栈帧中创建锁记录 <code>Lock Record</code>。</li><li>用 CAS 尝试把对象头的 <code>Mark Word</code> 替换为指向锁记录的指针。</li><li>CAS 成功，获得锁。</li><li>CAS 失败，说明有竞争。</li></ul><p>如果竞争线程短时间内拿不到锁，可能会自旋等待。</p><ol><li><strong>重量级锁</strong></li></ol><p>如果竞争激烈，或者自旋失败，轻量级锁会膨胀为重量级锁。</p><p>重量级锁依赖操作系统互斥量 <code>Monitor</code>。</p><p>特点：</p><ul><li>没拿到锁的线程会被阻塞挂起。</li><li>需要用户态和内核态切换。</li><li>线程阻塞和唤醒成本较高。</li><li>但适合锁持有时间长、竞争激烈的场景，避免一直自旋浪费 CPU。</li></ul><ol><li><strong>锁升级方向</strong></li></ol><p>锁一般是单向升级：</p><pre><code>偏向锁 → 轻量级锁 → 重量级锁</code></pre><p>通常不会降级。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>synchronized 的锁升级是 JVM 为了优化不同竞争场景下的锁开销。对象一开始是无锁状态；如果只有一个线程反复进入同步块，会使用偏向锁，把线程 ID 记录在对象头 Mark Word 中，之后同一线程进入几乎不需要同步操作；如果出现其他线程竞争，偏向锁撤销，升级为轻量级锁，通过 CAS 尝试获取锁，竞争不激烈时可以避免线程阻塞；如果竞争激烈或自旋失败，就升级为重量级锁，底层依赖 Monitor，未获得锁的线程会阻塞挂起，开销更大。锁升级一般是单向的，不能随意降级。</p></blockquote>"},{"t":"JVM对Synchornized的优化？","tag":"","p":"","html":"<p>面试可以从 <strong>锁状态优化 + 编译器优化</strong> 两类讲。</p><p>先纠正拼写：是 <code>synchronized</code>。</p><ol><li><strong>偏向锁</strong></li></ol><ul><li>适合没有竞争的场景。</li><li>如果一把锁总是被同一个线程获取，JVM 会让锁偏向这个线程。</li><li>后续这个线程再次进入同步块时，基本不用 CAS。</li><li>减少无竞争场景下的加锁开销。</li></ul><p>注意：</p><ul><li>JDK 15 之后偏向锁已废弃并默认关闭。</li><li>但面试里仍然经常问。</li></ul><ol><li><strong>轻量级锁</strong></li></ol><ul><li>适合多个线程交替进入同步块，但竞争不激烈的场景。</li><li>通过对象头 <code>Mark Word</code> 和线程栈中的锁记录配合 CAS 获取锁。</li><li>如果 CAS 成功，就不用阻塞线程。</li><li>避免直接使用重量级锁带来的系统调用开销。</li></ul><ol><li><strong>自旋锁 / 自适应自旋</strong></li></ol><ul><li>如果线程没拿到锁，不一定马上阻塞。</li><li>可能先自旋等待一会儿。</li><li>如果锁很快释放，就避免线程挂起和唤醒。</li><li>自适应自旋会根据之前锁竞争情况动态调整自旋次数。</li></ul><p>适合：</p><ul><li>锁持有时间很短。</li><li>多核 CPU 场景。</li></ul><ol><li><strong>锁粗化</strong></li></ol><p>如果 JVM 发现一段代码里频繁对同一个对象反复加锁、解锁，可能会把多个小锁合并成一个大锁。</p><p>例如：</p><pre><code>for (...) {<br/>    synchronized (lock) {<br/>        // 很短逻辑<br/>    }<br/>}</code></pre><p>可能优化成：</p><pre><code>synchronized (lock) {<br/>    for (...) {<br/>        // 很短逻辑<br/>    }<br/>}</code></pre><p>减少频繁加锁解锁开销。</p><ol><li><strong>锁消除</strong></li></ol><p>如果 JVM 通过逃逸分析发现某个对象不会被多个线程共享，那么这个对象上的锁就是多余的，可以直接消除。</p><p>比如局部对象只在当前方法里使用：</p><pre><code>StringBuffer sb = new StringBuffer();<br/>sb.append(\"a\");<br/>sb.append(\"b\");</code></pre><p><code>StringBuffer</code> 方法是同步的，但如果 <code>sb</code> 不会逃逸到其他线程，JVM 可能消除这些锁。</p><ol><li><strong>重量级锁</strong></li></ol><p>当竞争激烈时，锁会膨胀为重量级锁。</p><ul><li>基于对象监视器 <code>Monitor</code>。</li><li>没拿到锁的线程会阻塞。</li><li>需要操作系统参与。</li><li>开销较大，但避免长时间自旋浪费 CPU。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>JVM 对 synchronized 做了很多优化，主要包括偏向锁、轻量级锁、自旋锁、自适应自旋、锁粗化和锁消除。偏向锁优化无竞争场景，让锁偏向第一个获取它的线程；轻量级锁通过 CAS 避免线程阻塞，适合竞争不激烈的场景；自旋锁让线程短时间等待锁释放，减少阻塞和唤醒开销；锁粗化把连续频繁加锁合并，减少加锁次数；锁消除通过逃逸分析去掉不可能被共享对象上的锁。竞争激烈时，锁会膨胀为重量级锁，由 Monitor 实现，线程阻塞等待。</p></blockquote>"},{"t":"介绍一下AQS","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>AQS</code> 全称是 <code>AbstractQueuedSynchronizer</code>，是 JUC 里很多锁和同步工具的底层框架。</p><ol><li><strong>AQS 是什么</strong></li></ol><blockquote><p>AQS 是一个基于 <code>state</code> 状态变量和 FIFO 等待队列实现的同步器框架。</p></blockquote><p>很多并发工具底层都基于 AQS，比如：</p><ul><li><code>ReentrantLock</code></li><li><code>Semaphore</code></li><li><code>CountDownLatch</code></li><li><code>ReentrantReadWriteLock</code></li><li><code>FutureTask</code></li></ul><ol><li><strong>核心组成</strong></li></ol><p>AQS 主要有两个核心：</p><p><strong>state 状态变量</strong></p><pre><code>private volatile int state;</code></pre><p><code>state</code> 表示同步状态。</p><p>不同工具含义不同：</p><ul><li><code>ReentrantLock</code>：<code>state</code> 表示重入次数。</li><li><code>Semaphore</code>：<code>state</code> 表示可用许可证数量。</li><li><code>CountDownLatch</code>：<code>state</code> 表示剩余计数。</li><li><code>ReadWriteLock</code>：<code>state</code> 同时表示读锁和写锁状态。</li></ul><p><strong>FIFO 等待队列</strong></p><ul><li>如果线程获取同步状态失败，就会被封装成一个节点 <code>Node</code>。</li><li>加入 AQS 的 CLH 队列。</li><li>然后线程被挂起。</li><li>当前面线程释放锁后，再唤醒后继节点。</li></ul><ol><li><strong>获取锁大致流程</strong></li></ol><p>以独占锁为例：</p><pre><code>1. 线程尝试通过 CAS 修改 state<br/>2. 成功，获取锁<br/>3. 失败，说明锁被占用<br/>4. 当前线程封装成 Node 加入等待队列<br/>5. 线程 park 挂起<br/>6. 前驱节点释放锁后，唤醒当前线程<br/>7. 当前线程再次尝试获取锁</code></pre><ol><li><strong>释放锁大致流程</strong></li></ol><pre><code>1. 当前线程释放同步状态<br/>2. 修改 state<br/>3. 如果完全释放成功<br/>4. 唤醒等待队列中的后继节点</code></pre><ol><li><strong>独占模式和共享模式</strong></li></ol><p>AQS 支持两种模式：</p><p><strong>独占模式</strong></p><ul><li>同一时刻只有一个线程能获取资源。</li><li>比如：<ul><li><code>ReentrantLock</code></li></ul></li></ul><p><strong>共享模式</strong></p><ul><li>多个线程可以同时获取资源。</li><li>比如：<ul><li><code>Semaphore</code></li><li><code>CountDownLatch</code></li><li>读写锁中的读锁</li></ul></li></ul><ol><li><strong>AQS 用到了什么技术</strong></li></ol><ul><li><code>volatile state</code> 保证可见性。</li><li><code>CAS</code> 保证原子更新。</li><li><code>CLH FIFO</code> 队列管理等待线程。</li><li><code>LockSupport.park/unpark</code> 挂起和唤醒线程。</li><li>模板方法模式，让子类实现获取和释放逻辑。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>AQS 是 AbstractQueuedSynchronizer，是 JUC 中很多锁和同步工具的基础框架。它的核心是一个 volatile 的 state 状态变量和一个 FIFO 的等待队列。线程获取锁时，会先通过 CAS 修改 state，成功就获取同步资源；失败就被封装成 Node 加入 CLH 队列，并通过 LockSupport.park 挂起。释放锁时修改 state，并唤醒队列中的后继线程。AQS 支持独占和共享两种模式，ReentrantLock 是独占模式，Semaphore、CountDownLatch、读写锁的读锁是共享模式。AQS 通过 state、CAS、队列和 park/unpark 实现了通用同步框架。</p></blockquote>"},{"t":"CAS 和 AQS 有什么关系？","tag":"","p":"","html":"<p></p><span>[原文档此处有配图]</span><p><code>AQS</code> 全称是 <code>AbstractQueuedSynchronizer</code>，是 JUC 里很多锁和同步工具的底层框架。</p><ol><li><strong>AQS 是什么</strong></li></ol><blockquote><p>AQS 是一个基于 <code>state</code> 状态变量和 FIFO 等待队列实现的同步器框架。</p></blockquote><p>很多并发工具底层都基于 AQS，比如：</p><ul><li><code>ReentrantLock</code></li><li><code>Semaphore</code></li><li><code>CountDownLatch</code></li><li><code>ReentrantReadWriteLock</code></li><li><code>FutureTask</code></li></ul><ol><li><strong>核心组成</strong></li></ol><p>AQS 主要有两个核心：</p><p><strong>state 状态变量</strong></p><pre><code>private volatile int state;</code></pre><p><code>state</code> 表示同步状态。</p><p>不同工具含义不同：</p><ul><li><code>ReentrantLock</code>：<code>state</code> 表示重入次数。</li><li><code>Semaphore</code>：<code>state</code> 表示可用许可证数量。</li><li><code>CountDownLatch</code>：<code>state</code> 表示剩余计数。</li><li><code>ReadWriteLock</code>：<code>state</code> 同时表示读锁和写锁状态。</li></ul><p><strong>FIFO 等待队列</strong></p><ul><li>如果线程获取同步状态失败，就会被封装成一个节点 <code>Node</code>。</li><li>加入 AQS 的 CLH 队列。</li><li>然后线程被挂起。</li><li>当前面线程释放锁后，再唤醒后继节点。</li></ul><ol><li><strong>获取锁大致流程</strong></li></ol><p>以独占锁为例：</p><pre><code>1. 线程尝试通过 CAS 修改 state<br/>2. 成功，获取锁<br/>3. 失败，说明锁被占用<br/>4. 当前线程封装成 Node 加入等待队列<br/>5. 线程 park 挂起<br/>6. 前驱节点释放锁后，唤醒当前线程<br/>7. 当前线程再次尝试获取锁</code></pre><ol><li><strong>释放锁大致流程</strong></li></ol><pre><code>1. 当前线程释放同步状态<br/>2. 修改 state<br/>3. 如果完全释放成功<br/>4. 唤醒等待队列中的后继节点</code></pre><ol><li><strong>独占模式和共享模式</strong></li></ol><p>AQS 支持两种模式：</p><p><strong>独占模式</strong></p><ul><li>同一时刻只有一个线程能获取资源。</li><li>比如：<ul><li><code>ReentrantLock</code></li></ul></li></ul><p><strong>共享模式</strong></p><ul><li>多个线程可以同时获取资源。</li><li>比如：<ul><li><code>Semaphore</code></li><li><code>CountDownLatch</code></li><li>读写锁中的读锁</li></ul></li></ul><ol><li><strong>AQS 用到了什么技术</strong></li></ol><ul><li><code>volatile state</code> 保证可见性。</li><li><code>CAS</code> 保证原子更新。</li><li><code>CLH FIFO</code> 队列管理等待线程。</li><li><code>LockSupport.park/unpark</code> 挂起和唤醒线程。</li><li>模板方法模式，让子类实现获取和释放逻辑。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>AQS 是 AbstractQueuedSynchronizer，是 JUC 中很多锁和同步工具的基础框架。它的核心是一个 volatile 的 state 状态变量和一个 FIFO 的等待队列。线程获取锁时，会先通过 CAS 修改 state，成功就获取同步资源；失败就被封装成 Node 加入 CLH 队列，并通过 LockSupport.park 挂起。释放锁时修改 state，并唤醒队列中的后继线程。AQS 支持独占和共享两种模式，ReentrantLock 是独占模式，Semaphore、CountDownLatch、读写锁的读锁是共享模式。AQS 通过 state、CAS、队列和 park/unpark 实现了通用同步框架。</p><p></p></blockquote>"},{"t":"如何用 AQS 实现一个可重入的公平锁？","tag":"","p":"","html":"<p>面试讲思路就够了：<strong>继承 AQS，用 </strong><strong><code>state</code></strong><strong> 表示重入次数，用 </strong><strong><code>exclusiveOwnerThread</code></strong><strong> 记录持锁线程，公平性通过检查队列前面有没有等待线程实现。</strong></p><ol><li><strong>核心设计</strong></li></ol><ul><li><code>state = 0</code>：锁空闲。</li><li><code>state &gt; 0</code>：锁已被持有，值表示重入次数。</li><li><code>exclusiveOwnerThread</code>：当前持有锁的线程。</li><li>公平锁：获取锁前先判断是否有前驱节点排队。</li></ul><ol><li><strong>加锁逻辑</strong></li></ol><p>重写 AQS 的：</p><pre><code>tryAcquire(int acquires)</code></pre><p>逻辑：</p><ul><li>如果 <code>state == 0</code>：<ul><li>先判断队列前面有没有等待线程。</li><li>如果没有前驱节点，再 CAS 把 <code>state</code> 从 <code>0</code> 改成 <code>1</code>。</li><li>CAS 成功后设置当前线程为持锁线程。</li></ul></li><li>如果当前线程已经是持锁线程：<ul><li>说明是重入。</li><li><code>state + 1</code>。</li><li>返回成功。</li></ul></li><li>否则：<ul><li>获取失败，进入 AQS 队列排队。</li></ul></li></ul><p>公平性关键方法：</p><pre><code>hasQueuedPredecessors()</code></pre><p>表示当前线程前面是否有排队线程。</p><ol><li><strong>释放锁逻辑</strong></li></ol><p>重写：</p><pre><code>tryRelease(int releases)</code></pre><p>逻辑：</p><ul><li>先判断当前线程是不是持锁线程。</li><li>如果不是，抛异常。</li><li>如果是，<code>state - 1</code>。</li><li>如果减完后 <code>state == 0</code>：<ul><li>清空 <code>exclusiveOwnerThread</code>。</li><li>锁真正释放。</li><li>AQS 会唤醒后继节点。</li></ul></li><li>如果 <code>state &gt; 0</code>：<ul><li>说明只是减少一次重入次数。</li><li>锁还没真正释放。</li></ul></li></ul><ol><li><strong>简化代码</strong></li></ol><pre><code>class MyReentrantFairLock {<br/>    private final Sync sync = new Sync();<br/><br/>    public void lock() {<br/>        sync.acquire(1);<br/>    }<br/><br/>    public void unlock() {<br/>        sync.release(1);<br/>    }<br/><br/>    private static class Sync extends AbstractQueuedSynchronizer {<br/>        @Override<br/>        protected boolean tryAcquire(int acquires) {<br/>            Thread current = Thread.currentThread();<br/>            int state = getState();<br/><br/>            if (state == 0) {<br/>                if (!hasQueuedPredecessors()<br/>                        &amp;&amp; compareAndSetState(0, acquires)) {<br/>                    setExclusiveOwnerThread(current);<br/>                    return true;<br/>                }<br/>            } else if (current == getExclusiveOwnerThread()) {<br/>                int nextState = state + acquires;<br/>                setState(nextState);<br/>                return true;<br/>            }<br/><br/>            return false;<br/>        }<br/><br/>        @Override<br/>        protected boolean tryRelease(int releases) {<br/>            Thread current = Thread.currentThread();<br/><br/>            if (current != getExclusiveOwnerThread()) {<br/>                throw new IllegalMonitorStateException();<br/>            }<br/><br/>            int nextState = getState() - releases;<br/>            boolean free = false;<br/><br/>            if (nextState == 0) {<br/>                free = true;<br/>                setExclusiveOwnerThread(null);<br/>            }<br/><br/>            setState(nextState);<br/>            return free;<br/>        }<br/><br/>        @Override<br/>        protected boolean isHeldExclusively() {<br/>            return Thread.currentThread() == getExclusiveOwnerThread();<br/>        }<br/>    }<br/>}</code></pre><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>用 AQS 实现可重入公平锁，核心是用 state 表示锁的重入次数，用 exclusiveOwnerThread 记录当前持锁线程。加锁时重写 tryAcquire，如果 state 为 0，先通过 hasQueuedPredecessors 判断当前线程前面是否有排队线程，没有才 CAS 把 state 从 0 改成 1，并设置持锁线程；如果当前线程已经是持锁线程，就说明是重入，直接把 state 加一。释放锁时重写 tryRelease，只有持锁线程才能释放，每释放一次 state 减一，直到 state 变成 0 才清空 owner 并真正释放锁。公平性主要靠 hasQueuedPredecessors 保证先来先获取。</p></blockquote>"},{"t":"Threadlocal作用，原理，具体里面存的key value是啥，会有什么问题，如何解决?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>可以按 <strong>作用 → 原理 → key/value → 问题 → 解决</strong> 来答。</p><ol><li><strong>ThreadLocal 的作用</strong></li></ol><blockquote><p><code>ThreadLocal</code> 用来给每个线程保存一份独立变量副本，实现线程隔离。</p></blockquote><p>也就是说，同一个 <code>ThreadLocal</code> 变量，不同线程拿到的是各自线程里的值，互不影响。</p><p>常见场景：</p><ul><li>保存用户上下文。</li><li>保存 <code>traceId</code>。</li><li>保存数据库连接 / 事务上下文。</li><li>保存日期格式化对象。</li><li>保存登录态信息。</li></ul><ol><li><strong>ThreadLocal 的原理</strong></li></ol><p>每个 <code>Thread</code> 对象内部都有一个：</p><pre><code>ThreadLocalMap</code></pre><p>真正的数据不是存在 <code>ThreadLocal</code> 对象里，而是存在当前线程自己的 <code>ThreadLocalMap</code> 里。</p><p>可以理解为：</p><pre><code>Thread<br/>  └── ThreadLocalMap<br/>        ├── Entry(ThreadLocal对象1 -&gt; value1)<br/>        ├── Entry(ThreadLocal对象2 -&gt; value2)</code></pre><p>当调用：</p><pre><code>threadLocal.set(value);</code></pre><p>实际是：</p><pre><code>当前线程.threadLocals.set(threadLocal, value)</code></pre><p>当调用：</p><pre><code>threadLocal.get();</code></pre><p>实际是：</p><pre><code>从当前线程的 ThreadLocalMap 里，用 threadLocal 作为 key 找 value</code></pre><ol><li><strong>key 和 value 是什么</strong></li></ol><p>在 <code>ThreadLocalMap</code> 中：</p><ul><li><code>key</code>：当前这个 <code>ThreadLocal</code> 对象。</li><li><code>value</code>：你存进去的值。</li></ul><p>例如：</p><pre><code>ThreadLocal&lt;User&gt; userLocal = new ThreadLocal&lt;&gt;();<br/>userLocal.set(user);</code></pre><p>此时：</p><pre><code>key = userLocal 这个 ThreadLocal 对象<br/>value = user 对象</code></pre><p>注意：</p><ul><li><code>key</code> 是弱引用。</li><li><code>value</code> 是强引用。</li></ul><p>也就是：</p><pre><code>Entry extends WeakReference&lt;ThreadLocal&lt;?&gt;&gt;</code></pre><ol><li><strong>会有什么问题</strong></li></ol><p>最大问题是：<strong>内存泄漏</strong>。</p><p>因为 <code>ThreadLocalMap</code> 的 <code>key</code> 是弱引用。</p><p>如果外部没有强引用指向这个 <code>ThreadLocal</code> 对象，GC 后：</p><pre><code>key 被回收，变成 null<br/>value 还在</code></pre><p>就会出现：</p><pre><code>null -&gt; value</code></pre><p>如果线程一直不结束，比如线程池里的核心线程长期存活，那么这个 <code>value</code> 可能一直无法释放，造成内存泄漏。</p><ol><li><strong>为什么线程池里更容易出问题</strong></li></ol><p>普通线程执行完就结束了，线程的 <code>ThreadLocalMap</code> 也会被回收。</p><p>但线程池里的线程会复用，长期存在。</p><p>如果任务结束后不清理：</p><ul><li>下一个任务可能读到上一个任务的数据。</li><li>value 长时间不释放，造成内存泄漏。</li><li>用户上下文可能串号。</li></ul><ol><li><strong>如何解决</strong></li></ol><p>最重要：<strong>用完一定 remove。</strong></p><p>推荐写法：</p><pre><code>try {<br/>    threadLocal.set(value);<br/>    // 业务逻辑<br/>} finally {<br/>    threadLocal.remove();<br/>}</code></pre><p>尤其是在线程池、Web 请求、异步任务中，一定要清理。</p><p>其他建议：</p><ul><li>尽量定义为 <code>static final</code>，避免 key 被回收。</li><li>不要存大对象。</li><li>在线程池任务执行前后做统一清理。</li><li>使用框架提供的上下文清理机制。</li><li>父子线程传递上下文可以考虑 <code>InheritableThreadLocal</code>，但线程池中要谨慎。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>ThreadLocal 的作用是为每个线程提供一份独立变量副本，实现线程隔离。它的值不是存在 ThreadLocal 对象里，而是存在当前线程 Thread 对象内部的 ThreadLocalMap 中。ThreadLocalMap 里的 key 是 ThreadLocal 对象，value 是我们 set 进去的值。需要注意的是，key 是弱引用，value 是强引用，如果 ThreadLocal 对象被回收，key 会变成 null，但 value 还可能留在线程的 ThreadLocalMap 里；在线程池这种线程长期存活的场景下，就可能造成内存泄漏或数据串用。解决方式是使用完后在 finally 中调用 remove，同时避免存大对象，在线程池和 Web 请求场景尤其要注意清理。</p></blockquote>"},{"t":"悲观锁和乐观锁的区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>核心区别</strong></p><ul><li><strong>悲观锁</strong>：认为并发冲突经常发生，所以操作前先加锁。</li><li><strong>乐观锁</strong>：认为并发冲突较少，所以不提前加锁，提交更新时再检查冲突。</li></ul><ol><li><strong>悲观锁</strong></li></ol><p>思想：</p><blockquote><p>我正在操作数据，别人先别动。</p></blockquote><p>常见实现：</p><pre><code>SELECT *<br/>FROM product<br/>WHERE id = 1<br/>FOR UPDATE;</code></pre><p>特点：</p><ul><li>先锁住数据，再修改。</li><li>其他事务要改同一行，需要等待。</li><li>一致性强。</li><li>但并发性能较低。</li><li>可能出现锁等待或死锁。</li></ul><p>适合：</p><ul><li>写多读少。</li><li>冲突比较激烈。</li><li>对数据一致性要求高。</li><li>比如余额扣减、库存扣减。</li></ul><ol><li><strong>乐观锁</strong></li></ol><p>思想：</p><blockquote><p>我先不加锁，提交时看有没有被别人改过。</p></blockquote><p>常见实现：版本号 <code>version</code>。</p><pre><code>UPDATE product<br/>SET stock = stock - 1,<br/>    version = version + 1<br/>WHERE id = 1<br/>  AND version = 10;</code></pre><p>如果影响行数是 <code>1</code>，说明更新成功。</p><p>如果影响行数是 <code>0</code>，说明版本变了，被别人改过，需要重试或失败返回。</p><p>特点：</p><ul><li>不阻塞线程。</li><li>并发性能好。</li><li>但冲突多时会频繁失败重试。</li><li>实现上需要版本号、时间戳或 CAS。</li></ul><p>适合：</p><ul><li>读多写少。</li><li>冲突较少。</li><li>希望减少锁等待。</li><li>比如用户资料修改、配置更新。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>悲观锁和乐观锁的区别在于对并发冲突的假设不同。悲观锁认为冲突经常发生，所以操作前先加锁，比如 MySQL 的 <code>select ... for update</code>，适合写多、冲突激烈、一致性要求高的场景，但会带来阻塞和死锁风险。乐观锁认为冲突较少，不提前加锁，而是在更新时通过 version、时间戳或 CAS 判断数据是否被修改过，适合读多写少、冲突不严重的场景，优点是并发性能好，但冲突严重时会频繁重试。</p></blockquote>"},{"t":"Java中想实现一个乐观锁，都有哪些方式？","tag":"","p":"","html":"<p>Java 里实现乐观锁，常见有这几种方式：</p><ol><li><strong>CAS 原子类</strong></li></ol><p>JUC 里的原子类底层就是 CAS 思想。</p><span>[原文档此处有配图]</span><p>常用：</p><ul><li><code>AtomicInteger</code></li><li><code>AtomicLong</code></li><li><code>AtomicReference</code></li><li><code>AtomicBoolean</code></li></ul><p>例子：</p><pre><code>AtomicInteger count = new AtomicInteger(0);<br/><br/>count.compareAndSet(0, 1);</code></pre><p>含义是：</p><pre><code>如果当前值还是 0，就改成 1<br/>否则更新失败</code></pre><p>适合：</p><ul><li>计数器。</li><li>状态切换。</li><li>简单共享变量更新。</li></ul><ol><li><strong>版本号 version</strong></li></ol><p>数据库里常用。</p><p>表里加一个 <code>version</code> 字段：</p><pre><code>UPDATE product<br/>SET stock = stock - 1,<br/>    version = version + 1<br/>WHERE id = ?<br/>  AND version = ?;</code></pre><p>如果影响行数为 <code>1</code>，更新成功。</p><p>如果影响行数为 <code>0</code>，说明数据被别人改过，需要重试或失败返回。</p><p>适合：</p><ul><li>库存扣减。</li><li>订单状态流转。</li><li>数据编辑防覆盖。</li></ul><ol><li><strong>时间戳 timestamp</strong></li></ol><p>和版本号类似。</p><p>更新时判断数据的更新时间是否没变：</p><pre><code>UPDATE user<br/>SET name = ?,<br/>    update_time = NOW()<br/>WHERE id = ?<br/>  AND update_time = ?;</code></pre><p>适合：</p><ul><li>简单数据更新冲突判断。</li><li>但时间精度和时钟问题要注意。</li></ul><ol><li><strong>CAS 自旋重试</strong></li></ol><p>如果更新失败，可以循环重试：</p><pre><code>while (true) {<br/>    int oldValue = atomic.get();<br/>    int newValue = oldValue + 1;<br/><br/>    if (atomic.compareAndSet(oldValue, newValue)) {<br/>        break;<br/>    }<br/>}</code></pre><p>适合：</p><ul><li>冲突不激烈。</li><li>重试成本低。</li></ul><p>如果冲突很高，自旋会浪费 CPU。</p><ol><li><strong>带标记的 CAS 解决 ABA</strong></li></ol><p>CAS 有 ABA 问题：</p><pre><code>A → B → A</code></pre><p>表面看还是 A，但其实中间被改过。</p><p>可以用：</p><ul><li><code>AtomicStampedReference</code></li><li><code>AtomicMarkableReference</code></li></ul><p>通过版本戳或标记判断是否发生过变化。</p><p><strong>面试背诵版</strong></p><blockquote><p>Java 中实现乐观锁常见方式有 CAS、版本号和时间戳。JUC 里的 AtomicInteger、AtomicLong、AtomicReference 等原子类底层就是 CAS，适合简单变量的无锁更新；数据库场景通常加 version 字段，更新时带上旧版本号，成功则版本加一，失败说明被其他线程修改过；也可以用 update_time 时间戳做类似判断。CAS 失败时可以自旋重试，但冲突高时会浪费 CPU。另外 CAS 还有 ABA 问题，可以用 AtomicStampedReference 通过版本戳解决。</p></blockquote>"},{"t":"CAS 有什么缺点？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>CAS</code> 的主要缺点有三个：<strong>ABA 问题、自旋消耗 CPU、只能保证单个变量的原子性</strong>。</p><ol><li><strong>ABA 问题</strong></li></ol><p>CAS 判断的是：</p><pre><code>当前值是否等于旧值</code></pre><p>如果一个值经历了：</p><pre><code>A → B → A</code></pre><p>CAS 会认为它没变过，因为当前还是 <code>A</code>。</p><p>但实际上中间已经被其他线程改过了。</p><p>解决方式：</p><ul><li>加版本号。</li><li>使用 <code>AtomicStampedReference</code>。</li><li>使用 <code>AtomicMarkableReference</code>。</li></ul><ol><li><strong>自旋消耗 CPU</strong></li></ol><span>[原文档此处有配图]</span><p>CAS 失败时通常会不断重试。</p><p>比如：</p><pre><code>while (!compareAndSet(...)) {<br/>    // 自旋重试<br/>}</code></pre><p>如果并发冲突很激烈，很多线程一直 CAS 失败，就会空转消耗 CPU。</p><p>所以 CAS 更适合：</p><ul><li>冲突少。</li><li>操作很短。</li><li>重试成本低。</li></ul><p>不适合冲突特别激烈的场景。</p><ol><li><strong>只能保证单个变量原子性</strong></li></ol><p>普通 CAS 通常只能保证一个变量的原子更新。</p><p>比如：</p><pre><code>AtomicInteger<br/>AtomicLong<br/>AtomicReference</code></pre><p>如果要同时保证多个变量一致更新，就比较麻烦。</p><p>解决方式：</p><ul><li>把多个变量封装成一个对象，用 <code>AtomicReference</code> 更新整个对象。</li><li>或者直接使用锁。</li><li>或者用数据库事务。</li></ul><ol><li><strong>代码复杂度较高</strong></li></ol><p>CAS 重试逻辑、失败处理、ABA 处理都需要额外考虑。</p><p>如果业务逻辑复杂，用 CAS 可能不如锁清晰。</p><p><strong>面试背诵版</strong></p><blockquote><p>CAS 的缺点主要有三个。第一是 ABA 问题，值从 A 变成 B 又变回 A，CAS 会误以为没有变化，可以通过版本号或 AtomicStampedReference 解决。第二是自旋开销，如果竞争激烈，CAS 会不断失败重试，浪费 CPU。第三是 CAS 通常只能保证单个变量的原子性，如果要保证多个变量的一致更新，实现会比较复杂，可能需要 AtomicReference 封装对象或者直接使用锁。总体来说，CAS 适合冲突少、操作简单的场景。</p></blockquote>"},{"t":"为什么不能所有的锁都用CAS？","tag":"","p":"","html":"<p>不能所有锁都用 <code>CAS</code>，因为 <code>CAS</code> 适合<strong>短时间、低冲突、简单变量更新</strong>，不适合所有并发场景。</p><ol><li><strong>高竞争下会浪费 CPU</strong></li></ol><span>[原文档此处有配图]</span><p><strong><code>CAS</code></strong><strong> 失败通常会自旋重试：</strong></p><pre><code>比较 → 失败 → 再比较 → 再失败 → 继续重试</code></pre><p>如果很多线程同时竞争，一个线程成功，其他线程都失败并自旋。</p><p>这会导致：</p><ul><li>CPU 空转。</li><li>竞争越激烈，浪费越严重。</li><li>吞吐量反而下降。</li></ul><p>这时阻塞锁可能更合适，让线程挂起等待。</p><ol><li><strong>临界区耗时长不适合 CAS</strong></li></ol><p>CAS 适合很短的操作，比如：</p><pre><code>count++</code></pre><p>如果临界区里有：</p><ul><li>IO</li><li>网络请求</li><li>数据库操作</li><li>复杂业务逻辑</li><li>多步状态变更</li></ul><p>让线程一直自旋等待就很浪费 CPU。</p><p>这种场景更适合锁。</p><ol><li><strong>多变量一致性不好处理</strong></li></ol><p>CAS 通常只能原子更新一个变量。</p><p>如果要同时更新多个变量，比如：</p><pre><code>余额 + 订单状态 + 库存</code></pre><p>就很难只靠 CAS 保证一致性。</p><p>这时需要：</p><ul><li>锁</li><li>事务</li><li>更复杂的数据结构</li></ul><ol><li><strong>ABA 问题</strong></li></ol><span>[原文档此处有配图]</span><p>CAS 只判断当前值和期望值是否相等。</p><p>可能出现：</p><pre><code>A → B → A</code></pre><p>CAS 以为没变，但实际上变过。</p><p>虽然可以用版本号解决，但会增加复杂度。</p><ol><li><strong>公平性不好</strong></li></ol><p>CAS 自旋通常是谁抢到算谁的。</p><p>可能某些线程一直失败，一直拿不到机会。</p><p>也就是可能产生饥饿问题。</p><ol><li><strong>代码复杂度高</strong></li></ol><p>CAS 需要考虑：</p><ul><li>自旋次数</li><li>失败重试</li><li>ABA</li><li>回退策略</li><li>并发边界</li></ul><p>复杂业务里可读性和维护性不如锁。</p><p><strong>面试背诵版</strong></p><blockquote><p>不能所有锁都用 CAS，因为 CAS 只适合低冲突、短临界区、简单变量更新。高并发竞争激烈时，大量线程 CAS 失败会一直自旋，浪费 CPU；如果临界区耗时长，比如包含 IO、数据库操作，自旋等待也不划算。CAS 还通常只能保证单个变量的原子性，多个变量一致性很难处理，并且存在 ABA 问题和公平性问题。所以 CAS 适合计数器、状态标记这类简单场景，而复杂同步、高竞争或长时间持锁场景更适合使用锁或事务。</p></blockquote>"},{"t":"CAS 有什么问题，Java是怎么解决的？","tag":"","p":"","html":"<p>不能所有锁都用 <code>CAS</code>，因为 <code>CAS</code> 本质是<strong>乐观重试</strong>，只适合<strong>低冲突、短操作、简单状态更新</strong>的场景。</p><ol><li><strong>高并发竞争下会浪费 CPU</strong></li></ol><p><code>CAS</code> 失败后通常会自旋重试：</p><pre><code>CAS 失败 → 再试 → 又失败 → 继续试</code></pre><p>如果很多线程同时竞争：</p><ul><li>只有一个线程能成功。</li><li>其他线程会不断失败重试。</li><li>CPU 会被大量空转消耗掉。</li></ul><p>这种情况下，阻塞锁反而更合适，因为拿不到锁的线程可以挂起，避免一直烧 CPU。</p><ol><li><strong>临界区耗时长不适合</strong></li></ol><p><code>CAS</code> 适合很短的操作，比如：</p><pre><code>count++</code></pre><p>但如果临界区里有：</p><ul><li>数据库操作</li><li>网络请求</li><li>文件 IO</li><li>复杂业务计算</li></ul><p>那其他线程一直自旋等待就很浪费。</p><p>这种场景应该用锁，让线程阻塞等待。</p><ol><li><strong>多变量一致性难保证</strong></li></ol><p><code>CAS</code> 通常只能保证单个变量的原子更新。</p><p>比如：</p><pre><code>AtomicInteger<br/>AtomicReference</code></pre><p>但很多业务要同时改多个状态：</p><pre><code>库存、订单状态、余额、流水</code></pre><p>这时只靠单个 CAS 很难保证整体一致性。</p><p>通常要用：</p><ul><li>锁</li><li>数据库事务</li><li>分布式事务</li></ul><ol><li><strong>有 ABA 问题</strong></li></ol><p>CAS 判断的是：</p><pre><code>当前值 == 期望值</code></pre><p>如果值经历了：</p><pre><code>A → B → A</code></pre><p>CAS 会以为没变过，但实际上中间被修改过。</p><p>可以用版本号解决，但会增加复杂度。</p><ol><li><strong>公平性不好</strong></li></ol><p>CAS 自旋通常是谁抢到算谁。</p><p>可能某些线程一直失败，一直拿不到执行机会。</p><p>也就是可能产生线程饥饿。</p><ol><li><strong>代码复杂度高</strong></li></ol><p>CAS 需要处理：</p><ul><li>失败重试</li><li>自旋次数</li><li>ABA</li><li>回退策略</li><li>并发边界</li></ul><p>复杂业务里用 CAS，可读性和维护性可能比锁更差。</p><p><strong>面试背诵版</strong></p><blockquote><p>不能所有锁都用 CAS，因为 CAS 是乐观自旋机制，<strong>适合低冲突、操作很短、单变量更新的场景</strong>。高竞争下大量线程 CAS 失败会不断自旋，浪费 CPU；如果临界区耗时长，比如包含 IO 或数据库操作，自旋等待也不划算。<strong>CAS 还存在 ABA 问题，通常只能保证单个变量的原子性，多变量一致性处理复杂</strong>，而且公平性不好，可能导致线程饥饿。所以简单计数、状态更新可以用 CAS，复杂业务、高竞争或长耗时临界区更适合用锁或事务。</p></blockquote>"},{"t":"voliatle关键字有什么作用？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>先纠正拼写：是 <code>volatile</code>。</p><p><code>volatile</code> 主要有两个作用：<strong>保证可见性</strong> 和 <strong>禁止指令重排序</strong>。</p><ol><li><strong>保证可见性</strong></li></ol><p>普通变量在线程之间不一定马上可见。</p><p>比如线程 A 修改了一个变量，线程 B 可能还在读自己工作内存里的旧值。</p><p>加了 <code>volatile</code> 后：</p><pre><code>private volatile boolean running = true;</code></pre><p>一个线程修改：</p><pre><code>running = false;</code></pre><p>其他线程能及时看到这个变化。</p><p>所以它常用于线程停止标记：</p><pre><code>while (running) {<br/>    // 执行任务<br/>}</code></pre><ol><li><strong>禁止指令重排序</strong></li></ol><p><code>volatile</code> 会在读写时加入内存屏障，限制编译器和 CPU 对相关指令重排序。</p><p>经典场景是双重检查锁单例：</p><pre><code>private static volatile Singleton instance;</code></pre><p>防止对象初始化过程被重排序，导致其他线程拿到一个“半初始化”的对象。</p><ol><li><strong>volatile 不保证原子性</strong></li></ol><p>这是重点。</p><p>比如：</p><pre><code>volatile int count = 0;<br/>count++;</code></pre><p><code>count++</code> 不是原子操作，它包含：</p><pre><code>读取 count<br/>加 1<br/>写回 count</code></pre><p>多个线程同时执行仍然可能丢失更新。</p><p>如果要保证原子性，要用：</p><pre><code>AtomicInteger<br/>synchronized<br/>Lock</code></pre><ol><li><strong>适用场景</strong></li></ol><p>适合：</p><ul><li>状态标记。</li><li>开关变量。</li><li>单例双重检查。</li><li>一个线程写，多个线程读。</li><li>不依赖当前值的简单赋值。</li></ul><p>不适合：</p><ul><li>复合操作。</li><li>多线程累加。</li><li>多变量一致性。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>volatile 的作用主要是保证变量在线程之间的可见性，以及禁止一定程度的指令重排序。它通过内存屏障让一个线程对 volatile 变量的修改能及时被其他线程看到，也能避免某些重排序问题，比如双重检查锁单例中需要用 volatile 修饰实例对象。但 volatile 不保证原子性，比如 count++ 仍然不是线程安全的，因为它是读、改、写三个步骤。所以 volatile 适合状态标记、开关变量、一写多读场景，不适合复合更新操作。</p></blockquote>"},{"t":"指令重排序的原理是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>指令重排序</strong>就是：在不影响单线程执行结果的前提下，编译器和 CPU 可能会调整指令的执行顺序，以提高性能。</p><ol><li><strong>为什么要重排序</strong></li></ol><p>现代 CPU 很快，但内存访问很慢。</p><p>为了提高执行效率，编译器和 CPU 会做优化，比如：</p><ul><li>合并指令。</li><li>调整执行顺序。</li><li>利用流水线。</li><li>减少等待内存的时间。</li><li>提高缓存命中率。</li></ul><p>所以代码写的顺序，不一定完全等于底层实际执行顺序。</p><ol><li><strong>重排序的前提</strong></li></ol><p>重排序必须遵守一个原则：</p><blockquote><p>在单线程中，不能改变程序最终执行结果。</p></blockquote><p>也就是 <strong>as-if-serial</strong> 语义。</p><p>例如：</p><pre><code>int a = 1;<br/>int b = 2;<br/>int c = a + b;</code></pre><p><code>a = 1</code> 和 <code>b = 2</code> 没有依赖关系，可能被调整顺序。</p><p>但：</p><pre><code>int a = 1;<br/>int c = a + 1;</code></pre><p>这两个有数据依赖，不能随便重排。</p><ol><li><strong>重排序发生在哪些层面</strong></li></ol><p>常见有三类：</p><ul><li><strong>编译器重排序</strong><ul><li>编译器在生成机器码时调整指令顺序。</li></ul></li><li><strong>CPU 指令级重排序</strong><ul><li>CPU 为了流水线和乱序执行，可能不按程序顺序执行。</li></ul></li><li><strong>内存系统重排序</strong><ul><li>CPU 缓存、写缓冲区等可能导致其他线程看到的内存操作顺序不同。</li></ul></li></ul><ol><li><strong>单线程没问题，多线程可能出问题</strong></li></ol><p>单线程下重排序不影响结果。</p><p>但多线程下，一个线程的重排序可能被另一个线程观察到，导致问题。</p><p>经典例子：</p><pre><code>ready = true;<br/>data = 42;</code></pre><p>如果发生重排序，另一个线程可能看到：</p><pre><code>ready = true<br/>但 data 还不是 42</code></pre><p>所以多线程下需要同步机制保证有序性。</p><ol><li><strong>volatile 怎么限制重排序</strong></li></ol><p><code>volatile</code> 会插入内存屏障。</p><p>效果大概是：</p><ul><li><code>volatile</code> 写之前的普通写，不能重排到它后面。</li><li><code>volatile</code> 读之后的普通读写，不能重排到它前面。</li><li>保证其他线程看到更符合预期的顺序。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>指令重排序是编译器和 CPU 为了提高执行效率，在不改变单线程语义的前提下调整指令执行顺序的优化。它遵守 as-if-serial 原则，也就是单线程结果不能变。重排序可能发生在编译器、CPU 乱序执行和内存系统层面。单线程下通常没有问题，但多线程下，一个线程的重排序可能被另一个线程观察到，导致可见性或有序性问题。Java 中可以通过 volatile、synchronized、Lock 等同步机制建立 happens-before 关系，限制重排序。</p></blockquote>"},{"t":"volatile可以保证线程安全吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p><code>volatile</code> <strong>不能完全保证线程安全</strong>，它只能保证<strong>可见性</strong>和<strong>有序性</strong>，不能保证复合操作的<strong>原子性</strong>。</p><ol><li><strong>volatile 能保证什么</strong></li></ol><p><code>volatile</code> 可以保证：</p><ul><li>一个线程修改变量后，其他线程能及时看到。</li><li>禁止相关指令重排序。</li></ul><p>比如：</p><pre><code>private volatile boolean running = true;</code></pre><p>一个线程修改：</p><pre><code>running = false;</code></pre><p>另一个线程的循环能及时感知并退出。</p><p>这个场景是可以的。</p><ol><li><strong>volatile 不能保证什么</strong></li></ol><p>它不能保证复合操作原子性。</p><p>比如：</p><pre><code>private volatile int count = 0;<br/><br/>count++;</code></pre><p><code>count++</code> 看起来是一行，但实际是三步：</p><pre><code>1. 读取 count<br/>2. 加 1<br/>3. 写回 count</code></pre><p>多个线程同时执行时，还是可能都读到同一个旧值，导致更新丢失。</p><p>所以 <code>volatile int count</code> 做多线程累加是不安全的。</p><ol><li><strong>什么时候 volatile 可以算线程安全</strong></li></ol><p>如果操作本身是简单赋值，且不依赖旧值，可以用 <code>volatile</code>。</p><p>例如：</p><pre><code>volatile boolean flag;<br/>flag = true;</code></pre><p>适合：</p><ul><li>状态标记。</li><li>开关变量。</li><li>一写多读。</li><li>双重检查锁中的单例引用。</li></ul><ol><li><strong>什么时候不能只用 volatile</strong></li></ol><p>不能只用在：</p><ul><li><code>count++</code></li><li>多变量一致性更新</li><li>先判断再修改</li></ul><p>比如：</p><pre><code>if (stock &gt; 0) {<br/>    stock--;<br/>}</code></pre><p>这种需要锁或原子类。</p><p><strong>面试背诵版</strong></p><blockquote><p>volatile 不能完全保证线程安全。它能保证变量的可见性和禁止一定程度的指令重排序，但不能保证复合操作的原子性。比如 volatile 修饰的 count 执行 count++，仍然可能出现并发更新丢失，因为 count++ 包含读取、加一、写回三个步骤。volatile 适合状态标记、一写多读、简单赋值这类场景；如果涉及累加、判断后修改、多变量一致性，就需要使用 synchronized、Lock 或 Atomic 原子类。</p></blockquote>"},{"t":"volatile和sychronized比较？","tag":"","p":"","html":"<p>先纠正拼写：是 <code>synchronized</code>。</p><p><strong>核心区别</strong></p><ul><li><code>volatile</code> 是轻量级同步机制，主要保证<strong>可见性和有序性</strong>。</li><li><code>synchronized</code> 是锁机制，可以保证<strong>原子性、可见性和有序性</strong>。</li></ul><ol><li><strong>原子性不同</strong></li></ol><p><code>volatile</code> 不保证复合操作原子性。</p><pre><code>volatile int count = 0;<br/>count++;</code></pre><p><code>count++</code> 仍然不是线程安全的。</p><p><code>synchronized</code> 可以保证临界区同一时间只有一个线程执行，所以能保证原子性。</p><pre><code>synchronized (lock) {<br/>    count++;<br/>}</code></pre><ol><li><strong>可见性不同</strong></li></ol><p>两者都能保证可见性。</p><ul><li><code>volatile</code>：写入后其他线程能及时看到。</li><li><code>synchronized</code>：释放锁前会把修改刷新出去，获取锁后能看到最新值。</li></ul><ol><li><strong>有序性不同</strong></li></ol><p>两者都能保证一定有序性。</p><ul><li><code>volatile</code> 通过内存屏障禁止相关指令重排序。</li><li><code>synchronized</code> 通过锁的 happens-before 规则保证同步块内外的可见性和有序性。</li></ul><ol><li><strong>阻塞不同</strong></li></ol><ul><li><code>volatile</code> 不会阻塞线程。</li><li><code>synchronized</code> 获取不到锁时会阻塞。</li></ul><p>所以 <code>volatile</code> 开销更小，但能力也更弱。</p><ol><li><strong>使用场景不同</strong></li></ol><p><code>volatile</code> 适合：</p><ul><li>状态标记。</li><li>开关变量。</li><li>一写多读。</li><li>双重检查锁单例中的实例引用。</li></ul><p><code>synchronized</code> 适合：</p><ul><li>复合操作。</li><li>多线程修改共享数据。</li><li>临界区保护。</li><li>需要保证多个操作整体一致。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>volatile 和 synchronized 都能保证可见性和一定的有序性，但 volatile 不能保证原子性，而 synchronized 可以保证原子性。volatile 是轻量级的，不会阻塞线程，适合状态标记、一写多读、简单赋值等场景；synchronized 是锁机制，获取不到锁会阻塞，但可以保护一段临界区，适合 count++、判断后修改、多变量一致性更新等复合操作。所以 volatile 更轻量但能力有限，synchronized 更重但能保证完整的线程安全。</p></blockquote>"},{"t":"什么是公平锁和非公平锁？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>公平锁和非公平锁的区别</strong>：看线程获取锁时，是否严格按照排队顺序。</p><ol><li><strong>公平锁</strong></li></ol><p>公平锁就是：</p><blockquote><p>先来等待的线程，优先拿到锁。</p></blockquote><p>类似排队买票，谁先排队谁先买。</p><p>Java 示例：</p><pre><code>ReentrantLock lock = new ReentrantLock(true);</code></pre><p>特点：</p><ul><li>按等待顺序获取锁。</li><li>可以减少线程饥饿。</li><li>但性能通常低一些。</li><li>因为要维护队列顺序，不能随便插队。</li></ul><p>适合：</p><ul><li>对响应公平性要求高。</li><li>不希望某些线程长期拿不到锁。</li></ul><ol><li><strong>非公平锁</strong></li></ol><p>非公平锁就是：</p><blockquote><p>新来的线程可以直接尝试抢锁，不一定按排队顺序。</p></blockquote><p>Java 示例：</p><pre><code>ReentrantLock lock = new ReentrantLock(false);</code></pre><p>这也是默认方式。</p><p>特点：</p><ul><li>允许插队。</li><li>吞吐量通常更高。</li><li>但可能导致某些线程等待时间较长，甚至饥饿。</li></ul><p>为什么性能更高？</p><p>因为锁刚释放时，新来的线程可能正好在 CPU 上运行，直接抢到锁，比唤醒队列里的老线程更省上下文切换。</p><ol><li><strong>synchronized 是公平锁吗？</strong></li></ol><p><code>synchronized</code> 一般认为是<strong>非公平锁</strong>。</p><p>它不保证线程按申请锁的顺序获取锁。</p><p><strong>面试背诵版</strong></p><blockquote><p>公平锁是指多个线程竞争锁时，按照请求锁的先后顺序获取锁，先等待的线程优先拿到锁，可以避免饥饿，但性能相对低。非公平锁不保证顺序，新来的线程可以插队抢锁，吞吐量通常更高，但可能导致某些线程长时间获取不到锁。Java 中 ReentrantLock 可以通过构造参数指定公平或非公平，默认是非公平锁；synchronized 通常也认为是非公平的。</p></blockquote>"},{"t":"非公平锁吞吐量为什么比公平锁大？","tag":"","p":"","html":"<p>非公平锁吞吐量更高，核心原因是：<strong>它允许插队，减少线程唤醒和上下文切换开销。</strong></p><ol><li><strong>公平锁要按队列来</strong></li></ol><p>公平锁释放后，通常要唤醒等待队列里排最前的线程。</p><p>流程类似：</p><pre><code>释放锁<br/>  ↓<br/>唤醒队列头线程<br/>  ↓<br/>队列头线程从阻塞状态恢复<br/>  ↓<br/>被 CPU 调度<br/>  ↓<br/>获取锁执行</code></pre><p>这里会有：</p><ul><li>线程唤醒成本</li><li>上下文切换成本</li><li>调度延迟</li></ul><ol><li><strong>非公平锁允许新线程直接抢</strong></li></ol><p>非公平锁释放时，如果刚好有一个新线程正在 CPU 上运行并尝试加锁，它可以直接 CAS 抢锁成功。</p><p>不一定非要等队列里的老线程被唤醒。</p><p>这样就可能变成：</p><pre><code>锁刚释放<br/>  ↓<br/>当前正在运行的线程直接抢到<br/>  ↓<br/>马上执行</code></pre><p>少了阻塞线程唤醒和调度的等待成本。</p><ol><li><strong>利用 CPU 缓存局部性</strong></li></ol><p>刚运行的线程可能还在 CPU 上。</p><p>它直接拿到锁继续执行，CPU 缓存命中率更好。</p><p>公平锁可能要唤醒另一个睡眠线程，切换过去后缓存局部性更差。</p><ol><li><strong>代价是什么</strong></li></ol><p>非公平锁的代价是：</p><ul><li>不保证先来先得。</li><li>队列中的线程可能被插队。</li><li>极端情况下可能出现线程饥饿。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>非公平锁吞吐量通常比公平锁高，是因为它允许新来的线程插队抢锁。公平锁释放后要唤醒等待队列中排在前面的线程，这涉及线程唤醒、上下文切换和调度延迟；而非公平锁中，如果锁刚释放，当前正在运行的线程可以直接 CAS 抢到锁并继续执行，减少了线程切换成本，也更容易利用 CPU 缓存。因此非公平锁吞吐量更高，但代价是不保证公平，可能导致某些线程等待时间过长甚至饥饿。</p></blockquote>"},{"t":"Synchronized是公平锁吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>Synchronized</code> 一般认为是<strong>非公平锁</strong>。</p><p>准确写法是：<code>synchronized</code>。</p><p><strong>为什么</strong></p><ul><li><code>synchronized</code> 不保证线程按照申请锁的先后顺序获取锁。</li><li>当锁释放后，哪个线程能抢到锁，取决于 JVM 调度和操作系统调度。</li><li>新来的线程也可能比等待更久的线程先拿到锁。</li></ul><p>所以它不是严格的公平锁。</p><p><strong>对比一下</strong></p><p><code>ReentrantLock</code> 可以指定公平性：</p><pre><code>new ReentrantLock(true);  // 公平锁<br/>new ReentrantLock(false); // 非公平锁，默认</code></pre><p>但 <code>synchronized</code> 没有参数可以设置公平或非公平。</p><p><strong>面试背诵版</strong></p><blockquote><p>synchronized 不是公平锁，一般认为它是非公平锁。它不保证线程按照等待顺序获取锁，锁释放后哪个线程抢到锁由 JVM 和操作系统调度决定，可能出现新来的线程先获取锁的情况。相比之下，ReentrantLock 可以通过构造参数指定公平锁或非公平锁，而 synchronized 不能指定公平性。</p></blockquote>"},{"t":"ReentrantLock是怎么实现公平锁的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>ReentrantLock</code> 的公平锁底层是基于 <strong>AQS 队列</strong> 实现的，核心是：<strong>获取锁前先看队列前面有没有人排队。</strong></p><ol><li><strong>AQS 队列</strong></li></ol><p><code>ReentrantLock</code> 底层依赖 <code>AQS</code>。</p><p>AQS 里面有：</p><ul><li><code>state</code>：表示锁状态 / 重入次数。</li><li><code>CLH FIFO 队列</code>：保存等待获取锁的线程。</li><li><code>exclusiveOwnerThread</code>：当前持有锁的线程。</li></ul><ol><li><strong>公平锁怎么加锁</strong></li></ol><p>公平锁的获取逻辑大致是：</p><pre><code>1. 当前线程尝试获取锁<br/>2. 如果锁空闲，先判断队列前面有没有等待线程<br/>3. 如果没有前驱节点，才 CAS 把 state 从 0 改成 1<br/>4. 如果队列前面有人，就不插队，进入队列排队</code></pre><p>关键方法是：</p><pre><code>hasQueuedPredecessors()</code></pre><p>它会判断当前线程在等待队列中前面是否还有其他线程。</p><p>如果有，就说明别人先来，当前线程不能抢。</p><ol><li><strong>非公平锁区别</strong></li></ol><p>非公平锁上来会先尝试 CAS 抢锁：</p><pre><code>锁空闲 → 直接抢</code></pre><p>不管队列里有没有等待线程。</p><p>公平锁则是：</p><pre><code>锁空闲 → 先看队列前面有没有人 → 没人才抢</code></pre><ol><li><strong>可重入怎么处理</strong></li></ol><p>如果当前线程已经持有锁，再次加锁时：</p><pre><code>state + 1</code></pre><p>不需要排队。</p><p>释放时：</p><pre><code>state - 1</code></pre><p>直到 <code>state = 0</code>，锁才真正释放，并唤醒队列中的后继线程。</p><p><strong>面试背诵版</strong></p><blockquote><p>ReentrantLock 的公平锁是基于 AQS 的 FIFO 等待队列实现的。AQS 用 state 表示锁状态和重入次数，用 exclusiveOwnerThread 记录持锁线程。公平锁在 tryAcquire 时，如果 state 为 0，不会直接抢锁，而是先调用 hasQueuedPredecessors 判断等待队列中当前线程前面是否有人；如果前面没人，才通过 CAS 把 state 从 0 改成 1 获取锁；如果前面有人，就进入队列排队。这样就保证了尽量按照先来先得的顺序获取锁。</p></blockquote>"},{"t":"什么情况会产生死锁问题？如何解决？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>死锁就是：<strong>多个线程互相持有对方需要的资源，谁也不释放，导致一直等待。</strong></p><ol><li><strong>什么情况会产生死锁</strong></li></ol><p>最典型场景是：<strong>多个线程加锁顺序不一致</strong>。</p><p>比如：</p><pre><code>线程 A：先拿锁 1，再等锁 2<br/>线程 B：先拿锁 2，再等锁 1</code></pre><p>结果：</p><pre><code>A 等 B 释放锁 2<br/>B 等 A 释放锁 1</code></pre><p>就死锁了。</p><ol><li><strong>死锁的四个必要条件</strong></li></ol><p>死锁发生通常同时满足：</p><ul><li><strong>互斥条件</strong><ul><li>锁一次只能被一个线程持有。</li></ul></li><li><strong>请求并保持</strong><ul><li>已经拿着一把锁，还继续请求另一把锁。</li></ul></li><li><strong>不可剥夺</strong><ul><li>锁不能被强制抢走，只能持有者主动释放。</li></ul></li><li><strong>循环等待</strong><ul><li>多个线程形成环形等待关系。</li></ul></li></ul><ol><li><strong>如何避免 / 解决</strong></li></ol><p><strong>固定加锁顺序</strong></p><p>所有线程都按相同顺序拿锁。</p><p>比如统一规定：</p><pre><code>先锁账户 ID 小的，再锁账户 ID 大的</code></pre><p>可以破坏循环等待。</p><p><strong>减少锁持有时间</strong></p><ul><li>临界区尽量小。</li><li>锁里不要做耗时操作。</li><li>不要在锁里做：<ul><li>IO</li><li>RPC</li><li>数据库慢查询</li><li>sleep</li></ul></li></ul><p><strong>使用 tryLock / 超时锁</strong></p><p>不要无限等待。</p><pre><code>if (lock.tryLock(3, TimeUnit.SECONDS)) {<br/>    try {<br/>        // 业务逻辑<br/>    } finally {<br/>        lock.unlock();<br/>    }<br/>} else {<br/>    // 获取锁失败，重试或降级<br/>}</code></pre><p>可以避免线程一直卡死。</p><p><strong>避免锁嵌套</strong></p><p>能不嵌套就不嵌套。</p><p>如果必须嵌套，要保证顺序一致。</p><p><strong>发生死锁后排查</strong></p><p>Java 中可以用：</p><pre><code>jstack</code></pre><p>或者：</p><pre><code>jcmd</code></pre><p>查看线程栈，定位哪些线程持有哪些锁、等待哪些锁。</p><p><strong>数据库死锁</strong></p><p>MySQL 中也可能死锁。</p><p>解决思路：</p><ul><li>事务尽量短。</li><li>按固定顺序更新数据。</li><li>给查询条件加索引，减少锁范围。</li><li>捕获死锁异常后重试。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>死锁通常发生在多个线程持有部分资源，同时又等待对方释放资源的场景，最典型的是多个线程获取锁的顺序不一致。死锁产生需要满足互斥、请求并保持、不可剥夺、循环等待四个条件。解决思路是破坏其中一个条件，实际开发中常用固定加锁顺序、减少锁持有时间、避免锁嵌套、使用 tryLock 或超时锁。发生死锁后可以用 jstack 查看线程栈定位。数据库中则要缩短事务、按固定顺序更新数据、加合适索引，并在死锁后进行重试。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"线程池","questions":[{"t":"介绍一下线程池的工作原理","tag":"","p":"","html":"<p>线程池的核心作用是：<strong>复用线程，控制并发数量，避免频繁创建和销毁线程。</strong></p><p>以 <code>ThreadPoolExecutor</code> 为例，工作原理可以这样讲。</p><ol><li><strong>线程池核心参数</strong></li></ol><p>常见参数有：</p><ul><li><code>corePoolSize</code>：核心线程数。</li><li><code>maximumPoolSize</code>：最大线程数。</li><li><code>workQueue</code>：任务队列。</li><li><code>keepAliveTime</code>：非核心线程空闲存活时间。</li><li><code>threadFactory</code>：线程创建工厂。</li><li><code>handler</code>：拒绝策略。</li></ul><ol><li><strong>提交任务后的执行流程</strong></li></ol><p>当调用：</p><pre><code>executor.execute(task);</code></pre><p>线程池大致按这个顺序处理：</p><p><strong>第一步：核心线程没满，创建核心线程</strong></p><p>如果当前线程数 <code>&lt; corePoolSize</code>：</p><pre><code>创建核心线程执行任务</code></pre><p>即使其他核心线程空闲，也可能优先创建到核心数。</p><p><strong>第二步：核心线程已满，任务进入队列</strong></p><p>如果当前线程数已经达到 <code>corePoolSize</code>：</p><pre><code>任务放入 workQueue</code></pre><p>等待空闲线程来取。</p><p><strong>第三步：队列满了，创建非核心线程</strong></p><p>如果队列也满了，并且当前线程数 <code>&lt; maximumPoolSize</code>：</p><pre><code>创建非核心线程执行任务</code></pre><p><strong>第四步：线程数也到最大，执行拒绝策略</strong></p><p>如果队列满了，线程数也达到 <code>maximumPoolSize</code>：</p><pre><code>执行拒绝策略</code></pre><p>比如抛异常、调用者执行、丢弃任务等。</p><ol><li><strong>工作线程如何执行任务</strong></li></ol><p>线程池里的工作线程会循环从队列中取任务执行：</p><pre><code>取任务 → 执行任务 → 再取任务 → 再执行</code></pre><p>这就是线程复用。</p><p>核心线程默认不会被回收。</p><p>非核心线程空闲超过 <code>keepAliveTime</code> 会被回收。</p><p>如果设置：</p><pre><code>allowCoreThreadTimeOut(true)</code></pre><p>核心线程也可以超时回收。</p><ol><li><strong>拒绝策略</strong></li></ol><p>常见拒绝策略：</p><ul><li><code>AbortPolicy</code><ul><li>默认，直接抛异常。</li></ul></li><li><code>CallerRunsPolicy</code><ul><li>由提交任务的线程自己执行。</li></ul></li><li><code>DiscardPolicy</code><ul><li>直接丢弃任务，不抛异常。</li></ul></li><li><code>DiscardOldestPolicy</code><ul><li>丢弃队列中最老的任务，再尝试提交新任务。</li></ul></li></ul><ol><li><strong>为什么要用线程池</strong></li></ol><ul><li>减少线程创建销毁开销。</li><li>控制并发线程数量。</li><li>防止无限创建线程打爆系统。</li><li>统一管理任务执行。</li><li>可以配合队列削峰。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>线程池的核心原理是复用线程和控制并发。以 ThreadPoolExecutor 为例，提交任务后，如果当前线程数小于 corePoolSize，就创建核心线程执行；如果核心线程满了，就把任务放入阻塞队列；如果队列也满了，并且线程数还小于 maximumPoolSize，就创建非核心线程执行；如果线程数达到最大且队列也满了，就触发拒绝策略。工作线程执行完任务后不会马上销毁，而是继续从队列中取任务执行，从而实现线程复用。非核心线程空闲超过 keepAliveTime 会被回收。线程池可以减少线程创建销毁开销，控制并发数量，防止资源耗尽。</p></blockquote>"},{"t":"线程池的参数有哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p><code>ThreadPoolExecutor</code> 常见核心参数有 <strong>7 个</strong>：</p><p><strong>1. </strong><strong><code>corePoolSize</code></strong></p><ul><li>核心线程数。</li><li>线程池长期保留的线程数量。</li><li>当线程数小于 <code>corePoolSize</code> 时，提交任务会优先创建核心线程执行。</li></ul><p><strong>2. </strong><strong><code>maximumPoolSize</code></strong></p><ul><li>最大线程数。</li><li>线程池最多能创建多少线程。</li><li>当核心线程满了、队列也满了，才会继续创建非核心线程，直到 <code>maximumPoolSize</code>。</li></ul><p><strong>3. </strong><strong><code>keepAliveTime</code></strong></p><ul><li>非核心线程空闲存活时间。</li><li>非核心线程空闲超过这个时间会被回收。</li><li>如果设置 <code>allowCoreThreadTimeOut(true)</code>，核心线程也可以超时回收。</li></ul><p><strong>4. </strong><strong><code>unit</code></strong></p><ul><li><code>keepAliveTime</code> 的时间单位。</li><li>比如秒、毫秒等。</li></ul><pre><code>TimeUnit.SECONDS</code></pre><p><strong>5. </strong><strong><code>workQueue</code></strong></p><ul><li>任务队列。</li><li>当核心线程都忙时，新任务会先进入队列。</li></ul><p>常见队列：</p><ul><li><code>ArrayBlockingQueue</code></li><li><code>LinkedBlockingQueue</code></li><li><code>SynchronousQueue</code></li><li><code>PriorityBlockingQueue</code></li></ul><p><strong>6. </strong><strong><code>threadFactory</code></strong></p><ul><li>线程工厂。</li><li>用来创建线程。</li><li>可以自定义线程名称、是否守护线程、异常处理器等。</li></ul><p><strong>7. </strong><strong><code>handler</code></strong></p><ul><li>拒绝策略。</li><li>当线程数达到最大，队列也满了，新任务会触发拒绝策略。</li></ul><p>常见策略：</p><ul><li><code>AbortPolicy</code>：默认，抛异常。</li><li><code>CallerRunsPolicy</code>：调用者线程执行任务。</li><li><code>DiscardPolicy</code>：直接丢弃。</li><li><code>DiscardOldestPolicy</code>：丢弃队列最老任务。</li></ul><p><strong>构造方法示例</strong></p><pre><code>new ThreadPoolExecutor(<br/>    10,                       // corePoolSize<br/>    20,                       // maximumPoolSize<br/>    60,                       // keepAliveTime<br/>    TimeUnit.SECONDS,          // unit<br/>    new ArrayBlockingQueue&lt;&gt;(100),<br/>    threadFactory,<br/>    new ThreadPoolExecutor.AbortPolicy()<br/>);</code></pre><p><strong>面试背诵版</strong></p><blockquote><p>ThreadPoolExecutor 主要有 7 个参数：</p><ol><li>corePoolSize 核心线程数，</li><li>maximumPoolSize 最大线程数，</li><li>keepAliveTime 非核心线程空闲存活时间，</li><li>unit 时间单位，</li><li>workQueue 任务队列，</li><li>threadFactory 线程工厂，</li><li>handler 拒绝策略。</li></ol><p>任务提交后，<strong>（必背）</strong>先看核心线程是否满，没满就创建核心线程；满了就进队列；队列满了再创建非核心线程；线程数达到最大后就执行拒绝策略。线程工厂一般用于设置线程名称和异常处理，拒绝策略用于处理线程池无法接收新任务的情况。</p></blockquote>"},{"t":"线程池工作队列满了有哪些拒绝策略？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>ThreadPoolExecutor</code> 常见有 <strong>4 种拒绝策略</strong>。</p><p>当满足这两个条件时会触发拒绝策略：</p><pre><code>任务队列满了<br/>并且线程数已经达到 maximumPoolSize</code></pre><p><strong>1. </strong><strong><code>AbortPolicy</code></strong></p><p>默认策略。</p><ul><li>直接抛出 <code>RejectedExecutionException</code>。</li><li>让调用方感知任务提交失败。</li></ul><pre><code>new ThreadPoolExecutor.AbortPolicy()</code></pre><p>适合：</p><ul><li>任务不能丢。</li><li>希望失败后由上层处理。</li></ul><p><strong>2. </strong><strong><code>CallerRunsPolicy</code></strong></p><ul><li>不丢任务。</li><li>由提交任务的线程自己执行这个任务。</li></ul><pre><code>new ThreadPoolExecutor.CallerRunsPolicy()</code></pre><p>好处：</p><ul><li>可以降低提交速度。</li><li>有一定削峰效果。</li></ul><p>缺点：</p><ul><li>可能拖慢调用线程。</li><li>如果调用线程是主业务线程，要谨慎。</li></ul><p><strong>3. </strong><strong><code>DiscardPolicy</code></strong></p><ul><li>直接丢弃新任务。</li><li>不抛异常。</li></ul><pre><code>new ThreadPoolExecutor.DiscardPolicy()</code></pre><p>适合：</p><ul><li>任务允许丢失。</li><li>比如某些日志、监控、非核心统计任务。</li></ul><p><strong>4. </strong><strong><code>DiscardOldestPolicy</code></strong></p><ul><li>丢弃队列中最老的任务。</li><li>然后尝试重新提交当前任务。</li></ul><pre><code>new ThreadPoolExecutor.DiscardOldestPolicy()</code></pre><p>适合：</p><ul><li>更关注最新任务。</li><li>老任务过期价值不大。</li></ul><p>但要注意：</p><ul><li>可能导致老任务一直被丢。</li><li>如果队列是优先级队列，使用要谨慎。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>线程池常见拒绝策略有四种：AbortPolicy 是默认策略，直接抛 RejectedExecutionException；CallerRunsPolicy 是由提交任务的线程自己执行任务，不丢任务，也能降低提交速度；DiscardPolicy 是直接丢弃新任务，不抛异常；DiscardOldestPolicy 是丢弃队列中最老的任务，然后重新提交当前任务。拒绝策略会在线程池队列满且线程数达到 maximumPoolSize 时触发。实际项目中如果任务不能丢，通常用 AbortPolicy 或自定义拒绝策略；如果允许降速，可以考虑 CallerRunsPolicy。</p></blockquote>"},{"t":"有线程池参数设置的经验吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>面试可以这样答：<strong>线程池参数不是固定公式，要根据任务类型、机器资源和下游承载能力来设置。</strong></p><ol><li><strong>先区分任务类型</strong></li></ol><p><strong>CPU 密集型</strong></p><ul><li>主要消耗 CPU，比如计算、加密、压缩。</li><li>线程数不宜太多。</li><li>一般接近 CPU 核数。</li></ul><span>[原文档此处有配图]</span><p>常见设置：</p><p>线程数 ≈ CPU 核数 + 1<br/>原因：对于 <strong>CPU 密集型任务</strong>，线程数通常设置为 <strong>CPU 核数 + 1</strong>，主要是为了在极少数情况下，当某个线程因为缺页、GC、操作系统调度等原因暂时阻塞时，额外线程可以继续利用 CPU，提高 CPU 利用率。</p><p><strong>IO 密集型</strong></p><span>[原文档此处有配图]</span><ul><li>大量时间在等待 IO，比如数据库、Redis、HTTP 调用。</li><li>可以设置得比 CPU 核数大一些。</li><li>因为线程等待 IO 时，CPU 可以执行其他线程。</li></ul><p>常见经验：</p><pre><code>线程数 ≈ CPU 核数 * 2</code></pre><ol><li><strong>队列不要无界</strong></li></ol><p>不建议随便用无界队列，比如默认很大的 <code>LinkedBlockingQueue</code>。</p><p>原因：</p><ul><li>请求堆积时任务会无限排队。</li><li>可能导致内存膨胀。</li><li>最终 OOM。</li><li>延迟越来越高。</li></ul><p>更推荐：</p><ul><li>有界队列。</li><li>配合合理拒绝策略。</li><li>做监控告警。</li></ul><ol><li><strong>最大线程数要结合下游能力</strong></li></ol><p>不是线程越多越好。</p><p>如果线程池里任务要访问数据库，而数据库连接池只有 50 个连接：</p><pre><code>线程池开 500 个线程也没意义</code></pre><p>大量线程只会阻塞等待连接。</p><p>要考虑：</p><ul><li>数据库连接池大小。</li><li>Redis 连接数。</li><li>下游接口 QPS。</li><li>机器 CPU / 内存。</li><li>业务可接受延迟。</li></ul><ol><li><strong>拒绝策略要按业务选</strong></li></ol><ul><li>任务不能丢：<code>AbortPolicy</code> + 上层降级/重试。</li><li>可由调用线程执行：<code>CallerRunsPolicy</code>。</li><li>允许丢弃：<code>DiscardPolicy</code>。</li><li>只关心最新任务：<code>DiscardOldestPolicy</code>。</li><li>实际项目中也常自定义拒绝策略，记录日志、打点、告警。</li></ul><ol><li><strong>线程命名和异常处理</strong></li></ol><p>要自定义 <code>ThreadFactory</code>：</p><ul><li>设置线程名前缀。</li><li>设置异常处理器。</li><li>方便排查问题。</li></ul><p>比如：</p><pre><code>order-pay-pool-1<br/>order-pay-pool-2</code></pre><ol><li><strong>监控和动态调整</strong></li></ol><p>上线后要看指标：</p><ul><li>活跃线程数。</li><li>队列长度。</li><li>任务执行耗时。</li><li>拒绝次数。</li><li>CPU 使用率。</li><li>GC 情况。</li><li>下游响应时间。</li></ul><p>根据监控结果调参，而不是一次设置完就不管。</p><p><strong>面试背诵版</strong></p><blockquote><p>有。线程池参数设置我一般先看任务类型，CPU 密集型线程数接近 CPU 核数，比如 N+1；IO 密集型可以更大，比如 2N 估算。队列一定要有界，避免任务无限堆积导致 OOM。最大线程数还要结合下游资源，比如数据库连接池、Redis 连接数、接口限流能力，不是越大越好。拒绝策略根据业务选择，不能丢的任务要抛异常或自定义处理，可降速的可以用 CallerRunsPolicy。实际项目还要自定义线程名、做好异常处理，并通过监控活跃线程数、队列长度、拒绝次数和任务耗时来持续调整。</p></blockquote>"},{"t":"核心线程数设置为0可不可以？","tag":"","p":"","html":"<p>可以，<code>corePoolSize</code> <strong>可以设置为 0</strong>，但要看队列类型和使用场景。</p><ol><li><strong>corePoolSize = 0 是什么意思</strong></li></ol><p>表示线程池没有常驻核心线程。</p><p>也就是说：</p><ul><li>没任务时，线程池可以一个线程都没有。</li><li>有任务来时，再根据规则创建线程执行。</li><li>空闲线程超过 <code>keepAliveTime</code> 后会被回收。</li></ul><p>适合：</p><ul><li>任务很少。</li><li>任务不连续。</li><li>希望空闲时不占线程资源。</li></ul><ol><li><strong>要注意任务队列</strong></li></ol><p>如果使用普通有界队列，比如：</p><pre><code>new LinkedBlockingQueue&lt;&gt;()</code></pre><p>当 <code>corePoolSize = 0</code> 时，任务提交后可能先进入队列，然后线程池再创建线程处理。</p><p>但如果配置不合理，可能出现任务堆积或执行不及时。</p><p>如果使用：</p><pre><code>SynchronousQueue</code></pre><p>就更像缓存线程池：</p><ul><li>队列不存任务。</li><li>来一个任务就交给线程。</li><li>没有空闲线程就创建新线程。</li><li>最大线程数控制上限。</li></ul><p>类似：</p><pre><code>Executors.newCachedThreadPool()</code></pre><p>它的核心线程数就是 <code>0</code>。</p><ol><li><strong>示例</strong></li></ol><pre><code>new ThreadPoolExecutor(<br/>    0,<br/>    100,<br/>    60L,<br/>    TimeUnit.SECONDS,<br/>    new SynchronousQueue&lt;&gt;()<br/>);</code></pre><p>这类线程池适合短时间大量突发任务，但要小心最大线程数不能太大，否则可能创建过多线程。</p><ol><li><strong>风险</strong></li></ol><ul><li>任务刚来时可能有线程创建延迟。</li><li>如果最大线程数设置过大，可能瞬间创建大量线程。</li><li>如果队列是无界队列，任务可能堆积。</li><li>不适合持续高频任务，因为线程频繁创建销毁也有开销。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>corePoolSize 可以设置为 0，表示线程池没有常驻核心线程，空闲时可以不保留线程。典型例子是 cachedThreadPool，核心线程数为 0，配合 SynchronousQueue，任务来了就创建线程执行，空闲超过 keepAliveTime 后回收。它适合任务不连续、突发性强的场景。但要注意最大线程数和队列配置，如果 maximumPoolSize 太大可能创建过多线程，如果队列不合适可能导致任务堆积或执行延迟。持续高频任务一般不建议把核心线程数设为 0。</p></blockquote>"},{"t":"线程池种类有哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Java 线程池常见有这几种：</p><p><strong>1. </strong><strong><code>FixedThreadPool</code></strong></p><p>固定大小线程池。</p><pre><code>Executors.newFixedThreadPool(10);</code></pre><p>特点：</p><ul><li>核心线程数 = 最大线程数。</li><li>线程数量固定。</li><li>适合稳定并发量的任务。</li></ul><p>注意：</p><ul><li>默认使用无界队列，任务堆积可能导致 OOM。</li></ul><p><strong>2. </strong><strong><code>CachedThreadPool</code></strong></p><p>缓存线程池。</p><pre><code>Executors.newCachedThreadPool();</code></pre><p>特点：</p><ul><li>核心线程数是 <code>0</code>。</li><li>最大线程数非常大。</li><li>空闲线程会回收。</li><li>适合短生命周期、突发性任务。</li></ul><p>注意：</p><ul><li>任务太多时可能创建大量线程，导致资源耗尽。</li></ul><p><strong>3. </strong><strong><code>SingleThreadExecutor</code></strong></p><p>单线程线程池。</p><pre><code>Executors.newSingleThreadExecutor();</code></pre><p>特点：</p><ul><li>只有一个工作线程。</li><li>任务按顺序执行。</li><li>适合需要串行执行的场景。</li></ul><p>注意：</p><ul><li>默认无界队列，任务堆积也可能 OOM。</li></ul><p><strong>4. </strong><strong><code>ScheduledThreadPool</code></strong></p><p>定时任务线程池。</p><pre><code>Executors.newScheduledThreadPool(5);</code></pre><p>特点：</p><ul><li>支持延迟执行。</li><li>支持周期性执行。</li><li>适合定时任务、周期任务。</li></ul><p><strong>5. </strong><strong><code>WorkStealingPool</code></strong></p><p>工作窃取线程池。</p><pre><code>Executors.newWorkStealingPool();</code></pre><p>特点：</p><ul><li>底层基于 <code>ForkJoinPool</code>。</li><li>适合任务可以拆分、并行计算的场景。</li><li>空闲线程可以从其他队列“偷任务”。</li></ul><ol><li><strong>ForkJoinPool</strong></li></ol><p>分治任务线程池。</p><pre><code>ForkJoinPool</code></pre><p>适合：</p><ul><li>大任务拆成小任务。</li><li>并行计算。</li><li>递归任务，比如大数组求和。</li></ul><ol><li><strong>面试提醒</strong></li></ol><p>实际开发不太建议直接用 <code>Executors</code> 快捷方法。</p><p>原因：</p><ul><li>有些默认使用无界队列。</li><li>有些最大线程数过大。</li><li>容易导致 OOM。</li></ul><p>更推荐手动创建：</p><pre><code>new ThreadPoolExecutor(...)</code></pre><p>明确设置：</p><ul><li>核心线程数</li><li>最大线程数</li><li>有界队列</li><li>线程工厂</li><li>拒绝策略</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Java 常见线程池有 FixedThreadPool、CachedThreadPool、SingleThreadExecutor、ScheduledThreadPool、WorkStealingPool 和 ForkJoinPool。FixedThreadPool 是固定线程数，适合稳定并发任务；CachedThreadPool 核心线程数为 0，适合短时间突发任务，但可能创建大量线程；SingleThreadExecutor 单线程串行执行任务；ScheduledThreadPool 用于定时和周期任务；WorkStealingPool 和 ForkJoinPool 适合任务拆分和并行计算。实际开发中不建议直接用 Executors 创建线程池，因为可能有无界队列或线程数过大的风险，更推荐手动使用 ThreadPoolExecutor 配置有界队列和拒绝策略。</p></blockquote>"},{"t":"线程池一般是怎么用的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>线程池一般这样用：<strong>不要每次 new Thread，而是提前创建线程池，把任务提交进去执行。</strong></p><ol><li><strong>推荐用 </strong><strong><code>ThreadPoolExecutor</code></strong></li></ol><p>实际项目里一般不直接用 <code>Executors</code>，而是手动配置参数：</p><pre><code>ThreadPoolExecutor executor = new ThreadPoolExecutor(<br/>    10,<br/>    20,<br/>    60L,<br/>    TimeUnit.SECONDS,<br/>    new ArrayBlockingQueue&lt;&gt;(100),<br/>    new ThreadFactoryBuilder().setNameFormat(\"order-pool-%d\").build(),<br/>    new ThreadPoolExecutor.CallerRunsPolicy()<br/>);</code></pre><p>如果不用 Guava 的 <code>ThreadFactoryBuilder</code>，可以自己实现 <code>ThreadFactory</code>。</p><ol><li><strong>提交任务</strong></li></ol><p>没有返回值：</p><pre><code>executor.execute(() -&gt; {<br/>    // 执行业务逻辑<br/>});</code></pre><p>有返回值：</p><pre><code>Future&lt;Integer&gt; future = executor.submit(() -&gt; {<br/>    return 1 + 1;<br/>});<br/><br/>Integer result = future.get();</code></pre><p>异步编排也可以用：</p><pre><code>CompletableFuture.supplyAsync(() -&gt; queryUser(), executor);</code></pre><ol><li><strong>常见使用场景</strong></li></ol><ul><li>异步处理日志。</li><li>批量任务并行处理。</li><li>调用多个接口并行查询。</li><li>消息消费。</li><li>定时任务执行。</li><li>IO 密集型任务。</li></ul><ol><li><strong>使用注意点</strong></li></ol><ul><li>不要用无界队列，防止任务堆积 OOM。</li><li>不要无限创建线程。</li><li>自定义线程名称，方便排查问题。</li><li>设置合理拒绝策略。</li><li>任务内部要捕获异常，避免异常丢失。</li><li>应用关闭时要调用：</li></ul><pre><code>executor.shutdown();</code></pre><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>线程池一般用于复用线程、控制并发数量。实际项目中通常使用 ThreadPoolExecutor 手动创建线程池，明确配置核心线程数、最大线程数、有界队列、线程工厂和拒绝策略，而不是直接使用 Executors。无返回值任务用 execute，有返回值任务用 submit，也可以配合 CompletableFuture 做异步编排。使用时要注意线程数不能过大，队列要有界，线程要命名，任务异常要处理，服务关闭时要优雅 shutdown。</p></blockquote>"},{"t":"线程池中shutdown ()，shutdownNow()这两个方法有什么作用？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>shutdown()</code> 和 <code>shutdownNow()</code> 都是用来<strong>关闭线程池</strong>的，但关闭方式不一样。</p><p><strong>1. </strong><strong><code>shutdown()</code></strong><strong>：优雅关闭</strong></p><p>调用后：</p><ul><li>线程池不再接收新任务。</li><li>已经提交的任务会继续执行。</li><li>队列里的任务也会继续执行。</li><li>等所有任务执行完后，线程池才真正关闭。</li></ul><pre><code>executor.shutdown();</code></pre><p>适合：</p><ul><li>希望任务正常执行完。</li><li>服务优雅停机。</li></ul><p><strong>2.</strong><strong> </strong><strong><code>shutdownNow()</code></strong><strong>：尝试立即关闭</strong></p><p>调用后：</p><ul><li>不再接收新任务。</li><li>尝试中断正在执行的任务。</li><li>队列中还没开始执行的任务会被返回。</li><li>不保证正在执行的任务一定能停下来。</li></ul><pre><code>List&lt;Runnable&gt; notStartedTasks = executor.shutdownNow();</code></pre><p>为什么不保证？</p><p>因为它只是对工作线程调用 <code>interrupt()</code>。</p><p>如果任务不响应中断，比如：</p><pre><code>while (true) {<br/>    // 不检查中断<br/>}</code></pre><p>那它可能还会继续跑。</p><ol><li><strong>对比</strong></li></ol><sheet></sheet><ol><li><strong>常配合 </strong><strong><code>awaitTermination()</code></strong></li></ol><p>关闭后可以等待一段时间：</p><pre><code>executor.shutdown();<br/><br/>if (!executor.awaitTermination(30, TimeUnit.SECONDS)) {<br/>    executor.shutdownNow();<br/>}</code></pre><p>意思是：</p><ul><li>先优雅关闭。</li><li>等 30 秒。</li><li>如果还没关闭，再尝试强制关闭。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>shutdown 和 shutdownNow 都用于关闭线程池。shutdown 是优雅关闭，调用后不再接收新任务，但已经提交和队列中的任务会继续执行，直到执行完线程池才终止。shutdownNow 是尝试立即关闭，它会停止接收新任务，返回队列中还没执行的任务，并对正在执行的线程调用 interrupt，但不保证任务一定会停止，因为任务必须响应中断。实际使用中通常先 shutdown，再配合 awaitTermination 等待一段时间，如果超时还没结束，再调用 shutdownNow。</p></blockquote>"},{"t":"提交给线程池中的任务可以被撤回吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>可以，但要看任务处于什么状态，以及你是怎么提交的。</p><ol><li><strong>用 </strong><strong><code>submit()</code></strong><strong> 提交，可以通过 </strong><strong><code>Future.cancel()</code></strong><strong> 撤回</strong></li></ol><pre><code>Future&lt;?&gt; future = executor.submit(() -&gt; {<br/>    // task<br/>});<br/><br/>future.cancel(true);</code></pre><p><code>cancel(true)</code> 的含义：</p><ul><li>如果任务还在队列中没执行：可以取消。</li><li>如果任务正在执行：会尝试中断执行它的线程。</li><li>如果任务已经执行完：取消失败。</li></ul><p><strong>2. </strong><strong><code>cancel(false)</code></strong><strong> 和 </strong><strong><code>cancel(true)</code></strong><strong> 区别</strong></p><pre><code>future.cancel(false);</code></pre><ul><li>只取消还没开始执行的任务。</li><li>如果任务已经开始执行，不会中断。</li></ul><pre><code>future.cancel(true);</code></pre><ul><li>如果任务正在执行，会尝试 <code>interrupt()</code> 对应线程。</li><li>但能不能停，取决于任务是否响应中断。</li></ul><ol><li><strong>如果任务已经在运行，不一定能撤回</strong></li></ol><p>比如任务代码是：</p><pre><code>while (true) {<br/>    // 不检查中断<br/>}</code></pre><p>即使调用：</p><pre><code>future.cancel(true);</code></pre><p>它也可能继续跑。</p><p>更好的写法：</p><pre><code>while (!Thread.currentThread().isInterrupted()) {<br/>    // 执行任务<br/>}</code></pre><p>或者在 <code>sleep</code>、<code>wait</code>、<code>BlockingQueue.take</code> 抛出 <code>InterruptedException</code> 后退出。</p><ol><li><strong>用 </strong><strong><code>execute()</code></strong><strong> 提交不方便撤回</strong></li></ol><pre><code>executor.execute(task);</code></pre><p><code>execute()</code> 不返回 <code>Future</code>，所以不能直接通过 <code>Future.cancel()</code> 取消。</p><p>如果要支持撤回，建议用 <code>submit()</code>，或者自己保存任务引用并从队列里 <code>remove()</code>。</p><ol><li><strong>队列中的任务可以 remove</strong></li></ol><p>如果任务还没执行，可以尝试：</p><pre><code>executor.getQueue().remove(task);</code></pre><p>但实际项目中更常用 <code>Future.cancel()</code>。</p><p><strong>面试背诵版</strong></p><blockquote><p>提交到线程池的任务可以撤回，但取决于任务状态。用 submit 提交会返回 Future，可以调用 cancel 取消任务。如果任务还在队列中没执行，通常可以取消；如果任务正在执行，cancel(true) 只是尝试 interrupt 线程，能不能停止取决于任务是否响应中断；如果任务已经完成，则无法取消。cancel(false) 不会中断正在执行的任务，cancel(true) 会尝试中断。execute 提交没有返回 Future，不方便撤回，所以需要取消任务时一般用 submit。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"场景","questions":[{"t":"多线程打印奇偶数，怎么控制打印的顺序","tag":"必须会！这个要自己手撕哦","p":"","html":"<span>[原文档此处有配图]</span><p>面试可以说：<strong>用共享计数器 + 线程通信控制顺序</strong>。常见做法有 <code>synchronized + wait/notify</code>，或者 <code>Lock + Condition</code>。</p><p>下面给你一个最经典的写法：两个线程交替打印 <code>1~10</code>。</p><p><strong>1. </strong><strong><code>synchronized + wait/notify</code></strong></p><pre><code>public class PrintOddEven {<br/>    private static final Object lock = new Object();<br/>    private static int num = 1;<br/>    private static final int MAX = 10;<br/><br/>    public static void main(String[] args) {<br/>        Thread oddThread = new Thread(() -&gt; print(true), \"奇数线程\");<br/>        Thread evenThread = new Thread(() -&gt; print(false), \"偶数线程\");<br/><br/>        oddThread.start();<br/>        evenThread.start();<br/>    }<br/><br/>    private static void print(boolean printOdd) {<br/>        while (true) {<br/>            synchronized (lock) {<br/>                while (num &lt;= MAX &amp;&amp; (num % 2 == 1) != printOdd) {<br/>                    try {<br/>                        lock.wait();<br/>                    } catch (InterruptedException e) {<br/>                        Thread.currentThread().interrupt();<br/>                        return;<br/>                    }<br/>                }<br/><br/>                if (num &gt; MAX) {<br/>                    lock.notifyAll();<br/>                    return;<br/>                }<br/><br/>                System.out.println(Thread.currentThread().getName() + \": \" + num);<br/>                num++;<br/>                lock.notifyAll();<br/>            }<br/>        }<br/>    }<br/>}</code></pre><ol><li><strong>控制顺序的核心</strong></li></ol><ul><li>用 <code>num</code> 判断当前应该谁打印。</li><li>奇数线程只打印奇数。</li><li>偶数线程只打印偶数。</li><li>不该自己打印时调用 <code>wait()</code> 等待。</li><li>打印完后 <code>notifyAll()</code> 唤醒另一个线程。</li></ul><ol><li><strong>为什么用 </strong><strong><code>while</code></strong><strong> 不用 </strong><strong><code>if</code></strong></li></ol><pre><code>while (条件不满足) {<br/>    lock.wait();<br/>}</code></pre><p>原因：</p><ul><li>防止虚假唤醒。</li><li>被唤醒后条件不一定满足。</li><li>所以醒来后要重新判断。</li></ul><ol><li><strong>面试回答版</strong></li></ol><blockquote><p>可以用两个线程共享一个计数器 num，然后通过 synchronized 配合 wait/notify 控制顺序。奇数线程判断 num 是奇数才打印，否则 wait；偶数线程判断 num 是偶数才打印，否则 wait。每次打印后 num++，然后 notifyAll 唤醒另一个线程。wait 要放在 while 循环中，防止虚假唤醒。也可以用 ReentrantLock 配合两个 Condition 实现，思路类似。</p></blockquote>"},{"t":"单例模型既然已经用了synchronized，为什么还要在加volatile？","tag":"","p":"","html":"<p>这里说的是<strong>双重检查锁 DCL 单例</strong>。即使用了 <code>synchronized</code>，<code>instance</code> 仍然需要加 <code>volatile</code>，<strong>主要是为了防止指令重排序，避免其他线程拿到“半初始化对象”。</strong></p><span>[原文档此处有配图]</span><ol><li><strong>典型代码（必须会！自己抄几遍）</strong></li></ol><pre><code>public class Singleton {<br/>    private static volatile Singleton instance;<br/><br/>    private Singleton() {}<br/><br/>    public static Singleton getInstance() {<br/>        if (instance == null) {<br/>            synchronized (Singleton.class) {<br/>                if (instance == null) {<br/>                    instance = new Singleton();<br/>                }<br/>            }<br/>        }<br/>        return instance;<br/>    }<br/>}</code></pre><ol><li><strong>new 对象不是一步完成的</strong></li></ol><p>这句代码：</p><pre><code>instance = new Singleton();</code></pre><p>底层大致分三步：</p><pre><code>1. 分配内存<br/>2. 初始化对象<br/>3. 把内存地址赋值给 instance</code></pre><p>正常顺序应该是：</p><pre><code>1 → 2 → 3</code></pre><p>但如果没有 <code>volatile</code>，可能发生指令重排序：</p><pre><code>1 → 3 → 2</code></pre><p>也就是：</p><ul><li>先分配内存。</li><li>再把地址赋给 <code>instance</code>。</li><li>但对象还没初始化完成。</li></ul><ol><li><strong>会出什么问题</strong></li></ol><p>线程 A 进入锁中创建对象：</p><pre><code>分配内存<br/>instance 指向这块内存<br/>对象还没初始化完成</code></pre><p>这时线程 B 进来执行第一层判断：</p><pre><code>if (instance == null)</code></pre><p>发现 <code>instance != null</code>，就直接返回了。</p><p>结果线程 B 拿到的是一个<strong>还没初始化完成的对象</strong>。</p><ol><li><strong>synchronized 为什么不够</strong></li></ol><p><code>synchronized</code> 只保护了进入同步块的线程。</p><p>但双重检查锁外层有一层：</p><pre><code>if (instance == null)</code></pre><p>这个判断是在锁外面执行的。</p><p>线程 B 可能不进入 synchronized，直接读取 <code>instance</code>。</p><p>所以需要 <code>volatile</code> 保证：</p><ul><li><code>instance</code> 的写入对其他线程可见。</li><li>禁止 <code>new Singleton()</code> 中赋值和初始化发生重排序。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>双重检查锁单例中，即使用了 synchronized，也需要 volatile。因为 synchronized 只保护同步块内部，而第一次判断 instance 是否为空是在锁外面。<code>instance = new Singleton()</code> 不是原子操作，大致包括分配内存、初始化对象、把引用赋给 instance。如果没有 volatile，可能发生重排序，先把引用赋给 instance，再初始化对象。这样其他线程在锁外看到 instance 不为空，就可能拿到一个半初始化对象。volatile 可以禁止这种重排序，并保证 instance 对其他线程可见。</p></blockquote>"},{"t":"3个线程并发执行，1个线程等待这三个线程全部执行完在执行，怎么实现？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>最常用的是 <strong><code>CountDownLatch</code></strong>。</p><ol><li><strong>实现思路</strong></li></ol><ul><li>初始化计数器为 <code>3</code>。</li><li>三个线程每执行完一个，就调用一次 <code>countDown()</code>。</li><li>等待线程调用 <code>await()</code>。</li><li>当计数器减到 <code>0</code>，等待线程继续执行。</li></ul><ol><li><strong>示例代码</strong></li></ol><pre><code>CountDownLatch latch = new CountDownLatch(3);<br/><br/>for (int i = 0; i &lt; 3; i++) {<br/>    new Thread(() -&gt; {<br/>        try {<br/>            System.out.println(Thread.currentThread().getName() + \" 执行任务\");<br/>        } finally {<br/>            latch.countDown();<br/>        }<br/>    }).start();<br/>}<br/><br/>new Thread(() -&gt; {<br/>    try {<br/>        latch.await();<br/>        System.out.println(\"三个线程都执行完了，我再执行\");<br/>    } catch (InterruptedException e) {<br/>        Thread.currentThread().interrupt();<br/>    }<br/>}).start();</code></pre><ol><li><strong>为什么用 finally</strong></li></ol><p>因为即使子线程执行异常，也要调用 <code>countDown()</code>。</p><p>否则计数器可能永远减不到 <code>0</code>，等待线程会一直阻塞。</p><ol><li><strong>还可以用什么</strong></li></ol><p>也可以用：</p><ul><li><code>CompletableFuture.allOf()</code></li><li><code>join()</code></li><li><code>CyclicBarrier</code></li><li>线程池 + <code>Future</code></li></ul><p>比如 <code>CompletableFuture</code>：</p><pre><code>CompletableFuture&lt;Void&gt; f1 = CompletableFuture.runAsync(() -&gt; task1());<br/>CompletableFuture&lt;Void&gt; f2 = CompletableFuture.runAsync(() -&gt; task2());<br/>CompletableFuture&lt;Void&gt; f3 = CompletableFuture.runAsync(() -&gt; task3());<br/><br/>CompletableFuture.allOf(f1, f2, f3)<br/>    .thenRun(() -&gt; System.out.println(\"全部完成后执行\"));</code></pre><p><strong>面试回答版</strong></p><blockquote><p>可以用 <strong>CountDownLatch </strong>实现。初始化 CountDownLatch 为 3，三个工作线程每完成一个任务就调用 countDown，等待线程调用 await 阻塞等待。当计数器减到 0 时，等待线程被唤醒继续执行。实际写代码时，countDown 最好放在 finally 里，避免子线程异常导致等待线程一直阻塞。也可以用 CompletableFuture.allOf 或 Future 来实现。</p></blockquote>"},{"t":"假设两个线程并发读写同一个整型变量，初始值为零，每个线程加 50次，结果可能是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>如果没有任何同步控制，结果<strong>不一定是 100</strong>，可能是 <strong>50 到 100 之间的某个值</strong>。</p><ol><li><strong>为什么不是一定 100</strong></li></ol><p>因为：</p><pre><code>count++;</code></pre><p>不是原子操作。</p><p>它大致分三步：</p><pre><code>1. 读取 count<br/>2. count + 1<br/>3. 写回 count</code></pre><p>两个线程可能同时读到同一个旧值。</p><p>比如 <code>count = 0</code>：</p><pre><code>线程 A 读到 0<br/>线程 B 读到 0<br/>线程 A 加 1，写回 1<br/>线程 B 加 1，写回 1</code></pre><p>执行了两次加一，但结果只增加了 <code>1</code>，丢了一次更新。</p><ol><li><strong>结果可能是什么</strong></li></ol><p>两个线程各加 <code>50</code> 次，理论期望是：</p><pre><code>100</code></pre><p>但由于并发更新丢失，结果可能小于 100。</p><p>一般可以说：</p><pre><code>结果可能是 50 到 100 之间</code></pre><p>最小为什么可以理解为 50？</p><p>因为最极端情况下，每一轮两个线程都读到同一个值，然后分别写回相同的新值，相当于每两次加一只生效一次。</p><ol><li><strong>怎么保证结果是 100</strong></li></ol><p>可以用：</p><p><strong>synchronized</strong></p><pre><code>synchronized (lock) {<br/>    count++;<br/>}</code></pre><p><strong>AtomicInteger</strong></p><pre><code>AtomicInteger count = new AtomicInteger(0);<br/>count.incrementAndGet();</code></pre><p><strong>ReentrantLock</strong></p><pre><code>lock.lock();<br/>try {<br/>    count++;<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p><strong>面试背诵版</strong></p><blockquote><p>如果两个线程都对同一个普通 int 变量执行 50 次自增，最终结果不一定是 100，可能在 50 到 100 之间。因为 <code>count++</code> 不是原子操作，<strong>它包含读取、加一、写回三个步骤</strong>。两个线程可能同时读到同一个旧值，然后都写回相同的新值，导致一次更新丢失。要保证结果正确，可以使用 synchronized、ReentrantLock 或 AtomicInteger。</p></blockquote><span>[原文档此处有配图]</span>"}]}]};
