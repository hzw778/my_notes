window.TAB_DATA = window.TAB_DATA || {};
window.TAB_DATA["mysql"] = {"key":"mysql","title":"2小时速通MySQL","url":"https://utxc8uqzfk.feishu.cn/wiki/IbtNwN7oPipPHLk9cMMcHqMnnFf","chapters":[{"no":"","title":"SQL基础","questions":[{"t":"NOSQL和SQL的区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><blockquote><p>可以这样回答，适合面试时 1 分钟左右说明：</p><p><strong>核心区别</strong></p><ul><li><strong>SQL 数据库</strong>是关系型数据库，数据以<strong>表、行、列</strong>组织，强调结构化、强一致性和事务能力。</li><li><strong>NoSQL 数据库</strong>是非关系型数据库，数据模型更灵活，比如文档、键值、列族、图等，强调高扩展性、高并发和灵活存储。</li></ul><p><strong>对比回答</strong></p><ul><li><strong>数据模型不同</strong>：SQL 使用固定表结构，需要提前设计 schema；NoSQL schema 更灵活，适合字段变化频繁的业务。</li><li><strong>查询方式不同</strong>：SQL 使用标准化的 SQL 语言，适合复杂查询、多表 JOIN；<strong>NoSQL 通常使用各自 API 或查询语法，JOIN 能力较弱。</strong></li><li><strong>事务一致性不同</strong>：SQL 通常支持 ACID 事务，强一致性更好；NoSQL 更偏向 BASE 理论，常在可用性和分区容错下牺牲部分强一致性，但现在很多 NoSQL 也支持事务。</li><li><strong>扩展方式不同</strong>：SQL 传统上更擅长纵向扩展，比如提升单机配置；NoSQL 更容易做横向扩展，比如分片、集群。</li><li><strong>适用场景不同</strong>：SQL 适合金融、电商订单、库存等结构稳定、事务要求高的场景；NoSQL 适合日志、缓存、推荐、社交关系、内容存储等高并发、大数据量、结构灵活的场景。</li></ul><p></p></blockquote>"},{"t":"数据库三大范式是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试时你可以直接这样答：</p><p><strong>答法模板</strong></p><ul><li>数据库三大范式主要是用来<strong>规范表结构设计</strong>的，目的是<strong>减少数据冗余，避免数据更新、插入、删除异常</strong>。</li><li><strong>第一范式</strong>要求<strong>字段具有</strong><strong>原子性</strong>，也就是每一列都不能再拆分。<br/>比如手机号不能用一个字段存多个号码。</li><li><strong>第二范式</strong>是在第一范式基础上，<strong>要求非主键字段必须完全依赖主键，不能只依赖联合主键的一部分。</strong><br/>比如选课表的联合主键是学生 ID 和课程 ID，那么成绩依赖这两个字段，但学生姓名只依赖学生 ID，就应该拆到学生表里。</li><li><strong>第三范式</strong>是在第二范式基础上，<strong>要求非主键字段不能依赖其他非主键字段，也就是消除传递依赖。</strong><br/>比如学生表里有学院 ID、学院名称，学院名称其实依赖学院 ID，就应该把学院信息拆到学院表。</li><li>最后补一句：实际开发中一般会尽量满足第三范式，但也不是范式越高越好。有些读多写少、高并发查询场景下，会适当反范式，用冗余字段换查询性能。</li></ul></blockquote>"},{"t":"MySQL 怎么连表查询？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试时可以这样回答：</p><p><strong>答法模板</strong></p><ul><li>MySQL 连表查询主要用的是 <code>JOIN</code>，通过两张表之间的关联字段把数据查出来。</li><li>常见的连接方式有：<ul><li><code>INNER JOIN</code>：内连接，只返回两张表都匹配的数据。（取交集嘛）</li><li><code>LEFT JOIN</code>：左连接，返回左表全部数据，右表匹配不到则为 <code>NULL</code>。</li><li><code>RIGHT JOIN</code>：右连接，返回右表全部数据，左表匹配不到则为 <code>NULL</code>。</li></ul></li><li>实际开发中用得比较多的是 <code>INNER JOIN</code> 和 <code>LEFT JOIN</code>。</li></ul><p><strong>举个例子</strong></p><p>假设有两张表：</p><ul><li>用户表 <code>user</code>：<code>id, name</code></li><li>订单表 <code>orders</code>：<code>id, user_id, amount</code></li></ul><p>如果要查询用户和他的订单信息，可以写：</p><p>这就是内连接，只查有订单的用户。</p><p>如果想查所有用户，包括没有订单的用户，可以用左连接：</p><p><strong>可以补充一句</strong></p><ul><li>连表查询时要注意关联字段最好建立索引，比如 <code>orders.user_id</code>，否则数据量大时查询会比较慢。</li><li>如果是多表关联，也可以继续加 <code>JOIN</code>，但要注意 SQL 可读性和性能。</li></ul><p><strong>精简背诵版</strong></p><blockquote><p>MySQL 连表查询主要通过 JOIN 实现，常见有 INNER JOIN、LEFT JOIN、RIGHT JOIN。INNER JOIN 返回两边都匹配的数据，LEFT JOIN 返回左表全部数据，右表没有匹配则为 NULL。实际开发中常用 INNER JOIN 和 LEFT JOIN，关联条件一般写在 ON 后面，同时要注意关联字段加索引，避免数据量大时查询变慢。</p></blockquote></blockquote>"},{"t":"MySQL如何避免重复插入数据？","tag":"","p":"","html":"<blockquote><p>面试可以这样回答：</p><p><strong>答法模板</strong></p><ul><li>MySQL 避免重复插入，核心思路是：<strong>不要只靠业务代码判断，数据库层面也要加约束</strong>。</li><li>常见方式有这几种：</li></ul><ol><li><strong>唯一索引 / 唯一约束</strong></li></ol><ul><li>最常用、最可靠。</li><li>给不能重复的字段加 <code>UNIQUE</code> 约束。</li><li>比如手机号、邮箱、订单号、业务唯一 ID。</li></ul><p>这样相同手机号再次插入时，数据库会直接报重复键错误。</p><p><strong>2. </strong><strong><code>INSERT IGNORE</code></strong></p><ul><li>如果插入的数据重复，MySQL 会忽略这条插入，不报错。</li><li>适合不关心重复数据是否插入成功的场景。</li></ul><p><strong>3. </strong><strong><code>ON DUPLICATE KEY UPDATE</code></strong></p><ul><li>如果不存在就插入。</li><li>如果唯一键冲突，就执行更新。</li><li>适合“插入或更新”的场景。</li></ul><ol><li><strong>先查再插，不推荐单独使用</strong></li></ol><ul><li>可以先查询是否存在，再决定是否插入。</li><li>但在高并发下可能有问题：<ul><li>两个请求同时查都不存在</li><li>然后同时插入</li><li>最终还是重复</li></ul></li><li>所以即使先查再插，也要配合唯一索引。</li></ul><ol><li><strong>业务层幂等控制</strong></li></ol><ul><li>对一些业务操作，比如下单、支付回调、消息消费，可以用业务唯一号做幂等。</li><li>例如：<ul><li>订单号</li><li>请求流水号</li><li>第三方支付回调 ID</li><li>消息唯一 ID</li></ul></li><li>然后在数据库中对这个字段加唯一索引。</li></ul><p><strong>面试总结版</strong></p><blockquote><p>MySQL 避免重复插入，最核心的是在数据库层加唯一索引或唯一约束，比如手机号、订单号这类天然唯一字段。插入时可以配合 INSERT IGNORE 忽略重复数据，或者使用 ON DUPLICATE KEY UPDATE 实现存在则更新。单纯先查再插在高并发下不可靠，因为可能出现并发重复插入，所以通常要依赖唯一索引来保证最终一致性。</p></blockquote></blockquote>"},{"t":"CHAR 和 VARCHAR有什么区别？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：</p><p><strong>答法模板</strong></p><ul><li><code>CHAR</code> 和 <code>VARCHAR</code> 都是 MySQL 里存字符串的类型。</li><li>核心区别是：<strong><code>CHAR</code></strong><strong> 是定长字符串，</strong><strong><code>VARCHAR</code></strong><strong> 是变长字符串</strong>。</li></ul><ol><li><strong>存储方式不同</strong></li></ol><ul><li><code>CHAR(n)</code>：固定占用 <code>n</code> 个字符长度。<ul><li>如果实际内容不够，会用空格补齐。</li><li>比如 <code>CHAR(10)</code> 存 <code>\"abc\"</code>，也会按固定长度处理。</li></ul></li><li><code>VARCHAR(n)</code>：按实际内容长度存储。<ul><li>只会占用实际字符长度，再额外用 1 或 2 个字节记录长度。</li><li>比如 <code>VARCHAR(10)</code> 存 <code>\"abc\"</code>，主要只存 <code>\"abc\"</code> 本身。</li></ul></li></ul><ol><li><strong>适用场景不同</strong></li></ol><ul><li><code>CHAR</code> 适合长度固定的数据：<ul><li>性别</li><li>状态码</li><li>身份证号</li><li>手机号</li><li>MD5 值</li></ul></li><li><code>VARCHAR</code> 适合长度不固定的数据：<ul><li>用户名</li><li>昵称</li><li>邮箱</li><li>地址</li><li>文章标题</li></ul></li></ul><ol><li><strong>性能和空间不同</strong></li></ol><ul><li><code>CHAR</code> 因为长度固定，读取和更新相对简单，但可能浪费空间。</li><li><code>VARCHAR</code> 更节省空间，但由于长度可变，处理时会稍微复杂一些。</li><li>实际开发中，<strong>大部分变长文本都用 </strong><strong><code>VARCHAR</code></strong>。</li></ul><ol><li><strong>注意点</strong></li></ol><ul><li><code>CHAR</code> 查询时，尾部空格通常会被忽略。</li><li><code>VARCHAR</code> 会保留尾部空格。</li><li><code>CHAR(n)</code> 和 <code>VARCHAR(n)</code> 里的 <code>n</code> 表示字符数，不是字节数。</li></ul><p><strong>面试总结版</strong></p><blockquote><p>CHAR 是定长字符串，VARCHAR 是变长字符串。CHAR 会按照固定长度存储，不够会补空格，所以适合存长度固定的数据，比如状态码、手机号、MD5；VARCHAR 按实际长度存储，会额外用 1 到 2 个字节记录长度，更节省空间，适合用户名、邮箱、地址这类长度不固定的数据。实际开发中，如果长度固定可以用 CHAR，否则一般用 VARCHAR。</p></blockquote><p></p></blockquote>"},{"t":"varchar后面代表字节还是字符？","tag":"","p":"","html":"<blockquote><p>在 MySQL 里，<code>VARCHAR(n)</code> 后面的 <code>n</code> 代表的是<strong>字符数</strong>，不是字节数。</p><p>比如：</p><p>表示最多可以存 <strong>10 个字符</strong>。</p><p><strong>举例</strong></p><ul><li>如果字符集是 <code>utf8mb4</code></li><li>一个中文通常最多占 3 个字节</li><li>一个 emoji 可能占 4 个字节</li><li>但 <code>VARCHAR(10)</code> 仍然表示最多存 10 个字符，不是 10 个字节</li></ul><p>所以：VARCHAR(10)</p><p>可以存：abcdefg123</p><p>也可以存：张三李四王五赵六钱孙</p><p>只要不超过 <strong>10 个字符</strong>即可。</p><p><strong>面试可以这样答</strong></p><blockquote><p>MySQL 中 VARCHAR 后面的长度表示字符数，不是字节数。比如 VARCHAR(20) 表示最多存 20 个字符。实际占用的字节数和字符集有关，比如 utf8mb4 下中文和 emoji 可能占多个字节。另外 VARCHAR 本身还会额外用 1 到 2 个字节记录字符串长度。</p></blockquote></blockquote>"},{"t":"int(1) int(10) 在mysql有什么不同？","tag":"","p":"","html":"<blockquote><p>面试时可以这样回答：</p><p><strong>核心结论</strong></p><ul><li><code>INT(1)</code> 和 <code>INT(10)</code> 的<strong>存储范围、占用空间完全一样</strong>。</li><li>它们都是 <code>INT</code> 类型，固定占用 <strong>4 个字节</strong>。</li><li>括号里的数字不是能存几位数，也不是存储长度。</li></ul><p><strong>括号数字的含义</strong></p><ul><li><code>INT(M)</code> 里的 <code>M</code> 以前表示<strong>显示宽度</strong>。</li><li>比如 <code>INT(10)</code> 表示查询展示时的宽度建议是 10。</li><li>但这个显示宽度基本不会影响实际使用。</li></ul><p><strong>举个例子</strong></p><p>这两个字段都可以存：</p><p>不是说 <code>INT(1)</code> 只能存 1 位数字。</p><p><strong>和 </strong><strong><code>ZEROFILL</code></strong><strong> 有关</strong></p><p>以前显示宽度主要配合 <code>ZEROFILL</code> 使用：</p><p>num INT(5) ZEROFILL</p><p>如果存的是：</p><p>12</p><p>查询展示可能是：</p><p>00012</p><p>但不影响真实存储值。</p><p><strong>注意 MySQL 8</strong></p><ul><li>从 MySQL 8.0.17 开始，整数显示宽度已经被废弃。</li><li>所以现在一般直接写：INT</li></ul><p>而不是：INT(10)</p><p><strong>面试总结版</strong></p><blockquote><p>INT(1) 和 INT(10) 在 MySQL 中没有存储大小和取值范围的区别，都是 4 字节整数。括号里的数字以前表示显示宽度，不代表能存几位数，只有配合 ZEROFILL 时才可能影响展示效果。现在 MySQL 8 已经废弃整数显示宽度，所以实际开发中一般直接写 INT。</p></blockquote></blockquote>"},{"t":"Text数据类型可以无限大吗？","tag":"","p":"","html":"<blockquote><p>不是，<code>TEXT</code> <strong>不是无限大</strong>，它有长度上限。</p><p><strong>核心回答</strong></p><ul><li>MySQL 的 <code>TEXT</code> 类型可以存比较长的字符串。</li><li>但它不是无限大小。</li><li>不同 <code>TEXT</code> 类型有不同的最大容量。</li></ul><p><strong>常见类型大小</strong></p><ul><li><code>TINYTEXT</code>：最多约 <strong>255 字节</strong></li><li><code>TEXT</code>：最多约 <strong>64KB</strong></li><li><code>MEDIUMTEXT</code>：最多约 <strong>16MB</strong></li><li><code>LONGTEXT</code>：最多约 <strong>4GB</strong></li></ul><p>注意：这里说的是<strong>字节</strong>，不是字符数。</p><p><strong>为什么是字节</strong></p><ul><li><code>TEXT</code> 的上限按字节计算。</li><li>如果使用 <code>utf8mb4</code> 字符集：<ul><li>一个英文字符通常 1 字节</li><li>一个中文可能 3 字节</li><li>一个 emoji 可能 4 字节</li></ul></li><li>所以能存多少字符，取决于具体内容和字符集。</li></ul><p><strong>面试可以这样答</strong></p><blockquote><p>TEXT 类型不是无限大的，它有固定上限。MySQL 中常见的 TINYTEXT 最大约 255 字节，TEXT 最大约 64KB，MEDIUMTEXT 最大约 16MB，LONGTEXT 最大约 4GB。需要注意这些限制是按字节算的，不是按字符数算的，所以实际能存多少字符还和字符集有关，比如 utf8mb4 下中文和 emoji 会占多个字节。</p></blockquote></blockquote>"},{"t":"IP地址如何在数据库里存储？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：</p><p><strong>核心思路</strong></p><ul><li>IP 地址在数据库里可以用字符串存，也可以用整数存。</li><li>一般有两种方式：<ul><li><code>VARCHAR</code></li><li>整数类型</li></ul></li></ul><ol><li><strong>用字符串存</strong></li></ol><ul><li>IPv4 可以用：VARCHAR(15)</li><li>因为 IPv4 最长类似：255.255.255.255</li><li>IPv6 可以用：VARCHAR(39)</li><li>因为 IPv6 最长一般是 39 个字符。</li></ul><p>优点：</p><ul><li>直观，方便查看。</li><li>不需要转换。</li></ul><p>缺点：</p><ul><li>占用空间相对大。</li><li>查询、排序、范围比较不方便。</li></ul><ol><li><strong>用整数存 IPv4</strong></li></ol><ul><li>IPv4 本质上是 32 位整数。</li><li>可以用：INT UNSIGNED</li><li>MySQL 提供了函数转换：INET_ATON('192.168.1.1')</li></ul><p>可以把 IPv4 转成整数。INET_NTOA(3232235777)</p><p>可以把整数转回 IPv4。</p><p>示例：</p><p>INSERT INTO user_login(ip)</p><p>VALUES (INET_ATON('192.168.1.1'));</p><p>SELECT INET_NTOA(ip) FROM user_login;</p><p>优点：</p><ul><li>占用空间小。</li><li>方便做排序、范围查询。</li><li>查询效率更高。</li></ul><p>缺点：</p><ul><li>可读性差。</li><li>需要转换。</li></ul><ol><li><strong>IPv6 怎么办</strong></li></ol><ul><li>IPv6 不能用普通 <code>INT UNSIGNED</code> 存。</li><li>可以用：<ul><li><code>VARCHAR(39)</code> 字符串存</li><li>或者 <code>VARBINARY(16)</code> 二进制存</li></ul></li></ul><p>MySQL 也有对应函数：</p><p>INET6_ATON()</p><p>INET6_NTOA()</p><p>例如：</p><p>INSERT INTO user_login(ipv6)</p><p>VALUES (INET6_ATON('2001:db8::1'));</p><p>SELECT INET6_NTOA(ipv6) FROM user_login;</p><p><strong>面试总结版</strong></p><blockquote><p>IP 地址可以用字符串存，也可以转换成数字或二进制存。IPv4 如果追求简单，可以用 VARCHAR(15)；如果考虑存储空间、排序和范围查询，可以用 INT UNSIGNED，并通过 INET_ATON 和 INET_NTOA 转换。IPv6 可以用 VARCHAR(39)，更推荐用 VARBINARY(16) 配合 INET6_ATON 和 INET6_NTOA 存储，这样空间更小，查询也更高效。</p></blockquote><p></p></blockquote>"},{"t":"说一下外键约束","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试时可以这样回答：</p><ol><li><strong>外键是什么</strong></li></ol><ul><li>外键约束用于维护两张表之间的数据一致性。</li><li>它要求子表中的某个字段，必须能在父表的主键或唯一键中找到对应值。</li><li>简单说，就是<strong>子表不能引用一个不存在的父表数据</strong>。</li></ul><ol><li><strong>举个例子</strong></li></ol><p>比如有两张表：</p><ul><li>用户表 <code>user</code></li><li>订单表 <code>orders</code></li></ul><p>订单表里的 <code>user_id</code> 就可以作为外键，引用用户表的 <code>id</code>：</p><p>CREATE TABLE orders (</p><p>  id BIGINT PRIMARY KEY,</p><p>  user_id BIGINT,</p><p>  CONSTRAINT fk_orders_user</p><p>    FOREIGN KEY (user_id) REFERENCES user(id)</p><p>);</p><p>这样可以保证：</p><ul><li>插入订单时，<code>user_id</code> 必须在用户表中存在。</li><li>如果用户不存在，就不能插入这条订单。</li></ul><ol><li><strong>外键能解决什么问题</strong></li></ol><ul><li>防止插入无效数据。</li><li>防止删除父表数据后，子表留下“脏数据”。</li><li>保证表与表之间的引用完整性。</li></ul><ol><li><strong>常见约束行为</strong></li></ol><p>删除或更新父表数据时，可以设置外键策略：</p><ul><li><code>RESTRICT</code> / <code>NO ACTION</code>：如果子表有关联数据，不允许删除或更新父表。</li><li><code>CASCADE</code>：父表删除或更新时，子表跟着删除或更新。</li><li><code>SET NULL</code>：父表删除或更新时，子表外键字段设置为 <code>NULL</code>。</li><li><code>SET DEFAULT</code>：设置为默认值，MySQL 中支持有限。</li></ul><p>例如：</p><p>FOREIGN KEY (user_id) REFERENCES user(id)</p><p>ON DELETE CASCADE</p><p>ON UPDATE CASCADE</p><p>表示删除用户时，对应订单也会被删除。</p><ol><li><strong>实际开发中的看法</strong></li></ol><ul><li>外键能增强数据一致性。</li><li>但在高并发、大数据量、分库分表场景下，很多项目不会强依赖数据库外键。</li><li>因为外键会带来额外检查成本，也会增加表之间的耦合。</li><li>实际项目中常见做法是：<ul><li>数据库中不一定建立物理外键</li><li>但业务逻辑上仍然保留外键关系</li><li>由应用层保证数据一致性</li></ul></li></ul><p><strong>面试总结版</strong></p><blockquote><p>外键约束是用来保证表之间引用完整性的。比如订单表的 user_id 引用用户表的 id，外键可以保证订单不能关联一个不存在的用户。外键还可以设置删除或更新策略，比如 RESTRICT、CASCADE、SET NULL。实际开发中，外键可以保证一致性，但也会带来性能开销和表之间的强耦合，所以在高并发或分库分表场景下，很多项目不会建立物理外键，而是在业务层维护这种关联关系。</p></blockquote><p></p></blockquote>"},{"t":"MySQL的关键字in和exist","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><blockquote><p>你这里应该是问 <strong><code>IN</code></strong><strong> 和 </strong><strong><code>EXISTS</code></strong><strong> 的区别</strong>。面试可以这样答：</p><ol><li><strong>基本作用</strong></li></ol><ul><li><code>IN</code> 和 <code>EXISTS</code> 都可以用于子查询。</li><li>它们都能判断某条数据是否满足条件。</li><li>常见场景是：查询一张表中，是否存在另一张表的关联数据。</li></ul><p><strong>2. </strong><strong><code>IN</code></strong><strong> 的含义</strong></p><ul><li><code>IN</code> 判断某个字段的值是否在一个结果集合中。</li><li>更像是：<strong>拿外层字段去和子查询结果集做匹配</strong>。</li></ul><p>例如查询有订单的用户：</p><p>SELECT *</p><p>FROM user</p><p>WHERE id IN (</p><p>  SELECT user_id</p><p>  FROM orders</p><p>);</p><p>含义是：用户 <code>id</code> 是否在订单表的 <code>user_id</code> 集合里。</p><p><strong>3. </strong><strong><code>EXISTS</code></strong><strong> 的含义</strong></p><ul><li><code>EXISTS</code> 判断子查询是否能查出数据。</li><li>更像是：<strong>只关心子查询有没有结果，不关心具体返回什么</strong>。</li></ul><p>SELECT *</p><p>FROM user u</p><p>WHERE EXISTS (</p><p>  SELECT 1</p><p>  FROM orders o</p><p>  WHERE o.user_id = u.id</p><p>);</p><p>含义是：对每个用户，去订单表里看是否存在对应订单。</p><ol><li><strong>主要区别</strong></li></ol><ul><li><code>IN</code>：关注某个值是否在子查询结果集中。</li><li><code>EXISTS</code>：关注子查询是否有结果。</li><li><code>IN</code> 的子查询一般是非相关子查询。</li><li><code>EXISTS</code> 通常是相关子查询，会用到外层表字段。</li><li><code>EXISTS</code> 子查询一旦找到匹配数据，就可以停止继续查。</li></ul><ol><li><strong>性能怎么说</strong></li></ol><p>面试不要绝对说谁更快，可以这样说：</p><ul><li>如果子查询结果集比较小，<code>IN</code> 通常比较直观。</li><li>如果外层表大、子查询表有索引，<code>EXISTS</code> 可能更合适。</li><li>但 MySQL 优化器会做优化，实际性能要看执行计划。</li><li>关键是关联字段要有索引。</li></ul><p><strong>6. </strong><strong><code>NOT IN</code></strong><strong> 和 </strong><strong><code>NOT EXISTS</code></strong><strong> 注意点</strong></p><p>这个很容易加分：</p><ul><li><code>NOT IN</code> 遇到子查询结果中有 <code>NULL</code> 时，可能导致结果不符合预期。</li><li><code>NOT EXISTS</code> 一般更安全。</li></ul><p>例如：</p><p>SELECT *</p><p>FROM user</p><p>WHERE id NOT IN (</p><p>  SELECT user_id</p><p>  FROM orders</p><p>);</p><p>如果 <code>orders.user_id</code> 中存在 <code>NULL</code>，可能查不出预期结果。</p><p>更推荐：</p><p>SELECT *</p><p>FROM user u</p><p>WHERE NOT EXISTS (</p><p>  SELECT 1</p><p>  FROM orders o</p><p>  WHERE o.user_id = u.id</p><p>);</p><p><strong>面试总结版</strong></p><p>IN 是判断某个字段的值是否在子查询结果集中，EXISTS 是判断子查询是否存在结果。IN 更关注具体值匹配，EXISTS 更关注有没有匹配记录。</p><ul><li>一般子查询结果集较小时 IN 比较直观，</li><li>而 EXISTS 在相关子查询、有索引的场景下比较常用。不过 MySQL 优化器会做转换和优化，实际性能要看执行计划。需要注意的是，NOT IN 如果子查询结果中包含 NULL，可能导致结果异常，所以反向判断时通常更推荐 NOT EXISTS。</li></ul></blockquote>"},{"t":"mysql中的一些基本函数，你知道哪些？","tag":"","p":"","html":"<blockquote><p>MySQL 常用函数我一般按类型来记，主要有字符串函数、日期时间函数、聚合函数和条件判断函数。</p><p>字符串函数比如 <code>CONCAT</code> 拼接字符串、<code>SUBSTRING</code> 截取字符串；日期函数比如 <code>NOW</code> 获取当前时间、<code>DATE_FORMAT</code> 格式化时间；聚合函数比如 <code>COUNT</code> 统计数量、<code>SUM</code> 求和、<code>AVG</code> 求平均值；条件函数比如 <code>IFNULL</code> 处理空值、<code>CASE WHEN</code> 做多条件判断。<br/>实际项目里用得比较多的是时间格式化、统计聚合和空值处理。</p></blockquote>"},{"t":"SQL查询语句的执行顺序是怎么样的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试时可以这样回答：</p><p><strong>先说结论</strong></p><blockquote><p>SQL 的书写顺序和执行顺序不完全一样。<br/>常见查询语句的书写顺序是 <code>SELECT ... FROM ... WHERE ... GROUP BY ... HAVING ... ORDER BY ... LIMIT</code>，<br/>但逻辑执行顺序一般是：<code>FROM</code> → <code>WHERE</code> → <code>GROUP BY</code> → <code>HAVING</code> → <code>SELECT</code> → <code>ORDER BY</code> → <code>LIMIT</code>。</p></blockquote><p><strong>分点解释</strong></p><ul><li><strong><code>FROM</code></strong><ul><li>先确定从哪张表查数据。</li><li>如果有 <code>JOIN</code>，也会在这个阶段进行表连接。</li></ul></li><li><strong><code>WHERE</code></strong><ul><li>对原始数据进行过滤。</li><li>注意：这里不能用聚合函数过滤分组结果。</li></ul></li><li><strong><code>GROUP BY</code></strong><ul><li>按指定字段进行分组。</li></ul></li><li><strong><code>HAVING</code></strong><ul><li>对分组后的结果进行过滤。</li><li>常用于过滤聚合结果，比如 <code>COUNT(*) &gt; 1</code>。</li></ul></li><li><strong><code>SELECT</code></strong><ul><li>选择要返回的字段。</li><li>计算表达式、别名等。</li></ul></li><li><strong><code>ORDER BY</code></strong><ul><li>对最终结果进行排序。</li></ul></li><li><strong><code>LIMIT</code></strong><ul><li>限制返回的条数。</li></ul></li></ul><p><strong>举个例子</strong></p><p>SELECT user_id, COUNT(*) AS order_count</p><p>FROM orders</p><p>WHERE status = 'PAID'</p><p>GROUP BY user_id</p><p>HAVING COUNT(*) &gt; 3</p><p>ORDER BY order_count DESC</p><p>LIMIT 10;</p><p>逻辑执行过程是：</p><ul><li>先从 <code>orders</code> 表取数据。</li><li>用 <code>WHERE status = 'PAID'</code> 过滤已支付订单。</li><li>按 <code>user_id</code> 分组。</li><li>用 <code>HAVING COUNT(*) &gt; 3</code> 筛选订单数大于 3 的用户。</li><li><code>SELECT</code> 返回用户 ID 和订单数量。</li><li>按订单数量倒序排序。</li><li>最后取前 10 条。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>SQL 查询语句的书写顺序和执行顺序不同。书写顺序一般是 SELECT、FROM、WHERE、GROUP BY、HAVING、ORDER BY、LIMIT；逻辑执行顺序一般是 FROM、WHERE、GROUP BY、HAVING、SELECT、ORDER BY、LIMIT。也就是说，先确定数据来源，再过滤原始数据，然后分组，分组后用 HAVING 过滤，再选择返回字段，最后排序和分页。</p></blockquote><p></p></blockquote>"},{"t":"sql题：给学生表、课程成绩表，求没选01课程但选02课程的学生的成绩","tag":"","p":"","html":"<blockquote><p>可以，这题面试里重点是：<strong>查有 02 课程，但没有 01 课程的学生，并返回 02 课程成绩</strong>。</p><p>假设表结构是：</p><p>student(s_id, s_name)</p><p>score(s_id, c_id, score)</p><p><strong>推荐写法：</strong><strong><code>NOT EXISTS</code></strong></p><p>SELECT</p><p>  s.s_id,</p><p>  s.s_name,</p><p>  sc.score</p><p>FROM student s</p><p>JOIN score sc ON s.s_id = sc.s_id</p><p>WHERE sc.c_id = '02'</p><p>  AND NOT EXISTS (</p><p>    SELECT 1</p><p>    FROM score sc2</p><p>    WHERE sc2.s_id = s.s_id</p><p>      AND sc2.c_id = '01'</p><p>  );</p><p><strong>解释一下</strong></p><ul><li><code>JOIN score sc</code>：关联学生和成绩表。</li><li><code>sc.c_id = '02'</code>：先找有 02 课程成绩的学生。</li><li><code>NOT EXISTS</code>：再排除掉存在 01 课程成绩的学生。</li><li>最后返回的就是：<strong>没有 01 课，但有 02 课的学生的 02 课成绩</strong>。</li></ul></blockquote>"},{"t":"给定一个学生表 student_score（stu_id，subject_id，score），查询总分排名在5-10名的学生id及对应的总分","tag":"","p":"","html":"<blockquote><p>面试可以这样答：先按学生分组求总分，再对总分排名，最后取第 5 到第 10 名。</p><p><strong>MySQL 8 推荐写法：窗口函数</strong></p></blockquote><pre><code>SELECT<br/>  stu_id,<br/>  total_score<br/>FROM (<br/>  SELECT<br/>    stu_id,<br/>    SUM(score) AS total_score,<br/>    DENSE_RANK() OVER (ORDER BY SUM(score) DESC) AS ranking<br/>  FROM student_score<br/>  GROUP BY stu_id<br/>) t<br/>WHERE ranking BETWEEN 5 AND 10;</code></pre><p><strong>解释</strong></p><ul><li><code>GROUP BY stu_id</code>：按学生分组。</li><li><code>SUM(score)</code>：计算每个学生总分。</li><li><code>DENSE_RANK() OVER (ORDER BY SUM(score) DESC)</code>：按总分从高到低排名。</li><li><code>WHERE ranking BETWEEN 5 AND 10</code>：取排名第 5 到第 10 的学生。</li></ul><p><strong>注意排名并列</strong></p><p>如果面试官问并列怎么办：</p><ul><li><code>RANK()</code>：有并列名次，会跳名次。<ul><li>比如：1、2、2、4</li></ul></li><li><code>DENSE_RANK()</code>：有并列名次，不跳名次。<ul><li>比如：1、2、2、3</li></ul></li><li><code>ROW_NUMBER()</code>：不考虑并列，每行一个唯一序号。<ul><li>比如：1、2、3、4</li></ul></li></ul><p>如果题目说“排名 5-10 名”，我会优先用 <code>DENSE_RANK()</code> 或 <code>RANK()</code>，具体看业务对并列的定义。</p><p><strong>面试总结版</strong></p><blockquote><p>这题我会先按 stu_id 分组，用 SUM(score) 求每个学生的总分，然后按总分倒序排名。如果是 MySQL 8，可以用窗口函数，比如 DENSE_RANK 或 RANK，再筛选排名 between 5 and 10。如果不考虑并列排名，只是取排序后的第 5 到第 10 条，也可以用 ORDER BY total_score DESC LIMIT 4, 6。</p></blockquote>"},{"t":"SQL题：查某个班级下所有学生的选课情况","tag":"","p":"","html":"<p>这题面试可以先说清楚：<strong>要查某个班级下所有学生，</strong><strong>所以学生表要作为主表，用 </strong><strong><code>LEFT JOIN</code></strong><strong>，这样没选课的学生也能查出来。</strong></p><p>假设表结构：</p><pre><code>student(stu_id, stu_name, class_id)<br/>course(course_id, course_name)<br/>student_course(stu_id, course_id)</code></pre><p><strong>SQL 写法</strong></p><pre><code>SELECT<br/>  s.stu_id,<br/>  s.stu_name,<br/>  c.course_id,<br/>  c.course_name<br/>FROM student s<br/>LEFT JOIN student_course sc<br/>  ON s.stu_id = sc.stu_id<br/>LEFT JOIN course c<br/>  ON sc.course_id = c.course_id<br/>WHERE s.class_id = #{classId};</code></pre><p><strong>为什么用 </strong><strong><code>LEFT JOIN</code></strong></p><ul><li>题目说的是“某个班级下所有学生”。</li><li>所以即使学生没有选课，也应该查出来。</li><li>如果用 <code>INNER JOIN</code>，没选课的学生会被过滤掉。</li><li>所以这里应该用 <code>student</code> 表作为主表，再左连接选课表和课程表。</li></ul><p><strong>查出来的结果大概是</strong></p><sheet></sheet><p>这里王五没有选课，但因为用了 <code>LEFT JOIN</code>，所以还能显示出来。</p><p><strong>如果想按学生聚合课程</strong></p><p>MySQL 可以用 <code>GROUP_CONCAT</code>：</p><pre><code>SELECT<br/>  s.stu_id,<br/>  s.stu_name,<br/>  GROUP_CONCAT(c.course_name) AS course_names<br/>FROM student s<br/>LEFT JOIN student_course sc<br/>  ON s.stu_id = sc.stu_id<br/>LEFT JOIN course c<br/>  ON sc.course_id = c.course_id<br/>WHERE s.class_id = #{classId}<br/>GROUP BY s.stu_id, s.stu_name;</code></pre><p>结果类似：</p><sheet></sheet><p><strong>面试回答版</strong></p><blockquote><p>这题我会以学生表作为主表，因为题目要求查某个班级下所有学生，包括没有选课的学生。然后用 LEFT JOIN 连接学生选课中间表，再 LEFT JOIN 课程表，通过 class_id 过滤班级。如果要一行展示一个学生的所有课程，可以用 GROUP_CONCAT 对课程名称做聚合。</p></blockquote>"},{"t":"如何用 MySQL 实现一个可重入的锁？","tag":"","p":"","html":"<p>面试可以这样答：<strong>用一张锁表 + 唯一键 + 持有者标识 + 重入次数来实现。</strong></p><ol><li><strong>什么是可重入锁</strong></li></ol><ul><li>同一个线程 / 请求 / 实例已经拿到锁之后，再次获取同一把锁不会被阻塞。</li><li>每重入一次，计数加一。</li><li>每释放一次，计数减一。</li><li>只有计数减到 0，才真正释放锁。</li></ul><ol><li><strong>建一张锁表</strong></li></ol><pre><code>CREATE TABLE distributed_lock (<br/>  lock_key VARCHAR(128) PRIMARY KEY,<br/>  owner_id VARCHAR(128) NOT NULL,<br/>  reentrant_count INT NOT NULL DEFAULT 1,<br/>  expire_time DATETIME NOT NULL,<br/>  update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP<br/>);</code></pre><p>字段含义：</p><ul><li><code>lock_key</code>：锁名称，比如 <code>order:123</code></li><li><code>owner_id</code>：锁持有者，比如 <code>服务实例ID + 线程ID + 请求ID</code></li><li><code>reentrant_count</code>：重入次数</li><li><code>expire_time</code>：过期时间，防止死锁</li><li><code>lock_key</code> 加唯一约束，保证同一把锁只能有一个持有者</li></ul><ol><li><strong>加锁逻辑</strong></li></ol><p>加锁分三种情况：</p><ul><li>锁不存在：直接插入，表示加锁成功。</li><li>锁存在，且 <code>owner_id</code> 是自己：说明是重入，<code>reentrant_count + 1</code>。</li><li>锁存在，但不是自己：加锁失败，或者等待重试。</li></ul><p>示例 SQL：</p><pre><code>INSERT INTO distributed_lock (<br/>  lock_key,<br/>  owner_id,<br/>  reentrant_count,<br/>  expire_time<br/>)<br/>VALUES (<br/>  'order:123',<br/>  'server1-thread1',<br/>  1,<br/>  DATE_ADD(NOW(), INTERVAL 30 SECOND)<br/>)<br/>ON DUPLICATE KEY UPDATE<br/>  reentrant_count =<br/>    IF(owner_id = VALUES(owner_id), reentrant_count + 1, reentrant_count),<br/>  expire_time =<br/>    IF(owner_id = VALUES(owner_id), VALUES(expire_time), expire_time);</code></pre><p>但是这条 SQL 只能处理“自己重入”的情况，实际还要判断是否真的更新成功。</p><p>更严谨一点，可以拆成两步：</p><pre><code>UPDATE distributed_lock<br/>SET<br/>  reentrant_count = reentrant_count + 1,<br/>  expire_time = DATE_ADD(NOW(), INTERVAL 30 SECOND)<br/>WHERE lock_key = 'order:123'<br/>  AND owner_id = 'server1-thread1';</code></pre><p>如果更新行数是 <code>1</code>，说明自己重入成功。</p><p>如果更新行数是 <code>0</code>，再尝试插入：</p><pre><code>INSERT INTO distributed_lock (<br/>  lock_key,<br/>  owner_id,<br/>  reentrant_count,<br/>  expire_time<br/>)<br/>VALUES (<br/>  'order:123',<br/>  'server1-thread1',<br/>  1,<br/>  DATE_ADD(NOW(), INTERVAL 30 SECOND)<br/>);</code></pre><p>如果插入成功，说明第一次拿锁成功。</p><p>如果插入失败，说明别人持有锁。</p><ol><li><strong>释放锁逻辑</strong></li></ol><p>释放时也必须判断是不是自己持有的锁。</p><p>先把重入次数减一：</p><pre><code>UPDATE distributed_lock<br/>SET reentrant_count = reentrant_count - 1<br/>WHERE lock_key = 'order:123'<br/>  AND owner_id = 'server1-thread1'<br/>  AND reentrant_count &gt; 1;</code></pre><p>如果更新成功，说明只是减少重入次数，还没真正释放。</p><p>如果 <code>reentrant_count = 1</code>，就删除锁：</p><pre><code>DELETE FROM distributed_lock<br/>WHERE lock_key = 'order:123'<br/>  AND owner_id = 'server1-thread1'<br/>  AND reentrant_count = 1;</code></pre><ol><li><strong>处理锁过期</strong></li></ol><p>为了防止服务宕机后锁永远不释放，需要加过期时间。</p><p>加锁时可以允许抢占过期锁：</p><pre><code>UPDATE distributed_lock<br/>SET<br/>  owner_id = 'server2-thread1',<br/>  reentrant_count = 1,<br/>  expire_time = DATE_ADD(NOW(), INTERVAL 30 SECOND)<br/>WHERE lock_key = 'order:123'<br/>  AND expire_time &lt; NOW();</code></pre><p>如果更新成功，说明原锁已经过期，可以被新 owner 获取。</p><ol><li><strong>面试总结版</strong></li></ol><blockquote><p>用 MySQL 实现可重入锁，一般会建一张锁表，锁名称作为唯一键，同时记录 owner_id、重入次数和过期时间。加锁时，如果锁不存在就插入；如果锁存在但 owner_id 是当前线程或请求，就把重入次数加一；如果是别人持有，就加锁失败或重试。释放锁时要校验 owner_id，重入次数大于 1 就减一，等于 1 才真正删除锁。同时需要设置 expire_time，避免服务宕机导致死锁。实际生产中还要注意事务、唯一索引、超时续期和并发重试。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"存储引擎","questions":[{"t":"执行一条SQL请求的过程是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试可以按 <strong>“连接 → 解析 → 优化 → 执行 → 返回”</strong> 这个顺序回答。</p><ol><li><strong>客户端建立连接</strong></li></ol><ul><li>应用通过 MySQL 驱动和 MySQL Server 建立连接。</li><li>MySQL 会做身份认证、权限校验。</li><li>连接建立后，会分配一个线程处理这个请求。</li></ul><ol><li><strong>查询缓存</strong></li></ol><ul><li>如果是老版本 MySQL，可能会先查 Query Cache。</li><li>但 MySQL 8.0 已经移除了查询缓存。</li><li>所以现在面试简单提一句即可，不要重点说。</li></ul><ol><li><strong>解析 SQL</strong></li></ol><ul><li>MySQL 会先做词法分析和语法分析。</li><li>判断 SQL 写法是否合法。</li><li>比如表名、字段名、关键字是否正确。</li><li>如果 SQL 语法错误，会在这一阶段报错。</li></ul><ol><li><strong>预处理和权限校验</strong></li></ol><ul><li>检查表和字段是否存在。</li><li>检查当前用户是否有对应权限。</li><li>比如是否有 <code>SELECT</code>、<code>UPDATE</code> 权限。</li></ul><ol><li><strong>优化器生成执行计划</strong></li></ol><ul><li>优化器会选择怎么执行这条 SQL。</li><li>比如：<ul><li>选择用哪个索引</li><li>表连接顺序</li><li>是否全表扫描</li><li>条件下推</li></ul></li><li>可以通过 <code>EXPLAIN</code> 查看执行计划。</li></ul><ol><li><strong>执行器执行 SQL</strong></li></ol><ul><li>执行器根据执行计划调用存储引擎接口。</li><li>如果是 InnoDB，就去 InnoDB 里读取或修改数据。</li><li>InnoDB 会涉及：<ul><li>Buffer Pool</li><li>B+ 树索引</li><li>行锁 / MVCC</li><li>undo log</li><li>redo log</li><li>binlog</li></ul></li></ul><ol><li><strong>返回结果</strong></li></ol><ul><li>执行器把查询结果返回给客户端。</li><li>如果是更新语句，还会返回影响行数。</li><li>如果开启事务，则根据事务提交或回滚决定数据是否最终生效。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>一条 SQL 请求大致会经历连接、解析、优化、执行和返回几个阶段。客户端先通过连接器连接 MySQL，并完成认证和权限校验；然后解析器对 SQL 做词法、语法分析，检查 SQL 是否合法；接着优化器根据统计信息选择执行计划，比如使用哪个索引、表连接顺序；然后执行器按照执行计划调用存储引擎接口，比如 InnoDB 去读取或修改数据；最后把结果返回给客户端。如果是更新语句，还会涉及 redo log、undo log、binlog 以及事务提交过程。</p></blockquote></blockquote>"},{"t":"讲一讲mysql的引擎吧，你有什么了解？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试时可以这样回答，重点讲 <strong>InnoDB 和 MyISAM</strong> 就够了。</p><ol><li><strong>先整体说明</strong></li></ol><blockquote><p>MySQL 的存储引擎决定了数据怎么存、怎么建索引、是否支持事务、锁粒度等。常见的存储引擎有 InnoDB、MyISAM、Memory 等。实际开发中最常用的是 InnoDB，也是 MySQL 默认的存储引擎。</p></blockquote><ol><li><strong>InnoDB</strong></li></ol><ul><li>InnoDB 是现在最常用的引擎。</li><li>支持事务，满足 ACID。</li><li>支持行级锁，并发性能更好。</li><li>支持外键。</li><li>支持崩溃恢复，依赖 redo log、undo log。</li><li>索引结构是 B+ 树。</li><li>InnoDB 使用聚簇索引，数据和主键索引存放在一起。</li></ul><p>面试可以说：</p><blockquote><p>InnoDB 适合大多数业务场景，特别是需要事务、并发更新、数据一致性的场景，比如订单、支付、库存等。</p></blockquote><ol><li><strong>MyISAM</strong></li></ol><ul><li>MyISAM 是早期常用的引擎。</li><li>不支持事务。</li><li>不支持外键。</li><li>使用表级锁，并发写性能较差。</li><li>查询性能在某些只读场景下还可以。</li><li>崩溃恢复能力不如 InnoDB。</li><li>它的索引和数据是分开存储的，属于非聚簇索引。</li></ul><p>面试可以说：</p><blockquote><p>MyISAM 更适合读多写少、对事务要求不高的场景，但现在实际项目里用得比较少。</p></blockquote><ol><li><strong>Memory</strong></li></ol><ul><li>Memory 引擎把数据存放在内存中。</li><li>读写速度快。</li><li>但是数据库重启后数据会丢失。</li><li>适合临时数据、缓存类场景。</li><li>但实际项目中更多会用 Redis 做这类事情。</li></ul><ol><li><strong>InnoDB 和 MyISAM 对比</strong></li></ol><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 的存储引擎决定了表的数据存储方式、索引实现、事务支持和锁机制。常见引擎有 InnoDB、MyISAM、Memory，其中最常用的是 InnoDB。InnoDB 支持事务、行级锁、外键和崩溃恢复，适合大多数高并发和强一致性场景；MyISAM 不支持事务和外键，使用表级锁，适合读多写少但现在用得较少；Memory 把数据放在内存里，速度快但重启会丢数据。实际开发中一般默认选择 InnoDB。</p></blockquote><p></p></blockquote><ol><li><strong>InnoDB 和 MyISAM 对比</strong></li></ol><sheet></sheet>"},{"t":"MySQL为什么InnoDB是默认引擎？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：</p><p><strong>核心原因</strong></p><blockquote><p>InnoDB 之所以成为 MySQL 默认引擎，主要是因为它更适合现代业务系统，对事务、一致性、并发和可靠性支持更好。</p></blockquote><ol><li><strong>支持事务</strong></li></ol><ul><li>InnoDB 支持事务，满足 ACID。</li><li>可以通过 <code>COMMIT</code>、<code>ROLLBACK</code> 保证数据一致性。</li><li>比如订单、支付、库存这类业务，都需要事务支持。</li></ul><ol><li><strong>支持行级锁，并发能力更好</strong></li></ol><ul><li>InnoDB 支持行级锁。</li><li>多个事务操作不同行数据时，可以并发执行。</li><li>MyISAM 主要是表级锁，并发写性能差。</li><li>所以 InnoDB 更适合高并发读写场景。</li></ul><ol><li><strong>支持崩溃恢复</strong></li></ol><ul><li>InnoDB 有 <code>redo log</code>、<code>undo log</code>。</li><li>数据库异常宕机后，可以通过日志恢复数据。</li><li>可靠性比 MyISAM 更好。</li></ul><ol><li><strong>支持 MVCC</strong></li></ol><ul><li>InnoDB 支持 MVCC，多版本并发控制。</li><li>读写之间可以减少阻塞。</li><li>普通查询不一定会阻塞写操作，写操作也不一定阻塞普通读。</li><li>这对高并发系统非常重要。</li></ul><ol><li><strong>支持外键</strong></li></ol><ul><li>InnoDB 支持外键约束。</li><li>可以保证表之间的引用完整性。</li><li>虽然实际项目中不一定大量使用物理外键，但这是 InnoDB 的能力之一。</li></ul><ol><li><strong>综合更适合业务系统</strong></li></ol><ul><li>现代业务系统通常要求：<ul><li>数据不能乱</li><li>并发性能要好</li><li>宕机后能恢复</li><li>支持事务</li></ul></li><li>InnoDB 在这些方面都比 MyISAM 更适合。</li><li>所以 MySQL 默认选择 InnoDB。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>InnoDB 成为 MySQL 默认引擎，主要是因为它更适合现代业务场景。<strong>它支持事务，可以保证 ACID；支持行级锁和 MVCC，并发能力更好；支持 redo log 和 undo log，宕机后可以恢复数据；同时也支持外键。</strong>相比 MyISAM，InnoDB 在数据一致性、可靠性和高并发读写方面更强，所以现在 MySQL 默认使用 InnoDB。</p></blockquote></blockquote>"},{"t":"说一下mysql的innodb与MyISAM的区别？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：<strong>重点对比事务、锁、索引、外键、崩溃恢复和适用场景。</strong></p><ol><li><strong>事务支持不同</strong></li></ol><ul><li><code>InnoDB</code> 支持事务，满足 <code>ACID</code>，可以 <code>commit</code> 和 <code>rollback</code>。</li><li><code>MyISAM</code> 不支持事务。</li><li>所以订单、支付、库存这类对一致性要求高的场景，一般用 <code>InnoDB</code>。</li></ul><ol><li><strong>锁粒度不同</strong></li></ol><ul><li><code>InnoDB</code> 支持行级锁，也支持表锁。</li><li><code>MyISAM</code> 主要是表级锁。</li><li>所以高并发写入或更新时，<code>InnoDB</code> 并发性能更好。</li></ul><ol><li><strong>外键支持不同</strong></li></ol><ul><li><code>InnoDB</code> 支持外键。</li><li><code>MyISAM</code> 不支持外键。</li><li>不过实际项目中，有些公司即使用 <code>InnoDB</code>，也不一定建物理外键，而是在业务层保证关联关系。</li></ul><ol><li><strong>崩溃恢复能力不同</strong></li></ol><ul><li><code>InnoDB</code> 有 <code>redo log</code>、<code>undo log</code>，支持崩溃恢复。</li><li><code>MyISAM</code> 崩溃恢复能力较弱，异常宕机后更容易出现数据损坏或需要修复。</li></ul><ol><li><strong>索引结构不同</strong></li></ol><ul><li><code>InnoDB</code> 使用聚簇索引，主键索引的叶子节点存放整行数据。</li><li><code>MyISAM</code> 使用非聚簇索引，索引文件和数据文件分开，索引叶子节点存的是数据地址。</li><li>所以 <code>InnoDB</code> 查询通过主键通常效率较高，但二级索引需要先找到主键，再回表查询整行数据。</li></ul><ol><li><strong>表行数统计不同</strong></li></ol><ul><li><code>MyISAM</code> 会保存表的总行数，所以 <code>COUNT(*)</code> 通常很快。</li><li><code>InnoDB</code> 不直接保存精确总行数，需要扫描或根据索引统计。</li><li>但带 <code>WHERE</code> 条件时，两者都需要根据条件查询。</li></ul><ol><li><strong>适用场景不同</strong></li></ol><ul><li><code>InnoDB</code>：适合事务、高并发、数据一致性要求高的业务，是现在默认选择。</li><li><code>MyISAM</code>：适合读多写少、对事务要求不高的老场景，现在实际项目中用得比较少。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>InnoDB 和 MyISAM 最大的区别是，InnoDB 支持事务、行级锁、外键和崩溃恢复，而 MyISAM 不支持事务和外键，主要使用表级锁。索引方面，InnoDB 是聚簇索引，主键索引叶子节点存整行数据；MyISAM 是非聚簇索引，索引和数据分开存，叶子节点存数据地址。另外 MyISAM 会保存表总行数，所以简单 COUNT(*) 比较快。实际开发中，因为 InnoDB 在一致性、并发和可靠性方面更好，所以一般默认使用 InnoDB。</p></blockquote><p></p></blockquote>"},{"t":"数据管理里，数据文件大体分成哪几种数据文件？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><blockquote><p>如果面试官问的是 <strong>MySQL/InnoDB 的数据文件大体分哪几种</strong>，可以这样答：</p><ol><li><strong>表结构文件</strong></li></ol><ul><li>用来保存表结构定义。</li><li>比如表有哪些字段、字段类型、索引信息等。</li><li>MySQL 早期常见 <code>.frm</code> 文件。</li><li>MySQL 8.0 之后，表结构元数据更多放在数据字典里。</li></ul><ol><li><strong>表空间数据文件</strong></li></ol><ul><li>用来真正存储表数据和索引数据。</li><li>InnoDB 中常见的是 <code>.ibd</code> 文件。</li><li>如果开启独立表空间，每张 InnoDB 表通常会有自己的 <code>.ibd</code> 文件。</li><li>里面存的是：<ul><li>表数据</li><li>聚簇索引</li><li>二级索引</li></ul></li></ul><ol><li><strong>共享表空间文件</strong></li></ol><ul><li>常见文件是 <code>ibdata1</code>。</li><li>它属于 InnoDB 的系统表空间。</li><li>可能存放：<ul><li>数据字典</li><li>undo 信息</li><li>insert buffer/change buffer</li><li>早期也可能存表数据和索引</li></ul></li></ul><ol><li><strong>日志文件</strong></li></ol><p>严格来说日志文件不完全等同于“数据文件”，但面试经常一起问：</p><ul><li><code>redo log</code>：保证事务持久性，用于崩溃恢复。</li><li><code>undo log</code>：用于事务回滚和 MVCC。</li><li><code>binlog</code>：Server 层日志，用于主从复制和数据恢复。</li><li><code>relay log</code>：从库用于复制的中继日志。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 的数据相关文件大体可以分为<strong>表结构文件、表空间数据文件、共享表空间文件和日志文件</strong>。表结构文件保存表的元数据；InnoDB 的 <code>.ibd</code> 文件保存具体表的数据和索引；<code>ibdata1</code> 是共享表空间，保存系统表空间相关信息；另外还有 redo log、undo log、binlog 等日志文件，用来支持事务、崩溃恢复、MVCC 和主从复制。现在 InnoDB 最常见的是数据和索引都存放在表空间文件里。</p></blockquote></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"索引","questions":[{"t":"索引是什么？有什么好处？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试可以这样回答：</p><ol><li><strong>索引是什么</strong></li></ol><blockquote><p>索引是数据库为了提高查询效率而维护的一种数据结构，可以理解成书的目录。<br/>没有索引时，数据库可能要全表扫描；有索引时，可以通过索引快速定位到目标数据。</p></blockquote><ol><li><strong>索引的好处</strong></li></ol><ul><li><strong>提高查询效率</strong><ul><li>可以减少扫描的数据量。</li><li>比如根据 <code>user_id</code> 查询订单，如果 <code>user_id</code> 有索引，就不用全表一行行找。</li></ul></li><li><strong>提高排序和分组效率</strong><ul><li>如果 <code>ORDER BY</code>、<code>GROUP BY</code> 的字段命中索引，可以减少额外排序。</li></ul></li><li><strong>提高关联查询效率</strong><ul><li>两张表 <code>JOIN</code> 时，如果关联字段有索引，查询会更快。</li><li>比如 <code>orders.user_id</code> 关联 <code>user.id</code>。</li></ul></li><li><strong>保证数据唯一性</strong><ul><li>唯一索引可以防止重复数据。</li><li>比如手机号、邮箱、订单号可以加唯一索引。</li></ul></li></ul><ol><li><strong>索引的代价</strong></li></ol><p>面试最好补一句，显得全面：</p><ul><li>索引会占用额外存储空间。</li><li>插入、更新、删除时，需要维护索引，所以写入性能会下降。</li><li>索引不是越多越好，要根据查询场景合理设计。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>索引是数据库为了提高查询效率维护的一种数据结构，类似书的目录。没有索引时可能需要全表扫描，有索引后可以快速定位数据。索引的好处主要是提高查询效率，也可以优化排序、分组和连表查询；唯一索引还能保证数据唯一性。但索引也有代价，会占用额外空间，并且插入、更新、删除时需要维护索引，所以索引不是越多越好，要根据实际查询条件合理创建。</p></blockquote></blockquote>"},{"t":"讲讲索引的分类是什么？","tag":"","p":"","html":"<blockquote><p>面试可以按 <strong>“数据结构、物理存储、字段数量、功能约束”</strong> 这几个角度回答，不用一股脑乱说。</p><ol><li><strong>按数据结构分</strong></li></ol><ul><li><strong>B+ 树索引</strong><ul><li>MySQL/InnoDB 最常用的索引结构。</li><li>适合等值查询、范围查询、排序。</li></ul></li><li><strong>哈希索引</strong><ul><li>适合等值查询。</li><li>不适合范围查询和排序。</li><li>InnoDB 有自适应哈希索引，但我们平时建的主要还是 B+ 树索引。</li></ul></li><li><strong>全文索引</strong><ul><li>用于全文检索。</li><li>比如文章内容、标题搜索。</li><li>关键字是 <code>FULLTEXT</code>。</li></ul></li></ul><ol><li><strong>按物理存储分</strong></li></ol><ul><li><strong>聚簇索引</strong><ul><li>InnoDB 中，主键索引就是聚簇索引。</li><li>叶子节点存放的是整行数据。</li><li>一张表只能有一个聚簇索引。</li></ul></li><li><strong>非聚簇索引 / 二级索引</strong><ul><li>普通索引、唯一索引这些都属于二级索引。</li><li>InnoDB 二级索引的叶子节点存的是主键值。</li><li>查询非索引字段时，可能需要回表。</li></ul></li></ul><ol><li><strong>按字段数量分</strong></li></ol><ul><li><strong>单列索引</strong><ul><li>只基于一个字段建立索引。</li><li>比如给 <code>user_id</code> 建索引。</li></ul></li><li><strong>联合索引</strong><ul><li>基于多个字段建立索引。</li><li>比如 <code>(user_id, status, create_time)</code>。</li><li>联合索引要注意最左前缀原则。</li></ul></li></ul><ol><li><strong>按功能约束分</strong></li></ol><ul><li><strong>普通索引</strong><ul><li>只提高查询效率，不限制数据重复。</li></ul></li><li><strong>唯一索引</strong><ul><li>要求索引列的值不能重复。</li><li>比如手机号、邮箱、订单号。</li></ul></li><li><strong>主键索引</strong><ul><li>一种特殊的唯一索引。</li><li>不允许为 <code>NULL</code>。</li><li>一张表只能有一个主键索引。</li></ul></li></ul><p><strong>面试背诵版</strong></p><p>MySQL 索引可以从几个角度分类。</p><ol><li>按<strong>数据结构分</strong>，常见有 B+ 树索引、哈希索引、全文索引，其中 InnoDB 最常用的是 B+ 树索引；</li><li><strong>按物理存储分</strong>，有聚簇索引和非聚簇索引，InnoDB 的主键索引是聚簇索引，叶子节点存整行数据，二级索引叶子节点存主键值；</li><li><strong>按字段数量分，有单列索引和联合索引</strong>，联合索引要遵循最左前缀原则；</li><li><strong>按功能分</strong>，有普通索引、唯一索引和主键索引。实际开发中最常用的是主键索引、普通索引、唯一索引和联合索引。</li></ol><p></p></blockquote>"},{"t":"MySQL聚簇索引和非聚簇索引的区别是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试可以这样回答：</p><ol><li><strong>先说核心区别</strong></li></ol><p><strong>聚簇索引和非聚簇索引最大的区别在于：叶子节点存的内容不同。</strong></p><ul><li><strong>聚簇索引</strong>：叶子节点存的是<strong>整行数据</strong>。</li><li><strong>非聚簇索引</strong>：叶子节点存的是<strong>主键值或数据地址</strong>。</li></ul><p>在 <code>InnoDB</code> 里：</p><ul><li>主键索引就是聚簇索引。</li><li>普通索引、唯一索引这些二级索引，属于非聚簇索引。</li><li>二级索引叶子节点存的是主键值。</li></ul><ol><li><strong>InnoDB 里的聚簇索引</strong></li></ol><p>比如表：</p><p><code>id</code> 是主键，所以 <code>id</code> 索引就是聚簇索引。</p><p>它的结构可以理解为：</p><p>所以如果按主键查：</p><p>可以直接在主键索引里找到整行数据。</p><ol><li><strong>InnoDB 里的非聚簇索引</strong></li></ol><p><code>idx_name(name)</code> 是普通索引，也叫二级索引。</p><p>它的结构可以理解为：</p><p>如果执行：</p><p>流程是：</p><ul><li>先通过 <code>idx_name</code> 找到对应的主键 <code>id</code></li><li>再拿这个 <code>id</code> 去主键索引里查整行数据</li><li>这个过程叫<strong>回表</strong></li></ul><ol><li><strong>一张表能有几个</strong></li></ol><ul><li>聚簇索引：一张表只能有一个。<ul><li>因为数据只能按一种顺序存放。</li></ul></li><li>非聚簇索引：可以有多个。<ul><li>比如给 <code>name</code>、<code>phone</code>、<code>email</code> 分别建索引。</li></ul></li></ul><ol><li><strong>MyISAM 的情况</strong></li></ol><ul><li><code>MyISAM</code> 使用的是非聚簇索引。</li><li>它的索引和数据分开存。</li><li>索引叶子节点存的是数据文件的地址。</li><li>通过索引找到地址，再去数据文件里取数据。</li></ul><p><strong>面试背诵版</strong></p><ol><li>聚簇索引和非聚簇索引的<strong>核心区别是叶子节点存的内容不同</strong>。</li><li>聚簇索引的叶子节点存整行数据，非聚簇索引的叶子节点通常存主键值。在 InnoDB 中，主键索引就是聚簇索引，所以按主键查询可以直接拿到整行数据；普通索引、唯一索引属于二级索引，叶子节点存的是主键值，如果查询的字段不在二级索引里，<strong>就需要根据主键再回到主键索引查询，这个过程叫回表。</strong>一张表只能有一个聚簇索引，但可以有多个非聚簇索引。</li></ol></blockquote>"},{"t":"如果聚簇索引的数据更新，它的存储要不要变化？","tag":"","p":"","html":"<p>要看你更新的是不是<strong>聚簇索引键</strong>，也就是通常说的<strong>主键值</strong>。</p><ol><li><strong>更新普通字段</strong></li></ol><p>如果只是更新普通字段，比如：</p><pre><code>UPDATE user SET name = '李四' WHERE id = 1;</code></pre><ul><li><code>id</code> 没变。</li><li>这行数据在聚簇索引 B+ 树中的位置一般不需要变。</li><li>只需要修改叶子节点里这一行的字段值。</li><li>可能会涉及页内更新、redo/undo 记录，但整体存储位置通常不变。</li></ul><ol><li><strong>更新主键字段</strong></li></ol><p>如果更新的是主键，比如：</p><pre><code>UPDATE user SET id = 100 WHERE id = 1;</code></pre><ul><li><code>id</code> 是聚簇索引键。</li><li>InnoDB 的数据是按照主键组织在 B+ 树里的。</li><li>主键变了，这行数据在 B+ 树中的排序位置可能也变。</li><li>所以通常会类似于：<ul><li>删除旧主键对应的记录</li><li>插入新主键对应的记录</li></ul></li><li>同时，二级索引里存的是主键值，也可能需要维护相关索引。</li></ul><ol><li><strong>所以主键不建议频繁更新</strong></li></ol><ul><li>聚簇索引键一变，数据位置可能变化。</li><li>二级索引也要维护。</li><li>成本比较高。</li><li>所以实际设计中，主键最好：<ul><li>稳定</li><li>短小</li><li>有序</li><li>不频繁修改</li></ul></li></ul><blockquote><p><strong>面试背诵版</strong></p><p>如果更新的是普通字段，聚簇索引的键没有变，那么这行数据在 B+ 树中的位置一般不需要变化，只是在叶子节点中修改对应字段。但如果更新的是主键，也就是聚簇索引键，因为 InnoDB 的数据是按主键组织存储的，主键变化后记录在 B+ 树中的位置可能要变化，通常会涉及删除旧记录、插入新记录，同时还要维护二级索引。所以聚簇索引键不建议频繁更新，主键应该保持稳定。</p></blockquote>"},{"t":"MySQL主键是聚簇索引吗？","tag":"","p":"","html":"<blockquote><p>是的，<strong>在 InnoDB 里，主键就是聚簇索引</strong>。</p><p><strong>面试回答</strong></p><blockquote><p>在 MySQL 的 InnoDB 存储引擎中，主键索引就是聚簇索引。聚簇索引的叶子节点存放的是整行数据，所以通过主键查询可以直接定位到整行记录。</p></blockquote><p><strong>补充一点更完整</strong></p><ul><li>如果表里有主键，<code>InnoDB</code> 会用主键作为聚簇索引。</li><li>如果没有显式主键，<code>InnoDB</code> 会选择第一个不允许为 <code>NULL</code> 的唯一索引作为聚簇索引。</li><li>如果连这样的唯一索引也没有，<code>InnoDB</code> 会自动生成一个隐藏的 <code>row_id</code> 作为聚簇索引。</li></ul><p><strong>对比一下</strong></p><ul><li><code>InnoDB</code>：主键索引是聚簇索引，叶子节点存整行数据。</li><li><code>MyISAM</code>：没有聚簇索引，索引叶子节点存的是数据文件地址。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>在 InnoDB 中，主键就是聚簇索引，叶子节点存放整行数据。如果表没有主键，InnoDB 会选择第一个非空唯一索引作为聚簇索引；如果也没有，就会生成隐藏的 row_id。需要注意这是 InnoDB 的特点，MyISAM 的索引是非聚簇索引，叶子节点存的是数据地址。</p></blockquote></blockquote>"},{"t":"什么字段适合当做主键？","tag":"","p":"","html":"<blockquote><p>适合做主键的字段，一般要满足这几个特点：</p><ol><li><strong>唯一</strong></li></ol><ul><li>主键必须能唯一标识一行数据。</li><li>不能出现重复值。</li><li>比如用户表中的 <code>user_id</code>、订单表中的 <code>order_id</code>。</li></ul><ol><li><strong>非空</strong></li></ol><ul><li>主键不能为 <code>NULL</code>。</li><li>因为它要明确标识一条记录。</li></ul><ol><li><strong>稳定</strong></li></ol><ul><li>主键值最好一旦生成就不要修改。</li><li>因为在 <code>InnoDB</code> 中，主键是聚簇索引。</li><li>如果频繁修改主键，会导致数据位置变化，还会影响二级索引维护。</li></ul><ol><li><strong>短小</strong></li></ol><ul><li>主键字段不要太长。</li><li>因为 <code>InnoDB</code> 的二级索引叶子节点会存主键值。</li><li>如果主键太长，所有二级索引都会变大，占用更多空间。</li></ul><ol><li><strong>尽量有序递增</strong></li></ol><ul><li>有序递增的主键插入性能更好。</li><li>因为 <code>InnoDB</code> 数据按主键组织存储。</li><li>如果主键完全随机，比如随机 UUID，可能导致页分裂，影响插入性能。</li></ul><p><strong>常见选择</strong></p><ul><li>自增 <code>BIGINT</code> / <code>INT</code><ul><li>简单、高效、有序。</li><li>单库单表很常见。</li></ul></li><li>雪花算法 ID<ul><li>全局唯一，大致递增。</li><li>适合分布式系统。</li></ul></li><li>UUID<ul><li>唯一性好。</li><li>但普通 UUID 较长且随机，不太适合作为 InnoDB 主键。</li><li>如果要用，可以考虑有序 UUID 或改成二进制存储。</li></ul></li></ul><p><strong>面试背诵版</strong></p><p>适合做主键的字段应该唯一、非空、稳定、短小，并且最好有序递增。因为 InnoDB 中主键是聚簇索引，数据按照主键组织存储；如果主键太长，会让二级索引变大；如果主键频繁变化或完全随机，会影响插入和索引维护性能。所以实际开发中常用自增 ID、雪花 ID 这类稳定且相对有序的字段作为主键。</p></blockquote>"},{"t":"性别字段能加索引么？为啥？","tag":"","p":"","html":"<blockquote><p>可以加，但<strong>通常不建议单独给性别字段加普通索引</strong>。</p><p><strong>原因</strong></p><ul><li>性别字段的区分度太低。</li><li>比如只有：<ul><li>男</li><li>女</li><li>未知</li></ul></li><li>这种字段的值太少，索引选择性很差。</li><li>MySQL 即使用索引，也可能要扫描大量数据。</li><li>成本不一定比全表扫描低。</li></ul><p><strong>举个例子</strong></p><p>假设用户表有 100 万条数据：</p><p>可能匹配 50 万条。</p><p>这时候走索引的流程可能是：</p><ul><li>先从性别索引找到大量主键。</li><li>再根据主键回表查询数据。</li><li>回表次数很多，成本很高。</li></ul><p>所以优化器可能直接选择全表扫描。</p><p><strong>什么时候可以加</strong></p><ul><li>如果性别经常和其他高区分度字段一起查询，可以放进联合索引。</li><li>比如：</li></ul><p>可以考虑联合索引：</p><p>但一般更建议把区分度高的字段放前面，比如：</p><p>具体还要看查询条件和数据分布。</p><p><strong>面试背诵版</strong></p><p>性别字段可以加索引，但一般不建议单独加。因为性别字段取值很少，区分度低，索引选择性差，查询时可能命中大量数据，导致大量回表，成本不一定比全表扫描低，所以优化器可能不会走这个索引。实际开发中，如果性别经常和其他字段一起作为查询条件，可以考虑放到联合索引里，但通常会把区分度更高的字段放在联合索引前面。</p></blockquote>"},{"t":"表中十个字段，你主键用自增ID还是UUID，为什么？","tag":"","p":"","html":"<blockquote><p>面试里我会优先回答：<strong>一般选自增 ID，不优先选普通 UUID</strong><strong>。</strong></p><ol><li><strong>自增 ID 的优点</strong></li></ol><ul><li><strong>短小</strong><ul><li><code>BIGINT</code> 只占 8 字节。</li><li>普通 <code>UUID</code> 字符串通常占 36 个字符。</li><li>主键越短，索引占用越小。</li></ul></li><li><strong>有序递增</strong><ul><li><code>InnoDB</code> 的主键是聚簇索引。</li><li>数据按主键顺序组织。</li><li>自增 ID 插入通常追加到 B+ 树末尾。</li><li>插入性能好，页分裂少。</li></ul></li><li><strong>二级索引更省空间</strong><ul><li><code>InnoDB</code> 的二级索引叶子节点会存主键值。</li><li>如果主键是 UUID，所有二级索引都会变大。</li><li>表里字段越多、索引越多，影响越明显。</li></ul></li></ul><ol><li><strong>UUID 的问题</strong></li></ol><ul><li><strong>太长</strong><ul><li>存储空间大。</li><li>索引也更大。</li></ul></li><li><strong>随机无序</strong><ul><li>普通 UUID 插入位置随机。</li><li>容易导致 B+ 树页分裂。</li><li>插入性能和缓存命中率都会受影响。</li></ul></li><li><strong>可读性一般</strong><ul><li>排查数据时不如自增数字直观。</li></ul></li></ul><ol><li><strong>UUID 什么时候适合</strong></li></ol><ul><li>分布式系统需要全局唯一 ID。</li><li>多个库、多个服务都要独立生成 ID。</li><li>不希望暴露数据增长趋势。</li><li>离线生成 ID，不依赖数据库。</li></ul><p>但如果要用，最好不要直接用普通字符串 UUID，可以考虑：</p><ul><li>雪花算法 ID</li><li>有序 UUID</li><li><code>BINARY(16)</code> 存 UUID，减少空间</li></ul><p><strong>面试背诵版</strong></p><ol><li>如果是<strong>普通单库单表场景，我会优先选择自增 ID</strong>。因为 InnoDB 的主键是聚簇索引，自增 ID 短小、有序，插入时基本追加到 B+ 树末尾，页分裂少，索引占用也小；</li><li>而普通 UUID 比较长且随机，会让主键索引和所有二级索引变大，还容易造成页分裂，影响插入性能。UUID 更适合分布式、需要全局唯一或者不想暴露数据量的场景，但实际项目里通常会用雪花 ID、有序 UUID 或 BINARY(16) 优化。</li></ol></blockquote>"},{"t":"什么自增ID更快一些，UUID不快吗，它在B+树里面存储是有序的吗?","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>对，关键点就在 <strong>InnoDB 的 B+ 树是按主键值有序存储的</strong>。</p><ol><li><strong>自增 ID 为什么更快</strong></li></ol><p>在 <code>InnoDB</code> 里，主键是聚簇索引，整行数据挂在主键 B+ 树的叶子节点上。</p><p>如果主键是自增 ID：</p><p>新数据基本都插到 B+ 树最右边的叶子页，类似“顺序追加”。</p><p>好处是：</p><ul><li>插入位置稳定。</li><li>页分裂少。</li><li>磁盘/缓存局部性更好。</li><li>索引维护成本低。</li><li>二级索引里存的主键值也更短。</li></ul><p>所以自增 ID 通常插入性能更好。</p><ol><li><strong>普通 UUID 为什么不快</strong></li></ol><p>普通 UUID 大概长这样：</p><p>它的问题有两个：</p><p><strong>第一，长。</strong></p><ul><li><code>BIGINT</code>：8 字节。</li><li>UUID 字符串：通常 36 个字符。</li><li>主键越长，主键索引越大。</li><li>InnoDB 二级索引叶子节点还会存主键值，所以二级索引也会变大。</li></ul><p><strong>第二，随机。</strong></p><p>普通 UUID 不是递增的。</p><p>它插入 B+ 树时，位置可能在中间任意地方：</p><p>这会导致：</p><ul><li>插入位置随机。</li><li>频繁页分裂。</li><li>缓存命中率下降。</li><li>索引维护成本变高。</li></ul><p>所以普通随机 UUID 通常不如自增 ID 快。</p><ol><li><strong>UUID 在 B+ 树里是不是有序的？</strong></li></ol><p>是的，<strong>B+ 树里面一定是按索引键有序存储的</strong>。</p><p>但区别在于：</p><ul><li>B+ 树会把 UUID 按字典序/二进制序排好。</li><li>不是说 UUID 本身是递增生成的。</li><li>普通 UUID 生成出来是随机的，所以每次插入位置不固定。</li></ul><p>也就是说：</p><blockquote><p>B+ 树存储后是有序的，但 UUID 插入时不是顺序进入的。</p></blockquote><ol><li><strong>有没有快一点的 UUID？</strong></li></ol><p>有。</p><p>如果必须用 UUID，可以考虑：</p><ul><li>有序 UUID</li><li>UUIDv7</li><li>把 UUID 存成 <code>BINARY(16)</code>，不要用 <code>CHAR(36)</code></li><li>MySQL 的 <code>UUID_TO_BIN(uuid, 1)</code> 可以把时间相关部分调整到前面，提高局部有序性</li></ul><p>这样会比普通随机 UUID 友好很多。</p><p><strong>面试总结版</strong></p><ul><li>自增 ID 更快主要是因为 InnoDB 的主键是聚簇索引，B+ 树按主键有序组织数据。<strong>自增 ID 是递增的，新数据基本插入到最右侧叶子节点，页分裂少，缓存命中率好</strong>；</li><li>而普通 UUID 又长又随机，虽然在 B+ 树中最终会按索引键有序存储，但每次插入的位置可能是随机的，<strong>容易导致页分裂和索引维护成本增加</strong>。所以普通 UUID 通常不如自增 ID 快。如果必须用 UUID，可以考虑有序 UUID、UUIDv7 或用 BINARY(16) 存储来优化。</li></ul></blockquote>"},{"t":"Mysql中的索引是怎么实现的 ？","tag":"","p":"","html":"<blockquote><p>面试可以这样回答：<strong>MySQL 索引主要是由</strong><strong>存储引擎</strong><strong>实现的，InnoDB 最常用的是 B+ 树索引。</strong></p><ol><li><strong>先说整体</strong></li></ol><blockquote><p>MySQL 的索引不是 Server 层统一实现的，而是由具体存储引擎实现的。比如 InnoDB 主要使用 B+ 树索引，Memory 引擎可以使用哈希索引。实际开发中最常说的 MySQL 索引，一般指 InnoDB 的 B+ 树索引。</p></blockquote><ol><li><strong>InnoDB 为什么用 B+ 树</strong></li></ol><p><code>InnoDB</code> 使用 <code>B+Tree</code> 来组织索引数据。</p><p>B+ 树特点：</p><ul><li>多路平衡搜索树。</li><li>树高度低，磁盘 IO 次数少。</li><li>非叶子节点只存索引键和指针。</li><li>叶子节点存真实数据或主键值。</li><li>叶子节点之间有双向链表，适合范围查询和排序。</li></ul><ol><li><strong>主键索引怎么实现</strong></li></ol><p>在 <code>InnoDB</code> 中，主键索引是聚簇索引。</p><ul><li>B+ 树按照主键值有序排列。</li><li>叶子节点存放整行数据。</li><li>所以按主键查询时，可以直接通过主键索引找到整行记录。</li></ul><p>例如：</p><p>查询过程是：</p><ol><li><strong>二级索引怎么实现</strong></li></ol><p>普通索引、唯一索引都属于二级索引。</p><p>在 <code>InnoDB</code> 中：</p><ul><li>二级索引也是 B+ 树。</li><li>叶子节点存的是索引字段值 + 主键值。</li><li>如果查询字段不在二级索引里，需要根据主键再查一次主键索引。</li><li>这个过程叫回表。</li></ul><p>比如：</p><p>如果 <code>name</code> 有索引：</p><ol><li><strong>覆盖索引</strong></li></ol><p>如果查询的字段都在二级索引里，就不需要回表。</p><p>例如有联合索引：</p><p>执行：</p><p>只通过二级索引就能返回数据，这叫覆盖索引。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>MySQL 的索引由存储引擎实现，InnoDB 最常用的是 B+ 树索引。B+ 树高度低，可以减少磁盘 IO，并且叶子节点有链表，适合范围查询和排序。在 InnoDB 中，主键索引是聚簇索引，叶子节点存整行数据；普通索引和唯一索引是二级索引，叶子节点存索引字段和主键值。如果通过二级索引查不到全部字段，就需要再根据主键回到聚簇索引查询整行数据，这个过程叫回表。如果查询字段都在索引中，就叫覆盖索引，可以避免回表。</p></blockquote></blockquote>"},{"t":"查询数据时，到了B+树的叶子节点，之后的查找数据是如何做？","tag":"叶子节点里面怎么找到具体一条记录","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>要分 <strong>主键索引</strong> 和 <strong>二级索引</strong> 两种情况说。</p><ol><li><strong>如果走的是主键索引</strong></li></ol><p>在 <code>InnoDB</code> 里，主键索引就是聚簇索引。</p><ul><li>B+ 树叶子节点存的是<strong>整行数据</strong>。</li><li>所以查到叶子节点后，就可以直接拿到这行记录。</li><li>不需要再去别的地方找。</li></ul><p>例如：</p><p>执行过程：</p><ol><li><strong>如果走的是二级索引</strong></li></ol><p>普通索引、唯一索引属于二级索引。</p><ul><li>二级索引的叶子节点存的是：<strong>索引字段 + 主键值</strong>。</li><li>查到叶子节点后，先拿到主键值。</li><li>如果查询还需要其他字段，就用这个主键值再去主键索引里查整行数据。</li><li>这个过程叫<strong>回表</strong>。</li></ul><p>例如：</p><p>假设 <code>name</code> 有索引：</p><ol><li><strong>如果是覆盖索引</strong></li></ol><p>如果要查的字段都在二级索引里，就不用回表。</p><p>例如有联合索引：</p><p>执行：</p><p>因为 <code>name</code> 和 <code>age</code> 都在索引里：</p><p>这就叫<strong>覆盖索引</strong>。</p><ol><li><strong>如果是范围查询</strong></li></ol><p>比如：</p><p>B+ 树找到第一个满足条件的叶子节点后，会沿着叶子节点之间的链表继续往后扫描，直到不满足条件为止。</p><p><strong>面试背诵版</strong></p><blockquote><p>查询到 B+ 树叶子节点后，要看走的是主键索引还是二级索引。如果是 InnoDB 的主键索引，叶子节点存的是整行数据，可以直接返回；如果是二级索引，叶子节点存的是索引字段和主键值，如果查询字段不全，就需要拿主键值再去聚簇索引里查整行数据，这叫回表。如果查询字段都在索引里，就是覆盖索引，可以避免回表。范围查询时，找到起点后会沿着叶子节点链表继续扫描。</p></blockquote></blockquote>"},{"t":"B+树的特性是什么？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：</p><ol><li><strong>B+ 树是什么</strong></li></ol><blockquote><p>B+ 树是一种多路平衡搜索树，是 MySQL InnoDB 索引常用的数据结构。它的特点是树高比较低，适合磁盘存储和范围查询。</p></blockquote><ol><li><strong>B+ 树主要特性</strong></li></ol><ul><li><strong>多路平衡</strong><ul><li>一个节点可以有多个子节点。</li><li>树的高度比较低。</li><li>查询时磁盘 IO 次数少。</li></ul></li><li><strong>所有数据都在叶子节点</strong><ul><li>非叶子节点只存索引 key 和指针。</li><li>叶子节点存放数据，或者存放主键值。</li><li>所以非叶子节点可以存更多 key，让树更矮。</li></ul></li><li><strong>叶子节点有序</strong><ul><li>叶子节点按照索引 key 从小到大排列。</li><li>适合等值查询、范围查询和排序。</li></ul></li><li><strong>叶子节点之间有链表</strong><ul><li>InnoDB 中叶子节点之间是双向链表。</li><li>范围查询时，找到起点后可以顺着链表往后扫。</li><li>比如 <code>BETWEEN</code>、<code>&gt;</code>、<code>&lt;</code>。</li></ul></li><li><strong>查询路径稳定</strong><ul><li>因为所有数据都在叶子节点。</li><li>每次查询最终都会走到叶子节点。</li><li>查询性能相对稳定。</li></ul></li></ul><ol><li><strong>为什么适合做数据库索引</strong></li></ol><ul><li>树高低，减少磁盘 IO。</li><li>叶子节点有序，适合范围查询。</li><li>非叶子节点不存整行数据，可以放更多索引 key。</li><li>查询、插入、删除都能保持相对稳定的复杂度。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>B+ 树是一种多路平衡搜索树，InnoDB 索引底层主要用 B+ 树实现。它的特点是非叶子节点只存索引 key 和指针，真正的数据都在叶子节点；叶子节点按 key 有序排列，并且通过链表连接，所以范围查询很方便。因为一个节点可以存多个 key，树高比较低，可以减少磁盘 IO。相比普通二叉树或 B 树，B+ 树更适合数据库这种磁盘存储场景。</p></blockquote></blockquote>"},{"t":"说说B+树和B树的区别","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试可以这样答：</p><ol><li><strong>核心区别</strong></li></ol><blockquote><p>B 树和 B+ 树都是多路平衡搜索树。<br/>最大区别是：<strong>B 树的非叶子节点也可以存数据，而 B+ 树的数据只存放在叶子节点。</strong></p></blockquote><ol><li><strong>数据存储位置不同</strong></li></ol><ul><li><strong>B 树</strong><ul><li>非叶子节点和叶子节点都可以存 key 和数据。</li><li>查询时可能在非叶子节点就找到数据。</li></ul></li><li><strong>B+ 树</strong><ul><li>非叶子节点只存 key 和指针。</li><li>真正的数据都在叶子节点。</li><li>查询最终都会走到叶子节点。</li></ul></li></ul><ol><li><strong>范围查询能力不同</strong></li></ol><ul><li><strong>B 树</strong><ul><li>叶子节点之间通常没有链表。</li><li>范围查询需要中序遍历，比较麻烦。</li></ul></li><li><strong>B+ 树</strong><ul><li>叶子节点之间有链表。</li><li>找到范围起点后，顺着链表往后扫就行。</li><li>所以 B+ 树更适合范围查询、排序扫描。</li></ul></li></ul><ol><li><strong>树高和磁盘 IO 不同</strong></li></ol><ul><li><strong>B 树</strong><ul><li>非叶子节点存数据，所以一个节点能放的 key 相对少。</li><li>树可能更高。</li></ul></li><li><strong>B+ 树</strong><ul><li>非叶子节点不存整行数据，只存 key 和指针。</li><li>一个节点能放更多 key。</li><li>树更矮，磁盘 IO 更少。</li></ul></li></ul><ol><li><strong>查询稳定性不同</strong></li></ol><ul><li><strong>B 树</strong><ul><li>有些数据在根节点或中间节点就能找到。</li><li>有些数据要查到叶子节点。</li><li>查询路径长度不完全一致。</li></ul></li><li><strong>B+ 树</strong><ul><li>所有数据都在叶子节点。</li><li>每次查询都会走到叶子节点。</li><li>查询路径更稳定。</li></ul></li></ul><p><strong>面试背诵版</strong></p><blockquote><p>B 树和 B+ 树都是多路平衡搜索树。主要区别是 B 树的非叶子节点也能存数据，而 B+ 树非叶子节点只存 key 和指针，真正数据都在叶子节点。这样 B+ 树的非叶子节点能存更多 key，树高更低，磁盘 IO 更少。同时 B+ 树的叶子节点之间有链表，范围查询和排序扫描更方便。所以数据库索引，尤其是 MySQL InnoDB，更适合使用 B+ 树。</p></blockquote></blockquote>"},{"t":"B+树的好处是什么？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：</p><ol><li><strong>树高低，磁盘 IO 少</strong></li></ol><ul><li>B+ 树是多路平衡树。</li><li>一个节点可以存很多 key。</li><li>所以同样数据量下，树高通常很低。</li><li>查询时访问的磁盘页少，性能更好。</li></ul><ol><li><strong>范围查询效率高</strong></li></ol><ul><li>B+ 树的叶子节点是有序的。</li><li>叶子节点之间还有链表。</li><li>范围查询时，先定位到起点，再顺着叶子节点链表往后扫即可。</li></ul><p>比如：</p><p>B+ 树很适合这种查询。</p><ol><li><strong>查询性能稳定</strong></li></ol><ul><li>B+ 树的数据都在叶子节点。</li><li>每次查询都会从根节点走到叶子节点。</li><li>路径长度差不多。</li><li>所以查询性能比较稳定。</li></ul><ol><li><strong>更适合磁盘存储</strong></li></ol><ul><li>非叶子节点只存 key 和指针，不存整行数据。</li><li>一个页里可以放更多 key。</li><li>这样树更矮，也更适合数据库按页读取的方式。</li></ul><ol><li><strong>支持排序扫描</strong></li></ol><ul><li>因为叶子节点本身有序。</li><li>所以对索引字段做 <code>ORDER BY</code>、<code>GROUP BY</code> 时，有机会利用索引减少排序成本。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>B+ 树的好处主要有几个：第一，它是多路平衡树，树高低，可以减少磁盘 IO；第二，非叶子节点只存 key 和指针，一个节点能放更多索引项；第三，所有数据都在叶子节点，查询路径比较稳定；第四，叶子节点有序并通过链表连接，非常适合范围查询和排序扫描。所以 MySQL InnoDB 选择 B+ 树作为主要索引结构。</p></blockquote></blockquote>"},{"t":"B+树的叶子节点链表是单向还是双向？","tag":"","p":"","html":"<blockquote><p>在 <strong>MySQL InnoDB</strong> 里，B+ 树的叶子节点之间是<strong>双向链表</strong>。</p><p><strong>面试回答</strong></p><blockquote><p>B+ 树的叶子节点一般会通过链表连接起来。在 MySQL InnoDB 中，叶子节点之间是双向链表，这样既方便从小到大扫描，也方便从大到小扫描，有利于范围查询和排序。</p></blockquote><p><strong>为什么要双向</strong></p><ul><li>支持正向范围扫描：</li><li>也支持反向范围扫描：</li><li>找到范围起点后，可以沿着链表连续扫描。</li><li>不需要每查一条都重新从根节点走一遍。</li></ul><p><strong>一句话记忆</strong></p><blockquote><p>InnoDB 的 B+ 树叶子节点是有序的，并且通过双向链表连接，方便范围查询和正反向排序扫描。</p></blockquote><p></p></blockquote>"},{"t":"MySQL为什么用B+树结构？和其他结构比的优点？","tag":"","p":"","html":"<blockquote><p>面试可以这样回答：</p><ol><li><strong>先说结论</strong></li></ol><blockquote><p>MySQL InnoDB 使用 B+ 树做索引，主要是因为 B+ 树更适合磁盘存储场景，树高低、IO 次数少，同时叶子节点有序并通过链表连接，范围查询和排序扫描效率高。</p></blockquote><ol><li><strong>和二叉搜索树 / 红黑树比</strong></li></ol><ul><li>二叉树、红黑树一个节点通常只存一个 key。</li><li>数据量大时树高会比较高。</li><li>数据库索引通常存在磁盘页里，树高越高，磁盘 IO 越多。</li><li>B+ 树是多路树，一个节点能存很多 key。</li><li>所以 B+ 树更矮，查询 IO 更少。</li></ul><p>简单说：</p><blockquote><p>红黑树适合内存查找，B+ 树更适合磁盘查找。</p></blockquote><ol><li><strong>和 Hash 索引比</strong></li></ol><ul><li>Hash 索引等值查询很快。</li><li>但它不适合：<ul><li>范围查询</li><li>排序</li><li>前缀匹配</li></ul></li><li>例如：</li></ul><p>Hash 索引就不合适。</p><p>B+ 树索引是有序的，所以既支持等值查询，也支持范围查询和排序。</p><ol><li><strong>和 B 树比</strong></li></ol><ul><li>B 树的非叶子节点也存数据。</li><li>每个节点能放的 key 相对少。</li><li>树可能更高。</li><li>B+ 树的非叶子节点只存 key 和指针。</li><li>一个节点能放更多 key，树更矮，IO 更少。</li><li>B+ 树的数据都在叶子节点，并且叶子节点有链表，范围查询更方便。</li></ul><ol><li><strong>B+ 树自己的优点</strong></li></ol><ul><li>树高低，减少磁盘 IO。</li><li>叶子节点有序，支持范围查询。</li><li>叶子节点链表，适合顺序扫描。</li><li>查询路径稳定。</li><li>支持排序、分组等场景。</li><li>更适合数据库按页读取的存储模型。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>MySQL InnoDB 使用 B+ 树，是因为数据库索引主要存放在磁盘上，核心目标是减少磁盘 IO。B+ 树是多路平衡树，一个节点能存很多 key，树高很低；而且非叶子节点只存 key 和指针，能进一步降低树高。相比红黑树，B+ 树更矮，更适合磁盘页读取；相比 Hash，B+ 树不仅支持等值查询，还支持范围查询、排序和前缀匹配；相比 B 树，B+ 树叶子节点有序并通过链表连接，范围查询更高效。所以 InnoDB 主要使用 B+ 树作为索引结构。</p></blockquote><p></p></blockquote>"},{"t":"为什么 MysSQL 不用 跳表？","tag":"","p":"","html":"<blockquote><p>面试可以这样回答：</p><ol><li><strong>先说结论</strong></li></ol><blockquote><p>MySQL InnoDB 不用跳表，主要是因为数据库索引主要面向磁盘页存储，而 B+ 树更适合磁盘 IO 模型。B+ 树扇出高、树高低、范围查询稳定；跳表更适合内存场景，比如 Redis。</p></blockquote><ol><li><strong>B+ 树更适合磁盘页</strong></li></ol><ul><li>MySQL/InnoDB 的数据是按页管理的。</li><li>一个页通常可以存很多索引 key。</li><li>B+ 树一个节点正好可以对应一个数据页。</li><li>每次 IO 可以读出大量 key，减少磁盘访问次数。</li></ul><p>而跳表：</p><ul><li>是多层链表结构。</li><li>节点比较分散。</li><li>指针跳转多。</li><li>对磁盘页和缓存局部性不如 B+ 树友好。</li></ul><ol><li><strong>B+ 树查询 IO 更稳定</strong></li></ol><ul><li>B+ 树是多路平衡树。</li><li>树高通常很低，比如几千万数据可能也就 3 到 4 层。</li><li>查询时访问的页数量比较稳定。</li></ul><p>跳表虽然平均复杂度也是 <code>O(log n)</code>：</p><ul><li>但层数随机。</li><li>节点分布比较分散。</li><li>在磁盘场景下可能产生更多随机 IO。</li></ul><ol><li><strong>范围查询 B+ 树更合适</strong></li></ol><ul><li>B+ 树叶子节点有序，并且通过链表连接。</li><li>找到范围起点后，可以顺序扫描叶子页。</li><li>这对数据库的范围查询、排序扫描非常重要。</li></ul><p>跳表也能做范围查询，但它的节点通常更零散，磁盘顺序读优势不如 B+ 树明显。</p><ol><li><strong>跳表更适合内存数据库</strong></li></ol><ul><li>跳表结构简单。</li><li>插入、删除实现方便。</li><li>并发控制相对容易。</li><li>所以 Redis 的有序集合底层用了跳表。</li></ul><p>但 MySQL 这种磁盘型关系数据库，更看重：</p><ul><li>页利用率</li><li>磁盘 IO 次数</li><li>缓存局部性</li><li>范围扫描性能</li></ul><p>这些方面 B+ 树更有优势。</p><p><strong>面试背诵版</strong></p><blockquote><p>MySQL InnoDB 不用跳表，主要是因为 InnoDB 的索引是面向磁盘页设计的。B+ 树一个节点可以存很多 key，扇出高、树高低，每次磁盘 IO 能读取一整页数据，查询 IO 次数少且稳定；叶子节点有序并通过链表连接，也很适合范围查询和排序扫描。跳表虽然平均复杂度也是 O(log n)，但节点更分散、指针跳转多，更适合内存场景，比如 Redis。对于 MySQL 这种磁盘存储的数据库，B+ 树在页利用率、缓存局部性和范围扫描上更有优势。</p></blockquote><p></p></blockquote>"},{"t":"联合索引的实现原理？","tag":"","p":"","html":"<blockquote><p>面试可以这样回答：</p><ol><li><strong>先说结论</strong></li></ol><blockquote><p>联合索引就是把多个字段组合在一起建立一个 B+ 树索引。它不是给每个字段分别建索引，而是按照索引定义的字段顺序，把多个字段拼成一个联合 key，在 B+ 树中按这个联合 key 排序。</p></blockquote><p>比如：</p><p>这个联合索引的排序规则可以理解为：</p><ol><li><strong>底层怎么存</strong></li></ol><p>在 InnoDB 中，联合索引也是一棵 B+ 树。</p><p>如果是二级联合索引：</p><p>叶子节点大概存：</p><p>如果查询需要的字段都在这个索引里，就可以直接返回，形成覆盖索引。</p><ol><li><strong>为什么有最左前缀原则</strong></li></ol><p>因为联合索引是按字段顺序排序的。</p><p>对于：</p><p>B+ 树先按 <code>name</code> 排序，所以：</p><p>可以用索引。</p><p>也可以用索引。</p><p>也可以用索引。</p><p>但是：</p><p>一般不能有效使用这个联合索引。</p><p>因为整棵树不是先按 <code>age</code> 排序的，而是先按 <code>name</code> 排序。</p><ol><li><strong>遇到范围查询会怎么样</strong></li></ol><p>联合索引通常会用到范围条件之前的字段，以及范围字段本身。</p><p>比如索引是：</p><p>SQL 是：</p><p>一般可以用到：</p><p>但 <code>city</code> 很可能不能继续用于缩小索引扫描范围。</p><p>因为 <code>age &gt; 18</code> 是范围查询，范围后面的字段整体有序性就不能继续精确定位了。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>联合索引的底层也是一棵 B+ 树，只不过它的索引 key 是由多个字段按顺序组成的。比如建立 <code>(name, age, city)</code> 联合索引，<strong>B+ 树会先按 name 排序，name 相同再按 age 排序，age 相同再按 city 排序</strong>。InnoDB 的二级联合索引叶子节点存的是这些索引字段加主键值。因为排序是从第一个字段开始的，所以使用时要遵循最左前缀原则；如果跳过最左字段，比如只按 age 查，一般不能很好地利用这个联合索引。范围查询后面的字段通常也不能继续用于精确定位。</p></blockquote></blockquote>"},{"t":"创建联合索引时需要注意什么？","tag":"","p":"","html":"<p>面试可以这样回答：</p><ol><li><strong>遵循最左前缀原则</strong></li></ol><ul><li>联合索引是按字段顺序排序的。</li><li>例如：</li></ul><pre><code>KEY idx_a_b_c(a, b, c)</code></pre><p>能较好利用索引的情况：</p><pre><code>WHERE a = ?<br/>WHERE a = ? AND b = ?<br/>WHERE a = ? AND b = ? AND c = ?</code></pre><p>不太能利用完整索引的情况：</p><pre><code>WHERE b = ?<br/>WHERE b = ? AND c = ?</code></pre><p>因为跳过了最左边的 <code>a</code>。</p><ol><li><strong>区分度高的字段尽量靠前</strong></li></ol><ul><li>区分度高，过滤效果更好。</li><li>比如 <code>user_id</code> 通常比 <code>gender</code> 更适合放前面。</li><li>但也不是绝对，还要结合查询条件。</li></ul><ol><li><strong>高频查询字段优先</strong></li></ol><ul><li>联合索引不是随便建。</li><li>要根据 <code>WHERE</code>、<code>JOIN</code>、<code>ORDER BY</code>、<code>GROUP BY</code> 中经常出现的字段设计。</li><li>哪些 SQL 最常用、最慢，就优先优化哪些。</li></ul><ol><li><strong>等值条件字段放前，范围字段放后</strong></li></ol><p>例如：</p><pre><code>WHERE user_id = ?<br/>  AND status = ?<br/>  AND create_time BETWEEN ? AND ?</code></pre><p>可以考虑：</p><pre><code>(user_id, status, create_time)</code></pre><p>因为范围查询后面的字段通常不能继续用于精确定位，所以范围字段一般放后面。</p><ol><li><strong>尽量考虑覆盖索引</strong></li></ol><p>如果查询字段都在联合索引里，就可以避免回表。</p><p>例如常查：</p><pre><code>SELECT user_id, status, create_time<br/>FROM orders<br/>WHERE user_id = ?<br/>  AND status = ?;</code></pre><p>可以考虑：</p><pre><code>(user_id, status, create_time)</code></pre><p>这样可能形成覆盖索引。</p><ol><li><strong>不要建太多重复索引</strong></li></ol><p>比如已经有：</p><pre><code>KEY idx_a_b(a, b)</code></pre><p>通常没必要再单独建：</p><pre><code>KEY idx_a(a)</code></pre><p>因为 <code>(a, b)</code> 可以满足 <code>a</code> 的查询。</p><p>但如果有：</p><pre><code>KEY idx_b(b)</code></pre><p>那不一定重复，因为 <code>(a, b)</code> 不能很好支持单独按 <code>b</code> 查询。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>创建联合索引时，首先要遵循最左前缀原则，因为联合索引是按照字段顺序组织的。字段顺序要结合业务 SQL 来设计，通常把高频查询、过滤性好的字段放前面，等值条件字段放前，范围查询字段放后。同时可以考虑覆盖索引，把查询需要的字段放进索引中，减少回表。另外索引不是越多越好，要避免重复索引和低效索引，因为索引会占用空间，也会增加写入维护成本。</p></blockquote>"},{"t":"联合索引ABC，现在有个执行语句是A = XXX and C < XXX，索引怎么走","tag":"","p":"","html":"<p>对于联合索引：</p><pre><code>KEY idx_abc(a, b, c)</code></pre><p>查询条件是：</p><pre><code>WHERE a = xxx<br/>  AND c &lt; xxx</code></pre><p><strong>结论</strong></p><ul><li><code>A = xxx</code> 可以用到索引。</li><li><code>C &lt; xxx</code> 通常不能用于索引的连续精确定位。</li><li>因为中间跳过了 <code>B</code>，不满足最左前缀原则。</li><li>所以索引主要走到 <code>A</code> 这一层，然后在 <code>A = xxx</code> 的结果范围内再判断 <code>C &lt; xxx</code>。</li></ul><p><strong>为什么</strong></p><p>联合索引 <code>(A, B, C)</code> 的排序方式是：</p><pre><code>先按 A 排序<br/>A 相同，再按 B 排序<br/>B 相同，再按 C 排序</code></pre><p>也就是说，只有在 <code>A</code>、<code>B</code> 都确定的情况下，<code>C</code> 才是有序可利用的。</p><p>现在条件只有：</p><pre><code>A = xxx<br/>C &lt; xxx</code></pre><p>中间的 <code>B</code> 没有条件。</p><p>在 <code>A = xxx</code> 的这段数据里，整体是按 <code>B</code> 排序的；只有同一个 <code>B</code> 内部，<code>C</code> 才有序。</p><p>所以不能直接用 <code>C &lt; xxx</code> 去缩小连续索引范围。</p><p><strong>执行过程大概是</strong></p><pre><code>1. 通过联合索引定位到 A = xxx 的范围<br/>2. 扫描这个范围内的索引记录<br/>3. 对每条记录判断 C &lt; xxx<br/>4. 满足条件的返回</code></pre><p><strong>补充：索引下推 ICP</strong></p><p>MySQL 5.6 以后有索引下推 <code>Index Condition Pushdown</code>。</p><p>如果 <code>C</code> 在联合索引里，虽然不能用它做连续范围定位，但可以在扫描索引时直接判断 <code>C &lt; xxx</code>，减少回表。</p><p>也就是说：</p><ul><li><code>A</code> 用来定位索引范围。</li><li><code>C</code> 可以在索引层做过滤。</li><li>但 <code>C</code> 不是用来缩小 B+ 树扫描区间的关键条件。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>对于联合索引 <code>(A, B, C)</code>，条件是 <code>A = xxx AND C &lt; xxx</code>，一般只能利用到 <code>A</code> 来定位索引范围，因为中间跳过了 <code>B</code>，不满足最左前缀原则。</p><p>联合索引是先按 A 排序，A 相同再按 B 排序，B 相同再按 C 排序，所以在 B 不确定的情况下，C 不能用于连续范围扫描。</p><p><strong>不过因为 C 本身在索引里，MySQL 可能通过索引下推在扫描 </strong><strong><code>A = xxx</code></strong><strong> 的索引范围时提前过滤 </strong><strong><code>C &lt; xxx</code></strong><strong>，减少回表。</strong></p></blockquote>"},{"t":"联合索引(a,b,c) ，查询条件 where b > xxx and a = x 会生效吗","tag":"","p":"","html":"<p>会生效，但要分清楚：<strong>能用到 </strong><strong><code>a</code></strong><strong> 和 </strong><strong><code>b</code></strong><strong>，但 </strong><strong><code>b</code></strong><strong> 是范围条件。</strong></p><p>联合索引：</p><pre><code>KEY idx_a_b_c(a, b, c)</code></pre><p>查询条件：</p><pre><code>WHERE b &gt; xxx<br/>  AND a = x</code></pre><p>虽然 SQL 里写的是先 <code>b</code> 后 <code>a</code>，但优化器会调整条件顺序，逻辑上等价于：</p><pre><code>WHERE a = x<br/>  AND b &gt; xxx</code></pre><p>所以可以使用联合索引的最左前缀：</p><pre><code>a, b</code></pre><p><strong>执行过程</strong></p><ul><li>先通过 <code>a = x</code> 定位到 <code>a</code> 这一段索引范围。</li><li>在这段范围内，继续利用 <code>b &gt; xxx</code> 做范围扫描。</li><li>因为 <code>b</code> 是范围条件，所以后面的 <code>c</code> 通常不能继续用于精确定位。</li></ul><p><strong>重点</strong></p><ul><li>条件书写顺序不重要。</li><li>索引字段顺序才重要。</li><li>只要查询条件里有最左字段 <code>a</code>，就有机会用到联合索引。</li><li>这里 <code>a = x</code> 是等值，<code>b &gt; xxx</code> 是范围，所以可以用到 <code>(a, b)</code>。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>会生效。联合索引是 <code>(a,b,c)</code>，虽然 SQL 写的是 <code>where b &gt; xxx and a = x</code>，但 MySQL 优化器会调整条件顺序，等价于 <code>a = x and b &gt; xxx</code>。所以可以先用 <code>a = x</code> 定位索引范围，再用 <code>b &gt; xxx</code> 做范围扫描，也就是能用到联合索引的 <code>a,b</code>。但因为 <code>b</code> 是范围条件，后面的 <code>c</code> 一般就不能继续用于精确定位了。</p></blockquote>"},{"t":"联合索引 (a, b，c)，where条件是 a=2 and c = 1，能用到联合索引吗？","tag":"","p":"","html":"<p>能用到，但要说准确：<strong>主要用到 </strong><strong><code>a</code></strong><strong>，</strong><strong><code>c</code></strong><strong> 通常不能用于连续定位，但可能用于索引下推过滤。</strong></p><p>联合索引：</p><pre><code>KEY idx_a_b_c(a, b, c)</code></pre><p>查询：</p><pre><code>WHERE a = 2<br/>  AND c = 1</code></pre><p><strong>怎么走索引</strong></p><ul><li><code>a = 2</code> 符合最左前缀原则，所以能用联合索引。</li><li>但是中间跳过了 <code>b</code>。</li><li>联合索引顺序是：</li></ul><pre><code>先按 a 排序<br/>a 相同，再按 b 排序<br/>b 相同，再按 c 排序</code></pre><ul><li>在 <code>b</code> 不确定的情况下，<code>c</code> 不能用于缩小连续扫描范围。</li></ul><p><strong>执行过程</strong></p><pre><code>1. 通过索引定位到 a = 2 的范围<br/>2. 扫描 a = 2 的这段索引<br/>3. 判断 c = 1<br/>4. 返回符合条件的数据</code></pre><p><strong>MySQL 5.6 后的索引下推</strong></p><p>因为 <code>c</code> 也在联合索引里，MySQL 可能使用 <code>ICP</code> 索引下推：</p><ul><li>在扫描索引时就判断 <code>c = 1</code></li><li>不满足的记录少回表</li><li>但这不等于 <code>c</code> 用来定位索引范围</li></ul><p><strong>面试背诵版</strong></p><blockquote><p><strong>（一定要记住！）</strong>联合索引 <code>(a,b,c)</code>，条件是 <code>a=2 and c=1</code>，可以用到联合索引，但主要是用 <code>a=2</code> 定位索引范围。因为中间跳过了 <code>b</code>，不满足完整的最左前缀，所以 <code>c</code> 一般不能用于缩小 B+ 树的连续扫描范围。不过由于 <code>c</code> 在索引中，MySQL 可能通过索引下推在索引层过滤 <code>c=1</code>，减少回表。</p></blockquote>"},{"t":"索引失效有哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>面试可以这样答：<strong>索引失效就是建了索引，但 SQL 没法有效利用索引，导致全表扫描或扫描大量数据。</strong></p><ol><li><strong>不满足最左前缀原则</strong></li></ol><p>联合索引：</p><pre><code>KEY idx_a_b_c(a, b, c)</code></pre><p>可能生效：</p><pre><code>WHERE a = ? AND b = ?</code></pre><p>可能失效：</p><pre><code>WHERE b = ?<br/>WHERE b = ? AND c = ?</code></pre><p>因为跳过了最左边的 <code>a</code>。</p><ol><li><strong>对索引列使用函数或表达式</strong></li></ol><pre><code>WHERE DATE(create_time) = '2026-08-08'</code></pre><pre><code>WHERE age + 1 = 20</code></pre><p>索引列被计算后，可能无法直接利用索引。</p><p>更推荐：</p><pre><code>WHERE create_time &gt;= '2026-08-08 00:00:00'<br/>  AND create_time &lt; '2026-08-09 00:00:00'</code></pre><ol><li><strong>隐式类型转换</strong></li></ol><p>字段是字符串，但查询时不加引号：</p><pre><code>WHERE phone = 13800000000</code></pre><p>如果 <code>phone</code> 是 <code>VARCHAR</code>，可能导致类型转换，影响索引使用。</p><p>推荐：</p><pre><code>WHERE phone = '13800000000'</code></pre><p><strong>4. </strong><strong><code>LIKE</code></strong><strong> 左模糊查询</strong></p><p>可以用索引：</p><pre><code>WHERE name LIKE '张%'</code></pre><p>通常不能有效用索引：</p><pre><code>WHERE name LIKE '%张'<br/>WHERE name LIKE '%张%'</code></pre><p>因为不知道从索引哪里开始查。</p><p><strong>5. </strong><strong><code>OR</code></strong><strong> 条件使用不当</strong></p><pre><code>WHERE indexed_col = 1<br/>   OR no_index_col = 2</code></pre><p>如果 <code>OR</code> 一边没有索引，可能导致整体不走索引。</p><p>可以考虑改成：</p><pre><code>SELECT ... WHERE indexed_col = 1<br/>UNION ALL<br/>SELECT ... WHERE no_index_col = 2</code></pre><p>但要看具体场景。</p><ol><li><strong>范围查询后面的联合索引字段失效</strong></li></ol><p>联合索引：</p><pre><code>KEY idx_a_b_c(a, b, c)</code></pre><p>查询：</p><pre><code>WHERE a = ?<br/>  AND b &gt; ?<br/>  AND c = ?</code></pre><p>一般能用到 <code>a</code> 和 <code>b</code>，但 <code>c</code> 通常不能继续用于精确定位。</p><ol><li><strong>使用 </strong><strong><code>!=</code></strong><strong>、</strong><strong><code>&lt;&gt;</code></strong><strong>、</strong><strong><code>NOT IN</code></strong><strong>、</strong><strong><code>NOT LIKE</code></strong></li></ol><p>这些条件通常选择性较差，可能导致优化器不走索引。</p><pre><code>WHERE status != 1</code></pre><p>如果过滤不出多少数据，优化器可能认为全表扫描更划算。</p><ol><li><strong>查询返回数据量太大</strong></li></ol><p>即使条件字段有索引，如果命中数据太多，比如超过很大比例：</p><pre><code>WHERE gender = '男'</code></pre><p>优化器可能认为走索引再回表成本更高，于是选择全表扫描。</p><ol><li><strong>排序字段和索引顺序不一致</strong></li></ol><p>例如联合索引：</p><pre><code>KEY idx_a_b(a, b)</code></pre><p>可能利用索引排序：</p><pre><code>WHERE a = ?<br/>ORDER BY b</code></pre><p>不一定能很好利用：</p><pre><code>ORDER BY b</code></pre><p>因为跳过了 <code>a</code>。</p><p><strong>面试背诵版</strong></p><blockquote><p>常见索引失效场景有：</p><ol><li>联合索引不满足最左前缀原则；</li><li>对索引列使用函数或表达式；</li><li>发生隐式类型转换；</li><li>LIKE 以百分号开头；</li><li>OR 条件中有字段没索引；</li><li>范围查询后面的联合索引字段不能继续用于精确定位；</li><li>使用不等于、NOT IN、NOT LIKE 这类选择性差的条件；</li><li>以及查询返回数据量太大时，优化器可能认为全表扫描更划算。</li><li>实际排查时要用 EXPLAIN 看执行计划，不能只凭经验判断。</li></ol></blockquote>"},{"t":"什么情况下会回表查询","tag":"","p":"","html":"<p></p><p><strong>回表查询</strong>一般发生在：<strong>走了二级索引，但索引里没有查询所需的全部字段</strong><strong>。</strong></p><ol><li><strong>为什么会回表</strong></li></ol><p>在 <code>InnoDB</code> 里：</p><ul><li>主键索引是聚簇索引，叶子节点存整行数据。</li><li>二级索引的叶子节点存的是：<strong>索引字段 + 主键值</strong>。</li><li>如果通过二级索引查到了主键值，但还需要其他字段，就要再根据主键去主键索引查整行数据。</li></ul><p>这个过程就叫<strong>回表</strong>。</p><ol><li><strong>举个例子</strong></li></ol><p>表结构：</p><pre><code>CREATE TABLE user (<br/>  id BIGINT PRIMARY KEY,<br/>  name VARCHAR(50),<br/>  age INT,<br/>  city VARCHAR(50),<br/>  KEY idx_name(name)<br/>);</code></pre><p>执行：</p><pre><code>SELECT *<br/>FROM user<br/>WHERE name = '张三';</code></pre><p>执行过程：</p><pre><code>1. 先走 idx_name 二级索引，找到 name = '张三' 对应的 id<br/>2. 再用 id 去主键索引中查整行数据<br/>3. 返回 id、name、age、city</code></pre><p>因为 <code>idx_name</code> 里没有 <code>age</code>、<code>city</code>，所以需要回表。</p><ol><li><strong>什么情况下不回表</strong></li></ol><p>如果查询字段都在索引里，就不用回表，也就是覆盖索引。</p><p>例如：</p><pre><code>SELECT id, name<br/>FROM user<br/>WHERE name = '张三';</code></pre><p>因为 <code>idx_name</code> 叶子节点里有：</p><pre><code>name + id</code></pre><p>所以可以直接返回 <code>id</code> 和 <code>name</code>，不用回表。</p><ol><li><strong>主键查询不会回表</strong></li></ol><pre><code>SELECT *<br/>FROM user<br/>WHERE id = 1;</code></pre><p>因为 <code>id</code> 是主键索引，叶子节点存整行数据，直接拿数据，不需要回表。</p><p><strong>面试背诵版</strong></p><blockquote><p>回表通常发生在 InnoDB 使用二级索引查询时。二级索引的叶子节点只存索引字段和主键值，<strong>如果查询需要的字段不在这个二级索引里，就要拿主键值再去聚簇索引中查整行数据，这个过程叫回表</strong>。比如通过 name 索引查 <code>select *</code>，就需要回表；如果查询字段都在索引中，比如 <code>select id, name where name=?</code>，就是覆盖索引，不需要回表。主键查询因为聚簇索引叶子节点存整行数据，所以也不需要回表。</p></blockquote>"},{"t":"什么是覆盖索引？","tag":"","p":"","html":"<p><strong>覆盖索引</strong>就是：<strong>查询需要的字段都能从索引里拿到，不需要回表查询整行数据。</strong></p><ol><li><strong>举个例子</strong></li></ol><p>表结构：</p><pre><code>CREATE TABLE user (<br/>  id BIGINT PRIMARY KEY,<br/>  name VARCHAR(50),<br/>  age INT,<br/>  city VARCHAR(50),<br/>  KEY idx_name_age(name, age)<br/>);</code></pre><p>执行：</p><pre><code>SELECT name, age<br/>FROM user<br/>WHERE name = '张三';</code></pre><p>这里：</p><ul><li>查询条件字段：<code>name</code></li><li>查询返回字段：<code>name, age</code></li><li>这两个字段都在联合索引 <code>idx_name_age</code> 里</li></ul><p>所以 MySQL 只查二级索引就够了，不需要根据主键再回表查整行数据。</p><p>这就是覆盖索引。</p><ol><li><strong>什么是回表</strong></li></ol><p>如果执行：</p><pre><code>SELECT *<br/>FROM user<br/>WHERE name = '张三';</code></pre><p>虽然 <code>name</code> 有索引，但二级索引里没有完整的整行数据。</p><p>执行过程是：</p><pre><code>1. 先通过 name 索引找到主键 id<br/>2. 再根据 id 去主键索引查整行数据</code></pre><p>第二步就是<strong>回表</strong>。</p><ol><li><strong>覆盖索引的好处</strong></li></ol><ul><li>减少回表次数。</li><li>减少磁盘 IO。</li><li>查询性能更好。</li><li>对高频查询很有帮助。</li></ul><ol><li><strong>怎么判断是否用了覆盖索引</strong></li></ol><p>用 <code>EXPLAIN</code> 看执行计划。</p><p>如果 <code>Extra</code> 里出现：</p><pre><code>Using index</code></pre><p>通常表示用到了覆盖索引。</p><p>注意不是 <code>Using index condition</code>，那个一般是索引下推。</p><p><strong>面试背诵版</strong></p><blockquote><p><strong>覆盖索引指的是查询需要的字段都包含在索引中，数据库只通过索引就能返回结果，不需要再回表查询整行数据</strong>。比如有联合索引 <code>(name, age)</code>，执行 <code>select name, age from user where name = ?</code>，查询字段和条件字段都在索引里，就可以形成覆盖索引。它的好处是减少回表和磁盘 IO，提高查询性能。用 EXPLAIN 查看时，Extra 中出现 <code>Using index</code> 通常表示使用了覆盖索引。</p></blockquote>"},{"t":"如果一个列既是单列索引，又是联合索引，单独查它的话先走哪个？","tag":"","p":"","html":"<p>不一定固定走哪个，<strong>由 MySQL 优化器根据成本选择</strong>。</p><p>比如同时有两个索引：</p><pre><code>KEY idx_a(a)<br/>KEY idx_a_b(a, b)</code></pre><p>查询：</p><pre><code>SELECT *<br/>FROM user<br/>WHERE a = 1;</code></pre><p>这两个索引理论上都能用，因为联合索引 <code>(a,b)</code> 也满足最左前缀原则。</p><p><strong>优化器怎么选</strong></p><p>MySQL 会根据成本估算选择一个，比如看：</p><ul><li>索引区分度</li><li>索引大小</li><li>扫描行数</li><li>是否需要回表</li><li>是否能覆盖查询字段</li><li>统计信息</li></ul><p><strong>常见情况</strong></p><p>如果是：</p><pre><code>SELECT *<br/>FROM user<br/>WHERE a = 1;</code></pre><p>单列索引 <code>idx_a(a)</code> 通常更小，可能更容易被选中。</p><p>但如果是：</p><pre><code>SELECT a, b<br/>FROM user<br/>WHERE a = 1;</code></pre><p>联合索引 <code>idx_a_b(a,b)</code> 可能形成覆盖索引，不需要回表，反而可能被选中。</p><p><strong>注意</strong></p><p>如果已经有：</p><pre><code>KEY idx_a_b(a, b)</code></pre><p>很多时候单独的：</p><pre><code>KEY idx_a(a)</code></pre><p>可能是冗余索引。</p><p>因为 <code>(a,b)</code> 可以支持 <code>WHERE a = ?</code>。</p><p>但是否删除 <code>idx_a</code>，要看实际 SQL、执行计划和性能，不能一概而论。</p><p><strong>面试背诵版</strong></p><blockquote><p>如果一个字段既有单列索引，又作为联合索引的最左字段，单独按这个字段查询时，两个索引理论上都可以用，最终走哪个由 MySQL 优化器根据成本决定。优化器会考虑扫描行数、索引大小、区分度、是否需要回表、是否覆盖索引等因素。一般 <code>select * where a=?</code> 可能倾向于较小的单列索引；但如果查询字段能被联合索引覆盖，比如 <code>select a,b where a=?</code>，则可能走联合索引。实际要看 <code>EXPLAIN</code>。</p></blockquote>"},{"t":"索引已经建好了，那我再插入一条数据，索引会有哪些变化？","tag":"","p":"","html":"<p>面试可以这样答：<strong>插入一条数据时，不只是表数据变了，相关索引也要同步维护。</strong></p><ol><li><strong>主键索引会插入新记录</strong></li></ol><p>在 <code>InnoDB</code> 中，主键索引是聚簇索引。</p><ul><li>新数据会按照主键值插入到主键 B+ 树的合适位置。</li><li>聚簇索引的叶子节点存整行数据。</li><li>如果主键是自增 ID，通常插入到 B+ 树最右侧，成本较低。</li><li>如果主键是随机 UUID，可能插入到中间位置，成本更高。</li></ul><ol><li><strong>二级索引也要更新</strong></li></ol><p>如果表上还有普通索引、唯一索引、联合索引：</p><pre><code>KEY idx_name(name)<br/>KEY idx_age_city(age, city)</code></pre><p>插入一行时，这些索引也都要插入对应的索引项。</p><p>在 <code>InnoDB</code> 中，二级索引叶子节点存的是：</p><pre><code>索引字段值 + 主键值</code></pre><p>所以每个相关索引都要维护一份记录。</p><ol><li><strong>可能发生页分裂</strong></li></ol><p>B+ 树节点是按页存储的。</p><ul><li>如果目标叶子页还有空间，直接插入。</li><li>如果目标页满了，就可能发生页分裂。</li><li>页分裂会把部分数据移动到新页，并更新上层节点指针。</li><li>这会增加插入成本。</li></ul><p>所以：</p><ul><li>自增主键页分裂较少。</li><li>随机主键更容易导致页分裂。</li></ul><ol><li><strong>唯一索引要做唯一性检查</strong></li></ol><p>如果有唯一索引：</p><pre><code>UNIQUE KEY uk_phone(phone)</code></pre><p>插入时 MySQL 需要先检查这个值是否已存在。</p><ul><li>如果不存在，插入成功。</li><li>如果已存在，报重复键错误。</li></ul><ol><li><strong>写入日志</strong></li></ol><p>为了保证事务和崩溃恢复，插入还会写日志：</p><ul><li><code>undo log</code>：用于事务回滚。</li><li><code>redo log</code>：用于崩溃恢复。</li><li><code>binlog</code>：用于主从复制和恢复。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>插入一条数据时，InnoDB 会先把整行数据插入到主键聚簇索引的 B+ 树叶子节点中，同时还要维护这张表上的所有二级索引，因为二级索引叶子节点存的是索引字段加主键值。如果目标索引页满了，可能发生页分裂，并更新父节点。对于唯一索引，还需要做唯一性检查。另外，为了支持事务和崩溃恢复，还会写 undo log、redo log，主从复制场景下还会写 binlog。所以索引越多，插入成本越高。</p></blockquote>"},{"t":"索引字段是不是建的越多越好？","tag":"","p":"","html":"<blockquote><p>不是，<strong>索引不是越多越好</strong>。</p><ol><li><strong>索引的好处</strong></li></ol><ul><li>可以提高查询效率。</li><li>可以减少扫描数据量。</li><li>可以优化排序、分组、连表。</li><li>唯一索引还能保证数据唯一性。</li></ul><ol><li><strong>索引的代价</strong></li></ol><ul><li><strong>占用磁盘空间</strong><ul><li>每个索引本质上都是一棵 B+ 树。</li><li>索引越多，占用空间越大。</li></ul></li><li><strong>降低写入性能</strong><ul><li><code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code> 时，不仅要改数据，还要维护相关索引。</li><li>索引越多，维护成本越高。</li></ul></li><li><strong>增加优化器选择成本</strong><ul><li>索引太多，优化器需要评估更多执行计划。</li><li>有时还可能选错索引。</li></ul></li><li><strong>可能产生冗余索引</strong><ul><li>比如已有联合索引 <code>(a,b)</code>，再建单列索引 <code>(a)</code>，很多时候就是冗余的。</li></ul></li></ul><ol><li><strong>什么时候该建索引</strong></li></ol><p>适合建索引的字段：</p><ul><li>经常出现在 <code>WHERE</code> 条件中的字段。</li><li>经常用于 <code>JOIN</code> 关联的字段。</li><li>经常用于 <code>ORDER BY</code>、<code>GROUP BY</code> 的字段。</li><li>区分度高的字段。</li><li>有唯一性要求的字段，比如手机号、订单号。</li></ul><ol><li><strong>不适合建索引的字段</strong></li></ol><ul><li>很少查询的字段。</li><li>区分度很低的字段，比如性别、状态。</li><li>频繁更新的字段。</li><li>数据量很小的表。</li><li>大字段，比如很长的 <code>TEXT</code> 字段。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>索引不是越多越好。索引可以提高查询效率，但每个索引都需要占用额外空间，而且在插入、更新、删除时都要维护索引，会降低写入性能。索引太多还可能产生冗余索引，增加优化器选择成本。实际建索引要结合 SQL 场景，一般给高频查询条件、JOIN 字段、排序分组字段以及高区分度字段建索引，而低区分度、很少查询、频繁更新或者大字段不适合随便建索引。</p></blockquote></blockquote>"},{"t":"如果有一个字段是status值为0或者1，适合建索引吗","tag":"","p":"","html":"<p>一般<strong>不适合单独给 </strong><strong><code>status</code></strong><strong> 建普通索引</strong>，因为它只有 <code>0/1</code> 两种值，<strong>区分度太低</strong>。</p><p><strong>为什么不适合</strong></p><ul><li>假设表有 100 万条数据。</li><li><code>status = 0</code> 可能有 50 万条，<code>status = 1</code> 也可能有 50 万条。</li><li>即使走索引，也要扫描大量索引记录。</li><li>如果还要查其他字段，可能产生大量回表。</li><li>优化器可能认为还不如全表扫描。</li></ul><p><strong>什么时候可以建</strong></p><ul><li>如果数据分布很极端，可以考虑。<ul><li>比如 <code>status = 1</code> 只占 1%，经常查 <code>status = 1</code>。</li><li>这时索引可能有效。</li></ul></li><li>如果经常和其他字段一起查询，可以放到联合索引中。<ul><li>比如：</li></ul></li></ul><pre><code>WHERE status = 1<br/>  AND user_id = ?</code></pre><p>可以考虑：</p><pre><code>KEY idx_user_status(user_id, status)</code></pre><p>或者根据具体 SQL 设计字段顺序。</p><ul><li>如果经常用于排序、分组，也可以结合查询场景设计联合索引。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p><code>status</code> 这种只有 0 和 1 的字段，一般不适合单独建索引，因为区分度太低，命中数据量可能很大，走索引再回表的成本不一定比全表扫描低。但也不是绝对，如果数据分布很倾斜，比如 <code>status=1</code> 只占很小比例并且经常查询，索引可能有价值。另外更常见的做法是把 <code>status</code> 放到联合索引里，结合用户 ID、时间等高区分度字段一起使用。</p><p></p></blockquote>"},{"t":"索引的优缺点？","tag":"","p":"","html":"<blockquote><p><strong>优点</strong></p><ul><li><strong>提高查询效率</strong>：减少扫描数据量，避免全表扫描。</li><li><strong>加速排序和分组</strong>：<code>ORDER BY</code>、<code>GROUP BY</code> 字段命中索引时，可以减少额外排序。</li><li><strong>提高连接查询效率</strong>：<code>JOIN</code> 的关联字段有索引，匹配会更快。</li><li><strong>保证唯一性</strong>：唯一索引可以防止重复数据，比如手机号、订单号。</li></ul><p><strong>缺点</strong></p><ul><li><strong>占用额外空间</strong>：每个索引都需要单独存储，本质上也是一棵 B+ 树。</li><li><strong>降低写入性能</strong>：<code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code> 时，不仅要改数据，还要维护索引。</li><li><strong>增加维护成本</strong>：索引越多，页分裂、索引调整、统计信息维护成本越高。</li><li><strong>可能被错误使用</strong>：索引设计不合理，比如低区分度字段建索引，可能效果不好甚至拖慢查询。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>索引的优点是可以提高查询效率，减少全表扫描，也能优化排序、分组和关联查询，唯一索引还能保证数据唯一性。缺点是会占用额外存储空间，并且在插入、更新、删除时需要维护索引，所以会降低写入性能；索引太多还会增加维护成本，甚至影响优化器选择执行计划。因此索引不是越多越好，要根据实际查询场景合理设计。</p></blockquote></blockquote>"},{"t":"怎么决定建立哪些索引?","tag":"","p":"","html":"<p>面试可以这样答：<strong>建索引不是看字段，而是看 SQL 查询场景。</strong></p><ol><li><strong>看高频查询条件</strong></li></ol><ul><li>经常出现在 <code>WHERE</code> 里的字段适合建索引。</li><li>尤其是过滤性好的字段。</li></ul><p>例如：</p><pre><code>WHERE user_id = ?</code></pre><p>可以考虑给 <code>user_id</code> 建索引。</p><ol><li><strong>看关联字段</strong></li></ol><ul><li>经常用于 <code>JOIN</code> 的字段适合建索引。</li><li>一般子表的外键字段要建索引。</li></ul><p>例如：</p><pre><code>orders.user_id = user.id</code></pre><p>可以给 <code>orders.user_id</code> 建索引。</p><ol><li><strong>看排序和分组字段</strong></li></ol><ul><li>经常出现在 <code>ORDER BY</code>、<code>GROUP BY</code> 中的字段，可以考虑索引。</li><li>如果索引顺序和排序顺序匹配，可以减少额外排序。</li></ul><p>例如：</p><pre><code>WHERE user_id = ?<br/>ORDER BY create_time DESC</code></pre><p>可以考虑：</p><pre><code>KEY idx_user_time(user_id, create_time)</code></pre><ol><li><strong>看字段区分度</strong></li></ol><ul><li>区分度高的字段更适合建索引。</li><li>比如手机号、订单号、用户 ID。</li><li>区分度低的字段，比如性别、状态，不适合单独建索引。</li></ul><ol><li><strong>优先考虑联合索引</strong></li></ol><ul><li>多个字段经常一起查询时，优先考虑联合索引。</li><li>字段顺序要结合查询条件：<ul><li>等值查询字段放前面。</li><li>范围查询字段放后面。</li><li>区分度高、高频字段优先。</li></ul></li><li>注意最左前缀原则。</li></ul><ol><li><strong>尽量设计覆盖索引</strong></li></ol><ul><li>如果查询字段都包含在索引中，就可以避免回表。</li><li>适合高频查询接口。</li></ul><p>例如：</p><pre><code>SELECT user_id, status, create_time<br/>FROM orders<br/>WHERE user_id = ?<br/>  AND status = ?;</code></pre><p>可以考虑：</p><pre><code>KEY idx_user_status_time(user_id, status, create_time)</code></pre><ol><li><strong>用执行计划验证</strong></li></ol><ul><li>建索引后要用 <code>EXPLAIN</code> 看效果。</li><li>关注：<ul><li><code>type</code></li><li><code>key</code></li><li><code>rows</code></li><li><code>Extra</code></li></ul></li><li>不能只凭感觉建索引。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>决定建哪些索引，主要看业务 SQL，而不是单纯看字段。一般会给高频 WHERE 条件、JOIN 关联字段、ORDER BY 和 GROUP BY 字段建立索引，同时要考虑字段区分度，区分度高的字段更适合。多个字段经常一起查询时，优先设计联合索引，遵循最左前缀原则，通常等值条件放前，范围条件放后，也可以考虑覆盖索引减少回表。最后要通过 EXPLAIN 看执行计划，验证索引是否真的生效。</p></blockquote>"},{"t":"索引优化详细讲讲","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>面试讲“索引优化”，不要一上来背一堆点，可以按 <strong>“发现慢 SQL → 分析执行计划 → 设计索引 → 改写 SQL → 验证效果”</strong> 这个流程说。</p><ol><li><strong>先定位慢 SQL</strong></li></ol><ul><li>通过慢查询日志、监控、接口耗时，找到真正慢的 SQL。</li><li>不要凭感觉乱加索引。</li></ul><p>可以提一句：</p><pre><code>SHOW VARIABLES LIKE 'slow_query_log';</code></pre><p>实际项目中一般看慢 SQL 平台或日志。</p><ol><li><strong>用 EXPLAIN 分析执行计划</strong></li></ol><p>重点看几个字段：</p><ul><li><code>type</code>：访问类型，最好能到 <code>ref</code>、<code>range</code>、<code>const</code>。</li><li><code>key</code>：实际用了哪个索引。</li><li><code>rows</code>：预估扫描多少行。</li><li><code>Extra</code>：有没有 <code>Using filesort</code>、<code>Using temporary</code>、<code>Using index</code>。</li></ul><p>比如：</p><pre><code>EXPLAIN<br/>SELECT *<br/>FROM orders<br/>WHERE user_id = 100<br/>ORDER BY create_time DESC;</code></pre><p>如果发现没有走索引，或者扫描行数很大，就要考虑优化。</p><ol><li><strong>根据 WHERE 条件建索引</strong></li></ol><ul><li>高频查询字段适合建索引。</li><li>区分度高的字段优先。</li><li>低区分度字段不适合单独建索引。</li></ul><p>例如：</p><pre><code>WHERE user_id = ?</code></pre><p>可以给 <code>user_id</code> 建索引。</p><ol><li><strong>优先设计联合索引</strong></li></ol><p>如果多个字段经常一起查询，不要每个字段都单独建索引，通常用联合索引更合适。</p><p>例如：</p><pre><code>WHERE user_id = ?<br/>  AND status = ?<br/>ORDER BY create_time DESC</code></pre><p>可以考虑：</p><pre><code>KEY idx_user_status_time(user_id, status, create_time)</code></pre><p>这样：</p><ul><li><code>user_id</code>、<code>status</code> 用于过滤。</li><li><code>create_time</code> 可以用于排序。</li><li>可能减少 <code>filesort</code>。</li></ul><ol><li><strong>注意最左前缀原则</strong></li></ol><p>联合索引：</p><pre><code>KEY idx_a_b_c(a, b, c)</code></pre><p>能较好使用：</p><pre><code>WHERE a = ?<br/>WHERE a = ? AND b = ?<br/>WHERE a = ? AND b = ? AND c = ?</code></pre><p>不太适合：</p><pre><code>WHERE b = ?<br/>WHERE c = ?</code></pre><p>因为跳过了最左字段 <code>a</code>。</p><ol><li><strong>范围条件放后面</strong></li></ol><p>联合索引中，如果有范围查询：</p><pre><code>WHERE user_id = ?<br/>  AND status = ?<br/>  AND create_time &gt; ?</code></pre><p>一般把等值条件放前，范围字段放后：</p><pre><code>(user_id, status, create_time)</code></pre><p>因为范围条件后面的字段通常不能继续用于精确定位。</p><ol><li><strong>利用覆盖索引减少回表</strong></li></ol><p>如果查询字段都在索引里，就不需要回表。</p><p>例如：</p><pre><code>SELECT user_id, status, create_time<br/>FROM orders<br/>WHERE user_id = ?<br/>  AND status = ?;</code></pre><p>可以建：</p><pre><code>KEY idx_user_status_time(user_id, status, create_time)</code></pre><p><code>EXPLAIN Extra</code> 出现：</p><pre><code>Using index</code></pre><p>一般说明用了覆盖索引。</p><ol><li><strong>避免索引失效</strong></li></ol><p>常见情况：</p><ul><li>对索引列使用函数：</li></ul><pre><code>WHERE DATE(create_time) = '2026-08-08'</code></pre><p>改成：</p><pre><code>WHERE create_time &gt;= '2026-08-08 00:00:00'<br/>  AND create_time &lt; '2026-08-09 00:00:00'</code></pre><ul><li><code>LIKE</code> 左模糊：</li></ul><pre><code>WHERE name LIKE '%张'</code></pre><ul><li>隐式类型转换：</li></ul><pre><code>WHERE phone = 13800000000</code></pre><p>如果 <code>phone</code> 是字符串，要写：</p><pre><code>WHERE phone = '13800000000'</code></pre><ul><li>联合索引跳过最左字段。</li><li><code>OR</code> 一边没有索引。</li><li>查询返回数据量太大。</li></ul><ol><li><strong>优化排序和分页</strong></li></ol><p>排序字段尽量和索引顺序匹配。</p><p>例如：</p><pre><code>WHERE user_id = ?<br/>ORDER BY create_time DESC</code></pre><p>可以建：</p><pre><code>KEY idx_user_time(user_id, create_time)</code></pre><p>深分页不要直接：</p><pre><code>LIMIT 100000, 20</code></pre><p>可以改成基于上一次最大 ID 或时间游标：</p><pre><code>WHERE id &gt; last_id<br/>ORDER BY id<br/>LIMIT 20</code></pre><ol><li><strong>不要建太多索引</strong></li></ol><ul><li>索引会占空间。</li><li>写入时要维护索引。</li><li>索引太多会降低 <code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code> 性能。</li><li>还可能产生冗余索引。</li></ul><p>例如已有：</p><pre><code>KEY idx_a_b(a, b)</code></pre><p>通常不需要再建：</p><pre><code>KEY idx_a(a)</code></pre><blockquote><p><strong>面试总结版</strong></p><blockquote><p>索引优化我一般按流程来做：先通过慢查询日志定位慢 SQL，再用 EXPLAIN 看执行计划，比如有没有走索引、扫描行数、是否 filesort 或 temporary。设计索引时，会优先根据高频 WHERE 条件、JOIN 字段、ORDER BY、GROUP BY 来建；多个字段一起查询时优先联合索引，遵循最左前缀原则，等值字段放前，范围字段放后。同时尽量利用覆盖索引减少回表，避免函数、隐式转换、左模糊、跳过联合索引最左字段这些导致索引失效的写法。最后还要注意索引不是越多越好，因为会占空间并影响写入性能，优化后要用 EXPLAIN 验证效果。</p></blockquote><p></p></blockquote>"},{"t":"了解过前缀索引吗？","tag":"","p":"","html":"<p>面试可以这样回答：</p><ol><li><strong>前缀索引是什么</strong></li></ol><blockquote><p>前缀索引就是只对字符串字段的前一部分字符建立索引，而不是对整个字段建索引。</p></blockquote><p>例如：</p><pre><code>CREATE INDEX idx_email_prefix<br/>ON user(email(10));</code></pre><p>表示只取 <code>email</code> 字段的前 <code>10</code> 个字符建立索引。</p><ol><li><strong>为什么要用前缀索引</strong></li></ol><p>主要用于比较长的字符串字段，比如：</p><ul><li><code>VARCHAR</code></li><li><code>TEXT</code></li><li>长 URL</li><li>邮箱</li><li>地址</li><li>标题</li></ul><p>好处是：</p><ul><li>减少索引长度。</li><li>节省存储空间。</li><li>提高索引扫描效率。</li><li>降低索引维护成本。</li></ul><ol><li><strong>缺点是什么</strong></li></ol><ul><li>前缀索引只存字段前缀，不存完整值。</li><li>区分度可能不如完整字段索引。</li><li>前缀长度太短，会导致大量重复，过滤效果差。</li><li>前缀索引通常不能很好支持覆盖索引，因为索引里没有完整字段值。</li></ul><ol><li><strong>怎么选择前缀长度</strong></li></ol><p>核心看<strong>区分度</strong>。</p><p>可以比较完整字段和不同前缀长度的区分度：</p><pre><code>SELECT<br/>  COUNT(DISTINCT email) / COUNT(*) AS full_selectivity,<br/>  COUNT(DISTINCT LEFT(email, 10)) / COUNT(*) AS prefix_selectivity<br/>FROM user;</code></pre><p>如果前 10 位的区分度已经接近完整字段，就可以用 <code>email(10)</code>。</p><ol><li><strong>使用场景</strong></li></ol><p>适合：</p><pre><code>WHERE email = ?<br/>WHERE url = ?</code></pre><p>不太适合：</p><ul><li>前缀区分度很低的字段。</li><li>经常需要覆盖索引的查询。</li><li>需要完整字段排序的场景。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>前缀索引是指对字符串字段的前 N 个字符建立索引，比如 <code>email(10)</code>。它适合较长的 VARCHAR 或 TEXT 字段，可以减少索引大小，节省空间，提高索引维护和查询效率。但缺点是区分度可能下降，前缀长度太短会导致重复值多，过滤效果差，而且通常不能很好地做覆盖索引。前缀长度一般通过比较 <code>COUNT(DISTINCT LEFT(col, n)) / COUNT(*)</code> 和完整字段区分度来选择。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"事务","questions":[{"t":"事务的特性是什么？如何实现的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试可以按 <strong>ACID + InnoDB 实现机制</strong> 来答。</p><ol><li><strong>原子性 Atomicity</strong></li></ol><ul><li>含义：一个事务里的操作要么全部成功，要么全部失败。</li><li>不能只执行一半。</li><li>比如下单扣库存和生成订单，要么都成功，要么都回滚。</li></ul><p><strong>实现方式</strong></p><ul><li>主要依靠 <code>undo log</code>。</li><li>如果事务失败或回滚，InnoDB 可以根据 <code>undo log</code> 把数据恢复到修改前的状态。</li></ul><ol><li><strong>一致性 Consistency</strong></li></ol><ul><li>含义：事务执行前后，数据库都要处于合法一致的状态。</li><li>比如转账前后，总金额不能凭空增加或减少。</li><li>一致性是事务最终要达到的目标。</li></ul><p><strong>实现方式</strong></p><ul><li>由原子性、隔离性、持久性共同保证。</li><li>也依赖数据库约束，比如主键、唯一键、外键、非空约束。</li><li>还依赖业务逻辑正确性。</li></ul><ol><li><strong>隔离性 Isolation</strong></li></ol><ul><li>含义：多个事务并发执行时，彼此之间不能互相干扰。</li><li>一个事务中间状态不应该随便被其他事务看到。</li></ul><p><strong>实现方式</strong></p><ul><li>主要依靠锁机制和 <code>MVCC</code>。</li><li>锁解决写写冲突。</li><li><code>MVCC</code> 通过多版本数据实现读写并发。</li><li>不同隔离级别解决不同并发问题。</li></ul><ol><li><strong>持久性 Durability</strong></li></ol><ul><li>含义：事务一旦提交，对数据的修改就是永久的。</li><li>即使数据库宕机，也应该能恢复。</li></ul><p><strong>实现方式</strong></p><ul><li>主要依靠 <code>redo log</code>。</li><li>事务提交时，修改会先记录到 <code>redo log</code>。</li><li>数据库崩溃后，可以通过 <code>redo log</code> 进行恢复。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>事务有四个特性，也就是 ACID。A 是原子性，指事务中的操作要么全部成功，要么全部失败，InnoDB 主要通过 undo log 实现回滚；C 是一致性，指事务执行前后数据库都保持合法状态，它是最终目标，需要原子性、隔离性、持久性以及数据库约束和业务逻辑共同保证；I 是隔离性，指并发事务之间互不干扰，InnoDB 主要通过锁和 MVCC 实现；D 是持久性，指事务提交后数据不会丢失，主要通过 redo log 实现崩溃恢复。</p></blockquote><p></p></blockquote>"},{"t":"mysql可能出现什么和并发相关问题？","tag":"","p":"","html":"<p>面试可以这样答：<strong>MySQL 并发事务常见问题主要有脏读、不可重复读、幻读，以及更新丢失。</strong></p><ol><li><strong>脏读</strong></li></ol><ul><li>一个事务读到了另一个事务还没提交的数据。</li><li>如果对方后来回滚了，那读到的数据就是脏数据。</li></ul><p>例子：</p><pre><code>事务 A 修改余额为 1000，但还没提交<br/>事务 B 读到了 1000<br/>事务 A 回滚<br/>事务 B 刚才读到的就是无效数据</code></pre><ol><li><strong>不可重复读</strong></li></ol><ul><li>同一个事务中，两次读取同一行数据，结果不一样。</li><li>原因是中间有其他事务提交了修改。</li></ul><p>例子：</p><pre><code>事务 A 第一次读余额 = 100<br/>事务 B 修改余额为 200 并提交<br/>事务 A 第二次读余额 = 200</code></pre><p>重点是：<strong>同一行数据内容变了</strong>。</p><ol><li><strong>幻读</strong></li></ol><ul><li>同一个事务中，两次按同一条件查询，查到的记录数量不一样。</li><li>原因是中间有其他事务插入或删除了符合条件的数据。</li></ul><p>例子：</p><pre><code>事务 A 查询 age &gt; 18 的用户，有 10 条<br/>事务 B 插入一条 age = 20 的用户并提交<br/>事务 A 再查 age &gt; 18，变成 11 条</code></pre><p>重点是：<strong>记录数量变了，像出现了幻影行</strong>。</p><ol><li><strong>更新丢失</strong></li></ol><ul><li>两个事务同时修改同一条数据，后提交的覆盖了先提交的结果。</li><li>比如库存扣减、余额更新场景容易出现。</li></ul><p>例子：</p><pre><code>库存 = 10<br/>事务 A 读到 10，准备减 1<br/>事务 B 也读到 10，准备减 1<br/>A 更新为 9<br/>B 也更新为 9<br/>结果只扣了一次</code></pre><ol><li><strong>怎么解决</strong></li></ol><ul><li>通过事务隔离级别：<ul><li><code>READ UNCOMMITTED</code>：可能脏读、不可重复读、幻读。</li><li><code>READ COMMITTED</code>：解决脏读。</li><li><code>REPEATABLE READ</code>：解决脏读、不可重复读；InnoDB 通过 MVCC + Next-Key Lock 很大程度解决幻读。</li><li><code>SERIALIZABLE</code>：最严格，串行执行，性能最低。</li></ul></li><li>通过锁：<ul><li>行锁</li><li>间隙锁</li><li>临键锁</li><li><code>SELECT ... FOR UPDATE</code></li></ul></li><li>通过乐观锁：<ul><li>加 <code>version</code> 字段</li><li>更新时判断版本号</li></ul></li></ul><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 并发事务中常见问题有脏读、不可重复读、幻读和更新丢失。脏读是读到了别人未提交的数据；不可重复读是同一个事务两次读同一行，结果不一样；幻读是同一个事务两次按条件查询，记录数量不一样；更新丢失是多个事务同时修改同一数据，后面的更新覆盖前面的结果。MySQL 主要通过事务隔离级别、MVCC、锁机制以及乐观锁来解决这些问题。</p></blockquote>"},{"t":"哪些场景不适合脏读，举个例子？","tag":"","p":"","html":"<p><strong>不适合脏读的场景</strong>，核心就是：<strong>只要数据一错会影响钱、库存、状态判断、业务决策，就不能接受脏读。</strong></p><ol><li><strong>账户余额 / 支付场景</strong></li></ol><p>比如转账：</p><pre><code>事务 A：给用户余额 +100，还没提交<br/>事务 B：读取余额，认为用户已经到账<br/>事务 A：因为异常回滚</code></pre><p>结果：</p><ul><li>事务 B 读到了一笔根本不存在的余额。</li><li>如果后续允许提现、发货，就会出严重问题。</li></ul><ol><li><strong>库存扣减场景</strong></li></ol><p>比如下单扣库存：</p><pre><code>事务 A：把库存从 10 改成 0，还没提交<br/>事务 B：读取库存为 0，认为没货了<br/>事务 A：后来回滚，库存又变回 10</code></pre><p>结果：</p><ul><li>事务 B 做了错误判断。</li><li>可能导致商品误判无货，影响订单。</li></ul><p>反过来也可能读到未提交的“有库存”，导致超卖风险。</p><ol><li><strong>订单状态场景</strong></li></ol><p>比如订单支付状态：</p><pre><code>事务 A：把订单状态改成“已支付”，还没提交<br/>事务 B：读取到“已支付”，开始发货<br/>事务 A：支付更新失败并回滚</code></pre><p>结果：</p><ul><li>实际订单没支付成功。</li><li>但系统已经发货了。</li></ul><ol><li><strong>优惠券 / 资格判断场景</strong></li></ol><p>比如判断用户是否已经领取优惠券：</p><pre><code>事务 A：插入领取记录，还没提交<br/>事务 B：读到用户已领取<br/>事务 A：回滚</code></pre><p>结果：</p><ul><li>用户实际没领取成功。</li><li>但系统误以为他领过了。</li></ul><p><strong>面试回答版</strong></p><blockquote><p>涉及资金、库存、订单状态、资格判断这类强一致性场景都不适合脏读。比如支付场景中，一个事务把订单状态改成已支付但还没提交，另一个事务如果脏读到这个状态就开始发货，而前一个事务最后回滚了，就会出现用户没支付但系统已经发货的问题。所以这类业务至少要避免脏读，通常不会使用读未提交隔离级别。</p></blockquote>"},{"t":"mysql的是怎么解决并发问题的？","tag":"","p":"","html":"<p>面试可以这样回答：<strong>MySQL 主要通过事务隔离级别、锁机制和 MVCC 来解决并发问题。</strong></p><ol><li><strong>事务隔离级别</strong></li></ol><p>MySQL 用不同隔离级别控制事务之间的可见性：</p><ul><li><code>READ UNCOMMITTED</code>：读未提交，可能脏读。</li><li><code>READ COMMITTED</code>：读已提交，解决脏读。</li><li><code>REPEATABLE READ</code>：可重复读，解决脏读和不可重复读；InnoDB 默认级别。</li><li><code>SERIALIZABLE</code>：串行化，隔离最强，但并发性能最差。</li></ul><ol><li><strong>MVCC 解决读写冲突</strong></li></ol><p><code>MVCC</code> 是多版本并发控制。</p><ul><li>对普通 <code>SELECT</code>，InnoDB 通常不会加锁。</li><li>它通过版本链和 <code>Read View</code> 读取符合当前事务可见性的历史版本。</li><li>这样读和写可以并发执行，减少阻塞。</li></ul><p>简单说：</p><blockquote><p>MVCC 让读操作读快照版本，不直接阻塞写操作。</p></blockquote><ol><li><strong>锁机制解决写写冲突</strong></li></ol><p>写操作之间不能随便并发改同一行，所以需要锁。</p><p>常见锁：</p><ul><li>行锁：锁具体记录。</li><li>表锁：锁整张表。</li><li>间隙锁：锁记录之间的范围，防止插入幻影行。</li><li>临键锁：记录锁 + 间隙锁。</li><li>意向锁：表示事务准备对某些行加锁。</li></ul><p>比如：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE id = 1<br/>FOR UPDATE;</code></pre><p>会对查询到的行加排他锁，防止其他事务同时修改。</p><ol><li><strong>日志保证事务安全</strong></li></ol><p>并发下还要保证事务能回滚、能恢复：</p><ul><li><code>undo log</code>：支持回滚和 MVCC。</li><li><code>redo log</code>：保证崩溃恢复和持久性。</li><li><code>binlog</code>：用于主从复制和数据恢复。</li></ul><ol><li><strong>乐观锁也常用</strong></li></ol><p>业务层也经常用乐观锁解决并发更新：</p><pre><code>UPDATE product<br/>SET stock = stock - 1,<br/>    version = version + 1<br/>WHERE id = 1<br/>  AND version = 10;</code></pre><p>如果影响行数为 0，说明被别人改过，需要重试。</p><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 解决并发问题主要靠事务隔离级别、锁机制和 MVCC。隔离级别控制事务之间能看到什么数据；MVCC 通过 undo log 版本链和 Read View 实现快照读，让普通查询不阻塞写操作；锁机制主要解决写写冲突，比如行锁、间隙锁、临键锁，防止并发修改和幻读问题。另外，redo log 和 undo log 保证事务可以恢复和回滚；业务层也可以用乐观锁，比如 version 字段，解决并发更新覆盖的问题。</p></blockquote>"},{"t":"事务的隔离级别有哪些？","tag":"","p":"","html":"<p></p><p>面试可以这样答：</p><ol><li><strong>读未提交：</strong><strong><code>READ UNCOMMITTED</code></strong></li></ol><ul><li>最低隔离级别。</li><li>一个事务可以读到另一个事务还没提交的数据。</li><li>可能出现：<ul><li>脏读</li><li>不可重复读</li><li>幻读</li></ul></li></ul><ol><li><strong>读已提交：</strong><strong><code>READ COMMITTED</code></strong></li></ol><ul><li>一个事务只能读到其他事务已经提交的数据。</li><li>可以解决脏读。</li><li>但可能出现：<ul><li>不可重复读</li><li>幻读</li></ul></li></ul><ol><li><strong>可重复读：</strong><strong><code>REPEATABLE READ</code></strong></li></ol><ul><li>MySQL InnoDB 默认隔离级别。</li><li>同一个事务中，多次读取同一行数据结果一致。</li><li>可以解决：<ul><li>脏读</li><li>不可重复读</li></ul></li><li>在 InnoDB 中，通过 <code>MVCC</code> 和 <code>Next-Key Lock</code>，很大程度上解决幻读问题。</li></ul><ol><li><strong>串行化：</strong><strong><code>SERIALIZABLE</code></strong></li></ol><ul><li>最高隔离级别。</li><li>事务基本串行执行。</li><li>可以避免：<ul><li>脏读</li><li>不可重复读</li><li>幻读</li></ul></li><li>但并发性能最差。</li></ul><p><strong>对比表</strong></p><sheet></sheet><p><strong>面试背诵版</strong></p><blockquote><p>事务隔离级别有四种：读未提交、读已提交、可重复读和串行化。读未提交可能出现脏读、不可重复读和幻读；读已提交可以解决脏读；可重复读可以解决脏读和不可重复读，也是 MySQL InnoDB 默认隔离级别，InnoDB 通过 MVCC 和 Next-Key Lock 解决很多幻读场景；串行化隔离级别最高，可以避免这些并发问题，但性能最差。</p></blockquote><p></p><blockquote><p></p></blockquote>"},{"t":"mysql默认级别是什么？","tag":"","p":"","html":"<blockquote><p>MySQL InnoDB 默认事务隔离级别是：</p><p>也就是：<strong>可重复读</strong>。</p><p><strong>面试回答</strong></p><blockquote><p>MySQL InnoDB 默认隔离级别是可重复读，也就是 <code>REPEATABLE READ</code>。它可以避免脏读和不可重复读。在 InnoDB 中，还会结合 MVCC 和 Next-Key Lock，在很多场景下解决幻读问题。相比串行化，它兼顾了一致性和并发性能，所以 MySQL 默认选择可重复读。</p></blockquote><p></p></blockquote>"},{"t":"可重复读隔离级别下，A事务提交的数据，在B事务能看见吗？","tag":"","p":"","html":"<p>要分情况，<strong>看 B 事务是在什么时候读的，以及是快照读还是当前读。</strong></p><ol><li><strong>普通 </strong><strong><code>SELECT</code></strong><strong>：快照读</strong></li></ol><p>在 <code>REPEATABLE READ</code> 下，普通查询一般是快照读。</p><pre><code>SELECT * FROM user WHERE id = 1;</code></pre><p>如果 <strong>B 事务已经读过一次</strong>：</p><pre><code>B 开启事务<br/>B 第一次 SELECT，生成 Read View<br/>A 修改并提交<br/>B 再次 SELECT</code></pre><p>这时 <strong>B 通常看不到 A 后来提交的数据</strong>。</p><p>因为 B 会基于第一次查询时生成的快照读数据，所以可重复读。</p><ol><li><strong>如果 B 还没读过</strong></li></ol><pre><code>B 开启事务<br/>A 修改并提交<br/>B 第一次 SELECT</code></pre><p>这时 <strong>B 可以看到 A 已提交的数据</strong>。</p><p>因为 InnoDB 的 <code>Read View</code> 通常是在第一次快照读时生成的，不是开启事务时立刻生成。</p><ol><li><strong>当前读：可以看到最新已提交数据</strong></li></ol><p>如果 B 执行的是当前读，比如：</p><pre><code>SELECT * FROM user WHERE id = 1 FOR UPDATE;</code></pre><p>或者：</p><pre><code>UPDATE user SET name = 'x' WHERE id = 1;</code></pre><p>这类语句读的是最新数据，并且会加锁。</p><p>所以 <strong>B 可以看到 A 已经提交的数据</strong>。</p><p><strong>面试背诵版</strong></p><blockquote><p>在 MySQL InnoDB 的可重复读隔离级别下，A 事务提交的数据，B 事务能不能看到要看情况。如果 B 做的是普通 SELECT，也就是快照读，并且 B 已经执行过一次查询生成了 Read View，那么后续 A 提交的数据 B 看不到；如果 B 开启事务后还没查询，A 先提交了，B 第一次快照读时是可以看到的。除此之外，如果 B 执行的是当前读，比如 SELECT FOR UPDATE、UPDATE、DELETE，这类会读取最新已提交数据，所以可以看到 A 已提交的数据。</p></blockquote>"},{"t":"举个例子说可重复读下的幻读问题","tag":"","p":"","html":"<p>可以，先给你一个<strong>最容易理解的例子</strong>。</p><p>假设有一张订单表：</p><pre><code>orders(id, user_id, amount)</code></pre><p>当前表里有：</p><sheet></sheet><p>现在有两个事务。</p><ol><li><strong>事务 A 第一次查询</strong></li></ol><p>事务 A 开启事务，查询用户 <code>1</code> 的订单：</p><pre><code>BEGIN;<br/><br/>SELECT *<br/>FROM orders<br/>WHERE user_id = 1;</code></pre><p>查到 2 条：</p><pre><code>id = 1<br/>id = 2</code></pre><ol><li><strong>事务 B 插入一条新订单并提交</strong></li></ol><p>事务 B 执行：</p><pre><code>BEGIN;<br/><br/>INSERT INTO orders(id, user_id, amount)<br/>VALUES(3, 1, 300);<br/><br/>COMMIT;</code></pre><p>现在数据库里实际已经多了一条：</p><pre><code>id = 3, user_id = 1</code></pre><ol><li><strong>事务 A 再次查询同样条件</strong></li></ol><p>事务 A 再查：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1;</code></pre><p>如果这次事务 A 查到了 3 条：</p><pre><code>id = 1<br/>id = 2<br/>id = 3</code></pre><p>这就叫<strong>幻读</strong>。</p><p>因为对事务 A 来说：</p><blockquote><p>我明明同一个事务里，用同样条件查了两次，第二次却多出了一条像“幻影”一样的新记录。</p></blockquote><p><strong>重点理解</strong></p><ul><li>不可重复读：同一行数据变了。</li><li>幻读：符合条件的记录数量变了，多了或少了记录。</li></ul><p><strong>但注意 MySQL InnoDB</strong></p><p>在 <code>REPEATABLE READ</code> 下，普通 <code>SELECT</code> 是快照读：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1;</code></pre><p>事务 A 第二次普通查询通常还是看到 2 条，不会看到事务 B 新插入的那条。</p><p>但是如果事务 A 用的是当前读，比如：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1<br/>FOR UPDATE;</code></pre><p>或者执行插入、更新相关操作，就可能涉及幻读控制。</p><p>InnoDB 会通过：</p><ul><li><code>Next-Key Lock</code></li><li>间隙锁</li><li>行锁</li></ul><p>来防止其他事务在范围内插入新记录。</p><p><strong>面试背诵版</strong></p><blockquote><p>幻读指的是同一个事务里，使用相同条件查询两次，第二次查到了第一次没有的记录。比如事务 A 查询 user_id=1 的订单，第一次查到 2 条；事务 B 插入一条 user_id=1 的订单并提交；事务 A 再用同样条件查询，如果查到 3 条，这就是幻读。它和不可重复读的区别是，不可重复读关注同一行数据被修改，幻读关注符合条件的记录数量发生变化。在 MySQL InnoDB 的可重复读下，普通 SELECT 通过 MVCC 快照读通常不会出现幻读，当前读场景则通过 Next-Key Lock、间隙锁来避免幻读。</p></blockquote>"},{"t":"Mysql 设置了可重读隔离级后，怎么保证不发生幻读？","tag":"","p":"","html":"<p>在 <code>MySQL InnoDB</code> 的 <code>REPEATABLE READ</code> 下，防幻读主要靠两套机制：<strong>MVCC 快照读 + Next-Key Lock 当前读</strong>。</p><ol><li><strong>普通 SELECT：靠 MVCC</strong></li></ol><p>普通查询：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1;</code></pre><p>属于<strong>快照读</strong>。</p><p>在可重复读隔离级别下：</p><ul><li>事务第一次查询时会生成一个 <code>Read View</code>。</li><li>后续普通 <code>SELECT</code> 都基于这个快照读。</li><li>即使其他事务插入了新数据并提交，当前事务也看不到。</li><li>所以普通查询不会出现“第二次多出一行”的幻读。</li></ul><ol><li><strong>当前读：靠 Next-Key Lock</strong></li></ol><p>当前读包括：</p><pre><code>SELECT ... FOR UPDATE<br/>SELECT ... LOCK IN SHARE MODE<br/>UPDATE<br/>DELETE</code></pre><p>这类语句读的是最新数据，还会加锁。</p><p>为了防止别的事务在查询范围内插入新记录，InnoDB 会加：</p><ul><li><strong>记录锁 Record Lock</strong>：锁已有记录。</li><li><strong>间隙锁 Gap Lock</strong>：锁记录之间的空隙。</li><li><strong>临键锁 Next-Key Lock</strong>：记录锁 + 间隙锁。</li></ul><p>比如：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1<br/>FOR UPDATE;</code></pre><p>如果 <code>user_id</code> 上有索引，InnoDB 不只锁住已有的 <code>user_id = 1</code> 记录，还会锁住相关索引范围的间隙，防止其他事务插入新的 <code>user_id = 1</code> 记录。</p><p>这样当前事务再次查询时，就不会多出“幻影行”。</p><ol><li><strong>面试要注意一句话</strong></li></ol><blockquote><p>MVCC 解决的是快照读下的幻读现象，Next-Key Lock 解决的是当前读下的幻读问题。</p></blockquote><p><strong>面试背诵版</strong></p><blockquote><p>MySQL InnoDB 在可重复读隔离级别下，主要通过 MVCC 和 Next-Key Lock 来避免幻读。普通 SELECT 是快照读，事务第一次查询会生成 Read View，后续查询都基于同一个快照，所以其他事务后来插入并提交的数据看不到。对于当前读，比如 SELECT FOR UPDATE、UPDATE、DELETE，InnoDB 会使用 Next-Key Lock，也就是记录锁加间隙锁，锁住已有记录和索引范围之间的间隙，防止其他事务在这个范围内插入新数据，从而避免幻读。</p></blockquote><p></p><blockquote><p></p></blockquote>"},{"t":"串行化隔离级别是通过什么实现的？","tag":"","p":"","html":"<p><code>SERIALIZABLE</code> 串行化隔离级别主要是通过<strong>加锁</strong>实现的。</p><ol><li><strong>核心思想</strong></li></ol><blockquote><p>把并发事务尽量变成串行执行，避免脏读、不可重复读和幻读。</p></blockquote><p>也就是说，多个事务如果访问同一批数据，会互相等待，而不是同时读写。</p><ol><li><strong>在 MySQL InnoDB 中怎么做</strong></li></ol><p>在 <code>SERIALIZABLE</code> 隔离级别下：</p><ul><li>普通 <code>SELECT</code> 也会变成加锁读。</li><li>读操作会加共享锁。</li><li>写操作会加排他锁。</li><li>如果是范围查询，还会配合间隙锁 / 临键锁，阻止其他事务插入幻影行。</li></ul><p>比如：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1;</code></pre><p>在普通隔离级别下，这通常是快照读。</p><p>但在串行化级别下，它会类似加锁读：</p><pre><code>SELECT *<br/>FROM orders<br/>WHERE user_id = 1<br/>LOCK IN SHARE MODE;</code></pre><p>这样其他事务如果想修改这些数据，或者在相关范围内插入数据，就可能被阻塞。</p><ol><li><strong>锁类型</strong></li></ol><p>主要依赖：</p><ul><li>共享锁：读锁，多个事务可以一起读，但会阻塞写。</li><li>排他锁：写锁，写数据时使用，会阻塞其他读写。</li><li>间隙锁：锁索引记录之间的范围。</li><li>临键锁：记录锁 + 间隙锁，防止幻读。</li></ul><ol><li><strong>缺点</strong></li></ol><ul><li>并发性能差。</li><li>容易阻塞。</li><li>死锁概率可能增加。</li><li>所以实际业务中很少使用串行化，更多用 <code>READ COMMITTED</code> 或 <code>REPEATABLE READ</code> 配合锁来解决问题。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>MySQL InnoDB 的串行化隔离级别主要通过加锁实现。它会让普通 SELECT 也变成加锁读，对读取的数据加共享锁，对写入加排他锁；如果是范围查询，还会配合间隙锁或临键锁，防止其他事务插入新记录，从而避免幻读。它能提供最强隔离性，但代价是并发性能差、阻塞多，所以实际项目中很少直接使用。</p></blockquote>"},{"t":"介绍MVCC实现原理","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>面试可以按 <strong>“是什么 → 解决什么 → 依靠什么实现 → 查询流程”</strong> 来答。</p><ol><li><strong>MVCC 是什么</strong></li></ol><blockquote><p><code>MVCC</code> 是多版本并发控制。它的核心思想是：同一行数据可以有多个历史版本，读操作根据事务可见性规则读取合适的版本，从而实现读写并发。</p></blockquote><p>简单说：</p><ul><li>写操作生成新版本。</li><li>读操作读符合条件的旧版本。</li><li>普通读不阻塞写，写也不阻塞普通读。</li></ul><ol><li><strong>MVCC 解决什么问题</strong></li></ol><p>MVCC 主要解决的是<strong>读写冲突</strong>。</p><p>如果没有 MVCC：</p><ul><li>读数据时可能要加锁。</li><li>写数据时也要加锁。</li><li>读写互相阻塞，并发性能差。</li></ul><p>有了 MVCC：</p><ul><li>普通 <code>SELECT</code> 可以读快照。</li><li>不需要阻塞正在修改数据的事务。</li><li>提高并发性能。</li></ul><ol><li><strong>InnoDB MVCC 依靠什么实现</strong></li></ol><p>主要依靠三个东西：</p><ul><li><strong>隐藏字段</strong></li><li><strong>undo log 版本链</strong></li><li><strong>Read View</strong></li></ul><p><strong>隐藏字段</strong></p><p>InnoDB 每行记录里有一些隐藏字段，常说的有：</p><ul><li><code>trx_id</code>：最近一次修改这行记录的事务 ID。</li><li><code>roll_pointer</code>：回滚指针，指向 undo log 中的上一个历史版本。</li></ul><p><strong>undo log 版本链</strong></p><p>当一行数据被多次修改时，旧版本会记录在 <code>undo log</code> 里。</p><p>可以理解成：</p><pre><code>当前记录<br/>  trx_id = 100<br/>  roll_pointer<br/>      ↓<br/>旧版本 trx_id = 90<br/>      ↓<br/>更旧版本 trx_id = 80</code></pre><p>这条链就是版本链。</p><p><strong>Read View</strong></p><p><code>Read View</code> 是事务进行快照读时生成的视图，用来判断哪个版本对当前事务可见。</p><p>里面大概包含：</p><ul><li>当前活跃事务 ID 列表。</li><li>最小活跃事务 ID。</li><li>下一个将要分配的事务 ID。</li><li>当前事务 ID。</li></ul><p>通过这些信息判断：</p><ul><li>哪些版本是已经提交的。</li><li>哪些版本是未提交的。</li><li>当前事务能不能看到这个版本。</li></ul><ol><li><strong>查询时怎么找版本</strong></li></ol><p>普通 <code>SELECT</code> 查询时：</p><pre><code>SELECT *<br/>FROM user<br/>WHERE id = 1;</code></pre><p>流程大概是：</p><ul><li>先找到当前最新记录。</li><li>判断这条记录的 <code>trx_id</code> 对当前 <code>Read View</code> 是否可见。</li><li>如果可见，直接返回。</li><li>如果不可见，就通过 <code>roll_pointer</code> 去 <code>undo log</code> 找上一个版本。</li><li>一直往前找，直到找到可见版本或找不到。</li></ul><ol><li><strong>RC 和 RR 下 Read View 的区别</strong></li></ol><p>这个是加分点：</p><ul><li><code>READ COMMITTED</code><ul><li>每次普通 <code>SELECT</code> 都会生成新的 <code>Read View</code>。</li><li>所以同一事务里两次查询，可能看到其他事务新提交的数据。</li></ul></li><li><code>REPEATABLE READ</code><ul><li>第一次普通 <code>SELECT</code> 生成 <code>Read View</code>。</li><li>后续普通查询复用同一个 <code>Read View</code>。</li><li>所以同一事务里多次查询结果一致。</li></ul></li></ul><ol><li><strong>当前读不走 MVCC 快照</strong></li></ol><p>注意：</p><p>普通查询是快照读：</p><pre><code>SELECT * FROM user WHERE id = 1;</code></pre><p>当前读读的是最新数据，并加锁：</p><pre><code>SELECT * FROM user WHERE id = 1 FOR UPDATE;<br/>UPDATE user SET name = 'x' WHERE id = 1;<br/>DELETE FROM user WHERE id = 1;</code></pre><p>当前读主要靠锁来保证并发安全。</p><p><strong>面试背诵版</strong></p><blockquote><ul><li>MVCC 是多版本并发控制，核心是为同一行数据维护多个版本，让普通读可以读取符合当前事务可见性的历史版本，从而实现读写不阻塞。</li><li>InnoDB 的 MVCC 主要依靠隐藏字段、undo log 版本链和 Read View 实现。每行记录有 trx_id 和 roll_pointer，trx_id 表示最近修改它的事务 ID，roll_pointer 指向 undo log 中的旧版本。普通 SELECT 时会生成 Read View，根据活跃事务列表等信息判断当前版本是否可见；如果不可见，就沿着 undo log 版本链往前找可见版本。在 RC 下每次查询都会生成新的 Read View，在 RR 下第一次查询生成 Read View 后复用，所以能实现可重复读。</li></ul></blockquote>"},{"t":"一条update是不是原子性的？为什么？","tag":"","p":"","html":"<p>是的，<strong>单条 </strong><strong><code>UPDATE</code></strong><strong> 语句本身是原子性的</strong>。</p><ol><li><strong>先说结论</strong></li></ol><blockquote><p>在 MySQL InnoDB 中，一条 <code>UPDATE</code> 要么整体执行成功，要么整体失败回滚，不会出现只更新一半的情况。</p></blockquote><p>比如：</p><pre><code>UPDATE user<br/>SET balance = balance - 100<br/>WHERE id = 1;</code></pre><p>这条语句要么扣款成功，要么失败后恢复原样。</p><ol><li><strong>为什么它是原子性的</strong></li></ol><p>主要依靠：</p><ul><li><strong>事务机制</strong></li><li><strong>undo log</strong></li><li><strong>锁机制</strong></li></ul><p><strong>undo log 保证回滚</strong></p><p>执行 <code>UPDATE</code> 前，InnoDB 会记录修改前的数据到 <code>undo log</code>。</p><p>如果执行失败、事务回滚、数据库异常，InnoDB 可以根据 <code>undo log</code> 把数据恢复到修改前状态。</p><p>所以不会出现改了一半无法恢复。</p><p><strong>锁机制保证并发安全</strong></p><p><code>UPDATE</code> 会对修改的记录加排他锁。</p><p>这样其他事务不能同时修改同一行，避免并发更新冲突。</p><ol><li><strong>autocommit 下也是事务</strong></li></ol><p>MySQL 默认：</p><pre><code>autocommit = 1</code></pre><p>这意味着每条 SQL 都是一个独立事务。</p><p>所以单条 <code>UPDATE</code> 执行完会自动提交；失败就自动回滚。</p><ol><li><strong>多行 UPDATE 也是原子的吗</strong></li></ol><p>是的，单条语句即使更新多行，也是一个整体。</p><p>比如：</p><pre><code>UPDATE user<br/>SET status = 1<br/>WHERE age &gt; 18;</code></pre><p>如果更新 1000 行：</p><ul><li>要么 1000 行都更新成功。</li><li>要么失败后全部回滚。</li><li>不会只成功一部分。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>一条 UPDATE 在 InnoDB 中是原子性的。因为 MySQL 默认 autocommit 开启时，每条 SQL 都是一个独立事务；UPDATE 执行前会通过 undo log 记录旧值，如果执行失败或事务回滚，可以根据 undo log 恢复到修改前状态。同时 UPDATE 会对修改的记录加排他锁，避免并发修改冲突。所以单条 UPDATE 要么全部成功，要么全部失败回滚，即使更新多行也是一个整体。</p></blockquote>"},{"t":"滥用事务，或者一个事务里有特别多sql的弊端？","tag":"","p":"","html":"<blockquote><p>面试可以这样答：<strong>事务不是越大越好，长事务会带来锁、日志、MVCC 和性能问题。</strong></p><ol><li><strong>锁持有时间变长</strong></li></ol><ul><li>事务不提交，锁就可能一直不释放。</li><li>其他事务如果要修改同一批数据，就会被阻塞。</li><li>并发性能下降，甚至可能引发锁等待超时。</li></ul><p>比如：</p><ol><li><strong>死锁概率增加</strong></li></ol><ul><li>一个事务里 SQL 越多，访问的数据越多。</li><li>加锁范围越大、持锁时间越长。</li><li>和其他事务交叉等待的概率更高。</li><li>死锁风险增加。</li></ul><ol><li><strong>undo log 变大</strong></li></ol><ul><li>事务修改数据时会生成 <code>undo log</code>。</li><li>长事务不提交，旧版本不能及时清理。</li><li><code>undo log</code> 堆积，影响存储和性能。</li></ul><ol><li><strong>MVCC 历史版本无法清理</strong></li></ol><ul><li>InnoDB 需要保留旧版本给长事务做快照读。</li><li>长事务一直不结束，<code>purge</code> 线程不能清理旧版本。</li><li>可能导致版本链变长。</li><li>查询时沿版本链查找成本变高。</li></ul><ol><li><strong>redo log / binlog 压力变大</strong></li></ol><ul><li>大事务会产生大量日志。</li><li>提交时刷盘和同步成本更高。</li><li>主从复制时，大事务也可能造成复制延迟。</li></ul><ol><li><strong>回滚成本高</strong></li></ol><ul><li>如果一个大事务执行到最后失败了，需要回滚大量操作。</li><li>回滚可能非常慢。</li><li>甚至回滚时间比执行时间还长。</li></ul><ol><li><strong>连接资源占用</strong></li></ol><ul><li>长事务会长时间占用数据库连接。</li><li>连接池资源被占满后，其他请求可能拿不到连接。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>滥用事务或者一个事务里放太多 SQL，容易形成长事务。长事务会导致锁持有时间变长，阻塞其他事务，降低并发；同时加锁范围和持锁时间增加，也会提高死锁概率。它还会导致 undo log 堆积，MVCC 旧版本无法及时清理，影响查询性能。大事务产生的 redo log、binlog 更多，提交和主从复制压力也更大。如果最后失败，回滚成本也很高。所以实际开发中事务应该尽量短小，只包住必须保证一致性的操作，避免在事务里做 RPC、文件 IO 或耗时计算。</p></blockquote></blockquote>"}]},{"no":"","title":"锁","questions":[{"t":"讲一下mysql里有哪些锁？","tag":"","p":"","html":"<p>面试可以按 <strong>“粒度、读写、InnoDB 行锁算法、意向锁、乐观锁”</strong> 来讲。</p><ol><li><strong>按锁粒度分</strong></li></ol><ul><li><strong>表锁</strong><ul><li>锁整张表。</li><li>开销小，但并发能力差。</li><li><code>MyISAM</code> 主要使用表锁。</li></ul></li><li><strong>行锁</strong><ul><li>锁具体的数据行。</li><li>开销更大，但并发能力好。</li><li><code>InnoDB</code> 支持行锁。</li></ul></li></ul><ol><li><strong>按读写类型分</strong></li></ol><ul><li><strong>共享锁 S 锁</strong><ul><li>也叫读锁。</li><li>多个事务可以同时加共享锁。</li><li>但会阻塞其他事务加排他锁。</li></ul></li></ul><pre><code>SELECT *<br/>FROM user<br/>WHERE id = 1<br/>LOCK IN SHARE MODE;</code></pre><ul><li><strong>排他锁 X 锁</strong><ul><li>也叫写锁。</li><li>一个事务加了排他锁后，其他事务不能再加共享锁或排他锁。</li><li><code>UPDATE</code>、<code>DELETE</code>、<code>INSERT</code> 通常会加排他锁。</li></ul></li></ul><pre><code>SELECT *<br/>FROM user<br/>WHERE id = 1<br/>FOR UPDATE;</code></pre><ol><li><strong>InnoDB 常见行锁算法</strong></li></ol><ul><li><strong>记录锁 Record Lock</strong><ul><li>锁住某一条索引记录。</li><li>比如按主键更新一行。</li></ul></li><li><strong>间隙锁 Gap Lock</strong><ul><li>锁住两个索引记录之间的间隙。</li><li>防止其他事务插入新记录。</li><li>主要用于防止幻读。</li></ul></li><li><strong>临键锁 Next-Key Lock</strong><ul><li>记录锁 + 间隙锁。</li><li>锁住某条记录以及它前面的间隙。</li><li><code>REPEATABLE READ</code> 下范围查询常见。</li></ul></li></ul><ol><li><strong>意向锁</strong></li></ol><ul><li>意向锁是表级锁。</li><li>表示事务接下来想对表中的某些行加锁。</li><li>主要用于协调表锁和行锁，避免逐行检查。</li></ul><p>常见：</p><ul><li><code>IS</code>：意向共享锁。</li><li><code>IX</code>：意向排他锁。</li></ul><p>比如事务要给某行加排他锁前，会先在表上加 <code>IX</code> 锁。</p><ol><li><strong>自增锁</strong></li></ol><ul><li>插入自增主键时，InnoDB 会涉及自增锁或相关机制。</li><li>用于保证自增值分配安全。</li><li>新版本中并发能力已经优化很多。</li></ul><ol><li><strong>乐观锁 / 悲观锁</strong></li></ol><p>这是业务层常说的分类：</p><ul><li><strong>悲观锁</strong><ul><li>认为并发冲突经常发生。</li><li>先加锁再操作。</li><li>比如 <code>SELECT ... FOR UPDATE</code>。</li></ul></li><li><strong>乐观锁</strong><ul><li>认为冲突不多。</li><li>更新时通过版本号或时间戳判断有没有被别人改过。</li></ul></li></ul><pre><code>UPDATE product<br/>SET stock = stock - 1,<br/>    version = version + 1<br/>WHERE id = 1<br/>  AND version = 10;</code></pre><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 里的锁可以从几个角度看。按粒度分有表锁和行锁，InnoDB 支持行锁，MyISAM 主要是表锁；按读写分有共享锁和排他锁；InnoDB 行锁里常见有记录锁、间隙锁和临键锁，记录锁锁单条索引记录，间隙锁锁索引间隙防止插入，临键锁是记录锁加间隙锁，用来解决幻读问题。还有意向锁，它是表级锁，用来协调表锁和行锁。另外在业务开发中也会说悲观锁和乐观锁，悲观锁比如 <code>SELECT FOR UPDATE</code>，乐观锁一般通过 version 字段实现。</p></blockquote><span>[原文档此处有配图]</span>"},{"t":"数据库的表锁和行锁有什么作用？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>表锁和行锁的作用</strong>，都是为了在并发访问时保证数据一致性，只是<strong>锁的范围不同</strong>。</p><ol><li><strong>表锁</strong></li></ol><p>表锁是锁住整张表。</p><pre><code>事务 A 锁住 user 表<br/>事务 B 想修改 user 表中的其他行，也可能要等</code></pre><p><strong>作用</strong></p><ul><li>保证整张表在操作期间不被其他事务冲突修改。</li><li>实现简单，开销小。</li><li>适合整表操作或并发要求不高的场景。</li></ul><p><strong>缺点</strong></p><ul><li>锁范围大。</li><li>并发能力差。</li><li>一个事务锁表后，其他事务操作这张表容易被阻塞。</li></ul><ol><li><strong>行锁</strong></li></ol><p>行锁是锁住具体某一行或某些行。</p><pre><code>事务 A 修改 user.id = 1<br/>事务 B 修改 user.id = 2<br/>两者可以并发执行</code></pre><p><strong>作用</strong></p><ul><li>保证被修改的行不会被其他事务同时修改。</li><li>减少锁冲突。</li><li>提高并发性能。</li><li>适合高并发读写场景。</li></ul><p><strong>缺点</strong></p><ul><li>实现复杂。</li><li>加锁和维护成本比表锁高。</li><li>如果 SQL 没走索引，可能锁住更多数据，甚至退化成类似表锁的效果。</li></ul><ol><li><strong>对比</strong></li></ol><sheet></sheet><p><strong>面试背诵版</strong></p><blockquote><p>表锁和行锁都是为了解决并发访问下的数据一致性问题。表锁锁的是整张表，实现简单、开销小，但锁粒度大，并发能力差；行锁锁的是具体记录，锁粒度小，可以让不同事务同时操作不同行，并发性能更好，但实现和维护成本更高。在 InnoDB 中主要使用行锁，高并发业务一般更适合行锁；不过如果 SQL 没有走索引，可能会锁住更多记录，甚至接近表锁效果。</p></blockquote>"},{"t":"MySQL两个线程的update语句同时处理一条数据，会不会有阻塞？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>会，<strong>如果两个线程同时 </strong><strong><code>UPDATE</code></strong><strong> 同一条数据，通常会发生阻塞等待</strong>。</p><ol><li><strong>为什么会阻塞</strong></li></ol><p>在 <code>InnoDB</code> 中，<code>UPDATE</code> 会对要修改的记录加<strong>排他锁，也就是 X 锁</strong>。</p><p>排他锁的特点是：</p><ul><li>同一行同一时间只能被一个事务修改。</li><li>其他事务想修改这行数据，必须等待前一个事务提交或回滚。</li></ul><ol><li><strong>举个例子</strong></li></ol><p>事务 A：</p><pre><code>BEGIN;<br/><br/>UPDATE user<br/>SET balance = balance - 100<br/>WHERE id = 1;</code></pre><p>此时事务 A 持有 <code>id = 1</code> 这行的排他锁。</p><p>事务 B 同时执行：</p><pre><code>BEGIN;<br/><br/>UPDATE user<br/>SET balance = balance - 50<br/>WHERE id = 1;</code></pre><p>事务 B 会被阻塞，直到事务 A：</p><pre><code>COMMIT;</code></pre><p>或者：</p><pre><code>ROLLBACK;</code></pre><ol><li><strong>如果第一个事务提交后会怎样</strong></li></ol><p>假设初始余额是 <code>1000</code>。</p><p>事务 A：</p><pre><code>UPDATE user SET balance = balance - 100 WHERE id = 1;<br/>COMMIT;</code></pre><p>事务 B 等待后继续执行：</p><pre><code>UPDATE user SET balance = balance - 50 WHERE id = 1;<br/>COMMIT;</code></pre><p>最终余额是：</p><pre><code>850</code></pre><p>因为事务 B 会基于事务 A 提交后的最新数据继续更新。</p><ol><li><strong>如果等待太久</strong></li></ol><p>如果事务 B 等太久，可能报锁等待超时：</p><pre><code>Lock wait timeout exceeded</code></pre><p>这个时间由参数控制：</p><pre><code>innodb_lock_wait_timeout</code></pre><ol><li><strong>注意是否走索引</strong></li></ol><p>如果 <code>WHERE id = 1</code> 走主键索引，通常只锁这一行。</p><p>但如果条件没索引，比如：</p><pre><code>UPDATE user<br/>SET balance = balance - 50<br/>WHERE name = '张三';</code></pre><p>如果 <code>name</code> 没索引，可能扫描并锁住更多记录，导致阻塞范围变大。</p><p><strong>面试背诵版</strong></p><blockquote><p>会阻塞。InnoDB 中 UPDATE 会给被修改的记录加排他锁。如果两个线程同时更新同一行数据，第一个事务拿到锁后，第二个事务需要等待第一个事务提交或回滚后才能继续执行。如果等待太久可能出现锁等待超时。需要注意的是，如果 WHERE 条件走索引，通常锁的是对应行；如果没走索引，可能扫描并锁住更多记录，导致更大范围的阻塞。</p></blockquote>"},{"t":"两条update语句处理一张表的不同的主键范围的记录，>15，会不会遇到阻塞？底层是为什么的？","tag":"","p":"","html":"<p>有可能会阻塞，<strong>关键看两个 UPDATE 的范围是否重叠、是否走索引、以及隔离级别下加了什么锁</strong>。</p><p>我先按你说的“不同主键范围”来讲。</p><p>假设表主键是 <code>id</code>。</p><ol><li><strong>如果两个范围完全不重叠</strong></li></ol><p>事务 A：</p><pre><code>UPDATE user<br/>SET status = 1<br/>WHERE id &gt; 15 AND id &lt;= 20;</code></pre><p>事务 B：</p><pre><code>UPDATE user<br/>SET status = 2<br/>WHERE id &gt; 30 AND id &lt;= 40;</code></pre><p>如果 <code>id</code> 是主键索引，范围不重叠，一般不会互相阻塞。</p><p>原因：</p><ul><li><code>UPDATE</code> 会走主键索引。</li><li>InnoDB 对扫描到的记录加排他锁。</li><li>两个事务锁的是不同的索引记录范围。</li><li>没有锁冲突，所以通常可以并发执行。</li></ul><ol><li><strong>如果两个范围有重叠</strong></li></ol><p>事务 A：</p><pre><code>UPDATE user<br/>SET status = 1<br/>WHERE id &gt; 15;</code></pre><p>事务 B：</p><pre><code>UPDATE user<br/>SET status = 2<br/>WHERE id &gt; 20;</code></pre><p>这两个范围是重叠的。</p><p>事务 A 锁住了 <code>id &gt; 15</code> 的记录，事务 B 要更新 <code>id &gt; 20</code> 的记录，也属于 A 的锁范围。</p><p>这时会阻塞。</p><ol><li><strong>如果是 </strong><strong><code>id &gt; 15</code></strong><strong> 这种范围更新</strong></li></ol><p>在 <code>REPEATABLE READ</code> 下，<code>UPDATE</code> 是当前读。</p><p>范围更新可能加：</p><ul><li>记录锁：锁住满足条件的已有记录。</li><li>间隙锁：锁住索引记录之间的间隙。</li><li>临键锁：记录锁 + 间隙锁。</li></ul><p>比如：</p><pre><code>UPDATE user<br/>SET status = 1<br/>WHERE id &gt; 15;</code></pre><p>它不只是锁住已有的 <code>id &gt; 15</code> 记录，还可能锁住相关索引范围，防止其他事务在这个范围内插入新记录。</p><p>所以另一个事务如果也操作这个范围，或者往这个范围插入数据，就可能被阻塞。</p><ol><li><strong>如果 SQL 没走索引</strong></li></ol><p>如果条件不是主键，比如：</p><pre><code>UPDATE user<br/>SET status = 1<br/>WHERE age &gt; 15;</code></pre><p>但 <code>age</code> 没有索引。</p><p>InnoDB 可能全表扫描。</p><p>这时它会扫描很多记录，并对符合条件的记录加锁；在一些情况下，会造成大量锁等待，看起来像“锁表”。</p><p>所以底层很重要的一点是：</p><blockquote><p>InnoDB 的行锁是加在索引上的，不走索引就可能扫描很多数据，导致锁范围扩大。</p></blockquote><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>两条 UPDATE 更新同一张表的不同主键范围，是否阻塞要看范围是否重叠。如果都是按主键索引更新，而且范围完全不重叠，一般不会阻塞，因为 InnoDB 会在索引记录上加排他锁，锁的是不同记录范围。</p><p>但如果范围有重叠，比如一个是 <code>id &gt; 15</code>，另一个是 <code>id &gt; 20</code>，就会锁冲突而阻塞。另外在可重复读隔离级别下，范围更新属于当前读，可能加 Next-Key Lock，也就是记录锁加间隙锁，防止范围内插入幻读数据。如果条件没走索引，可能扫描并锁住更多记录，导致阻塞范围扩大。</p></blockquote>"},{"t":"如果2个范围不是主键或索引？还会阻塞吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>会，<strong>更容易阻塞</strong>。</p><p>如果两个 <code>UPDATE</code> 的范围条件不是主键，也没有索引，InnoDB 不能直接定位到目标行，只能<strong>全表扫描</strong>。</p><ol><li><strong>为什么会阻塞</strong></li></ol><p>比如字段 <code>age</code> 没有索引：</p><pre><code>UPDATE user<br/>SET status = 1<br/>WHERE age &gt; 15 AND age &lt;= 20;</code></pre><p>另一个事务：</p><pre><code>UPDATE user<br/>SET status = 2<br/>WHERE age &gt; 30 AND age &lt;= 40;</code></pre><p>虽然逻辑上这两个范围不重叠，但因为 <code>age</code> 没有索引，MySQL 需要扫描整张表。</p><p>在扫描过程中，<code>UPDATE</code> 是当前读，会对扫描到并准备修改的记录加排他锁。</p><p>结果可能是：</p><ul><li>扫描大量数据。</li><li>加锁范围变大。</li><li>两个事务都在全表扫描。</li><li>即使最终更新的记录不重叠，也可能因为扫描、锁等待、执行顺序导致阻塞。</li></ul><ol><li><strong>底层原因</strong></li></ol><p>关键点：</p><blockquote><p>InnoDB 的行锁是加在索引上的。</p></blockquote><p>如果条件字段有索引：</p><pre><code>WHERE age BETWEEN 15 AND 20</code></pre><p>可以通过 <code>age</code> 索引定位范围，只锁相关范围。</p><p>但如果 <code>age</code> 没索引：</p><ul><li>无法通过索引快速定位。</li><li>只能走聚簇索引全表扫描。</li><li>可能检查大量记录。</li><li>更新语句会对符合条件的记录加锁。</li><li>在 <code>REPEATABLE READ</code> 下，还可能涉及范围锁、间隙锁等。</li></ul><p>所以并发冲突概率更高。</p><ol><li><strong>注意不是一定锁整张表</strong></li></ol><p>严格说，InnoDB 不是直接把表锁住。</p><p>但由于没有索引，需要全表扫描，扫描和加锁范围很大，效果上可能接近锁表。</p><p>所以面试可以说：</p><blockquote><p>不是一定退化成真正的表锁，但会导致锁范围扩大，并发性能变差，看起来像锁表。</p></blockquote><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>如果两个 UPDATE 的范围条件不是主键也没有索引，更容易发生阻塞。因为 InnoDB 的行锁是基于索引实现的，没有索引时无法直接定位目标记录，只能走聚簇索引全表扫描。虽然最终要更新的逻辑范围可能不重叠，但扫描过程中会访问大量记录并对符合条件的数据加排他锁，导致锁范围扩大、锁等待增加。严格说不一定是真正的表锁，但效果上可能接近锁表，所以更新条件字段最好建立合适索引。</p></blockquote>"}]},{"no":"","title":"日志","questions":[{"t":"日志文件是分成了哪几种？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>MySQL 里常见日志文件可以这样分：</p><p><strong>1. </strong><strong><code>redo log</code></strong><strong> 重做日志</strong></p><ul><li>InnoDB 引擎层日志。</li><li>主要保证事务的<strong>持久性</strong>。</li><li>数据页还没来得及刷盘时，如果数据库宕机，可以用 <code>redo log</code> 恢复。</li><li>解决的是：<strong>已提交事务的数据不能丢</strong>。</li></ul><p><strong>2. </strong><strong><code>undo log</code></strong><strong> 回滚日志</strong></p><ul><li>InnoDB 引擎层日志。</li><li>主要用于：<ul><li>事务回滚</li><li>MVCC 多版本控制</li></ul></li><li>事务修改数据前，会记录旧版本。</li><li>回滚时可以根据 <code>undo log</code> 恢复旧值。</li></ul><p><strong>3. </strong><strong><code>binlog</code></strong><strong> 二进制日志</strong></p><ul><li>MySQL Server 层日志。</li><li>主要用于：<ul><li>主从复制</li><li>数据恢复</li><li>审计变更</li></ul></li><li>记录的是数据库的逻辑变更。</li><li>比如执行了什么 SQL，或者哪一行发生了变化。</li></ul><p><strong>4. </strong><strong><code>relay log</code></strong><strong> 中继日志</strong></p><ul><li>主从复制中，从库使用。</li><li>从库把主库的 <code>binlog</code> 拉过来后，先写到自己的 <code>relay log</code>。</li><li>然后 SQL 线程再根据 <code>relay log</code> 重放数据。</li></ul><ol><li><strong>慢查询日志 </strong><strong><code>slow query log</code></strong></li></ol><ul><li>记录执行时间超过阈值的 SQL。</li><li>用于排查慢 SQL 和做性能优化。</li></ul><ol><li><strong>错误日志 </strong><strong><code>error log</code></strong></li></ol><ul><li>记录 MySQL 启动、关闭、运行错误、崩溃信息等。</li><li>排查数据库异常时经常看。</li></ul><ol><li><strong>通用查询日志 </strong><strong><code>general log</code></strong></li></ol><ul><li>记录客户端连接和执行过的 SQL。</li><li>信息很全，但性能开销大。</li><li>生产环境一般不长期打开。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 常见日志有 redo log、undo log、binlog、relay log、慢查询日志、错误日志和通用查询日志。redo log 是 InnoDB 的重做日志，用于崩溃恢复，保证事务持久性；undo log 用于事务回滚和 MVCC；binlog 是 Server 层二进制日志，用于主从复制和数据恢复；relay log 是从库复制时使用的中继日志；慢查询日志用于定位慢 SQL；错误日志用于排查 MySQL 运行异常；通用查询日志会记录所有 SQL，但开销较大，生产一般不长期开启。</p></blockquote><p></p></blockquote>"},{"t":"讲一下binlog","tag":"","p":"","html":"<blockquote><p>面试可以这样答：<strong><code>binlog</code></strong><strong> 是 MySQL Server 层的二进制日志，主要记录数据库的变更操作。</strong></p><ol><li><strong>binlog 是什么</strong></li></ol><ul><li><code>binlog</code> 全称是 Binary Log。</li><li>它是 MySQL Server 层日志。</li><li>记录对数据库产生变更的操作，比如：</li></ul><p>INSERT</p><p>UPDATE</p><p>DELETE</p><p>CREATE</p><p>ALTER</p><p>DROP</p><ul><li>一般不记录普通 <code>SELECT</code>，因为 <code>SELECT</code> 不修改数据。</li></ul><ol><li><strong>binlog 有什么作用</strong></li></ol><p>主要有两个作用：</p><ul><li><strong>主从复制</strong><ul><li>主库把变更写入 <code>binlog</code>。</li><li>从库拉取主库的 <code>binlog</code>，写成 <code>relay log</code>。</li><li>然后从库重放日志，实现数据同步。</li></ul></li><li><strong>数据恢复</strong><ul><li>可以结合全量备份和 <code>binlog</code> 做时间点恢复。</li><li>比如先恢复昨天的全量备份，再重放今天某个时间点之前的 <code>binlog</code>。</li></ul></li></ul><ol><li><strong>binlog 的格式</strong></li></ol><p>常见有三种：</p><ul><li><strong>Statement</strong><ul><li>记录原始 SQL。</li><li>日志量小。</li><li>但某些 SQL 可能导致主从不一致，比如 <code>NOW()</code>、<code>UUID()</code>、<code>LIMIT</code> 不确定场景。</li></ul></li><li><strong>Row</strong><ul><li>记录每一行数据的变更。</li><li>更准确，不容易主从不一致。</li><li>但日志量可能比较大。</li><li>现在生产常用这个。</li></ul></li><li><strong>Mixed</strong><ul><li>混合模式。</li><li>MySQL 根据情况自动选择 Statement 或 Row。</li></ul></li></ul><ol><li><strong>binlog 和 redo log 的区别</strong></li></ol><p>这是面试高频追问。</p><ul><li><code>binlog</code> 是 Server 层日志，所有引擎都可以用。</li><li><code>redo log</code> 是 InnoDB 引擎层日志。</li><li><code>binlog</code> 主要用于主从复制和数据恢复。</li><li><code>redo log</code> 主要用于崩溃恢复，保证事务持久性。</li><li><code>binlog</code> 记录逻辑变更。</li><li><code>redo log</code> 记录物理页修改。</li><li><code>binlog</code> 是追加写。</li><li><code>redo log</code> 是循环写。</li></ul><ol><li><strong>和事务提交的关系</strong></li></ol><p>执行更新时，MySQL 会写：</p><ul><li><code>redo log</code></li><li><code>binlog</code></li></ul><p>为了保证两者一致，MySQL 使用<strong>两阶段提交</strong>：</p><ol><li>先写 <code>redo log prepare</code></li><li>再写 <code>binlog</code></li><li>最后写 <code>redo log commit</code></li></ol><p>这样可以保证崩溃恢复和主从复制的数据一致。</p><p><strong>面试背诵版</strong></p><p>binlog 是 MySQL Server 层的二进制日志，主要记录数据库的变更操作，比如 insert、update、delete 和 DDL，一般不记录普通 select。它主要用于主从复制和数据恢复。binlog 有 statement、row、mixed 三种格式，生产中常用 row，因为它记录行级变更，更不容易出现主从不一致。binlog 和 redo log 不同，binlog 是 Server 层日志，偏逻辑变更，用于复制和恢复；redo log 是 InnoDB 引擎层日志，偏物理页修改，用于崩溃恢复。事务提交时会通过两阶段提交保证 binlog 和 redo log 的一致性。</p></blockquote>"},{"t":"UndoLog日志的作用是什么？","tag":"","p":"","html":"<p><code>undo log</code> 主要有两个作用：<strong>事务回滚</strong> 和 <strong>MVCC 多版本控制</strong>。</p><ol><li><strong>用于事务回滚</strong></li></ol><p>事务修改数据前，InnoDB 会先把旧值记录到 <code>undo log</code>。</p><p>比如原来：</p><pre><code>name = 张三</code></pre><p>执行：</p><pre><code>UPDATE user<br/>SET name = '李四'<br/>WHERE id = 1;</code></pre><p>InnoDB 会在 <code>undo log</code> 里记录：</p><pre><code>把 name 从 李四 恢复成 张三</code></pre><p>如果事务执行失败或主动回滚：</p><pre><code>ROLLBACK;</code></pre><p>就可以根据 <code>undo log</code> 把数据恢复到修改前的状态。</p><p>所以它保证了事务的<strong>原子性</strong>。</p><ol><li><strong>用于 MVCC</strong></li></ol><p><code>undo log</code> 还会保存数据的历史版本。</p><p>在 InnoDB 中，一行数据被修改后，新版本在数据页里，旧版本在 <code>undo log</code> 里，并通过回滚指针连接起来，形成版本链。</p><p>普通 <code>SELECT</code> 做快照读时：</p><ul><li>会根据 <code>Read View</code> 判断当前版本是否可见。</li><li>如果当前版本不可见，就沿着 <code>undo log</code> 版本链找更旧的可见版本。</li></ul><p>所以 <code>undo log</code> 也是 MVCC 的基础。</p><ol><li><strong>它不是用来崩溃恢复的主日志</strong></li></ol><p>要注意区分：</p><ul><li><code>undo log</code>：主要用于回滚和 MVCC。</li><li><code>redo log</code>：主要用于崩溃恢复，保证持久性。</li><li><code>binlog</code>：主要用于主从复制和数据恢复。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>undo log 是 InnoDB 的回滚日志，主要有两个作用：第一是事务回滚，事务修改数据前会记录旧值，如果事务失败或回滚，可以根据 undo log 恢复到修改前状态，从而保证原子性；第二是支持 MVCC，undo log 中保存了数据的历史版本，普通快照读会根据 Read View 沿着版本链找到对当前事务可见的版本。需要注意，崩溃恢复主要依靠 redo log，而不是 undo log。</p></blockquote><span>[原文档此处有配图]</span>"},{"t":"有了undolog为啥还需要redolog呢？","tag":"","p":"","html":"<p>因为 <code>undo log</code> 和 <code>redo log</code> 解决的是<strong>两个不同问题</strong>。</p><p><strong>一句话区分</strong></p><ul><li><code>undo log</code>：保证<strong>能回滚</strong>，解决“改错了怎么撤销”。</li><li><code>redo log</code>：保证<strong>提交后不丢</strong>，解决“提交了但还没刷盘，宕机后怎么恢复”。</li></ul><ol><li><strong>undo log 解决什么</strong></li></ol><p>事务修改数据前，会记录旧值。</p><p>比如余额原来是 <code>100</code>：</p><pre><code>UPDATE account SET balance = 80 WHERE id = 1;</code></pre><p><code>undo log</code> 记录的是：</p><pre><code>如果回滚，把 balance 从 80 改回 100</code></pre><p>作用：</p><ul><li>事务失败可以回滚。</li><li>支持 MVCC 历史版本。</li><li>保证原子性。</li></ul><p>但是它不能保证提交后的数据不丢。</p><ol><li><strong>redo log 解决什么</strong></li></ol><p>InnoDB 修改数据时，通常先改内存里的 Buffer Pool 数据页，不会每次都立刻把数据页刷到磁盘。</p><p>如果事务提交了，但数据页还没刷盘，MySQL 宕机了：</p><pre><code>事务已经提交<br/>内存里的数据页还没落盘<br/>数据库宕机</code></pre><p>这时候就需要 <code>redo log</code>。</p><p><code>redo log</code> 记录的是“对数据页做了什么修改”。</p><p>数据库重启后，可以根据 <code>redo log</code> 把已经提交的修改重新做一遍。</p><p>作用：</p><ul><li>崩溃恢复。</li><li>保证持久性。</li><li>提高性能，因为不用每次提交都刷整页数据。</li></ul><ol><li><strong>为什么不能只用 undo log</strong></li></ol><p>因为 <code>undo log</code> 记录的是旧值，用来回滚。</p><p>如果事务已经提交了，数据页还没刷盘，宕机后你需要的是：</p><pre><code>把已提交的数据恢复回来</code></pre><p>这需要“重做修改”，也就是 <code>redo log</code>。</p><p><code>undo log</code> 做不到这个。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>undo log 和 redo log 作用不同。undo log 记录数据修改前的旧版本，用于事务回滚和 MVCC，保证原子性；redo log 记录数据页的修改，用于崩溃恢复，保证持久性。InnoDB 修改数据时通常先改 Buffer Pool，不会每次都立刻刷盘，如果事务提交后数据页还没落盘数据库就宕机了，就需要通过 redo log 把已提交的修改恢复回来。所以有了 undo log 还必须要 redo log，一个负责回滚，一个负责重做。</p></blockquote><blockquote><ul><li>Buﬀer Pool 是提高了读写效率没错，但是问题来了，Buﬀer Pool 是基于内存的，而内存总是不可靠，万一断电重启，还没来得及落盘的脏页数据就会丢失。</li><li>为了防止断电导致数据丢失的问题，当有一条记录需要更新的时候，InnoDB 引擎就会先更新内存（同时标记为脏页），然后将本次对这个页的修改以 redo log 的形式记录下来，这个时候更新就算完成了。</li><li>后续，InnoDB 引擎会在适当的时候，由后台线程将缓存在 Buﬀer Pool 的脏页刷新到磁盘里，这就是 WAL（Write-Ahead Logging）技术。</li><li>WAL 技术指的是， MySQL 的写操作并不是立刻写到磁盘上，而是先写日志，然后在合适的时间再写到磁盘上。</li></ul><p>过程如下图：</p></blockquote><span>[原文档此处有配图]</span>"},{"t":"redo log怎么保证持久性的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><code>redo log</code> 保证持久性的核心是：<strong>事务提交时先把修改记录写入 redo log，数据页可以之后再慢慢刷盘。宕机后根据 redo log 重做已提交修改。</strong></p><ol><li><strong>为什么需要 redo log</strong></li></ol><p>InnoDB 修改数据时，不会每次都直接改磁盘上的数据页，而是：</p><pre><code>先修改内存中的 Buffer Pool 数据页<br/>再择机刷盘</code></pre><p>这样性能更好。</p><p>但问题是：</p><pre><code>事务提交了<br/>数据页还在内存里<br/>还没刷到磁盘<br/>MySQL 宕机</code></pre><p>如果没有 <code>redo log</code>，这次已提交的数据就丢了。</p><ol><li><strong>redo log 怎么做</strong></li></ol><p>事务更新数据时，InnoDB 会记录一份 <code>redo log</code>，里面描述了对数据页的修改。</p><p>提交事务时，只要保证 <code>redo log</code> 已经刷到磁盘，就可以认为事务持久化了。</p><p>之后即使数据页还没刷盘，只要数据库宕机重启，InnoDB 可以根据磁盘上的 <code>redo log</code> 把修改重新做一遍。</p><ol><li><strong>WAL 机制</strong></li></ol><p>这叫 <code>WAL</code>，也就是 Write-Ahead Logging，<strong>先写日志，再写数据页</strong>。</p><p>流程可以理解为：</p><pre><code>1. 修改 Buffer Pool 中的数据页<br/>2. 记录 redo log<br/>3. 事务提交时刷 redo log<br/>4. 数据页之后异步刷盘<br/>5. 如果宕机，重启后用 redo log 恢复</code></pre><ol><li><strong>为什么写 redo log 比直接刷数据页快</strong></li></ol><ul><li><code>redo log</code> 是顺序写。</li><li>数据页刷盘可能是随机写。</li><li>顺序写性能更好。</li><li>所以提交时先刷较小的 redo log，比每次刷完整数据页更高效。</li></ul><ol><li><strong>参数影响</strong></li></ol><p><code>innodb_flush_log_at_trx_commit</code> 会影响持久性级别：</p><ul><li><code>1</code>：每次提交都把 redo log 刷到磁盘，最安全，默认常用。</li><li><code>2</code>：每次提交写到操作系统缓存，每秒刷盘，宕机可能丢最近 1 秒。</li><li><code>0</code>：每秒写入并刷盘，性能高但风险更大。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p></p></blockquote><p></p><blockquote><p>redo log 通过 WAL 机制保证持久性，也就是先写日志再写数据页。InnoDB 更新数据时先修改 Buffer Pool 中的数据页，同时记录 redo log；事务提交时只要 redo log 刷盘成功，就认为事务提交成功。即使此时真正的数据页还没刷到磁盘，数据库宕机后也可以根据 redo log 把已提交的修改重新做一遍，从而保证已提交事务不丢失。redo log 是顺序写，比每次随机刷数据页性能更好。</p></blockquote>"},{"t":"能不能只用binlog不用relo log？","tag":"","p":"","html":"<p>一般不行，<strong>InnoDB 不能只用 </strong><strong><code>binlog</code></strong><strong> 替代 </strong><strong><code>redo log</code></strong>。</p><p>你这里的 <code>relo log</code> 应该是想说 <strong><code>redo log</code></strong>。</p><ol><li><strong>两者层级不同</strong></li></ol><ul><li><code>redo log</code> 是 <code>InnoDB</code> 引擎层日志。</li><li><code>binlog</code> 是 <code>MySQL Server</code> 层日志。</li></ul><p><code>InnoDB</code> 做崩溃恢复时，依赖的是自己的 <code>redo log</code>。</p><ol><li><strong>两者作用不同</strong></li></ol><ul><li><code>redo log</code>：保证事务持久性，用于崩溃恢复。</li><li><code>binlog</code>：主要用于主从复制和基于时间点的数据恢复。</li></ul><p><code>binlog</code> 不是专门给 InnoDB 做崩溃恢复用的。</p><ol><li><strong>两者记录内容不同</strong></li></ol><ul><li><code>redo log</code> 记录的是数据页的物理修改，比如某个页改了什么。</li><li><code>binlog</code> 记录的是逻辑变更，比如执行了什么 SQL，或者哪一行变成什么。</li></ul><p>数据库宕机恢复时，InnoDB 需要快速恢复数据页状态，这更适合用 <code>redo log</code>。</p><ol><li><strong>两者写入方式不同</strong></li></ol><ul><li><code>redo log</code> 是循环写，空间固定，适合高频写入和崩溃恢复。</li><li><code>binlog</code> 是追加写，用于保留历史变更。</li><li><code>redo log</code> 更适合事务提交时快速落盘。</li></ul><ol><li><strong>如果只用 binlog 会有什么问题</strong></li></ol><p>假设：</p><pre><code>事务提交了<br/>binlog 写了<br/>数据页还没刷盘<br/>MySQL 宕机</code></pre><p>理论上你可能想用 <code>binlog</code> 重放恢复。</p><p>但问题是：</p><ul><li><code>binlog</code> 是 Server 层逻辑日志，不知道 InnoDB 页的具体状态。</li><li>重放 binlog 成本高。</li><li>可能存在幂等、顺序、事务状态判断等复杂问题。</li><li>不能像 redo log 那样快速恢复 Buffer Pool 中的脏页修改。</li></ul><p>所以 InnoDB 仍然需要 <code>redo log</code>。</p><p><strong>面试背诵版</strong></p><blockquote><p>不能简单只用 binlog 不用 redo log。binlog 是 Server 层日志，主要用于主从复制和数据恢复；redo log 是 InnoDB 引擎层日志，主要用于崩溃恢复和保证事务持久性。redo log 记录的是数据页的物理修改，适合宕机后快速恢复脏页；binlog 记录的是逻辑变更，不适合作为 InnoDB 的崩溃恢复日志。而且 redo log 是顺序循环写，提交性能更好。所以两者作用不同，不能互相替代。</p></blockquote>"},{"t":"binlog 两阶段提交过程是怎么样的？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>面试可以这样答：<strong>两阶段提交是为了保证 </strong><strong><code>redo log</code></strong><strong> 和 </strong><strong><code>binlog</code></strong><strong> 的一致性。</strong></p><ol><li><strong>为什么需要两阶段提交</strong></li></ol><p>一次更新既要写：</p><ul><li><code>redo log</code>：InnoDB 用于崩溃恢复</li><li><code>binlog</code>：MySQL Server 用于主从复制和数据恢复</li></ul><p>如果两者不一致，就会出问题：</p><ul><li><code>redo log</code> 有，<code>binlog</code> 没有：主库恢复后有这条数据，但从库没有。</li><li><code>binlog</code> 有，<code>redo log</code> 没有：从库有这条数据，但主库恢复后没有。</li></ul><p>所以需要两阶段提交保证一致。</p><ol><li><strong>两阶段提交过程</strong></li></ol><p>以一条 <code>UPDATE</code> 为例：</p><pre><code>UPDATE user SET name = '李四' WHERE id = 1;</code></pre><p>大致过程：</p><p><strong>第一阶段：prepare</strong></p><ul><li>InnoDB 执行更新，修改 Buffer Pool。</li><li>写 <code>undo log</code>。</li><li>写 <code>redo log</code>，状态为 <code>prepare</code>。</li><li>此时事务还没有真正提交。</li></ul><pre><code>redo log: prepare</code></pre><p><strong>第二阶段：写 binlog</strong></p><ul><li>MySQL Server 层写入 <code>binlog</code>。</li><li><code>binlog</code> 写成功后，事务才可以继续提交。</li></ul><pre><code>binlog: 写入成功</code></pre><p><strong>第三步：commit redo log</strong></p><ul><li>InnoDB 把 <code>redo log</code> 状态改成 <code>commit</code>。</li><li>事务提交完成。</li></ul><pre><code>redo log: commit</code></pre><p>所以顺序是：</p><pre><code>redo log prepare<br/>→ 写 binlog<br/>→ redo log commit</code></pre><ol><li><strong>崩溃恢复时怎么判断</strong></li></ol><p>如果 MySQL 在中间宕机：</p><p><strong>情况 1：redo log 还没 prepare</strong></p><ul><li>事务没完成。</li><li>直接回滚。</li></ul><p><strong>情况 2：redo log prepare 了，但 binlog 没写成功</strong></p><ul><li>说明事务没有完整提交。</li><li>回滚事务。</li></ul><p><strong>情况 3：redo log prepare 了，binlog 写成功了，但 redo log 还没 commit</strong></p><ul><li>恢复时发现 binlog 存在。</li><li>认为事务可以提交。</li><li>补提交 redo log。</li></ul><p><strong>情况 4：redo log commit 完成</strong></p><ul><li>正常提交。</li><li>恢复即可。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>binlog 两阶段提交是为了解决 redo log 和 binlog 的一致性问题。</p><ul><li>一次更新会先写 InnoDB 的 redo log，并把状态标记为 prepare；</li><li>然后 Server 层写 binlog；binlog 写成功后，再把 redo log 标记为 commit。</li><li>顺序就是 redo log prepare、写 binlog、redo log commit。崩溃恢复时，如果 redo log 是 prepare 状态，就会检查 binlog 是否存在：如果 binlog 存在，说明事务应该提交；如果 binlog 不存在，就回滚。这样可以保证主库崩溃恢复和主从复制的数据一致。</li></ul></blockquote>"},{"t":"update语句的具体执行过程是怎样的？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>面试可以按 <strong>Server 层 + InnoDB 层 + 日志提交</strong> 来讲。</p><p>假设执行：</p><pre><code>UPDATE user<br/>SET name = '李四'<br/>WHERE id = 1;</code></pre><blockquote><ol><li><strong>连接和解析</strong></li></ol><ul><li>客户端把 SQL 发给 MySQL。</li><li>连接器负责连接、认证、权限校验。</li><li>解析器做词法、语法分析，确认 SQL 合法。</li><li>预处理器检查表、字段是否存在。</li></ul><ol><li><strong>优化器生成执行计划</strong></li></ol><ul><li>优化器判断怎么查这条数据。</li><li>比如 <code>id = 1</code> 是主键条件，就选择走主键索引。</li><li>生成执行计划后交给执行器。</li></ul><ol><li><strong>执行器调用 InnoDB</strong></li></ol><ul><li>执行器调用 InnoDB 存储引擎接口。</li><li>InnoDB 根据主键索引找到 <code>id = 1</code> 的记录。</li><li>如果数据页不在 Buffer Pool，就从磁盘加载到内存。</li></ul><ol><li><strong>加锁</strong></li></ol><ul><li><code>UPDATE</code> 是当前读。</li><li>InnoDB 会对要更新的记录加排他锁，也就是 X 锁。</li><li>防止其他事务同时修改这行数据。</li></ul><ol><li><strong>写 undo log</strong></li></ol><ul><li>修改前，先记录旧值到 <code>undo log</code>。</li><li>用于事务回滚。</li><li>也用于 MVCC 版本链。</li></ul></blockquote><p>比如记录：</p><pre><code>name 原来是 张三</code></pre><ol><li><strong>修改内存数据页</strong></li></ol><ul><li>InnoDB 在 Buffer Pool 中修改这行记录。</li><li>把 <code>name</code> 从 <code>张三</code> 改成 <code>李四</code>。</li><li>此时数据页变成脏页，但不一定立刻刷盘。</li></ul><ol><li><strong>写 redo log</strong></li></ol><ul><li>InnoDB 记录 <code>redo log</code>。</li><li>用于崩溃恢复。</li><li>事务提交时先进入 <code>prepare</code> 状态。</li></ul><ol><li><strong>写 binlog</strong></li></ol><ul><li>Server 层写 <code>binlog</code>。</li><li>用于主从复制和数据恢复。</li><li>如果开启 binlog，这一步非常关键。</li></ul><ol><li><strong>两阶段提交</strong></li></ol><p>为了保证 <code>redo log</code> 和 <code>binlog</code> 一致，事务提交采用两阶段提交：</p><pre><code>redo log prepare<br/>→ 写 binlog<br/>→ redo log commit</code></pre><p>这样即使中间宕机，也能根据日志判断事务该提交还是回滚。</p><ol><li><strong>提交事务并释放锁</strong></li></ol><ul><li>事务提交成功。</li><li>释放行锁。</li><li>返回影响行数给客户端。</li></ul><p>如果是默认 <code>autocommit = 1</code>，这条 <code>UPDATE</code> 自己就是一个事务，执行成功后自动提交。</p><p><strong>面试背诵版</strong></p><blockquote><ul><li>一条 update 语句会先经过连接器、解析器、预处理器和优化器，生成执行计划。</li><li>执行器调用 InnoDB，根据索引找到要更新的记录；update 是当前读，会对记录加排他锁。</li><li>修改前先写 undo log，用于回滚和 MVCC；然后在 Buffer Pool 中修改数据页，数据页变成脏页；接着写 redo log，用于崩溃恢复。如果开启 binlog，还要写 binlog。提交时通过两阶段提交保证 redo log 和 binlog 一致，顺序是 redo log prepare、写 binlog、redo log commit。最后事务提交，释放锁，返回影响行数。</li></ul></blockquote>"},{"t":"MySQL是如何保障数据不丢失的？","tag":"","p":"","html":"<p>面试可以从 <strong>事务 + 日志 + 刷盘机制 + 主从备份</strong> 这几个角度答。</p><ol><li><strong>redo log 保证已提交事务不丢</strong></li></ol><ul><li>InnoDB 更新数据时，先改内存中的 <code>Buffer Pool</code>。</li><li>数据页不会每次立刻刷盘。</li><li>为了防止宕机丢数据，会先写 <code>redo log</code>。</li><li>事务提交时，只要 <code>redo log</code> 持久化了，即使数据页没刷盘，宕机后也能恢复。</li></ul><p>核心机制是：</p><pre><code>WAL：先写日志，再写数据页</code></pre><ol><li><strong>undo log 保证事务可回滚</strong></li></ol><ul><li>修改数据前，会记录旧版本到 <code>undo log</code>。</li><li>如果事务失败或回滚，可以恢复到修改前状态。</li><li>它主要保证事务原子性。</li><li>同时也支持 <code>MVCC</code>。</li></ul><ol><li><strong>binlog 支持复制和恢复</strong></li></ol><ul><li><code>binlog</code> 是 Server 层日志。</li><li>记录数据库变更操作。</li><li>主要用于：<ul><li>主从复制</li><li>基于时间点的数据恢复</li></ul></li><li>比如误删数据后，可以用全量备份 + binlog 恢复到某个时间点。</li></ul><ol><li><strong>redo log 和 binlog 两阶段提交</strong></li></ol><ul><li>一条更新既要写 <code>redo log</code>，也要写 <code>binlog</code>。</li><li>MySQL 用两阶段提交保证两者一致：</li></ul><pre><code>redo log prepare<br/>→ 写 binlog<br/>→ redo log commit</code></pre><p>这样可以避免主库崩溃恢复和从库复制结果不一致。</p><ol><li><strong>刷盘参数影响可靠性</strong></li></ol><p>关键参数：</p><pre><code>innodb_flush_log_at_trx_commit<br/>sync_binlog</code></pre><p>比较安全的配置通常是：</p><pre><code>innodb_flush_log_at_trx_commit = 1<br/>sync_binlog = 1</code></pre><p>含义：</p><ul><li>每次事务提交都把 <code>redo log</code> 刷盘。</li><li>每次事务提交都把 <code>binlog</code> 刷盘。</li><li>安全性高，但性能成本更高。</li></ul><ol><li><strong>主从复制和备份兜底</strong></li></ol><p>为了进一步防止数据丢失：</p><ul><li>做主从复制。</li><li>做定期全量备份。</li><li>保留 binlog。</li><li>重要场景可以使用半同步复制，降低主库宕机导致数据未同步的风险。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>MySQL 防止数据丢失主要依赖事务日志和刷盘机制。</p><p>InnoDB 通过 redo log 保证事务持久性，事务提交时先把 redo log 刷盘，即使数据页还没刷到磁盘，宕机后也可以通过 redo log 恢复；undo log 用于事务回滚，保证原子性；binlog 用于主从复制和基于时间点的数据恢复。为了保证 redo log 和 binlog 一致，MySQL 使用两阶段提交。生产中还会通过<code>innodb_flush_log_at_trx_commit=1</code> 和 <code>sync_binlog=1</code> 提高安全性，并结合主从复制和定期备份来进一步降低数据丢失风险。</p></blockquote>"},{"t":"RedoLog是在内存里吗？","tag":"","p":"","html":"<p><code>redo log</code> <strong>既有内存里的部分，也有磁盘上的文件</strong>。</p><ol><li><strong>内存中的 redo log buffer</strong></li></ol><p>事务执行更新时，InnoDB 会先把 redo 日志写到内存中的：</p><pre><code>redo log buffer</code></pre><p>也就是重做日志缓冲区。</p><p>这样可以先写内存，提高性能。</p><ol><li><strong>磁盘上的 redo log file</strong></li></ol><p>事务提交时，redo log buffer 中的内容会根据刷盘策略写入磁盘上的：</p><pre><code>redo log file</code></pre><p>这样即使数据库宕机，也可以靠磁盘上的 redo log 恢复已提交事务。</p><ol><li><strong>提交流程简单理解</strong></li></ol><pre><code>执行 UPDATE<br/>  ↓<br/>修改 Buffer Pool 中的数据页<br/>  ↓<br/>写 redo log buffer<br/>  ↓<br/>事务提交时刷到 redo log file<br/>  ↓<br/>返回提交成功</code></pre><p>如果配置比较安全：</p><pre><code>innodb_flush_log_at_trx_commit = 1</code></pre><p>表示每次事务提交都会把 redo log 写入并刷到磁盘。</p><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>redo log 不是只在内存里。事务执行时会先写到内存中的 redo log buffer，提交时再根据 <code>innodb_flush_log_at_trx_commit</code> 参数刷到磁盘上的 redo log file。真正保证崩溃恢复和持久性的是已经落盘的 redo log。如果只停留在内存里，数据库宕机后这部分日志也会丢失。</p></blockquote>"},{"t":"为什么要写RedoLog，而不是直接写到B+树里面？","tag":"","p":"","html":"<p>因为<strong>直接把数据页写回 B+ 树所在的磁盘文件成本太高，而且随机 IO 多</strong>；<code>redo log</code> 可以把随机写变成顺序写，提高性能，同时保证宕机恢复。</p><ol><li><strong>B+ 树数据页写盘是随机 IO</strong></li></ol><p>InnoDB 的数据和索引都在 B+ 树的数据页里。</p><p>一次 <code>UPDATE</code> 可能只改了一行，但这一行所在的数据页可能分布在磁盘不同位置。</p><p>如果每次提交都直接刷 B+ 树数据页：</p><ul><li>要找到对应数据页。</li><li>可能是随机写。</li><li>还可能要写整个页。</li><li>性能很差。</li></ul><ol><li><strong>redo log 是顺序写，更快</strong></li></ol><p><code>redo log</code> 是连续追加写。</p><p>事务提交时，先把修改记录写到 <code>redo log</code>，比随机刷数据页快很多。</p><pre><code>随机写数据页：慢<br/>顺序写 redo log：快</code></pre><p>这就是 <code>WAL</code> 机制：</p><pre><code>Write-Ahead Logging：先写日志，再写数据页</code></pre><ol><li><strong>数据页可以延迟刷盘</strong></li></ol><p>有了 <code>redo log</code> 后：</p><ul><li>更新时先改内存里的 <code>Buffer Pool</code>。</li><li>数据页变成脏页。</li><li>事务提交时保证 <code>redo log</code> 落盘。</li><li>真正的数据页可以之后由后台线程异步刷盘。</li></ul><p>这样既保证了提交后不丢，又提高了性能。</p><ol><li><strong>宕机后可以恢复</strong></li></ol><p>如果事务提交后：</p><pre><code>redo log 已经落盘<br/>B+ 树数据页还没刷盘<br/>数据库宕机</code></pre><p>重启时 InnoDB 可以根据 <code>redo log</code> 把这次修改重新应用到数据页上。</p><p>所以已提交事务不会丢。</p><p><strong>面试背诵版</strong></p><blockquote><p>不直接写 B+ 树数据页，是因为数据页分布在磁盘不同位置，每次提交都刷数据页会产生大量随机 IO，性能很差。而 redo log 是顺序写，写入成本低。InnoDB 采用 WAL 机制，先修改 Buffer Pool 中的数据页，再写 redo log，事务提交时只要 redo log 落盘，就可以认为提交成功，数据页后续异步刷盘。即使宕机，也能通过 redo log 恢复已提交的数据。所以 redo log 既提升性能，又保证持久性。</p></blockquote>"},{"t":"mysql 两次写（double write buﬀer）了解吗？","tag":"","p":"","html":"<p>面试可以这样答：<strong>Double Write Buffer 是 InnoDB 用来防止数据页写坏的一种机制。</strong></p><span>[原文档此处有配图]</span><ol><li><strong>它解决什么问题</strong></li></ol><p>InnoDB 的数据页通常是 <code>16KB</code>。</p><p>操作系统或磁盘一次写入可能不是完整 <code>16KB</code> 原子写。</p><p>如果 MySQL 正在把一个数据页刷到磁盘时宕机，可能出现：</p><pre><code>一个 16KB 页只写了一半</code></pre><p>这种情况叫<strong>部分页写入</strong>，也叫 <strong>partial page write</strong>。</p><p>这时数据页本身可能已经损坏。</p><p>问题是：</p><ul><li><code>redo log</code> 可以重放修改。</li><li>但如果原始数据页已经坏了，redo log 也没法正确应用。</li><li>所以需要先保证数据页本身是完整的。</li></ul><ol><li><strong>Double Write 怎么做</strong></li></ol><p>InnoDB 刷脏页时，不是直接把页写到最终表空间位置，而是分两步：</p><pre><code>第一步：先把脏页写到 doublewrite buffer<br/>第二步：再把脏页写到真正的数据文件位置</code></pre><p>这里的 doublewrite buffer 包括：</p><ul><li>内存中的 doublewrite buffer</li><li>磁盘上的 doublewrite 区域</li></ul><ol><li><strong>宕机后怎么恢复</strong></li></ol><p>如果第二步写真实数据页时宕机，导致数据页损坏：</p><ul><li>MySQL 重启后会检查页的完整性。</li><li>如果发现表空间中的页损坏。</li><li>就从 doublewrite 区域找到完整副本。</li><li>先恢复这个完整页。</li><li>再结合 redo log 重放修改。</li></ul><p>这样就能避免部分页写入导致数据不可恢复。</p><ol><li><strong>为什么叫两次写</strong></li></ol><p>因为同一个数据页刷盘时会写两次：</p><pre><code>写 doublewrite 区域一次<br/>写真正数据文件一次</code></pre><p>所以叫 Double Write。</p><ol><li><strong>代价</strong></li></ol><ul><li>会增加一次额外写入。</li><li>有一定性能开销。</li><li>但换来的是数据页完整性和崩溃恢复可靠性。</li><li>现在 SSD 和新版本 MySQL 对这块也有优化。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>Double Write Buffer 是 InnoDB 为了解决部分页写入问题的机制。</p><p>InnoDB 数据页通常是 16KB，如果刷脏页时数据库或操作系统宕机，可能导致一个页只写了一半，数据页损坏。redo log 只能重做页上的修改，但前提是数据页本身完整，所以 InnoDB 刷脏页时会先把页写到 doublewrite 区域，再写到真正的数据文件位置。如果宕机后发现数据页损坏，就先从 doublewrite 区域恢复完整页，再应用 redo log。它会带来额外写入开销，但提高了数据可靠性。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"性能调优","questions":[{"t":"mysql的explain有什么作用？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p><code>EXPLAIN</code> 的作用是：<strong>查看 SQL 的执行计划，用来分析 SQL 是否走索引、扫描多少数据、有没有排序/临时表等问题。</strong></p><ol><li><strong>它能看什么</strong></li></ol><p>执行：</p><pre><code>EXPLAIN<br/>SELECT *<br/>FROM orders<br/>WHERE user_id = 100;</code></pre><p>可以看到 MySQL 计划怎么执行这条 SQL，比如：</p><ul><li>访问哪张表。</li><li>表的访问顺序。</li><li>可能用哪些索引。</li><li>实际选择了哪个索引。</li><li>预计扫描多少行。</li><li>是否需要回表、排序、临时表。</li></ul><ol><li><strong>常看字段</strong></li></ol><ul><li><code>type</code><ul><li>表示访问类型。</li><li>常见从好到差大概是：</li></ul></li></ul><pre><code>system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code></pre><ul><li>如果是 <code>ALL</code>，通常表示全表扫描，要重点关注。</li><li><code>possible_keys</code><ul><li>可能用到的索引。</li></ul></li><li><code>key</code><ul><li>实际使用的索引。</li><li>如果是 <code>NULL</code>，说明没用索引。</li></ul></li><li><code>rows</code><ul><li>预计扫描的行数。</li><li>越小越好。</li></ul></li><li><code>Extra</code><ul><li>额外信息。</li><li>比如：<ul><li><code>Using index</code>：使用覆盖索引。</li><li><code>Using where</code>：Server 层还要做条件过滤。</li><li><code>Using filesort</code>：需要额外排序。</li><li><code>Using temporary</code>：使用临时表。</li></ul></li></ul></li></ul><ol><li><strong>用来做什么优化</strong></li></ol><p>通过 <code>EXPLAIN</code> 可以判断：</p><ul><li>SQL 有没有走索引。</li><li>联合索引是否用完整。</li><li>是否发生全表扫描。</li><li>是否有大量回表。</li><li>是否出现文件排序。</li><li>是否用了临时表。</li><li>表连接顺序是否合理。</li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>EXPLAIN 用来查看 SQL 的执行计划，是 SQL 优化的重要工具。它可以告诉我们 MySQL 访问表的顺序、可能使用的索引、实际使用的索引、访问类型、预估扫描行数以及是否使用临时表或文件排序。分析时我会重点看 type、key、rows 和 Extra。如果 type 是 ALL、key 为 NULL、rows 很大，或者 Extra 中出现 Using filesort、Using temporary，就说明这条 SQL 可能需要优化。</p></blockquote>"},{"t":"给你张表，发现查询速度很慢，你有那些解决方案","tag":"","p":"","html":"<p>面试可以按<strong>排查思路</strong>来答，不要一上来就说“加索引”。</p><ol><li><strong>先定位慢 SQL</strong></li></ol><ul><li>通过慢查询日志、监控平台、接口耗时定位具体慢 SQL。</li><li>确认是偶发慢，还是长期慢。</li><li>看数据量、返回行数、并发量。</li></ul><ol><li><strong>用 </strong><strong><code>EXPLAIN</code></strong><strong> 分析执行计划</strong></li></ol><p>重点看：</p><ul><li><code>type</code> 是否是 <code>ALL</code> 全表扫描。</li><li><code>key</code> 是否使用了索引。</li><li><code>rows</code> 预估扫描行数是否很大。</li><li><code>Extra</code> 是否有：<ul><li><code>Using filesort</code></li><li><code>Using temporary</code></li><li>大量 <code>Using where</code></li></ul></li></ul><ol><li><strong>优化索引</strong></li></ol><p>根据 SQL 场景建合适索引：</p><ul><li><code>WHERE</code> 高频过滤字段建索引。</li><li><code>JOIN</code> 关联字段建索引。</li><li><code>ORDER BY</code>、<code>GROUP BY</code> 字段结合索引优化。</li><li>多条件查询考虑联合索引。</li><li>遵循最左前缀原则。</li><li>尽量设计覆盖索引，减少回表。</li><li>避免低区分度字段单独建索引。</li></ul><ol><li><strong>优化 SQL 写法</strong></li></ol><p>避免索引失效：</p><pre><code>WHERE DATE(create_time) = '2026-08-08'</code></pre><p>改成：</p><pre><code>WHERE create_time &gt;= '2026-08-08 00:00:00'<br/>  AND create_time &lt; '2026-08-09 00:00:00'</code></pre><p>其他常见点：</p><ul><li>避免 <code>LIKE '%xxx'</code> 左模糊。</li><li>避免隐式类型转换。</li><li>避免 <code>SELECT *</code>，只查必要字段。</li><li>大分页优化，不要直接深度 <code>LIMIT offset, size</code>。</li><li><code>OR</code> 条件不走索引时可以考虑改写为 <code>UNION ALL</code>。</li></ul><ol><li><strong>优化表结构和数据量</strong></li></ol><p>如果单表数据太大：</p><ul><li>做冷热数据拆分。</li><li>历史数据归档。</li><li>分库分表。</li><li>分区表。</li><li>冗余字段减少复杂 <code>JOIN</code>。</li></ul><ol><li><strong>优化排序、分组、分页</strong></li></ol><ul><li><code>ORDER BY</code> 尽量走索引。</li><li><code>GROUP BY</code> 字段结合索引。</li><li>深分页用游标方式：</li></ul><pre><code>WHERE id &gt; last_id<br/>ORDER BY id<br/>LIMIT 20</code></pre><p>比：</p><pre><code>LIMIT 100000, 20</code></pre><p>更好。</p><ol><li><strong>看系统和配置层面</strong></li></ol><p>如果 SQL 本身没问题，再看：</p><ul><li>Buffer Pool 是否太小。</li><li>磁盘 IO 是否高。</li><li>CPU 是否打满。</li><li>连接数是否过多。</li><li>锁等待是否严重。</li><li>是否有大事务、慢事务。</li></ul><ol><li><strong>加缓存</strong></li></ol><p>对于读多写少、热点数据：</p><ul><li>可以加 Redis 缓存。</li><li>本地缓存。</li><li>但要考虑缓存一致性、过期策略和击穿/穿透/雪崩。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>如果一张表查询很慢，我会先通过慢查询日志或监控定位具体 SQL，再用 EXPLAIN 看执行计划，重点看有没有走索引、扫描行数、是否 filesort 或 temporary。然后根据 WHERE、JOIN、ORDER BY、GROUP BY 场景设计合适索引，尤其是联合索引和覆盖索引。同时优化 SQL 写法，避免函数、隐式转换、左模糊、SELECT *、深分页等问题。如果单表数据量太大，还可以考虑冷热数据拆分、归档、分区、分库分表。最后再看系统层面的 IO、Buffer Pool、锁等待和连接数，读多写少的热点数据也可以加缓存。</p></blockquote>"},{"t":"如果Explain用到的索引不正确的话，有什么办法干预吗？","tag":"","p":"","html":"<p>可以干预，但面试要说清楚：<strong>优先让优化器自然选对，强制干预是最后手段。</strong></p><ol><li><strong>先更新统计信息</strong></li></ol><p>有时候优化器选错索引，是因为统计信息不准。</p><p>可以执行：</p><pre><code>ANALYZE TABLE table_name;</code></pre><p>让 MySQL 重新统计索引基数和数据分布。</p><ol><li><strong>调整或重建索引</strong></li></ol><p>如果索引设计不合理，优化器可能选不到最优索引。</p><p>可以考虑：</p><ul><li>新建更合适的联合索引。</li><li>调整联合索引字段顺序。</li><li>删除冗余或干扰索引。</li><li>设计覆盖索引减少回表。</li></ul><p>比如：</p><pre><code>WHERE user_id = ?<br/>  AND status = ?<br/>ORDER BY create_time DESC</code></pre><p>可以考虑：</p><pre><code>(user_id, status, create_time)</code></pre><ol><li><strong>改写 SQL</strong></li></ol><p>有些 SQL 写法会影响优化器选择：</p><ul><li>避免函数包裹索引列。</li><li>避免隐式类型转换。</li><li>避免复杂 <code>OR</code>。</li><li>拆分复杂 SQL。</li><li>必要时用 <code>UNION ALL</code> 改写。</li></ul><ol><li><strong>使用索引提示 Hint</strong></li></ol><p>MySQL 提供索引提示：</p><p><strong>强制使用某个索引</strong></p><pre><code>SELECT *<br/>FROM orders FORCE INDEX(idx_user_id)<br/>WHERE user_id = 100;</code></pre><p><strong>建议使用某个索引</strong></p><pre><code>SELECT *<br/>FROM orders USE INDEX(idx_user_id)<br/>WHERE user_id = 100;</code></pre><p><strong>忽略某个索引</strong></p><pre><code>SELECT *<br/>FROM orders IGNORE INDEX(idx_status)<br/>WHERE user_id = 100;</code></pre><p>区别：</p><ul><li><code>USE INDEX</code>：建议优化器考虑这些索引。</li><li><code>FORCE INDEX</code>：强制倾向使用指定索引。</li><li><code>IGNORE INDEX</code>：让优化器不要用某个索引。</li></ul><ol><li><strong>注意 Hint 不要滥用</strong></li></ol><ul><li>数据量和分布会变化。</li><li>今天强制某个索引快，以后可能变慢。</li><li>Hint 会降低优化器灵活性。</li><li>所以一般先优化统计信息、索引和 SQL，最后才用 Hint。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>如果 EXPLAIN 发现优化器选的索引不正确，可以先用 ANALYZE TABLE 更新统计信息，因为可能是统计信息不准导致误判。然后看索引设计是否合理，比如是否需要调整联合索引顺序、增加覆盖索引或删除冗余索引，也可以改写 SQL 避免索引失效。如果确实需要人工干预，可以使用 USE INDEX、FORCE INDEX、IGNORE INDEX 这类索引提示。但 Hint 不建议滥用，因为数据分布变化后可能反而变慢，一般作为最后手段。</p></blockquote>"}]},{"no":"","title":"架构","questions":[{"t":"MySQL主从复制了解吗","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>面试可以这样回答：<strong>MySQL 主从复制是通过 binlog 实现主库数据同步到从库的机制。</strong></p><ol><li><strong>主从复制是什么</strong></li></ol><blockquote><p>主从复制就是主库负责写入数据，从库通过复制主库的变更日志，把数据同步过来。常用于读写分离、备份容灾和高可用。</p></blockquote><ol><li><strong>基本原理</strong></li></ol><p>主要依赖三个线程 / 组件：</p><ul><li><strong>主库 dump thread</strong><ul><li>主库把 <code>binlog</code> 发送给从库。</li></ul></li><li><strong>从库 IO thread</strong><ul><li>从库连接主库，请求读取 <code>binlog</code>。</li><li>拉取后写入自己的 <code>relay log</code> 中继日志。</li></ul></li><li><strong>从库 SQL thread</strong><ul><li>读取 <code>relay log</code>。</li><li>重放里面的 SQL 或行变更。</li><li>最终让从库数据和主库一致。</li></ul></li></ul><p>流程是：</p><pre><code>主库写入数据<br/>  ↓<br/>记录 binlog<br/>  ↓<br/>从库 IO 线程拉取 binlog<br/>  ↓<br/>写入 relay log<br/>  ↓<br/>从库 SQL 线程重放 relay log<br/>  ↓<br/>从库数据同步</code></pre><ol><li><strong>binlog 格式</strong></li></ol><p>常见格式：</p><ul><li><code>statement</code><ul><li>记录 SQL。</li><li>日志小，但可能有主从不一致风险。</li></ul></li><li><code>row</code><ul><li>记录行数据变更。</li><li>更准确，生产更常用。</li><li>但日志量较大。</li></ul></li><li><code>mixed</code><ul><li>混合模式。</li></ul></li></ul><ol><li><strong>主从复制的作用</strong></li></ol><ul><li><strong>读写分离</strong><ul><li>主库写，从库读，分担查询压力。</li></ul></li><li><strong>数据备份</strong><ul><li>从库可以作为热备。</li></ul></li><li><strong>高可用</strong><ul><li>主库故障时，可以提升从库为主库。</li></ul></li><li><strong>数据分析</strong><ul><li>报表查询可以放到从库，避免影响主库。</li></ul></li></ul><ol><li><strong>主从复制的问题</strong></li></ol><ul><li><strong>复制延迟</strong><ul><li>主库写入后，从库可能还没同步到。</li><li>读从库可能读到旧数据。</li></ul></li><li><strong>主从不一致</strong><ul><li>网络、异常中断、非确定性 SQL 都可能导致不一致。</li></ul></li><li><strong>单线程复制瓶颈</strong><ul><li>老版本从库 SQL 线程可能重放慢。</li><li>新版本支持并行复制改善。</li></ul></li></ul><ol><li><strong>面试背诵版</strong></li></ol><blockquote><p>MySQL 主从复制主要依赖 binlog。</p><p>主库执行写操作后记录 binlog，</p><p>从库的 IO 线程连接主库拉取 binlog，并写入 relay log；</p><p>从库的 SQL 线程再读取 relay log 并重放，从而让从库数据和主库保持一致。</p><p>主从复制常用于读写分离、备份容灾和高可用。需要注意的是它通常是异步复制，所以可能存在主从延迟；生产中一般使用 row 格式 binlog，减少主从不一致风险。</p></blockquote>"},{"t":"主从延迟都有什么处理方法？","tag":"","p":"","html":"<p>面试可以这样答：<strong>主从延迟要先定位原因，再从架构、SQL、复制机制和业务读策略上处理。</strong></p><ol><li><strong>先定位延迟原因</strong></li></ol><p>常见原因：</p><ul><li>主库写入压力太大，从库重放跟不上。</li><li>大事务执行时间长。</li><li>慢 SQL 或缺少索引导致从库执行慢。</li><li>从库机器配置比主库差。</li><li>网络延迟。</li><li>从库还承担大量查询，影响复制线程。</li><li>单线程复制瓶颈。</li></ul><ol><li><strong>优化主库写入和 SQL</strong></li></ol><ul><li>拆分大事务，避免一次更新太多数据。</li><li>优化慢 SQL，补充合适索引。</li><li>避免大批量 <code>UPDATE</code> / <code>DELETE</code>。</li><li>控制写入峰值，削峰填谷。</li></ul><ol><li><strong>提升从库能力</strong></li></ol><ul><li>提高从库硬件配置，比如 CPU、IO、内存。</li><li>增大 <code>innodb_buffer_pool_size</code>。</li><li>从库不要跑太重的统计查询。</li><li>把报表、分析类查询拆到专门从库。</li></ul><ol><li><strong>开启并行复制</strong></li></ol><p>MySQL 支持并行复制，可以提高从库重放速度。</p><p>常见思路：</p><ul><li>基于库并行。</li><li>基于组提交并行。</li><li>基于 WriteSet 并行。</li></ul><p>面试说到这里就够：</p><blockquote><p>可以开启 MySQL 的并行复制，让从库多个 worker 并行重放 relay log，提升复制速度。</p></blockquote><ol><li><strong>使用半同步复制</strong></li></ol><ul><li>异步复制下，主库提交后不等从库确认。</li><li>半同步复制要求至少一个从库确认收到日志后，主库再返回成功。</li><li>可以降低主库宕机导致数据丢失风险。</li><li>但它不能完全解决延迟，只是提高数据可靠性。</li></ul><ol><li><strong>业务层避免读到旧数据</strong></li></ol><p>对于强一致读：</p><ul><li>写后立即读主库。</li><li>重要查询走主库。</li><li>根据用户 ID 做读写绑定。</li><li>判断主从延迟，延迟高时切回主库。</li><li>写入后短时间内读主库。</li></ul><p>比如：</p><pre><code>用户刚修改个人资料后，下一次查询直接读主库</code></pre><ol><li><strong>监控和告警</strong></li></ol><ul><li>监控 <code>Seconds_Behind_Master</code> / 复制延迟指标。</li><li>监控 relay log 堆积。</li><li>监控复制线程状态。</li><li>延迟超过阈值时告警或自动切换读策略。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>主从延迟首先要定位原因，常见原因有主库写入压力大、大事务、慢 SQL、从库机器性能差、网络问题、从库查询压力大以及复制单线程瓶颈。处理上可以优化 SQL 和索引、拆分大事务、减少大批量更新，提高从库硬件配置，把报表查询和复制从库隔离；也可以开启并行复制，提高 relay log 重放速度。</p><p>业务层面，对于写后马上读的强一致场景，可以直接读主库，或者在检测到延迟较大时把读流量切回主库。生产中还需要做好复制延迟监控和告警。</p></blockquote>"},{"t":"分表和分库是什么？有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><blockquote><p>面试可以这样答：<strong>分表解决单表太大，分库解决单库压力太大。</strong></p></blockquote><ol><li><strong>分表是什么</strong></li></ol><blockquote><p>分表是把一张大表拆成多张小表。</p></blockquote><p>比如原来一张订单表：</p><pre><code>orders</code></pre><p>拆成：</p><pre><code>orders_0<br/>orders_1<br/>orders_2<br/>orders_3</code></pre><p>常见分表方式：</p><ul><li>按用户 ID 取模：</li></ul><pre><code>user_id % 4</code></pre><ul><li>按时间拆分：</li></ul><pre><code>orders_202601<br/>orders_202602</code></pre><p><strong>分表主要解决</strong></p><ul><li>单表数据量太大。</li><li>索引太大。</li><li>查询和写入变慢。</li><li>单表维护困难。</li></ul><ol><li><strong>分库是什么</strong></li></ol><blockquote><p>分库是把数据拆到多个数据库实例中。</p></blockquote><p>比如原来所有表都在一个库：</p><pre><code>db_order</code></pre><p>拆成：</p><pre><code>db_order_0<br/>db_order_1<br/>db_order_2</code></pre><p>每个库可以部署在不同机器上。</p><p><strong>分库主要解决</strong></p><ul><li>单库连接数压力大。</li><li>单机 CPU / 内存 / IO 瓶颈。</li><li>写入压力太高。</li><li>存储容量不够。</li></ul><ol><li><strong>分库分表区别</strong></li></ol><sheet></sheet><ol><li><strong>垂直拆分和水平拆分</strong></li></ol><p>也可以补充这个，面试加分。</p><p><strong>垂直拆分</strong></p><ul><li>按业务或字段拆。</li><li>比如用户基础信息表、用户扩展信息表。</li><li>适合字段太多、冷热字段明显的场景。</li></ul><p><strong>水平拆分</strong></p><ul><li>按数据行拆。</li><li>比如按 <code>user_id</code> 取模，把不同用户的数据放到不同表/库。</li><li>适合数据量特别大的场景。</li></ul><ol><li><strong>分库分表带来的问题</strong></li></ol><ul><li>分布式事务变复杂。</li><li>跨库 JOIN 困难。</li><li>全局唯一 ID 需要单独设计。</li><li>分页、排序、聚合变复杂。</li><li>扩容迁移成本高。</li><li>SQL 路由和分片规则需要维护。</li></ul><p><strong>面试背诵版</strong></p><blockquote><p>分表是把一张大表拆成多张小表，主要解决单表数据量过大、索引膨胀、查询变慢的问题；分库是把数据拆到多个数据库实例中，主要解决单库连接数、CPU、IO、存储和写入压力问题。分表不一定跨机器，分库通常可以把压力分散到多台机器上。常见拆分方式有垂直拆分和水平拆分。分库分表能提升扩展性，但也会带来分布式事务、跨库 JOIN、全局 ID、分页排序和扩容迁移等复杂问题。</p></blockquote>"}]}]};
