window.TAB_DATA = window.TAB_DATA || {};
window.TAB_DATA["java"] = {"key":"java","title":"2小时速通Java","url":"https://utxc8uqzfk.feishu.cn/wiki/MWlXwd9F1i08xOkYrAhcDDdhn9c","chapters":[{"no":"一","title":"Java 基础","questions":[{"t":"Java 有哪些基本数据类型？包装类型有什么区别？","tag":"高频","p":"high","html":"<span>[原文档此处有配图]</span><p>核心回答：Java 有 8 种基本数据类型，分别是 <code>byte</code>、<code>short</code>、<code>int</code>、<code>long</code>、<code>float</code>、<code>double</code>、<code>char</code>、<code>boolean</code>；包装类型是它们对应的对象类型，比如 <code>int</code> 对应 <code>Integer</code>，主要用于泛型、集合、对象场景。</p><ol><li><strong>Java 8 种基本数据类型</strong></li></ol><p>Java 基本数据类型可以分为 4 类：</p><pre><code>整数类型：byte、short、int、long<br/>浮点类型：float、double<br/>字符类型：char<br/>布尔类型：boolean</code></pre><p>常见大小：</p><p>常见大小：</p><sheet></sheet><p>注意：<code>boolean</code> 的具体占用大小 JVM 规范没有强制固定。</p><ol><li><strong>包装类型是什么</strong></li></ol><p>每个基本类型都有对应包装类：</p><sheet></sheet><p>包装类型本质是对象，可以为 <code>null</code>，也可以调用方法。</p><p>比如：</p><pre><code>Integer num = 10;<br/>String s = num.toString();</code></pre><ol><li><strong>基本类型和包装类型区别</strong></li></ol><sheet></sheet><p>比如集合不能写：</p><pre><code>List&lt;int&gt; list; // 错误</code></pre><p>要写：</p><pre><code>List&lt;Integer&gt; list;</code></pre><p>因为 Java 泛型只能使用引用类型。</p><ol><li><strong>自动装箱和拆箱</strong></li></ol><p>Java 会自动在基本类型和包装类型之间转换。</p><p>装箱：</p><pre><code>Integer a = 10; // int -&gt; Integer</code></pre><p>拆箱：</p><pre><code>int b = a; // Integer -&gt; int</code></pre><p>但要注意空指针：</p><pre><code>Integer x = null;<br/>int y = x; // NullPointerException</code></pre><p>因为拆箱时要把 <code>null</code> 转成 <code>int</code>，会报错。</p><ol><li><strong>Integer 缓存问题</strong></li></ol><p>面试经常问：</p><pre><code>Integer a = 127;<br/>Integer b = 127;<br/>System.out.println(a == b); // true<br/><br/>Integer c = 128;<br/>Integer d = 128;<br/>System.out.println(c == d); // false</code></pre><p>原因是 <code>Integer</code> 默认缓存了：</p><pre><code>-128 到 127</code></pre><p>这个范围内的对象会复用。</p><p>所以包装类型比较值时，不要用 <code>==</code>，要用：</p><pre><code>a.equals(b)</code></pre><p><code>==</code> 比较的是对象引用，<code>equals</code> 比较的是值。</p><ol><li><strong>实际开发怎么选</strong></li></ol><p>一般建议：</p><pre><code>局部变量、计算密集场景：优先基本类型<br/>集合、泛型、可能为空的字段：使用包装类型<br/>数据库实体字段：常用包装类型</code></pre><p>比如数据库中年龄字段可能为 <code>NULL</code>：</p><pre><code>private Integer age;</code></pre><p>比：</p><pre><code>private int age;</code></pre><p>更能表达“可能没有值”。</p><p><strong>面试背诵版</strong></p><p>问题：Java 有哪些基本数据类型？包装类型有什么区别？</p><blockquote><p>Java 有 8 种基本数据类型，分别是 <code>byte</code>、<code>short</code>、<code>int</code>、<code>long</code>、<code>float</code>、<code>double</code>、<code>char</code> 和 <code>boolean</code>。</p><p>其中 <code>byte</code>、<code>short</code>、<code>int</code>、<code>long</code> 是整数类型，<code>float</code>、<code>double</code> 是浮点类型，<code>char</code> 是字符类型，<code>boolean</code> 是布尔类型。</p><p>每个基本类型都有对应的包装类型，比如 <code>int</code> 对应 <code>Integer</code>，<code>long</code> 对应 <code>Long</code>，<code>char</code> 对应 <code>Character</code>。</p><p>基本类型存的是具体值，不能为 <code>null</code>，性能更高；包装类型是对象，可以为 <code>null</code>，可以用于泛型、集合和需要对象的场景。</p><p><strong>Java 支持自动装箱和拆箱</strong>，但拆箱时如果包装对象是 <code>null</code>，会触发 <code>NullPointerException</code>。另外，包装类型比较值时不要用 <code>==</code>，因为 <code>==</code> 比较的是对象引用，应该使用 <code>equals</code>。</p><p>面试中可以这样收口：</p><p>简单说，<strong>基本类型轻量、不能为 </strong><strong><code>null</code></strong><strong>、适合计算；包装类型是对象、可以为 </strong><strong><code>null</code></strong><strong>、适合集合、泛型和数据库字段。</strong></p></blockquote>"},{"t":"== 和 equals() 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>==</code> 和 <code>equals()</code> 的核心区别是：<code>==</code> 比较的是“变量里存的值”，而 <code>equals()</code> 比较的是“对象内容是否相等”；对于引用类型来说，<code>==</code> 默认比较地址，<code>equals()</code> 通常可以被重写成比较内容。</p><ol><li><code>==</code> 比较什么？</li></ol><p><code>==</code> 是 Java 的运算符。</p><p>如果比较的是基本数据类型：</p><pre><code>int a = 10;<br/>int b = 10;<br/>System.out.println(a == b); // true</code></pre><p>这里比较的是数值是否相等。</p><p>基本类型包括：</p><pre><code>byte<br/>short<br/>int<br/>long<br/>float<br/>double<br/>char<br/>boolean</code></pre><p>所以基本类型用 <code>==</code> 没问题。</p><ol><li>引用类型用 <code>==</code> 比较什么？</li></ol><p>如果比较的是引用类型，<code>==</code> 比较的是两个引用是否指向同一个对象。</p><pre><code>String a = new String(\"hello\");<br/>String b = new String(\"hello\");<br/><br/>System.out.println(a == b); // false</code></pre><p>虽然内容都是 <code>\"hello\"</code>，但它们是两个不同对象。</p><p>可以理解为：</p><pre><code>a ───&gt; String 对象 1：\"hello\"<br/>b ───&gt; String 对象 2：\"hello\"</code></pre><p>所以 <code>a == b</code> 是 <code>false</code>。</p><ol><li><code>equals()</code> 比较什么？</li></ol><p><code>equals()</code> 是 <code>Object</code> 类里的方法。</p><p>默认情况下，<code>Object</code> 的 <code>equals()</code> 本质上也是比较地址。</p><p>源码逻辑类似：</p><pre><code>public boolean equals(Object obj) {<br/>    return this == obj;<br/>}</code></pre><p>也就是说，如果一个类没有重写 <code>equals()</code>，那它和 <code>==</code> 差不多，都是比较是不是同一个对象。</p><ol><li>为什么 String 的 equals 比较内容？</li></ol><p>因为 <code>String</code> 重写了 <code>equals()</code> 方法。</p><pre><code>String a = new String(\"hello\");<br/>String b = new String(\"hello\");<br/><br/>System.out.println(a.equals(b)); // true</code></pre><p>这里比较的是字符串内容是否相同，而不是对象地址。</p><p>很多类也重写了 <code>equals()</code>：</p><pre><code>String<br/>Integer<br/>Long<br/>BigDecimal<br/>Date<br/>自定义实体类</code></pre><ol><li>自定义对象要不要重写 equals？</li></ol><p>如果你希望两个对象“属性一样就认为相等”，就要重写 <code>equals()</code>。</p><p>比如：</p><pre><code>class User {<br/>    private Long id;<br/>    private String name;<br/>}</code></pre><p>如果不重写：</p><pre><code>User u1 = new User(1L, \"Tom\");<br/>User u2 = new User(1L, \"Tom\");<br/><br/>System.out.println(u1.equals(u2)); // false</code></pre><p>因为默认比较地址。</p><p>如果重写 <code>equals()</code>，可以按 <code>id</code> 或字段内容比较。</p><ol><li>重写 equals 要同时重写 hashCode</li></ol><p>这是面试高频追问。</p><p>如果重写了 <code>equals()</code>，通常必须重写 <code>hashCode()</code>。</p><p>原因是 HashMap、HashSet 这类集合依赖：</p><pre><code>hashCode 定位桶<br/>equals 判断是否真相等</code></pre><p>要求是：</p><pre><code>如果两个对象 equals 为 true，那么它们的 hashCode 必须相等</code></pre><p>否则放进 <code>HashSet</code> 或作为 <code>HashMap</code> 的 key 时可能出问题。</p><ol><li>String 的特殊情况</li></ol><p>有时候你会看到：</p><pre><code>String a = \"hello\";<br/>String b = \"hello\";<br/><br/>System.out.println(a == b); // true</code></pre><p>这是因为字符串常量池。</p><p>两个字面量 <code>\"hello\"</code> 可能指向常量池里的同一个对象。</p><p>但如果写成：</p><pre><code>String a = new String(\"hello\");<br/>String b = new String(\"hello\");<br/><br/>System.out.println(a == b);      // false<br/>System.out.println(a.equals(b)); // true</code></pre><p>所以比较字符串内容，永远用 <code>equals()</code>，不要用 <code>==</code>。</p><ol><li>避免空指针的小技巧</li></ol><p>如果这样写：</p><pre><code>str.equals(\"hello\")</code></pre><p>当 <code>str</code> 是 <code>null</code> 时会报 <code>NullPointerException</code>。</p><p>更推荐：</p><pre><code>\"hello\".equals(str)</code></pre><p>或者：</p><pre><code>Objects.equals(str1, str2)</code></pre><p><code>Objects.equals()</code> 可以安全处理 null：</p><pre><code>Objects.equals(null, \"hello\"); // false<br/>Objects.equals(null, null);    // true</code></pre><ol><li>总结对比</li></ol><pre><code>==：<br/>基本类型比较值<br/>引用类型比较地址<br/><br/>equals()：<br/>默认比较地址<br/>如果被重写，通常比较对象内容<br/><br/>String：<br/>== 比较地址<br/>equals 比较字符串内容<br/><br/>自定义对象：<br/>要比较内容，需要重写 equals 和 hashCode</code></pre><ol><li><strong>面试背诵版：== 和 equals() 有什么区别？</strong></li></ol><p><code>==</code> 和 <code>equals()</code> 的区别要分基本类型和引用类型来看。对于基本数据类型，<code>==</code> 比较的是值是否相等，比如两个 <code>int</code> 是否相等；对于引用类型，<code>==</code> 比较的是两个引用是否指向同一个对象，也就是对象地址是否相同。<code>equals()</code> 是 <code>Object</code> 类的方法，默认实现其实也是比较地址，和 <code>==</code> 类似；但是很多类会重写 <code>equals()</code>，比如 <code>String</code>，重写后比较的是对象内容是否相等。所以比较字符串内容时应该用 <code>equals()</code>，不能用 <code>==</code>，因为 <code>==</code> 只判断是不是同一个对象。对于自定义对象，如果希望按照字段内容判断相等，就需要重写 <code>equals()</code>，并且通常要同时重写 <code>hashCode()</code>，否则在 <code>HashMap</code>、<code>HashSet</code> 这类集合中可能出现问题。</p>"},{"t":"为什么重写 equals() 必须重写 hashCode()？","tag":"高频","p":"high","html":""},{"t":"String 为什么是不可变的？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>String</code> 不可变的核心原因是：为了安全、缓存、线程安全和性能优化。字符串一旦创建，内容不能被修改，所有“修改”都会生成新的字符串对象。</p><ol><li>什么叫 String 不可变？</li></ol><p>比如：</p><pre><code>String s = \"abc\";<br/>s = s + \"d\";</code></pre><p>看起来像是把 <code>\"abc\"</code> 改成了 <code>\"abcd\"</code>。</p><p>但实际上不是修改原来的 <code>\"abc\"</code>，而是创建了一个新的字符串 <code>\"abcd\"</code>，然后让 <code>s</code> 指向新对象。</p><pre><code>原来：<br/>s → \"abc\"<br/><br/>后来：<br/>s → \"abcd\"</code></pre><p>原来的 <code>\"abc\"</code> 没有被改。</p><ol><li>String 为什么设计成不可变？</li></ol><p>主要有几个原因：</p><pre><code>安全性<br/>字符串常量池<br/>线程安全<br/>hash 缓存<br/>作为 HashMap key 更可靠</code></pre><p>面试重点说这几个就够了。</p><ol><li>原因一：安全性</li></ol><p>字符串经常用来表示敏感或关键数据，比如：</p><pre><code>文件路径<br/>数据库连接地址<br/>用户名<br/>密码<br/>网络 URL<br/>类加载路径<br/>权限信息</code></pre><p>如果 <code>String</code> 是可变的，某个地方拿到字符串引用后，就可能偷偷修改内容。</p><p>比如：</p><pre><code>String path = \"/safe/file\";<br/>checkPermission(path);<br/><br/>// 如果 String 可变，检查后 path 被改成 \"/etc/passwd\"<br/>readFile(path);</code></pre><p>这样就会产生安全问题。</p><p>不可变可以保证：检查过的字符串不会被别人改掉。</p><ol><li>原因二：支持字符串常量池</li></ol><p>Java 有字符串常量池。</p><pre><code>String a = \"hello\";<br/>String b = \"hello\";</code></pre><p><code>a</code> 和 <code>b</code> 可以共享同一个 <code>\"hello\"</code> 对象。</p><pre><code>a ─┐<br/>   ├──&gt; \"hello\"<br/>b ─┘</code></pre><p>如果 <code>String</code> 可变，那么 <code>a</code> 修改了内容，<code>b</code> 也会受影响。</p><p>这会非常危险。</p><p>所以为了让字符串常量池安全共享，<code>String</code> 必须不可变。</p><ol><li>原因三：线程安全</li></ol><p>因为 <code>String</code> 不可变，所以多个线程可以安全共享同一个字符串对象。</p><pre><code>String s = \"hello\";</code></pre><p>多个线程同时读 <code>s</code>，不用加锁。</p><p>因为没有线程能修改它的内容。</p><p>如果字符串可变，就要考虑并发修改、加锁、可见性等问题。</p><p>不可变天然线程安全。</p><ol><li>原因四：可以缓存 hashCode</li></ol><p><code>String</code> 经常作为 <code>HashMap</code> 的 key。</p><p>比如：</p><pre><code>Map&lt;String, Object&gt; map = new HashMap&lt;&gt;();<br/>map.put(\"userId\", 123);</code></pre><p>HashMap 查找时依赖 <code>hashCode()</code>。</p><p>如果字符串内容会变，那么 hashCode 也会变，HashMap 可能找不到原来的 key。</p><p>不可变保证 hashCode 稳定。</p><p>而且 <code>String</code> 可以缓存 hash 值，提高多次查找性能。</p><ol><li>原因五：性能优化</li></ol><p>不可变带来很多优化空间：</p><pre><code>字符串常量池复用<br/>hashCode 缓存<br/>多线程共享无需加锁<br/>减少防御性拷贝</code></pre><p>比如很多 API 接收字符串参数时，不需要担心被外部修改。</p><p>这减少了复制成本。</p><ol><li>String 真的完全不能变吗？</li></ol><p>正常 Java 代码不能修改 <code>String</code> 内容。</p><p>但通过反射、Unsafe 等非常规手段，理论上可以破坏它。</p><p>但这不属于正常使用。</p><p>从 Java 语言设计和普通开发角度看，<code>String</code> 是不可变类。</p><ol><li>String 不可变和 final 的关系</li></ol><p><code>String</code> 类本身是 <code>final</code> 的：</p><pre><code>public final class String</code></pre><p>这表示不能被继承，防止子类破坏不可变语义。</p><p>同时内部保存字符数据的字段也是私有的，并且不会对外提供修改方法。</p><p>早期是：</p><pre><code>private final char[] value;</code></pre><p>后面版本改成了：</p><pre><code>private final byte[] value;</code></pre><p>但核心思想一样：内部数据不允许被外部修改。</p><ol><li>面试常见追问：频繁拼接怎么办？</li></ol><p>因为 <code>String</code> 不可变，频繁拼接会创建很多临时对象。</p><p>比如：</p><pre><code>String s = \"\";<br/>for (int i = 0; i &lt; 1000; i++) {<br/>    s += i;<br/>}</code></pre><p>这种场景更推荐：</p><pre><code>StringBuilder sb = new StringBuilder();<br/>for (int i = 0; i &lt; 1000; i++) {<br/>    sb.append(i);<br/>}<br/>String s = sb.toString();</code></pre><p>如果多线程共享拼接，可以考虑 <code>StringBuffer</code>，但一般优先 <code>StringBuilder</code>。</p><ol><li><strong>面试背诵版：String 为什么是不可变的？</strong></li></ol><p>Java 中 <code>String</code> 被设计成不可变，主要是为了安全性、字符串常量池、线程安全和性能优化。首先，字符串经常用于文件路径、URL、数据库连接、类加载路径和权限校验等场景，如果字符串可变，可能出现检查前后内容被篡改的安全问题。其次，Java 有字符串常量池，多个变量可以共享同一个字符串对象，如果 <code>String</code> 可变，一个引用修改内容会影响其他引用，所以必须不可变才能安全复用。第三，<code>String</code> 不可变后天然线程安全，多个线程可以共享同一个字符串对象，不需要加锁。第四，<code>String</code> 经常作为 <code>HashMap</code> 的 key，不可变可以保证 <code>hashCode</code> 稳定，并且可以缓存 hash 值，提高查询性能。实现上，<code>String</code> 类是 <code>final</code> 的，不能被继承，内部字符数据是私有的，也不提供修改内容的方法。需要注意，因为 <code>String</code> 不可变，频繁字符串拼接会产生很多临时对象，循环拼接时一般使用 <code>StringBuilder</code>。</p>"},{"t":"String、StringBuilder、StringBuffer 有什么区别？","tag":"高频","p":"high","html":""},{"t":"final 关键字有什么作用？","tag":"高频","p":"high","html":""},{"t":"static 关键字有什么作用？","tag":"高频","p":"high","html":""},{"t":"Java 是值传递还是引用传递？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>Java 只有值传递，没有引用传递。核心是：方法参数传递时，传过去的是“变量值的副本”；如果变量是对象引用，那传过去的是“引用地址的副本”，不是引用本身。</p><ol><li>先说结论</li></ol><p>Java 方法参数传递只有一种：</p><pre><code>值传递</code></pre><p>没有真正意义上的引用传递。</p><p>但是要注意：</p><pre><code>基本类型传的是值的副本<br/>引用类型传的是引用地址的副本</code></pre><p>所以引用类型传参时，方法内部可以通过这个引用副本修改对象内容，但不能让外部引用指向新对象。</p><ol><li>基本类型例子</li></ol><pre><code>public static void change(int x) {<br/>    x = 100;<br/>}<br/><br/>public static void main(String[] args) {<br/>    int a = 10;<br/>    change(a);<br/>    System.out.println(a); // 10<br/>}</code></pre><p>为什么还是 <code>10</code>？</p><p>因为传给 <code>change</code> 的是 <code>a</code> 的值副本。</p><pre><code>main 中 a = 10<br/>change 中 x = 10 的副本<br/>x 改成 100<br/>a 不受影响</code></pre><ol><li>引用类型例子：修改对象内容</li></ol><pre><code>class User {<br/>    String name;<br/>}<br/><br/>public static void change(User u) {<br/>    u.name = \"Bob\";<br/>}<br/><br/>public static void main(String[] args) {<br/>    User user = new User();<br/>    user.name = \"Tom\";<br/><br/>    change(user);<br/><br/>    System.out.println(user.name); // Bob<br/>}</code></pre><p>这里为什么外部变了？</p><p>因为 <code>user</code> 和 <code>u</code> 都指向同一个对象。</p><pre><code>user ─┐<br/>      ├──&gt; User 对象<br/>u    ─┘</code></pre><p>虽然 <code>u</code> 是引用副本，但它指向同一个对象，所以可以修改对象内容。</p><ol><li>引用类型例子：重新赋值引用</li></ol><pre><code>class User {<br/>    String name;<br/>}<br/><br/>public static void change(User u) {<br/>    u = new User();<br/>    u.name = \"Bob\";<br/>}<br/><br/>public static void main(String[] args) {<br/>    User user = new User();<br/>    user.name = \"Tom\";<br/><br/>    change(user);<br/><br/>    System.out.println(user.name); // Tom<br/>}</code></pre><p>为什么还是 <code>Tom</code>？</p><p>因为 <code>u = new User()</code> 只是让方法内部的引用副本 <code>u</code> 指向新对象。</p><p>不会影响外部的 <code>user</code>。</p><pre><code>调用前：<br/>user ─┐<br/>      ├──&gt; User(\"Tom\")<br/>u    ─┘<br/><br/>u 重新赋值后：<br/>user ───&gt; User(\"Tom\")<br/>u    ───&gt; User(\"Bob\")</code></pre><p>所以外部 <code>user</code> 仍然指向原对象。</p><ol><li>这就是为什么容易误解</li></ol><p>很多人说 Java 是引用传递，是因为引用类型参数可以修改对象内容。</p><p>但这不是真正的引用传递。</p><p>真正的引用传递应该能在方法里改变外部变量本身的指向。</p><p>但 Java 做不到。</p><p>比如：</p><pre><code>public static void swap(User a, User b) {<br/>    User temp = a;<br/>    a = b;<br/>    b = temp;<br/>}</code></pre><p>这个方法不能交换外部两个引用。</p><p>因为交换的只是局部引用副本。</p><ol><li>swap 为什么无效？</li></ol><pre><code>User u1 = new User(\"A\");<br/>User u2 = new User(\"B\");<br/><br/>swap(u1, u2);</code></pre><p>调用时：</p><pre><code>u1 ───&gt; A<br/>u2 ───&gt; B<br/><br/>方法内：<br/>a ───&gt; A<br/>b ───&gt; B</code></pre><p><code>swap</code> 里交换的是 <code>a</code> 和 <code>b</code>：</p><pre><code>a ───&gt; B<br/>b ───&gt; A</code></pre><p>但外部仍然是：</p><pre><code>u1 ───&gt; A<br/>u2 ───&gt; B</code></pre><p>所以 Java 不是引用传递。</p><ol><li>一句话区分</li></ol><pre><code>值传递：<br/>传变量值的副本，方法不能改变外部变量本身。<br/><br/>引用传递：<br/>传变量引用本身，方法可以改变外部变量指向。</code></pre><p>Java 的引用类型传参只是：</p><pre><code>把对象地址这个值复制了一份</code></pre><p>所以本质仍然是值传递。</p><ol><li>面试常见说法</li></ol><p>可以这样说：</p><pre><code>Java 参数传递是值传递。<br/>对于基本类型，传的是具体数值的副本。<br/>对于引用类型，传的是对象引用地址的副本。<br/>所以方法内部可以通过引用副本修改对象内容，<br/>但不能改变外部引用变量指向哪个对象。</code></pre><p>这个说法最稳。</p><ol><li><strong>面试背诵版：Java 是值传递还是引用传递？</strong></li></ol><p>Java 只有值传递，没有引用传递。对于基本数据类型，方法传参时传递的是值的副本，所以方法内部修改参数不会影响外部变量。对于引用类型，传递的也是值的副本，只不过这个值是对象引用地址的副本。因此，方法内部的参数引用和外部引用可以指向同一个对象，所以可以通过这个引用副本修改对象内部的属性；但如果在方法内部让参数引用指向一个新对象，只是改变了局部引用副本的指向，不会影响外部引用变量。比如方法里修改 <code>user.name</code>，外部能看到变化；但方法里执行 <code>user = new User()</code>，外部引用不会变。所以 Java 本质上是值传递，引用类型只是传递了引用值的副本。</p>"},{"t":"Object 类有哪些常用方法？","tag":"高频","p":"high","html":""},{"t":"Java 创建对象有哪些方式？","tag":"了解即可","p":"know","html":""}]},{"no":"二","title":"Java 面向对象","questions":[{"t":"面向对象三大特性是什么？","tag":"必须掌握","p":"core","html":"<span>[原文档此处有配图]</span><p>面向对象三大特性的核心是：<strong>封装、继承、多态</strong>。它们分别解决“数据如何保护和组织”“代码如何复用和扩展”“同一行为如何表现出不同实现”。</p><ol><li>封装</li></ol><p>封装就是把对象的属性和行为放在一起，并通过访问控制隐藏内部实现细节，只暴露必要接口。</p><p>比如：</p><pre><code>class User {<br/>    private String name;<br/><br/>    public String getName() {<br/>        return name;<br/>    }<br/><br/>    public void changeName(String name) {<br/>        if (name == null || name.isEmpty()) {<br/>            throw new IllegalArgumentException(\"name invalid\");<br/>        }<br/>        this.name = name;<br/>    }<br/>}</code></pre><p>这里 <code>name</code> 是 <code>private</code>，外部不能随便改，只能通过方法访问。</p><p>封装的好处：</p><pre><code>保护数据安全<br/>隐藏实现细节<br/>降低耦合<br/>方便修改内部实现<br/>对外提供稳定接口</code></pre><p>面试可以说：封装不是简单写 getter/setter，而是要把校验、约束和业务规则封装在对象内部。</p><ol><li>继承</li></ol><p>继承就是子类复用父类的属性和方法，并且可以扩展或重写父类行为。</p><p>比如：</p><pre><code>class Animal {<br/>    public void eat() {<br/>        System.out.println(\"eat\");<br/>    }<br/>}<br/><br/>class Dog extends Animal {<br/>    public void bark() {<br/>        System.out.println(\"bark\");<br/>    }<br/>}</code></pre><p><code>Dog</code> 继承了 <code>Animal</code> 的 <code>eat()</code> 方法。</p><p>继承的好处：</p><pre><code>代码复用<br/>表达 is-a 关系<br/>方便扩展<br/>支持多态</code></pre><p>但继承也不能滥用。</p><p>如果只是复用代码，不一定非要继承，也可以用组合。</p><p>常见原则：</p><pre><code>多用组合，少用继承<br/>继承用于明确的 is-a 关系<br/>组合用于 has-a 关系</code></pre><ol><li>多态</li></ol><p>多态就是同一个接口或父类引用，在运行时可以指向不同子类对象，并执行不同实现。</p><p>比如：</p><pre><code>class Animal {<br/>    public void speak() {<br/>        System.out.println(\"animal\");<br/>    }<br/>}<br/><br/>class Dog extends Animal {<br/>    @Override<br/>    public void speak() {<br/>        System.out.println(\"wang\");<br/>    }<br/>}<br/><br/>class Cat extends Animal {<br/>    @Override<br/>    public void speak() {<br/>        System.out.println(\"miao\");<br/>    }<br/>}</code></pre><p>使用：</p><pre><code>Animal a1 = new Dog();<br/>Animal a2 = new Cat();<br/><br/>a1.speak(); // wang<br/>a2.speak(); // miao</code></pre><p>同样是 <code>Animal.speak()</code>，运行时表现不同，这就是多态。</p><ol><li>多态的实现条件</li></ol><p>Java 中多态通常需要：</p><pre><code>继承或实现接口<br/>方法重写<br/>父类引用指向子类对象</code></pre><p>比如：</p><pre><code>List&lt;String&gt; list = new ArrayList&lt;&gt;();</code></pre><p>这里 <code>List</code> 是接口，<code>ArrayList</code> 是具体实现。</p><p>以后也可以换成：</p><pre><code>List&lt;String&gt; list = new LinkedList&lt;&gt;();</code></pre><p>调用方只依赖接口，不依赖具体实现。</p><ol><li>多态的好处</li></ol><p>多态最大的价值是解耦和扩展。</p><p>比如支付系统：</p><pre><code>interface PayService {<br/>    void pay();<br/>}<br/><br/>class AliPay implements PayService {<br/>    public void pay() {}<br/>}<br/><br/>class WeChatPay implements PayService {<br/>    public void pay() {}<br/>}</code></pre><p>业务代码只依赖：</p><pre><code>PayService payService;<br/>payService.pay();</code></pre><p>新增一种支付方式时，只需要新增实现类，不用大改原有业务逻辑。</p><p>这符合开闭原则：</p><pre><code>对扩展开放，对修改关闭</code></pre><ol><li>三者之间的关系</li></ol><pre><code>封装：<br/>把数据和行为组织到对象里，隐藏内部细节。<br/><br/>继承：<br/>让子类复用和扩展父类能力。<br/><br/>多态：<br/>让不同子类通过统一接口表现出不同行为。</code></pre><p>可以这样理解：</p><pre><code>封装解决对象内部怎么管理<br/>继承解决类之间怎么复用<br/>多态解决调用方怎么面向抽象编程</code></pre><ol><li>面试容易加分的点</li></ol><p>可以补充：</p><pre><code>封装体现的是高内聚、低耦合<br/>继承要谨慎使用，优先组合<br/>多态是面向接口编程和设计模式的基础</code></pre><p>比如策略模式、工厂模式、模板方法模式都大量依赖多态。</p><ol><li><strong>面试背诵版：面向对象三大特性是什么？</strong></li></ol><p>面向对象三大特性是封装、继承和多态。封装是把对象的属性和行为放在一起，通过访问控制隐藏内部实现细节，只暴露必要接口，目的是保护数据、降低耦合、提高可维护性。继承是子类复用父类的属性和方法，并在此基础上扩展或重写行为，适合表达明确的 <code>is-a</code> 关系，但实际开发中要避免滥用继承，很多场景更推荐组合。多态是指同一个父类或接口引用，在运行时可以指向不同子类对象，并执行不同实现。Java 中多态通常依赖继承或接口、方法重写和父类引用指向子类对象。多态的价值是让调用方依赖抽象而不是具体实现，从而降低耦合、提高扩展性，比如新增一种支付方式时，只需要新增实现类，不需要修改原有调用逻辑。简单来说，封装解决对象内部怎么组织，继承解决代码怎么复用，多态解决系统怎么扩展。</p>"},{"t":"Java 多态是如何实现的？","tag":"高频","p":"high","html":""},{"t":"重载和重写有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>重载和重写的核心区别是：<strong>重载发生在同一个类中，方法名相同但参数不同；重写发生在父子类之间，子类重新实现父类的方法。</strong></p><ol><li>重载是什么？</li></ol><p>重载叫 Overload。</p><p>它指的是：同一个类中，多个方法方法名相同，但参数列表不同。</p><p>比如：</p><pre><code>class Calculator {<br/>    public int add(int a, int b) {<br/>        return a + b;<br/>    }<br/><br/>    public double add(double a, double b) {<br/>        return a + b;<br/>    }<br/><br/>    public int add(int a, int b, int c) {<br/>        return a + b + c;<br/>    }<br/>}</code></pre><p>这些方法都叫 <code>add</code>，但参数不同，所以是重载。</p><ol><li>重载看什么？</li></ol><p>重载主要看参数列表。</p><p>参数列表不同包括：</p><pre><code>参数个数不同<br/>参数类型不同<br/>参数顺序不同</code></pre><p>比如：</p><pre><code>void test(int a, String b) {}<br/>void test(String a, int b) {}</code></pre><p>这也是重载，因为参数顺序不同。</p><ol><li>重载不能只靠返回值区分</li></ol><p>下面这样不算重载，会编译报错：</p><pre><code>int test() {<br/>    return 1;<br/>}<br/><br/>String test() {<br/>    return \"hello\";<br/>}</code></pre><p>因为方法调用时：</p><pre><code>test();</code></pre><p>编译器不知道你想调用哪个。</p><p>所以 Java 中方法重载不能只靠返回值不同。</p><ol><li>重写是什么？</li></ol><p>重写叫 Override。</p><p>它指的是：子类对父类已有方法进行重新实现。</p><p>比如：</p><pre><code>class Animal {<br/>    public void speak() {<br/>        System.out.println(\"animal\");<br/>    }<br/>}<br/><br/>class Dog extends Animal {<br/>    @Override<br/>    public void speak() {<br/>        System.out.println(\"wang\");<br/>    }<br/>}</code></pre><p>这里 <code>Dog</code> 重写了 <code>Animal</code> 的 <code>speak()</code> 方法。</p><ol><li>重写看什么？</li></ol><p>重写要求：</p><pre><code>发生在父子类之间<br/>方法名相同<br/>参数列表相同<br/>返回值类型相同或是父类返回值的子类型<br/>访问权限不能更严格<br/>不能抛出更宽泛的检查异常</code></pre><p>比如父类方法是 <code>public</code>，子类不能重写成 <code>private</code>。</p><ol><li>重载是编译期，重写是运行期</li></ol><p>这是面试重点。</p><p>重载属于编译期多态。</p><p>编译器根据参数类型决定调用哪个方法。</p><pre><code>add(1, 2);       // 调 int 版本<br/>add(1.0, 2.0);   // 调 double 版本</code></pre><p>重写属于运行期多态。</p><p>运行时根据真实对象类型决定调用哪个方法。</p><pre><code>Animal animal = new Dog();<br/>animal.speak(); // 调 Dog 的 speak()</code></pre><p>虽然变量类型是 <code>Animal</code>，但实际对象是 <code>Dog</code>，所以运行时调用 <code>Dog</code> 的方法。</p><ol><li>构造方法能不能重载和重写？</li></ol><p>构造方法可以重载。</p><pre><code>class User {<br/>    public User() {}<br/><br/>    public User(String name) {}<br/>}</code></pre><p>但构造方法不能重写。</p><p>因为构造方法不被继承，也没有返回值类型。</p><ol><li>static 方法能不能重写？</li></ol><p>严格来说，<code>static</code> 方法不能被重写，只能被隐藏。</p><p>比如：</p><pre><code>class Parent {<br/>    static void test() {}<br/>}<br/><br/>class Child extends Parent {<br/>    static void test() {}<br/>}</code></pre><p>这不是运行期多态。</p><p>调用哪个方法取决于引用类型，而不是实际对象类型。</p><ol><li>总结对比</li></ol><pre><code>重载 Overload：<br/>同一个类中<br/>方法名相同<br/>参数列表不同<br/>返回值不能作为唯一依据<br/>编译期确定<br/>属于编译期多态<br/><br/>重写 Override：<br/>父子类之间<br/>方法名和参数列表相同<br/>子类重新实现父类方法<br/>运行期确定<br/>属于运行期多态</code></pre><ol><li><strong>面试背诵版：重载和重写有什么区别？</strong></li></ol><p>重载和重写的区别主要在发生位置、方法签名和绑定时机。重载是 Overload，发生在同一个类中，多个方法方法名相同，但参数列表不同，比如参数个数、类型或顺序不同；重载不能只通过返回值类型不同来区分，因为调用时编译器无法判断。重载属于编译期多态，编译器根据实参类型决定调用哪个方法。重写是 Override，发生在父子类之间，子类对父类已有方法进行重新实现，要求方法名和参数列表相同，返回值类型相同或是父类返回值的子类型，访问权限不能比父类更严格，不能抛出更宽泛的检查异常。重写属于运行期多态，运行时根据对象的真实类型决定调用哪个方法。简单来说，重载是同名不同参，重写是子类改父类实现。</p>"},{"t":"抽象类和接口有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>抽象类和接口的核心区别是：抽象类更强调“是什么”，用于抽取一类对象的公共属性和行为；接口更强调“能做什么”，用于定义能力规范和扩展点。</p><ol><li>抽象类是什么？</li></ol><p>抽象类用 <code>abstract class</code> 定义。</p><p>它不能直接创建对象，主要用于被子类继承。</p><pre><code>abstract class Animal {<br/>    protected String name;<br/><br/>    public void eat() {<br/>        System.out.println(\"eat\");<br/>    }<br/><br/>    public abstract void speak();<br/>}</code></pre><p>它可以包含：</p><pre><code>成员变量<br/>普通方法<br/>抽象方法<br/>构造方法<br/>静态方法</code></pre><p>子类继承后，需要实现抽象方法：</p><pre><code>class Dog extends Animal {<br/>    @Override<br/>    public void speak() {<br/>        System.out.println(\"wang\");<br/>    }<br/>}</code></pre><ol><li>接口是什么？</li></ol><p>接口用 <code>interface</code> 定义。</p><p>它主要用来定义一组能力或规范。</p><pre><code>interface Flyable {<br/>    void fly();<br/>}</code></pre><p>实现类通过 <code>implements</code> 实现接口：</p><pre><code>class Bird implements Flyable {<br/>    @Override<br/>    public void fly() {<br/>        System.out.println(\"fly\");<br/>    }<br/>}</code></pre><p>接口更像是“契约”，规定实现类必须具备某种能力。</p><ol><li>语义上的区别</li></ol><p>这是面试最重要的点。</p><pre><code>抽象类：<br/>表示 is-a 关系，强调一类事物的公共模板。<br/><br/>接口：<br/>表示 can-do 关系，强调某种能力或行为规范。</code></pre><p>比如：</p><pre><code>Dog extends Animal：<br/>狗是一种动物。<br/><br/>Bird implements Flyable：<br/>鸟具备飞的能力。</code></pre><p>再比如：</p><pre><code>Car extends Vehicle：<br/>汽车是一种交通工具。<br/><br/>Car implements Runnable：<br/>汽车具备运行能力。</code></pre><ol><li>继承数量不同</li></ol><p>Java 中类只能单继承。</p><pre><code>class Dog extends Animal {<br/>}</code></pre><p>一个类只能继承一个抽象类。</p><p>但一个类可以实现多个接口：</p><pre><code>class Bird implements Flyable, Eatable, Runnable {<br/>}</code></pre><p>所以接口更适合扩展多个能力。</p><ol><li>成员变量区别</li></ol><p>抽象类中可以有普通成员变量：</p><pre><code>abstract class Animal {<br/>    protected String name;<br/>}</code></pre><p>接口中的变量默认是：</p><pre><code>public static final</code></pre><p>也就是常量。</p><pre><code>interface Config {<br/>    int MAX_SIZE = 100;<br/>}</code></pre><p>等价于：</p><pre><code>public static final int MAX_SIZE = 100;</code></pre><ol><li>方法区别</li></ol><p>抽象类可以有：</p><pre><code>抽象方法<br/>普通方法<br/>静态方法<br/>构造方法</code></pre><p>接口在 Java 8 之前主要是抽象方法。</p><p>Java 8 以后接口可以有：</p><pre><code>default 方法<br/>static 方法</code></pre><p>Java 9 以后还可以有：</p><pre><code>private 方法</code></pre><p>但接口的主要定位仍然是定义规范，不应该承载太多状态和复杂实现。</p><ol><li>构造方法区别</li></ol><p>抽象类可以有构造方法。</p><p>虽然抽象类不能直接 new，但子类创建对象时会调用父类构造方法。</p><pre><code>abstract class Animal {<br/>    public Animal() {<br/>        System.out.println(\"Animal init\");<br/>    }<br/>}</code></pre><p>接口没有构造方法。</p><p>因为接口不是对象模板，不保存实例状态。</p><ol><li>使用场景区别</li></ol><p>适合用抽象类的场景：</p><pre><code>多个类有共同属性<br/>多个类有共同默认行为<br/>需要复用代码<br/>存在明确的父子关系</code></pre><p>比如：</p><pre><code>Animal<br/>Vehicle<br/>BaseController<br/>AbstractService</code></pre><p>适合用接口的场景：</p><pre><code>定义能力规范<br/>面向接口编程<br/>需要多实现<br/>需要多继承能力<br/>系统扩展点</code></pre><p>比如：</p><pre><code>Runnable<br/>Comparable<br/>Serializable<br/>PayService<br/>MessageSender</code></pre><ol><li>实际开发中怎么选？</li></ol><p>可以这样判断：</p><pre><code>如果是“某类对象的公共模板”，用抽象类。<br/>如果是“某种能力或规范”，用接口。<br/>如果需要多实现、多扩展，优先接口。<br/>如果需要复用公共代码和状态，可以考虑抽象类。</code></pre><p>现代 Java 开发中，很多时候更推荐：</p><pre><code>接口定义规范<br/>抽象类提供部分公共实现<br/>具体类完成业务逻辑</code></pre><p>比如：</p><pre><code>interface PayService {<br/>    void pay();<br/>}<br/><br/>abstract class AbstractPayService implements PayService {<br/>    protected void checkAmount() {<br/>        // 公共校验<br/>    }<br/>}<br/><br/>class AliPayService extends AbstractPayService {<br/>    public void pay() {<br/>        checkAmount();<br/>        // 支付逻辑<br/>    }<br/>}</code></pre><ol><li>总结对比</li></ol><pre><code>抽象类：<br/>is-a 关系<br/>只能单继承<br/>可以有成员变量<br/>可以有构造方法<br/>可以有普通方法和抽象方法<br/>适合代码复用和公共模板<br/><br/>接口：<br/>can-do 关系<br/>可以多实现<br/>变量默认 public static final<br/>主要定义规范<br/>Java 8 后支持 default/static 方法<br/>适合能力抽象和系统扩展</code></pre><ol><li><strong>面试背诵版：抽象类和接口有什么区别？</strong></li></ol><p>抽象类和接口的区别可以从语义、继承关系、成员和使用场景来看。抽象类更强调 <code>is-a</code> 关系，表示一类对象的公共模板，比如 <code>Dog extends Animal</code>，适合抽取共同属性、共同方法和默认实现；接口更强调 <code>can-do</code> 能力，表示某个类具备某种行为规范，比如 <code>Bird implements Flyable</code>，适合定义能力、规范和扩展点。Java 中类只能继承一个抽象类，但可以实现多个接口，所以接口更灵活。抽象类可以有成员变量、普通方法、抽象方法、静态方法和构造方法；接口中的变量默认是 <code>public static final</code> 常量，方法主要用于定义规范，Java 8 以后接口也支持 <code>default</code> 和 <code>static</code> 方法。使用上，如果多个类有共同状态和默认行为，可以用抽象类；如果只是定义某种能力或需要面向接口编程、多实现扩展，优先用接口。实际开发中常见做法是接口定义规范，抽象类提供公共实现，具体类实现业务逻辑。</p>"},{"t":"为什么 Java 不支持多继承？","tag":"高频","p":"high","html":""},{"t":"接口中的 default 方法是什么？","tag":"了解即可","p":"know","html":""},{"t":"为什么推荐组合优于继承？","tag":"了解即可","p":"know","html":""}]},{"no":"三","title":"Java 集合","questions":[{"t":"Java 集合体系有哪些？","tag":"必须掌握","p":"core","html":"<span>[原文档此处有配图]</span><p>Java 集合体系的核心是：主要分为两大接口体系，<code>Collection</code> 存单个元素，<code>Map</code> 存键值对。</p><ol><li>总体结构</li></ol><p>Java 集合大体可以分成：</p><pre><code>Collection：单列集合，存一个一个元素<br/>Map：双列集合，存 key-value 键值对</code></pre><p>结构图：</p><pre><code>Java 集合<br/>├── Collection<br/>│   ├── List<br/>│   ├── Set<br/>│   └── Queue / Deque<br/>└── Map<br/>    ├── HashMap<br/>    ├── LinkedHashMap<br/>    ├── TreeMap<br/>    ├── Hashtable<br/>    └── ConcurrentHashMap</code></pre><ol><li>Collection 是什么？</li></ol><p><code>Collection</code> 是单列集合的顶层接口。</p><p>它下面主要有：</p><pre><code>List：有序、可重复<br/>Set：无序或有序、不可重复<br/>Queue：队列结构</code></pre><p>所谓单列，就是每个元素单独存：</p><pre><code>List&lt;String&gt; list = new ArrayList&lt;&gt;();<br/>list.add(\"A\");<br/>list.add(\"B\");</code></pre><ol><li>List 体系</li></ol><p><code>List</code> 的特点是：</p><pre><code>有序<br/>可重复<br/>可以通过下标访问</code></pre><p>常见实现类：</p><pre><code>ArrayList<br/>LinkedList<br/>Vector</code></pre><p><code>ArrayList</code>：</p><pre><code>底层是动态数组<br/>查询快<br/>尾部添加快<br/>中间插入删除慢<br/>线程不安全</code></pre><p><code>LinkedList</code>：</p><pre><code>底层是双向链表<br/>头尾插入删除方便<br/>随机访问慢<br/>也可以当队列使用</code></pre><p><code>Vector</code>：</p><pre><code>早期集合类<br/>线程安全<br/>方法加 synchronized<br/>性能较低<br/>现在较少使用</code></pre><ol><li>Set 体系</li></ol><p><code>Set</code> 的特点是：</p><pre><code>元素不重复<br/>通常不保证插入顺序</code></pre><p>常见实现类：</p><pre><code>HashSet<br/>LinkedHashSet<br/>TreeSet</code></pre><p><code>HashSet</code>：</p><pre><code>底层基于 HashMap<br/>无序<br/>查找、添加、删除平均 O(1)<br/>依赖 hashCode 和 equals 去重</code></pre><p><code>LinkedHashSet</code>：</p><pre><code>底层是 HashMap + 链表<br/>可以保持插入顺序</code></pre><p><code>TreeSet</code>：</p><pre><code>底层是红黑树<br/>元素有序<br/>添加、删除、查找 O(log n)<br/>需要元素可比较或传入 Comparator</code></pre><ol><li>Queue / Deque 体系</li></ol><p><code>Queue</code> 是队列，一般先进先出。</p><p>常见实现：</p><pre><code>LinkedList<br/>PriorityQueue<br/>ArrayDeque</code></pre><p><code>PriorityQueue</code>：</p><pre><code>优先队列<br/>底层是堆<br/>不是按插入顺序出队<br/>按优先级出队</code></pre><p><code>Deque</code> 是双端队列，两端都可以插入和删除。</p><p>常用实现：</p><pre><code>ArrayDeque<br/>LinkedList</code></pre><p>实际开发中，如果需要栈或队列，很多时候推荐 <code>ArrayDeque</code>。</p><ol><li>Map 体系</li></ol><p><code>Map</code> 是双列集合，存 key-value。</p><p>特点是：</p><pre><code>key 不能重复<br/>value 可以重复<br/>通过 key 查 value</code></pre><p>常见实现：</p><pre><code>HashMap<br/>LinkedHashMap<br/>TreeMap<br/>Hashtable<br/>ConcurrentHashMap</code></pre><p>示例：</p><pre><code>Map&lt;String, Integer&gt; map = new HashMap&lt;&gt;();<br/>map.put(\"Tom\", 18);</code></pre><ol><li>HashMap</li></ol><p><code>HashMap</code> 是最常用的 Map。</p><p>特点：</p><pre><code>底层是数组 + 链表 + 红黑树<br/>key 无序<br/>允许 null key 和 null value<br/>线程不安全<br/>查询、插入、删除平均 O(1)</code></pre><p>Java 8 以后，链表过长会转成红黑树，提高极端情况下性能。</p><ol><li>LinkedHashMap</li></ol><p><code>LinkedHashMap</code> 是有顺序的 HashMap。</p><p>特点：</p><pre><code>底层是 HashMap + 双向链表<br/>可以保持插入顺序<br/>也可以实现访问顺序<br/>常用于 LRU 缓存</code></pre><ol><li>TreeMap</li></ol><p><code>TreeMap</code> 底层是红黑树。</p><p>特点：</p><pre><code>key 有序<br/>查找、插入、删除 O(log n)<br/>不允许 key 不可比较<br/>适合范围查询、排序场景</code></pre><p>比如：</p><pre><code>按 key 排序<br/>找大于某个 key 的最小值<br/>范围遍历</code></pre><ol><li>Hashtable 和 ConcurrentHashMap</li></ol><p><code>Hashtable</code>：</p><pre><code>早期线程安全 Map<br/>方法加 synchronized<br/>性能较低<br/>不允许 null key 和 null value<br/>现在较少用</code></pre><p><code>ConcurrentHashMap</code>：</p><pre><code>并发安全 Map<br/>性能比 Hashtable 更好<br/>适合多线程场景<br/>不允许 null key 和 null value</code></pre><p>多线程环境下，不推荐用 <code>HashMap</code>，可以用 <code>ConcurrentHashMap</code>。</p><ol><li>怎么选择集合？</li></ol><pre><code>需要有序可重复、按下标访问：ArrayList<br/>需要频繁中间插入删除：LinkedList，但实际也要谨慎<br/>需要去重：HashSet<br/>需要去重且保持插入顺序：LinkedHashSet<br/>需要去重且排序：TreeSet<br/>需要 key-value：HashMap<br/>需要 key-value 且保持顺序：LinkedHashMap<br/>需要 key 排序：TreeMap<br/>需要线程安全 Map：ConcurrentHashMap<br/>需要队列/栈：ArrayDeque<br/>需要优先级队列：PriorityQueue</code></pre><ol><li><strong>面试背诵版：Java 集合体系有哪些？</strong></li></ol><p>Java 集合体系主要分为两大类：<code>Collection</code> 和 <code>Map</code>。<code>Collection</code> 是单列集合，用来存一个一个的元素，下面主要有 <code>List</code>、<code>Set</code>、<code>Queue</code> 和 <code>Deque</code>。<code>List</code> 有序、可重复、支持下标访问，常见实现有 <code>ArrayList</code>、<code>LinkedList</code> 和 <code>Vector</code>，其中 <code>ArrayList</code> 底层是动态数组，查询快，<code>LinkedList</code> 底层是双向链表，头尾插入删除方便。<code>Set</code> 不允许重复元素，常见实现有 <code>HashSet</code>、<code>LinkedHashSet</code> 和 <code>TreeSet</code>，其中 <code>HashSet</code> 底层基于 <code>HashMap</code>，<code>LinkedHashSet</code> 可以保持插入顺序，<code>TreeSet</code> 底层是红黑树，可以排序。<code>Queue</code> 和 <code>Deque</code> 表示队列和双端队列，常见实现有 <code>ArrayDeque</code>、<code>LinkedList</code> 和 <code>PriorityQueue</code>。<code>Map</code> 是双列集合，存储 key-value，key 不能重复，常见实现有 <code>HashMap</code>、<code>LinkedHashMap</code>、<code>TreeMap</code>、<code>Hashtable</code> 和 <code>ConcurrentHashMap</code>。其中 <code>HashMap</code> 最常用，底层是数组、链表和红黑树，线程不安全；<code>LinkedHashMap</code> 可以保持顺序；<code>TreeMap</code> 按 key 排序；<code>ConcurrentHashMap</code> 适合多线程场景。</p>"},{"t":"ArrayList 和 LinkedList 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>ArrayList</code> 和 <code>LinkedList</code> 的核心区别是：<code>ArrayList</code> 底层是动态数组，随机访问快；<code>LinkedList</code> 底层是双向链表，理论上插入删除更灵活，但随机访问慢。</p><ol><li>底层结构不同</li></ol><p><code>ArrayList</code> 底层是数组。</p><pre><code>ArrayList：<br/>[元素0][元素1][元素2][元素3]</code></pre><p>元素在内存中按下标连续存储。</p><p><code>LinkedList</code> 底层是双向链表。</p><pre><code>LinkedList：<br/>node1 ⇄ node2 ⇄ node3 ⇄ node4</code></pre><p>每个节点存：</p><pre><code>当前元素<br/>前一个节点引用<br/>后一个节点引用</code></pre><ol><li>查询效率不同</li></ol><p><code>ArrayList</code> 支持通过下标直接访问。</p><pre><code>list.get(100);</code></pre><p>时间复杂度是：</p><pre><code>O(1)</code></pre><p>因为数组可以直接按下标定位。</p><p><code>LinkedList</code> 不能直接跳到某个位置，需要从头或尾遍历。</p><pre><code>get(index)：O(n)</code></pre><p>所以随机访问时，<code>ArrayList</code> 明显更快。</p><ol><li>插入删除效率不同</li></ol><p>理论上，<code>LinkedList</code> 插入删除节点只需要改指针。</p><pre><code>前节点 ⇄ 新节点 ⇄ 后节点</code></pre><p>如果已经定位到节点，插入删除是：</p><pre><code>O(1)</code></pre><p>但问题是：如果按下标插入或删除，<code>LinkedList</code> 也要先遍历找到位置。</p><pre><code>linkedList.remove(1000);</code></pre><p>这个查找过程是 <code>O(n)</code>。</p><p><code>ArrayList</code> 中间插入删除需要移动元素。</p><pre><code>删除中间元素：<br/>后面的元素整体前移</code></pre><p>所以中间插入删除通常是 <code>O(n)</code>。</p><ol><li>尾部添加区别</li></ol><p><code>ArrayList</code> 尾部添加通常很快。</p><pre><code>arrayList.add(x);</code></pre><p>平均复杂度是：</p><pre><code>均摊 O(1)</code></pre><p>但如果数组容量不够，会扩容并复制数组。</p><p><code>LinkedList</code> 尾部添加也是 <code>O(1)</code>，因为它维护了尾节点引用。</p><ol><li>内存占用不同</li></ol><p><code>ArrayList</code> 只存元素引用，结构比较紧凑。</p><p>但可能有一部分预留容量没用。</p><p><code>LinkedList</code> 每个节点除了元素，还要存前驱和后继引用。</p><pre><code>item<br/>prev<br/>next</code></pre><p>所以 <code>LinkedList</code> 额外内存开销更大。</p><p>而且链表节点分散在内存中，缓存局部性不如数组。</p><ol><li>遍历性能不同</li></ol><p><code>ArrayList</code> 遍历通常更快。</p><p>原因是：</p><pre><code>数组连续存储<br/>CPU 缓存友好<br/>内存访问更高效</code></pre><p><code>LinkedList</code> 节点分散，需要不断跟着引用跳转。</p><pre><code>node.next.next.next</code></pre><p>CPU 缓存命中率较低。</p><p>所以实际开发中，<code>LinkedList</code> 不一定比 <code>ArrayList</code> 快。</p><ol><li>线程安全</li></ol><p>两者都不是线程安全的。</p><pre><code>ArrayList：线程不安全<br/>LinkedList：线程不安全</code></pre><p>多线程场景可以考虑：</p><pre><code>Collections.synchronizedList()<br/>CopyOnWriteArrayList<br/>并发队列</code></pre><p>但具体要看场景。</p><ol><li>使用场景怎么选？</li></ol><p>大多数情况下优先用 <code>ArrayList</code>。</p><p>适合：</p><pre><code>随机访问多<br/>遍历多<br/>尾部添加多<br/>数据量较大<br/>读多写少</code></pre><p><code>LinkedList</code> 适合：</p><pre><code>需要频繁在头尾插入删除<br/>需要作为队列或双端队列使用</code></pre><p>但如果是队列，实际更推荐：</p><pre><code>ArrayDeque</code></pre><p>而不是 <code>LinkedList</code>。</p><ol><li>面试常见误区</li></ol><p>不要简单说：</p><pre><code>ArrayList 查询快，LinkedList 增删快</code></pre><p>这个说法太粗糙。</p><p>更准确是：</p><pre><code>ArrayList 随机访问快，中间插入删除需要移动元素。<br/>LinkedList 如果已经定位到节点，插入删除快；但按下标操作仍要遍历，整体不一定快。</code></pre><ol><li>总结对比</li></ol><pre><code>ArrayList：<br/>底层动态数组<br/>随机访问 O(1)<br/>中间插入删除 O(n)<br/>尾部添加均摊 O(1)<br/>内存更紧凑<br/>遍历性能好<br/>大多数场景更常用<br/><br/>LinkedList：<br/>底层双向链表<br/>随机访问 O(n)<br/>定位后插入删除 O(1)<br/>按下标插入删除仍然 O(n)<br/>额外存 prev/next，内存占用更高<br/>适合头尾操作，但队列更推荐 ArrayDeque</code></pre><ol><li><strong>面试背诵版：ArrayList 和 LinkedList 有什么区别？</strong></li></ol><p><code>ArrayList</code> 和 <code>LinkedList</code> 的主要区别在底层结构和性能特点。<code>ArrayList</code> 底层是动态数组，支持按下标随机访问，所以 <code>get(index)</code> 是 O(1)，遍历性能也比较好，因为数组内存连续、缓存友好；但如果在中间插入或删除元素，需要移动后面的元素，所以是 O(n)，尾部添加通常是均摊 O(1)，扩容时会复制数组。<code>LinkedList</code> 底层是双向链表，每个节点保存元素以及前后节点引用，随机访问需要从头或尾遍历，所以 <code>get(index)</code> 是 O(n)；如果已经定位到节点，插入删除只需要修改指针，是 O(1)，但按下标插入删除仍然要先遍历定位，因此整体不一定比 <code>ArrayList</code> 快。内存上，<code>LinkedList</code> 每个节点要额外存前驱和后继引用，开销更大，缓存局部性也更差。实际开发中，大多数场景优先使用 <code>ArrayList</code>；如果需要频繁头尾操作，可以考虑 <code>LinkedList</code>，但队列或栈场景通常更推荐 <code>ArrayDeque</code>。</p>"},{"t":"ArrayList 扩容机制是什么？","tag":"高频","p":"high","html":""},{"t":"ArrayList 为什么查询快，插入慢？","tag":"高频","p":"high","html":""},{"t":"LinkedList 为什么现在很少使用？","tag":"了解即可","p":"know","html":""},{"t":"HashMap 底层原理是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p><code>HashMap</code> 底层原理的核心是：<strong>用哈希表存 key-value，通过 key 的 hash 值快速定位数组下标，冲突时用链表或红黑树解决。</strong></p><ol><li>HashMap 底层结构</li></ol><p>JDK 8 之后，<code>HashMap</code> 底层主要是：</p><pre><code>数组 + 链表 + 红黑树</code></pre><p>可以理解为：</p><pre><code>table 数组<br/>  ↓<br/>每个位置叫 bucket / 桶<br/>  ↓<br/>桶里存 Node<br/>  ↓<br/>冲突时形成链表<br/>  ↓<br/>链表太长时转红黑树</code></pre><p>结构图：</p><pre><code>table<br/>[0] -&gt; null<br/>[1] -&gt; Node(k1,v1) -&gt; Node(k2,v2)<br/>[2] -&gt; null<br/>[3] -&gt; TreeNode(...)<br/>[4] -&gt; Node(k3,v3)</code></pre><ol><li>put 的大致流程</li></ol><p>执行：</p><pre><code>map.put(\"name\", \"Tom\");</code></pre><p>底层大概做这些事：</p><pre><code>计算 key 的 hash 值<br/>根据 hash 定位数组下标<br/>如果桶为空，直接放入<br/>如果桶不为空，说明发生哈希冲突<br/>比较 key 是否相等<br/>如果 key 已存在，覆盖 value<br/>如果 key 不存在，插入到链表或红黑树中<br/>插入后判断是否需要扩容</code></pre><ol><li>如何定位数组下标？</li></ol><p>HashMap 会先算 key 的 hash 值。</p><p>然后根据数组长度定位桶位置。</p><p>大致类似：</p><pre><code>index = (n - 1) &amp; hash;</code></pre><p>这里 <code>n</code> 是数组长度。</p><p>因为 HashMap 的容量通常是 2 的幂，所以可以用位运算代替取模，提高效率。</p><pre><code>hash % n</code></pre><p>可以优化成：</p><pre><code>(n - 1) &amp; hash</code></pre><ol><li>为什么容量是 2 的幂？</li></ol><p>主要原因是方便用位运算定位下标。</p><p>当数组长度是 2 的幂时：</p><pre><code>index = hash &amp; (n - 1)</code></pre><p>等价于取模，而且更快。</p><p>另外扩容时也更容易判断元素新位置：</p><pre><code>要么在原位置<br/>要么移动到 原位置 + oldCapacity</code></pre><p>这降低了 rehash 成本。</p><ol><li>哈希冲突怎么解决？</li></ol><p>不同 key 可能定位到同一个桶。</p><p>这叫哈希冲突。</p><p>JDK 8 之后：</p><pre><code>冲突较少：用链表<br/>冲突较多：链表转红黑树</code></pre><p>链表结构：</p><pre><code>Node1 -&gt; Node2 -&gt; Node3</code></pre><p>红黑树结构用于降低极端冲突下的查询复杂度。</p><ol><li>什么时候链表转红黑树？</li></ol><p>JDK 8 中，链表转红黑树主要看两个条件：</p><pre><code>链表长度 &gt;= 8<br/>数组容量 &gt;= 64</code></pre><p>如果链表长度达到 8，但数组容量还小于 64，通常优先扩容，而不是树化。</p><p>因为容量太小时，冲突可能是数组太小导致的，扩容更有效。</p><ol><li>为什么要红黑树？</li></ol><p>链表查询是：</p><pre><code>O(n)</code></pre><p>如果很多 key 都落到同一个桶，性能会下降。</p><p>红黑树查询是：</p><pre><code>O(log n)</code></pre><p>所以红黑树是为了防止哈希冲突严重时性能退化。</p><ol><li>get 的大致流程</li></ol><p>执行：</p><pre><code>map.get(\"name\");</code></pre><p>流程是：</p><pre><code>计算 key 的 hash<br/>定位数组下标<br/>查看桶中第一个节点<br/>如果 key 相等，直接返回<br/>如果不相等：<br/>  - 链表：逐个比较<br/>  - 红黑树：按树查找<br/>找到返回 value，找不到返回 null</code></pre><p>比较 key 时一般会用：</p><pre><code>hash 是否相等<br/>equals 是否相等</code></pre><p>所以 HashMap 的 key 要正确重写 <code>hashCode()</code> 和 <code>equals()</code>。</p><ol><li>扩容机制</li></ol><p>HashMap 默认负载因子是：</p><pre><code>0.75</code></pre><p>当元素个数超过：</p><pre><code>capacity * loadFactor</code></pre><p>就会扩容。</p><p>比如默认容量 16：</p><pre><code>threshold = 16 * 0.75 = 12</code></pre><p>超过 12 个元素就触发扩容。</p><p>扩容通常是容量变为原来的 2 倍。</p><ol><li>扩容为什么有成本？</li></ol><p>扩容时需要：</p><pre><code>创建新数组<br/>把旧数组中的元素迁移到新数组<br/>重新分布桶位置</code></pre><p>虽然 JDK 8 做了优化，不需要完全重新计算每个 hash，但迁移仍然有成本。</p><p>所以如果能预估数据量，可以初始化容量，减少扩容次数。</p><ol><li>HashMap 线程安全吗？</li></ol><p><code>HashMap</code> 不是线程安全的。</p><p>多线程同时 put 可能导致：</p><pre><code>数据覆盖<br/>数据丢失<br/>结构异常<br/>读到不一致状态</code></pre><p>多线程场景应该用：</p><pre><code>ConcurrentHashMap</code></pre><p>或者加锁。</p><ol><li>null key 和 null value</li></ol><p><code>HashMap</code> 允许：</p><pre><code>一个 null key<br/>多个 null value</code></pre><p><code>null key</code> 通常放在 0 号桶附近处理。</p><p>但 <code>ConcurrentHashMap</code> 不允许 null key 和 null value。</p><ol><li>JDK 7 和 JDK 8 区别</li></ol><p>面试可以简单说：</p><pre><code>JDK 7：<br/>数组 + 链表<br/><br/>JDK 8：<br/>数组 + 链表 + 红黑树</code></pre><p>JDK 8 引入红黑树，是为了在哈希冲突严重时提升查询性能。</p><ol><li><strong>面试背诵版：HashMap 底层原理是什么？</strong></li></ol><p><code>HashMap</code> 底层是基于哈希表实现的，在 JDK 8 之后主要结构是数组、链表和红黑树。put 元素时，会先根据 key 计算 hash 值，然后通过 <code>(n - 1) &amp; hash</code> 定位到数组下标，也就是桶的位置。如果桶为空，就直接插入；如果桶不为空，说明发生哈希冲突，就会比较 key 是否相等，如果 key 已存在则覆盖 value，如果不存在则插入到链表或红黑树中。JDK 8 中，当链表长度达到 8 且数组容量至少为 64 时，链表会转成红黑树，用来降低哈希冲突严重时的查询复杂度，从 O(n) 降到 O(log n)。HashMap 默认负载因子是 0.75，当元素数量超过容量乘以负载因子时会触发扩容，容量通常变为原来的 2 倍，并重新分布元素。HashMap 的 key 需要正确重写 <code>hashCode()</code> 和 <code>equals()</code>，因为定位桶依赖 hash，判断 key 是否相等依赖 equals。需要注意，HashMap 不是线程安全的，多线程环境下应该使用 <code>ConcurrentHashMap</code>。</p>"},{"t":"HashMap 为什么线程不安全？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>HashMap</code> 线程不安全的核心原因是：它内部没有同步控制，多个线程同时 <code>put</code>、<code>remove</code>、<code>resize</code> 时，会并发修改数组、链表或红黑树结构，导致数据丢失、覆盖、结构异常或读到不一致结果。</p><ol><li>HashMap 本身没有加锁</li></ol><p><code>HashMap</code> 的方法不是线程安全的。</p><p>比如：</p><pre><code>map.put(key, value);<br/>map.get(key);<br/>map.remove(key);</code></pre><p>这些操作内部不会自动加锁。</p><p>所以多个线程同时操作同一个 <code>HashMap</code> 时，可能会同时修改内部结构：</p><pre><code>table 数组<br/>链表<br/>红黑树<br/>size<br/>modCount</code></pre><p>这些共享状态一旦并发修改，就可能出问题。</p><ol><li>多线程 put 可能数据覆盖</li></ol><p>假设两个线程同时往同一个桶插入不同 key。</p><pre><code>线程 A 判断桶为空<br/>线程 B 判断桶为空<br/>线程 A 放入 NodeA<br/>线程 B 放入 NodeB</code></pre><p>最后可能只有一个节点留下来。</p><p>另一个线程的写入被覆盖。</p><p>结果就是：</p><pre><code>数据丢失</code></pre><ol><li>size 统计可能不准确</li></ol><p><code>HashMap</code> 里有 <code>size</code> 记录元素数量。</p><p>多个线程同时插入时，都可能执行：</p><pre><code>size++;</code></pre><p>但 <code>size++</code> 不是原子操作。</p><p>它大概分成：</p><pre><code>读取 size<br/>加 1<br/>写回 size</code></pre><p>两个线程并发执行时，可能发生丢失更新。</p><p>比如本来应该加 2，结果只加了 1。</p><ol><li>resize 扩容时更危险</li></ol><p><code>HashMap</code> 最容易出问题的是扩容。</p><p>当元素数量超过阈值时，会触发 resize：</p><pre><code>创建新数组<br/>迁移旧数组里的节点<br/>重新分布桶位置</code></pre><p>如果多个线程同时触发扩容或一边扩容一边 put，就可能导致：</p><pre><code>数据丢失<br/>链表结构异常<br/>节点迁移不完整<br/>读到旧表或新表不一致</code></pre><p>尤其在 JDK 7 中，并发扩容时曾经可能出现链表成环，导致 get 时死循环。</p><ol><li>JDK 7 中可能出现链表成环</li></ol><p>JDK 7 的 <code>HashMap</code> 扩容迁移链表时使用头插法。</p><p>多线程并发 resize 时，链表节点顺序可能被反转并交错修改。</p><p>严重时会形成环：</p><pre><code>A → B → C<br/>    ↑   ↓<br/>    ←───</code></pre><p>这样 <code>get()</code> 遍历链表时可能一直循环，CPU 飙高。</p><p>JDK 8 之后扩容逻辑优化了，降低了链表成环风险，但 <code>HashMap</code> 仍然不是线程安全的。</p><ol><li>get 也可能不安全</li></ol><p>很多人以为只有写不安全，读没事。</p><p>如果一个线程在 <code>put</code> 或 <code>resize</code>，另一个线程同时 <code>get</code>，可能读到：</p><pre><code>旧数据<br/>不完整数据<br/>null<br/>迁移中的结构<br/>不一致状态</code></pre><p>所以“读写并发”也不安全。</p><p>如果初始化后只读，不再修改，一般问题不大。</p><p>但只要有并发写，就不能保证安全。</p><ol><li>fail-fast 不是线程安全保证</li></ol><p><code>HashMap</code> 的迭代器有 fail-fast 机制。</p><p>比如遍历时其他线程修改，可能抛：</p><pre><code>ConcurrentModificationException</code></pre><p>但这只是“尽力检测并发修改”。</p><p>它不是线程安全机制。</p><p>不能依赖它保证并发正确性。</p><ol><li>为什么 Hashtable 安全但不常用？</li></ol><p><code>Hashtable</code> 的方法基本加了 <code>synchronized</code>。</p><p>所以线程安全。</p><p>但它的问题是：</p><pre><code>锁粒度大<br/>并发性能差<br/>老旧类</code></pre><p>现在多线程环境更推荐：</p><pre><code>ConcurrentHashMap</code></pre><ol><li>ConcurrentHashMap 为什么更适合并发？</li></ol><p><code>ConcurrentHashMap</code> 专门为并发设计。</p><p>JDK 8 中主要通过：</p><pre><code>CAS<br/>synchronized 锁桶头节点<br/>volatile 保证可见性<br/>更细粒度锁控制</code></pre><p>来保证并发安全，同时性能比整个 Map 加锁更好。</p><p>所以多线程场景不要用 <code>HashMap</code> 共享写。</p><ol><li>如何安全使用 Map？</li></ol><p>可以选择：</p><pre><code>单线程：HashMap<br/>多线程读写：ConcurrentHashMap<br/>读多写少：CopyOnWrite 或不可变 Map<br/>简单同步：Collections.synchronizedMap()<br/>初始化后只读：可以使用 HashMap，但要安全发布</code></pre><p>最常见建议：</p><pre><code>Map&lt;String, String&gt; map = new ConcurrentHashMap&lt;&gt;();</code></pre><ol><li><strong>面试背诵版：HashMap 为什么线程不安全？</strong></li></ol><p><code>HashMap</code> 线程不安全，主要是因为它内部没有同步控制，多个线程同时 <code>put</code>、<code>remove</code> 或触发 <code>resize</code> 时，会并发修改底层数组、链表、红黑树、<code>size</code> 等共享结构。比如两个线程同时向同一个桶插入元素，可能都判断桶为空，最后一个线程的写入覆盖另一个线程，导致数据丢失；<code>size++</code> 也不是原子操作，多线程下可能统计不准确。更严重的是扩容过程，<code>HashMap</code> 扩容时会创建新数组并迁移节点，如果多个线程同时扩容或一边扩容一边读写，可能导致节点丢失、结构不一致，JDK 7 中甚至可能因为链表迁移使用头插法导致链表成环，<code>get()</code> 时出现死循环。JDK 8 优化了扩容逻辑，降低了成环风险，但仍然不能保证线程安全。多线程环境下应该使用 <code>ConcurrentHashMap</code>，它通过 CAS、volatile 和更细粒度的锁来保证并发安全。</p>"},{"t":"HashMap JDK1.7 和 JDK1.8 有什么区别？","tag":"高频","p":"high","html":""},{"t":"HashMap 为什么使用红黑树？","tag":"高频","p":"high","html":""},{"t":"HashMap 扩容过程是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>HashMap</code> 扩容过程的核心是：当元素数量超过阈值时，<strong>创建一个更大的数组</strong>，然后把<strong>旧数组中的节点迁移到新数组里</strong>，容量通常变为原来的 2 倍。</p><ol><li>什么时候触发扩容？</li></ol><p><code>HashMap</code> 有两个重要参数：</p><pre><code>capacity：数组容量<br/>loadFactor：负载因子<br/>threshold：扩容阈值</code></pre><p>默认情况下：</p><pre><code>初始容量：16<br/>负载因子：0.75<br/>扩容阈值：16 * 0.75 = 12</code></pre><p>当元素数量超过阈值时，就会触发扩容。</p><pre><code>size &gt; threshold</code></pre><p>比如默认容量 16，插入第 13 个元素时，就可能扩容。</p><ol><li>扩容后容量变多少？</li></ol><p>一般情况下，扩容后容量变为原来的 2 倍。</p><pre><code>16 → 32 → 64 → 128</code></pre><p>阈值也会重新计算：</p><pre><code>newThreshold = newCapacity * loadFactor</code></pre><p>例如：</p><pre><code>32 * 0.75 = 24</code></pre><ol><li>扩容大致做什么？</li></ol><p>扩容主要做三件事：</p><pre><code>创建新数组<br/>遍历旧数组<br/>把旧节点迁移到新数组</code></pre><p>流程：</p><pre><code>旧 table<br/>  ↓<br/>创建 newTable，容量翻倍<br/>  ↓<br/>遍历旧 table 每个桶<br/>  ↓<br/>把桶里的链表或红黑树节点重新分配到新 table</code></pre><ol><li>为什么要重新分配位置？</li></ol><p>因为数组容量变了，节点下标也可能变化。</p><p>HashMap 定位下标用：</p><pre><code>index = (n - 1) &amp; hash;</code></pre><p><code>n</code> 是数组容量。</p><p>扩容后 <code>n</code> 变了，所以 <code>(n - 1) &amp; hash</code> 的结果可能也变。</p><p>因此元素需要迁移到新位置。</p><ol><li>JDK 8 的优化</li></ol><p>JDK 8 有一个重要优化：</p><p>扩容为 2 倍时，一个节点的新位置只有两种可能：</p><pre><code>原下标<br/>原下标 + oldCapacity</code></pre><p>也就是说不需要完全重新计算 hash。</p><p>判断方式是：</p><pre><code>(hash &amp; oldCapacity) == 0</code></pre><p>如果为 0：</p><pre><code>位置不变</code></pre><p>如果不为 0：</p><pre><code>移动到 原位置 + oldCapacity</code></pre><ol><li>举个例子</li></ol><p>假设旧容量是 16，新容量是 32。</p><p>某个节点原来在下标 5。</p><p>扩容后，它只可能在：</p><pre><code>5<br/>或者<br/>5 + 16 = 21</code></pre><p>所以 JDK 8 迁移时，会把原链表拆成两条链：</p><pre><code>低位链表 lo：仍放原位置<br/>高位链表 hi：放原位置 + oldCapacity</code></pre><ol><li>链表迁移过程</li></ol><p>旧桶里如果是链表，JDK 8 会把它拆成两组：</p><pre><code>loHead / loTail：位置不变<br/>hiHead / hiTail：位置变成 index + oldCapacity</code></pre><p>然后放到新数组：</p><pre><code>newTable[index] = loHead<br/>newTable[index + oldCapacity] = hiHead</code></pre><p>这样可以保持链表相对顺序，也避免 JDK 7 头插法带来的链表成环问题。</p><ol><li>红黑树迁移过程</li></ol><p>如果桶里是红黑树，也要拆分。</p><p>扩容后，树节点同样会根据：</p><pre><code>hash &amp; oldCapacity</code></pre><p>分成低位和高位两组。</p><p>如果拆分后节点数量较少，可能会退化回链表。</p><p>如果节点仍然较多，就继续保持红黑树结构。</p><ol><li>扩容有什么成本？</li></ol><p>扩容成本比较高，因为要遍历并迁移元素。</p><p>成本主要有：</p><pre><code>创建更大数组<br/>遍历旧 table<br/>迁移链表或红黑树节点<br/>消耗 CPU<br/>短时间内增加内存占用</code></pre><p>所以如果知道大概元素数量，建议初始化时指定容量，减少扩容次数。</p><p>比如：</p><pre><code>Map&lt;String, Object&gt; map = new HashMap&lt;&gt;(1024);</code></pre><ol><li>初始化容量怎么估算？</li></ol><p>如果预计要放 <code>n</code> 个元素，可以大概设置：</p><pre><code>capacity &gt;= n / loadFactor</code></pre><p>比如预计放 1000 个元素：</p><pre><code>1000 / 0.75 ≈ 1334</code></pre><p>然后 HashMap 会调整到大于等于它的 2 的幂。</p><p>比如可能是：</p><pre><code>2048</code></pre><ol><li>扩容和线程安全</li></ol><p><code>HashMap</code> 扩容不是线程安全的。</p><p>多线程同时扩容或一边扩容一边 put，可能导致：</p><pre><code>数据丢失<br/>结构异常<br/>读到不一致结果</code></pre><p>JDK 7 中甚至可能链表成环。</p><p>所以并发场景用：</p><pre><code>ConcurrentHashMap</code></pre><ol><li><strong>面试背诵版：HashMap 扩容过程是什么？</strong></li></ol><p><code>HashMap</code> 扩容发生在元素数量超过阈值时，阈值等于容量乘以负载因子。默认容量是 16，负载因子是 0.75，所以默认阈值是 12，插入元素超过阈值后会触发扩容。扩容时，<code>HashMap</code> 会创建一个新数组，容量通常变为原来的 2 倍，然后遍历旧数组，把旧桶中的节点迁移到新数组中。因为 HashMap 定位桶下标使用 <code>(n - 1) &amp; hash</code>，扩容后数组长度变了，所以元素位置可能变化。JDK 8 中做了优化，由于容量是 2 的幂，扩容为 2 倍后，每个节点的新位置只有两种可能：要么保持原下标，要么移动到 <code>原下标 + oldCapacity</code>，判断条件是 <code>(hash &amp; oldCapacity)</code> 是否为 0。链表迁移时会拆成低位链表和高位链表，分别放到原位置和原位置加旧容量的位置；红黑树也会做类似拆分，节点少时可能退化回链表。扩容成本比较高，因为要创建新数组并迁移元素，所以如果能预估元素数量，建议初始化时指定合适容量。并且 <code>HashMap</code> 扩容不是线程安全的，多线程场景要使用 <code>ConcurrentHashMap</code>。</p>"},{"t":"HashMap 为什么要进行 hash 扰动？","tag":"高频","p":"high","html":""},{"t":"ConcurrentHashMap 底层原理是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span>"},{"t":"HashTable 和 ConcurrentHashMap 有什么区别？","tag":"高频","p":"high","html":""}]},{"no":"四","title":"JVM","questions":[{"t":"JVM 内存结构是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>JVM 内存结构的核心是：<strong>JVM 运行 Java 程序时，会把内存划分成多个区域</strong>，<strong>主要包括程序计数器、虚拟机栈、本地方法栈、堆、方法区，以及运行时常量池等</strong>。</p><ol><li>JVM 运行时数据区有哪些？</li></ol><p>JVM 内存结构通常说的是运行时数据区，主要包括：</p><pre><code>程序计数器<br/>Java 虚拟机栈<br/>本地方法栈<br/>堆<br/>方法区<br/>运行时常量池</code></pre><p>从线程角度可以分成：</p><pre><code>线程私有：<br/>程序计数器、虚拟机栈、本地方法栈<br/><br/>线程共享：<br/>堆、方法区</code></pre><ol><li>程序计数器</li></ol><p>程序计数器是线程私有的。</p><p>它可以理解为当前线程执行字节码的行号指示器。</p><p>作用是记录：</p><pre><code>当前线程执行到哪一条字节码指令</code></pre><p>因为 CPU 会在线程之间切换，切回来时要知道这个线程上次执行到哪里。</p><p>所以每个线程都需要自己的程序计数器。</p><p>特点：</p><pre><code>线程私有<br/>占用内存很小<br/>唯一一个不会出现 OutOfMemoryError 的区域</code></pre><ol><li>Java 虚拟机栈</li></ol><p>虚拟机栈也是线程私有的。</p><p>每个方法调用都会创建一个栈帧。</p><p>栈帧里主要保存：</p><pre><code>局部变量表<br/>操作数栈<br/>动态链接<br/>方法返回地址</code></pre><p>比如：</p><pre><code>public int add(int a, int b) {<br/>    int c = a + b;<br/>    return c;<br/>}</code></pre><p>方法执行时，<code>a</code>、<code>b</code>、<code>c</code> 这类局部变量就放在栈帧的局部变量表里。</p><ol><li>虚拟机栈可能出现什么异常？</li></ol><p>常见异常：</p><pre><code>StackOverflowError<br/>OutOfMemoryError</code></pre><p>如果方法递归太深，栈帧不断入栈，可能出现：</p><pre><code>StackOverflowError</code></pre><p>如果线程太多，栈空间申请不到，也可能出现：</p><pre><code>OutOfMemoryError</code></pre><ol><li>本地方法栈</li></ol><p>本地方法栈和虚拟机栈类似。</p><p>区别是：</p><pre><code>虚拟机栈：服务 Java 方法<br/>本地方法栈：服务 native 方法</code></pre><p>比如 Java 调用 C/C++ 写的本地方法，就可能用到本地方法栈。</p><p>它也是线程私有的。</p><ol><li>堆</li></ol><p>堆是 JVM 中最大的一块内存区域。</p><p>它是线程共享的。</p><p>主要存放：</p><pre><code>对象实例<br/>数组</code></pre><p>比如：</p><pre><code>User user = new User();<br/>int[] arr = new int[10];</code></pre><p>这些对象和数组通常都在堆里分配。</p><p>堆也是垃圾回收的主要区域。</p><p>所以常说：</p><pre><code>GC 主要管理堆</code></pre><ol><li>堆的分代</li></ol><p>在传统分代模型中，堆通常分为：</p><pre><code>新生代<br/>老年代</code></pre><p>新生代又分为：</p><pre><code>Eden 区<br/>Survivor From<br/>Survivor To</code></pre><p>大多数对象先分配在 Eden 区。</p><p>经历多次 GC 仍然存活的对象，可能晋升到老年代。</p><ol><li>方法区</li></ol><p>方法区是线程共享的。</p><p>主要存放类相关信息，比如：</p><pre><code>类信息<br/>字段信息<br/>方法信息<br/>运行时常量池<br/>静态变量<br/>即时编译后的代码缓存</code></pre><p>在 HotSpot 虚拟机中：</p><pre><code>JDK 8 之前：永久代 PermGen<br/>JDK 8 之后：元空间 Metaspace</code></pre><p>注意：方法区是 JVM 规范中的概念，永久代和元空间是 HotSpot 的具体实现。</p><ol><li>运行时常量池</li></ol><p>运行时常量池属于方法区的一部分。</p><p>它主要存放：</p><pre><code>字面量<br/>符号引用<br/>运行期间生成的常量</code></pre><p>比如类文件中的常量池信息加载后，会进入运行时常量池。</p><p>字符串常量池在不同 JDK 版本中位置有变化，面试里可以简单说：运行时常量池是方法区的一部分，字符串常量池在 JDK 7 后移到了堆中。</p><ol><li>直接内存</li></ol><p>直接内存不是 JVM 运行时数据区的一部分，但经常一起问。</p><p>它是堆外内存。</p><p>常见于：</p><pre><code>NIO<br/>DirectByteBuffer<br/>Netty</code></pre><p>优点是减少 Java 堆和 native 堆之间的数据拷贝，提高 IO 性能。</p><p>但直接内存也可能 OOM：</p><pre><code>OutOfMemoryError: Direct buffer memory</code></pre><ol><li>总结图</li></ol><pre><code>JVM 运行时数据区<br/><br/>线程私有：<br/>┌────────────────┐<br/>│ 程序计数器       │<br/>│ Java 虚拟机栈    │<br/>│ 本地方法栈       │<br/>└────────────────┘<br/><br/>线程共享：<br/>┌────────────────┐<br/>│ 堆              │<br/>│ 方法区           │<br/>│ 运行时常量池      │<br/>└────────────────┘<br/><br/>额外常问：<br/>┌────────────────┐<br/>│ 直接内存          │<br/>└────────────────┘</code></pre><ol><li><strong>面试背诵版：JVM 内存结构是什么？</strong></li></ol><p>JVM 内存结构通常指运行时数据区，主要包括程序计数器、Java 虚拟机栈、本地方法栈、堆、方法区和运行时常量池。从线程角度看，程序计数器、虚拟机栈和本地方法栈是线程私有的；堆和方法区是线程共享的。程序计数器记录当前线程执行到哪条字节码指令，是唯一一个不会出现 OOM 的区域。Java 虚拟机栈用于 Java 方法调用，每次方法执行都会创建栈帧，里面包含局部变量表、操作数栈、动态链接和返回地址，递归过深可能导致 <code>StackOverflowError</code>。本地方法栈服务 native 方法。堆是 JVM 最大的一块内存，主要存放对象实例和数组，也是垃圾回收的主要区域，通常分为新生代和老年代。方法区存放类信息、字段、方法、运行时常量池、静态变量和 JIT 编译后的代码等，在 HotSpot 中 JDK 8 前用永久代实现，JDK 8 后用元空间实现。运行时常量池属于方法区的一部分。另外，直接内存不属于 JVM 运行时数据区，但 NIO、Netty 等会用到，也可能发生 OOM。</p>"},{"t":"Java 对象创建过程是什么？","tag":"高频","p":"high","html":""},{"t":"对象在 JVM 中如何分配？","tag":"高频","p":"high","html":""},{"t":"什么情况下对象会进入老年代？","tag":"高频","p":"high","html":""},{"t":"JVM 垃圾回收机制是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>JVM 垃圾回收机制的核心是：<strong>自动判断哪些对象不再被使用</strong>，<strong>然后回收这些对象占用的堆内存</strong>，避免程序员手动释放内存。</p><ol><li>什么是垃圾回收？</li></ol><p>Java 中对象一般创建在堆里：</p><pre><code>User user = new User();</code></pre><p>当一个对象不再被任何地方使用时，它就可以被回收。</p><p>JVM 的垃圾回收器会自动完成：</p><pre><code>判断对象是否存活<br/>回收无用对象<br/>整理或释放内存<br/>尽量减少内存泄漏和碎片</code></pre><p>所以 Java 不需要像 C/C++ 那样手动 <code>free</code>。</p><ol><li>GC 主要回收哪里？</li></ol><p>GC 主要回收的是：</p><pre><code>堆内存</code></pre><p>堆中主要存：</p><pre><code>对象实例<br/>数组</code></pre><p>方法区或元空间也可能发生回收，比如类卸载、常量回收等，但平时说 GC，重点一般是堆。</p><ol><li>怎么判断对象是不是垃圾？</li></ol><p>主流 JVM 使用的是可达性分析算法。</p><p>它从一组 GC Roots 出发，沿着引用链往下找。</p><pre><code>GC Roots<br/>  ↓<br/>对象 A<br/>  ↓<br/>对象 B</code></pre><p>能从 GC Roots 找到的对象是存活对象。</p><p>找不到的对象就是不可达对象，可能被回收。</p><ol><li>哪些对象可以作为 GC Roots？</li></ol><p>常见 GC Roots 包括：</p><pre><code>虚拟机栈中的局部变量引用<br/>静态变量引用<br/>常量引用<br/>本地方法栈 JNI 引用<br/>正在运行的线程对象<br/>被 synchronized 持有的对象</code></pre><p>比如方法里的局部变量还引用某个对象，这个对象就不会被回收。</p><ol><li>引用计数为什么不用？</li></ol><p>引用计数就是每个对象记录有多少引用指向它。</p><p>引用数为 0 就回收。</p><p>但 Java 主流 GC 不用它作为主要算法，因为它解决不了循环引用。</p><p>比如：</p><pre><code>a.ref = b;<br/>b.ref = a;</code></pre><p>如果外部已经没有引用了，两个对象互相引用，引用计数都不为 0，但其实应该被回收。</p><p>可达性分析可以解决这个问题。</p><ol><li>常见垃圾回收算法</li></ol><p>常见 GC 算法有：</p><pre><code>标记-清除<br/>复制算法<br/>标记-整理<br/>分代收集</code></pre><p>标记-清除：</p><pre><code>先标记存活对象<br/>再清除未标记对象</code></pre><p>缺点是容易产生内存碎片。</p><p>复制算法：</p><pre><code>把存活对象复制到另一块区域<br/>然后清空原区域</code></pre><p>适合新生代，因为新生代对象大多朝生夕死。</p><p>标记-整理：</p><pre><code>先标记存活对象<br/>再把存活对象向一端移动<br/>最后清理边界外内存</code></pre><p>适合老年代，能减少碎片。</p><p>分代收集：</p><pre><code>新生代用复制算法<br/>老年代用标记-清除或标记-整理</code></pre><ol><li>为什么要分代？</li></ol><p>JVM 基于一个经验：</p><pre><code>大多数对象很快死亡<br/>少数对象存活很久</code></pre><p>所以堆通常分为：</p><pre><code>新生代<br/>老年代</code></pre><p>新对象一般先进入新生代。</p><p>如果对象经历多次 GC 还活着，就可能晋升到老年代。</p><p>这样可以针对不同对象生命周期使用不同回收策略，提高效率。</p><ol><li>新生代怎么回收？</li></ol><p>新生代通常分为：</p><pre><code>Eden<br/>Survivor From<br/>Survivor To</code></pre><p>对象一般先分配到 Eden。</p><p>Minor GC 时：</p><pre><code>Eden 和一个 Survivor 中还活着的对象<br/>复制到另一个 Survivor<br/>年龄 +1<br/>清空 Eden 和原 Survivor</code></pre><p>对象年龄达到阈值，或者 Survivor 放不下，就可能进入老年代。</p><ol><li>老年代怎么回收？</li></ol><p>老年代存放生命周期较长的对象。</p><p>老年代回收通常叫：</p><pre><code>Major GC<br/>Full GC</code></pre><p>老年代对象存活率高，所以不适合复制算法。</p><p>一般使用：</p><pre><code>标记-清除<br/>标记-整理</code></pre><p>Full GC 通常开销更大，停顿时间更长，需要尽量避免频繁发生。</p><ol><li>什么是 Stop The World？</li></ol><p>GC 时，很多阶段需要暂停用户线程。</p><p>这叫：</p><pre><code>Stop The World，简称 STW</code></pre><p>也就是：</p><pre><code>应用线程暂停<br/>GC 线程工作<br/>GC 完成后应用继续</code></pre><p>STW 时间过长会影响接口响应和用户体验。</p><p>现代垃圾回收器的目标之一就是减少 STW 时间。</p><ol><li>常见垃圾回收器</li></ol><p>常见垃圾回收器包括：</p><pre><code>Serial<br/>Parallel<br/>CMS<br/>G1<br/>ZGC<br/>Shenandoah</code></pre><p>简单记：</p><pre><code>Serial：单线程，简单<br/>Parallel：多线程，吞吐量优先<br/>CMS：低停顿，老年代并发回收，已逐渐淘汰<br/>G1：面向服务端，兼顾吞吐和低停顿<br/>ZGC / Shenandoah：超低延迟</code></pre><p>现在面试重点常问：</p><pre><code>G1<br/>ZGC<br/>CMS</code></pre><ol><li>对象一定马上被回收吗？</li></ol><p>不一定。</p><p>对象不可达只是“可以被回收”，不是立刻回收。</p><p>GC 什么时候发生由 JVM 根据内存情况和垃圾回收器策略决定。</p><p>比如：</p><pre><code>Eden 满了触发 Minor GC<br/>老年代空间不足触发 Major/Full GC<br/>元空间不足也可能触发 Full GC<br/>System.gc() 可能建议触发 GC，但不保证立即执行</code></pre><ol><li>面试可以这样总结机制</li></ol><p>JVM GC 大致流程是：</p><pre><code>对象分配到堆<br/>  ↓<br/>通过可达性分析判断对象是否存活<br/>  ↓<br/>根据分代策略选择回收区域<br/>  ↓<br/>使用标记-清除、复制、标记-整理等算法回收<br/>  ↓<br/>必要时移动对象、整理内存<br/>  ↓<br/>应用继续运行</code></pre><ol><li><strong>面试背诵版：JVM 垃圾回收机制是什么？</strong></li></ol><p>JVM 垃圾回收机制是 JVM 自动管理堆内存的一套机制，主要负责判断哪些对象不再被使用，并回收它们占用的内存。主流 JVM 通过可达性分析判断对象是否存活，从 GC Roots 出发沿引用链查找，能被找到的对象是存活对象，找不到的对象就是不可达对象，可能被回收。常见 GC Roots 包括虚拟机栈中的局部变量引用、静态变量引用、常量引用、JNI 引用、正在运行的线程等。JVM 通常采用分代收集思想，因为大多数对象朝生夕死，少数对象存活较久，所以堆分为新生代和老年代。新对象一般先进入 Eden 区，新生代回收通常使用复制算法；对象经历多次 Minor GC 仍然存活后可能晋升到老年代，老年代一般使用标记-清除或标记-整理算法。垃圾回收过程中可能发生 Stop The World，也就是暂停用户线程。常见垃圾回收器有 Serial、Parallel、CMS、G1、ZGC 等，现代垃圾回收器主要目标是在保证吞吐量的同时减少停顿时间。</p>"},{"t":"如何判断对象是否死亡？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>判断对象是否死亡的核心是：<strong>看这个对象是否还能从 GC Roots 通过引用链访问到</strong>；能访问到就是存活，访问不到就是不可达，可能被回收。</p><ol><li>主流方法：可达性分析</li></ol><p>Java 主流 JVM 判断对象是否死亡，主要用<strong>可达性分析算法</strong>。</p><p>它从一组根对象开始，这些根对象叫：</p><pre><code>GC Roots</code></pre><p>然后沿着引用关系往下搜索。</p><pre><code>GC Roots<br/>  ↓<br/>对象 A<br/>  ↓<br/>对象 B</code></pre><p>如果某个对象能从 GC Roots 走到，说明它还被使用，是存活对象。</p><p>如果走不到，说明它不可达，可能已经死亡。</p><ol><li>什么是 GC Roots？</li></ol><p>GC Roots 可以理解成“肯定还活着的引用起点”。</p><p>常见 GC Roots 包括：</p><pre><code>虚拟机栈中的局部变量引用<br/>本地方法栈中的 JNI 引用<br/>方法区中的静态变量引用<br/>方法区中的常量引用<br/>正在运行的线程对象<br/>被 synchronized 锁持有的对象</code></pre><p>比如：</p><pre><code>public void test() {<br/>    User user = new User();<br/>}</code></pre><p>方法还在执行时，局部变量 <code>user</code> 在虚拟机栈中，它引用的 <code>User</code> 对象就是可达的。</p><ol><li>可达对象不会被回收</li></ol><p>如果对象可以从 GC Roots 访问到：</p><pre><code>GC Roots → A → B → C</code></pre><p>那么 <code>A</code>、<code>B</code>、<code>C</code> 都是存活对象。</p><p>即使你没有直接引用 <code>C</code>，只要能通过引用链到达它，它就不会被回收。</p><ol><li>不可达对象可能被回收</li></ol><p>如果某个对象从任何 GC Roots 都访问不到：</p><pre><code>GC Roots    X<br/><br/>对象 D → 对象 E</code></pre><p>即使 <code>D</code> 和 <code>E</code> 互相引用，但只要它们和 GC Roots 没有引用链，它们就是不可达对象。</p><p>这类对象就可以被回收。</p><ol><li>为什么不用引用计数？</li></ol><p>引用计数法是给每个对象记录引用数量。</p><pre><code>引用数为 0 → 回收</code></pre><p>但引用计数解决不了循环引用问题。</p><p>比如：</p><pre><code>class Node {<br/>    Node next;<br/>}<br/><br/>Node a = new Node();<br/>Node b = new Node();<br/><br/>a.next = b;<br/>b.next = a;<br/><br/>a = null;<br/>b = null;</code></pre><p>此时 <code>a</code> 和 <code>b</code> 互相引用，引用计数都不为 0。</p><p>但外部已经访问不到它们了，应该被回收。</p><p>可达性分析可以发现它们不可达，所以能回收。</p><ol><li>对象不可达后一定马上死亡吗？</li></ol><p>不一定。</p><p>对象不可达后，通常还要经历一次“拯救机会”。</p><p>如果对象重写了 <code>finalize()</code>，并且还没有执行过，JVM 可能会把它放入队列，等待执行 <code>finalize()</code>。</p><p>如果在 <code>finalize()</code> 里重新让对象和 GC Roots 建立引用，对象可能逃过回收。</p><p>但注意：</p><pre><code>finalize() 不可靠<br/>执行时机不确定<br/>只能被系统自动调用一次<br/>不推荐使用</code></pre><p>实际开发中不要依赖 <code>finalize()</code>。</p><ol><li>四种引用类型也会影响回收</li></ol><p>Java 里有四种引用：</p><pre><code>强引用<br/>软引用<br/>弱引用<br/>虚引用</code></pre><p>强引用：</p><pre><code>Object obj = new Object();</code></pre><p>只要强引用存在，对象就不会被回收。</p><p>软引用：</p><pre><code>内存不足时可能回收</code></pre><p>常用于缓存。</p><p>弱引用：</p><pre><code>下一次 GC 时就可能被回收</code></pre><p>常用于 ThreadLocal 的 key 等场景。</p><p>虚引用：</p><pre><code>不能通过引用获取对象<br/>主要用于跟踪对象回收</code></pre><ol><li>强引用下的对象死亡</li></ol><p>如果对象还有强引用链连接到 GC Roots，就不会死亡。</p><p>比如：</p><pre><code>User user = new User();</code></pre><p>只要 <code>user</code> 还在可达作用域中，对象就不会被回收。</p><p>如果：</p><pre><code>user = null;</code></pre><p>且没有其他引用指向这个对象，它就可能变成不可达对象。</p><ol><li>判断流程总结</li></ol><p>JVM 判断对象是否死亡大致是：</p><pre><code>从 GC Roots 出发<br/>  ↓<br/>沿引用链遍历对象图<br/>  ↓<br/>能到达的对象标记为存活<br/>  ↓<br/>不能到达的对象标记为不可达<br/>  ↓<br/>不可达对象根据引用类型、finalize 等规则决定是否回收</code></pre><ol><li><strong>面试背诵版：如何判断对象是否死亡？</strong></li></ol><p>Java 主流 JVM 判断对象是否死亡主要使用可达性分析算法。它会从一组称为 GC Roots 的对象出发，沿着引用链向下搜索，如果一个对象能够从 GC Roots 访问到，说明它仍然存活；如果从任何 GC Roots 都无法访问到，说明它不可达，就可能被垃圾回收。常见的 GC Roots 包括虚拟机栈中的局部变量引用、方法区中的静态变量引用、常量引用、本地方法栈中的 JNI 引用、正在运行的线程对象以及被 synchronized 锁持有的对象。相比引用计数法，可达性分析可以解决循环引用问题，比如两个对象互相引用但都无法从 GC Roots 到达，它们仍然可以被回收。需要注意，对象不可达并不一定马上被回收，如果对象重写了 <code>finalize()</code>，理论上还有一次自救机会，但 <code>finalize()</code> 不可靠且不推荐使用。另外，Java 中强引用、软引用、弱引用、虚引用也会影响对象的回收时机。</p>"},{"t":"常见垃圾回收算法有哪些？","tag":"必须掌握","p":"core","html":"<span>[原文档此处有配图]</span><p>常见垃圾回收算法的核心是：先判断哪些对象还活着，再用不同方式回收死亡对象，主要有<strong>标记-清除、复制、标记-整理和分代收集</strong>。</p><ol><li><strong>标记-清除算法</strong></li></ol><p>标记-清除分两步：</p><pre><code>标记：标记所有存活对象<br/>清除：清理未被标记的垃圾对象</code></pre><p>流程：</p><pre><code>堆内存：<br/>[活][垃][活][垃][活]<br/><br/>标记后：<br/>[活*][垃][活*][垃][活*]<br/><br/>清除后：<br/>[活][空][活][空][活]</code></pre><p>优点：</p><pre><code>实现简单<br/>不需要移动对象</code></pre><p>缺点：</p><pre><code>会产生内存碎片<br/>标记和清除效率不稳定</code></pre><p>内存碎片多了以后，虽然总空间够，但可能找不到连续空间分配大对象。</p><ol><li>复制算法</li></ol><p>复制算法把内存分成两块。</p><p>每次只使用其中一块。</p><p>GC 时，把存活对象复制到另一块，然后一次性清空原来的区域。</p><pre><code>From 区：<br/>[活][垃][活][垃]<br/><br/>复制到 To 区：<br/>[活][活]<br/><br/>清空 From 区</code></pre><p>优点：</p><pre><code>没有内存碎片<br/>分配内存快<br/>回收效率高</code></pre><p>缺点：</p><pre><code>需要额外空间<br/>如果存活对象很多，复制成本高</code></pre><p>复制算法适合新生代，因为新生代对象大多“朝生夕死”，存活对象少，复制成本低。</p><ol><li>标记-整理算法</li></ol><p>标记-整理也分两步：</p><pre><code>标记：标记存活对象<br/>整理：把存活对象向一端移动<br/>清理边界外空间</code></pre><p>流程：</p><pre><code>整理前：<br/>[活][垃][活][垃][活]<br/><br/>整理后：<br/>[活][活][活][空][空]</code></pre><p>优点：</p><pre><code>不会产生内存碎片<br/>适合存活对象多的场景</code></pre><p>缺点：</p><pre><code>需要移动对象<br/>移动成本较高<br/>暂停时间可能更长</code></pre><p>标记-整理常用于老年代，因为老年代对象存活率高，不适合复制算法。</p><ol><li>分代收集算法</li></ol><p>分代收集不是单独一种具体回收算法，而是一种综合策略。</p><p>它基于经验：</p><pre><code>大多数对象很快死亡<br/>少数对象存活很久</code></pre><p>所以把堆分为：</p><pre><code>新生代<br/>老年代</code></pre><p>不同区域用不同算法：</p><pre><code>新生代：<br/>对象存活率低，通常用复制算法<br/><br/>老年代：<br/>对象存活率高，通常用标记-清除或标记-整理</code></pre><p>这就是 JVM 常见的分代回收思想。</p><ol><li>新生代为什么用复制算法？</li></ol><p>新生代通常分为：</p><pre><code>Eden<br/>Survivor From<br/>Survivor To</code></pre><p>大多数新对象分配在 Eden。</p><p>Minor GC 时：</p><pre><code>把 Eden 和 From 中存活对象复制到 To<br/>清空 Eden 和 From<br/>对象年龄 +1</code></pre><p>因为新生代存活对象少，所以复制量小，效率高。</p><ol><li>老年代为什么不用复制算法？</li></ol><p>老年代对象存活率高。</p><p>如果用复制算法，会导致：</p><pre><code>大量对象需要复制<br/>需要额外大块空间<br/>复制成本很高</code></pre><p>所以老年代更适合：</p><pre><code>标记-清除<br/>标记-整理</code></pre><p>其中标记-整理可以减少内存碎片。</p><ol><li>三种基础算法对比</li></ol><pre><code>标记-清除：<br/>不移动对象，但有碎片<br/><br/>复制算法：<br/>无碎片，速度快，但浪费空间，适合存活对象少<br/><br/>标记-整理：<br/>无碎片，不浪费太多空间，但移动对象成本高</code></pre><ol><li>和垃圾回收器的关系</li></ol><p>垃圾回收算法是理论基础。</p><p>垃圾回收器是具体实现。</p><p>比如：</p><pre><code>Serial / ParNew：<br/>新生代常用复制算法<br/><br/>CMS：<br/>老年代主要用标记-清除<br/><br/>G1：<br/>按 Region 管理，整体也有复制/整理思想<br/><br/>ZGC：<br/>更复杂，强调并发和低延迟</code></pre><p>所以面试时不要把“算法”和“垃圾回收器”混为一谈。</p><ol><li><strong>面试背诵版：常见垃圾回收算法有哪些？</strong></li></ol><p>常见垃圾回收算法主要有标记-清除、复制、标记-整理和分代收集。标记-清除算法先标记存活对象，再清理未标记的垃圾对象，优点是实现简单、不移动对象，缺点是会产生内存碎片。复制算法把内存分成两块，每次只使用其中一块，GC 时把存活对象复制到另一块，然后清空原区域，优点是没有碎片、分配快，缺点是需要额外空间，适合新生代这种存活对象少的场景。标记-整理算法先标记存活对象，再把存活对象向一端移动，清理边界外的空间，优点是没有碎片，适合老年代，缺点是移动对象成本较高。分代收集是一种综合策略，基于大多数对象朝生夕死、少数对象长期存活的特点，把堆分为新生代和老年代，新生代通常用复制算法，老年代通常用标记-清除或标记-整理算法。需要注意，垃圾回收算法是理论基础，垃圾回收器是这些算法的具体实现。</p>"},{"t":"新生代为什么使用复制算法？","tag":"高频","p":"high","html":""},{"t":"CMS 和 G1 有什么区别？","tag":"高频","p":"high","html":""},{"t":"G1 垃圾回收器原理是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>G1 垃圾回收器的核心是：把堆划分成很多 Region，不再固定区分连续的新生代和老年代，而是<strong>按 Region 管理</strong>，并<strong>优先回收垃圾最多、收益最高</strong>的区域，从而在可预测停顿时间内完成 GC。</p><ol><li>G1 是什么？</li></ol><p><strong>G1 全称是 Garbage First。</strong></p><p>意思是：</p><pre><code>优先回收垃圾最多的区域</code></pre><p>它是面向服务端应用的垃圾回收器，目标是：</p><pre><code>在尽量保证吞吐量的同时，控制 GC 停顿时间</code></pre><p>G1 常用于大堆内存场景，也适合对停顿时间有一定要求的服务。</p><ol><li>G1 的堆结构</li></ol><p>传统分代垃圾回收器通常是：</p><pre><code>新生代是一整块连续区域<br/>老年代是一整块连续区域</code></pre><p>G1 不一样。</p><p>它把整个堆划分成多个大小相等的 Region。</p><pre><code>Heap:<br/>[Region][Region][Region][Region]<br/>[Region][Region][Region][Region]</code></pre><p>每个 Region 可以扮演不同角色：</p><pre><code>Eden Region<br/>Survivor Region<br/>Old Region<br/>Humongous Region</code></pre><p>也就是说，新生代和老年代在逻辑上仍然存在，但物理上不要求连续。</p><ol><li>为什么叫 Garbage First？</li></ol><p>G1 会统计每个 Region 的垃圾比例和回收收益。</p><p>然后优先选择“垃圾最多、回收价值最高”的 Region 进行回收。</p><pre><code>Region A：垃圾 80%<br/>Region B：垃圾 20%<br/>Region C：垃圾 60%<br/><br/>优先回收 A，再考虑 C</code></pre><p>这就是 Garbage First。</p><ol><li>G1 如何控制停顿时间？</li></ol><p>G1 可以设置一个期望最大停顿时间，比如：</p><pre><code>-XX:MaxGCPauseMillis=200</code></pre><p>它会根据这个目标，在一次 GC 中选择合适数量的 Region 回收。</p><pre><code>停顿预算有限：<br/>少回收几个 Region<br/><br/>停顿预算较宽：<br/>多回收几个 Region</code></pre><p>这不是绝对保证，而是尽量满足。</p><ol><li>G1 的回收方式</li></ol><p>G1 使用的是复制/整理思想。</p><p>回收时，会把选中 Region 中的存活对象复制到其他空闲 Region。</p><p>然后整个旧 Region 就可以被清空。</p><pre><code>回收前：<br/>Region A：[活][垃][垃][活][垃]<br/><br/>复制存活对象到 Region B：<br/>Region B：[活][活]<br/><br/>清空 Region A</code></pre><p>好处是：</p><pre><code>减少内存碎片<br/>回收后 Region 可以整体复用</code></pre><ol><li>G1 的 Remembered Set</li></ol><p>因为 G1 把堆分成很多 Region，一个 Region 里的对象可能引用另一个 Region 里的对象。</p><p>GC 某个 Region 时，需要知道：</p><pre><code>有没有其他 Region 引用了这里的对象</code></pre><p>如果每次都扫描整个堆，成本太高。</p><p>所以 G1 为每个 Region 维护 Remembered Set。</p><p>它记录：</p><pre><code>哪些其他 Region 引用了当前 Region</code></pre><p>这样回收某些 Region 时，不用扫描全堆。</p><ol><li>G1 的写屏障</li></ol><p>Remembered Set 需要维护引用关系。</p><p>当程序运行时，如果对象引用发生变化，G1 需要记录跨 Region 引用。</p><p>这通常通过写屏障完成。</p><p>简单理解：</p><pre><code>对象引用发生写入<br/>  ↓<br/>写屏障拦截<br/>  ↓<br/>记录跨 Region 引用关系<br/>  ↓<br/>更新 Remembered Set</code></pre><p>这会带来一定额外开销，但换来了更高效的局部回收。</p><ol><li>G1 的主要 GC 类型</li></ol><p>G1 常见过程包括：</p><pre><code>Young GC<br/>Concurrent Marking<br/>Mixed GC<br/>Full GC</code></pre><p>Young GC：</p><pre><code>回收 Eden 和 Survivor Region<br/>复制存活对象到 Survivor 或 Old Region</code></pre><p>Concurrent Marking：</p><pre><code>并发标记老年代存活对象<br/>尽量和用户线程同时执行</code></pre><p>Mixed GC：</p><pre><code>同时回收新生代 Region 和一部分老年代 Region<br/>优先选择垃圾比例高的 Old Region</code></pre><p>Full GC：</p><pre><code>如果 G1 来不及回收或内存不足<br/>可能退化成 Full GC<br/>停顿时间长<br/>应尽量避免</code></pre><ol><li>G1 的大对象处理</li></ol><p>G1 中有 Humongous Region。</p><p>如果对象特别大，比如超过 Region 大小的一半，可能会被认为是大对象。</p><p>这类对象会放在一个或多个连续的 Humongous Region 中。</p><p>大对象如果很多，可能影响 G1 效率，也可能更容易触发 Full GC。</p><ol><li>G1 的优点</li></ol><p>G1 的优点：</p><pre><code>支持大堆<br/>可设置停顿时间目标<br/>按 Region 回收，灵活性高<br/>优先回收垃圾多的区域<br/>减少内存碎片<br/>整体比 CMS 更容易管理碎片</code></pre><ol><li>G1 的缺点</li></ol><p>G1 也有成本：</p><pre><code>Remembered Set 占用额外内存<br/>写屏障有额外开销<br/>调优复杂度较高<br/>停顿时间目标不是绝对保证<br/>小堆场景未必比其他 GC 更好</code></pre><ol><li>G1 和 CMS 的区别</li></ol><p>简单对比：</p><pre><code>CMS：<br/>主要基于标记-清除<br/>容易产生内存碎片<br/>关注低停顿<br/>老年代并发回收<br/><br/>G1：<br/>基于 Region<br/>复制/整理思想<br/>减少碎片<br/>可预测停顿<br/>可做 Mixed GC</code></pre><p>G1 在很多场景下是 CMS 的替代方案。</p><ol><li><strong>面试背诵版：G1 垃圾回收器原理是什么？</strong></li></ol><p>G1 垃圾回收器的核心思想是把整个 Java 堆划分成多个大小相等的 Region，不再要求新生代和老年代在物理上连续，而是每个 Region 可以扮演 Eden、Survivor、Old 或 Humongous 等角色。G1 仍然保留分代思想，但回收时以 Region 为单位，并根据每个 Region 的垃圾比例和回收收益，优先回收垃圾最多、收益最高的区域，所以叫 Garbage First。G1 可以通过 <code>MaxGCPauseMillis</code> 设置期望停顿时间，它会根据停顿预算选择合适数量的 Region 回收。回收时，G1 采用复制/整理思想，把选中 Region 中的存活对象复制到其他空闲 Region，然后整体清空旧 Region，从而减少内存碎片。为了避免回收某个 Region 时扫描整个堆，G1 为每个 Region 维护 Remembered Set，记录其他 Region 对当前 Region 的引用，并通过写屏障维护这些跨 Region 引用。G1 的主要过程包括 Young GC、并发标记、Mixed GC 和 Full GC，其中 Mixed GC 会同时回收新生代和部分垃圾较多的老年代 Region。G1 的优点是适合大堆、可控制停顿时间、减少碎片；缺点是 Remembered Set 和写屏障带来额外开销，调优也相对复杂。</p><p></p>"},{"t":"Minor GC、Major GC、Full GC 有什么区别？","tag":"高频","p":"high","html":""},{"t":"JVM 如何进行性能调优？","tag":"了解即可","p":"know","html":""}]},{"no":"五","title":"Java 并发编程","questions":[{"t":"线程和进程有什么区别？","tag":"必须掌握","p":"core","html":""},{"t":"Java 创建线程有哪些方式？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>Java 创建线程的核心方式有：<strong>继承 </strong><strong><code>Thread</code></strong><strong>、实现 </strong><strong><code>Runnable</code></strong><strong>、实现 </strong><strong><code>Callable</code></strong><strong> 配合 </strong><strong><code>FutureTask</code></strong>，以及实际开发中更推荐使用线程池。</p><ol><li>继承 Thread 类</li></ol><p>第一种方式是继承 <code>Thread</code>，重写 <code>run()</code> 方法。</p><pre><code>class MyThread extends Thread {<br/>    @Override<br/>    public void run() {<br/>        System.out.println(\"线程执行\");<br/>    }<br/>}<br/><br/>public class Demo {<br/>    public static void main(String[] args) {<br/>        new MyThread().start();<br/>    }<br/>}</code></pre><p>注意：</p><pre><code>启动线程要调用 start()<br/>不要直接调用 run()</code></pre><p><code>start()</code> 会真正创建新线程，<code>run()</code> 只是普通方法调用。</p><ol><li>实现 Runnable 接口</li></ol><p>第二种方式是实现 <code>Runnable</code> 接口。</p><pre><code>class MyTask implements Runnable {<br/>    @Override<br/>    public void run() {<br/>        System.out.println(\"任务执行\");<br/>    }<br/>}<br/><br/>public class Demo {<br/>    public static void main(String[] args) {<br/>        Thread thread = new Thread(new MyTask());<br/>        thread.start();<br/>    }<br/>}</code></pre><p>这种方式比继承 <code>Thread</code> 更常用。</p><p>因为 Java 是单继承，如果继承了 <code>Thread</code>，就不能再继承其他类。</p><p>实现 <code>Runnable</code> 更灵活，也更符合“任务和线程分离”的思想。</p><ol><li>使用 Lambda 创建 Runnable</li></ol><p>因为 <code>Runnable</code> 是函数式接口，所以可以用 Lambda。</p><pre><code>Thread thread = new Thread(() -&gt; {<br/>    System.out.println(\"线程执行\");<br/>});<br/><br/>thread.start();</code></pre><p>这种写法比较简洁。</p><ol><li>实现 Callable 接口</li></ol><p><code>Runnable</code> 没有返回值，也不能直接抛出检查异常。</p><p>如果需要返回结果，可以用 <code>Callable</code>。</p><pre><code>import java.util.concurrent.Callable;<br/>import java.util.concurrent.FutureTask;<br/><br/>class MyCallable implements Callable&lt;Integer&gt; {<br/>    @Override<br/>    public Integer call() {<br/>        return 100;<br/>    }<br/>}<br/><br/>public class Demo {<br/>    public static void main(String[] args) throws Exception {<br/>        FutureTask&lt;Integer&gt; task = new FutureTask&lt;&gt;(new MyCallable());<br/>        Thread thread = new Thread(task);<br/>        thread.start();<br/><br/>        Integer result = task.get();<br/>        System.out.println(result);<br/>    }<br/>}</code></pre><p>特点：</p><pre><code>Callable 有返回值<br/>Callable 可以抛异常<br/>FutureTask 可以获取执行结果</code></pre><ol><li>使用线程池</li></ol><p>实际开发中更推荐线程池。</p><pre><code>import java.util.concurrent.ExecutorService;<br/>import java.util.concurrent.Executors;<br/><br/>public class Demo {<br/>    public static void main(String[] args) {<br/>        ExecutorService executor = Executors.newFixedThreadPool(5);<br/><br/>        executor.submit(() -&gt; {<br/>            System.out.println(\"线程池执行任务\");<br/>        });<br/><br/>        executor.shutdown();<br/>    }<br/>}</code></pre><p>线程池的好处：</p><pre><code>复用线程<br/>减少频繁创建销毁线程的开销<br/>控制并发数量<br/>统一管理任务<br/>支持任务队列和拒绝策略</code></pre><p>实际项目中，不建议频繁 <code>new Thread()</code>。</p><ol><li>更推荐 ThreadPoolExecutor</li></ol><p>面试可以补充：实际生产中不建议直接用 <code>Executors</code> 创建线程池。</p><p>因为一些工厂方法可能创建无界队列或过多线程，导致 OOM 风险。</p><p>更推荐手动使用 <code>ThreadPoolExecutor</code>。</p><pre><code>ThreadPoolExecutor executor = new ThreadPoolExecutor(<br/>    5,<br/>    10,<br/>    60,<br/>    TimeUnit.SECONDS,<br/>    new ArrayBlockingQueue&lt;&gt;(100),<br/>    Executors.defaultThreadFactory(),<br/>    new ThreadPoolExecutor.AbortPolicy()<br/>);</code></pre><p>这样可以明确控制：</p><pre><code>核心线程数<br/>最大线程数<br/>空闲线程存活时间<br/>任务队列<br/>线程工厂<br/>拒绝策略</code></pre><ol><li>几种方式对比</li></ol><pre><code>继承 Thread：<br/>写法简单，但不灵活，受 Java 单继承限制。<br/><br/>实现 Runnable：<br/>无返回值，适合普通任务，任务和线程解耦。<br/><br/>实现 Callable：<br/>有返回值，可以抛异常，配合 Future/FutureTask 使用。<br/><br/>线程池：<br/>生产环境推荐，线程可复用，可控制并发和资源。</code></pre><ol><li>启动线程的关键点</li></ol><p>无论哪种方式，真正启动线程都要通过：</p><pre><code>start()</code></pre><p>或者提交给线程池：</p><pre><code>executor.submit()<br/>executor.execute()</code></pre><p>不要直接调用：</p><pre><code>run()</code></pre><p>直接调用 <code>run()</code> 不会创建新线程，只是在当前线程执行普通方法。</p><ol><li><strong>面试背诵版：Java 创建线程有哪些方式？</strong></li></ol><p>Java 创建线程常见方式有四种。第一种是继承 <code>Thread</code> 类，重写 <code>run()</code> 方法，然后调用 <code>start()</code> 启动线程；第二种是实现 <code>Runnable</code> 接口，把任务传给 <code>Thread</code>，这种方式更常用，因为 Java 是单继承，实现接口更灵活，也能把任务和线程解耦；第三种是实现 <code>Callable</code> 接口，配合 <code>FutureTask</code> 或线程池使用，它和 <code>Runnable</code> 的区别是可以返回结果，也可以抛出异常；第四种是使用线程池，比如 <code>ExecutorService</code> 或 <code>ThreadPoolExecutor</code>，实际开发中最推荐这种方式，因为线程池可以复用线程，减少频繁创建和销毁线程的开销，并且可以控制并发数量、任务队列和拒绝策略。需要注意，启动线程要调用 <code>start()</code>，直接调用 <code>run()</code> 不会创建新线程，只是普通方法调用。生产环境中一般不建议频繁 <code>new Thread()</code>，也不建议无脑使用 <code>Executors</code>，更推荐手动配置 <code>ThreadPoolExecutor</code>。</p>"},{"t":"Runnable 和 Callable 有什么区别？","tag":"高频","p":"high","html":""},{"t":"Thread 生命周期有哪些状态？","tag":"高频","p":"high","html":""},{"t":"sleep() 和 wait() 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>sleep()</code> 和 <code>wait()</code> 的核心区别是：<code>sleep()</code> 是让线程暂停一段时间，<strong>不释放锁</strong>；<code>wait()</code> 是让线程进入等待，<strong>并释放当前对象锁</strong>，等待其他线程 <code>notify()</code> 或 <code>notifyAll()</code> 唤醒。</p><ol><li>所属类不同</li></ol><p><code>sleep()</code> 是 <code>Thread</code> 类的静态方法：</p><pre><code>Thread.sleep(1000);</code></pre><p><code>wait()</code> 是 <code>Object</code> 类的方法：</p><pre><code>obj.wait();</code></pre><p>为什么 <code>wait()</code> 在 <code>Object</code> 里？</p><p>因为 Java 中每个对象都可以作为锁对象，所以等待和唤醒机制绑定在对象监视器上。</p><ol><li>是否释放锁不同</li></ol><p>这是最核心区别。</p><p><code>sleep()</code> 不会释放锁。</p><pre><code>synchronized (lock) {<br/>    Thread.sleep(1000);<br/>}</code></pre><p>线程 sleep 期间，仍然持有 <code>lock</code>，其他线程进不来这个同步代码块。</p><p><code>wait()</code> 会释放锁。</p><pre><code>synchronized (lock) {<br/>    lock.wait();<br/>}</code></pre><p>线程 wait 后，会释放 <code>lock</code>，其他线程可以进入同步代码块。</p><ol><li>使用位置不同</li></ol><p><code>sleep()</code> 可以在任何地方调用。</p><pre><code>Thread.sleep(1000);</code></pre><p><code>wait()</code> 必须在 <code>synchronized</code> 同步块或同步方法中调用。</p><pre><code>synchronized (lock) {<br/>    lock.wait();<br/>}</code></pre><p>否则会抛出：</p><pre><code>IllegalMonitorStateException</code></pre><p>因为调用 <code>wait()</code> 的线程必须先持有这个对象的监视器锁。</p><ol><li>唤醒方式不同</li></ol><p><code>sleep()</code> 到时间后自动醒来。</p><pre><code>Thread.sleep(1000);</code></pre><p>大约 1 秒后线程进入可运行状态。</p><p><code>wait()</code> 需要其他线程唤醒，或者设置超时时间。</p><pre><code>lock.wait();       // 等待 notify/notifyAll<br/>lock.wait(1000);   // 最多等待 1 秒</code></pre><p>唤醒方式：</p><pre><code>lock.notify();<br/>lock.notifyAll();</code></pre><ol><li>状态不同</li></ol><p>调用 <code>sleep()</code> 后，线程进入：</p><pre><code>TIMED_WAITING</code></pre><p>调用无参 <code>wait()</code> 后，线程进入：</p><pre><code>WAITING</code></pre><p>调用带超时的 <code>wait(timeout)</code> 后，线程进入：</p><pre><code>TIMED_WAITING</code></pre><p>虽然状态可能都叫等待，但语义不同。</p><ol><li>用途不同</li></ol><p><code>sleep()</code> 主要用于：</p><pre><code>让当前线程暂停一段时间<br/>模拟耗时<br/>定时等待<br/>控制执行节奏</code></pre><p><code>wait()</code> 主要用于线程间通信。</p><p>比如：</p><pre><code>生产者消费者<br/>等待条件满足<br/>一个线程等待另一个线程通知</code></pre><p>例如：</p><pre><code>synchronized (lock) {<br/>    while (!condition) {<br/>        lock.wait();<br/>    }<br/>    // 条件满足后继续执行<br/>}</code></pre><ol><li>wait 为什么通常放在 while 里？</li></ol><p>因为线程被唤醒后，不代表条件一定满足。</p><p>可能出现：</p><pre><code>虚假唤醒<br/>多个线程竞争后条件又不满足<br/>notifyAll 唤醒了很多线程</code></pre><p>所以推荐：</p><pre><code>synchronized (lock) {<br/>    while (!condition) {<br/>        lock.wait();<br/>    }<br/>}</code></pre><p>不要只用 <code>if</code>。</p><ol><li>notify 后 wait 线程立刻执行吗？</li></ol><p>不是。</p><p><code>notify()</code> 只是唤醒等待线程，让它从等待队列进入锁竞争队列。</p><p>被唤醒线程还要重新竞争锁。</p><p>只有重新拿到锁后，才能从 <code>wait()</code> 后继续执行。</p><p>流程：</p><pre><code>线程 A wait，释放锁<br/>线程 B notify<br/>线程 A 被唤醒<br/>线程 A 重新竞争锁<br/>线程 B 退出 synchronized 释放锁<br/>线程 A 拿到锁后继续执行</code></pre><ol><li>对比总结</li></ol><pre><code>sleep：<br/>Thread 类方法<br/>不释放锁<br/>可以在任何地方调用<br/>时间到了自动醒<br/>用于暂停执行<br/><br/>wait：<br/>Object 类方法<br/>释放对象锁<br/>必须在 synchronized 中调用<br/>需要 notify/notifyAll 或超时唤醒<br/>用于线程间通信</code></pre><ol><li><strong>面试背诵版：sleep() 和 wait() 有什么区别？</strong></li></ol><p><code>sleep()</code> 和 <code>wait()</code> 的区别主要有几个方面。首先，<code>sleep()</code> 是 <code>Thread</code> 类的静态方法，而 <code>wait()</code> 是 <code>Object</code> 类的方法，因为 Java 中每个对象都可以作为锁对象，等待和唤醒机制绑定在对象监视器上。其次，<code>sleep()</code> 不会释放锁，如果线程在 <code>synchronized</code> 中调用 <code>sleep()</code>，睡眠期间仍然持有锁；而 <code>wait()</code> 会释放当前对象锁，让其他线程可以进入同步代码块。第三，<code>sleep()</code> 可以在任何地方调用，时间到了自动进入可运行状态；<code>wait()</code> 必须在 <code>synchronized</code> 同步块或同步方法中调用，否则会抛出 <code>IllegalMonitorStateException</code>，并且通常需要其他线程通过 <code>notify()</code> 或 <code>notifyAll()</code> 唤醒，也可以设置超时时间。用途上，<code>sleep()</code> 主要用于让线程暂停一段时间，而 <code>wait()</code> 主要用于线程间通信，比如生产者消费者模型。需要注意，<code>wait()</code> 被唤醒后不会立刻执行，而是要重新竞争锁，拿到锁后才能继续执行，并且一般要放在 <code>while</code> 循环里判断条件，防止虚假唤醒。</p><p></p>"},{"t":"notify() 和 notifyAll() 有什么区别？","tag":"高频","p":"high","html":""},{"t":"synchronized 原理是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>synchronized</code> 原理的核心是：它基于<strong>对象监视器 Monitor</strong> 实现互斥，同一时刻只有拿<strong>到对象锁</strong>的线程才能进入同步代码块或同步方法。</p><ol><li>synchronized 是什么？</li></ol><p><code>synchronized</code> 是 Java 提供的内置锁机制。</p><p>它可以保证：</p><pre><code>原子性<br/>可见性<br/>有序性</code></pre><p>主要用来解决多线程并发访问共享资源的问题。</p><p>比如：</p><pre><code>synchronized (lock) {<br/>    count++;<br/>}</code></pre><p>同一时刻只有一个线程能进入这个同步代码块。</p><ol><li>synchronized 锁的是什么？</li></ol><p><code>synchronized</code> 锁的是对象。</p><p>不同写法锁对象不同。</p><p>同步代码块：</p><pre><code>synchronized (lock) {<br/>    ...<br/>}</code></pre><p>锁的是 <code>lock</code> 这个对象。</p><p>普通同步方法：</p><pre><code>public synchronized void method() {<br/>    ...<br/>}</code></pre><p>锁的是当前对象：</p><pre><code>this</code></pre><p>静态同步方法：</p><pre><code>public static synchronized void method() {<br/>    ...<br/>}</code></pre><p>锁的是当前类的 <code>Class</code> 对象：</p><pre><code>当前类.class</code></pre><ol><li>底层和 Monitor 有关</li></ol><p>每个 Java 对象都可以关联一个 Monitor。</p><p>Monitor 可以理解为对象监视器锁。</p><p>当线程进入 <code>synchronized</code> 时，需要先获取对象关联的 Monitor。</p><pre><code>线程获取 Monitor 成功 → 进入同步代码<br/>线程获取 Monitor 失败 → 阻塞等待</code></pre><p>退出同步代码块时释放 Monitor。</p><ol><li>字节码层面怎么实现？</li></ol><p>同步代码块使用两个字节码指令：</p><pre><code>monitorenter<br/>monitorexit</code></pre><p>例如：</p><pre><code>synchronized (lock) {<br/>    count++;<br/>}</code></pre><p>大致会编译成：</p><pre><code>monitorenter<br/>执行同步代码<br/>monitorexit</code></pre><p>如果发生异常，也必须执行 <code>monitorexit</code>，所以编译器会生成异常处理逻辑，保证锁能释放。</p><ol><li>同步方法怎么实现？</li></ol><p>同步方法不一定显式生成 <code>monitorenter</code> / <code>monitorexit</code>。</p><p>它会在方法访问标志上加：</p><pre><code>ACC_SYNCHRONIZED</code></pre><p>JVM 调用方法时看到这个标志，就会自动获取对应锁。</p><p>普通同步方法锁 <code>this</code>。</p><p>静态同步方法锁 <code>Class</code> 对象。</p><ol><li>synchronized 如何保证原子性？</li></ol><p>同一时刻只有一个线程能拿到锁。</p><p>所以被锁保护的代码不会被多个线程同时执行。</p><p>比如：</p><pre><code>synchronized (lock) {<br/>    count++;<br/>}</code></pre><p>虽然 <code>count++</code> 本身不是原子操作，但放在 synchronized 里后，同一时刻只有一个线程执行它。</p><p>这就保证了临界区操作的原子性。</p><ol><li>synchronized 如何保证可见性？</li></ol><p>线程进入和退出 <code>synchronized</code> 会有内存语义。</p><p>简单说：</p><pre><code>进入 synchronized：<br/>会从主内存读取最新共享变量<br/><br/>退出 synchronized：<br/>会把修改刷新回主内存</code></pre><p>所以一个线程在同步块里修改的变量，另一个线程后续进入同一把锁保护的同步块时能看到。</p><ol><li>synchronized 如何保证有序性？</li></ol><p><code>synchronized</code> 会形成 happens-before 关系。</p><p>规则是：</p><pre><code>对同一把锁的 unlock happens-before 后续对这把锁的 lock</code></pre><p>也就是说：</p><pre><code>线程 A 释放锁前的操作<br/>对<br/>线程 B 后续获取同一把锁后的操作<br/>可见且有序</code></pre><p>这保证了同步块之间的内存可见性和顺序关系。</p><ol><li>synchronized 是可重入锁</li></ol><p><code>synchronized</code> 支持可重入。</p><p>同一个线程已经拿到某个锁后，可以再次进入同一把锁保护的代码。</p><pre><code>public synchronized void a() {<br/>    b();<br/>}<br/><br/>public synchronized void b() {<br/>    ...<br/>}</code></pre><p>同一个线程调用 <code>a()</code> 后再进入 <code>b()</code> 不会死锁。</p><p>底层 Monitor 会记录：</p><pre><code>持有锁的线程<br/>重入次数</code></pre><p>每进入一次，计数加 1；每退出一次，计数减 1；减到 0 才真正释放锁。</p><ol><li>synchronized 锁升级</li></ol><p>JVM 对 <code>synchronized</code> 做了很多优化。</p><p>传统说法包括：</p><pre><code>无锁<br/>偏向锁<br/>轻量级锁<br/>重量级锁</code></pre><p>锁会根据竞争情况升级。</p><p>大致是：</p><pre><code>没有竞争：偏向锁<br/>轻微竞争：轻量级锁，CAS 自旋<br/>竞争激烈：重量级锁，线程阻塞</code></pre><p>注意：偏向锁在新版本 JDK 中已经被废弃或移除，但面试中仍然常问历史机制，可以说明版本差异。</p><ol><li>轻量级锁和重量级锁区别</li></ol><p>轻量级锁：</p><pre><code>竞争不激烈<br/>主要用 CAS 和自旋<br/>避免线程阻塞和内核态切换</code></pre><p>重量级锁：</p><pre><code>竞争激烈<br/>线程阻塞挂起<br/>需要操作系统参与调度<br/>开销更大</code></pre><p>所以 synchronized 不是一上来就很重，JVM 已经做了优化。</p><ol><li>synchronized 和 Lock 的区别简单提一句</li></ol><p><code>synchronized</code>：</p><pre><code>JVM 内置<br/>自动释放锁<br/>语法简单<br/>不可中断等待<br/>不能公平控制</code></pre><p><code>ReentrantLock</code>：</p><pre><code>JUC 提供<br/>需要手动 unlock<br/>支持公平锁<br/>支持可中断<br/>支持超时获取锁<br/>支持多个 Condition</code></pre><ol><li><strong>面试背诵版：synchronized 原理是什么？</strong></li></ol><p><code>synchronized</code> 是 Java 的内置锁机制，底层基于对象监视器 Monitor 实现互斥。每个 Java 对象都可以关联一个 Monitor，线程进入 <code>synchronized</code> 代码块时，需要先获取锁对象对应的 Monitor，获取成功才能进入临界区，获取失败就阻塞等待；退出同步代码块时释放 Monitor。同步代码块在字节码层面通过 <code>monitorenter</code> 和 <code>monitorexit</code> 实现，编译器还会生成异常处理逻辑，保证异常情况下也能释放锁；同步方法则通过方法访问标志 <code>ACC_SYNCHRONIZED</code> 实现，普通同步方法锁的是 <code>this</code>，静态同步方法锁的是当前类的 <code>Class</code> 对象。<code>synchronized</code> 可以保证原子性，因为同一时刻只有一个线程进入临界区；也能保证可见性和有序性，因为释放锁前的修改会对后续获取同一把锁的线程可见，并形成 happens-before 关系。它还是可重入锁，同一个线程重复获取同一把锁不会死锁，Monitor 会记录持有线程和重入次数。JVM 对 <code>synchronized</code> 做了锁优化，比如轻量级锁、自旋和重量级锁等，竞争不激烈时不一定会直接阻塞线程。</p>"},{"t":"synchronized 锁升级过程是什么？","tag":"高频","p":"high","html":""},{"t":"volatile 原理是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p><code>volatile</code> 原理的核心是：通过<strong>内存屏障和缓存一致性机制</strong>，保证变量的可见性和一定程度的<strong>有序性</strong>，但<strong>不保证复合操作的原子性</strong>。</p><ol><li>volatile 是什么？</li></ol><p><code>volatile</code> 是 Java 提供的轻量级同步机制。</p><p>可以修饰变量：</p><pre><code>private volatile boolean running = true;</code></pre><p>它主要保证两件事：</p><pre><code>可见性<br/>禁止特定指令重排序</code></pre><p>但它不保证：</p><pre><code>复合操作的原子性</code></pre><ol><li>什么是可见性？</li></ol><p>多线程中，每个线程可能把共享变量缓存到自己的工作内存或 CPU 缓存中。</p><p>一个线程修改了变量，另一个线程不一定马上看到。</p><p>比如：</p><pre><code>boolean running = true;<br/><br/>while (running) {<br/>    // do something<br/>}</code></pre><p>如果一个线程把 <code>running</code> 改成 <code>false</code>，另一个线程可能一直读到旧值 <code>true</code>。</p><p>加上 <code>volatile</code> 后：</p><pre><code>volatile boolean running = true;</code></pre><p>一个线程修改后，其他线程能及时看到最新值。</p><ol><li>volatile 如何保证可见性？</li></ol><p>简单理解：</p><pre><code>写 volatile 变量：<br/>会把修改刷新到主内存<br/><br/>读 volatile 变量：<br/>会从主内存读取最新值</code></pre><p>底层通过：</p><pre><code>内存屏障<br/>CPU 缓存一致性协议</code></pre><p>来保证。</p><p>所以 <code>volatile</code> 能解决“一个线程改了，其他线程看不见”的问题。</p><ol><li>什么是禁止重排序？</li></ol><p>为了优化性能，编译器和 CPU 可能会调整指令执行顺序。</p><p>只要单线程结果不变，就可能重排序。</p><p>比如：</p><pre><code>a = 1;<br/>flag = true;</code></pre><p>如果没有约束，可能被重排序成：</p><pre><code>flag = true;<br/>a = 1;</code></pre><p>多线程下另一个线程看到 <code>flag = true</code>，但可能还看不到 <code>a = 1</code>。</p><p><code>volatile</code> 可以禁止与 volatile 变量相关的特定重排序。</p><ol><li>volatile 如何保证有序性？</li></ol><p>JVM 会在 volatile 读写前后插入内存屏障。</p><p>简单理解：</p><pre><code>volatile 写之前的普通写，不能重排序到 volatile 写之后<br/>volatile 读之后的普通读，不能重排序到 volatile 读之前</code></pre><p>例如：</p><pre><code>a = 1;<br/>volatileFlag = true;</code></pre><p><code>volatileFlag = true</code> 之前的写操作，对其他线程读到 <code>volatileFlag</code> 后是可见的。</p><ol><li>happens-before 规则</li></ol><p>Java 内存模型中有一条规则：</p><pre><code>对 volatile 变量的写 happens-before 后续对这个变量的读</code></pre><p>意思是：</p><pre><code>线程 A 写 volatile 变量前的操作<br/>对<br/>线程 B 读到这个 volatile 变量后的操作<br/>可见</code></pre><p>例子：</p><pre><code>int data = 0;<br/>volatile boolean ready = false;<br/><br/>// 线程 A<br/>data = 100;<br/>ready = true;<br/><br/>// 线程 B<br/>if (ready) {<br/>    System.out.println(data); // 能看到 100<br/>}</code></pre><p>如果线程 B 读到 <code>ready == true</code>，那么它也能看到线程 A 在写 <code>ready</code> 之前对 <code>data</code> 的修改。</p><ol><li>volatile 不保证原子性</li></ol><p>这是面试重点。</p><p>比如：</p><pre><code>volatile int count = 0;<br/><br/>count++;</code></pre><p><code>count++</code> 不是原子操作，它大概分成三步：</p><pre><code>读取 count<br/>加 1<br/>写回 count</code></pre><p>多个线程同时执行，仍然可能丢失更新。</p><p>所以 <code>volatile</code> 不能保证 <code>count++</code> 线程安全。</p><p>这种场景要用：</p><pre><code>synchronized<br/>Lock<br/>AtomicInteger<br/>LongAdder</code></pre><ol><li>volatile 适合什么场景？</li></ol><p>适合一个线程写、多个线程读，或者状态标记场景。</p><p>比如：</p><pre><code>volatile boolean stop = false;</code></pre><p>一个线程修改停止标志：</p><pre><code>stop = true;</code></pre><p>另一个线程感知：</p><pre><code>while (!stop) {<br/>    ...<br/>}</code></pre><p>还适合：</p><pre><code>配置开关<br/>状态标志<br/>双重检查锁中的单例引用</code></pre><ol><li>双重检查锁为什么要 volatile？</li></ol><p>比如单例：</p><pre><code>class Singleton {<br/>    private static volatile Singleton instance;<br/><br/>    public static Singleton getInstance() {<br/>        if (instance == null) {<br/>            synchronized (Singleton.class) {<br/>                if (instance == null) {<br/>                    instance = new Singleton();<br/>                }<br/>            }<br/>        }<br/>        return instance;<br/>    }<br/>}</code></pre><p><code>volatile</code> 防止对象创建过程重排序。</p><p>对象创建大致可能包括：</p><pre><code>分配内存<br/>初始化对象<br/>引用指向内存</code></pre><p>如果重排序成“引用先指向内存，初始化还没完成”，其他线程可能拿到半初始化对象。</p><p><code>volatile</code> 可以避免这个问题。</p><ol><li>volatile 和 synchronized 区别</li></ol><pre><code>volatile：<br/>轻量<br/>保证可见性和有序性<br/>不保证复合操作原子性<br/>不能保护临界区<br/><br/>synchronized：<br/>较重<br/>保证原子性、可见性、有序性<br/>可以保护一段代码<br/>会涉及锁竞争</code></pre><p>所以：</p><pre><code>状态标志用 volatile<br/>复合操作和临界区用 synchronized / Lock</code></pre><ol><li><strong>面试背诵版：volatile 原理是什么？</strong></li></ol><p><code>volatile</code> 是 Java 提供的轻量级同步机制，主要保证变量的可见性和有序性，但不保证复合操作的原子性。可见性是指一个线程修改了 <code>volatile</code> 变量后，其他线程能及时看到最新值；底层主要依赖内存屏障和 CPU 缓存一致性机制，写 <code>volatile</code> 变量时会把修改刷新到主内存，读 <code>volatile</code> 变量时会读取最新值。有序性是指 <code>volatile</code> 可以禁止与该变量相关的特定指令重排序，JVM 会在 volatile 读写前后插入内存屏障，并且 Java 内存模型规定，对一个 volatile 变量的写 happens-before 后续对这个变量的读。因此，如果线程 A 在写 volatile 标志前修改了普通变量，线程 B 读到这个 volatile 标志后，也能看到之前普通变量的修改。需要注意，<code>volatile</code> 不保证原子性，比如 <code>count++</code> 包含读取、加一、写回三个步骤，多线程下仍然可能丢失更新。它适合状态标志、配置开关、一个线程写多个线程读的场景；如果要保护复合操作，需要使用 <code>synchronized</code>、<code>Lock</code> 或原子类。</p>"},{"t":"volatile 为什么不能保证原子性？","tag":"高频","p":"high","html":""},{"t":"CAS 是什么？","tag":"面试必考","p":"must","html":""},{"t":"ABA 问题是什么？如何解决？","tag":"高频","p":"high","html":""},{"t":"AQS 原理是什么？","tag":"面试必考","p":"must","html":""},{"t":"ReentrantLock 和 synchronized 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p></p>"},{"t":"ThreadLocal 原理是什么？","tag":"高频","p":"high","html":""},{"t":"ThreadLocal 为什么会内存泄漏？","tag":"高频","p":"high","html":""}]},{"no":"六","title":"线程池","questions":[{"t":"为什么需要线程池？","tag":"必须掌握","p":"core","html":"<span>[原文档此处有配图]</span><p><code>ReentrantLock</code> 和 <code>synchronized</code> 的核心区别是：<code>synchronized</code> 是 <strong>JVM 内置锁，使用简单、自动释放</strong>；<code>ReentrantLock</code> 是 JUC 提供的<strong>显式锁，功能更强</strong>，比如<strong>可中断、公平锁、超时获取锁和多个条件队列</strong>。</p><ol><li>来源不同</li></ol><p><code>synchronized</code> 是 Java 语言级关键字。</p><pre><code>synchronized (lock) {<br/>    // 临界区<br/>}</code></pre><p>它由 JVM 实现，底层和对象 Monitor 有关。</p><p><code>ReentrantLock</code> 是 <code>java.util.concurrent.locks</code> 包下的类。</p><pre><code>ReentrantLock lock = new ReentrantLock();<br/><br/>lock.lock();<br/>try {<br/>    // 临界区<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>它基于 AQS 实现。</p><ol><li>加锁和释放锁方式不同</li></ol><p><code>synchronized</code> 是隐式加锁和释放锁。</p><p>进入同步块自动加锁，退出同步块自动释放。</p><pre><code>synchronized (lock) {<br/>    doSomething();<br/>}</code></pre><p>即使发生异常，JVM 也会保证释放锁。</p><p><code>ReentrantLock</code> 需要手动释放锁。</p><pre><code>lock.lock();<br/>try {<br/>    doSomething();<br/>} finally {<br/>    lock.unlock();<br/>}</code></pre><p>必须写在 <code>finally</code> 里，否则异常时可能导致锁不释放。</p><ol><li>是否可重入</li></ol><p>两者都是可重入锁。</p><p>也就是说，同一个线程拿到锁后，可以再次获取同一把锁，不会死锁。</p><pre><code>public synchronized void a() {<br/>    b();<br/>}<br/><br/>public synchronized void b() {<br/>}</code></pre><p><code>ReentrantLock</code> 也是一样，内部会记录持有线程和重入次数。</p><ol><li>是否支持公平锁</li></ol><p><code>synchronized</code> 不支持公平锁。</p><p>它的线程竞争由 JVM 调度，不保证先等待的线程先拿到锁。</p><p><code>ReentrantLock</code> 支持公平锁和非公平锁。</p><pre><code>ReentrantLock fairLock = new ReentrantLock(true);<br/>ReentrantLock nonFairLock = new ReentrantLock(false);</code></pre><p>默认是非公平锁。</p><p>公平锁：</p><pre><code>等待时间最长的线程优先获取锁</code></pre><p>非公平锁：</p><pre><code>新来的线程可以尝试插队<br/>吞吐量通常更高</code></pre><ol><li>是否支持可中断获取锁</li></ol><p><code>synchronized</code> 等锁时不能响应中断。</p><p>线程如果阻塞在进入 <code>synchronized</code> 的过程中，不能通过中断让它退出等待。</p><p><code>ReentrantLock</code> 支持可中断锁：</p><pre><code>lock.lockInterruptibly();</code></pre><p>如果线程等待锁期间被中断，可以抛出 <code>InterruptedException</code>，不再继续等。</p><p>这在避免死锁时很有用。</p><ol><li>是否支持超时获取锁</li></ol><p><code>synchronized</code> 不支持尝试一段时间拿锁。</p><p>要么拿到锁，要么一直阻塞。</p><p><code>ReentrantLock</code> 支持 <code>tryLock()</code>。</p><pre><code>if (lock.tryLock()) {<br/>    try {<br/>        // 拿到锁<br/>    } finally {<br/>        lock.unlock();<br/>    }<br/>}</code></pre><p>也支持超时：</p><pre><code>if (lock.tryLock(3, TimeUnit.SECONDS)) {<br/>    try {<br/>        // 3 秒内拿到锁<br/>    } finally {<br/>        lock.unlock();<br/>    }<br/>}</code></pre><p>如果拿不到锁，可以放弃或降级处理。</p><ol><li>条件队列不同</li></ol><p><code>synchronized</code> 配合：</p><pre><code>wait()<br/>notify()<br/>notifyAll()</code></pre><p>它只有一个等待队列。</p><p><code>ReentrantLock</code> 配合 <code>Condition</code> 使用。</p><pre><code>Condition notEmpty = lock.newCondition();<br/>Condition notFull = lock.newCondition();</code></pre><p>它可以创建多个条件队列。</p><p>这在生产者消费者模型里很有用：</p><pre><code>notEmpty：队列非空条件<br/>notFull：队列未满条件</code></pre><p>可以更精确地唤醒线程。</p><ol><li>性能区别</li></ol><p>早期 <code>ReentrantLock</code> 性能比 <code>synchronized</code> 更好。</p><p>但 JDK 6 以后，<code>synchronized</code> 做了很多优化，比如：</p><pre><code>锁消除<br/>锁粗化<br/>轻量级锁<br/>自旋优化<br/>偏向锁（老版本）</code></pre><p>所以现在普通场景下，两者性能差距不一定明显。</p><p>选择时更看功能需求，而不是单纯性能。</p><ol><li>使用建议</li></ol><p>简单同步场景：</p><pre><code>优先 synchronized</code></pre><p>因为语法简单，自动释放锁，不容易写错。</p><p>复杂同步场景：</p><pre><code>使用 ReentrantLock</code></pre><p>比如需要：</p><pre><code>公平锁<br/>可中断等待<br/>超时获取锁<br/>多个 Condition<br/>手动控制锁释放</code></pre><ol><li>对比总结</li></ol><pre><code>synchronized：<br/>JVM 内置<br/>自动释放锁<br/>使用简单<br/>不可中断等待<br/>不支持公平锁<br/>一个等待队列<br/>适合简单同步<br/><br/>ReentrantLock：<br/>JUC 显式锁<br/>需要手动 unlock<br/>基于 AQS<br/>支持公平锁<br/>支持 lockInterruptibly<br/>支持 tryLock 超时<br/>支持多个 Condition<br/>适合复杂并发控制</code></pre><ol><li><strong>面试背诵版：ReentrantLock 和 synchronized 有什么区别？</strong></li></ol><p><code>ReentrantLock</code> 和 <code>synchronized</code> 都是可重入锁，都可以保证临界区的线程安全，但实现和功能不同。<code>synchronized</code> 是 Java 语言级关键字，由 JVM 实现，底层基于对象 Monitor，进入同步块自动加锁，退出时自动释放锁，即使发生异常也能释放，使用简单；<code>ReentrantLock</code> 是 JUC 包下的显式锁，基于 AQS 实现，需要手动调用 <code>lock()</code> 和 <code>unlock()</code>，通常必须在 <code>finally</code> 中释放。功能上，<code>ReentrantLock</code> 更灵活，它支持公平锁和非公平锁，支持 <code>lockInterruptibly()</code> 让等待锁的线程响应中断，支持 <code>tryLock()</code> 和超时获取锁，还支持多个 <code>Condition</code> 条件队列，可以更精确地控制线程等待和唤醒；而 <code>synchronized</code> 不支持公平锁、超时获取锁和可中断等待，只能配合一个对象的 <code>wait/notify/notifyAll</code>。性能上，JDK 6 之后 <code>synchronized</code> 做了很多优化，普通场景下性能差距不大。实际开发中，简单同步优先用 <code>synchronized</code>，需要公平锁、超时、可中断或多个条件队列时使用 <code>ReentrantLock</code>。</p>"},{"t":"ThreadPoolExecutor 核心参数有哪些？","tag":"面试必考","p":"must","html":""},{"t":"线程池执行流程是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>线程池执行流程的核心是：提交任务后，<strong>线程池先看核心线程是否满</strong>，<strong>再看队列是否能放</strong>，再看能否创建<strong>非核心线程</strong>，最后<strong>才触发拒绝策略</strong>。</p><ol><li>先看线程池核心参数</li></ol><p>理解执行流程前，要知道几个参数：</p><pre><code>corePoolSize：核心线程数<br/>maximumPoolSize：最大线程数<br/>workQueue：任务队列<br/>keepAliveTime：非核心线程空闲存活时间<br/>RejectedExecutionHandler：拒绝策略</code></pre><p>线程池执行任务就是围绕这些参数来判断。</p><ol><li>提交任务后第一步：核心线程是否已满？</li></ol><p>当调用：</p><pre><code>executor.execute(task);</code></pre><p>线程池会先判断当前运行线程数是否小于 <code>corePoolSize</code>。</p><p>如果小于：</p><pre><code>直接创建核心线程执行任务</code></pre><p>即使其他核心线程空闲，也可能优先创建新核心线程，直到达到核心线程数。</p><pre><code>当前线程数 &lt; corePoolSize<br/>  ↓<br/>创建核心线程执行任务</code></pre><ol><li>第二步：核心线程满了，任务进队列</li></ol><p>如果当前线程数已经达到 <code>corePoolSize</code>，新任务不会马上创建非核心线程。</p><p>而是先尝试放入任务队列 <code>workQueue</code>。</p><pre><code>当前线程数 &gt;= corePoolSize<br/>  ↓<br/>尝试放入任务队列</code></pre><p>如果队列没满：</p><pre><code>任务进入队列等待<br/>核心线程从队列中取任务执行</code></pre><ol><li>第三步：队列满了，创建非核心线程</li></ol><p>如果任务队列也满了，线程池才会判断当前线程数是否小于 <code>maximumPoolSize</code>。</p><p>如果小于：</p><pre><code>创建非核心线程执行任务</code></pre><p>流程：</p><pre><code>队列满<br/>  ↓<br/>当前线程数 &lt; maximumPoolSize<br/>  ↓<br/>创建非核心线程执行任务</code></pre><p>注意：不是核心线程满了就立刻创建非核心线程，而是队列也满了才会创建。</p><ol><li>第四步：线程数也满了，触发拒绝策略</li></ol><p>如果队列满了，并且当前线程数已经达到 <code>maximumPoolSize</code>，线程池无法再接收任务。</p><p>这时会执行拒绝策略。</p><pre><code>队列满<br/>线程数 &gt;= maximumPoolSize<br/>  ↓<br/>触发拒绝策略</code></pre><p>常见拒绝策略：</p><pre><code>AbortPolicy：直接抛异常，默认策略<br/>CallerRunsPolicy：由提交任务的线程自己执行<br/>DiscardPolicy：直接丢弃任务，不抛异常<br/>DiscardOldestPolicy：丢弃队列中最老任务，再尝试提交新任务</code></pre><ol><li>完整执行流程图</li></ol><pre><code>提交任务<br/>  ↓<br/>当前线程数 &lt; corePoolSize？<br/>  ├── 是：创建核心线程执行任务<br/>  ↓ 否<br/>任务队列是否未满？<br/>  ├── 是：放入队列等待执行<br/>  ↓ 否<br/>当前线程数 &lt; maximumPoolSize？<br/>  ├── 是：创建非核心线程执行任务<br/>  ↓ 否<br/>触发拒绝策略</code></pre><ol><li>线程执行完任务后做什么？</li></ol><p>线程执行完当前任务后，不会马上销毁。</p><p>它会继续从任务队列取任务。</p><pre><code>线程执行任务<br/>  ↓<br/>任务完成<br/>  ↓<br/>从 workQueue 取下一个任务</code></pre><p>核心线程默认一直存活。</p><p>非核心线程如果空闲超过 <code>keepAliveTime</code>，就会被回收。</p><ol><li>核心线程会不会被回收？</li></ol><p>默认情况下：</p><pre><code>核心线程不会被回收</code></pre><p>即使空闲，也会保留在线程池中。</p><p>但可以设置：</p><pre><code>allowCoreThreadTimeOut(true);</code></pre><p>这样核心线程空闲超过 <code>keepAliveTime</code> 也可以被回收。</p><ol><li>不同队列会影响执行流程</li></ol><p>线程池行为和队列类型关系很大。</p><p>常见队列：</p><pre><code>ArrayBlockingQueue：<br/>有界队列，满了后可能创建非核心线程<br/><br/>LinkedBlockingQueue：<br/>默认容量很大，可能导致任务堆积，不容易创建非核心线程<br/><br/>SynchronousQueue：<br/>不存任务，任务必须直接交给线程，常用于 cached 线程池<br/><br/>PriorityBlockingQueue：<br/>优先级队列，按优先级执行任务</code></pre><p>所以线程池参数不能只看线程数，队列也很关键。</p><ol><li>面试容易踩坑</li></ol><p>不要说：</p><pre><code>核心线程满了就创建非核心线程</code></pre><p>更准确是：</p><pre><code>核心线程满了先入队，队列满了才创建非核心线程。</code></pre><p>除非使用的是 <code>SynchronousQueue</code>，它不存储任务，所以会更快触发创建新线程。</p><ol><li><strong>面试背诵版：线程池执行流程是什么？</strong></li></ol><p>线程池执行流程可以概括为四步。第一，当提交一个任务时，线程池先判断当前线程数是否小于 <code>corePoolSize</code>，如果小于，就创建核心线程执行任务。第二，如果核心线程数已经满了，就尝试把任务放入 <code>workQueue</code> 任务队列；如果队列没满，任务就在队列中等待核心线程取出执行。第三，如果任务队列也满了，线程池会判断当前线程数是否小于 <code>maximumPoolSize</code>，如果小于，就创建非核心线程来执行任务。第四，如果队列满了，并且线程数也已经达到最大线程数，就触发拒绝策略，比如默认的 <code>AbortPolicy</code> 会抛异常，<code>CallerRunsPolicy</code> 会让提交任务的线程自己执行。任务执行完后，线程不会马上销毁，而是继续从队列中取任务；核心线程默认常驻，非核心线程空闲超过 <code>keepAliveTime</code> 后会被回收。需要注意，线程池不是核心线程满了就立刻创建非核心线程，而是核心线程满了先入队，队列满了才创建非核心线程。</p>"},{"t":"线程池有哪些拒绝策略？","tag":"高频","p":"high","html":""},{"t":"为什么不推荐使用 Executors 创建线程池？","tag":"高频","p":"high","html":""},{"t":"如何合理设置线程池大小？","tag":"高频","p":"high","html":""}]},{"no":"七","title":"Java IO/NIO","questions":[{"t":"BIO、NIO、AIO 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>BIO、NIO、AIO 的核心区别是：<strong>BIO 是阻塞 IO，一个连接通常对应一个线程</strong>；<strong>NIO 是非阻塞 IO，可以通过 Selector 一个线程管理多个连接</strong>；<strong>AIO 是异步 IO，操作系统完成 IO 后再通知应用。</strong></p><ol><li>BIO 是什么？</li></ol><p>BIO 全称是 Blocking IO，阻塞 IO。</p><p>特点是：</p><pre><code>读写会阻塞<br/>一个连接通常对应一个线程</code></pre><p>比如服务端调用：</p><pre><code>socket.accept();<br/>inputStream.read();</code></pre><p>如果没有连接进来，<code>accept()</code> 会阻塞。</p><p>如果没有数据可读，<code>read()</code> 会阻塞。</p><p>流程：</p><pre><code>客户端连接<br/>  ↓<br/>服务端分配一个线程<br/>  ↓<br/>线程阻塞等待数据<br/>  ↓<br/>读到数据后处理</code></pre><ol><li>BIO 的优缺点</li></ol><p>优点：</p><pre><code>模型简单<br/>代码容易写<br/>适合连接数少、并发不高的场景</code></pre><p>缺点：</p><pre><code>线程资源消耗大<br/>大量连接时线程数暴涨<br/>上下文切换成本高<br/>不适合高并发长连接</code></pre><p>比如 1 万个连接，如果每个连接一个线程，系统压力会很大。</p><ol><li>NIO 是什么？</li></ol><p>NIO 可以理解为 Non-blocking IO，也常说 New IO。</p><p>它的特点是：</p><pre><code>非阻塞<br/>基于 Buffer 和 Channel<br/>配合 Selector 实现 IO 多路复用<br/>一个线程可以管理多个连接</code></pre><p>NIO 里几个核心组件：</p><pre><code>Channel：数据通道<br/>Buffer：数据缓冲区<br/>Selector：多路复用器</code></pre><ol><li>NIO 如何处理多个连接？</li></ol><p>NIO 可以把多个 Channel 注册到 Selector 上。</p><p>Selector 监听这些 Channel 的事件：</p><pre><code>连接事件<br/>读事件<br/>写事件</code></pre><p>流程：</p><pre><code>多个连接注册到 Selector<br/>  ↓<br/>Selector 监听事件<br/>  ↓<br/>哪个 Channel 就绪<br/>  ↓<br/>线程处理哪个 Channel</code></pre><p>图：</p><pre><code>线程<br/>  ↓<br/>Selector<br/>  ├── Channel 1<br/>  ├── Channel 2<br/>  ├── Channel 3<br/>  └── Channel 4</code></pre><p>所以一个线程可以处理大量连接。</p><ol><li>NIO 的优缺点</li></ol><p>优点：</p><pre><code>适合高并发<br/>线程数量少<br/>资源消耗低<br/>适合长连接</code></pre><p>缺点：</p><pre><code>编程复杂<br/>要处理事件、Buffer、半包粘包<br/>代码维护成本高</code></pre><p>所以实际项目常用 Netty 这类框架封装 NIO。</p><ol><li>AIO 是什么？</li></ol><p>AIO 全称是 Asynchronous IO，异步 IO。</p><p>特点是：</p><pre><code>应用发起 IO 操作后立即返回<br/>操作系统完成 IO 后通知应用</code></pre><p>也就是说，应用线程不用自己一直轮询或等待。</p><p>流程：</p><pre><code>应用发起异步读<br/>  ↓<br/>立即返回<br/>  ↓<br/>操作系统完成数据读取<br/>  ↓<br/>回调通知应用处理结果</code></pre><p>Java 里 AIO 对应：</p><pre><code>AsynchronousSocketChannel<br/>AsynchronousServerSocketChannel</code></pre><ol><li>AIO 和 NIO 的区别</li></ol><p>NIO 是非阻塞，但应用仍然要通过 Selector 关注 IO 是否就绪。</p><pre><code>NIO：<br/>我问操作系统：哪些连接准备好了？<br/>准备好了我自己去读写。</code></pre><p>AIO 是异步，应用发起操作后，等操作系统完成再通知。</p><pre><code>AIO：<br/>我告诉操作系统：帮我读。<br/>读完了你通知我。</code></pre><p>简单说：</p><pre><code>NIO 关注的是“就绪”<br/>AIO 关注的是“完成”</code></pre><ol><li>三者对比</li></ol><pre><code>BIO：<br/>同步阻塞<br/>一个连接一个线程<br/>实现简单<br/>并发能力差<br/><br/>NIO：<br/>同步非阻塞<br/>一个线程管理多个连接<br/>基于 Selector<br/>并发能力强<br/>编程复杂<br/><br/>AIO：<br/>异步非阻塞<br/>操作系统完成后通知应用<br/>理论上更彻底异步<br/>使用场景相对少</code></pre><ol><li>使用场景</li></ol><pre><code>BIO：<br/>小规模连接、传统阻塞服务、简单内部工具<br/><br/>NIO：<br/>高并发网络服务、长连接、网关、Netty、Redis 类模型<br/><br/>AIO：<br/>异步文件或网络 IO，适合操作系统异步 IO 支持好的场景</code></pre><p>在 Java 网络编程里，实际高性能网络框架更多基于 NIO，比如 Netty。</p><ol><li>面试常见补充</li></ol><p>可以说：</p><pre><code>BIO 是阻塞等待<br/>NIO 是就绪通知<br/>AIO 是完成通知</code></pre><p>再进一步：</p><pre><code>BIO：线程等数据<br/>NIO：线程问谁准备好了<br/>AIO：系统读完后通知线程</code></pre><ol><li><strong>面试背诵版：BIO、NIO、AIO 有什么区别？</strong></li></ol><p>BIO、NIO、AIO 是 Java 中三种不同的 IO 模型。BIO 是同步阻塞 IO，读写操作会阻塞线程，服务端通常是一个连接对应一个线程，模型简单但线程资源消耗大，不适合高并发连接。NIO 是同步非阻塞 IO，基于 Channel、Buffer 和 Selector，一个线程可以通过 Selector 监听多个 Channel 的连接、读、写事件，哪个连接就绪就处理哪个连接，所以更适合高并发和长连接场景，但编程复杂，需要处理 Buffer、事件和粘包拆包等问题，实际项目中常用 Netty 来封装 NIO。AIO 是异步 IO，应用发起 IO 操作后立即返回，真正的读写由操作系统完成，完成后通过回调通知应用处理结果。简单来说，BIO 是线程阻塞等待数据，NIO 是线程监听哪些连接已经就绪，AIO 是操作系统完成 IO 后通知应用。实际高性能 Java 网络框架中，NIO 使用最广泛。</p><span>[原文档此处有配图]</span>"},{"t":"什么是 IO 多路复用？","tag":"高频","p":"high","html":""},{"t":"Selector 原理是什么？","tag":"高频","p":"high","html":""},{"t":"Java NIO 为什么性能更高？","tag":"高频","p":"high","html":""},{"t":"零拷贝是什么？","tag":"高频","p":"high","html":""}]},{"no":"八","title":"Spring","questions":[{"t":"Spring IOC 是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>Spring IOC 的核心是：把对象的创建、依赖注入和生命周期管理交给 Spring 容器，而<strong>不是由程序自己 </strong><strong><code>new</code></strong><strong> 对象。</strong></p><ol><li>IOC 是什么？</li></ol><p>IOC 全称是 Inversion of Control，控制反转。</p><p>意思是：对象控制权反转了。</p><p>传统写法是自己创建依赖：</p><pre><code>class UserService {<br/>    private UserDao userDao = new UserDao();<br/>}</code></pre><p>这种方式里，<code>UserService</code> 自己控制 <code>UserDao</code> 的创建。</p><p>IOC 之后：</p><pre><code>class UserService {<br/>    private UserDao userDao;<br/><br/>    public UserService(UserDao userDao) {<br/>        this.userDao = userDao;<br/>    }<br/>}</code></pre><p><code>UserDao</code> 由 Spring 容器创建并注入给 <code>UserService</code>。</p><ol><li>控制权反转了什么？</li></ol><p>反转的是对象创建和依赖管理的控制权。</p><p>原来：</p><pre><code>对象自己 new 依赖<br/>对象自己管理依赖</code></pre><p>现在：</p><pre><code>Spring 容器创建对象<br/>Spring 容器组装依赖<br/>Spring 容器管理生命周期</code></pre><p>所以叫控制反转。</p><ol><li>DI 是什么？</li></ol><p>DI 全称是 Dependency Injection，依赖注入。</p><p>它是实现 IOC 的主要方式。</p><p>IOC 是思想，DI 是实现手段。</p><pre><code>IOC：控制权交给容器<br/>DI：容器把依赖注入进来</code></pre><p>比如：</p><pre><code>@Service<br/>public class UserService {<br/>    private final UserDao userDao;<br/><br/>    public UserService(UserDao userDao) {<br/>        this.userDao = userDao;<br/>    }<br/>}</code></pre><p>Spring 会自动把 <code>UserDao</code> 注入到 <code>UserService</code> 中。</p><ol><li>Spring 容器是什么？</li></ol><p>Spring IOC 容器负责管理 Bean。</p><p>Bean 就是被 Spring 管理的对象。</p><p>常见容器接口：</p><pre><code>BeanFactory<br/>ApplicationContext</code></pre><p><code>BeanFactory</code> 是基础容器。</p><p><code>ApplicationContext</code> 是更常用的高级容器，提供更多功能，比如：</p><pre><code>国际化<br/>事件发布<br/>资源加载<br/>AOP 支持<br/>自动装配</code></pre><ol><li>Bean 是怎么来的？</li></ol><p>常见方式：</p><pre><code>@Component<br/>@Service<br/>@Repository<br/>@Controller<br/>@Configuration<br/>@Bean</code></pre><p>比如：</p><pre><code>@Service<br/>public class UserService {<br/>}</code></pre><p>Spring 启动时扫描到这个类，就会把它注册成 Bean。</p><p>或者：</p><pre><code>@Configuration<br/>public class AppConfig {<br/>    @Bean<br/>    public UserDao userDao() {<br/>        return new UserDao();<br/>    }<br/>}</code></pre><ol><li>Spring IOC 做了什么？</li></ol><p>Spring IOC 容器主要做：</p><pre><code>扫描 Bean 定义<br/>创建 Bean 对象<br/>解析 Bean 之间依赖<br/>进行依赖注入<br/>管理 Bean 生命周期<br/>处理 BeanPostProcessor<br/>支持 AOP 代理<br/>销毁 Bean</code></pre><p>简单流程：</p><pre><code>读取配置 / 扫描注解<br/>  ↓<br/>生成 BeanDefinition<br/>  ↓<br/>创建 Bean<br/>  ↓<br/>依赖注入<br/>  ↓<br/>初始化<br/>  ↓<br/>放入容器<br/>  ↓<br/>使用 Bean</code></pre><ol><li>IOC 有什么好处？</li></ol><p>主要好处：</p><pre><code>降低耦合<br/>提高扩展性<br/>方便测试<br/>统一管理对象生命周期<br/>方便 AOP、事务、缓存等增强</code></pre><p>比如不使用 IOC：</p><pre><code>private PayService payService = new AliPayService();</code></pre><p>代码依赖具体实现。</p><p>使用 IOC：</p><pre><code>private PayService payService;</code></pre><p>具体注入哪个实现由配置或容器决定。</p><p>新增微信支付时，不需要大改业务代码。</p><ol><li>常见注入方式</li></ol><p>Spring 常见依赖注入方式：</p><pre><code>构造器注入<br/>Setter 注入<br/>字段注入</code></pre><p>构造器注入：</p><pre><code>@Service<br/>public class UserService {<br/>    private final UserDao userDao;<br/><br/>    public UserService(UserDao userDao) {<br/>        this.userDao = userDao;<br/>    }<br/>}</code></pre><p>Setter 注入：</p><pre><code>@Autowired<br/>public void setUserDao(UserDao userDao) {<br/>    this.userDao = userDao;<br/>}</code></pre><p>字段注入：</p><pre><code>@Autowired<br/>private UserDao userDao;</code></pre><p>实际开发更推荐构造器注入，因为依赖更明确，也方便测试。</p><ol><li>面试常见追问</li></ol><p>可以补充：</p><pre><code>IOC 是思想<br/>DI 是实现方式<br/>Bean 是容器管理的对象<br/>ApplicationContext 是常用 IOC 容器<br/>Spring 通过 BeanDefinition 描述 Bean<br/>容器负责创建对象、注入依赖、管理生命周期</code></pre><ol><li><strong>面试背诵版：Spring IOC 是什么？</strong></li></ol><p>Spring IOC 是控制反转，核心思想是把对象的创建、依赖注入和生命周期管理交给 Spring 容器，而不是由程序自己 <code>new</code> 对象。传统开发中，一个类通常自己创建依赖对象，这会导致类和具体实现强耦合；使用 IOC 后，Spring 容器负责创建 Bean，并把需要的依赖注入进去，业务类只依赖接口或抽象，从而降低耦合。DI，也就是依赖注入，是实现 IOC 的主要方式，IOC 是思想，DI 是手段。Spring IOC 容器常见接口有 <code>BeanFactory</code> 和 <code>ApplicationContext</code>，实际开发中更多使用 <code>ApplicationContext</code>。容器启动时会扫描配置或注解，生成 <code>BeanDefinition</code>，然后创建 Bean、注入依赖、执行初始化、应用 BeanPostProcessor，最后把 Bean 放入容器中管理。IOC 的好处是降低耦合、提高扩展性、方便测试，并且为 AOP、事务、缓存等能力提供基础。</p>"},{"t":"Spring Bean 生命周期是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>Spring Bean 生命周期的核心是：<strong>Spring 容器从 Bean 定义开始</strong>，<strong>经历实例化、属性注入、初始化、使用、销毁</strong>这几个阶段。</p><ol><li>先说总体流程</li></ol><p>一个 Spring Bean 的生命周期大致是：</p><pre><code>读取 BeanDefinition<br/>  ↓<br/>实例化 Bean<br/>  ↓<br/>属性填充 / 依赖注入<br/>  ↓<br/>Aware 回调<br/>  ↓<br/>BeanPostProcessor 前置处理<br/>  ↓<br/>初始化方法<br/>  ↓<br/>BeanPostProcessor 后置处理<br/>  ↓<br/>Bean 可以使用<br/>  ↓<br/>容器关闭时销毁 Bean</code></pre><p>简单记：</p><pre><code>实例化 → 依赖注入 → 初始化 → 使用 → 销毁</code></pre><ol><li>第一步：读取 BeanDefinition</li></ol><p>Spring 启动时，会根据：</p><pre><code>XML 配置<br/>注解扫描<br/>@Configuration + @Bean</code></pre><p>解析出 BeanDefinition。</p><p>BeanDefinition 可以理解为 Bean 的“配方”。</p><p>里面记录：</p><pre><code>Bean 的 class<br/>作用域<br/>是否懒加载<br/>依赖关系<br/>初始化方法<br/>销毁方法<br/>属性值</code></pre><ol><li>第二步：实例化 Bean</li></ol><p>Spring 根据 BeanDefinition 创建 Bean 对象。</p><p>比如通过构造方法反射创建：</p><pre><code>UserService userService = new UserService();</code></pre><p>这一步只是创建对象，还没有完成依赖注入。</p><p>注意：</p><pre><code>实例化是创建对象<br/>初始化是执行初始化逻辑</code></pre><p>这两个不要混淆。</p><ol><li>第三步：属性填充 / 依赖注入</li></ol><p>实例化之后，Spring 会给 Bean 注入依赖。</p><p>比如：</p><pre><code>@Autowired<br/>private UserDao userDao;</code></pre><p>或者构造器注入、Setter 注入。</p><p>这一步会把其他 Bean 注入到当前 Bean 中。</p><pre><code>UserService<br/>  ↓ 注入<br/>UserDao</code></pre><ol><li>第四步：Aware 回调</li></ol><p>如果 Bean 实现了某些 Aware 接口，Spring 会回调它们。</p><p>常见的有：</p><pre><code>BeanNameAware<br/>BeanFactoryAware<br/>ApplicationContextAware</code></pre><p>比如：</p><pre><code>class MyBean implements ApplicationContextAware {<br/>    public void setApplicationContext(ApplicationContext ctx) {<br/>        ...<br/>    }<br/>}</code></pre><p>这些接口可以让 Bean 获取 Spring 容器相关信息。</p><ol><li>第五步：BeanPostProcessor 前置处理</li></ol><p>初始化前，Spring 会调用：</p><pre><code>postProcessBeforeInitialization()</code></pre><p>这是 BeanPostProcessor 的前置处理。</p><p>它可以在 Bean 初始化前做增强。</p><p>比如：</p><pre><code>处理注解<br/>生成代理前准备<br/>修改 Bean 属性</code></pre><p>很多 Spring 扩展能力都依赖 BeanPostProcessor。</p><ol><li>第六步：初始化</li></ol><p>初始化阶段有几种方式：</p><pre><code>@PostConstruct<br/>InitializingBean.afterPropertiesSet()<br/>自定义 init-method</code></pre><p>执行顺序通常是：</p><pre><code>@PostConstruct<br/>InitializingBean.afterPropertiesSet()<br/>init-method</code></pre><p>初始化一般用于：</p><pre><code>校验配置<br/>建立连接<br/>加载缓存<br/>初始化资源</code></pre><ol><li>第七步：BeanPostProcessor 后置处理</li></ol><p>初始化后，Spring 会调用：</p><pre><code>postProcessAfterInitialization()</code></pre><p>很多 AOP 代理就是在这个阶段创建的。</p><p>比如某个 Bean 需要事务增强：</p><pre><code>原始 Bean<br/>  ↓<br/>BeanPostProcessor 后置处理<br/>  ↓<br/>生成代理对象</code></pre><p>所以你从容器中拿到的 Bean，有时不是原始对象，而是代理对象。</p><ol><li>第八步：Bean 可以使用</li></ol><p>初始化完成后，Bean 就可以被业务代码使用了。</p><p>对于单例 Bean：</p><pre><code>默认会放入 Spring 单例池<br/>整个容器生命周期内复用</code></pre><p>对于 prototype Bean：</p><pre><code>每次获取都会创建新对象<br/>Spring 只负责创建和初始化<br/>一般不负责销毁</code></pre><ol><li>第九步：销毁 Bean</li></ol><p>容器关闭时，会销毁单例 Bean。</p><p>销毁方式包括：</p><pre><code>@PreDestroy<br/>DisposableBean.destroy()<br/>自定义 destroy-method</code></pre><p>销毁一般用于：</p><pre><code>关闭连接<br/>释放线程池<br/>清理缓存<br/>释放文件句柄</code></pre><p>执行顺序通常是：</p><pre><code>@PreDestroy<br/>DisposableBean.destroy()<br/>destroy-method</code></pre><ol><li>单例和原型生命周期区别</li></ol><p>单例 Bean：</p><pre><code>容器创建<br/>容器初始化<br/>容器管理完整生命周期<br/>容器关闭时销毁</code></pre><p>Prototype Bean：</p><pre><code>容器创建和初始化<br/>交给调用方使用<br/>Spring 通常不负责销毁</code></pre><p>所以原型 Bean 如果持有资源，需要自己释放。</p><ol><li>面试简化版流程</li></ol><p>可以这样记：</p><pre><code>BeanDefinition<br/>  ↓<br/>实例化<br/>  ↓<br/>依赖注入<br/>  ↓<br/>Aware<br/>  ↓<br/>BeanPostProcessor 前置<br/>  ↓<br/>初始化<br/>  ↓<br/>BeanPostProcessor 后置<br/>  ↓<br/>使用<br/>  ↓<br/>销毁</code></pre><ol><li><strong>面试背诵版：Spring Bean 生命周期是什么？</strong></li></ol><p>Spring Bean 生命周期可以概括为：读取 BeanDefinition、实例化、属性填充、初始化、使用和销毁。Spring 启动时会根据 XML、注解扫描或 <code>@Bean</code> 方法解析出 BeanDefinition，它描述了 Bean 的类信息、作用域、依赖关系、初始化和销毁方法等。然后 Spring 通过构造方法或反射实例化 Bean，接着进行属性填充，也就是依赖注入。之后如果 Bean 实现了 Aware 接口，比如 <code>BeanNameAware</code>、<code>BeanFactoryAware</code>、<code>ApplicationContextAware</code>，Spring 会回调这些方法，让 Bean 感知容器信息。然后执行 <code>BeanPostProcessor</code> 的前置处理，接着进入初始化阶段，常见方式包括 <code>@PostConstruct</code>、<code>InitializingBean.afterPropertiesSet()</code> 和自定义 <code>init-method</code>。初始化完成后，会执行 <code>BeanPostProcessor</code> 的后置处理，很多 AOP 代理就是在这个阶段生成的。之后 Bean 就可以被业务使用。容器关闭时，单例 Bean 会执行销毁逻辑，比如 <code>@PreDestroy</code>、<code>DisposableBean.destroy()</code> 和自定义 <code>destroy-method</code>。需要注意，Spring 默认完整管理单例 Bean 的生命周期，而 prototype Bean 通常只负责创建和初始化，不负责销毁。</p>"},{"t":"Spring 如何解决循环依赖？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>Spring 解决循环依赖的核心是：对单例 Bean，Spring 通过三级缓存提前暴露“<strong>半成品 Bean”的引用，</strong><strong>让两个 Bean 可以先完成属性注入</strong>，<strong>后续再完成初始化</strong>。</p><ol><li>什么是循环依赖？</li></ol><p>循环依赖就是两个或多个 Bean 互相依赖。</p><p>比如：</p><pre><code>class A {<br/>    @Autowired<br/>    private B b;<br/>}<br/><br/>class B {<br/>    @Autowired<br/>    private A a;<br/>}</code></pre><p>关系是：</p><pre><code>A 依赖 B<br/>B 依赖 A</code></pre><p>如果没有特殊处理，就会出现：</p><pre><code>创建 A → 需要 B<br/>创建 B → 需要 A<br/>创建 A → ...</code></pre><p>无限递归。</p><ol><li>Spring 能解决哪种循环依赖？</li></ol><p>Spring 主要能解决：</p><pre><code>单例 Bean 的属性注入循环依赖</code></pre><p>比如：</p><pre><code>A 属性注入 B<br/>B 属性注入 A</code></pre><p>但不能很好解决：</p><pre><code>构造器注入循环依赖<br/>prototype Bean 循环依赖</code></pre><p>构造器循环依赖为什么不行？</p><p>因为构造方法创建对象时必须先拿到依赖。</p><pre><code>class A {<br/>    public A(B b) {}<br/>}<br/><br/>class B {<br/>    public B(A a) {}<br/>}</code></pre><p>A 没创建完需要 B，B 没创建完又需要 A，没有“半成品对象”可以提前暴露。</p><ol><li>Spring 解决循环依赖的关键</li></ol><p>关键是：</p><pre><code>实例化和属性赋值是分开的</code></pre><p>Spring 创建 Bean 大概分三步：</p><pre><code>实例化：new 一个对象<br/>属性注入：给字段赋值<br/>初始化：执行初始化方法、AOP 等</code></pre><p>对于属性注入来说，A 可以先实例化出来，虽然属性还没注入完成，但已经有对象引用了。</p><p>Spring 就可以提前暴露这个引用。</p><ol><li>三级缓存是什么？</li></ol><p>Spring 用三级缓存处理单例 Bean 创建过程。</p><p>大概是：</p><pre><code>一级缓存 singletonObjects：<br/>存放完整初始化好的单例 Bean<br/><br/>二级缓存 earlySingletonObjects：<br/>存放提前暴露的半成品 Bean<br/><br/>三级缓存 singletonFactories：<br/>存放可以生成早期 Bean 引用的 ObjectFactory</code></pre><p>简单记：</p><pre><code>一级：成品<br/>二级：半成品<br/>三级：半成品工厂</code></pre><ol><li>创建 A 和 B 的流程</li></ol><p>假设 A 和 B 属性循环依赖。</p><p>流程大概是：</p><pre><code>创建 A<br/>  ↓<br/>实例化 A<br/>  ↓<br/>把 A 的 ObjectFactory 放入三级缓存<br/>  ↓<br/>给 A 注入属性，发现需要 B<br/>  ↓<br/>创建 B<br/>  ↓<br/>实例化 B<br/>  ↓<br/>把 B 的 ObjectFactory 放入三级缓存<br/>  ↓<br/>给 B 注入属性，发现需要 A<br/>  ↓<br/>从缓存中找 A<br/>  ↓<br/>三级缓存中找到 A 的 ObjectFactory<br/>  ↓<br/>生成 A 的早期引用，放入二级缓存<br/>  ↓<br/>把 A 注入给 B<br/>  ↓<br/>B 初始化完成，放入一级缓存<br/>  ↓<br/>回到 A，注入 B<br/>  ↓<br/>A 初始化完成，放入一级缓存</code></pre><p>这样循环就被打破了。</p><ol><li>为什么需要三级缓存？</li></ol><p>如果没有 AOP，二级缓存似乎也能解决循环依赖。</p><p>但 Spring 需要考虑代理对象。</p><p>比如 A 需要被 AOP 代理：</p><pre><code>事务代理<br/>切面代理</code></pre><p>如果 B 注入的是 A 的原始对象，而最终容器里放的是 A 的代理对象，就会不一致。</p><p>三级缓存里的 <code>ObjectFactory</code> 可以在需要早期引用时，提前生成代理对象。</p><p>所以三级缓存主要是为了解决循环依赖下的 AOP 代理一致性问题。</p><ol><li>为什么不是二级缓存就够？</li></ol><p>二级缓存只能存一个已经生成的早期对象。</p><p>三级缓存存的是工厂：</p><pre><code>需要时再决定返回原始对象还是代理对象</code></pre><p>这样可以把“是否需要生成代理”的逻辑延迟到真正需要提前暴露引用时。</p><p>所以三级缓存更灵活。</p><ol><li>一级缓存什么时候放？</li></ol><p>Bean 完整初始化完成后，才放入一级缓存。</p><p>也就是：</p><pre><code>实例化完成<br/>属性注入完成<br/>初始化完成<br/>AOP 代理处理完成</code></pre><p>这个时候它才是完整可用的成品 Bean。</p><ol><li>哪些循环依赖解决不了？</li></ol><p>常见解决不了的情况：</p><pre><code>构造器注入循环依赖<br/>prototype Bean 循环依赖<br/>多例 Bean 循环依赖<br/>某些复杂 AOP 场景</code></pre><p>比如构造器注入：</p><pre><code>@Component<br/>class A {<br/>    public A(B b) {}<br/>}<br/><br/>@Component<br/>class B {<br/>    public B(A a) {}<br/>}</code></pre><p>这种通常会报错。</p><ol><li>如何避免循环依赖？</li></ol><p>循环依赖通常说明设计可能有问题。</p><p>可以通过：</p><pre><code>重新拆分职责<br/>引入第三个中间服务<br/>使用事件发布<br/>使用懒加载 @Lazy<br/>改为 setter 注入<br/>抽象接口解耦</code></pre><p>但不要滥用循环依赖。</p><p>Spring 能解决不代表设计合理。</p><ol><li><strong>面试背诵版：Spring 如何解决循环依赖？</strong></li></ol><p>Spring 主要通过三级缓存解决单例 Bean 的属性注入循环依赖。Spring 创建 Bean 分为实例化、属性注入和初始化三个阶段，对于属性注入来说，Bean 在实例化之后虽然还没有完成依赖注入和初始化，但已经有了对象引用，Spring 可以提前暴露这个“半成品 Bean”。三级缓存分别是：一级缓存 <code>singletonObjects</code>，保存已经完整初始化好的单例 Bean；二级缓存 <code>earlySingletonObjects</code>，保存提前暴露的早期 Bean；三级缓存 <code>singletonFactories</code>，保存可以生成早期 Bean 引用的 <code>ObjectFactory</code>。比如 A 依赖 B，B 又依赖 A，Spring 创建 A 时先实例化 A，并把 A 的 ObjectFactory 放入三级缓存；然后注入 A 的属性时发现需要 B，就去创建 B；创建 B 时又需要 A，于是从三级缓存中通过 ObjectFactory 拿到 A 的早期引用，放入二级缓存并注入给 B；B 创建完成后再注入给 A，最终 A 和 B 都完成初始化并进入一级缓存。三级缓存存在的一个重要原因是为了解决循环依赖下 AOP 代理对象的一致性问题，可以在提前暴露引用时返回代理对象而不是原始对象。需要注意，Spring 主要解决的是单例 Bean 的属性注入循环依赖，构造器注入循环依赖和 prototype Bean 循环依赖通常无法解决。</p>"},{"t":"Spring AOP 原理是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>Spring AOP 原理的核心是：Spring 在运行时为目标对象创建代理对象，<strong>方法调用先进入代理对象</strong>，再由<strong>代理对象在目标方法前后织入增强逻辑。</strong></p><ol><li>AOP 是什么？</li></ol><p>AOP 全称是 Aspect Oriented Programming，面向切面编程。</p><p>它的作用是：把和业务无关但很多地方都需要的通用逻辑抽出来统一处理。</p><p>比如：</p><pre><code>日志<br/>事务<br/>权限校验<br/>性能统计<br/>缓存<br/>异常处理<br/>接口限流</code></pre><p>这些逻辑如果写在每个业务方法里，会导致大量重复代码。</p><p>AOP 可以把它们从业务代码中解耦出来。</p><ol><li>AOP 的核心思想</li></ol><p>业务代码只关心业务：</p><pre><code>public void createOrder() {<br/>    // 下单逻辑<br/>}</code></pre><p>切面负责增强逻辑：</p><pre><code>方法执行前：权限校验<br/>方法执行中：业务逻辑<br/>方法执行后：日志记录<br/>异常时：异常处理</code></pre><p>也就是：</p><pre><code>不改业务代码<br/>在方法执行前后插入额外逻辑</code></pre><ol><li>Spring AOP 的底层原理</li></ol><p>Spring AOP 底层主要靠动态代理。</p><p>它不会直接修改目标类源码。</p><p>而是在运行时生成一个代理对象。</p><p>调用关系变成：</p><pre><code>调用方<br/>  ↓<br/>代理对象 Proxy<br/>  ↓<br/>增强逻辑<br/>  ↓<br/>目标对象 Target</code></pre><p>也就是说，你从 Spring 容器里拿到的 Bean，可能不是原始对象，而是代理对象。</p><ol><li>JDK 动态代理</li></ol><p>如果目标类实现了接口，Spring 默认可以使用 JDK 动态代理。</p><pre><code>interface UserService {<br/>    void createUser();<br/>}<br/><br/>class UserServiceImpl implements UserService {<br/>    public void createUser() {<br/>        System.out.println(\"create user\");<br/>    }<br/>}</code></pre><p>JDK 动态代理生成的是接口的代理对象。</p><p>特点：</p><pre><code>基于接口<br/>代理类实现同样接口<br/>通过 InvocationHandler 拦截方法调用</code></pre><ol><li>CGLIB 动态代理</li></ol><p>如果目标类没有实现接口，Spring 可以使用 CGLIB。</p><p>CGLIB 是通过继承目标类生成子类代理。</p><pre><code>目标类 UserService<br/>  ↓<br/>生成 UserService 的子类代理</code></pre><p>特点：</p><pre><code>基于继承<br/>可以代理没有接口的类<br/>不能代理 final 类<br/>不能代理 final 方法</code></pre><p>因为 <code>final</code> 不能被继承或重写。</p><ol><li>JDK 动态代理和 CGLIB 区别</li></ol><pre><code>JDK 动态代理：<br/>基于接口<br/>要求目标类实现接口<br/>生成接口代理对象<br/><br/>CGLIB：<br/>基于继承<br/>不要求接口<br/>生成目标类子类代理对象<br/>不能代理 final 类和 final 方法</code></pre><p>Spring Boot 现在很多场景默认倾向于使用 CGLIB 代理，但面试中说 Spring AOP 主要有 JDK 动态代理和 CGLIB 两种即可。</p><ol><li>AOP 相关概念</li></ol><p>常见术语：</p><pre><code>Aspect：切面，通用增强逻辑的模块<br/>JoinPoint：连接点，可以被增强的位置，Spring AOP 中通常是方法执行<br/>Pointcut：切点，匹配哪些方法需要增强<br/>Advice：通知，具体增强逻辑<br/>Target：目标对象<br/>Proxy：代理对象<br/>Weaving：织入，把增强逻辑应用到目标对象</code></pre><p>通知类型包括：</p><pre><code>@Before：前置通知<br/>@After：后置通知<br/>@AfterReturning：返回后通知<br/>@AfterThrowing：异常通知<br/>@Around：环绕通知</code></pre><ol><li>Spring AOP 执行流程</li></ol><p>比如一个方法加了事务：</p><pre><code>@Transactional<br/>public void createOrder() {<br/>    ...<br/>}</code></pre><p>执行时大概是：</p><pre><code>调用 createOrder()<br/>  ↓<br/>实际调用的是代理对象<br/>  ↓<br/>代理对象开启事务<br/>  ↓<br/>调用目标对象 createOrder()<br/>  ↓<br/>如果成功，提交事务<br/>  ↓<br/>如果异常，回滚事务</code></pre><p>所以事务本质上也是 AOP 的典型应用。</p><ol><li>AOP 代理什么时候创建？</li></ol><p>在 Spring Bean 生命周期中，AOP 代理通常是在 Bean 初始化后的 <code>BeanPostProcessor</code> 阶段创建的。</p><p>大致流程：</p><pre><code>创建原始 Bean<br/>  ↓<br/>依赖注入<br/>  ↓<br/>初始化<br/>  ↓<br/>BeanPostProcessor 后置处理<br/>  ↓<br/>判断是否需要 AOP<br/>  ↓<br/>如果需要，创建代理对象<br/>  ↓<br/>放入容器</code></pre><p>所以容器里最终暴露给外部的可能是代理对象。</p><ol><li>Spring AOP 的局限</li></ol><p>Spring AOP 是基于代理的，所以有一些限制。</p><p>常见限制：</p><pre><code>只能增强 Spring 容器管理的 Bean<br/>默认只能拦截 public 方法<br/>同类内部方法调用可能不会走代理<br/>final 类或 final 方法不容易代理<br/>static 方法不能通过普通 AOP 代理</code></pre><p>最常见问题是自调用失效：</p><pre><code>public void a() {<br/>    b(); // this.b()，不会走代理<br/>}<br/><br/>@Transactional<br/>public void b() {<br/>}</code></pre><p>因为 <code>a()</code> 里调用 <code>b()</code> 是当前对象内部调用，没有经过代理对象，所以事务可能不生效。</p><ol><li>和 AspectJ 的区别</li></ol><p>Spring AOP：</p><pre><code>基于运行时动态代理<br/>主要拦截方法调用<br/>简单易用<br/>适合大多数业务场景</code></pre><p>AspectJ：</p><pre><code>真正的 AOP 框架<br/>可以编译期或类加载期织入<br/>能力更强<br/>可以增强字段、构造器等更多连接点</code></pre><p>日常 Spring 项目用 Spring AOP 就够了。</p><ol><li><strong>面试背诵版：Spring AOP 原理是什么？</strong></li></ol><p>Spring AOP 的核心原理是动态代理。Spring 不会直接修改目标类源码，而是在运行时为目标 Bean 创建一个代理对象，调用方实际调用的是代理对象，代理对象在调用目标方法前后织入增强逻辑，比如日志、权限、事务、缓存等。如果目标类实现了接口，Spring 可以使用 JDK 动态代理，代理对象实现同样接口，通过 <code>InvocationHandler</code> 拦截方法调用；如果目标类没有实现接口，Spring 可以使用 CGLIB，通过继承目标类生成子类代理，因此 final 类和 final 方法不能被 CGLIB 正常代理。AOP 中常见概念包括切面 Aspect、切点 Pointcut、通知 Advice、连接点 JoinPoint、目标对象 Target 和代理对象 Proxy。Spring AOP 代理通常在 Bean 生命周期的 BeanPostProcessor 后置处理阶段创建，所以容器中最终暴露的可能是代理对象。比如 <code>@Transactional</code> 就是典型 AOP 应用，方法调用会先进入事务代理，代理开启事务后调用目标方法，成功则提交，异常则回滚。需要注意，Spring AOP 是基于代理的，所以只能增强 Spring 管理的 Bean，同类内部方法调用如果没有经过代理，AOP 可能不会生效。</p>"},{"t":"JDK 动态代理和 CGLIB 有什么区别？","tag":"高频","p":"high","html":""},{"t":"Spring MVC 请求流程是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span>"},{"t":"Spring Boot 自动配置原理是什么？","tag":"高频","p":"high","html":""},{"t":"Spring Bean 默认单例安全吗？","tag":"高频","p":"high","html":""}]},{"no":"九","title":"MyBatis","questions":[{"t":"MyBatis 工作流程是什么？","tag":"高频","p":"high","html":""},{"t":"MyBatis 一级缓存和二级缓存有什么区别？","tag":"高频","p":"high","html":""},{"t":"MyBatis 如何防止 SQL 注入？","tag":"面试必考","p":"must","html":""},{"t":"MyBatis 中 #{} 和 ${} 有什么区别？","tag":"面试必考","p":"must","html":""},{"t":"MyBatis 为什么需要 Mapper 接口？","tag":"了解即可","p":"know","html":""}]},{"no":"十","title":"设计模式","questions":[{"t":"单例模式有哪些实现方式？","tag":"高频","p":"high","html":""},{"t":"双重检查锁为什么需要 volatile？","tag":"面试必考","p":"must","html":""},{"t":"工厂模式解决什么问题？","tag":"高频","p":"high","html":""},{"t":"代理模式有哪些应用场景？","tag":"高频","p":"high","html":""},{"t":"Spring 使用了哪些设计模式？","tag":"面试必考","p":"must","html":""}]},{"no":"十一","title":"Java 8 新特性","questions":[{"t":"Lambda 表达式原理是什么？","tag":"高频","p":"high","html":""},{"t":"Stream 流是什么？","tag":"高频","p":"high","html":""},{"t":"Optional 为什么出现？","tag":"了解即可","p":"know","html":""},{"t":"CompletableFuture 如何实现异步编程？","tag":"高频","p":"high","html":""}]},{"no":"十二","title":"综合场景","questions":[{"t":"如何设计一个高并发 Java 服务？","tag":"面试必考","p":"must","html":""},{"t":"Java 服务 CPU 飙高如何排查？","tag":"面试必考","p":"must","html":""},{"t":"Java 服务内存泄漏如何排查？","tag":"高频","p":"high","html":""},{"t":"线上接口 RT 突然升高如何定位？","tag":"面试必考","p":"must","html":""},{"t":"如何设计一个秒杀系统？","tag":"高频","p":"high","html":""},{"t":"如何保证接口幂等？","tag":"高频","p":"high","html":""},{"t":"Java 服务如何优雅关闭？","tag":"了解即可","p":"know","html":""}]}]};
