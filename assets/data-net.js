window.TAB_DATA = window.TAB_DATA || {};
window.TAB_DATA["net"] = {"key":"net","title":"2小时速通计算机网络","url":"https://utxc8uqzfk.feishu.cn/wiki/SwHXwDDnqi5kP5kVWYscY9NpnFh","chapters":[{"no":"一","title":"网络基础","questions":[{"t":"什么是计算机网络？","tag":"","p":"","html":""},{"t":"OSI 七层模型是什么？","tag":"","p":"","html":""},{"t":"TCP/IP 四层模型是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>TCP/IP 四层模型的核心是：<strong>把网络通信分成应用层、传输层、网络层、网络接口层，每一层负责不同的事情，下层为上层提供服务。</strong></p><ol><li><strong>四层分别是什么？</strong></li></ol><p>TCP/IP 四层模型通常分为：</p><pre><code>应用层<br/>传输层<br/>网络层<br/>网络接口层</code></pre><p>从上到下是：</p><pre><code>应用层：HTTP、DNS、FTP、SMTP<br/>传输层：TCP、UDP<br/>网络层：IP、ICMP<br/>网络接口层：以太网、Wi-Fi、ARP</code></pre><ol><li>应用层负责什么？</li></ol><p>应用层负责具体应用之间的数据格式和交互规则。</p><p>比如：</p><pre><code>HTTP：网页请求和响应<br/>DNS：域名解析<br/>FTP：文件传输<br/>SMTP：邮件传输</code></pre><p>应用层关心的是“应用数据怎么组织、怎么表达”。</p><p>例如浏览器发送 HTTP 请求：</p><pre><code>GET /index.html HTTP/1.1<br/>Host: example.com</code></pre><ol><li>传输层负责什么？</li></ol><p>传输层负责端到端进程之间的数据传输。</p><p>典型协议是：</p><pre><code>TCP：可靠、面向连接、有序传输<br/>UDP：无连接、尽力而为、速度快</code></pre><p>传输层通过端口号区分不同进程。</p><pre><code>IP 地址：找到哪台机器<br/>端口号：找到机器上的哪个进程</code></pre><ol><li>网络层负责什么？</li></ol><p>网络层负责主机到主机之间的寻址和路由。</p><p>核心协议是 IP。</p><p>它关心的是：</p><pre><code>目标 IP 是什么<br/>数据包从哪条路径走<br/>怎么跨网络转发</code></pre><p>比如从你的电脑访问远程服务器，中间经过很多路由器，主要就是网络层在工作。</p><ol><li>网络接口层负责什么？</li></ol><p>网络接口层也叫链路层或网络接入层。</p><p>它负责在具体物理网络上传输数据帧。</p><p>比如：</p><pre><code>以太网<br/>Wi-Fi<br/>网卡驱动<br/>MAC 地址<br/>ARP</code></pre><p>这一层关心的是同一个局域网内，数据怎么通过网卡、MAC 地址、交换机传过去。</p><ol><li>数据发送时怎么封装？</li></ol><p>发送数据时，从上到下逐层封装。</p><pre><code>应用层：HTTP 数据<br/>传输层：加 TCP 头<br/>网络层：加 IP 头<br/>网络接口层：加以太网帧头</code></pre><p>可以理解为一层一层套信封：</p><pre><code>HTTP 数据<br/>↓<br/>TCP 头 + HTTP 数据<br/>↓<br/>IP 头 + TCP 头 + HTTP 数据<br/>↓<br/>以太网头 + IP 头 + TCP 头 + HTTP 数据</code></pre><ol><li>数据接收时怎么拆封？</li></ol><p>接收数据时，从下到上逐层解封。</p><pre><code>网卡收到以太网帧<br/>↓<br/>网络接口层去掉帧头<br/>↓<br/>网络层处理 IP 头<br/>↓<br/>传输层处理 TCP/UDP 头<br/>↓<br/>应用层拿到 HTTP/DNS 等数据</code></pre><ol><li>和 OSI 七层模型的关系</li></ol><p>TCP/IP 四层模型更偏实际互联网使用。</p><p>OSI 七层模型更偏理论划分。</p><p>大致对应关系是：</p><pre><code>OSI 应用层/表示层/会话层 ≈ TCP/IP 应用层<br/>OSI 传输层 ≈ TCP/IP 传输层<br/>OSI 网络层 ≈ TCP/IP 网络层<br/>OSI 数据链路层/物理层 ≈ TCP/IP 网络接口层</code></pre><ol><li><strong>面试背诵版：TCP/IP 四层模型是什么？</strong></li></ol><blockquote><p>TCP/IP 四层模型包括应用层、传输层、网络层和网络接口层。应用层负责具体应用协议的数据格式和交互规则，比如 HTTP、DNS、FTP；传输层负责端到端进程之间的数据传输，通过端口区分进程，典型协议有 TCP 和 UDP；网络层负责主机到主机之间的寻址和路由，核心协议是 IP，也包括 ICMP；网络接口层负责在具体物理网络中传输数据帧，比如以太网、Wi-Fi、MAC 地址和 ARP。数据发送时会从应用层到网络接口层逐层封装，接收时再从网络接口层到应用层逐层解封。</p></blockquote>"},{"t":"OSI 七层和 TCP/IP 四层有什么区别？","tag":"","p":"","html":""},{"t":"为什么网络需要分层？","tag":"","p":"","html":""},{"t":"每一层分别解决什么问题？","tag":"","p":"","html":""},{"t":"数据在网络中是怎么传输的？","tag":"","p":"","html":""},{"t":"什么是封装和解封装？","tag":"","p":"","html":""},{"t":"MAC 地址、IP 地址、端口号分别是什么？","tag":"","p":"","html":""},{"t":"IP 地址和 MAC 地址为什么都需要？","tag":"","p":"","html":""}]},{"no":"二","title":"数据链路层","questions":[{"t":"什么是物理层？","tag":"","p":"","html":""},{"t":"什么是数据链路层？","tag":"","p":"","html":""},{"t":"什么是 MAC 地址？","tag":"","p":"","html":""},{"t":"什么是 ARP？","tag":"","p":"","html":""},{"t":"ARP 是怎么工作的？","tag":"","p":"","html":""},{"t":"为什么需要 ARP？","tag":"","p":"","html":""},{"t":"什么是交换机？","tag":"","p":"","html":""},{"t":"交换机是怎么转发数据的？","tag":"","p":"","html":""},{"t":"交换机和路由器有什么区别？","tag":"","p":"","html":""},{"t":"什么是广播域？","tag":"","p":"","html":""},{"t":"什么是 VLAN？","tag":"","p":"","html":""},{"t":"VLAN 解决什么问题？","tag":"","p":"","html":""}]},{"no":"三","title":"网络层","questions":[{"t":"什么是 IP？","tag":"","p":"","html":""},{"t":"IPv4 和 IPv6 有什么区别？","tag":"","p":"","html":""},{"t":"IP 地址和子网掩码是什么？","tag":"","p":"","html":""},{"t":"什么是子网？","tag":"","p":"","html":""},{"t":"什么是默认网关？","tag":"","p":"","html":""},{"t":"什么是路由？","tag":"","p":"","html":""},{"t":"路由器是怎么转发 IP 数据包的？","tag":"","p":"","html":""},{"t":"什么是路由表？","tag":"","p":"","html":""},{"t":"什么是 NAT？","tag":"","p":"","html":""},{"t":"NAT 为什么存在？","tag":"","p":"","html":""},{"t":"内网 IP 和公网 IP 有什么区别？","tag":"","p":"","html":""},{"t":"什么是 ICMP？","tag":"","p":"","html":""},{"t":"Ping 的原理是什么？","tag":"重点","p":"core","html":"<p>核心回答：<code>ping</code> 的原理是使用 <strong>ICMP Echo Request / Echo Reply</strong> 报文测试网络连通性和延迟；它会向目标主机发送 ICMP 请求，对方收到后返回 ICMP 响应，发送方根据往返时间判断是否可达。</p><ol><li><strong>Ping 是什么</strong></li></ol><p><code>ping</code> 是一个网络诊断工具。</p><p>常用于检查：</p><pre><code>目标主机是否可达<br/>网络延迟 RTT<br/>是否丢包<br/>DNS 是否解析正常<br/>网络是否抖动</code></pre><p>比如：</p><pre><code>ping baidu.com</code></pre><p>会持续向目标地址发送探测包。</p><ol><li><strong>Ping 使用什么协议</strong></li></ol><p><code>ping</code> 使用的是：</p><pre><code>ICMP 协议</code></pre><p>ICMP 全称：</p><pre><code>Internet Control Message Protocol</code></pre><p>它工作在网络层，和 IP 协议关系很近。</p><p><code>ping</code> 主要用两类 ICMP 报文：</p><pre><code>Echo Request：回显请求<br/>Echo Reply：回显响应</code></pre><ol><li><strong>Ping 的基本流程</strong></li></ol><p>流程是：</p><pre><code>用户执行 ping<br/>  ↓<br/>解析域名得到 IP<br/>  ↓<br/>发送 ICMP Echo Request<br/>  ↓<br/>目标主机收到请求<br/>  ↓<br/>返回 ICMP Echo Reply<br/>  ↓<br/>本机收到响应<br/>  ↓<br/>计算 RTT 和丢包率</code></pre><p>RTT 是 Round Trip Time，也就是请求发出到响应返回的往返时间。</p><ol><li><strong>Ping 输出怎么看</strong></li></ol><p>比如：</p><pre><code>64 bytes from 8.8.8.8: icmp_seq=1 ttl=115 time=20.3 ms</code></pre><p>常见字段：</p><pre><code>64 bytes：响应包大小<br/>icmp_seq：包序号<br/>ttl：剩余生命周期<br/>time：往返延迟</code></pre><p><code>time</code> 越低，说明延迟越小。</p><p>如果出现：</p><pre><code>Request timeout</code></pre><p>可能是网络不通、丢包、防火墙拦截，或者目标禁用了 ICMP。</p><ol><li><strong>TTL 是什么</strong></li></ol><p>TTL 是 Time To Live。</p><p>它表示 IP 包最多还能经过多少跳路由。</p><p>每经过一个路由器，TTL 减 1。</p><p>如果减到 0，包会被丢弃。</p><p>TTL 可以用于：</p><pre><code>防止数据包在网络中无限循环<br/>辅助判断大概经过多少跳</code></pre><p><code>traceroute</code> 也利用了 TTL 机制。</p><ol><li><strong>Ping 不通代表服务不可用吗</strong></li></ol><p>不一定。</p><p>Ping 不通只能说明：</p><pre><code>ICMP 没有收到响应</code></pre><p>但可能原因有很多：</p><pre><code>目标服务器禁 ping<br/>防火墙屏蔽 ICMP<br/>网络策略不允许<br/>中间路由丢弃 ICMP<br/>目标主机真的不可达</code></pre><p>所以：</p><pre><code>Ping 不通，不一定 HTTP 服务不通<br/>Ping 通，也不代表业务端口可用</code></pre><p>比如服务器可以 ping 通，但 80/443 端口没开。</p><ol><li><strong>Ping 和 TCP/HTTP 的区别</strong></li></ol><sheet></sheet><p>如果要测试端口，应该用：</p><pre><code>telnet host port<br/>nc -vz host port<br/>curl http://host</code></pre><ol><li><strong>常见排查思路</strong></li></ol><p>如果访问服务失败，可以这样排查：</p><pre><code>ping 域名：看 DNS 和主机连通性<br/>ping IP：排除 DNS 问题<br/>traceroute：看路由路径<br/>nc/telnet：看端口是否通<br/>curl：看 HTTP 服务是否正常<br/>查看防火墙和安全组</code></pre><p><strong>面试背诵版</strong></p><p>问题：Ping 的原理是什么？</p><p><code>ping</code> 是一个网络连通性诊断工具，它的底层原理是使用 ICMP 协议中的 Echo Request 和 Echo Reply 报文。执行 <code>ping</code> 时，主机会先把目标域名解析成 IP，然后向目标主机发送 ICMP Echo Request 报文；如果目标主机可达并且允许响应 ICMP，就会返回 ICMP Echo Reply。发送方收到响应后，会根据请求发出到响应返回的时间计算 RTT，也就是往返延迟，同时根据发送和收到的包数量计算丢包率。<code>ping</code> 输出中的 <code>time</code> 表示延迟，<code>ttl</code> 表示 IP 包的剩余生命周期，每经过一个路由器 TTL 会减 1。需要注意的是，<code>ping</code> 不通不一定代表服务不可用，因为目标主机、防火墙或云安全组可能禁用了 ICMP；<code>ping</code> 通也不代表 HTTP 或 TCP 端口可用，因为它只说明 ICMP 层面可达。</p><p>面试中可以这样收口：</p><p>简单说，<code>ping</code> 就是发送 ICMP 回显请求，看目标是否返回回显响应；它能测主机连通性和延迟，但不能直接证明某个业务端口一定可用。</p>"},{"t":"Traceroute 的原理是什么？","tag":"","p":"","html":"<p>核心回答：<code>traceroute</code> 的原理是利用 IP 包的 <strong>TTL 逐跳递减机制</strong>，从 TTL=1 开始逐步增加，诱导路径上的每一跳路由器返回 ICMP 超时报文，从而探测到到目标主机经过的路由路径。</p><ol><li><strong>traceroute 是什么</strong></li></ol><p><code>traceroute</code> 是网络路径诊断工具。</p><p>它用来查看：</p><pre><code>从本机到目标主机经过哪些路由器<br/>每一跳延迟是多少<br/>在哪一跳出现丢包或超时<br/>网络路径是否绕路</code></pre><p>比如：</p><pre><code>traceroute google.com</code></pre><p>Linux/macOS 常用 <code>traceroute</code>，Windows 上常用：</p><pre><code>tracert</code></pre><ol><li><strong>TTL 是关键</strong></li></ol><p>IP 包里有一个字段叫 TTL。</p><p>TTL 全称是：</p><pre><code>Time To Live</code></pre><p>它表示数据包最多还能经过多少跳。</p><p>每经过一个路由器：</p><pre><code>TTL 减 1</code></pre><p>如果 TTL 变成 0，路由器会丢弃该包，并返回：</p><pre><code>ICMP Time Exceeded</code></pre><p>也就是超时报文。</p><ol><li><strong>traceroute 的基本流程</strong></li></ol><p><code>traceroute</code> 会先发一个 TTL=1 的包。</p><pre><code>TTL = 1<br/>  ↓<br/>到达第 1 个路由器<br/>  ↓<br/>TTL 减为 0<br/>  ↓<br/>第 1 个路由器丢包并返回 ICMP Time Exceeded</code></pre><p>这样本机就知道第一跳是谁。</p><p>然后发送 TTL=2：</p><pre><code>TTL = 2<br/>  ↓<br/>经过第 1 跳，TTL 变 1<br/>  ↓<br/>到达第 2 跳，TTL 变 0<br/>  ↓<br/>第 2 跳返回 ICMP Time Exceeded</code></pre><p>依次增加 TTL，就能逐跳发现路径。</p><ol><li><strong>到达目标时会怎样</strong></li></ol><p>当 TTL 足够大，包到达目标主机。</p><p>目标主机不会返回 Time Exceeded，而会返回某种结束响应。</p><p>不同实现略有差异：</p><pre><code>Linux/macOS traceroute：通常发送 UDP 包到高端口，目标返回 ICMP Port Unreachable<br/>Windows tracert：通常使用 ICMP Echo Request，目标返回 ICMP Echo Reply</code></pre><p>收到目标响应后，traceroute 就知道路径结束。</p><ol><li><strong>每一跳为什么有 3 个时间</strong></li></ol><p>常见输出：</p><pre><code>1  192.168.1.1  1.2 ms  1.0 ms  1.1 ms<br/>2  10.0.0.1     5.3 ms  5.5 ms  5.2 ms</code></pre><p>通常每一跳会发送多个探测包，比如 3 个。</p><p>所以会显示 3 个 RTT。</p><p>作用是观察：</p><pre><code>延迟是否稳定<br/>是否有丢包<br/>是否偶发超时</code></pre><ol><li><strong>星号 * 是什么</strong></li></ol><p>如果看到：</p><pre><code>5  * * *</code></pre><p>表示这一跳没有收到响应。</p><p>可能原因：</p><pre><code>路由器不回 ICMP<br/>防火墙过滤<br/>ICMP 被限速<br/>网络丢包<br/>该跳隐藏</code></pre><p>不一定代表网络断了。</p><p>如果后面还能继续显示下一跳，说明这台路由器只是不响应 traceroute。</p><ol><li><strong>traceroute 和 ping 的区别</strong></li></ol><sheet></sheet><p><code>ping</code> 告诉你终点通不通。</p><p><code>traceroute</code> 告诉你中间怎么走。</p><ol><li><strong>traceroute 的局限</strong></li></ol><p><code>traceroute</code> 结果不一定完全准确。</p><p>原因包括：</p><pre><code>路由器可能不响应 ICMP<br/>ICMP 被限速<br/>往返路径可能不一致<br/>负载均衡导致路径变化<br/>防火墙过滤探测包<br/>NAT 隐藏内部路径</code></pre><p>所以要结合：</p><pre><code>ping<br/>mtr<br/>curl<br/>tcpdump<br/>服务端日志</code></pre><p>一起判断。</p><ol><li><strong>mtr 是什么</strong></li></ol><p><code>mtr</code> 可以理解为：</p><pre><code>ping + traceroute</code></pre><p>它会持续探测每一跳的延迟和丢包率。</p><p>适合持续观察网络质量。</p><p><strong>面试背诵版</strong></p><p>问题：Traceroute 的原理是什么</p><p><code>traceroute</code> 的原理是利用 IP 包 TTL 逐跳递减的机制来探测网络路径。TTL 表示一个 IP 包最多还能经过多少跳路由器，每经过一个路由器 TTL 会减 1；当 TTL 减到 0 时，路由器会丢弃该包，并返回 ICMP Time Exceeded 超时报文。<code>traceroute</code> 会从 TTL=1 开始发送探测包，这样第一个路由器会返回超时消息，本机就知道第一跳是谁；然后发送 TTL=2，第二个路由器返回超时消息，以此类推，逐步增加 TTL，就能探测出从本机到目标主机经过的每一跳路由。当探测包最终到达目标主机时，目标会返回结束响应，比如 Linux/macOS 默认 UDP traceroute 通常收到 ICMP Port Unreachable，Windows tracert 通常使用 ICMP Echo Request 并收到 Echo Reply。输出中的每一跳延迟就是探测包到该跳再返回的 RTT。</p><p>面试中可以这样收口：</p><p>简单说，<code>traceroute</code> 是“故意设置很小的 TTL，让每一跳路由器依次返回超时消息”；通过逐步增加 TTL，就能看到到目标主机经过的路由路径。</p><hr/>"}]},{"no":"四","title":"TCP","questions":[{"t":"TCP 是什么？","tag":"必须掌握","p":"core","html":"<span>[原文档此处有配图]</span><p>TCP 的核心是：<strong>一种面向连接、可靠的传输层协议，用来保证数据按顺序、无丢失、无重复地从一端传到另一端。</strong></p><ol><li>TCP 位于哪一层？</li></ol><p><strong>TCP 位于传输层。</strong></p><p>它运行在 IP 协议之上。</p><p>IP 只负责把包尽力送到目标地址，但不保证可靠。</p><p>TCP 在 IP 之上提供可靠传输能力。</p><ol><li>TCP 的特点</li></ol><p>TCP 主要特点：</p><ul><li>面向连接</li><li>可靠传输</li><li>字节流协议</li><li>有序传输</li><li>错误校验</li><li>超时重传</li><li>流量控制</li><li>拥塞控制</li><li>全双工通信</li></ul><p>它适合对可靠性要求高的场景。</p><ol><li>面向连接是什么意思？</li></ol><p>通信前，双方要先建立连接。</p><p>也就是 TCP 三次握手。</p><p>流程是：</p><pre><code>客户端 SYN<br/>服务器 SYN + ACK<br/>客户端 ACK</code></pre><p>连接建立后，双方才能正式传输数据。</p><p>传输完成后，还要通过四次挥手释放连接。</p><ol><li>可靠传输怎么保证？</li></ol><p>TCP 通过多种机制保证可靠：</p><ul><li>序列号</li><li>确认应答 ACK</li><li>超时重传</li><li>校验和</li><li>滑动窗口</li><li>流量控制</li><li>拥塞控制</li></ul><p>比如发送方发出数据后，如果长时间没收到 ACK，就会重传。</p><ol><li>有序传输是什么意思？</li></ol><p>TCP 给每个字节流编号。</p><p>接收方可以根据序列号重新排序。</p><p>即使底层 IP 包乱序到达，TCP 也能整理成正确顺序再交给应用层。</p><p>所以应用层看到的是有序字节流。</p><ol><li>字节流协议是什么意思？</li></ol><p>TCP 不保留消息边界。</p><p>应用层写入的数据会被 TCP 看作连续字节流。</p><p>比如应用发送两次：</p><pre><code>hello<br/>world</code></pre><p>接收方可能一次收到：</p><pre><code>helloworld</code></pre><p>也可能分多次收到。</p><p>所以 TCP 应用层需要自己处理粘包、拆包。</p><ol><li>TCP 和 UDP 区别</li></ol><p>TCP：</p><ul><li>面向连接</li><li>可靠</li><li>有序</li><li>开销较大</li><li>速度相对慢</li></ul><p>UDP：</p><ul><li>无连接</li><li>不保证可靠</li><li>不保证顺序</li><li>开销小</li><li>延迟低</li></ul><p>TCP 适合：</p><ul><li>HTTP/HTTPS</li><li>文件传输</li><li>邮件</li><li>数据库连接</li><li>SSH</li></ul><p>UDP 适合：</p><ul><li>直播</li><li>游戏</li><li>DNS</li><li>VoIP</li><li>QUIC/HTTP3</li></ul><ol><li><strong>面试背诵版：TCP 是什么？</strong></li></ol><blockquote><p>TCP 是传输层协议，全称 Transmission Control Protocol。它运行在 IP 协议之上，为应用层提供面向连接、可靠、有序的字节流传输服务。IP 只负责尽力而为地传输数据包，不保证可靠性；TCP 则通过连接管理、确认应答、序列号、重传、流量控制和拥塞控制来保证数据可靠传输。</p><p>TCP 通信前需要通过三次握手建立连接，通信结束后通过四次挥手释放连接。传输过程中，TCP 会给数据编号，接收方通过序列号保证数据有序；发送方通过 ACK 确认和超时重传处理丢包；通过滑动窗口和流量控制避免接收方处理不过来；通过拥塞控制避免网络过载。</p><p>简单说，TCP 是一种“可靠、有序、面向连接的传输协议”。它适合 HTTP、HTTPS、文件传输、数据库连接等对可靠性要求高的场景，但相比 UDP 开销更大、延迟更高。</p></blockquote>"},{"t":"TCP 和 UDP 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>TCP 和 UDP 的核心区别是：<strong>TCP 可靠、面向连接、有序，但开销大；UDP 不保证可靠、无连接、速度快，但需要应用自己处理可靠性。</strong></p><ol><li><strong>是否面向连接</strong></li></ol><p>TCP 是面向连接的。</p><p>通信前要先三次握手：</p><pre><code>客户端 SYN<br/>服务端 SYN + ACK<br/>客户端 ACK</code></pre><p>连接建立后才能传输数据。</p><p>UDP 是无连接的。</p><p>发送前不需要建立连接。</p><p>只要知道对方 IP 和端口，就可以直接发数据。</p><ol><li>是否可靠</li></ol><p>TCP 可靠。</p><p>它通过这些机制保证数据尽量可靠到达：</p><ul><li>序列号</li><li>ACK 确认</li><li>超时重传</li><li>校验和</li><li>滑动窗口</li><li>流量控制</li><li>拥塞控制</li></ul><p>UDP 不保证可靠。</p><p>它只负责把数据包发出去，不保证：</p><ul><li>一定到达</li><li>不丢包</li><li>不乱序</li><li>不重复</li></ul><p>如果需要可靠性，要应用层自己实现。</p><ol><li>是否保证顺序</li></ol><p>TCP 保证有序。</p><p>如果底层网络包乱序到达，TCP 会根据序列号重新排序，再交给应用层。</p><p>UDP 不保证顺序。</p><p>应用层收到的包可能乱序，需要自己处理。</p><ol><li>数据传输形式不同</li></ol><p>TCP 是字节流协议。</p><p>它不保留消息边界。</p><p>比如发送方两次发送：</p><pre><code>hello<br/>world</code></pre><p>接收方可能收到：</p><pre><code>helloworld</code></pre><p>也可能拆开收到。</p><p>所以 TCP 可能有粘包、拆包问题。</p><p>UDP 是报文协议。</p><p>一次发送一个 UDP datagram，接收方一次接收一个完整报文。</p><p>消息边界天然保留。</p><ol><li>速度和开销不同</li></ol><p>TCP 因为要建立连接、确认、重传、拥塞控制，所以开销更大，延迟相对高。</p><p>UDP 没有连接建立和复杂可靠性机制，头部更小，延迟低，速度快。</p><p>但代价是不可靠。</p><ol><li>头部大小不同</li></ol><p>TCP 头部至少：</p><pre><code>20 字节</code></pre><p>UDP 头部只有：</p><pre><code>8 字节</code></pre><p>所以 UDP 更轻量。</p><ol><li>使用场景不同</li></ol><p>TCP 适合可靠性优先的场景：</p><ul><li>HTTP / HTTPS</li><li>文件传输</li><li>邮件</li><li>SSH</li><li>数据库连接</li><li>支付订单</li><li>登录注册</li></ul><p>UDP 适合实时性优先、能容忍少量丢包的场景：</p><ul><li>DNS</li><li>视频直播</li><li>语音通话</li><li>在线游戏</li><li>IoT</li><li>QUIC / HTTP/3</li></ul><ol><li>一句话对比</li></ol><p>可以这样记：</p><pre><code>TCP：先连接，可靠传，慢一点<br/>UDP：不连接，直接发，快一点</code></pre><p>或者：</p><pre><code>TCP 像打电话，要先接通；<br/>UDP 像发传单，发出去不管对方一定收到。</code></pre><ol><li><strong>面试背诵版：TCP 和 UDP 有什么区别？</strong></li></ol><blockquote><p>TCP 和 UDP 都是传输层协议，但设计目标不同。TCP 是面向连接的可靠传输协议，通信前需要三次握手建立连接，通信结束后四次挥手释放连接。它通过序列号、ACK 确认、超时重传、滑动窗口、流量控制和拥塞控制来保证数据可靠、有序、不重复地到达。TCP 是字节流协议，不保留消息边界，所以应用层需要处理粘包和拆包问题。</p><p>UDP 是无连接协议，发送前不需要建立连接，只要知道目标 IP 和端口就可以直接发送。它不保证可靠性、不保证顺序，也不负责重传和拥塞控制，但头部小、开销低、延迟低，并且保留报文边界。UDP 如果需要可靠性，需要应用层自己实现。</p><p>简单说，TCP 适合可靠性要求高的场景，比如 HTTP、HTTPS、文件传输、邮件、数据库连接；UDP 适合实时性要求高、能容忍少量丢包的场景，比如 DNS、直播、语音、游戏，以及基于 UDP 的 QUIC/HTTP3。TCP 是“可靠但重”，UDP 是“快但不保证可靠”。</p></blockquote>"},{"t":"TCP 为什么是可靠的？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>TCP 之所以可靠，核心是：<strong>它在不可靠的 IP 之上，加了确认、重传、排序、校验、流控和拥塞控制机制。</strong></p><ol><li><strong>序列号保证有序</strong></li></ol><p>TCP 会给发送的数据按字节编号。</p><p>比如：</p><pre><code>第 1~1000 字节<br/>第 1001~2000 字节</code></pre><p>接收方收到后，可以根据序列号判断：</p><ul><li>数据是否乱序</li><li>是否重复</li><li>是否丢失</li><li>应该按什么顺序交给应用层</li></ul><p>所以即使 IP 层乱序到达，TCP 也能重新排序。</p><ol><li>ACK 确认机制</li></ol><p>TCP 接收方收到数据后，会返回 ACK。</p><p>ACK 表示：</p><pre><code>我已经收到哪些数据了</code></pre><p>比如 ACK=1001 表示：</p><pre><code>1001 之前的数据都收到了，下一步希望收到第 1001 字节。</code></pre><p>发送方通过 ACK 知道哪些数据已经成功到达。</p><ol><li>超时重传</li></ol><p>如果发送方发出数据后，迟迟没有收到 ACK，就认为数据可能丢了。</p><p>然后会重传。</p><p>流程是：</p><pre><code>发送数据<br/>  ↓<br/>等待 ACK<br/>  ↓<br/>超时没收到<br/>  ↓<br/>重新发送</code></pre><p>这样可以处理丢包问题。</p><ol><li>快速重传</li></ol><p>如果接收方发现中间缺了一段数据，会持续发送重复 ACK。</p><p>发送方如果收到多个重复 ACK，就可以判断某个包可能丢了。</p><p>不用等超时，直接重传。</p><p>这叫快速重传。</p><ol><li>校验和保证数据正确</li></ol><p>TCP 头部有 checksum。</p><p>接收方会校验数据在传输过程中有没有损坏。</p><p>如果校验失败，就丢弃该包。</p><p>发送方收不到 ACK，后续会重传。</p><ol><li>滑动窗口提高效率</li></ol><p>TCP 不会发一个包等一个 ACK 再发下一个。</p><p>它使用滑动窗口，可以连续发送多个包。</p><p>窗口内的数据可以批量发送。</p><p>这样既保证可靠，又提高吞吐。</p><ol><li>流量控制保护接收方</li></ol><p>TCP 接收方会告诉发送方自己的接收窗口大小。</p><p>如果接收方处理不过来，窗口变小。</p><p>发送方就少发点。</p><p>这样避免把接收方缓冲区打爆。</p><ol><li>拥塞控制保护网络</li></ol><p>TCP 还会根据网络情况调整发送速度。</p><p>如果出现丢包、延迟上升，TCP 会认为网络可能拥塞。</p><p>然后降低发送速率。</p><p>常见机制包括：</p><ul><li>慢启动</li><li>拥塞避免</li><li>快速重传</li><li>快速恢复</li></ul><p>这能避免把网络打爆。</p><ol><li><strong>面试背诵版：TCP 为什么是可靠的？</strong></li></ol><blockquote><p>TCP 是可靠的，是因为它在不可靠的 IP 协议之上实现了一套<strong>可靠传输机制</strong>。首先，TCP 会给<strong>数据按字节编号</strong>，通过序列号保证接收方能够识别乱序、重复和丢失的数据，并按正确顺序交给应用层。其次，<strong>TCP 使用 ACK 确认机制</strong>，接收方收到数据后会返回确认，发送方根据 ACK 判断哪些数据已经成功到达。</p><p>如果发送方在超时时间内没有收到 ACK，就会进行超时重传；如果收到多个重复 ACK，也可以触发快速重传。TCP 还通过校验和检测数据是否损坏，损坏的数据会被丢弃并等待重传。为了提升效率，TCP 使用滑动窗口连续发送多个数据段；为了避免接收方处理不过来，使用流量控制；为了避免网络拥塞，使用慢启动、拥塞避免、快速重传和快速恢复等拥塞控制机制。</p><p>简单说，TCP 的可靠性来自“<strong>序列号保证有序、ACK 确认到达、重传解决丢包、校验和发现错误、流量控制保护接收方、拥塞控制保护网络</strong>”。</p></blockquote><blockquote><p></p></blockquote>"},{"t":"TCP 如何保证数据不丢失？","tag":"重点","p":"core","html":"<p>核心回答：TCP 通过<strong>序列号、确认应答 ACK、超时重传、滑动窗口、校验和、流量控制和拥塞控制</strong>来保证数据可靠传输，尽量做到不丢、不乱、不重复。</p><ol><li><strong>TCP 是可靠传输协议</strong></li></ol><p>IP 层本身是不可靠的。</p><p>它可能出现：</p><pre><code>丢包<br/>乱序<br/>重复<br/>损坏</code></pre><p>TCP 在 IP 之上提供可靠传输。</p><p>它要保证应用层看到的是：</p><pre><code>有序、完整、无重复的字节流</code></pre><ol><li><strong>序列号保证顺序</strong></li></ol><p>TCP 会给每个字节编号。</p><p>这个编号叫：</p><pre><code>Sequence Number</code></pre><p>接收方根据序列号判断：</p><pre><code>数据是否乱序<br/>是否重复<br/>是否缺失</code></pre><p>如果收到乱序数据，可以先缓存。</p><p>等缺失的数据补齐后，再按顺序交给应用层。</p><ol><li><strong>ACK 确认应答</strong></li></ol><p>接收方收到数据后，会返回 ACK。</p><p>ACK 表示：</p><pre><code>我已经收到某些数据了<br/>下一个希望收到的是哪个序列号</code></pre><p>比如 ACK=1001 表示：</p><pre><code>1000 之前的数据都收到了<br/>下一次请从 1001 开始发</code></pre><p>发送方根据 ACK 判断哪些数据已经成功到达。</p><ol><li><strong>超时重传</strong></li></ol><p>发送方发出数据后，会启动定时器。</p><p>如果超时时间内没有收到 ACK，就认为可能丢包。</p><p>然后重新发送数据。</p><p>流程：</p><pre><code>发送数据<br/>  ↓<br/>等待 ACK<br/>  ↓<br/>超时未收到<br/>  ↓<br/>重传数据</code></pre><p>这是 TCP 保证不丢包的重要机制。</p><ol><li><strong>快速重传</strong></li></ol><p>如果接收方发现中间缺了一段数据，会不断 ACK 同一个序列号。</p><p>比如：</p><pre><code>收到了 1、2、4、5<br/>缺少 3</code></pre><p>接收方会重复 ACK：</p><pre><code>我还要 3</code></pre><p>发送方收到多个重复 ACK 后，不用等超时，就会快速重传缺失的数据。</p><p>这叫快速重传。</p><ol><li><strong>校验和保证数据没损坏</strong></li></ol><p>TCP 头里有校验和。</p><p>接收方会检查数据在传输过程中是否损坏。</p><p>如果校验失败：</p><pre><code>丢弃这个包<br/>不确认 ACK<br/>等待发送方重传</code></pre><p>这样可以避免错误数据被交给应用层。</p><ol><li><strong>滑动窗口提高效率</strong></li></ol><p>如果每发一个包都等 ACK，效率很低。</p><p>TCP 使用滑动窗口，允许发送方连续发送多个数据段。</p><pre><code>窗口内的数据可以连续发送<br/>收到 ACK 后窗口向前滑动</code></pre><p>这样既保证可靠性，又提升吞吐。</p><ol><li><strong>流量控制防止接收方被压垮</strong></li></ol><p>接收方会通过 TCP 窗口告诉发送方：</p><pre><code>我还能接收多少数据</code></pre><p>如果接收方处理不过来，窗口会变小。</p><p>甚至变成 0。</p><p>发送方就会减少发送，避免把接收缓冲区打爆。</p><ol><li><strong>拥塞控制防止网络被压垮</strong></li></ol><p>TCP 还会根据网络状况调整发送速度。</p><p>常见机制：</p><pre><code>慢启动<br/>拥塞避免<br/>快速重传<br/>快速恢复</code></pre><p>如果网络丢包严重，TCP 会认为可能发生拥塞，从而降低发送速率。</p><p>这可以减少网络进一步恶化。</p><ol><li><strong>TCP 可靠性总结</strong></li></ol><sheet></sheet><p><strong>面试背诵版</strong></p><p>问题：TCP 如何保证数据不丢失？</p><blockquote><p>TCP 是基于 IP 之上的可靠传输协议，它通过多种机制保证数据尽量不丢、不乱、不重复。首先，TCP 会给发送的数据按字节编号，也就是序列号，接收方可以根据序列号判断数据是否乱序、重复或缺失，并按顺序交给应用层。其次，TCP 有 ACK 确认机制，接收方收到数据后会回复确认号，告诉发送方哪些数据已经收到、下一段期望收到哪个序列号。发送方如果在超时时间内没有收到 ACK，就会触发超时重传；如果收到多个重复 ACK，也可以通过快速重传提前补发丢失的数据。TCP 还通过校验和检测数据是否损坏，损坏的数据会被丢弃并等待重传。为了提高效率，TCP 使用滑动窗口批量发送数据，同时通过流量控制避免接收方缓冲区被打爆，通过拥塞控制根据网络拥塞情况调整发送速率。</p><p>面试中可以这样收口：</p><p>简单说，<strong>TCP 靠“序列号确认顺序，ACK 确认收到，超时和重复 ACK 触发重传，校验和发现损坏，再配合窗口控制发送节奏</strong>”来保证可靠传输。</p></blockquote>"},{"t":"TCP 如何保证数据有序？","tag":"重点","p":"core","html":"<p>核心回答：TCP 保证数据有序，主要靠<strong>序列号 Sequence Number</strong>；接收方会根据序列号对数据排序、缓存乱序包，等缺失数据到达后再按顺序交给应用层。</p><ol><li><strong>TCP 是字节流协议</strong></li></ol><p>TCP 传输的不是一条条独立消息，而是连续字节流。</p><p>它会给每个字节编号。</p><p>这个编号就是：</p><pre><code>Sequence Number</code></pre><p>比如发送：</p><pre><code>1000 ~ 1999<br/>2000 ~ 2999<br/>3000 ~ 3999</code></pre><p>接收方就知道这些数据的顺序。</p><ol><li><strong>序列号如何保证顺序</strong></li></ol><p>每个 TCP 报文段都会带有序列号。</p><p>接收方收到数据后，会根据序列号判断：</p><pre><code>这个包应该放在哪个位置<br/>是否是期望的下一个包<br/>是否乱序<br/>是否重复<br/>是否缺失</code></pre><p>所以即使网络中包乱序到达，TCP 也能重新排列。</p><ol><li><strong>乱序到达怎么办</strong></li></ol><p>比如发送方发送：</p><pre><code>1<br/>2<br/>3<br/>4</code></pre><p>但接收方收到顺序是：</p><pre><code>1<br/>3<br/>4</code></pre><p>此时发现：</p><pre><code>2 缺失</code></pre><p>接收方不会把 3、4 直接交给应用层。</p><p>它会先缓存 3、4，并继续等待 2。</p><p>等 2 到达后，再按顺序交付：</p><pre><code>1<br/>2<br/>3<br/>4</code></pre><ol><li><strong>ACK 也参与顺序控制</strong></li></ol><p>TCP 的 ACK 通常是累计确认。</p><p>比如接收方返回：</p><pre><code>ACK = 2000</code></pre><p>表示：</p><pre><code>2000 之前的数据都已经按顺序收到<br/>下一个期待从 2000 开始</code></pre><p>如果中间缺了一段，接收方会一直 ACK 缺失位置。</p><p>比如缺少 2，就一直告诉发送方：</p><pre><code>我还在等 2</code></pre><p>发送方收到多个重复 ACK 后，会触发快速重传。</p><ol><li><strong>重复包怎么办</strong></li></ol><p>如果网络重传导致接收方收到重复数据，TCP 会根据序列号判断：</p><pre><code>这段数据我已经收过了</code></pre><p>然后丢弃重复数据，不会重复交给应用层。</p><p>所以应用层看到的是无重复、有序字节流。</p><ol><li><strong>应用层为什么看到有序数据</strong></li></ol><p>接收方 TCP 协议栈会维护接收缓冲区。</p><p>流程是：</p><pre><code>收到 TCP 段<br/>  ↓<br/>检查校验和<br/>  ↓<br/>根据序列号放入接收缓冲区<br/>  ↓<br/>如果前面没有缺口，按顺序交给应用层<br/>  ↓<br/>如果有缺口，先缓存等待</code></pre><p>应用层 <code>read()</code> 到的数据已经是 TCP 排好序后的数据。</p><ol><li><strong>TCP 只能保证连接内有序</strong></li></ol><p>TCP 保证的是：</p><pre><code>同一个 TCP 连接内的数据有序</code></pre><p>它不保证：</p><pre><code>多个 TCP 连接之间的顺序</code></pre><p>比如你开了两个连接分别发消息，两个连接之间谁先到不一定。</p><p>如果业务使用多个连接，需要应用层自己处理顺序。</p><ol><li><strong>和 UDP 对比</strong></li></ol><sheet></sheet><p><strong>面试背诵版</strong></p><p>问题：TCP 如何保证数据有序？</p><blockquote><p>TCP 保证数据有序主要依赖序列号机制。TCP 是面向字节流的协议，会给每个字节分配一个 Sequence Number，发送的每个 TCP 报文段都会带上对应的序列号。接收方收到数据后，会根据序列号判断这段数据应该处在字节流的哪个位置。如果数据按顺序到达，就可以直接放入接收缓冲区并交给应用层；如果数据乱序到达，比如后面的数据先到了，而中间某一段还没到，TCP 会先把乱序数据缓存起来，不会直接交给应用层，而是等待缺失数据到达后再按顺序交付。ACK 机制也会配合保证顺序，接收方通过累计确认告诉发送方已经连续收到哪些数据、下一段期望收到哪个序列号。如果收到重复数据，TCP 也能通过序列号识别并丢弃。最终应用层通过 <code>read()</code> 读到的就是有序、无重复的字节流。</p><p>面试中可以这样收口：</p><p>简单说，TCP 靠序列号给字节流排序；乱序包先缓存，缺失包等重传，只有连续有序的数据才会交给应用层。</p></blockquote>"},{"t":"TCP 如何处理重复数据？","tag":"","p":"","html":""},{"t":"TCP 三次握手是什么？","tag":"面试必考","p":"must","html":"<p>核心回答：TCP 三次握手是建立连接的过程，客户端和服务端通过 <strong>SYN、SYN+ACK、ACK</strong> 三次报文确认双方收发能力正常，并同步初始序列号。</p><ol><li><strong>为什么需要三次握手</strong></li></ol><p>TCP 是可靠的、面向连接的协议。</p><p>在真正传数据前，双方要先确认：</p><pre><code>客户端能发、能收<br/>服务端能发、能收<br/>双方初始序列号是多少<br/>连接参数是否可用</code></pre><p>三次握手就是为了建立这个可靠连接。</p><ol><li><strong>第一次握手：客户端发 SYN</strong></li></ol><p>客户端向服务端发送：</p><pre><code>SYN = 1<br/>seq = x</code></pre><p>含义是：</p><pre><code>客户端请求建立连接<br/>客户端初始序列号是 x</code></pre><p>此时客户端进入：</p><pre><code>SYN_SENT</code></pre><p>状态。</p><ol><li><strong>第二次握手：服务端发 SYN + ACK</strong></li></ol><p>服务端收到 SYN 后，回复：</p><pre><code>SYN = 1<br/>ACK = 1<br/>seq = y<br/>ack = x + 1</code></pre><p>含义是：</p><pre><code>我收到了你的 SYN<br/>我同意建立连接<br/>我的初始序列号是 y<br/>我期望你下次从 x+1 开始发</code></pre><p>此时服务端进入：</p><pre><code>SYN_RECEIVED</code></pre><p>状态。</p><ol><li><strong>第三次握手：客户端发 ACK</strong></li></ol><p>客户端收到服务端的 SYN+ACK 后，回复：</p><pre><code>ACK = 1<br/>ack = y + 1</code></pre><p>含义是：</p><pre><code>我收到了你的 SYN<br/>你的初始序列号 y 我确认了<br/>连接建立完成</code></pre><p>客户端发送完 ACK 后进入：</p><pre><code>ESTABLISHED</code></pre><p>服务端收到 ACK 后也进入：</p><pre><code>ESTABLISHED</code></pre><p>此时双方可以开始传输数据。</p><ol><li><strong>三次握手流程图</strong></li></ol><pre><code>Client                              Server<br/><br/>SYN, seq=x        ────────────────&gt;<br/>                  &lt;────────────────  SYN+ACK, seq=y, ack=x+1<br/>ACK, ack=y+1      ────────────────&gt;<br/><br/>连接建立</code></pre><ol><li><strong>三次握手确认了什么</strong></li></ol><p>三次握手确认：</p><pre><code>客户端发送能力正常<br/>客户端接收能力正常<br/>服务端发送能力正常<br/>服务端接收能力正常<br/>双方初始序列号同步</code></pre><p>具体来说：</p><pre><code>第一次：服务端知道客户端能发<br/>第二次：客户端知道服务端能收、能发<br/>第三次：服务端知道客户端能收</code></pre><ol><li><strong>为什么不是两次握手</strong></li></ol><p>如果只有两次握手，服务端发出 SYN+ACK 后就认为连接建立。</p><p>但服务端无法确认：</p><pre><code>客户端是否真的收到了服务端的 SYN<br/>客户端接收能力是否正常</code></pre><p>另外，两次握手可能导致历史失效连接请求被服务端误认为新连接。</p><p>三次握手可以避免服务端过早建立连接，浪费资源。</p><ol><li><strong>为什么不是四次握手</strong></li></ol><p>理论上可以四次。</p><p>比如服务端先 ACK 客户端 SYN，再单独发 SYN。</p><p>但 TCP 把服务端的 ACK 和 SYN 合并成一次发送。</p><p>所以三次就够了。</p><ol><li><strong>初始序列号为什么重要</strong></li></ol><p>TCP 是字节流协议，需要序列号保证：</p><pre><code>可靠传输<br/>有序交付<br/>去重<br/>重传</code></pre><p>握手时双方会各自选择一个初始序列号。</p><p>后续数据传输基于这个序列号递增。</p><p><strong>面试背诵版</strong></p><p>问题：TCP 三次握手是什么？</p><blockquote><p>TCP 三次握手是建立 TCP 连接的过程，客户端和服务端通过三次报文交换确认双方的发送和接收能力，并同步初始序列号。第一次握手，客户端向服务端发送 SYN 报文，表示请求建立连接，并携带客户端初始序列号 <code>seq=x</code>，客户端进入 <code>SYN_SENT</code> 状态。第二次握手，服务端收到后回复 SYN+ACK，表示同意建立连接，同时确认客户端的序列号，返回 <code>ack=x+1</code>，并携带自己的初始序列号 <code>seq=y</code>，服务端进入 <code>SYN_RECEIVED</code> 状态。第三次握手，客户端收到服务端的 SYN+ACK 后，再发送 ACK，确认服务端的序列号 <code>ack=y+1</code>，之后客户端进入 <code>ESTABLISHED</code> 状态，服务端收到 ACK 后也进入 <code>ESTABLISHED</code> 状态。三次握手的核心目的是确认双方收发能力正常，并同步初始序列号，之后才能可靠传输数据。</p><p>面试中可以这样收口：</p><p>简单说，三次握手就是“客户端发 SYN，服务端回 SYN+ACK，客户端再回 ACK”；它确认双方都能收发，并同步初始序列号。</p></blockquote>"},{"t":"为什么 TCP 需要三次握手？","tag":"面试必考","p":"must","html":"<p>核心回答：TCP 需要三次握手，是为了<strong>确认双方的发送和接收能力都正常，并同步双方的初始序列号</strong>；两次握手无法让服务端确认客户端能收到服务端的响应。</p><ol><li><strong>TCP 建连接要确认什么</strong></li></ol><p>TCP 是可靠传输协议。</p><p>建立连接前，双方要确认：</p><pre><code>客户端能发送<br/>客户端能接收<br/>服务端能发送<br/>服务端能接收<br/>双方初始序列号</code></pre><p>三次握手就是完成这些确认。</p><ol><li><strong>第一次握手确认什么</strong></li></ol><p>客户端发送：</p><pre><code>SYN, seq=x</code></pre><p>服务端收到后可以确认：</p><pre><code>客户端能发送<br/>服务端能接收</code></pre><p>但此时客户端还不知道服务端是否能发送、能接收。</p><p>服务端也不知道客户端是否能接收。</p><ol><li><strong>第二次握手确认什么</strong></li></ol><p>服务端回复：</p><pre><code>SYN + ACK, seq=y, ack=x+1</code></pre><p>客户端收到后可以确认：</p><pre><code>服务端能接收客户端消息<br/>服务端能发送消息<br/>客户端能接收服务端消息</code></pre><p>但此时服务端还不知道：</p><pre><code>客户端是否收到了自己的 SYN+ACK<br/>客户端是否具备接收能力</code></pre><ol><li><strong>第三次握手确认什么</strong></li></ol><p>客户端再发送：</p><pre><code>ACK, ack=y+1</code></pre><p>服务端收到后确认：</p><pre><code>客户端能接收<br/>客户端收到了服务端的 SYN</code></pre><p>至此双方都确认了彼此的收发能力。</p><p>连接才能进入：</p><pre><code>ESTABLISHED</code></pre><ol><li><strong>为什么两次不够</strong></li></ol><p>如果只有两次握手：</p><pre><code>Client -&gt; SYN<br/>Server -&gt; SYN+ACK</code></pre><p>服务端发完 SYN+ACK 就认为连接建立。</p><p>但服务端无法确认：</p><pre><code>客户端是否收到了 SYN+ACK<br/>客户端接收能力是否正常</code></pre><p>这样可能导致服务端为一个客户端并未确认的连接分配资源。</p><ol><li><strong>防止历史连接请求造成资源浪费</strong></li></ol><p>网络中可能存在延迟的旧 SYN 报文。</p><p>比如客户端很久以前发过一个 SYN，因为网络延迟现在才到服务端。</p><p>如果两次握手，服务端可能回复 SYN+ACK 后就建立连接。</p><p>但客户端并不想建立这个连接。</p><p>三次握手中，如果客户端发现这是旧连接请求，不会回复最终 ACK。</p><p>服务端就不会真正进入已建立状态。</p><ol><li><strong>为什么不是四次</strong></li></ol><p>理论上可以四次：</p><pre><code>客户端 SYN<br/>服务端 ACK<br/>服务端 SYN<br/>客户端 ACK</code></pre><p>但服务端的 ACK 和 SYN 可以合并成一个报文：</p><pre><code>SYN + ACK</code></pre><p>所以三次就够了。</p><ol><li><strong>三次握手还同步序列号</strong></li></ol><p>TCP 依赖序列号实现：</p><pre><code>有序传输<br/>确认应答<br/>去重<br/>重传</code></pre><p>双方都要选择自己的初始序列号。</p><p>三次握手过程中：</p><pre><code>客户端告诉服务端自己的 ISN<br/>服务端告诉客户端自己的 ISN<br/>双方互相确认</code></pre><p>这样后续数据传输才有可靠基础。</p><p><strong>面试背诵版</strong></p><p>问题：为什么 TCP 需要三次握手？</p><p>TCP 需要三次握手，主要是为了确认客户端和服务端双方的发送、接收能力都正常，并同步双方的初始序列号。第一次握手时，客户端发送 SYN，服务端收到后只能确认客户端能发送、服务端能接收；第二次握手时，服务端返回 SYN+ACK，客户端收到后可以确认服务端能接收、能发送，自己也能接收；但此时服务端还不知道客户端是否收到了自己的 SYN+ACK，也不知道客户端的接收能力是否正常。第三次握手客户端发送 ACK，服务端收到后，才能确认客户端也能接收，双方收发能力都被验证。两次握手不够，因为服务端无法确认客户端是否真的收到了响应，可能导致服务端过早建立连接并浪费资源。三次握手还可以避免网络中延迟的历史 SYN 报文导致错误连接，并完成双方初始序列号的同步。</p><p>面试中可以这样收口：</p><p>简单说，三次握手是为了确认“双方都能发、也都能收”，并同步初始序列号；两次握手服务端无法确认客户端能不能收到自己的响应。</p><blockquote><p></p></blockquote>"},{"t":"两次握手为什么不行？","tag":"面试必考","p":"must","html":"<p>核心回答：两次握手不行，是因为服务端发出 <code>SYN+ACK</code> 后就无法确认客户端是否收到了，也就无法确认客户端的接收能力；同时还可能把网络中的历史旧连接请求误当成新连接，浪费服务端资源。</p><ol><li><strong>两次握手是什么</strong></li></ol><p>如果只有两次握手，流程是：</p><pre><code>Client  -&gt;  Server：SYN<br/>Server  -&gt;  Client：SYN+ACK</code></pre><p>服务端发完 <code>SYN+ACK</code> 后就认为连接建立。</p><p>问题就在这里：</p><pre><code>服务端没有收到客户端最终确认</code></pre><p>所以服务端不知道客户端是否真的准备好了。</p><ol><li><strong>服务端无法确认客户端能接收</strong></li></ol><p>第一次握手：</p><pre><code>客户端发 SYN<br/>服务端收到</code></pre><p>服务端只能知道：</p><pre><code>客户端能发送<br/>服务端能接收</code></pre><p>第二次握手：</p><pre><code>服务端发 SYN+ACK</code></pre><p>客户端如果收到，可以知道：</p><pre><code>服务端能发送<br/>服务端能接收<br/>客户端能接收</code></pre><p>但服务端并不知道客户端有没有收到这个 <code>SYN+ACK</code>。</p><p>所以服务端无法确认：</p><pre><code>客户端是否能接收<br/>客户端是否愿意继续建立连接</code></pre><ol><li><strong>可能造成服务端资源浪费</strong></li></ol><p>如果两次握手后服务端直接建立连接，就会分配资源。</p><p>比如：</p><pre><code>连接状态<br/>接收缓冲区<br/>发送缓冲区<br/>控制块 TCB</code></pre><p>但如果客户端根本没收到 <code>SYN+ACK</code>，或者已经不想连接了，服务端就会白白维持一个半无效连接。</p><p>三次握手通过第三次 ACK 告诉服务端：</p><pre><code>客户端确实收到了你的响应<br/>连接可以建立</code></pre><ol><li><strong>历史 SYN 报文问题</strong></li></ol><p>网络中可能存在延迟很久的旧 SYN 报文。</p><p>比如客户端之前发过一个连接请求，但因为网络延迟滞留了。</p><p>客户端早就放弃了。</p><p>后来这个旧 SYN 到了服务端。</p><p>如果是两次握手：</p><pre><code>服务端收到旧 SYN<br/>  ↓<br/>回复 SYN+ACK<br/>  ↓<br/>直接认为连接建立</code></pre><p>服务端就会误以为客户端要建立新连接。</p><p>但客户端并没有这个意图。</p><ol><li><strong>三次握手如何解决</strong></li></ol><p>三次握手要求客户端再发一次 ACK：</p><pre><code>Client -&gt; SYN<br/>Server -&gt; SYN+ACK<br/>Client -&gt; ACK</code></pre><p>如果是历史旧 SYN，客户端收到服务端响应后发现不对，通常不会返回有效 ACK。</p><p>服务端收不到第三次 ACK，就不会真正进入稳定的连接建立状态。</p><p>这样可以避免错误连接。</p><ol><li><strong>两次握手不能完整同步序列号</strong></li></ol><p>TCP 需要双方同步初始序列号。</p><pre><code>客户端 ISN = x<br/>服务端 ISN = y</code></pre><p>两次握手中：</p><pre><code>服务端确认了客户端的 x<br/>客户端可能确认服务端的 y</code></pre><p>但服务端不知道客户端是否确认了自己的 <code>y</code>。</p><p>第三次 ACK 的作用就是：</p><pre><code>客户端确认服务端的初始序列号 y</code></pre><p>这样双方序列号同步才完整。</p><ol><li><strong>一句话理解</strong></li></ol><p>两次握手的问题是：</p><pre><code>服务端只听到了客户端说“我要连”<br/>服务端也回答了“我同意”<br/>但服务端不知道客户端有没有听到“我同意”</code></pre><p>第三次 ACK 就是客户端告诉服务端：</p><pre><code>我听到了，你也没问题，可以开始了</code></pre><p><strong>面试背诵版</strong></p><p>问题：两次握手为什么不行？</p><blockquote><p>两次握手不行，主要有两个原因。第一，服务端无法确认客户端的接收能力。客户端第一次发送 SYN 后，服务端只能确认客户端能发送、服务端能接收；服务端第二次返回 SYN+ACK 后，客户端如果收到，可以确认服务端能发送、能接收，但服务端并不知道客户端是否收到了自己的 SYN+ACK，也就无法确认客户端能不能接收。第二，两次握手可能导致历史旧连接请求被误认为新连接。网络中可能存在延迟的旧 SYN 报文，如果服务端收到后只经过两次握手就建立连接，就可能为一个客户端并不需要的连接分配资源，造成浪费。三次握手通过客户端最后一次 ACK，让服务端确认客户端确实收到了响应，并确认双方初始序列号同步完成，连接才真正建立。</p><p>面试中可以这样收口：</p><p>简单说，两次握手少了客户端最后的确认，服务端不知道客户端有没有收到自己的响应；所以无法确认双方收发能力完整，也容易被历史 SYN 误导。</p></blockquote>"},{"t":"三次握手过程中发生了什么？","tag":"重点","p":"core","html":"<p>核心回答：三次握手过程中，客户端和服务端依次交换 <code>SYN</code>、<code>SYN+ACK</code>、<code>ACK</code> 报文，完成<strong>连接建立、双方收发能力确认、初始序列号同步</strong>。</p><ol><li><strong>第一次握手：客户端发 SYN</strong></li></ol><p>客户端主动发起连接，向服务端发送 SYN 报文。</p><pre><code>Client -&gt; Server：SYN=1, seq=x</code></pre><p>含义是：</p><pre><code>我想建立 TCP 连接<br/>我的初始序列号是 x</code></pre><p>此时客户端状态变为：</p><pre><code>SYN_SENT</code></pre><p>服务端收到后可以确认：</p><pre><code>客户端能发送<br/>服务端能接收</code></pre><ol><li><strong>第二次握手：服务端发 SYN+ACK</strong></li></ol><p>服务端收到客户端 SYN 后，返回 SYN+ACK。</p><pre><code>Server -&gt; Client：SYN=1, ACK=1, seq=y, ack=x+1</code></pre><p>含义是：</p><pre><code>我收到了你的连接请求<br/>我同意建立连接<br/>你的序列号 x 我确认了<br/>我的初始序列号是 y</code></pre><p>此时服务端状态变为：</p><pre><code>SYN_RECEIVED</code></pre><p>客户端收到后可以确认：</p><pre><code>客户端能发送、能接收<br/>服务端能发送、能接收</code></pre><ol><li><strong>第三次握手：客户端发 ACK</strong></li></ol><p>客户端收到服务端的 SYN+ACK 后，再发送 ACK。</p><pre><code>Client -&gt; Server：ACK=1, ack=y+1</code></pre><p>含义是：</p><pre><code>我收到了你的 SYN<br/>你的初始序列号 y 我确认了<br/>连接可以建立</code></pre><p>客户端发送 ACK 后进入：</p><pre><code>ESTABLISHED</code></pre><p>服务端收到 ACK 后也进入：</p><pre><code>ESTABLISHED</code></pre><p>之后双方就可以正式传输数据。</p><ol><li><strong>三次握手流程图</strong></li></ol><pre><code>Client                                  Server<br/><br/>SYN, seq=x          ──────────────────&gt;<br/>                    &lt;──────────────────  SYN+ACK, seq=y, ack=x+1<br/>ACK, ack=y+1        ──────────────────&gt;<br/><br/>Client: ESTABLISHED<br/>Server: ESTABLISHED</code></pre><ol><li><strong>握手期间确认了什么</strong></li></ol><p>三次握手确认：</p><pre><code>客户端发送能力正常<br/>客户端接收能力正常<br/>服务端发送能力正常<br/>服务端接收能力正常<br/>客户端初始序列号 x<br/>服务端初始序列号 y</code></pre><p>分别看：</p><pre><code>第一次：服务端确认客户端能发<br/>第二次：客户端确认服务端能收、能发<br/>第三次：服务端确认客户端能收</code></pre><ol><li><strong>为什么要同步初始序列号</strong></li></ol><p>TCP 是可靠字节流协议。</p><p>后续需要依赖序列号实现：</p><pre><code>有序传输<br/>ACK 确认<br/>丢包重传<br/>去重<br/>流量控制</code></pre><p>所以连接建立时，双方要交换自己的初始序列号。</p><p>后续数据会基于这个序列号继续递增。</p><ol><li><strong>握手期间双方状态变化</strong></li></ol><p>客户端：</p><pre><code>CLOSED<br/>  ↓<br/>SYN_SENT<br/>  ↓<br/>ESTABLISHED</code></pre><p>服务端：</p><pre><code>LISTEN<br/>  ↓<br/>SYN_RECEIVED<br/>  ↓<br/>ESTABLISHED</code></pre><p>服务端一开始要处于 <code>LISTEN</code> 状态，等待客户端连接。</p><ol><li><strong>第三次 ACK 可以携带数据吗</strong></li></ol><p>理论上第三次 ACK 可以携带数据。</p><p>因为客户端收到服务端 SYN+ACK 后，连接在客户端看来已经建立。</p><p>但实际是否携带数据取决于实现和场景。</p><p>面试一般不展开，知道即可。</p><p><strong>面试背诵版</strong></p><p>问题：三次握手过程中发生了什么？</p><blockquote><p>TCP 三次握手是建立连接的过程。第一次握手，客户端向服务端发送 SYN 报文，携带自己的初始序列号 <code>seq=x</code>，表示请求建立连接，此时客户端进入 <code>SYN_SENT</code> 状态，服务端收到后可以确认客户端能发送、服务端能接收。第二次握手，服务端回复 SYN+ACK 报文，携带自己的初始序列号 <code>seq=y</code>，并通过 <code>ack=x+1</code> 确认客户端的 SYN，表示同意建立连接，此时服务端进入 <code>SYN_RECEIVED</code> 状态。第三次握手，客户端收到服务端的 SYN+ACK 后，再发送 ACK 报文，通过 <code>ack=y+1</code> 确认服务端的初始序列号，随后客户端进入 <code>ESTABLISHED</code> 状态，服务端收到 ACK 后也进入 <code>ESTABLISHED</code> 状态。三次握手完成后，双方确认了彼此的发送和接收能力，并同步了初始序列号，之后就可以进行可靠数据传输了。</p><p>面试中可以这样收口：</p><p>简单说，三次握手就是客户端发 SYN，服务端回 SYN+ACK，客户端再回 ACK；这个过程确认双方都能收发，并同步各自的初始序列号。</p></blockquote>"},{"t":"SYN 和 ACK 是什么？","tag":"","p":"","html":""},{"t":"TCP 连接建立后，客户端和服务端分别处于什么状态？","tag":"","p":"","html":""},{"t":"TCP 四次挥手是什么？","tag":"面试必考","p":"must","html":"<p>核心回答：TCP 四次挥手是关闭连接的过程，双方通过 <strong>FIN、ACK、FIN、ACK</strong> 四个报文分别关闭各自的发送方向，因为 TCP 是全双工通信，客户端和服务端的发送通道要分别关闭。</p><ol><li><strong>为什么需要四次挥手</strong></li></ol><p>TCP 是全双工协议。</p><p>也就是说：</p><pre><code>客户端可以向服务端发数据<br/>服务端也可以向客户端发数据</code></pre><p>关闭连接时，不是一下子两边都关掉。</p><p>而是：</p><pre><code>一方先说：我不发了<br/>另一方确认：我知道了<br/>另一方处理完剩余数据后也说：我也不发了<br/>最初一方确认：我知道了</code></pre><p>所以通常需要四次。</p><ol><li><strong>第一次挥手：主动方发 FIN</strong></li></ol><p>假设客户端主动关闭连接。</p><p>客户端发送：</p><pre><code>FIN=1, seq=u</code></pre><p>含义是：</p><pre><code>我这边数据发完了<br/>我不再发送数据了</code></pre><p>客户端进入：</p><pre><code>FIN_WAIT_1</code></pre><p>注意：客户端不再发送数据，但仍然可以接收服务端数据。</p><ol><li><strong>第二次挥手：被动方回 ACK</strong></li></ol><p>服务端收到 FIN 后，回复：</p><pre><code>ACK=1, ack=u+1</code></pre><p>含义是：</p><pre><code>我知道你不发了</code></pre><p>服务端进入：</p><pre><code>CLOSE_WAIT</code></pre><p>客户端收到 ACK 后进入：</p><pre><code>FIN_WAIT_2</code></pre><p>此时连接处于半关闭状态：</p><pre><code>客户端不能再发<br/>服务端还可以继续发</code></pre><ol><li><strong>第三次挥手：被动方发 FIN</strong></li></ol><p>服务端处理完剩余数据后，也准备关闭发送方向。</p><p>服务端发送：</p><pre><code>FIN=1, seq=v</code></pre><p>含义是：</p><pre><code>我这边也发完了<br/>我也不再发送数据了</code></pre><p>服务端进入：</p><pre><code>LAST_ACK</code></pre><ol><li><strong>第四次挥手：主动方回 ACK</strong></li></ol><p>客户端收到服务端 FIN 后，回复：</p><pre><code>ACK=1, ack=v+1</code></pre><p>含义是：</p><pre><code>我知道你也不发了</code></pre><p>客户端进入：</p><pre><code>TIME_WAIT</code></pre><p>服务端收到 ACK 后进入：</p><pre><code>CLOSED</code></pre><p>客户端等待一段时间后也进入：</p><pre><code>CLOSED</code></pre><ol><li><strong>四次挥手流程图</strong></li></ol><pre><code>Client                                  Server<br/><br/>FIN, seq=u          ──────────────────&gt;<br/>                    &lt;──────────────────  ACK, ack=u+1<br/><br/>                    &lt;──────────────────  FIN, seq=v<br/>ACK, ack=v+1        ──────────────────&gt;<br/><br/>Client: TIME_WAIT -&gt; CLOSED<br/>Server: CLOSED</code></pre><ol><li><strong>为什么不是三次</strong></li></ol><p>因为服务端收到客户端 FIN 后，可能还有数据没发完。</p><p>所以不能立刻发送 FIN。</p><p>它只能先回复 ACK：</p><pre><code>我知道你要关了</code></pre><p>等自己数据发送完，再单独发送 FIN：</p><pre><code>我也可以关了</code></pre><p>所以 ACK 和 FIN 通常分开发，形成四次挥手。</p><p>如果服务端刚好没有数据要发，第二次 ACK 和第三次 FIN 也可能合并，但逻辑上仍然是双方分别关闭。</p><ol><li><strong>TIME_WAIT 是什么</strong></li></ol><p>主动关闭方最后会进入 <code>TIME_WAIT</code>。</p><p>它会等待一段时间，通常是：</p><pre><code>2MSL</code></pre><p>MSL 是 Maximum Segment Lifetime，报文最大生存时间。</p><p><code>TIME_WAIT</code> 的作用：</p><pre><code>确保最后一个 ACK 能让对方收到<br/>让旧连接中的延迟报文在网络中消失</code></pre><p>这样可以避免旧报文影响后续新连接。</p><ol><li><strong>四次挥手状态变化</strong></li></ol><p>主动关闭方：</p><pre><code>ESTABLISHED<br/>  ↓<br/>FIN_WAIT_1<br/>  ↓<br/>FIN_WAIT_2<br/>  ↓<br/>TIME_WAIT<br/>  ↓<br/>CLOSED</code></pre><p>被动关闭方：</p><pre><code>ESTABLISHED<br/>  ↓<br/>CLOSE_WAIT<br/>  ↓<br/>LAST_ACK<br/>  ↓<br/>CLOSED</code></pre><ol><li><strong>常见面试点</strong></li></ol><p>面试可能追问：</p><pre><code>为什么需要 TIME_WAIT<br/>为什么挥手是四次不是三次<br/>CLOSE_WAIT 过多是什么问题<br/>TIME_WAIT 过多怎么处理</code></pre><p>可以先回答主流程，再补充：</p><pre><code>四次是因为 TCP 全双工，双方发送方向要分别关闭</code></pre><p><strong>面试背诵版</strong></p><p>问题：TCP 四次挥手是什么？</p><blockquote><p>TCP 四次挥手是关闭 TCP 连接的过程，因为 TCP 是全双工协议，客户端到服务端和服务端到客户端两个方向的数据传输需要分别关闭。假设客户端主动关闭连接，第一次挥手是客户端发送 FIN，表示自己已经没有数据要发送了，进入 <code>FIN_WAIT_1</code>。第二次挥手是服务端收到 FIN 后回复 ACK，表示已经知道客户端不再发送数据，服务端进入 <code>CLOSE_WAIT</code>，客户端收到 ACK 后进入 <code>FIN_WAIT_2</code>。此时连接处于半关闭状态，客户端不能再发送数据，但服务端如果还有数据没发完，仍然可以继续发送。第三次挥手是服务端处理完剩余数据后发送 FIN，表示自己也没有数据要发送了，进入 <code>LAST_ACK</code>。第四次挥手是客户端收到服务端 FIN 后回复 ACK，并进入 <code>TIME_WAIT</code>，服务端收到 ACK 后关闭连接，客户端等待 2MSL 后也进入关闭状态。四次挥手的核心原因是 TCP 两个方向的发送通道要分别关闭。</p><p>面试中可以这样收口：</p><p>简单说，四次挥手就是“一方说我不发了，对方确认；对方发完后也说我不发了，一方再确认”；因为 TCP 是全双工，所以两个方向要分别关闭。</p></blockquote>"},{"t":"为什么 TCP 需要四次挥手？","tag":"面试必考","p":"must","html":"<p>核心回答：TCP 需要四次挥手，是因为 TCP 是<strong>全双工通信</strong>，两端的发送方向要分别关闭；一方说“我不发了”，不代表另一方也立刻没数据要发。</p><ol><li><strong>TCP 是全双工协议</strong></li></ol><p>TCP 连接有两个方向：</p><pre><code>客户端 -&gt; 服务端<br/>服务端 -&gt; 客户端</code></pre><p>这两个方向可以同时传输数据。</p><p>关闭连接时，也要分别关闭：</p><pre><code>客户端发送方向<br/>服务端发送方向</code></pre><p>所以不能简单一来一回就直接关掉。</p><ol><li><strong>第一次挥手：主动方说我不发了</strong></li></ol><p>假设客户端主动关闭。</p><p>客户端发送：</p><pre><code>FIN</code></pre><p>含义是：</p><pre><code>我这边数据发完了<br/>我不再发送数据</code></pre><p>但这只表示：</p><pre><code>客户端 -&gt; 服务端 这个方向关闭</code></pre><p>客户端仍然可以接收服务端数据。</p><ol><li><strong>第二次挥手：服务端确认</strong></li></ol><p>服务端收到 FIN 后，回复：</p><pre><code>ACK</code></pre><p>含义是：</p><pre><code>我知道你不发了</code></pre><p>但服务端此时可能还有数据没发完。</p><p>所以它不能马上关闭自己的发送方向。</p><p>这时连接进入半关闭状态：</p><pre><code>客户端不再发<br/>服务端还可以发</code></pre><ol><li><strong>第三次挥手：服务端说我也不发了</strong></li></ol><p>等服务端把剩余数据发送完后，发送：</p><pre><code>FIN</code></pre><p>含义是：</p><pre><code>我这边也发完了<br/>我也不再发送数据</code></pre><p>这才表示：</p><pre><code>服务端 -&gt; 客户端 这个方向也准备关闭</code></pre><ol><li><strong>第四次挥手：客户端确认</strong></li></ol><p>客户端收到服务端 FIN 后，回复：</p><pre><code>ACK</code></pre><p>含义是：</p><pre><code>我知道你也不发了</code></pre><p>至此两个方向都关闭。</p><p>连接才可以真正结束。</p><ol><li><strong>为什么不能三次</strong></li></ol><p>如果服务端收到客户端 FIN 后：</p><pre><code>ACK 和 FIN 一起发</code></pre><p>那就类似三次。</p><p>但问题是：</p><pre><code>服务端不一定已经把数据发完</code></pre><p>它必须先 ACK 客户端的关闭请求。</p><p>等自己业务层数据处理完、发送完，再单独 FIN。</p><p>所以通常是四次。</p><p>当然，如果服务端刚好没有剩余数据要发，ACK 和 FIN 可能合并，但逻辑上仍然是：</p><pre><code>确认对方关闭<br/>自己再发起关闭</code></pre><ol><li><strong>和三次握手为什么不同</strong></li></ol><p>三次握手时，服务端的：</p><pre><code>ACK 和 SYN</code></pre><p>可以合并，因为：</p><pre><code>确认客户端请求<br/>自己也请求建立连接</code></pre><p>这两个动作可以同时做。</p><p>但四次挥手时，服务端收到 FIN 后，通常还不能立刻 FIN，因为可能还有数据要发。</p><p>所以 ACK 和 FIN 通常分开。</p><ol><li><strong>一句话类比</strong></li></ol><p>四次挥手就像打电话：</p><pre><code>A：我说完了<br/>B：我知道了<br/>B：我也说完了<br/>A：我知道了</code></pre><p>A 说完了，不代表 B 也说完了。</p><p>所以需要分别确认。</p><ol><li><strong>面试常带一句 TIME_WAIT</strong></li></ol><p>主动关闭方最后会进入 <code>TIME_WAIT</code>。</p><p>作用是：</p><pre><code>保证最后一个 ACK 能被对方收到<br/>让旧连接中的延迟报文在网络中消失</code></pre><p>所以连接不是发完最后 ACK 就立刻完全消失。</p><p><strong>面试背诵版</strong></p><p>问题：为什么 TCP 需要四次挥手？</p><blockquote><p>TCP 需要四次挥手，核心原因是 TCP 是全双工通信，客户端到服务端和服务端到客户端两个方向的数据传输是相互独立的，关闭连接时也要分别关闭两个发送方向。假设客户端主动关闭连接，它第一次发送 FIN，只表示客户端已经没有数据要发了，客户端到服务端这个方向可以关闭，但客户端仍然可以继续接收服务端的数据。服务端收到 FIN 后，第二次回复 ACK，表示知道客户端不再发送数据，但此时服务端可能还有数据没有发送完，所以不能立刻关闭自己的发送方向。等服务端数据发送完成后，第三次再发送 FIN，表示服务端也没有数据要发了。客户端收到后第四次回复 ACK，确认服务端的关闭请求。这样两个方向都完成关闭，连接才真正结束。相比三次握手，挥手时 ACK 和 FIN 通常不能合并，是因为被动关闭方可能还有数据要继续发送。</p><p>面试中可以这样收口：</p><p>简单说，四次挥手是因为 TCP 是全双工；一方发 FIN 只代表自己不发了，不代表对方也发完了，所以双方要分别 FIN、分别 ACK。</p></blockquote>"},{"t":"为什么不能三次挥手？","tag":"重点","p":"core","html":"<p>核心回答：不能简单固定成三次挥手，是因为 TCP 是全双工的，服务端收到客户端 <code>FIN</code> 后，可能还有数据没发完，所以只能先回 <code>ACK</code>，等自己也发完后再单独发 <code>FIN</code>。</p><ol><li><strong>先看四次挥手逻辑</strong></li></ol><p>假设客户端主动关闭：</p><pre><code>1. Client -&gt; Server：FIN<br/>2. Server -&gt; Client：ACK<br/>3. Server -&gt; Client：FIN<br/>4. Client -&gt; Server：ACK</code></pre><p>含义是：</p><pre><code>客户端：我不发了<br/>服务端：我知道了<br/>服务端：我也不发了<br/>客户端：我知道了</code></pre><p>这是两个方向分别关闭。</p><ol><li><strong>三次挥手想省哪一步</strong></li></ol><p>所谓三次挥手，通常是想把服务端的：</p><pre><code>ACK<br/>FIN</code></pre><p>合并成一次：</p><pre><code>FIN + ACK</code></pre><p>流程变成：</p><pre><code>Client -&gt; Server：FIN<br/>Server -&gt; Client：FIN + ACK<br/>Client -&gt; Server：ACK</code></pre><p>这只有在服务端收到客户端 FIN 时，自己也刚好没有数据要发，才可能成立。</p><ol><li><strong>为什么通常不能合并</strong></li></ol><p>客户端发 <code>FIN</code> 的意思只是：</p><pre><code>客户端没有数据要发了</code></pre><p>但不代表服务端也没数据要发。</p><p>服务端收到 FIN 后，应用层可能还有剩余数据要发送给客户端。</p><p>比如：</p><pre><code>客户端请求关闭上传方向<br/>服务端还要返回最后结果<br/>服务端缓冲区还有数据没发完<br/>应用层还没调用 close</code></pre><p>所以服务端必须先回：</p><pre><code>ACK：我知道你不发了</code></pre><p>等自己数据发完后，再发：</p><pre><code>FIN：我也不发了</code></pre><p>因此 ACK 和 FIN 通常要分开。</p><ol><li><strong>TCP 是全双工</strong></li></ol><p>TCP 有两个独立方向：</p><pre><code>Client -&gt; Server<br/>Server -&gt; Client</code></pre><p>客户端关闭发送方向，只关闭了：</p><pre><code>Client -&gt; Server</code></pre><p>但：</p><pre><code>Server -&gt; Client</code></pre><p>仍然可以继续发送。</p><p>这叫半关闭状态。</p><p>三次挥手如果强制合并，就破坏了这种“对方还能继续发数据”的能力。</p><ol><li><strong>为什么三次握手可以合并，挥手不一定行</strong></li></ol><p>三次握手时，服务端收到 SYN 后：</p><pre><code>ACK 客户端 SYN<br/>自己也发 SYN</code></pre><p>这两个动作可以同时发生，所以合并成：</p><pre><code>SYN + ACK</code></pre><p>但挥手时，服务端收到 FIN 后：</p><pre><code>ACK 可以立即回<br/>FIN 要等自己数据发完才能发</code></pre><p>两个动作不一定能同时发生。</p><p>所以通常不能合并。</p><ol><li><strong>什么时候可能三次挥手</strong></li></ol><p>如果服务端收到客户端 FIN 时，刚好也没有数据要发，并且也准备关闭连接。</p><p>那服务端可能把：</p><pre><code>ACK 和 FIN</code></pre><p>合并到一个包里。</p><p>看起来就像三次挥手。</p><p>但逻辑上仍然是：</p><pre><code>确认客户端关闭<br/>关闭自己发送方向</code></pre><p>只是两个动作被合并发送了。</p><ol><li><strong>面试要点</strong></li></ol><p>不要说：</p><pre><code>TCP 绝对不能三次挥手</code></pre><p>更准确是：</p><pre><code>TCP 逻辑上需要双方分别关闭发送方向，通常表现为四次；<br/>如果 ACK 和 FIN 合并，可能看到三次，但不能固定要求三次。</code></pre><p><strong>面试背诵版</strong></p><p>问题：为什么不能三次挥手？</p><p>TCP 不能简单固定成三次挥手，是因为 TCP 是全双工协议，客户端到服务端和服务端到客户端两个方向的数据传输是独立的。客户端发送 FIN 只表示客户端这一侧没有数据要发送了，也就是关闭客户端到服务端的发送方向，但服务端可能还有数据没有发送完，所以服务端收到 FIN 后通常只能先回复 ACK，表示已经知道客户端不再发送数据。等服务端自己的数据也发送完成、应用层也准备关闭连接后，才会再发送 FIN，表示服务端也不再发送数据。客户端收到服务端 FIN 后再回复 ACK，连接才真正关闭。三次挥手想要把服务端的 ACK 和 FIN 合并，但这只有在服务端刚好也没有数据要发、并且准备关闭时才可能发生。逻辑上仍然是双方分别关闭，只是报文可能被合并。</p><p>面试中可以这样收口：</p><p>简单说，客户端 FIN 不代表服务端也发完了；服务端必须先 ACK，再等自己数据发完后 FIN，所以通常是四次，只有 ACK 和 FIN 恰好合并时才可能看起来像三次。</p><blockquote><p></p></blockquote>"},{"t":"TIME_WAIT 是什么？","tag":"面试必考","p":"must","html":"<p>核心回答：<code>TIME_WAIT</code> 是 TCP 主动关闭方在四次挥手结束后进入的等待状态，主要作用是<strong>确保最后一个 ACK 能被对方收到</strong>，并让旧连接中的迟到报文在网络中自然消失。</p><ol><li><strong>TIME_WAIT 出现在哪里</strong></li></ol><p>TCP 四次挥手最后阶段：</p><pre><code>主动关闭方发送最后一个 ACK<br/>  ↓<br/>进入 TIME_WAIT<br/>  ↓<br/>等待 2MSL<br/>  ↓<br/>真正关闭连接</code></pre><p>通常谁主动关闭连接，谁进入 <code>TIME_WAIT</code>。</p><p>比如客户端主动断开：</p><pre><code>客户端进入 TIME_WAIT</code></pre><p>如果服务端主动断开：</p><pre><code>服务端进入 TIME_WAIT</code></pre><ol><li><strong>为什么需要 TIME_WAIT</strong></li></ol><p>主要有两个原因。</p><p>第一个原因：保证最后一个 ACK 能可靠到达。</p><p>四次挥手最后一步是：</p><pre><code>主动关闭方 → 被动关闭方：ACK</code></pre><p>如果这个 ACK 丢了，被动关闭方会重新发送 <code>FIN</code>。</p><p>如果主动关闭方还在 <code>TIME_WAIT</code>，就可以再次发送 ACK。</p><p>如果主动关闭方直接关闭了，就无法处理这个重传 FIN，可能导致对方一直等。</p><ol><li><strong>防止旧连接报文影响新连接</strong></li></ol><p>TCP 连接由四元组唯一标识：</p><pre><code>源 IP<br/>源端口<br/>目标 IP<br/>目标端口</code></pre><p>如果一个连接刚关闭，马上又用相同四元组创建新连接，网络中旧连接的迟到报文可能会误伤新连接。</p><p><code>TIME_WAIT</code> 等待一段时间，就是为了让旧连接中的残留报文在网络中失效。</p><ol><li><strong>为什么是 2MSL</strong></li></ol><p><code>MSL</code> 是 Maximum Segment Lifetime，表示一个 TCP 报文在网络中最大存活时间。</p><p>等待 <code>2MSL</code> 是因为：</p><pre><code>一个方向的报文最多存活 MSL<br/>另一个方向的响应报文最多存活 MSL</code></pre><p>所以等待 <code>2MSL</code> 后，基本可以认为旧连接相关报文都已经消失。</p><ol><li><strong>TIME_WAIT 过多有什么问题</strong></li></ol><p>如果服务器频繁主动关闭连接，就可能出现大量 <code>TIME_WAIT</code>。</p><p>问题包括：</p><pre><code>占用端口<br/>占用连接资源<br/>影响新连接建立</code></pre><p>尤其是短连接、高并发场景下，<code>TIME_WAIT</code> 会比较明显。</p><p>不过 <code>TIME_WAIT</code> 本身不是错误，它是 TCP 正常机制。</p><ol><li><strong>如何优化 TIME_WAIT 过多</strong></li></ol><p>常见思路：</p><pre><code>尽量使用长连接 / 连接池<br/>让客户端主动关闭连接<br/>减少短连接频繁创建<br/>调整内核参数<br/>开启端口复用</code></pre><p>比如 HTTP 服务中可以使用：</p><pre><code>Keep-Alive<br/>连接池</code></pre><p>减少频繁建立和关闭 TCP 连接。</p><p>但内核参数不能随便改，尤其生产环境要评估风险。</p><ol><li><strong>面试容易问的点</strong></li></ol><p>面试里要重点说清楚：</p><pre><code>TIME_WAIT 是主动关闭方进入的状态<br/>等待时间通常是 2MSL<br/>作用是保证最后 ACK 可重传<br/>作用是防止旧报文影响新连接<br/>TIME_WAIT 多不一定是异常</code></pre><p><strong>面试背诵版</strong></p><p>问题：TIME_WAIT 是什么？为什么需要 TIME_WAIT？</p><p>TIME_WAIT 是 TCP 主动关闭方在四次挥手完成后进入的等待状态。它不会立刻释放连接，而是等待 2MSL 时间后才真正关闭。TIME_WAIT 主要有两个作用：第一，保证最后一个 ACK 能被对方收到，如果最后 ACK 丢失，被动关闭方会重传 FIN，主动关闭方在 TIME_WAIT 状态下还能再次发送 ACK；第二，防止旧连接中延迟到达的报文影响后续使用相同四元组的新连接。TIME_WAIT 过多通常出现在短连接、高并发、服务端频繁主动关闭连接的场景，可以通过长连接、连接池、Keep-Alive、减少频繁建连等方式优化。</p><p>面试中可以这样收口：</p><p>简单说，TIME_WAIT 是 TCP 为了“可靠关闭”和“清理旧报文”设计的等待状态；谁主动关闭，谁通常进入 TIME_WAIT。</p><blockquote><p></p></blockquote>"},{"t":"为什么需要 TIME_WAIT？","tag":"面试必考","p":"must","html":"<p>核心回答：需要 <code>TIME_WAIT</code> 是为了保证 TCP 连接能<strong>可靠关闭</strong>，并防止旧连接的延迟报文影响新连接；它不是异常，而是 TCP 主动关闭方必须承担的“善后状态”。</p><ol><li><strong>TIME_WAIT 出现在哪一步</strong></li></ol><p>TCP 四次挥手最后是：</p><pre><code>被动关闭方 → 主动关闭方：FIN<br/>主动关闭方 → 被动关闭方：ACK</code></pre><p>主动关闭方发送最后一个 <code>ACK</code> 后，不会马上释放连接，而是进入：</p><pre><code>TIME_WAIT</code></pre><p>等待一段时间后才真正关闭。</p><p>通常是：</p><pre><code>谁主动关闭连接，谁进入 TIME_WAIT</code></pre><ol><li><strong>原因一：保证最后一个 ACK 能被对方收到</strong></li></ol><p>最后一个 <code>ACK</code> 也可能丢包。</p><p>如果主动关闭方发送完 <code>ACK</code> 后立刻关闭，一旦这个 <code>ACK</code> 丢了，被动关闭方会一直认为自己的 <code>FIN</code> 没被确认，于是会重传 <code>FIN</code>。</p><p>这时如果主动关闭方还在 <code>TIME_WAIT</code>，就能收到重传的 <code>FIN</code>，并再次发送 <code>ACK</code>。</p><p>流程是：</p><pre><code>主动关闭方发送 ACK<br/>  ↓<br/>ACK 丢失<br/>  ↓<br/>被动关闭方重传 FIN<br/>  ↓<br/>主动关闭方仍在 TIME_WAIT<br/>  ↓<br/>主动关闭方重新发送 ACK</code></pre><p>所以 <code>TIME_WAIT</code> 的第一个作用是：</p><pre><code>保证四次挥手能够可靠完成</code></pre><ol><li><strong>原因二：防止旧报文影响新连接</strong></li></ol><p>TCP 连接由四元组标识：</p><pre><code>源 IP + 源端口 + 目标 IP + 目标端口</code></pre><p>如果连接刚关闭，马上又用相同四元组创建新连接，网络中旧连接的延迟报文可能还没消失。</p><p>如果这些旧报文被新连接收到，就可能造成数据混乱。</p><p><code>TIME_WAIT</code> 等待一段时间，就是为了让旧连接中的残留报文在网络中自然过期。</p><ol><li><strong>为什么等待 2MSL</strong></li></ol><p><code>MSL</code> 是 Maximum Segment Lifetime，表示一个 TCP 报文在网络中的最大生存时间。</p><p>等待 <code>2MSL</code> 的含义是：</p><pre><code>旧连接最后一个报文最多存活 MSL<br/>对方可能重传的报文最多也存活 MSL</code></pre><p>所以等待 <code>2MSL</code> 后，可以认为旧连接相关报文基本都已经失效。</p><ol><li><strong>TIME_WAIT 一定是坏事吗</strong></li></ol><p>不是。</p><p><code>TIME_WAIT</code> 是 TCP 正常机制，不是错误。</p><p>但如果大量短连接频繁创建和关闭，就会产生很多 <code>TIME_WAIT</code>，可能带来：</p><pre><code>占用端口资源<br/>占用内核连接表资源<br/>影响新连接建立</code></pre><p>尤其是服务端主动关闭大量连接时，服务端容易堆积 <code>TIME_WAIT</code>。</p><ol><li><strong>如何减少 TIME_WAIT 过多</strong></li></ol><p>常见优化方式：</p><pre><code>使用长连接<br/>使用连接池<br/>开启 HTTP Keep-Alive<br/>减少服务端主动关闭连接<br/>合理调整内核参数</code></pre><p>但面试里要注意，不要一上来就说改内核参数。</p><p>更好的回答顺序是：</p><pre><code>先从业务和连接模型优化<br/>再考虑系统参数调优</code></pre><p><strong>面试背诵版</strong></p><p>问题：53. 为什么需要 TIME_WAIT？（面试必考）</p><p>TIME_WAIT 是 TCP 主动关闭方在发送最后一个 ACK 后进入的等待状态，它的核心作用有两个。第一，保证连接能够可靠关闭。因为最后一个 ACK 也可能丢失，如果主动关闭方立刻释放连接，被动关闭方重传 FIN 时就没人响应了；而主动关闭方处于 TIME_WAIT 时，可以收到重传的 FIN 并再次发送 ACK。第二，防止旧连接中的延迟报文影响新连接。TCP 连接通过源 IP、源端口、目标 IP、目标端口四元组标识，如果马上复用同一个四元组建立新连接，旧连接中滞留在网络里的报文可能被新连接误收。TIME_WAIT 通常等待 2MSL，就是为了让旧报文在网络中自然失效。所以 TIME_WAIT 不是异常，而是 TCP 为可靠关闭和报文隔离设计的机制。</p><p>面试中可以这样收口：</p><p>简单说，TIME_WAIT 是为了两件事：最后的 ACK 丢了还能补发，旧连接的残留报文不会污染新连接。</p><blockquote><p></p></blockquote>"},{"t":"TIME_WAIT 为什么是 2MSL？","tag":"重点","p":"core","html":"<p>核心回答：<code>TIME_WAIT</code> 等待 <code>2MSL</code>，是为了给 TCP 连接关闭过程留出足够时间：<strong>一个 MSL 等旧报文在网络中消失，另一个 MSL 等可能的 FIN 重传和 ACK 补发完成</strong>。</p><ol><li><strong>MSL 是什么</strong></li></ol><p><code>MSL</code> 全称是：</p><pre><code>Maximum Segment Lifetime</code></pre><p>意思是：</p><pre><code>一个 TCP 报文段在网络中的最大生存时间</code></pre><p>也就是说，一个 TCP 包在网络里不可能无限存在，超过 <code>MSL</code> 后就应该被丢弃或失效。</p><ol><li><strong>TIME_WAIT 发生在哪一端</strong></li></ol><p>通常是主动关闭方进入 <code>TIME_WAIT</code>。</p><p>流程是：</p><pre><code>被动关闭方发送 FIN<br/>  ↓<br/>主动关闭方回复 ACK<br/>  ↓<br/>主动关闭方进入 TIME_WAIT</code></pre><p>主动关闭方需要等待一段时间，不能马上释放连接。</p><ol><li><strong>为什么不是 1MSL</strong></li></ol><p>因为关闭连接时，不只要考虑旧数据包，还要考虑最后一次挥手是否可靠。</p><p>如果最后一个 <code>ACK</code> 丢了，被动关闭方会重传 <code>FIN</code>。</p><p>这个重传 <code>FIN</code> 可能最多经过一个 <code>MSL</code> 才到主动关闭方。</p><p>主动关闭方收到后，还要再发一次 <code>ACK</code>，这个 <code>ACK</code> 也可能在网络中最多存活一个 <code>MSL</code>。</p><p>所以需要：</p><pre><code>FIN 最多存活 MSL<br/>ACK 最多存活 MSL<br/>合起来就是 2MSL</code></pre><ol><li><strong>2MSL 解决了什么问题</strong></li></ol><p>主要解决两个问题：</p><pre><code>保证最后 ACK 丢失时还能重传 ACK<br/>让旧连接中的延迟报文全部过期</code></pre><p>如果不等 <code>2MSL</code>，旧连接刚关闭就复用相同四元组，新连接可能收到旧连接的迟到报文。</p><p>TCP 连接由四元组标识：</p><pre><code>源 IP + 源端口 + 目标 IP + 目标端口</code></pre><p><code>2MSL</code> 后，旧连接相关报文基本都已经从网络中消失。</p><ol><li><strong>可以这样理解</strong></li></ol><p>用一个简单流程表示：</p><pre><code>最后 ACK 丢失<br/>  ↓<br/>对方重传 FIN       最多等待 1MSL<br/>  ↓<br/>本端重发 ACK       ACK 最多存活 1MSL<br/>  ↓<br/>连接安全关闭</code></pre><p>所以 <code>2MSL</code> 不是随便定的，而是为了覆盖关闭阶段两个方向报文的最大生命周期。</p><ol><li><strong>面试注意点</strong></li></ol><p>面试里要避免只说：</p><pre><code>为了让旧报文消失</code></pre><p>更完整的答案应该是：</p><pre><code>既为了旧报文过期<br/>也为了最后 ACK 丢失时能处理 FIN 重传</code></pre><p>这才是高分回答。</p><p><strong>面试背诵版</strong></p><p>问题：TIME_WAIT 为什么是 2MSL？</p><blockquote><p>MSL 是 TCP 报文段在网络中的最大生存时间。TIME_WAIT 等待 2MSL，是为了保证 TCP 连接能够可靠关闭，并防止旧连接报文影响新连接。具体来说，主动关闭方发送最后一个 ACK 后，如果这个 ACK 丢失，被动关闭方会重传 FIN，这个 FIN 在网络中最多存活一个 MSL；主动关闭方收到重传 FIN 后会重新发送 ACK，这个 ACK 也可能在网络中最多存活一个 MSL，所以合起来需要等待 2MSL。另外，等待 2MSL 也可以让旧连接中滞留的报文自然过期，避免相同四元组的新连接收到旧报文。</p><p>面试中可以这样收口：</p><p>简单说，<strong>2MSL 是给“对方重传 FIN”和“我方补发 ACK”各留一个最大报文生命周期，同时也确保旧连接报文彻底失效。</strong></p></blockquote>"},{"t":"CLOSE_WAIT 是什么？","tag":"面试必考","p":"must","html":"<p>核心回答：<code>CLOSE_WAIT</code> 是 TCP <strong>被动关闭方</strong>收到对方 <code>FIN</code> 后进入的状态，表示“对方已经不发数据了，我已经 ACK 了，但我这边应用还没调用 <code>close()</code> 关闭连接”。</p><ol><li><strong>CLOSE_WAIT 出现在哪一端</strong></li></ol><p>TCP 四次挥手中，如果对方先关闭连接：</p><pre><code>对方发送 FIN<br/>  ↓<br/>本端收到 FIN<br/>  ↓<br/>本端回复 ACK<br/>  ↓<br/>本端进入 CLOSE_WAIT</code></pre><p>所以：</p><pre><code>CLOSE_WAIT 出现在被动关闭方<br/>TIME_WAIT 通常出现在主动关闭方</code></pre><ol><li><strong>CLOSE_WAIT 表示什么</strong></li></ol><p><code>CLOSE_WAIT</code> 的含义是：</p><pre><code>对方已经关闭发送方向<br/>本端已经确认了对方的 FIN<br/>但本端应用还没有关闭自己的发送方向</code></pre><p>TCP 是全双工协议。</p><p>对方发 <code>FIN</code> 只代表：</p><pre><code>对方不再发送数据</code></pre><p>不代表：</p><pre><code>本端不能继续发送数据</code></pre><p>所以本端还需要等应用层处理完剩余逻辑，再调用：</p><pre><code>close(fd)</code></pre><p>然后内核才会发送自己的 <code>FIN</code>。</p><ol><li><strong>CLOSE_WAIT 正常流程</strong></li></ol><p>正常情况下，<code>CLOSE_WAIT</code> 应该很短暂：</p><pre><code>收到 FIN<br/>  ↓<br/>回复 ACK<br/>  ↓<br/>应用读到 EOF<br/>  ↓<br/>应用调用 close()<br/>  ↓<br/>发送 FIN<br/>  ↓<br/>进入 LAST_ACK</code></pre><p>也就是说，<code>CLOSE_WAIT</code> 是一个过渡状态。</p><ol><li><strong>CLOSE_WAIT 过多说明什么</strong></li></ol><p>如果系统中出现大量 <code>CLOSE_WAIT</code>，通常说明：</p><pre><code>对方已经关闭连接<br/>但本地程序没有及时 close socket</code></pre><p>这往往是应用层 bug。</p><p>常见原因包括：</p><pre><code>连接使用完没有关闭<br/>异常分支没有释放 fd<br/>线程阻塞导致 close 没执行<br/>连接池管理不当<br/>读到 EOF 后没有处理</code></pre><p>所以 <code>CLOSE_WAIT</code> 多，一般不是网络问题，而是应用程序没有正确释放连接。</p><ol><li><strong>CLOSE_WAIT 和 TIME_WAIT 的区别</strong></li></ol><sheet></sheet><ol><li><strong>如何排查 CLOSE_WAIT 过多</strong></li></ol><p>可以先看连接状态：</p><pre><code>netstat -antp | grep CLOSE_WAIT</code></pre><p>或：</p><pre><code>ss -antp state close-wait</code></pre><p>重点看：</p><pre><code>哪个进程占用<br/>哪个远端连接<br/>是否持续增长<br/>应用代码是否 close<br/>异常分支是否释放资源<br/>连接池是否归还连接</code></pre><p>如果是 Java、Python、Go 服务，要重点检查连接、响应体、文件描述符是否正确关闭。</p><p><strong>面试背诵版</strong></p><p>问题：CLOSE_WAIT 是什么？</p><blockquote><p>CLOSE_WAIT 是 TCP 被动关闭方收到对方 FIN 后进入的状态。它表示对方已经关闭了发送方向，本端也已经回复 ACK，但本端应用层还没有调用 close 关闭自己的 socket。由于 TCP 是全双工协议，对方发送 FIN 只代表对方不再发送数据，并不代表本端不能继续发送，所以本端需要等应用处理完剩余数据后主动调用 close，内核才会发送自己的 FIN，并进入 LAST_ACK。正常情况下 CLOSE_WAIT 应该很短暂，如果系统中出现大量 CLOSE_WAIT，通常说明应用程序没有及时关闭 socket，比如异常分支没有释放连接、读到 EOF 后没有 close、连接池管理不当等。</p><p>面试中可以这样收口：</p><p>简单说，CLOSE_WAIT 是“对方关了，我知道了，但我这边应用还没关”；大量 CLOSE_WAIT 通常不是 TCP 的问题，而是应用没有正确释放连接。</p></blockquote>"},{"t":"大量 CLOSE_WAIT 说明什么问题？","tag":"面试必考","p":"must","html":"<p>核心回答：大量 <code>CLOSE_WAIT</code> 通常说明<strong>对端已经关闭连接，但本机应用没有及时关闭 socket</strong>，本质上多半是应用层资源释放问题，而不是 TCP 协议本身的问题。</p><ol><li><strong>CLOSE_WAIT 怎么产生</strong></li></ol><p>TCP 连接中，如果对端主动关闭：</p><pre><code>对端发送 FIN<br/>  ↓<br/>本机内核回复 ACK<br/>  ↓<br/>本机进入 CLOSE_WAIT</code></pre><p>这时含义是：</p><pre><code>对端已经不再发送数据<br/>本机已经确认<br/>但本机应用还没调用 close()</code></pre><p>只有应用调用：</p><pre><code>close(fd)</code></pre><p>本机才会继续发送自己的 <code>FIN</code>，进入后续关闭流程。</p><ol><li><strong>大量 CLOSE_WAIT 说明什么</strong></li></ol><p>如果 <code>CLOSE_WAIT</code> 很多，说明连接卡在这里没有继续关闭。</p><p>也就是：</p><pre><code>内核已经收到对端关闭请求<br/>但应用层没有关闭连接</code></pre><p>常见结论是：</p><pre><code>应用没有正确释放 socket / fd</code></pre><p>所以大量 <code>CLOSE_WAIT</code> 通常是代码问题、连接管理问题、资源释放问题。</p><ol><li><strong>常见原因</strong></li></ol><p>常见原因包括：</p><pre><code>读到 EOF 后没有 close<br/>异常分支没有释放连接<br/>HTTP 响应体没有关闭<br/>数据库 / Redis / HTTP 连接池没有归还连接<br/>业务线程阻塞，导致 close 逻辑没执行<br/>死锁或慢请求卡住<br/>连接泄漏</code></pre><p>比如在 HTTP 客户端里，只发请求但不关闭响应体，就可能导致连接长期不释放。</p><ol><li><strong>CLOSE_WAIT 和 TIME_WAIT 不一样</strong></li></ol><sheet></sheet><p>所以面试里要强调：</p><pre><code>TIME_WAIT 多不一定有问题<br/>CLOSE_WAIT 多通常要重点排查应用</code></pre><ol><li><strong>如何排查</strong></li></ol><p>先定位哪个进程：</p><pre><code>ss -antp state close-wait</code></pre><p>或：</p><pre><code>netstat -antp | grep CLOSE_WAIT</code></pre><p>再看文件描述符是否持续增长：</p><pre><code>lsof -p &lt;pid&gt;</code></pre><p>然后结合应用日志排查：</p><pre><code>是否有异常堆栈<br/>是否有请求超时<br/>线程是否阻塞<br/>连接池是否耗尽<br/>是否忘记 close</code></pre><p>如果是 Java，可以重点看：</p><pre><code>HTTP Response 是否关闭<br/>InputStream 是否关闭<br/>数据库连接是否归还连接池<br/>线程 dump 是否卡住</code></pre><p>如果是 Go，要检查：</p><pre><code>resp.Body.Close() 是否执行<br/>defer close 是否在正确位置<br/>goroutine 是否阻塞</code></pre><p>如果是 Python，要检查：</p><pre><code>requests response 是否关闭<br/>文件 / socket 是否用 with 管理<br/>连接池是否释放</code></pre><ol><li><strong>解决思路</strong></li></ol><p>解决大量 <code>CLOSE_WAIT</code> 的核心是：</p><pre><code>确保应用在所有路径都能关闭连接</code></pre><p>常见做法：</p><pre><code>使用 finally / defer / with 释放资源<br/>读到 EOF 后及时 close<br/>连接池正确归还连接<br/>设置合理超时<br/>排查阻塞线程<br/>避免异常路径漏关</code></pre><p>不要指望单纯调内核参数解决 <code>CLOSE_WAIT</code>，因为根因通常在应用层。</p><p><strong>面试背诵版</strong></p><blockquote><p></p><p>大量 CLOSE_WAIT 通常说明对端已经关闭连接，本机内核也已经回复 ACK，但本机应用程序没有及时调用 close 关闭 socket，导致连接一直停留在 CLOSE_WAIT 状态。CLOSE_WAIT 出现在被动关闭方，它本质上是在等待应用层释放连接，所以如果数量很多，通常不是 TCP 协议问题，而是应用层资源释放问题，比如读到 EOF 后没有 close、异常分支没有释放连接、HTTP 响应体没有关闭、连接池没有正确归还、业务线程阻塞或连接泄漏。排查时可以用 ss 或 netstat 查看哪个进程有大量 CLOSE_WAIT，再结合 lsof、线程栈、应用日志和连接池状态，重点检查所有代码路径是否正确关闭 socket。</p><p>面试中可以这样收口：</p><p>简单说，大量 CLOSE_WAIT 说明“对方已经关了，但我方应用没关”；它通常是应用连接泄漏或资源释放不完整导致的。</p></blockquote>"},{"t":"大量 TIME_WAIT 说明什么问题？","tag":"面试必考","p":"must","html":"<p>核心回答：大量 <code>TIME_WAIT</code> 说明本机作为<strong>主动关闭方</strong>关闭了很多 TCP 连接，常见于短连接、高并发、连接复用不足的场景；它不一定是 bug，但可能带来端口和连接资源压力。</p><ol><li><strong>TIME_WAIT 怎么产生</strong></li></ol><p>TCP 四次挥手最后阶段：</p><pre><code>本机主动关闭连接<br/>  ↓<br/>对端回复 FIN<br/>  ↓<br/>本机发送最后 ACK<br/>  ↓<br/>本机进入 TIME_WAIT</code></pre><p>所以 <code>TIME_WAIT</code> 的关键点是：</p><pre><code>谁主动关闭，谁通常进入 TIME_WAIT</code></pre><p>如果服务器上大量 <code>TIME_WAIT</code>，往往说明服务器主动关闭了大量连接，或者服务器作为客户端频繁访问下游服务。</p><ol><li><strong>大量 TIME_WAIT 说明什么</strong></li></ol><p>大量 <code>TIME_WAIT</code> 通常说明：</p><pre><code>短连接太多<br/>连接频繁创建和关闭<br/>长连接 / 连接池使用不足<br/>本机频繁主动关闭连接</code></pre><p>比如：</p><pre><code>HTTP 没开 Keep-Alive<br/>客户端没有复用连接<br/>服务端主动断开连接<br/>微服务之间频繁短连接调用<br/>爬虫 / 网关 / 代理高并发请求</code></pre><p>它不一定表示程序泄漏，因为 <code>TIME_WAIT</code> 会在等待 <code>2MSL</code> 后自动释放。</p><ol><li><strong>TIME_WAIT 和 CLOSE_WAIT 的区别</strong></li></ol><sheet></sheet><p>面试里要特别强调：</p><pre><code>TIME_WAIT 多不一定是异常<br/>CLOSE_WAIT 多更像应用 bug</code></pre><ol><li><strong>大量 TIME_WAIT 的影响</strong></li></ol><p>如果数量非常多，可能带来：</p><pre><code>占用本地端口<br/>占用内核连接表资源<br/>增加连接管理开销<br/>导致新连接建立失败</code></pre><p>尤其是本机作为客户端频繁访问同一个目标时，本地临时端口可能耗尽。</p><p>典型报错可能是：</p><pre><code>Cannot assign requested address<br/>Address already in use</code></pre><ol><li><strong>如何排查</strong></li></ol><p>查看 <code>TIME_WAIT</code> 数量：</p><pre><code>ss -ant state time-wait | wc -l</code></pre><p>查看连接分布：</p><pre><code>ss -ant state time-wait</code></pre><p>统计远端地址：</p><pre><code>ss -ant state time-wait | awk '{print $4, $5}' | sort | uniq -c | sort -nr | head</code></pre><p>重点看：</p><pre><code>是否集中访问某个下游<br/>是否 HTTP 短连接过多<br/>是否连接池未生效<br/>是否服务端主动关闭连接<br/>是否 QPS 突增</code></pre><ol><li><strong>如何优化</strong></li></ol><p>优先从应用和架构层优化：</p><pre><code>使用 HTTP Keep-Alive<br/>使用连接池<br/>减少短连接<br/>避免服务端主动关闭<br/>控制请求并发<br/>复用下游连接</code></pre><p>系统层可以考虑：</p><pre><code>扩大本地临时端口范围<br/>缩短 FIN/TIME_WAIT 相关等待时间<br/>开启端口复用相关参数</code></pre><p>但内核参数要谨慎，不能一上来就乱调。</p><p><strong>面试背诵版</strong></p><p>问题：大量 TIME_WAIT 说明什么问题？</p><blockquote><p>大量 TIME_WAIT 通常说明本机主动关闭了大量 TCP 连接，常见于短连接、高并发、连接池未生效、HTTP Keep-Alive 没开启、服务端主动断开连接，或者本机作为客户端频繁访问下游服务的场景。TIME_WAIT 是 TCP 正常关闭机制，主要用于保证最后一个 ACK 可以重传，并让旧连接报文在网络中失效，所以它不一定是 bug，等待 2MSL 后会自动释放。但如果数量非常多，可能会占用本地端口和内核连接资源，严重时导致新连接建立失败。排查时应先看连接是否集中到某个远端、是否存在大量短连接、连接池是否生效，再考虑调整系统参数。</p><p>面试中可以这样收口：</p><p>简单说，大量 TIME_WAIT 说明“我方主动关闭了太多连接”；它通常是短连接和连接复用不足导致的，不一定是程序 bug，但需要关注端口和连接资源压力。</p></blockquote>"},{"t":"TCP 半连接队列和全连接队列是什么？","tag":"","p":"","html":""},{"t":"SYN Flood 是什么？","tag":"","p":"","html":""},{"t":"TCP Keepalive 是什么？","tag":"","p":"","html":""}]},{"no":"五","title":"TCP 可靠传输","questions":[{"t":"TCP 的序列号是什么？","tag":"","p":"","html":""},{"t":"TCP 的 ACK 是什么？","tag":"","p":"","html":""},{"t":"TCP 如何实现可靠传输？","tag":"面试必考","p":"must","html":"<p>核心回答：TCP 通过<strong>序列号、确认应答、超时重传、滑动窗口、流量控制、拥塞控制、校验和</strong>等机制，实现面向连接、可靠、有序、不丢不重的字节流传输。</p><ol><li><strong>序列号保证可识别</strong></li></ol><p>TCP 会给每个字节分配一个序列号。</p><p>比如：</p><pre><code>发送方发送 seq = 1000，长度 = 500</code></pre><p>表示发送的是：</p><pre><code>1000 ~ 1499 这段字节</code></pre><p>接收方可以根据序列号判断：</p><pre><code>有没有丢包<br/>有没有重复包<br/>数据顺序是否正确</code></pre><p>所以序列号是 TCP 可靠传输的基础。</p><ol><li><strong>ACK 确认机制</strong></li></ol><p>接收方收到数据后，会返回确认号 <code>ACK</code>。</p><p>比如收到 <code>1000 ~ 1499</code> 后，会回复：</p><pre><code>ack = 1500</code></pre><p>意思是：</p><pre><code>1500 之前的数据我都收到了，下次从 1500 开始发</code></pre><p>TCP 的 ACK 是累计确认，不是每个包都必须单独确认。</p><ol><li><strong>超时重传</strong></li></ol><p>发送方发出数据后，会启动定时器。</p><p>如果一段时间内没有收到对应 ACK，就认为数据可能丢失，然后重传。</p><p>流程是：</p><pre><code>发送数据<br/>  ↓<br/>等待 ACK<br/>  ↓<br/>超时未收到 ACK<br/>  ↓<br/>重传数据</code></pre><p>这保证了丢包后仍然可以恢复。</p><ol><li><strong>快速重传</strong></li></ol><p>如果接收方发现中间有包丢了，会不断 ACK 已经连续收到的最后位置。</p><p>比如：</p><pre><code>收到 1、2、4、5</code></pre><p>缺少 3，就会重复发送：</p><pre><code>ACK 3<br/>ACK 3<br/>ACK 3</code></pre><p>发送方收到多个重复 ACK 后，不用等超时，就可以提前重传丢失的数据。</p><p>这就是快速重传。</p><ol><li><strong>滑动窗口提高效率</strong></li></ol><p>如果每发一个包都等 ACK，效率很低。</p><p>TCP 使用滑动窗口，允许发送方连续发送多个未确认的数据包。</p><pre><code>窗口内的数据可以连续发送<br/>收到 ACK 后窗口向前滑动</code></pre><p>这样既保证可靠性，又提升吞吐量。</p><ol><li><strong>流量控制防止接收方被打爆</strong></li></ol><p>接收方会通过 TCP 头部里的窗口字段告诉发送方：</p><pre><code>我还能接收多少数据</code></pre><p>这叫接收窗口 <code>rwnd</code>。</p><p>如果接收方处理慢，窗口变小，发送方就少发。</p><p>如果窗口为 0，发送方会暂停发送，并通过窗口探测等待恢复。</p><ol><li><strong>拥塞控制防止网络被打爆</strong></li></ol><p>流量控制保护接收方，拥塞控制保护网络。</p><p>TCP 会根据网络状况动态调整发送速度，常见机制包括：</p><pre><code>慢启动<br/>拥塞避免<br/>快重传<br/>快恢复</code></pre><p>如果网络丢包严重，TCP 会降低发送速率；如果网络稳定，再逐步提升。</p><ol><li><strong>校验和保证数据正确</strong></li></ol><p>TCP 头部有校验和。</p><p>接收方会校验数据是否在传输过程中损坏。</p><p>如果校验失败，就丢弃该报文，不返回确认，发送方之后会重传。</p><ol><li><strong>有序交付和去重</strong></li></ol><p>接收方根据序列号处理数据：</p><pre><code>重复数据：丢弃<br/>乱序数据：缓存<br/>缺失数据：等待重传<br/>连续数据：交给应用层</code></pre><p>所以应用层看到的是：</p><pre><code>有序、可靠的字节流</code></pre><p>而不是乱序的数据包。</p><p><strong>面试背诵版</strong></p><p>问题：TCP 如何实现可靠传输？</p><p>TCP 主要通过序列号、确认应答、超时重传、快速重传、滑动窗口、流量控制、拥塞控制和校验和来实现可靠传输。发送方会给每个字节分配序列号，接收方根据序列号判断数据是否丢失、重复或乱序，并通过 ACK 告诉发送方哪些数据已经收到。如果发送方在超时时间内没有收到 ACK，就会重传数据；如果收到多个重复 ACK，也可以触发快速重传。为了提高效率，TCP 使用滑动窗口，允许多个数据包连续发送；同时通过接收窗口实现流量控制，避免接收方处理不过来；通过拥塞控制根据网络状况调整发送速度，避免把网络打爆。最后，TCP 还通过校验和检测数据损坏，并通过序列号实现去重和有序交付。</p><p>面试中可以这样收口：</p><p>简单说，TCP 靠“编号、确认、重传、窗口、限速、校验”实现可靠传输；丢了能重传，乱了能排序，重复了能丢弃。</p><blockquote><p></p></blockquote>"},{"t":"什么是超时重传？","tag":"面试必考","p":"must","html":"<p>核心回答：超时重传是 TCP 的可靠传输机制：<strong>发送方发出数据后，如果在规定时间内没有收到 ACK，就认为数据或 ACK 可能丢失，于是重新发送这段数据</strong>。</p><ol><li><strong>为什么需要超时重传</strong></li></ol><p>网络传输并不可靠，可能出现：</p><pre><code>数据包丢失<br/>ACK 丢失<br/>网络拥塞导致延迟过大<br/>路由异常</code></pre><p>如果 TCP 只发送一次，数据丢了就无法恢复。</p><p>所以 TCP 需要一种兜底机制：</p><pre><code>没收到确认，就重新发</code></pre><p>这就是超时重传。</p><ol><li><strong>超时重传的基本流程</strong></li></ol><p>流程可以理解为：</p><pre><code>发送方发送数据<br/>  ↓<br/>启动重传定时器<br/>  ↓<br/>等待接收方 ACK<br/>  ↓<br/>超时时间内收到 ACK：取消定时器<br/>  ↓<br/>超时时间内没收到 ACK：重传数据</code></pre><p>比如：</p><pre><code>发送 seq = 1000 的数据<br/>等待 ack = 1500<br/>如果超时还没收到 ack = 1500<br/>就重传 seq = 1000 这段数据</code></pre><ol><li><strong>超时可能意味着什么</strong></li></ol><p>超时不一定说明数据包丢了，也可能是：</p><pre><code>数据包丢了<br/>ACK 丢了<br/>ACK 在路上延迟了<br/>网络拥塞了</code></pre><p>所以 TCP 不能区分具体原因，只能保守地重传。</p><ol><li><strong>RTO 是什么</strong></li></ol><p>超时重传的等待时间叫：</p><pre><code>RTO：Retransmission Timeout</code></pre><p>也就是重传超时时间。</p><p>RTO 不能太短，也不能太长：</p><sheet></sheet><p>所以 TCP 会根据网络 RTT 动态计算 RTO。</p><ol><li><strong>RTT 和 RTO 的关系</strong></li></ol><p><code>RTT</code> 是一次数据发送到收到 ACK 的往返时间。</p><pre><code>RTT = 数据发出到 ACK 返回的时间</code></pre><p>TCP 会不断估算网络 RTT，然后设置合适的 RTO。</p><p>一般来说：</p><pre><code>网络延迟大，RTO 变大<br/>网络延迟小，RTO 变小</code></pre><p>这样可以适应不同网络环境。</p><ol><li><strong>超时重传和快速重传的区别</strong></li></ol><sheet></sheet><p>所以：</p><pre><code>超时重传是兜底机制<br/>快速重传是优化机制</code></pre><ol><li><strong>超时重传的副作用</strong></li></ol><p>超时重传通常会被 TCP 认为是网络拥塞信号。</p><p>因此除了重传数据，TCP 还可能会：</p><pre><code>降低拥塞窗口<br/>减少发送速率<br/>重新慢启动</code></pre><p>这样可以避免继续加重网络拥塞。</p><p><strong>面试背诵版</strong></p><p>问题：什么是超时重传？</p><p>超时重传是 TCP 实现可靠传输的重要机制。发送方发送数据后，会启动一个重传定时器，如果在 RTO 时间内没有收到接收方的 ACK，就认为这个数据包或者对应 ACK 可能丢失，于是重新发送这段数据。RTO 不能固定得太短或太长，太短会导致不必要的重传，太长会导致丢包恢复慢，所以 TCP 会根据网络 RTT 动态计算 RTO。超时重传和快速重传不同，超时重传依赖定时器，是一种兜底机制；快速重传依赖重复 ACK，可以更早发现部分丢包。超时重传发生后，TCP 通常还会认为网络可能拥塞，从而降低发送速率。</p><p>面试中可以这样收口：</p><p>简单说，超时重传就是“发出去的数据迟迟没人确认，就再发一次”；它是 TCP 可靠传输的兜底机制。</p><blockquote><p></p></blockquote>"},{"t":"TCP 为什么需要重传？","tag":"","p":"","html":""},{"t":"什么是快速重传？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"快速重传和超时重传有什么区别？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"什么是 SACK？","tag":"","p":"","html":""},{"t":"TCP 如何检测数据包丢失？","tag":"","p":"","html":""},{"t":"TCP 如何处理乱序数据？","tag":"","p":"","html":""},{"t":"TCP 滑动窗口是什么？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"为什么需要滑动窗口？","tag":"","p":"","html":""},{"t":"TCP 接收窗口是什么？","tag":"","p":"","html":""},{"t":"TCP 发送窗口是什么？","tag":"","p":"","html":""},{"t":"什么是流量控制？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"TCP 如何实现流量控制？","tag":"","p":"","html":""},{"t":"零窗口是什么？","tag":"","p":"","html":""},{"t":"什么是 Zero Window Probe？","tag":"","p":"","html":""}]},{"no":"六","title":"TCP 拥塞控制","questions":[{"t":"什么是拥塞控制？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"流量控制和拥塞控制有什么区别？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"TCP 拥塞窗口 cwnd 是什么？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"什么是慢启动？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"为什么 TCP 需要慢启动？","tag":"","p":"","html":""},{"t":"什么是拥塞避免？","tag":"","p":"","html":""},{"t":"什么是快速重传？","tag":"","p":"","html":""},{"t":"什么是快速恢复？","tag":"","p":"","html":""},{"t":"TCP Reno 是什么？","tag":"","p":"","html":""},{"t":"TCP CUBIC 是什么？","tag":"","p":"","html":""},{"t":"BBR 是什么？","tag":"","p":"","html":""},{"t":"TCP 拥塞控制的完整过程是什么？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote><p></p><hr/>"}]},{"no":"七","title":"UDP","questions":[{"t":"UDP 是什么？","tag":"","p":"","html":""},{"t":"UDP 为什么不可靠？","tag":"","p":"","html":""},{"t":"UDP 和 TCP 最大的区别是什么？","tag":"","p":"","html":""},{"t":"UDP 为什么没有三次握手？","tag":"","p":"","html":""},{"t":"UDP 为什么速度快？","tag":"","p":"","html":""},{"t":"UDP 适合什么场景？","tag":"","p":"","html":""},{"t":"DNS 为什么使用 UDP？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"UDP 能不能实现可靠传输？","tag":"","p":"","html":""},{"t":"QUIC 为什么基于 UDP？","tag":"","p":"","html":""}]},{"no":"八","title":"HTTP","questions":[{"t":"HTTP 是什么？","tag":"必须掌握","p":"core","html":""},{"t":"HTTP 请求和响应是什么？","tag":"","p":"","html":""},{"t":"HTTP 请求由哪些部分组成？","tag":"","p":"","html":""},{"t":"HTTP 响应由哪些部分组成？","tag":"","p":"","html":""},{"t":"HTTP 常见状态码有哪些？","tag":"","p":"","html":""},{"t":"200、301、302、304、400、401、403、404、500、502、503 分别是什么？","tag":"面试必考","p":"must","html":"<ol><li>这些 HTTP 状态码分别是什么？</li></ol><p>HTTP 状态码核心是：服务器用三位数字告诉客户端这次请求的处理结果。</p><ol><li>2xx：成功</li></ol><p>200 OK</p><p>表示请求成功。</p><p>比如：</p><pre><code>GET /user/1</code></pre><p>服务器正常返回用户数据。</p><p>这是最常见的成功状态码。</p><ol><li>3xx：重定向或缓存</li></ol><p>301 Moved Permanently</p><p>表示永久重定向。</p><p>资源已经永久换了地址。</p><p>浏览器和搜索引擎可能会记住新地址。</p><p>常见场景：</p><pre><code>http://xxx.com → https://xxx.com</code></pre><p>302 Found</p><p>表示临时重定向。</p><p>资源临时换了地址，以后还可能变回来。</p><p>常见场景：</p><pre><code>未登录用户访问页面 → 临时跳转登录页</code></pre><p>304 Not Modified</p><p>表示资源未修改，可以使用缓存。</p><p>浏览器发送条件请求，比如带：</p><pre><code>If-None-Match<br/>If-Modified-Since</code></pre><p>服务器发现资源没变，就返回 304，不返回响应体。</p><ol><li>4xx：客户端错误</li></ol><p>400 Bad Request</p><p>表示请求有问题，服务器无法理解或处理。</p><p>常见原因：</p><ul><li>参数格式错误</li><li>JSON 格式错误</li><li>必填参数缺失</li><li>请求语法错误</li></ul><p>401 Unauthorized</p><p>表示未认证。</p><p>通常是：</p><pre><code>没有登录<br/>token 缺失<br/>token 无效</code></pre><p>重点是：</p><pre><code>你是谁还没确认</code></pre><p>403 Forbidden</p><p>表示已认证，但没有权限。</p><p>比如用户已经登录，但访问管理员接口。</p><p>重点是：</p><pre><code>知道你是谁，但你没权限</code></pre><p>404 Not Found</p><p>表示资源不存在。</p><p>常见原因：</p><ul><li>URL 写错</li><li>路由不存在</li><li>资源已删除</li><li>查询对象不存在</li></ul><ol><li>5xx：服务器错误</li></ol><p>500 Internal Server Error</p><p>表示服务器内部错误。</p><p>通常是后端程序异常。</p><p>比如：</p><ul><li>空指针</li><li>数据库异常</li><li>代码 bug</li><li>未捕获异常</li></ul><p>502 Bad Gateway</p><p>表示网关从上游服务器收到无效响应。</p><p>常见于：</p><pre><code>浏览器 → Nginx / Gateway → 后端服务</code></pre><p>如果后端挂了、返回异常、连接失败，网关可能返回 502。</p><p>503 Service Unavailable</p><p>表示服务暂时不可用。</p><p>常见原因：</p><ul><li>服务过载</li><li>正在维护</li><li>限流</li><li>熔断</li><li>后端实例不可用</li><li>资源耗尽</li></ul><p>一般是临时状态。</p><ol><li>401 和 403 区别</li></ol><p>这个面试很爱问。</p><p>401：</p><pre><code>没认证，先登录</code></pre><p>403：</p><pre><code>已认证，但没权限</code></pre><p>比如：</p><pre><code>没带 token → 401<br/>普通用户访问管理员接口 → 403</code></pre><ol><li>301 和 302 区别</li></ol><p>301：</p><pre><code>永久重定向</code></pre><p>浏览器和搜索引擎可以缓存新地址。</p><p>302：</p><pre><code>临时重定向</code></pre><p>只是这次临时跳转，不代表资源永久迁移。</p><ol><li>502 和 503 区别</li></ol><p>502：</p><pre><code>网关访问上游服务时，上游响应异常</code></pre><p>比如 Nginx 连不上后端，或后端返回非法响应。</p><p>503：</p><pre><code>服务当前不可用</code></pre><p>比如过载、维护、限流、熔断。</p><ol><li><strong>面试背诵版：200、301、302、304、400、401、403、404、500、502、503 分别是什么？</strong></li></ol><p></p><blockquote><p>200 表示请求成功。301 表示永久重定向，资源永久迁移到新地址，浏览器或搜索引擎可能缓存新地址；302 表示临时重定向，资源只是临时跳转；304 表示资源未修改，客户端可以直接使用缓存，通常配合 If-None-Match 或 If-Modified-Since 使用。</p><p>400 表示客户端请求错误，比如参数格式错误、JSON 解析失败、必填参数缺失；401 表示未认证，比如没有登录、token 缺失或无效；403 表示已认证但没有权限；404 表示请求的资源不存在，比如 URL 错误、路由不存在或资源已删除。</p><p>500 表示服务器内部错误，通常是后端程序异常；502 表示网关从上游服务收到无效响应，比如 Nginx 或网关连不上后端、后端异常返回；503 表示服务暂时不可用，比如服务过载、维护、限流或熔断。简单记：401 是“你还没登录”，403 是“你登录了但没权限”；502 是“网关找上游出问题”，503 是“服务暂时不可用”。</p></blockquote>"},{"t":"HTTP GET 和 POST 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>GET 和 POST 的核心区别是：<strong>GET 通常用于获取资源，POST 通常用于提交数据或创建资源。</strong></p><ol><li>语义不同</li></ol><p>GET 的语义是：</p><pre><code>获取资源</code></pre><p>比如：</p><pre><code>GET /users/1</code></pre><p>表示查询用户 1 的信息。</p><p>POST 的语义是：</p><pre><code>提交数据，让服务器处理</code></pre><p>比如：</p><pre><code>POST /users</code></pre><p>表示创建一个用户。</p><ol><li>参数位置不同</li></ol><p>GET 参数通常放在 URL query string 里。</p><p>比如：</p><pre><code>GET /search?keyword=java&amp;page=1</code></pre><p>POST 参数通常放在请求体 body 里。</p><p>比如：</p><pre><code>{<br/>  \"username\": \"tom\",<br/>  \"password\": \"123456\"<br/>}</code></pre><p>但严格说，HTTP 规范并不是说 GET 绝对不能有 body，只是实际中很少这么用，兼容性也不好。</p><ol><li>安全性不同</li></ol><p>这里的“安全”不是指加密安全。</p><p>HTTP 语义里的 safe method 指的是：</p><pre><code>请求不应该改变服务器资源状态</code></pre><p>GET 是安全方法。</p><p>因为它应该只查询，不修改数据。</p><p>POST 不是安全方法。</p><p>因为它可能创建、修改或触发服务端操作。</p><ol><li>幂等性不同</li></ol><p>GET 是幂等的。</p><p>意思是同一个 GET 请求执行多次，结果应该一样，不产生额外副作用。</p><p>比如多次查询用户信息，不应该改变用户数据。</p><p>POST 通常不是幂等的。</p><p>比如重复提交订单：</p><pre><code>可能创建多个订单</code></pre><p>所以 POST 需要防重复提交。</p><ol><li>缓存不同</li></ol><p>GET 更容易被缓存。</p><p>浏览器、代理、CDN 都可以缓存 GET 响应。</p><p>POST 默认通常不缓存。</p><p>除非服务器明确设置缓存头。</p><p>所以静态资源、搜索结果、查询接口更常用 GET。</p><ol><li>URL 长度限制</li></ol><p>GET 参数在 URL 里。</p><p>URL 长度会受到浏览器、服务器或代理限制。</p><p>POST 数据在 body 里，通常更适合提交较大的数据。</p><p>比如表单、JSON、文件上传。</p><ol><li>可见性不同</li></ol><p>GET 参数显示在 URL 中。</p><p>可能出现在：</p><ul><li>浏览器地址栏</li><li>历史记录</li><li>服务器日志</li><li>代理日志</li></ul><p>所以敏感信息不应该放在 GET 参数里。</p><p>POST 参数在 body 中，不会显示在地址栏。</p><p>但注意：</p><pre><code>POST 本身不等于安全</code></pre><p>如果没有 HTTPS，body 也可能被抓包看到。</p><ol><li>使用场景</li></ol><p>GET 适合：</p><ul><li>查询数据</li><li>获取列表</li><li>搜索</li><li>下载资源</li><li>访问页面</li></ul><p>POST 适合：</p><ul><li>创建资源</li><li>提交表单</li><li>登录</li><li>上传文件</li><li>触发服务端处理</li><li>提交复杂 JSON</li></ul><ol><li><strong>面试背诵版：HTTP GET 和 POST 有什么区别？</strong></li></ol><blockquote><p>GET 和 POST 的主要区别在语义、参数位置、安全性、幂等性和缓存。GET 的语义是获取资源，通常用于查询数据，参数一般放在 URL query string 中；POST 的语义是提交数据给服务器处理，通常用于创建资源、提交表单、上传文件或触发服务端操作，参数一般放在请求体 body 中。</p><p>从 HTTP 语义上看，GET 是 safe 和 idempotent 的，也就是不应该修改服务器资源，并且多次请求结果应该一致；POST 通常不是 safe，也不一定幂等，比如重复提交订单可能创建多个订单。GET 更容易被浏览器、代理或 CDN 缓存，而 POST 默认通常不缓存。</p><p>另外，GET 参数会暴露在 URL、浏览器历史和日志中，也受到 URL 长度限制；POST 可以提交更大的 body，但 POST 本身不代表安全，敏感数据仍然必须使用 HTTPS。简单说，GET 用来“查”，POST 用来“提交或创建”。</p></blockquote>"},{"t":"GET 为什么通常用于查询？","tag":"","p":"","html":""},{"t":"POST 为什么通常用于提交数据？","tag":"","p":"","html":""},{"t":"PUT 和 PATCH 有什么区别？","tag":"","p":"","html":""},{"t":"HTTP Header 有什么作用？","tag":"","p":"","html":""},{"t":"Content-Type 是什么？","tag":"","p":"","html":""},{"t":"Content-Length 是什么？","tag":"","p":"","html":""},{"t":"HTTP Cookie 是什么？","tag":"","p":"","html":""},{"t":"Session 和 Cookie 有什么区别？","tag":"","p":"","html":""},{"t":"HTTP 为什么是无状态的？","tag":"","p":"","html":""},{"t":"什么是 HTTP Keep-Alive？","tag":"面试必考","p":"must","html":"<ol><li>什么是 HTTP Keep-Alive？</li></ol><p>HTTP Keep-Alive 的核心是：复用同一个 TCP 连接发送多个 HTTP 请求，避免每次请求都重新建立和关闭连接。</p><ol><li>没有 Keep-Alive 会怎样？</li></ol><p>早期 HTTP/1.0 默认是短连接。</p><p>每请求一个资源，都要：</p><pre><code>建立 TCP 连接<br/>发送 HTTP 请求<br/>接收 HTTP 响应<br/>关闭 TCP 连接</code></pre><p>如果一个页面有很多资源：</p><ul><li>HTML</li><li>CSS</li><li>JS</li><li>图片</li><li>字体</li></ul><p>就要频繁建立和关闭 TCP 连接。</p><p>开销很大。</p><ol><li>Keep-Alive 是什么？</li></ol><p>Keep-Alive 也叫持久连接。</p><p>它允许一个 TCP 连接处理多个 HTTP 请求。</p><p>流程变成：</p><pre><code>建立 TCP 连接<br/>请求 HTML<br/>响应 HTML<br/>请求 CSS<br/>响应 CSS<br/>请求 JS<br/>响应 JS<br/>...<br/>最后再关闭连接</code></pre><p>这样不用每次都三次握手和四次挥手。</p><ol><li>它解决什么问题？</li></ol><p>Keep-Alive 主要减少：</p><ul><li>TCP 三次握手开销</li><li>TCP 四次挥手开销</li><li>TLS 握手开销</li><li>连接建立延迟</li><li>服务器连接创建成本</li><li>网络资源浪费</li></ul><p>如果是 HTTPS，复用连接收益更大，因为 TLS 握手也很耗时。</p><ol><li>HTTP/1.0 和 HTTP/1.1 区别</li></ol><p>HTTP/1.0 默认短连接。</p><p>如果要开启 Keep-Alive，需要请求头：</p><pre><code>Connection: keep-alive</code></pre><p>HTTP/1.1 默认开启持久连接。</p><p>除非显式声明：</p><pre><code>Connection: close</code></pre><ol><li>Keep-Alive 和 TCP Keepalive 区别</li></ol><p>这两个容易混。</p><p>HTTP Keep-Alive：</p><pre><code>应用层机制，复用 TCP 连接发送多个 HTTP 请求</code></pre><p>TCP Keepalive：</p><pre><code>传输层机制，检测 TCP 连接是否还活着</code></pre><p>一个是为了连接复用。</p><p>一个是为了连接保活检测。</p><ol><li>Keep-Alive 有什么缺点？</li></ol><p>Keep-Alive 也不是越久越好。</p><p>如果连接长期不关闭，会占用服务器资源：</p><ul><li>socket</li><li>文件描述符</li><li>内存</li><li>连接池资源</li></ul><p>所以服务器一般会设置：</p><ul><li>keepalive timeout</li><li>最大请求数</li><li>最大空闲连接数</li></ul><p>超过限制就关闭连接。</p><ol><li>和 HTTP/2 的关系</li></ol><p>HTTP/1.1 Keep-Alive 可以复用 TCP 连接。</p><p>但同一个连接上的请求通常还是有队头阻塞问题。</p><p>HTTP/2 在一个 TCP 连接上支持多路复用。</p><p>可以同时并发多个请求和响应。</p><p>所以 HTTP/2 比 HTTP/1.1 Keep-Alive 更进一步。</p><ol><li><strong>面试背诵版：什么是 HTTP Keep-Alive？</strong></li></ol><blockquote><p>HTTP Keep-Alive，也叫持久连接，指的是在一次 TCP 连接建立后，不立即关闭连接，而是在同一个 TCP 连接上连续发送多个 HTTP 请求和响应。这样可以避免每次请求都重新进行 TCP 三次握手和四次挥手，如果是 HTTPS，还可以减少 TLS 握手开销，从而降低延迟、提高性能。</p><p>在 HTTP/1.0 中，默认是短连接，需要通过 <code>Connection: keep-alive</code> 开启持久连接；在 HTTP/1.1 中，默认使用持久连接，除非显式设置 <code>Connection: close</code>。Keep-Alive 对一个包含 HTML、CSS、JS、图片等多个资源的页面非常有用，因为多个资源可以复用同一个连接。</p><p>需要注意，HTTP Keep-Alive 和 TCP Keepalive 不一样。HTTP Keep-Alive 是应用层的连接复用机制；TCP Keepalive 是传输层用来检测连接是否存活的机制。Keep-Alive 也不是越久越好，连接长期空闲会占用服务器 socket、文件描述符和内存，所以服务端通常会设置 keepalive timeout 和最大请求数。</p></blockquote>"},{"t":"HTTP 长连接是什么？","tag":"","p":"","html":""},{"t":"HTTP 短连接是什么？","tag":"","p":"","html":""},{"t":"HTTP 1.0 和 HTTP 1.1 有什么区别？","tag":"","p":"","html":""},{"t":"HTTP 1.1 为什么默认使用长连接？","tag":"","p":"","html":""}]},{"no":"九","title":"HTTP 缓存","questions":[{"t":"HTTP 缓存是什么？","tag":"","p":"","html":""},{"t":"强缓存和协商缓存有什么区别？","tag":"面试必考","p":"must","html":"<p>强缓存和协商缓存有什么区别？</p><p>强缓存和协商缓存的核心区别是：强缓存命中时不发请求，直接用本地缓存；协商缓存会发请求问服务器资源有没有变。</p><ol><li>强缓存是什么？</li></ol><p>强缓存是浏览器直接判断缓存是否过期。</p><p>如果没过期，就直接使用本地缓存。</p><p>不会向服务器发送请求。</p><p>常见响应头：</p><pre><code>Cache-Control: max-age=3600<br/>Expires: Wed, 21 Oct 2026 07:28:00 GMT</code></pre><p>其中更常用的是：</p><pre><code>Cache-Control</code></pre><ol><li>强缓存流程</li></ol><p>流程是：</p><pre><code>浏览器请求资源<br/>  ↓<br/>检查本地缓存是否还在有效期<br/>  ↓<br/>如果没过期<br/>  ↓<br/>直接使用缓存，不请求服务器</code></pre><p>比如：</p><pre><code>from memory cache<br/>from disk cache</code></pre><p>这就是强缓存命中。</p><ol><li>协商缓存是什么？</li></ol><p>协商缓存是浏览器缓存过期后，向服务器确认资源有没有变化。</p><p>如果资源没变，服务器返回：</p><pre><code>304 Not Modified</code></pre><p>浏览器继续使用本地缓存。</p><p>如果资源变了，服务器返回：</p><pre><code>200 OK + 新资源</code></pre><ol><li>协商缓存常见响应头</li></ol><p>协商缓存主要有两组头。</p><p>第一组：</p><pre><code>Last-Modified<br/>If-Modified-Since</code></pre><p>第二组：</p><pre><code>ETag<br/>If-None-Match</code></pre><p>优先级通常是：</p><pre><code>ETag &gt; Last-Modified</code></pre><p>因为 ETag 更精确。</p><ol><li>协商缓存流程</li></ol><p>流程是：</p><pre><code>浏览器发现强缓存过期<br/>  ↓<br/>带 If-None-Match / If-Modified-Since 请求服务器<br/>  ↓<br/>服务器判断资源是否变化<br/>  ↓<br/>没变：304，浏览器用缓存<br/>  ↓<br/>变了：200，返回新资源</code></pre><p>所以协商缓存仍然会发生网络请求。</p><ol><li>强缓存和协商缓存区别</li></ol><p>可以这样记：</p><pre><code>强缓存：不问服务器，直接用<br/>协商缓存：问服务器，没变再用</code></pre><p>对比：</p><sheet></sheet><ol><li>实际使用策略</li></ol><p>静态资源常用强缓存。</p><p>比如：</p><pre><code>app.a1b2c3.js<br/>style.9x8y7z.css</code></pre><p>文件名带 hash，可以设置很长缓存：</p><pre><code>Cache-Control: max-age=31536000</code></pre><p>HTML 通常不设置太长强缓存。</p><p>因为入口文件要及时更新。</p><p>可以用协商缓存或较短缓存。</p><ol><li><strong>面试背诵版：强缓存和协商缓存有什么区别？</strong></li></ol><blockquote><p>强缓存和协商缓存都是 HTTP 缓存机制，区别在于是否需要向服务器发请求。强缓存命中时，浏览器根据 <code>Cache-Control</code> 或 <code>Expires</code> 判断资源是否还在有效期内，如果没有过期，就直接使用本地缓存，不会发送网络请求，所以速度最快。<code>Cache-Control</code> 是 HTTP/1.1 的缓存控制头，优先级高于 <code>Expires</code>。</p><p>协商缓存是在强缓存失效后，浏览器向服务器发送条件请求，询问资源是否发生变化。常见头有 <code>ETag / If-None-Match</code> 和 <code>Last-Modified / If-Modified-Since</code>。如果服务器判断资源没变，就返回 <code>304 Not Modified</code>，浏览器继续使用本地缓存；如果资源变了，就返回 <code>200 OK</code> 和新的资源内容。通常 <code>ETag</code> 比 <code>Last-Modified</code> 更精确，优先级也更高。</p><p>简单说，强缓存是“不问服务器，没过期直接用”；协商缓存是“问服务器，没变就用缓存，变了就重新下载”。实际中，带 hash 的静态资源适合长强缓存，HTML 入口文件通常用协商缓存或短缓存，保证更新及时。</p></blockquote>"},{"t":"Cache-Control 是什么？","tag":"","p":"","html":""},{"t":"Expires 是什么？","tag":"","p":"","html":""},{"t":"ETag 是什么？","tag":"","p":"","html":""},{"t":"Last-Modified 是什么？","tag":"","p":"","html":""},{"t":"ETag 和 Last-Modified 有什么区别？","tag":"","p":"","html":""},{"t":"304 是怎么产生的？","tag":"面试必考","p":"must","html":"<p>304 是怎么产生的？</p><p>304 的核心是：浏览器缓存过期后，向服务器发起条件请求，服务器发现资源没有变化，就返回 304 Not Modified，让浏览器继续用本地缓存。</p><ol><li>304 是什么？</li></ol><p>304 全称是：</p><pre><code>304 Not Modified</code></pre><p>意思是：</p><pre><code>资源没有修改</code></pre><p>它不是错误状态码，而是缓存相关状态码。</p><p>表示客户端本地缓存还能用。</p><ol><li>什么时候会出现 304？</li></ol><p>通常发生在协商缓存阶段。</p><p>也就是：</p><pre><code>强缓存过期<br/>  ↓<br/>浏览器不能直接用缓存<br/>  ↓<br/>向服务器确认资源是否变化<br/>  ↓<br/>服务器发现没变<br/>  ↓<br/>返回 304</code></pre><p>浏览器收到 304 后，不下载资源 body，直接使用本地缓存。</p><ol><li>通过 Last-Modified 产生 304</li></ol><p>服务器第一次返回资源时，带上：</p><pre><code>Last-Modified: Wed, 21 Oct 2026 07:28:00 GMT</code></pre><p>浏览器下次请求时带上：</p><pre><code>If-Modified-Since: Wed, 21 Oct 2026 07:28:00 GMT</code></pre><p>服务器比较资源最后修改时间。</p><p>如果资源没有更新，就返回：</p><pre><code>304 Not Modified</code></pre><p>如果资源更新了，就返回：</p><pre><code>200 OK + 新资源</code></pre><ol><li>通过 ETag 产生 304</li></ol><p>服务器第一次返回资源时，带上：</p><pre><code>ETag: \"abc123\"</code></pre><p>浏览器下次请求时带上：</p><pre><code>If-None-Match: \"abc123\"</code></pre><p>服务器比较当前资源的 ETag。</p><p>如果 ETag 一样，说明资源没变，返回：</p><pre><code>304 Not Modified</code></pre><p>如果 ETag 不一样，返回：</p><pre><code>200 OK + 新资源</code></pre><ol><li>ETag 和 Last-Modified 谁优先？</li></ol><p>通常：</p><pre><code>ETag 优先级高于 Last-Modified</code></pre><p>因为 ETag 更精确。</p><p>Last-Modified 只能精确到时间，可能存在问题：</p><ul><li>秒级以内修改感知不到</li><li>文件内容没变但时间变了</li><li>分布式服务器时间不一致</li></ul><p>ETag 可以基于内容 hash 或版本号，更准确判断资源是否变化。</p><ol><li>304 返回什么？</li></ol><p>304 响应通常没有响应体。</p><p>因为它的意思是：</p><pre><code>你本地缓存的资源还能用</code></pre><p>所以服务器只返回响应头，浏览器直接读取本地缓存。</p><p>这样可以节省带宽。</p><ol><li>304 和 200 from cache 区别</li></ol><p>304：</p><pre><code>发了请求到服务器，服务器说没变</code></pre><p>200 from cache：</p><pre><code>强缓存命中，根本没发请求</code></pre><p>所以：</p><pre><code>强缓存更快<br/>协商缓存会产生 304</code></pre><ol><li><strong>面试背诵版：304 是怎么产生的？</strong></li></ol><blockquote><p>304 是 HTTP 协商缓存产生的状态码，全称是 304 Not Modified，表示资源没有发生变化，客户端可以继续使用本地缓存。它通常发生在强缓存过期后，浏览器向服务器发起条件请求，询问资源是否更新。</p><p>条件请求主要有两种方式。第一种是 <code>Last-Modified / If-Modified-Since</code>：服务器第一次返回资源时带上 <code>Last-Modified</code>，浏览器下次请求时带上 <code>If-Modified-Since</code>，服务器比较资源最后修改时间，如果没变就返回 304。第二种是 <code>ETag / If-None-Match</code>：服务器返回资源时带上 <code>ETag</code>，浏览器下次请求时带上 <code>If-None-Match</code>，服务器比较 ETag，如果一致就返回 304。</p><p>通常 ETag 优先级高于 Last-Modified，因为它更精确，可以避免秒级时间误差和分布式服务器时间不一致问题。304 响应一般不返回资源 body，浏览器收到后直接使用本地缓存。简单说，304 是“浏览器问服务器资源变没变，服务器回答没变，你用缓存吧”。</p></blockquote>"},{"t":"浏览器访问一个 URL 时，HTTP 缓存是怎么工作的？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote><p></p><hr/>"}]},{"no":"十","title":"HTTP/2","questions":[{"t":"HTTP/2 解决了 HTTP/1.1 的什么问题？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"HTTP/2 有哪些核心特性？","tag":"","p":"","html":""},{"t":"什么是二进制分帧？","tag":"","p":"","html":""},{"t":"什么是 Stream？","tag":"","p":"","html":""},{"t":"HTTP/2 为什么可以多路复用？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>HTTP/2 可以多路复用，核心原因是：<strong>它把 HTTP 消息拆成二进制帧，并用 Stream ID 标识不同请求，让多个请求和响应可以在同一个 TCP 连接上交错传输。</strong></p><ol><li>HTTP/1.1 的问题</li></ol><p>HTTP/1.1 虽然有 Keep-Alive，可以复用 TCP 连接。</p><p>但同一个连接上，请求和响应通常还是按顺序处理。</p><p>如果前一个响应很慢，后面的响应就会被阻塞。</p><p>这叫应用层队头阻塞。</p><p>所以浏览器通常会开多个 TCP 连接来并发请求。</p><ol><li>HTTP/2 的核心变化</li></ol><p>HTTP/2 引入了二进制分帧层。</p><p>它不再直接传输纯文本 HTTP 报文。</p><p>而是把请求和响应拆成更小的 frame。</p><p>比如：</p><ul><li>HEADERS frame</li><li>DATA frame</li><li>SETTINGS frame</li><li>WINDOW_UPDATE frame</li></ul><p>每个 frame 都带有 Stream ID。</p><ol><li>Stream 是什么？</li></ol><p>Stream 可以理解成一个逻辑请求通道。</p><p>一个 HTTP 请求和响应对应一个 stream。</p><p>比如：</p><pre><code>Stream 1：请求 HTML<br/>Stream 3：请求 CSS<br/>Stream 5：请求 JS</code></pre><p>这些 stream 都可以复用同一个 TCP 连接。</p><ol><li>为什么能多路复用？</li></ol><p>因为不同 stream 的 frame 可以交错发送。</p><p>比如同一个 TCP 连接里可以这样传：</p><pre><code>Stream 1 的一部分数据<br/>Stream 3 的一部分数据<br/>Stream 5 的一部分数据<br/>Stream 1 的下一部分数据<br/>Stream 3 的下一部分数据</code></pre><p>接收方根据 Stream ID 把 frame 重新组装回对应的请求或响应。</p><p>所以不需要一个请求完全结束后才能传下一个。</p><ol><li>和 HTTP/1.1 的区别</li></ol><p>HTTP/1.1：</p><pre><code>一个连接上响应基本按顺序返回<br/>前面慢，后面容易等</code></pre><p>HTTP/2：</p><pre><code>一个连接上多个 stream 交错传输<br/>谁有数据谁先发</code></pre><p>这样减少了应用层队头阻塞。</p><ol><li>多路复用的好处</li></ol><p>好处包括：</p><ul><li>减少 TCP 连接数量</li><li>降低握手开销</li><li>提高连接利用率</li><li>多个请求并发传输</li><li>减少 HTTP/1.1 队头阻塞</li><li>更适合加载页面多个资源</li></ul><ol><li>但 TCP 层仍有队头阻塞</li></ol><p>重点要补一句。</p><p>HTTP/2 多路复用解决的是 HTTP 应用层队头阻塞。</p><p>但它仍然跑在 TCP 上。</p><p>如果 TCP 丢了一个包，后续数据即使属于其他 stream，也要等丢失包重传。</p><p>这叫 TCP 层队头阻塞。</p><p>HTTP/3 基于 QUIC，就是为了解决这个问题。</p><ol><li><strong>面试背诵版：HTTP/2 为什么可以多路复用？</strong></li></ol><blockquote><ul><li>HTTP/2 可以多路复用，是因为它引入了二进制分帧层，把 HTTP 请求和响应拆成多个 frame，并通过 Stream ID 标识每个 frame 属于哪个请求。一个请求和响应对应一个 stream，多个 stream 可以共享同一个 TCP 连接。</li><li>在同一个连接中，不同 stream 的 frame 可以交错传输，比如先发送 Stream 1 的一部分数据，再发送 Stream 3 的一部分数据，接收方根据 Stream ID 把这些 frame 重新组装成完整的请求或响应。因此 HTTP/2 不需要等一个请求响应完全结束后再处理下一个请求，从而实现多路复用。</li><li>它解决了 HTTP/1.1 在应用层的队头阻塞问题，减少了 TCP 连接数量，提高了连接利用率。不过 HTTP/2 仍然基于 TCP，如果底层 TCP 丢包，所有 stream 都可能受影响，所以仍然存在 TCP 层队头阻塞；HTTP/3 使用 QUIC/UDP，就是为了进一步解决这个问题。</li></ul></blockquote>"},{"t":"HTTP/1.1 为什么存在队头阻塞？","tag":"面试必考","p":"must","html":"<p>HTTP/1.1 存在队头阻塞的核心是：同一个 TCP 连接上的请求和响应通常要按顺序处理，前面的请求/响应慢了，后面的就算已经准备好，也可能被卡住。</p><ol><li>什么是队头阻塞？</li></ol><p>队头阻塞就是：排在队伍前面的任务卡住了，后面的任务也被迫等待。</p><pre><code>请求队列：<br/>请求1 → 请求2 → 请求3<br/><br/>如果请求1处理很慢：<br/>请求1 卡住<br/>请求2 等着<br/>请求3 等着</code></pre><p>这就叫 Head-of-Line Blocking，队头阻塞。</p><ol><li>HTTP/1.1 为什么会这样？</li></ol><p>HTTP/1.1 虽然支持 Keep-Alive，可以复用一个 TCP 连接发送多个请求。</p><p>但是在一个连接里，响应通常要按照请求顺序返回。</p><pre><code>客户端发送：<br/>请求1 → 请求2 → 请求3<br/><br/>服务端返回：<br/>响应1 → 响应2 → 响应3</code></pre><p>如果响应1很慢，响应2、响应3 即使服务端已经处理好了，也不能随便插到前面返回。</p><ol><li>举个例子</li></ol><p>比如浏览器请求三个资源：</p><pre><code>请求1：big.js     需要 2 秒<br/>请求2：logo.png   需要 10 毫秒<br/>请求3：style.css  需要 20 毫秒</code></pre><p>如果它们在同一个 HTTP/1.1 连接里按顺序处理：</p><pre><code>big.js 没返回完<br/>logo.png 等着<br/>style.css 等着</code></pre><p>这就是 HTTP/1.1 的队头阻塞。</p><ol><li>HTTP/1.1 管道化为什么没解决？</li></ol><p>HTTP/1.1 有一个叫 Pipelining 的机制，可以连续发送多个请求，不用等上一个响应回来再发下一个请求。</p><p>但是它要求响应必须按请求顺序返回。</p><pre><code>发送：<br/>请求1 → 请求2 → 请求3<br/><br/>返回：<br/>必须 响应1 → 响应2 → 响应3</code></pre><p>所以只要响应1慢，后面的响应还是会被堵住。</p><ol><li>浏览器怎么缓解？</li></ol><p>浏览器通常不会只用一个 TCP 连接，而是对同一个域名建立多个并发连接。</p><pre><code>连接1：请求1<br/>连接2：请求2<br/>连接3：请求3</code></pre><p>这样可以减轻队头阻塞。</p><p>但问题是：连接多了会带来更多 TCP 握手、TLS 握手、慢启动和服务器资源消耗。</p><ol><li>HTTP/2 怎么解决？</li></ol><p>HTTP/2 把请求和响应拆成 Frame，每个请求对应一个 Stream。</p><p>不同 Stream 的 Frame 可以在同一个 TCP 连接里交错传输。</p><pre><code>HTTP/2：<br/>Stream1-Frame1<br/>Stream2-Frame1<br/>Stream3-Frame1<br/>Stream2-Frame2<br/>Stream1-Frame2</code></pre><p>所以 HTTP/2 解决的是 HTTP 应用层的队头阻塞。</p><ol><li>注意一个面试坑</li></ol><p>HTTP/2 解决了 HTTP/1.1 的应用层队头阻塞，但没有完全解决 TCP 层队头阻塞。</p><p>因为 HTTP/2 底层仍然是 TCP，TCP 要保证有序交付，一个包丢了，后面的数据也要等重传。</p><ol><li><strong>面试背诵版：HTTP/1.1 为什么存在队头阻塞？</strong></li></ol><blockquote><p>HTTP/1.1 存在队头阻塞，主要是因为一个 TCP 连接上的请求和响应通常要按顺序处理。虽然 HTTP/1.1 支持 Keep-Alive，可以复用连接，也支持 Pipelining 连续发送多个请求，但响应仍然必须按照请求顺序返回。如果前面的请求处理很慢，后面的请求即使已经处理完成，也不能提前返回，就会被阻塞。浏览器通常通过对同一域名建立多个 TCP 连接来缓解这个问题，但会增加握手、慢启动和服务器资源开销。HTTP/2 通过二进制分帧和多路复用解决了 HTTP 层的队头阻塞，但底层 TCP 仍可能存在队头阻塞。</p></blockquote>"},{"t":"HTTP/2 如何解决 HTTP 层面的队头阻塞？","tag":"","p":"","html":"<p>HTTP/2 解决 HTTP 层队头阻塞的核心是：把“按顺序传一整个响应”改成“把多个请求响应拆成小帧，在同一个 TCP 连接里交错传输”。</p><ol><li>HTTP/1.1 的问题</li></ol><p>HTTP/1.1 一个连接里，响应通常要按请求顺序返回。</p><pre><code>请求顺序：<br/>请求1 → 请求2 → 请求3<br/><br/>响应顺序：<br/>响应1 → 响应2 → 响应3</code></pre><p>如果响应1很慢，响应2、响应3 就算已经准备好了，也可能被卡住。</p><p>这就是 HTTP 层面的队头阻塞。</p><ol><li>HTTP/2 的核心改造</li></ol><p>HTTP/2 不再用纯文本方式传输完整请求/响应，而是使用二进制分帧。</p><p>它会把一个请求或响应拆成多个 Frame。</p><pre><code>响应1 = Frame1 + Frame2 + Frame3<br/>响应2 = Frame1 + Frame2<br/>响应3 = Frame1 + Frame2</code></pre><p>这些 Frame 可以在同一个 TCP 连接中交错发送。</p><ol><li>Stream 是什么？</li></ol><p>HTTP/2 中，每个请求/响应对应一个 Stream。</p><p>每个 Stream 都有自己的 Stream ID，用来区分这些 Frame 属于哪个请求。</p><pre><code>Stream 1：请求 A / 响应 A<br/>Stream 3：请求 B / 响应 B<br/>Stream 5：请求 C / 响应 C</code></pre><p>客户端收到 Frame 后，根据 Stream ID 再把它们组装回对应的响应。</p><ol><li>多路复用怎么工作？</li></ol><p>HTTP/2 多个 Stream 共享同一个 TCP 连接，但 Frame 可以交错传输。</p><pre><code>同一个 TCP 连接里：<br/><br/>S1-F1 → S3-F1 → S5-F1 → S3-F2 → S1-F2 → S5-F2</code></pre><p>这意味着某个请求的响应比较大，不会阻塞其他请求的小响应先返回。</p><ol><li>举个例子</li></ol><p>比如同时请求三个资源：</p><pre><code>请求1：big.js      很大<br/>请求2：logo.png    很小<br/>请求3：style.css   很小</code></pre><p>HTTP/1.1 可能是：</p><pre><code>big.js 返回完 → logo.png → style.css</code></pre><p>HTTP/2 可以是：</p><pre><code>big.js 一部分 → logo.png → style.css → big.js 剩余部分</code></pre><p>所以小资源不用一直等大资源返回完。</p><ol><li>它解决的是哪一层？</li></ol><p>HTTP/2 解决的是 HTTP 应用层面的队头阻塞。</p><p>也就是说，它解决了 HTTP/1.1 中“响应必须按顺序返回”的问题。</p><p>但它没有解决 TCP 层面的队头阻塞。</p><ol><li>为什么还有 TCP 队头阻塞？</li></ol><p>因为 HTTP/2 底层还是 TCP。</p><p>TCP 要保证字节流可靠、有序。如果某个 TCP 包丢失，后面的包即使已经到了，也不能先交给应用层。</p><p>所以一个 TCP 包丢失，可能导致这个连接上的多个 HTTP/2 Stream 都被阻塞。</p><ol><li><strong>面试背诵版：HTTP/2 如何解决 HTTP 层面的队头阻塞？</strong></li></ol><blockquote><p>HTTP/2 通过二进制分帧和多路复用解决 HTTP 层面的队头阻塞。它会把每个请求和响应拆成多个 Frame，并给每个请求分配一个 Stream ID。多个 Stream 共享同一个 TCP 连接，但不同 Stream 的 Frame 可以交错传输，客户端收到后再根据 Stream ID 重新组装。这样一个大响应不会阻塞其他小响应返回，解决了 HTTP/1.1 中响应必须按顺序返回导致的应用层队头阻塞。不过 HTTP/2 底层仍然基于 TCP，所以如果 TCP 包丢失，仍然可能出现 TCP 层面的队头阻塞。</p></blockquote>"},{"t":"HTTP/2 的 Header Compression 是什么？","tag":"","p":"","html":""},{"t":"HPACK 是什么？","tag":"","p":"","html":""},{"t":"HTTP/2 为什么还存在队头阻塞？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote><p></p><hr/>"}]},{"no":"十一","title":"HTTP/3 & QUIC","questions":[{"t":"HTTP/3 为什么出现？","tag":"","p":"","html":""},{"t":"HTTP/2 和 HTTP/3 有什么区别？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"QUIC 是什么？","tag":"","p":"","html":""},{"t":"QUIC 为什么基于 UDP？","tag":"","p":"","html":""},{"t":"QUIC 如何实现可靠传输？","tag":"","p":"","html":""},{"t":"QUIC 如何解决 TCP 队头阻塞？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"HTTP/3 为什么速度更快？","tag":"","p":"","html":""},{"t":"QUIC 的连接迁移是什么？","tag":"","p":"","html":""},{"t":"HTTP/3 和 HTTPS 是什么关系？","tag":"","p":"","html":""}]},{"no":"十二","title":"HTTPS / TLS","questions":[{"t":"HTTPS 是什么？","tag":"","p":"","html":""},{"t":"HTTP 和 HTTPS 有什么区别？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>HTTP 和 HTTPS 的核心区别是：<strong>HTTP 是明文传输，HTTPS 是在 HTTP 外面加了一层 TLS/SSL 加密，解决了安全传输问题。</strong></p><ol><li>HTTP 是什么？</li></ol><p>HTTP 是<strong>超文本传输协议</strong>，用来在客户端和服务端之间传输数据。</p><p>比如浏览器访问网页，本质上就是发送 HTTP 请求，服务端返回 HTTP 响应。</p><p>但 HTTP 默认是明文传输。</p><pre><code>客户端  —— 明文 HTTP ——&gt;  服务端</code></pre><p>如果中间有人抓包，就可能看到请求内容、Cookie、账号密码等信息。</p><ol><li>HTTPS 是什么？</li></ol><p>HTTPS 可以理解为：</p><pre><code>HTTPS = HTTP + TLS/SSL</code></pre><p>它不是一个全新的应用层协议，而是在 HTTP 和 TCP 之间加了一层安全层。</p><pre><code>HTTP<br/>TLS/SSL<br/>TCP<br/>IP</code></pre><p>TLS 主要负责加密、身份认证和数据完整性校验。</p><ol><li>第一个区别：是否加密</li></ol><p>HTTP 数据是明文传输，中间人可以直接看到内容。</p><p>HTTPS 会对传输内容进行加密，中间人即使抓到包，也很难看懂真实内容。</p><pre><code>HTTP：<br/>用户名=zhangsan&amp;password=123456<br/><br/>HTTPS：<br/>a8f3x9sK...  看不懂的密文</code></pre><ol><li>第二个区别：是否验证身份</li></ol><p>HTTP 无法确认服务端是不是真的目标网站。</p><p>HTTPS 通过数字证书验证服务端身份，防止访问到伪造网站。</p><p>比如你访问银行网站，HTTPS 会通过证书证明这个服务端确实是银行的服务器。</p><ol><li>第三个区别：是否保证完整性</li></ol><p>HTTP 数据在传输过程中可能被篡改，客户端不一定知道。</p><p>HTTPS 会通过消息认证码、摘要等机制保证数据完整性。</p><p>如果数据被中途修改，接收方可以检测出来。</p><ol><li>第四个区别：端口不同</li></ol><p>HTTP 默认使用 80 端口。</p><p>HTTPS 默认使用 443 端口。</p><pre><code>http://example.com   → 80<br/>https://example.com  → 443</code></pre><ol><li>第五个区别：性能开销不同</li></ol><p>HTTPS 需要 TLS 握手、证书验证、加解密，所以比 HTTP 多一些计算和握手成本。</p><p>但现在硬件、TLS 1.3、会话复用等优化已经让 HTTPS 的性能开销很小了。</p><p>实际生产环境基本都使用 HTTPS。</p><ol><li>面试常见补充</li></ol><p>HTTPS 主要解决三个问题：</p><pre><code>加密：防止内容被窃听<br/>认证：防止访问假服务器<br/>完整性：防止数据被篡改</code></pre><p>但是 HTTPS 不等于绝对安全。</p><p>如果服务端本身有漏洞、证书被错误签发、用户被钓鱼，仍然可能出问题。</p><ol><li><strong>面试背诵版：HTTP 和 HTTPS 有什么区别？</strong></li></ol><p></p><blockquote><p>HTTP 和 HTTPS 最大区别是安全性。HTTP 是明文传输，数据在网络中可能被窃听、篡改，也无法确认服务端身份。HTTPS 是在 HTTP 和 TCP 之间加入 TLS/SSL 安全层，通过加密保证内容不被窃听，通过数字证书验证服务端身份，通过完整性校验防止数据被篡改。另外 HTTP 默认端口是 80，HTTPS 默认端口是 443。HTTPS 相比 HTTP 会多 TLS 握手和加解密开销，但现在优化后成本已经比较小，生产环境一般都使用 HTTPS。</p></blockquote>"},{"t":"为什么需要 HTTPS？","tag":"","p":"","html":""},{"t":"HTTPS 为什么安全？","tag":"","p":"","html":""},{"t":"TLS 是什么？","tag":"","p":"","html":""},{"t":"SSL 和 TLS 有什么区别？","tag":"","p":"","html":""},{"t":"HTTPS 完整握手过程是什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>HTTPS 完整握手过程的核心是：客户端和服务端先通过<strong> TLS 握手完成身份认证、密钥协商，</strong>然后再用协商出来的对称密钥加密 HTTP 数据。</p><ol><li>先说整体流程</li></ol><p>HTTPS 不是一上来就直接传 HTTP 数据，而是先建立 TCP 连接，再进行 TLS 握手。</p><pre><code>1. TCP 三次握手<br/>2. TLS 握手<br/>3. 加密传输 HTTP 请求和响应</code></pre><p>所以完整链路是：</p><pre><code>客户端<br/>  ↓ TCP 三次握手<br/>服务端<br/>  ↓ TLS 握手<br/>双方得到会话密钥<br/>  ↓<br/>加密传输 HTTP 数据</code></pre><ol><li>第一步：ClientHello</li></ol><p>客户端先发一个 ClientHello，告诉服务端自己支持哪些 TLS 能力。</p><p>里面通常包括：</p><pre><code>支持的 TLS 版本<br/>支持的加密套件<br/>客户端随机数 Client Random<br/>支持的扩展</code></pre><p>可以理解为客户端说：</p><pre><code>我支持 TLS1.2 / TLS1.3<br/>我支持这些加密算法<br/>这是我的随机数<br/>你选一个吧</code></pre><ol><li>第二步：ServerHello</li></ol><p>服务端返回 ServerHello，选择双方都支持的一套方案。</p><p>里面通常包括：</p><pre><code>选定的 TLS 版本<br/>选定的加密套件<br/>服务端随机数 Server Random</code></pre><p>可以理解为服务端说：</p><pre><code>那我们用 TLS1.3<br/>用这个加密套件<br/>这是我的随机数</code></pre><ol><li>第三步：服务端发送证书</li></ol><p>服务端会把自己的数字证书发给客户端。</p><p>证书里面包括：</p><pre><code>域名信息<br/>服务端公钥<br/>证书颁发机构 CA<br/>证书有效期<br/>数字签名</code></pre><p>客户端会验证证书是否合法，比如：</p><pre><code>证书是不是可信 CA 签发的<br/>证书有没有过期<br/>证书域名和访问域名是否一致<br/>证书有没有被吊销</code></pre><p>这一步是为了确认：你访问的服务端是真的，不是中间人伪造的。</p><ol><li>第四步：密钥协商</li></ol><p>证书验证通过后，客户端和服务端会协商出后续加密通信使用的对称密钥。</p><p>这里要注意：真正传输数据时一般用对称加密，因为速度快。</p><p>非对称加密主要用于身份认证和密钥协商。</p><pre><code>非对称加密：解决身份认证、密钥协商<br/>对称加密：解决后续数据加密传输</code></pre><p>在 TLS 1.2 中，常见方式有 RSA 或 ECDHE。</p><p>在 TLS 1.3 中，基本使用 ECDHE 这类密钥交换方式，安全性更好，还支持前向安全。</p><ol><li>第五步：生成会话密钥</li></ol><p>双方根据握手过程中产生的信息生成相同的会话密钥。</p><p>通常会用到：</p><pre><code>Client Random<br/>Server Random<br/>密钥交换产生的共享秘密</code></pre><p>最终双方各自计算出一样的对称密钥。</p><pre><code>客户端算出 session key<br/>服务端算出 session key</code></pre><p>之后 HTTP 数据就用这个 session key 加密传输。</p><ol><li>第六步：Finished 校验</li></ol><p>双方会互相发送 Finished 消息，表示握手完成。</p><p>这个消息会对前面的握手内容做完整性校验。</p><p>如果校验通过，说明：</p><pre><code>密钥协商成功<br/>握手过程没有被篡改<br/>双方可以开始加密通信</code></pre><ol><li>第七步：加密传输 HTTP 数据</li></ol><p>TLS 握手完成后，客户端再发送 HTTP 请求。</p><p>只不过这个 HTTP 请求已经不是明文了，而是通过 TLS 加密后的数据。</p><pre><code>客户端 —— 加密 HTTP 请求 ——&gt; 服务端<br/>客户端 &lt;—— 加密 HTTP 响应 —— 服务端</code></pre><ol><li>简化流程图</li></ol><pre><code>客户端                          服务端<br/><br/>TCP 三次握手  -----------------&gt;<br/><br/>ClientHello  ------------------&gt;<br/>        支持版本、加密套件、Client Random<br/><br/>                 &lt;------------- ServerHello<br/>                         选择版本、加密套件、Server Random<br/><br/>                 &lt;------------- Certificate<br/>                         服务端证书、公钥<br/><br/>验证证书是否合法<br/><br/>密钥协商  &lt;--------------------&gt; <br/>        生成共享秘密<br/><br/>双方生成会话密钥<br/><br/>Finished  ---------------------&gt;<br/>                 &lt;------------- Finished<br/><br/>加密 HTTP 请求  ---------------&gt;<br/>                 &lt;------------- 加密 HTTP 响应</code></pre><ol><li><strong>面试背诵版：HTTPS 完整握手过程是什么？</strong></li></ol><blockquote><p>HTTPS 完整过程是先建立 TCP 连接，然后进行 TLS 握手，最后再加密传输 HTTP 数据。TLS 握手时，客户端先发送 ClientHello，包含支持的 TLS 版本、加密套件和客户端随机数；服务端返回 ServerHello，选择加密套件并返回服务端随机数；然后服务端发送数字证书，客户端验证证书是否合法，包括 CA、有效期、域名和吊销状态。证书验证通过后，双方通过 RSA 或 ECDHE 等方式进行密钥协商，并结合客户端随机数、服务端随机数和共享秘密生成相同的会话密钥。最后双方发送 Finished 消息校验握手完整性，握手完成后就使用这个对称会话密钥加密传输 HTTP 请求和响应。</p></blockquote>"},{"t":"HTTPS 为什么需要证书？","tag":"","p":"","html":""},{"t":"数字证书是什么？","tag":"","p":"","html":""},{"t":"CA 是什么？","tag":"","p":"","html":""},{"t":"什么是非对称加密？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"什么是对称加密？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"HTTPS 为什么不直接使用非对称加密？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"HTTPS 为什么需要同时使用对称加密和非对称加密？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"HTTPS 如何防止中间人攻击？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"什么是数字签名？","tag":"","p":"","html":""},{"t":"数字签名和数字证书有什么区别？","tag":"","p":"","html":""},{"t":"TLS 1.2 和 TLS 1.3 有什么区别？","tag":"","p":"","html":""}]},{"no":"十三","title":"DNS","questions":[{"t":"DNS 是什么？","tag":"","p":"","html":""},{"t":"为什么需要 DNS？","tag":"","p":"","html":""},{"t":"DNS 查询过程是什么？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"浏览器输入一个域名后发生了什么？","tag":"面试必考","p":"must","html":"<p>浏览器输入一个域名后发生的核心流程是：URL 解析 → DNS 解析 → 建立连接 → 发送 HTTP/HTTPS 请求 → 服务端处理 → 返回响应 → 浏览器渲染页面。</p><ol><li>URL 解析</li></ol><p>浏览器先解析你输入的内容。</p><p>比如输入：</p><pre><code>https://www.example.com/index.html</code></pre><p>浏览器会拆成：</p><pre><code>协议：https<br/>域名：www.example.com<br/>端口：默认 443<br/>路径：/index.html</code></pre><p>如果你没写协议，浏览器可能会自动补全 <code>https://</code> 或根据历史记录、搜索引擎判断。</p><ol><li>DNS 解析</li></ol><p>浏览器需要把域名转换成 IP 地址。</p><p>因为网络通信真正找的是 IP，不是域名。</p><p>DNS 查询大致顺序是：</p><pre><code>浏览器 DNS 缓存<br/>操作系统 DNS 缓存<br/>hosts 文件<br/>本地 DNS 服务器<br/>根域名服务器<br/>顶级域名服务器<br/>权威域名服务器</code></pre><p>最后拿到类似这样的 IP：</p><pre><code>www.example.com → 93.184.216.34</code></pre><ol><li>建立 TCP 连接</li></ol><p>拿到 IP 后，浏览器会和服务器建立 TCP 连接。</p><p>TCP 连接通过三次握手完成：</p><pre><code>客户端 → SYN → 服务端<br/>客户端 ← SYN + ACK ← 服务端<br/>客户端 → ACK → 服务端</code></pre><p>建立连接后，双方就可以可靠传输数据。</p><ol><li>如果是 HTTPS，还要 TLS 握手</li></ol><p>如果访问的是 <code>https://</code>，TCP 连接建立后，还要进行 TLS 握手。</p><p>TLS 握手主要做三件事：</p><pre><code>验证服务端证书<br/>协商加密算法<br/>生成会话密钥</code></pre><p>之后 HTTP 数据就会被加密传输。</p><ol><li>浏览器发送 HTTP 请求</li></ol><p>连接建立好之后，浏览器发送 HTTP 请求。</p><p>请求里通常包括：</p><pre><code>请求方法：GET / POST<br/>请求路径<br/>请求头<br/>Cookie<br/>请求体</code></pre><p>比如：</p><pre><code>GET /index.html HTTP/1.1<br/>Host: www.example.com<br/>User-Agent: Chrome<br/>Cookie: token=xxx</code></pre><ol><li>服务端处理请求</li></ol><p>请求到达服务端后，可能会经过：</p><pre><code>负载均衡<br/>网关 / Nginx<br/>应用服务器<br/>业务逻辑<br/>数据库 / 缓存</code></pre><p>比如服务端查询数据库、读取缓存、生成 HTML 或 JSON。</p><ol><li>服务端返回 HTTP 响应</li></ol><p>服务端处理完后返回响应。</p><p>响应包括：</p><pre><code>状态码<br/>响应头<br/>响应体</code></pre><p>比如：</p><pre><code>HTTP/1.1 200 OK<br/>Content-Type: text/html<br/>Cache-Control: max-age=3600<br/><br/>&lt;html&gt;...&lt;/html&gt;</code></pre><p>如果是重定向，可能返回 <code>301</code> 或 <code>302</code>。</p><p>如果命中协商缓存，可能返回 <code>304</code>。</p><ol><li>浏览器解析和渲染页面</li></ol><p>浏览器拿到 HTML 后开始渲染。</p><p>大致过程是：</p><pre><code>解析 HTML → 构建 DOM 树<br/>解析 CSS → 构建 CSSOM 树<br/>合成 Render Tree<br/>布局 Layout<br/>绘制 Paint<br/>合成 Composite</code></pre><p>如果 HTML 中还有 CSS、JS、图片等资源，浏览器会继续发起请求加载这些资源。</p><ol><li>整体流程图</li></ol><pre><code>输入 URL<br/>  ↓<br/>解析 URL<br/>  ↓<br/>DNS 解析域名得到 IP<br/>  ↓<br/>TCP 三次握手<br/>  ↓<br/>HTTPS 场景进行 TLS 握手<br/>  ↓<br/>发送 HTTP 请求<br/>  ↓<br/>服务端处理请求<br/>  ↓<br/>返回 HTTP 响应<br/>  ↓<br/>浏览器解析 HTML/CSS/JS<br/>  ↓<br/>构建 DOM、CSSOM、Render Tree<br/>  ↓<br/>布局、绘制、合成<br/>  ↓<br/>页面展示</code></pre><ol><li>面试加分点</li></ol><p>可以补充几个细节：</p><pre><code>DNS 有多级缓存<br/>HTTPS 比 HTTP 多 TLS 握手<br/>HTTP/2 可以多路复用<br/>浏览器会检查强缓存和协商缓存<br/>服务端可能经过 CDN、负载均衡、网关<br/>页面渲染中 JS 可能阻塞 DOM 解析</code></pre><ol><li><strong>面试背诵版：浏览器输入一个域名后发生了什么？</strong></li></ol><blockquote><p>浏览器输入一个域名后，首先会解析 URL，确定协议、域名、端口和路径；然后进行 DNS 解析，把域名转换成 IP 地址，DNS 会依次查浏览器缓存、系统缓存、hosts、本地 DNS 以及权威 DNS。拿到 IP 后，浏览器会和服务器建立 TCP 连接，如果是 HTTPS，还会进行 TLS 握手，完成证书校验、密钥协商和加密通道建立。之后浏览器发送 HTTP 请求，服务端经过负载均衡、网关、应用服务、缓存或数据库处理后返回 HTTP 响应。浏览器收到响应后解析 HTML、CSS、JavaScript，构建 DOM 树和 CSSOM 树，生成渲染树，最后经过布局、绘制和合成，把页面展示出来。</p></blockquote>"},{"t":"DNS 解析有哪些步骤？","tag":"","p":"","html":""},{"t":"什么是递归 DNS？","tag":"","p":"","html":""},{"t":"什么是权威 DNS？","tag":"","p":"","html":""},{"t":"DNS 为什么使用 UDP？","tag":"","p":"","html":""},{"t":"DNS 使用 TCP 的场景有哪些？","tag":"","p":"","html":""},{"t":"DNS 缓存是什么？","tag":"","p":"","html":""},{"t":"DNS 劫持是什么？","tag":"","p":"","html":""},{"t":"DNS 负载均衡是什么？","tag":"","p":"","html":""}]},{"no":"十四","title":"Socket","questions":[{"t":"Socket 是什么？","tag":"","p":"","html":""},{"t":"Socket 和 TCP 有什么关系？","tag":"","p":"","html":""},{"t":"Socket 和 HTTP 有什么关系？","tag":"","p":"","html":""},{"t":"TCP Socket 通信流程是什么？","tag":"面试必考","p":"must","html":"<p>TCP Socket 通信流程的核心是：服务端先创建监听 Socket，客户端主动连接，双方通过三次握手建立 TCP 连接，然后用读写 Socket 进行数据通信，最后关闭连接。</p><ol><li>服务端流程</li></ol><p>服务端一般是被动等待连接，流程是：</p><pre><code>socket()<br/>bind()<br/>listen()<br/>accept()<br/>read()/write()<br/>close()</code></pre><p>具体解释：</p><pre><code>socket()：创建一个 Socket<br/>bind()：绑定 IP 和端口<br/>listen()：开始监听连接<br/>accept()：接收客户端连接<br/>read()/write()：收发数据<br/>close()：关闭连接</code></pre><p>服务端调用 <code>accept()</code> 后会阻塞等待客户端连接。</p><ol><li>客户端流程</li></ol><p>客户端一般是主动发起连接，流程是：</p><pre><code>socket()<br/>connect()<br/>write()/read()<br/>close()</code></pre><p>具体解释：</p><pre><code>socket()：创建 Socket<br/>connect()：向服务端发起连接<br/>write()/read()：发送和接收数据<br/>close()：关闭连接</code></pre><p>客户端调用 <code>connect()</code> 时，底层会触发 TCP 三次握手。</p><ol><li>建立连接过程</li></ol><p>客户端 <code>connect()</code> 和服务端 <code>accept()</code> 之间，底层会完成 TCP 三次握手。</p><pre><code>客户端                         服务端<br/><br/>socket()                       socket()<br/>                               bind()<br/>                               listen()<br/>connect()  —— SYN ————————&gt;<br/>           &lt;— SYN + ACK ———<br/>           —— ACK ————————&gt;<br/>                               accept() 返回连接</code></pre><p>三次握手成功后，连接才真正建立。</p><ol><li>accept 返回的是什么？</li></ol><p>服务端一开始创建的是监听 Socket。</p><p>当有客户端连接时，<code>accept()</code> 会返回一个新的已连接 Socket。</p><pre><code>监听 Socket：负责接收新连接<br/>连接 Socket：负责和某个客户端通信</code></pre><p>也就是说：</p><pre><code>server_fd = socket()<br/>bind(server_fd)<br/>listen(server_fd)<br/><br/>client_fd = accept(server_fd)</code></pre><p><code>server_fd</code> 继续监听新连接，<code>client_fd</code> 专门负责和这个客户端收发数据。</p><ol><li>数据通信过程</li></ol><p>连接建立后，客户端和服务端就可以双向通信。</p><pre><code>客户端 write()  —— 数据 ——&gt;  服务端 read()<br/>客户端 read()   &lt;— 数据 ——  服务端 write()</code></pre><p>TCP 是全双工的，双方可以同时读写。</p><p>并且 TCP 提供可靠、有序、面向字节流的传输。</p><ol><li>关闭连接过程</li></ol><p>通信完成后，双方会关闭连接。</p><p>通常底层会触发 TCP 四次挥手。</p><pre><code>主动关闭方 —— FIN ————&gt;<br/>主动关闭方 &lt;— ACK ————<br/>主动关闭方 &lt;— FIN ————<br/>主动关闭方 —— ACK ————&gt;</code></pre><p>应用层调用的是 <code>close()</code>，TCP 层负责完成挥手过程。</p><ol><li>整体流程图</li></ol><pre><code>服务端                                  客户端<br/><br/>socket()<br/>bind()<br/>listen()<br/><br/>                                        socket()<br/>                                        connect()<br/>             &lt;—— TCP 三次握手 ——&gt;<br/><br/>accept()<br/>read()     &lt;———————— 数据 ————————     write()<br/>write()    ———————— 数据 ————————&gt;     read()<br/><br/>close()                                  close()<br/>             &lt;—— TCP 四次挥手 ——&gt;</code></pre><ol><li>面试常见追问</li></ol><p>可以补充：</p><pre><code>bind 是绑定本地 IP 和端口<br/>listen 是把 Socket 变成监听状态<br/>accept 返回新的连接 Socket<br/>connect 会触发 TCP 三次握手<br/>read/write 是应用层读写，底层由 TCP 保证可靠传输<br/>close 会触发四次挥手</code></pre><ol><li><strong>面试背诵版：TCP Socket 通信流程是什么？</strong></li></ol><blockquote><p>TCP Socket 通信流程分为服务端和客户端两部分。服务端先调用 socket 创建套接字，然后 bind 绑定 IP 和端口，listen 进入监听状态，accept 阻塞等待客户端连接；客户端调用 socket 创建套接字，然后调用 connect 主动连接服务端，底层会触发 TCP 三次握手。连接建立后，服务端的 accept 会返回一个新的已连接 Socket，原来的监听 Socket 继续接收新连接，新的 Socket 专门负责和该客户端通过 read、write 进行数据收发。通信结束后，双方调用 close 关闭连接，底层会进行 TCP 四次挥手。总体来说，服务端流程是 socket、bind、listen、accept、read/write、close，客户端流程是 socket、connect、read/write、close。</p></blockquote>"},{"t":"Server Socket 和 Client Socket 有什么区别？","tag":"","p":"","html":""},{"t":"bind、listen、accept 分别做什么？","tag":"","p":"","html":""},{"t":"connect 做什么？","tag":"","p":"","html":""},{"t":"send 和 recv 做什么？","tag":"","p":"","html":""},{"t":"为什么一个服务器可以同时处理很多客户端？","tag":"","p":"","html":""},{"t":"Socket 阻塞和非阻塞有什么区别？","tag":"","p":"","html":""},{"t":"什么是 IO 多路复用？","tag":"面试必考","p":"must","html":"<p>IO 多路复用的核心是：一个线程可以同时监听多个 Socket，一旦某个 Socket 可读或可写，就通知程序去处理，从而避免一个连接占用一个线程。</p><ol><li>为什么需要 IO 多路复用？</li></ol><p>如果不用 IO 多路复用，最简单的服务器模型是：</p><pre><code>一个连接 → 一个线程</code></pre><p>比如有 1 万个连接，就可能需要 1 万个线程。</p><p>问题是：</p><pre><code>线程数量太多<br/>上下文切换成本高<br/>内存占用大<br/>大量线程其实都在等待 IO</code></pre><p>所以高并发场景下，这种方式效率不高。</p><ol><li>IO 多路复用是什么？</li></ol><p>IO 多路复用就是让一个线程同时管理多个 IO 连接。</p><p>它会把多个 Socket 注册到一个监听器上。</p><p>当某个 Socket 有事件发生，比如可读、可写、连接到来，就通知应用程序处理。</p><pre><code>一个线程<br/>  ↓<br/>监听多个 Socket<br/>  ↓<br/>哪个 Socket 就绪，就处理哪个</code></pre><ol><li>它复用的是什么？</li></ol><p>这里“复用”的不是连接，也不是数据，而是复用线程。</p><p>也就是说，一个线程可以监听多个 IO。</p><pre><code>传统方式：<br/>连接1 → 线程1<br/>连接2 → 线程2<br/>连接3 → 线程3<br/><br/>IO 多路复用：<br/>连接1、连接2、连接3 → 一个线程监听</code></pre><ol><li>常见实现有哪些？</li></ol><p>常见的 IO 多路复用机制有：</p><pre><code>select<br/>poll<br/>epoll</code></pre><p>Linux 下高性能服务器通常用 <code>epoll</code>。</p><p>比如 Redis、Nginx、Netty 底层都大量依赖 IO 多路复用思想。</p><ol><li>select / poll / epoll 简单区别</li></ol><p><code>select</code> 和 <code>poll</code> 每次都需要遍历所有连接，检查哪些连接就绪。</p><p><code>epoll</code> 会把就绪事件通知给应用，不需要每次全量遍历所有连接。</p><pre><code>select / poll：<br/>我挨个问：你准备好了吗？<br/><br/>epoll：<br/>谁准备好了，谁主动通知我。</code></pre><p>所以连接数很大时，<code>epoll</code> 性能更好。</p><ol><li>IO 多路复用的工作流程</li></ol><p>以 <code>epoll</code> 为例：</p><pre><code>1. 创建 epoll 实例<br/>2. 把 Socket 注册到 epoll<br/>3. 调用 epoll_wait 等待事件<br/>4. 某个 Socket 就绪后返回<br/>5. 程序处理对应 Socket 的读写</code></pre><p>流程图：</p><pre><code>多个 Socket<br/>   ↓ 注册<br/>epoll<br/>   ↓ 等待事件<br/>epoll_wait()<br/>   ↓ 返回就绪 Socket<br/>应用程序 read / write</code></pre><ol><li>它有什么优点？</li></ol><p>IO 多路复用的优点是：</p><pre><code>减少线程数量<br/>减少上下文切换<br/>节省内存<br/>适合大量连接、少量活跃的场景<br/>提高高并发处理能力</code></pre><p>比如聊天室、网关、Redis、Nginx 这类长连接或高并发场景都很适合。</p><ol><li>它不是万能的</li></ol><p>IO 多路复用适合 IO 密集型场景。</p><p>如果每个请求都要做大量 CPU 计算，一个线程还是会被计算任务占住，这时候需要多线程、多进程或异步任务配合。</p><ol><li><strong>面试背诵版：什么是 IO 多路复用？</strong></li></ol><blockquote><p>IO 多路复用是指一个线程可以同时监听多个 Socket 连接，当某个 Socket 发生可读、可写或连接到来等事件时，操作系统通知应用程序处理。它解决的是一个连接对应一个线程导致线程过多、上下文切换成本高和内存占用大的问题。常见实现有 select、poll 和 epoll，其中 select 和 poll 通常需要遍历所有连接，而 epoll 基于事件通知机制，只返回就绪的连接，更适合高并发场景。IO 多路复用本质上复用的是线程，让少量线程处理大量连接，常用于 Redis、Nginx、Netty 等高性能网络系统中。</p></blockquote>"},{"t":"select、poll、epoll 有什么区别？","tag":"面试必考","p":"must","html":"<p>select、poll、epoll 的核心区别是：select 和 poll 每次都要遍历所有连接找就绪事件，epoll 通过事件通知只返回就绪连接，所以 epoll 更适合高并发大量连接场景。</p><ol><li>select 是什么？</li></ol><p><code>select</code> 是比较早的 IO 多路复用机制。</p><p>它把要监听的文件描述符放到一个集合里，然后调用 <code>select()</code> 等待事件。</p><pre><code>应用程序把 fd 集合交给内核<br/>内核检查哪些 fd 就绪<br/>返回后应用程序再遍历 fd 集合<br/>找到可读/可写的 fd</code></pre><p>问题是：每次调用都要把 fd 集合从用户态拷贝到内核态，返回后还要遍历整个集合。</p><ol><li>select 的缺点</li></ol><p><code>select</code> 主要有三个问题：</p><pre><code>1. fd 数量有限制，通常默认 1024<br/>2. 每次都要拷贝 fd 集合<br/>3. 每次返回后都要遍历所有 fd</code></pre><p>所以连接数一多，性能会下降。</p><ol><li>poll 是什么？</li></ol><p><code>poll</code> 和 <code>select</code> 思路类似，也是把一批 fd 交给内核，让内核检查哪些就绪。</p><p>区别是 <code>poll</code> 用的是数组结构，没有 <code>select</code> 那种固定 1024 的 fd 数量限制。</p><pre><code>select：用 bitmap<br/>poll：用 pollfd 数组</code></pre><p>但 <code>poll</code> 仍然需要每次拷贝 fd 数组，并且返回后仍然要遍历所有 fd。</p><ol><li>poll 的优缺点</li></ol><p><code>poll</code> 的优点是：</p><pre><code>没有 select 默认 1024 的数量限制</code></pre><p>但缺点仍然明显：</p><pre><code>每次调用都要拷贝 fd 数组<br/>每次返回后都要遍历所有 fd<br/>连接多时性能也会下降</code></pre><p>所以 <code>poll</code> 只是解决了数量限制，没有从根本上解决遍历问题。</p><ol><li>epoll 是什么？</li></ol><p><code>epoll</code> 是 Linux 下更高效的 IO 多路复用机制。</p><p>它把 fd 注册到内核维护的结构中，之后不需要每次重复传入所有 fd。</p><p>流程是：</p><pre><code>epoll_create：创建 epoll 实例<br/>epoll_ctl：注册、修改、删除 fd<br/>epoll_wait：等待就绪事件</code></pre><p>当某个 fd 就绪时，内核会把它放到就绪队列，<code>epoll_wait</code> 直接返回就绪 fd。</p><ol><li>epoll 为什么更快？</li></ol><p><code>epoll</code> 快主要因为：</p><pre><code>不用每次重复拷贝所有 fd<br/>不用每次遍历所有 fd<br/>只返回真正就绪的 fd<br/>支持大量连接</code></pre><p>可以理解为：</p><pre><code>select / poll：<br/>你把所有人名单给我，我帮你挨个问谁准备好了。<br/><br/>epoll：<br/>谁准备好了，我直接把谁放进就绪队列，你来拿就行。</code></pre><ol><li>三者对比</li></ol><pre><code>select：<br/>有 fd 数量限制；每次拷贝；每次遍历；适合连接少的场景<br/><br/>poll：<br/>无固定 fd 数量限制；每次拷贝；每次遍历；连接多仍然效率低<br/><br/>epoll：<br/>无明显连接数限制；事件驱动；只返回就绪 fd；适合高并发</code></pre><ol><li>epoll 的触发模式</li></ol><p><code>epoll</code> 支持两种触发模式：</p><pre><code>LT：水平触发<br/>ET：边缘触发</code></pre><p>水平触发是只要 fd 还有数据没读完，就会一直通知。</p><p>边缘触发是状态发生变化时才通知一次，性能更高，但要求一次性把数据读完，通常要配合非阻塞 IO。</p><ol><li><strong>面试背诵版：select、poll、epoll 有什么区别？</strong></li></ol><blockquote><p>select、poll、epoll 都是 IO 多路复用机制。select 使用 fd 集合监听多个连接，但有默认 1024 的数量限制，并且每次调用都要把 fd 集合从用户态拷贝到内核态，返回后还要遍历所有 fd。poll 使用数组结构，解决了 select 的数量限制，但仍然需要每次拷贝和遍历所有 fd。epoll 是 Linux 下更高效的实现，它通过 epoll_create 创建实例，通过 epoll_ctl 注册 fd，内核维护监听集合，事件就绪后放入就绪队列，epoll_wait 只返回真正就绪的 fd，因此避免了重复拷贝和全量遍历，更适合高并发、大量连接的场景。</p></blockquote><hr/>"}]},{"no":"十五","title":"Linux 网络 / IO","questions":[{"t":"什么是文件描述符 FD？","tag":"","p":"","html":""},{"t":"Socket 为什么也是文件？","tag":"","p":"","html":""},{"t":"什么是 IO 多路复用？","tag":"","p":"","html":""},{"t":"为什么需要 IO 多路复用？","tag":"","p":"","html":""},{"t":"select 的原理是什么？","tag":"","p":"","html":""},{"t":"poll 的原理是什么？","tag":"","p":"","html":""},{"t":"epoll 的原理是什么？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"epoll 为什么比 select 快？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"epoll LT 和 ET 有什么区别？","tag":"面试必考","p":"must","html":"<p>epoll LT 和 ET 的核心区别是：LT 是“只要还有数据就一直通知”，ET 是“状态变化时只通知一次”。</p><ol><li>LT 是什么？</li></ol><p>LT 是 Level Trigger，水平触发。</p><p>只要文件描述符处于就绪状态，<code>epoll_wait</code> 就会反复通知你。</p><p>比如 socket 缓冲区里还有数据没读完，它下次还会继续通知。</p><pre><code>缓冲区有数据<br/>  ↓<br/>epoll_wait 返回<br/>  ↓<br/>应用只读了一部分<br/>  ↓<br/>缓冲区还有数据<br/>  ↓<br/>下次 epoll_wait 继续返回</code></pre><p>所以 LT 比较安全，不容易漏事件。</p><ol><li>ET 是什么？</li></ol><p>ET 是 Edge Trigger，边缘触发。</p><p>它只在状态发生变化时通知一次。</p><p>比如从“没有数据”变成“有数据”的那一刻通知你。</p><p>如果你这次没有把数据读完，后面可能不会再通知。</p><pre><code>缓冲区从无数据 → 有数据<br/>  ↓<br/>epoll_wait 返回一次<br/>  ↓<br/>应用只读了一部分<br/>  ↓<br/>缓冲区还有数据<br/>  ↓<br/>不一定再通知</code></pre><p>所以 ET 性能更高，但更容易写错。</p><ol><li>用水位理解</li></ol><p>LT 像水位报警器：</p><pre><code>只要水位高于警戒线，就一直报警</code></pre><p>ET 像水位变化报警器：</p><pre><code>只有水位从低变高那一刻报警一次</code></pre><p>所以 LT 看的是“当前状态”，ET 看的是“状态变化”。</p><ol><li>LT 的优缺点</li></ol><p>LT 的优点：</p><pre><code>编程简单<br/>不容易漏读数据<br/>默认模式<br/>适合大多数场景</code></pre><p>LT 的缺点：</p><pre><code>如果数据一直没读完，会反复触发<br/>事件通知次数可能更多<br/>性能略低一些</code></pre><ol><li>ET 的优缺点</li></ol><p>ET 的优点：</p><pre><code>通知次数少<br/>性能更高<br/>适合高性能网络服务器</code></pre><p>ET 的缺点：</p><pre><code>编程复杂<br/>必须配合非阻塞 IO<br/>必须一次性读到 EAGAIN<br/>否则可能漏事件</code></pre><ol><li>为什么 ET 要配合非阻塞 IO？</li></ol><p>因为 ET 模式下通常要在一次事件通知后，把数据尽可能读完。</p><p>也就是循环读：</p><pre><code>while (true) {<br/>    n = read(fd, buf);<br/>    if (n &gt; 0) {<br/>        继续处理<br/>    } else if (n == -1 &amp;&amp; errno == EAGAIN) {<br/>        说明读完了，退出循环<br/>    }<br/>}</code></pre><p>如果使用阻塞 IO，当缓冲区数据读完后，<code>read()</code> 可能会阻塞住，导致整个线程卡住。</p><p>所以 ET 一般必须使用非阻塞 IO。</p><ol><li>面试常见对比</li></ol><pre><code>LT：<br/>只要 fd 处于就绪状态就通知<br/>可以不一次读完<br/>默认模式<br/>更安全<br/><br/>ET：<br/>只有状态变化时通知<br/>需要一次读到 EAGAIN<br/>必须配合非阻塞 IO<br/>性能更高</code></pre><ol><li><strong>面试背诵版：epoll LT 和 ET 有什么区别？</strong></li></ol><blockquote><p>epoll 的 LT 是水平触发，ET 是边缘触发。LT 关注的是当前状态，只要文件描述符还处于可读或可写状态，epoll_wait 就会不断返回这个事件，所以即使一次没有把数据读完，下次还会继续通知，编程更简单，也更安全。ET 关注的是状态变化，只有从不可读变为可读、从不可写变为可写时才通知一次，如果这次没有把数据读完，后面可能不会再次通知，所以 ET 必须配合非阻塞 IO，并且通常要循环读到 EAGAIN。LT 默认更安全，ET 通知次数更少，性能更高，但实现更复杂。</p></blockquote>"},{"t":"epoll 为什么需要非阻塞 Socket？","tag":"","p":"","html":""},{"t":"Reactor 模型是什么？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"一个高并发 Web Server 是怎么工作的？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"sendfile 是什么？","tag":"","p":"","html":""},{"t":"什么是零拷贝？","tag":"面试必考","p":"must","html":"<p>零拷贝的核心是：减少数据在用户态和内核态之间的拷贝次数，让数据尽量在内核中直接从磁盘发送到网卡，从而提升 IO 性能。</p><ol><li>传统 IO 发生了什么？</li></ol><p>比如服务端要把磁盘文件发送给客户端，传统写法可能是：</p><pre><code>read(file, buffer)<br/>write(socket, buffer)</code></pre><p>流程大概是：</p><pre><code>磁盘 → 内核缓冲区 → 用户缓冲区 → Socket 内核缓冲区 → 网卡</code></pre><p>这里有多次数据拷贝，也有用户态和内核态切换。</p><ol><li>传统 IO 的问题</li></ol><p>传统方式的问题是：</p><pre><code>数据拷贝次数多<br/>用户态和内核态切换多<br/>CPU 参与大量搬运数据<br/>性能浪费</code></pre><p>尤其是大文件传输时，CPU 其实不应该忙着“搬数据”，而应该让 DMA 和内核机制来做。</p><ol><li>零拷贝是什么？</li></ol><p>零拷贝不是说真的一次拷贝都没有。</p><p>它的意思是：减少 CPU 参与的数据拷贝，尤其是避免数据从内核态拷贝到用户态，再从用户态拷贝回内核态。</p><pre><code>传统：<br/>内核 → 用户 → 内核<br/><br/>零拷贝：<br/>内核内部直接转发</code></pre><ol><li>sendfile 怎么实现零拷贝？</li></ol><p>Linux 中常见的零拷贝方式是 <code>sendfile</code>。</p><p>它可以把文件数据直接从文件描述符发送到 Socket。</p><pre><code>sendfile(socket_fd, file_fd)</code></pre><p>大致流程：</p><pre><code>磁盘 → 内核页缓存 → Socket 缓冲区 → 网卡</code></pre><p>相比传统方式，少了用户态缓冲区这一步。</p><ol><li>配合 DMA 后的过程</li></ol><p>DMA 可以让硬件直接把数据从磁盘拷贝到内存，或者从内存拷贝到网卡，不需要 CPU 一字节一字节搬。</p><pre><code>磁盘 --DMA--&gt; 内核缓冲区<br/>内核缓冲区 --DMA--&gt; 网卡</code></pre><p>CPU 主要负责控制和描述数据位置，而不是实际搬运数据。</p><ol><li>mmap 算不算零拷贝？</li></ol><p><code>mmap</code> 也是一种减少拷贝的方式。</p><p>它把文件映射到用户进程地址空间，用户程序可以像访问内存一样访问文件。</p><pre><code>磁盘 → 内核页缓存<br/>          ↑<br/>       用户空间映射访问</code></pre><p><code>mmap</code> 可以减少从内核缓冲区到用户缓冲区的一次拷贝，但发送到 Socket 时通常还需要一次拷贝。</p><ol><li>零拷贝的优点</li></ol><p>零拷贝的优点是：</p><pre><code>减少数据拷贝<br/>减少上下文切换<br/>降低 CPU 消耗<br/>提高文件传输性能<br/>适合大文件传输</code></pre><p>典型应用：</p><pre><code>Nginx 静态文件传输<br/>Kafka 日志文件发送<br/>文件服务器<br/>高性能网络框架</code></pre><ol><li>零拷贝不是万能的</li></ol><p>零拷贝更适合“不需要应用层修改数据”的场景。</p><p>比如静态文件传输、日志传输。</p><p>如果应用需要对数据进行压缩、加密、修改内容，就必须把数据读到用户态处理，这时零拷贝效果会受限。</p><ol><li><strong>面试背诵版：什么是零拷贝？</strong></li></ol><blockquote><p>零拷贝是指在 IO 过程中尽量减少数据在用户态和内核态之间的拷贝，避免数据从内核缓冲区拷贝到用户缓冲区，再从用户缓冲区拷贝回内核 Socket 缓冲区。传统文件发送通常是磁盘到内核缓冲区、内核缓冲区到用户缓冲区、用户缓冲区到 Socket 缓冲区、再到网卡，拷贝和上下文切换都比较多。零拷贝通过 sendfile、mmap 等机制，让数据尽量在内核中直接从文件页缓存发送到 Socket，再由 DMA 发送到网卡，从而减少 CPU 拷贝和用户态内核态切换，提高大文件传输性能。它常用于 Nginx、Kafka、文件服务器等场景，但更适合不需要应用层修改数据的传输。</p></blockquote>"},{"t":"mmap 和 sendfile 有什么区别？","tag":"","p":"","html":""}]},{"no":"十六","title":"RPC / 微服务网络","questions":[{"t":"什么是 RPC？","tag":"","p":"","html":""},{"t":"RPC 和 HTTP 有什么区别？","tag":"","p":"","html":""},{"t":"RPC 的完整调用过程是什么？","tag":"","p":"","html":""},{"t":"gRPC 是什么？","tag":"","p":"","html":""},{"t":"gRPC 为什么使用 HTTP/2？","tag":"","p":"","html":""},{"t":"Protobuf 是什么？","tag":"","p":"","html":""},{"t":"RESTful API 和 RPC 有什么区别？","tag":"","p":"","html":""},{"t":"RPC 如何实现服务发现？","tag":"","p":"","html":""},{"t":"RPC 如何实现负载均衡？","tag":"","p":"","html":""},{"t":"RPC 如何处理超时？","tag":"","p":"","html":""},{"t":"RPC 如何处理重试？","tag":"","p":"","html":""},{"t":"RPC 为什么需要连接池？","tag":"","p":"","html":""}]},{"no":"十七","title":"综合场景题","questions":[{"t":"浏览器输入 www.xxx.com 后发生了什么？","tag":"面试必考","p":"must","html":"<span>[原文档此处有配图]</span><p>浏览器输入网址后，核心流程是：URL 解析 → DNS 解析 → 建立连接 → 发送 HTTP 请求 → 服务器处理 → 返回响应 → 浏览器解析渲染页面。</p><ol><li>URL 解析</li></ol><p>浏览器先解析用户输入的地址。</p><p>比如：</p><pre><code>www.xxx.com</code></pre><p>如果没有写协议，浏览器会补全：</p><pre><code>http://www.xxx.com</code></pre><p>或者优先尝试：</p><pre><code>https://www.xxx.com</code></pre><p>它会解析出：</p><ul><li>协议</li><li>域名</li><li>端口</li><li>路径</li><li>查询参数</li></ul><p>比如：</p><pre><code>https://www.xxx.com/index.html?id=1</code></pre><p>对应：</p><ul><li>协议：https</li><li>域名：<a href=\"http://www.xxx.com/\" target=\"_blank\" rel=\"noopener\">www.xxx.com</a></li><li>端口：443</li><li>路径：/index.html</li><li>参数：id=1</li></ul><ol><li>浏览器缓存检查</li></ol><p>浏览器会先看本地是否有缓存。</p><p>比如：</p><ul><li>浏览器缓存</li><li>Service Worker 缓存</li><li>HTTP 缓存</li><li>DNS 缓存</li><li>HSTS 记录</li></ul><p>如果资源没过期，可能直接使用缓存，减少网络请求。</p><ol><li>DNS 解析</li></ol><p>浏览器需要把域名解析成 IP 地址。</p><p>因为网络通信最终靠 IP。</p><p>DNS 查询顺序大概是：</p><pre><code>浏览器 DNS 缓存<br/>本机 hosts 文件<br/>操作系统 DNS 缓存<br/>本地 DNS 服务器<br/>根 DNS<br/>顶级域 DNS<br/>权威 DNS</code></pre><p>最终得到：</p><pre><code>www.xxx.com → 目标服务器 IP</code></pre><ol><li>建立 TCP 连接</li></ol><p>如果是 HTTP/1.1 或 HTTP/2，一般基于 TCP。</p><p>浏览器通过三次握手和服务器建立 TCP 连接：</p><pre><code>SYN<br/>SYN + ACK<br/>ACK</code></pre><p>连接建立后，双方才能可靠传输数据。</p><ol><li>如果是 HTTPS，还要 TLS 握手</li></ol><p>如果访问的是 HTTPS，需要在 TCP 连接之上建立 TLS 加密通道。</p><p>TLS 握手会做：</p><ul><li>协商 TLS 版本</li><li>协商加密套件</li><li>服务器发送证书</li><li>浏览器校验证书</li><li>生成会话密钥</li><li>建立加密通信</li></ul><p>之后 HTTP 数据都会加密传输。</p><ol><li>发送 HTTP 请求</li></ol><p>连接建立后，浏览器发送 HTTP 请求。</p><p>请求内容包括：</p><ul><li>请求行</li><li>请求头</li><li>请求体</li></ul><p>比如：</p><pre><code>GET /index.html HTTP/1.1<br/>Host: www.xxx.com<br/>User-Agent: ...<br/>Cookie: ...<br/>Accept: ...</code></pre><p>如果是 POST，还会带请求体。</p><ol><li>服务器处理请求</li></ol><p>请求到达服务器后，可能经过：</p><pre><code>负载均衡<br/>网关<br/>Nginx<br/>应用服务器<br/>缓存<br/>数据库<br/>微服务</code></pre><p>服务器会处理业务逻辑。</p><p>比如：</p><ul><li>查缓存</li><li>查数据库</li><li>调用服务</li><li>渲染模板</li><li>返回静态资源</li><li>鉴权和校验 Cookie</li></ul><ol><li>返回 HTTP 响应</li></ol><p>服务器返回响应。</p><p>响应包括：</p><ul><li>状态码</li><li>响应头</li><li>响应体</li></ul><p>比如：</p><pre><code>HTTP/1.1 200 OK<br/>Content-Type: text/html<br/>Set-Cookie: ...<br/>Cache-Control: ...<br/><br/>&lt;html&gt;...&lt;/html&gt;</code></pre><p>常见状态码：</p><ul><li>200：成功</li><li>301 / 302：重定向</li><li>304：使用缓存</li><li>404：资源不存在</li><li>500：服务器错误</li></ul><ol><li>浏览器解析 HTML</li></ol><p>浏览器收到 HTML 后开始解析。</p><p>会构建：</p><pre><code>DOM Tree</code></pre><p>解析过程中如果遇到：</p><ul><li>CSS</li><li>JS</li><li>图片</li><li>字体</li><li>视频</li></ul><p>会继续发起请求下载这些资源。</p><ol><li>CSS 和 JS 处理</li></ol><p>浏览器解析 CSS，构建：</p><pre><code>CSSOM Tree</code></pre><p>然后 DOM + CSSOM 合成：</p><pre><code>Render Tree</code></pre><p>JS 可能会阻塞 HTML 解析。</p><p>因为 JS 可能修改 DOM 和 CSS。</p><p>所以脚本加载策略很重要，比如：</p><pre><code>defer<br/>async</code></pre><ol><li>页面渲染</li></ol><p>浏览器渲染流程大概是：</p><pre><code>构建 DOM<br/>构建 CSSOM<br/>合成 Render Tree<br/>Layout 布局<br/>Paint 绘制<br/>Composite 合成</code></pre><p>最终页面显示出来。</p><ol><li>后续连接复用</li></ol><p>如果是 HTTP/1.1，可能使用 keep-alive 复用 TCP 连接。</p><p>HTTP/2 可以在一个 TCP 连接上多路复用多个请求。</p><p>HTTP/3 基于 QUIC / UDP，可以减少连接建立成本。</p><ol><li><strong>面试背诵版：浏览器输入 </strong><a href=\"http://www.xxx.com/\" target=\"_blank\" rel=\"noopener\"><strong>www.xxx.com</strong></a><strong> 后发生了什么？</strong></li></ol><blockquote><p>浏览器输入 <a href=\"http://www.xxx.com/\" target=\"_blank\" rel=\"noopener\">www.xxx.com</a> 后，首先会进行 URL 解析，确定协议、域名、端口、路径和参数，如果没有写协议，浏览器可能会补全 HTTP 或 HTTPS。然后浏览器会检查缓存，包括浏览器缓存、DNS 缓存、Service Worker 缓存和 HSTS 记录。</p><p>如果需要发起网络请求，就会进行 DNS 解析，把域名解析成 IP 地址。DNS 查询可能经过浏览器缓存、hosts 文件、操作系统缓存、本地 DNS、根 DNS、顶级域 DNS 和权威 DNS。拿到 IP 后，如果是 HTTP/1.1 或 HTTP/2，浏览器会通过 TCP 三次握手建立连接；如果是 HTTPS，还会进行 TLS 握手，校验证书、协商加密套件并生成会话密钥。</p><p>连接建立后，浏览器发送 HTTP 请求，服务器经过负载均衡、网关、Nginx、应用服务、缓存、数据库等处理后返回 HTTP 响应。浏览器收到 HTML 后解析生成 DOM Tree，解析 CSS 生成 CSSOM Tree，二者合成 Render Tree，然后经过 Layout、Paint、Composite，最终把页面渲染出来。过程中遇到 CSS、JS、图片、字体等资源，还会继续发起请求。简单说，就是“解析 URL → 查缓存 → DNS → TCP/TLS → HTTP 请求响应 → 浏览器解析渲染”。</p></blockquote>"},{"t":"一个 HTTP 请求从客户端到服务器经历了什么？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"TCP 三次握手之后，数据是怎么传输的？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"为什么访问一个网站第一次很慢，后面变快了？","tag":"","p":"","html":""},{"t":"为什么 TCP 建立连接需要三次握手？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"为什么 TCP 断开连接需要四次挥手？","tag":"面试必考","p":"must","html":"<p>TCP 断开连接需要四次挥手的核心是：TCP 是全双工通信，客户端和服务端两个方向的数据通道需要分别关闭，所以通常需要双方各发一次 FIN、各回一次 ACK。</p><ol><li>TCP 是全双工的</li></ol><p>TCP 连接是全双工的，意思是双方可以同时发送和接收数据。</p><pre><code>客户端  —— 数据 ——&gt;  服务端<br/>客户端  &lt;— 数据 ——   服务端</code></pre><p>所以断开连接时，不能只关一个方向。</p><p>需要分别关闭：</p><pre><code>客户端 → 服务端 这个方向<br/>服务端 → 客户端 这个方向</code></pre><ol><li>FIN 表示什么？</li></ol><p><code>FIN</code> 表示：我这边没有数据要发了，我要关闭发送方向。</p><p>注意，它不是说“整个连接立刻关闭”。</p><p>它只是关闭自己到对方的发送通道。</p><pre><code>客户端发送 FIN：<br/>客户端不再给服务端发数据<br/>但客户端仍然可以接收服务端数据</code></pre><ol><li>第一次挥手</li></ol><p>客户端发送 <code>FIN</code> 给服务端。</p><p>意思是：</p><pre><code>我这边数据发完了，我要关闭客户端 → 服务端方向。</code></pre><p>流程：</p><pre><code>客户端 —— FIN ——&gt; 服务端</code></pre><ol><li>第二次挥手</li></ol><p>服务端收到 <code>FIN</code> 后，先回复 <code>ACK</code>。</p><p>意思是：</p><pre><code>我知道你不发了，但我这边可能还有数据没发完。</code></pre><p>流程：</p><pre><code>客户端 &lt;— ACK —— 服务端</code></pre><p>此时客户端到服务端方向关闭，但服务端到客户端方向还可以继续发数据。</p><ol><li>第三次挥手</li></ol><p>等服务端剩余数据也发完后，服务端再发送 <code>FIN</code>。</p><p>意思是：</p><pre><code>我这边也发完了，我也要关闭服务端 → 客户端方向。</code></pre><p>流程：</p><pre><code>客户端 &lt;— FIN —— 服务端</code></pre><ol><li>第四次挥手</li></ol><p>客户端收到服务端的 <code>FIN</code> 后，回复 <code>ACK</code>。</p><p>意思是：</p><pre><code>我知道你也不发了，连接可以关闭了。</code></pre><p>流程：</p><pre><code>客户端 —— ACK ——&gt; 服务端</code></pre><p>完整流程：</p><pre><code>客户端                         服务端<br/><br/>FIN  ------------------------&gt;<br/>      我不发了<br/><br/>      &lt;------------------------ ACK<br/>      知道了，但我可能还没发完<br/><br/>      &lt;------------------------ FIN<br/>      我也发完了<br/><br/>ACK  ------------------------&gt;<br/>      知道了，关闭</code></pre><ol><li>为什么不能三次？</li></ol><p>因为服务端收到客户端 <code>FIN</code> 时，只能表示“客户端不发了”。</p><p>但服务端可能还有数据要继续发给客户端，所以不能马上把自己的 <code>FIN</code> 和 <code>ACK</code> 一起发出去。</p><p>因此通常要先回 <code>ACK</code>，等数据发完后再发 <code>FIN</code>。</p><pre><code>ACK：确认你关闭<br/>FIN：我也要关闭</code></pre><p>这两个动作语义不同，时间点也可能不同，所以拆成两次。</p><ol><li>有没有可能三次挥手？</li></ol><p>有可能。</p><p>如果服务端收到客户端 <code>FIN</code> 时，自己也刚好没有数据要发了，就可以把 <code>ACK</code> 和 <code>FIN</code> 合并发送。</p><pre><code>客户端 —— FIN ——&gt; 服务端<br/>客户端 &lt;— ACK + FIN —— 服务端<br/>客户端 —— ACK ——&gt; 服务端</code></pre><p>所以 TCP 断开连接通常是四次挥手，但特殊情况下也可能是三次。</p><ol><li><strong>面试背诵版：为什么 TCP 断开连接需要四次挥手？</strong></li></ol><blockquote><p>TCP 断开连接需要四次挥手，主要是因为 TCP 是全双工通信，客户端到服务端、服务端到客户端两个方向的数据通道需要分别关闭。FIN 只表示发送方没有数据要发送了，但仍然可以接收数据。第一次挥手是主动关闭方发送 FIN，表示自己不再发送数据；第二次挥手是被动关闭方回复 ACK，表示已经收到关闭请求，但自己可能还有数据要发送；第三次挥手是被动关闭方数据发送完后再发送 FIN，表示自己也要关闭发送方向；第四次挥手是主动关闭方回复 ACK，连接最终关闭。因为 ACK 和 FIN 的语义不同、发送时机可能不同，所以通常需要四次挥手。如果被动关闭方没有数据要发送，也可能把 ACK 和 FIN 合并，变成三次挥手。</p></blockquote>"},{"t":"为什么大量 TIME_WAIT 会导致端口耗尽？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"为什么大量 CLOSE_WAIT 说明服务端可能有问题？","tag":"面试必考","p":"must","html":"<blockquote><p></p></blockquote>"},{"t":"一个服务 QPS 很高，应该如何设计网络 IO？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"一个 HTTP 服务突然出现大量 502，应该怎么排查？","tag":"","p":"","html":""},{"t":"一个接口突然变慢，从网络层应该怎么排查？","tag":"","p":"","html":""},{"t":"为什么 HTTP/2 还需要 HTTP/3？","tag":"重点","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"为什么 Redis 使用 TCP？","tag":"","p":"","html":""},{"t":"为什么 Kafka 使用 TCP？","tag":"","p":"","html":""},{"t":"为什么 DNS 通常使用 UDP？","tag":"","p":"","html":""},{"t":"为什么 WebSocket 建立连接需要 HTTP？","tag":"","p":"","html":""},{"t":"HTTP、TCP、Socket 三者到底是什么关系？","tag":"面试必考","p":"must","html":"<p>HTTP、TCP、Socket 三者的核心关系是：HTTP 是应用层协议，TCP 是传输层协议，Socket 是程序使用 TCP/UDP 的编程接口。</p><ol><li>先一句话区分</li></ol><pre><code>HTTP：规定应用数据怎么组织<br/>TCP：负责把数据可靠地传过去<br/>Socket：程序用来操作网络连接的接口</code></pre><p>也可以这样理解：</p><pre><code>HTTP 是沟通内容格式<br/>TCP 是运输通道<br/>Socket 是程序打开和使用通道的工具</code></pre><ol><li>HTTP 是什么？</li></ol><p>HTTP 是应用层协议。</p><p>它规定客户端和服务端之间请求和响应的格式。</p><p>比如：</p><pre><code>GET /user/1 HTTP/1.1<br/>Host: example.com</code></pre><p>HTTP 关心的是：</p><pre><code>请求方法是什么<br/>URL 是什么<br/>请求头有哪些<br/>响应状态码是什么<br/>响应体是什么</code></pre><p>但 HTTP 自己不负责把数据可靠送到对方，它需要依赖下层协议，比如 TCP。</p><ol><li>TCP 是什么？</li></ol><p>TCP 是传输层协议。</p><p>它负责在两台主机之间建立可靠连接，并保证数据可靠、有序、不丢失、不重复。</p><p>TCP 关心的是：</p><pre><code>三次握手<br/>四次挥手<br/>序列号<br/>ACK<br/>重传<br/>滑动窗口<br/>拥塞控制<br/>流量控制</code></pre><p>HTTP 请求最终会被交给 TCP，由 TCP 拆分、编号、传输、重组。</p><ol><li>Socket 是什么？</li></ol><p>Socket 不是一种网络协议，而是操作系统提供给程序使用网络的编程接口。</p><p>程序通过 Socket 调用 TCP 或 UDP 能力。</p><p>比如服务端：</p><pre><code>socket()<br/>bind()<br/>listen()<br/>accept()<br/>read()/write()</code></pre><p>客户端：</p><pre><code>socket()<br/>connect()<br/>read()/write()</code></pre><p>你可以理解为：应用程序不是直接操作 TCP 协议栈，而是通过 Socket 这个接口操作。</p><ol><li>三者在网络分层中的位置</li></ol><pre><code>应用层：HTTP<br/>传输层：TCP<br/>网络层：IP<br/>链路层：以太网 / Wi-Fi<br/><br/>编程接口：Socket</code></pre><p>注意：Socket 不属于 OSI 某一层协议，它是应用程序访问传输层的 API。</p><ol><li>浏览器发 HTTP 请求时发生什么？</li></ol><p>比如浏览器访问：</p><pre><code>http://example.com</code></pre><p>大致过程是：</p><pre><code>浏览器生成 HTTP 请求<br/>        ↓<br/>通过 Socket 写入数据<br/>        ↓<br/>TCP 负责可靠传输<br/>        ↓<br/>IP 负责寻址路由<br/>        ↓<br/>网卡发出去</code></pre><p>服务端收到后：</p><pre><code>网卡收到数据<br/>        ↓<br/>IP 层处理<br/>        ↓<br/>TCP 重组成字节流<br/>        ↓<br/>应用通过 Socket 读取<br/>        ↓<br/>Web 服务器解析 HTTP 请求</code></pre><ol><li>一个类比</li></ol><p>可以这样记：</p><pre><code>HTTP：快递单和包裹内容格式<br/>TCP：快递运输服务，保证送达<br/>Socket：你寄快递和收快递的窗口<br/>IP：收件地址和路线选择</code></pre><ol><li>HTTPS 又是什么关系？</li></ol><p>HTTPS 可以理解为：</p><pre><code>HTTPS = HTTP + TLS + TCP</code></pre><p>它是在 HTTP 和 TCP 中间加了 TLS 安全层。</p><pre><code>HTTP<br/>TLS<br/>TCP<br/>IP</code></pre><p>HTTP 数据先经过 TLS 加密，再交给 TCP 传输。</p><ol><li><strong>面试背诵版：HTTP、TCP、Socket 三者到底是什么关系？</strong></li></ol><blockquote><p>HTTP、TCP、Socket 不是同一类东西。HTTP 是应用层协议，规定客户端和服务端之间请求和响应的数据格式，比如方法、URL、请求头、状态码和响应体。TCP 是传输层协议，负责在两台主机之间建立可靠连接，通过三次握手、序列号、ACK、重传、滑动窗口和拥塞控制保证数据可靠有序传输。Socket 不是协议，而是操作系统提供给应用程序使用网络的编程接口，程序通过 socket、connect、read、write 等接口使用 TCP 或 UDP。实际发送 HTTP 请求时，应用先生成 HTTP 报文，通过 Socket 写入连接，由 TCP 负责可靠传输，最终到达服务端后再通过 Socket 读出并解析 HTTP。</p></blockquote><p></p>"}]}]};
