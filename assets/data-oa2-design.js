window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["design什么是原型模式？"] = "<p><strong>核心回答：</strong>原型模式（Prototype）通过<strong>复制已有实例来创建新对象</strong>，而不是 new 从头构建；适用于创建成本高（初始化复杂、查库组装）但与现有对象结构相似的场景。</p><ol><li><strong>核心结构</strong>：原型对象实现 clone 方法（Java 里实现 Cloneable 覆写 Object.clone，或自定义 copy 构造/拷贝工厂）；客户端调 prototype.clone() 拿到新副本再按需微调。</li><li><strong>两种拷贝</strong>：浅拷贝（字段直接复制，引用类型共享）vs 深拷贝（引用对象也递归复制）。</li><li><strong>典型场景</strong>：a) 对象初始化要读库/远程调用（先造一个模板实例缓存，之后 clone）；b) 避免继承体系的爆炸（和工厂模式互补）；c) 需要保存对象快照（撤销/恢复）。</li><li><strong>JDK 中的例子</strong>：Object.clone()、Arrays.copyOf。</li><li><strong>和工厂模式对比</strong>：工厂关注“创建哪种对象”（类选择），原型关注“如何快速得到一个相似对象”（复制既有实例）。</li></ol>";

  A["design浅拷贝和深拷贝有什么区别？"] = "<p><strong>核心回答：</strong>区别在于<strong>引用类型字段是否共享</strong>：浅拷贝只复制基本类型和引用地址（新旧对象共用内层对象），深拷贝递归复制所有层级（完全独立）。</p><ol><li><strong>浅拷贝</strong>：clone 后修改新对象的内层 List，原对象的 List 也变（同一个引用）。Object.clone() 默认就是浅拷贝。优点：快、省内存。</li><li><strong>深拷贝</strong>：内层集合/对象也要 new + 复制（或序列化反序列化 round-trip：JSON/Java 序列化）。优点：完全隔离；缺点：成本高、循环引用要处理。</li><li><strong>怎么选</strong>：对象含可变引用类型且副本会被修改 → 必须深拷贝，否则出现隐蔽的共享状态 bug；全是基本类型/不可变对象（String、Integer）→ 浅拷贝够用。</li><li><strong>陷阱题</strong>：String 是不可变对象，浅拷贝共享 String 是安全的——这是常考点。</li><li><strong>实现深拷贝方式</strong>：手动递归 copy 构造器（推荐，可控）、实现 Cloneable 里逐字段深 clone、序列化反序列化（简单但慢）、MapStruct 等映射工具。</li></ol>";

  A["designJava Cloneable 为什么被认为设计不好？"] = "<p><strong>核心回答：</strong>Cloneable 被诟病因为它<strong>打破了接口语义</strong>：空接口却改变 Object.clone() 行为，机制脆弱、易出错、还破坏 final 语义。</p><ol><li><strong>奇怪的机制</strong>：Cloneable 没有任何方法（marker interface），但 Object.clone() 运行时检查类是否实现它——没实现就抛 CloneNotSupportedException。接口改变父类方法行为，违反直觉。</li><li><strong>clone() 是 protected</strong>：不主动重写为 public 就没法用；重写时 super.clone() 返回类型靠协变，容易漏字段。</li><li><strong>浅拷贝陷阱</strong>：默认浅拷贝，内层可变对象共享，很多 bug 由此而来；要深拷贝必须自己逐字段处理，代码冗长易漏。</li><li><strong>绕过构造器</strong>：clone 不调用构造函数创建对象，和普通对象创建路径不一致，理论上破坏不变式（final 字段在构造器里保证的约束，clone 复制时可能不一致）。</li><li><strong> Effective Java 建议</strong>：用<strong>拷贝构造器（copy constructor）或拷贝工厂</strong>替代：new Foo(other)，类型明确、支持深浅选择、不依赖魔法机制。Spring/阿里规范也倾向不用 Cloneable。</li></ol>";

  A["design什么是桥接模式？"] = "<p><strong>核心回答：</strong>桥接模式（Bridge）把<strong>“抽象层次”和“实现层次”拆成两个独立继承体系</strong>，通过组合（抽象持有实现接口的引用）连接，让两个维度可以独立变化，避免多维度继承导致的类爆炸。</p><ol><li><strong>结构</strong>：Abstraction（抽象类，持有 Implementor 引用）→ RefinedAbstraction（抽象扩展）；Implementor（实现接口）→ ConcreteImplementorA/B。</li><li><strong>经典例子</strong>：消息发送 = 消息类型（普通/加急）× 通道（短信/邮件/Push）。继承实现要 2×3=6 个类；桥接后类型类持有通道接口，各 2+3 个类独立扩展。<pre><code>abstract class Msg { protected Channel ch; abstract void send(String t); }\nclass UrgentMsg extends Msg { void send(String t){ ch.send(“[加急]”+t); } }\ninterface Channel { void send(String t); }\nclass SmsChannel implements Channel { ... }</code></pre></li><li><strong>JDK 例子</strong>：JDBC——DriverManager（抽象）与具体 Driver 实现（mysql/pg 驱动）桥接；SLF4J 与 logback 绑定也是这个思想。</li><li><strong>核心口诀</strong>：<strong>合成复用原则</strong>——能用组合就别用继承；多维变化用桥接拆开。</li></ol>";

  A["design桥接模式解决什么问题？"] = "<p><strong>核心回答：</strong>解决<strong>“多个独立变化的维度用继承组合导致的类爆炸”</strong>问题：n 个维度各有 m 种变化，继承要 mⁿ 个类，桥接只要 Σm 个类，且新增维度值不影响其他代码。</p><ol><li><strong>类爆炸演示</strong>：形状（圆/方）× 渲染方式（OpenGL/Vulkan/DX）= 6 个子类；再加“填充样式”维度变 18 个。每加一个值都要改多处。</li><li><strong>桥接解法</strong>：Shape 持有 Renderer 接口，形状只依赖抽象渲染；新增形状不动渲染体系，新增渲染器不动形状体系——符合开闭原则。</li><li><strong>运行时切换</strong>：实现对象可以注入替换（同一形状对象换渲染器），继承做不到。</li><li><strong>判断信号</strong>：看到“X 类型的 Y 实现”这种二维交叉命名（SmsNotifier、EmailNotifier、SmsService...），就该考虑桥接/策略拆维度。</li><li><strong>和策略的区别</strong>：结构几乎一样（都持有接口引用），桥接强调<strong>两个维度都长期演化</strong>且有抽象层级；策略通常只是替换一种算法实现。</li></ol>";

  A["design什么是组合模式？"] = "<p><strong>核心回答：</strong>组合模式（Composite）用<strong>树形结构统一“单个对象”和“组合对象”</strong>：两者实现同一接口，客户端递归处理整棵树时无需区分叶子节点和容器节点。</p><ol><li><strong>结构</strong>：Component（统一接口，如 render/count）→ Leaf（叶子，无子节点）+ Composite（容器，持有 List&lt;Component&gt;，操作委托给子节点递归）。</li><li><strong>示例</strong>：<pre><code>interface Node { long size(); }\nclass File implements Node { long size(){ return len; } }\nclass Folder implements Node {\n  List&lt;Node&gt; children;\n  long size(){ return children.stream().mapToLong(Node::size).sum(); }\n}</code></pre></li><li><strong>JDK/框架例子</strong>：java.awt.Container/Component、MyBatis 的 MixedSqlNode（各种 SqlNode 组合成 SQL）、前端虚拟 DOM、菜单/菜单项。</li><li><strong>适用信号</strong>：业务天然是树（文件系统、组织架构、UI 嵌套、权限树），且对整体和部分要执行一致操作。</li><li><strong>注意</strong>：透明式（接口含 add/remove，叶子抛异常）vs 安全式（add 只在 Composite），Java 常用安全式；组合模式常和访问者/迭代器搭配遍历。</li></ol>";

  A["design文件系统为什么适合组合模式？"] = "<p><strong>核心回答：</strong>因为文件系统天然是<strong>树形递归结构</strong>：文件（叶子）和文件夹（容器）语义上“部分-整体”统一——算大小、搜索、删除、统计数量等操作对文件夹就是“对子节点递归聚合”，组合模式把这个递归结构直接映射为对象模型。</p><ol><li><strong>统一操作</strong>：size()、delete()、search(name) 在 File 和 Folder 上接口一致；Folder 的实现就是把调用分发给所有 children 再聚合（size 求和、delete 逐个删、search 收集匹配结果）。</li><li><strong>无限层级免费获得</strong>：Folder 里可以再放 Folder，递归天然支持任意深度，客户端代码用一行 tree.size() 处理整棵树，不用写 if-else 判断类型。</li><li><strong>扩展性</strong>：新增节点类型（如快捷方式 Shortcut，size 指向目标）只需实现同一接口，遍历/统计代码零修改。</li><li><strong>如果不用组合</strong>：客户端要 instanceof File/Folder 分别处理，每加一种操作都要写一遍类型分支判断，重复且易漏。</li><li><strong>其他同构场景</strong>：组织架构树（人力成本统计）、评论楼中楼、权限树、UI 组件树——凡是“容器与元素同构”的树都适用。</li></ol>";

  A["designAgent Workflow 为什么适合使用责任链模式？"] = "<p><strong>核心回答：</strong>Agent Workflow 的执行本身就是<strong>一串可插拔的处理节点串联</strong>（意图识别 → 上下文构建 → 检索 → 生成 → 审查 → 后处理），责任链让每个节点只关心自己的输入输出，新增/跳过/重排节点不改主干代码。</p><ol><li><strong>天然契合点</strong>：a) <strong>中间件/拦截器</strong>：日志、脱敏、限流、trace 注入都是“请求流过一串处理器”；b) <strong>流程编排</strong>：LangChain 的 chain、LangGraph 的节点边，本质就是责任链/管道变体；c) <strong>护栏（guardrails）</strong>：输入过滤 → 安全检查 → 输出校验，一层不通过就短路返回。</li><li><strong>代码形态</strong>：<pre><code>interface Step { Ctx handle(Ctx c); }\nclass Pipeline { List&lt;Step&gt; steps; Ctx run(Ctx c){ for(s:steps) c=s.handle(c); return c; } }</code></pre>每步拿到上下文（用户输入、检索结果、历史），处理后传给下一步。</li><li><strong>收益</strong>：节点可独立测试；按场景组装不同链（闲聊链跳过检索）；失败快速短路（输入违规直接拒绝，不浪费 LLM 调用）。</li><li><strong>实例</strong>：Spring AI 的 ChatClient advise 链、LangGraph 的 middleware、各家 Agent 平台的 pre/post processor 都是责任链。</li></ol>";

  A["designAgent Tool 调用为什么适合使用策略模式？"] = "<p><strong>核心回答：</strong>Agent 的工具调用本质是<strong>“运行时按 LLM 决策选择执行哪个能力”</strong>——哪个工具被调用在编译期未知，策略模式（接口统一 + 注册表按名分发）正是运行时算法选择的标配。</p><ol><li><strong>结构映射</strong>：Strategy 接口 = Tool 的 execute(params)；ConcreteStrategy = 天气查询、数据库查询、发邮件等具体工具；Context = ToolRegistry/Dispatcher，按 LLM 输出的 tool name 找到策略执行。</li><li><strong>代码形态</strong>：<pre><code>interface Tool { String name(); String run(JsonObject args); }\nMap&lt;String,Tool&gt; registry; // 注册所有策略\nTool t = registry.get(llmCall.toolName); t.run(llmCall.args);</code></pre></li><li><strong>收益</strong>：新增工具 = 实现接口 + 注册，调度逻辑零修改（开闭原则）；每个工具独立测试；可以按用户/租户注册不同工具子集（权限）。</li><li><strong>与 if-else 对比</strong>：硬编码 switch(toolName) 每加工具改 dispatcher；策略+注册表把“选择”数据化（Map 查找），“实现”对象化（独立类）。</li><li><strong>变体</strong>：配合注解扫描（@Tool 注解自动注册）、SPI 机制加载第三方工具插件。</li></ol>";

  A["designAgent Tool Registry 为什么使用工厂模式？"] = "<p><strong>核心回答：</strong>Tool Registry 要解决<strong>“按名称/描述动态创建或获取工具实例，并屏蔽构造细节”</strong>——这正是工厂模式的职责；同时工厂支持懒加载、单例复用、依赖注入等生命周期管理。</p><ol><li><strong>简单工厂形态</strong>：registry.get(name) 内部按名字从 Map 取或懒创建；调用方不关心工具怎么构造（要不要连 DB、要不要读配置）。</li><li><strong>注册-工厂结合</strong>：启动时扫描注解/配置文件，把工具的构造逻辑（Supplier&lt;Tool&gt;）注册进工厂；运行时按需实例化。Spring 的 ApplicationContext 本身就是超级工厂，Agent 框架常用 Spring bean 作工具来源。</li><li><strong>收益</strong>：a) 解耦：LLM 调度层只依赖 Tool 接口和工厂；b) 生命周期：重工具（向量库连接）单例复用，轻工具每次新建；c) 可配置：按租户/环境返回不同实现（mock 工具用于测试）。</li><li><strong>组合拳</strong>：工厂（创建/获取）+ 策略（执行接口统一）+ 注册表（名称映射）构成 Agent 工具系统的三件套，LangChain 的 tool bind、MCP 的 tool list 都是这个模式。</li></ol>";

  A["designMCP Server 设计中有哪些设计模式？"] = "<p><strong>核心回答：</strong>MCP（Model Context Protocol）Server 的设计中能看到<strong>注册表+工厂（工具管理）、策略（多工具实现）、观察者（订阅通知）、代理（资源转发）、模板方法（请求处理骨架）</strong>等模式。</p><ol><li><strong>注册表/工厂</strong>：tools/list 返回所有工具元数据（name、schema），tools/call 按名分发——ToolRegistry 是典型注册表+工厂。</li><li><strong>策略</strong>：每个工具（读文件、查库、调 API）实现统一的执行接口，具体行为各异；同一能力可有多个实现（MySQL/PG 版本的 query 工具）。</li><li><strong>观察者/订阅</strong>：MCP 的 resources/subscribe 机制——客户端订阅资源变更，服务端变化时推送 notifications/resources/updated，就是观察者模式。</li><li><strong>代理</strong>：MCP Server 常作为外部系统（GitHub、DB、内部服务）的代理：Agent 请求 → Server 转发并适配格式，控制访问权限（只暴露白名单操作）。</li><li><strong>模板方法/中间件</strong>：请求处理骨架固定（鉴权 → 参数校验 → 执行 → 结果格式化），具体工具只填执行步骤；JSON-RPC 层统一处理 id 匹配和错误码。</li><li><strong>面试收口</strong>：MCP 本质是“把 Agent 的能力依赖标准化”，其服务端就是一套“工具即插件”的插件架构，模式组合和操作系统驱动管理类似。</li></ol>";

  A["designAgent Memory 如何使用策略模式设计多种存储？"] = "<p><strong>核心回答：</strong>把<strong>记忆的读写接口抽象成 MemoryStore 策略接口</strong>，短期（会话内）、Redis（跨会话）、向量库（语义检索）、关系库（结构化档案）各做一个实现，Agent 核心只面向接口编程，按配置组合使用。</p><ol><li><strong>接口抽象</strong>：<pre><code>interface MemoryStore {\n  void save(String sessionId, Memory m);\n  List&lt;Memory&gt; recall(String sessionId, MemoryQuery q);\n  void forget(String sessionId, String memId);\n}</code></pre></li><li><strong>多实现（多策略）</strong>：InMemoryStore（会话内快存快取）、RedisStore（跨会话按 key 取最近 N 条）、VectorStore（embedding 相似度召回相关记忆）、GraphStore（实体关系记忆）。</li><li><strong>组合策略</strong>：MemRouter/CompositeStore 按记忆类型路由——事实类进向量库、偏好类进 Redis、流程类进会话上下文；读写时可多级查询合并。</li><li><strong>收益</strong>：换存储不动 Agent 逻辑（本地开发用内存实现、生产用 Redis+向量库）；按租户配置不同策略；每个 store 独立压测。</li><li><strong>配合工厂</strong>：配置文件声明 memory.type=redis+vector，工厂按配置装配组合策略注入 Agent——LangChain 的各 BaseStore、LlamaIndex 的 memory 模块都是这个思路。</li></ol>";

  A["designAgent Planner 如何使用状态模式管理执行状态？"] = "<p><strong>核心回答：</strong>Agent 任务的生命周期是<strong>状态机</strong>（planning → executing → waiting_tool → waiting_approval → reflecting → done/failed），状态模式把每个状态的<strong>允许转移和状态内行为封装成独立状态类</strong>，避免 Planner 主类里堆巨型 switch。</p><ol><li><strong>状态类设计</strong>：<pre><code>interface TaskState { TaskState handle(Event e, Task t); }\nclass PlanningState  { handle(PLAN_DONE){ t.setPlan(); return EXECUTING; } }\nclass ExecutingState { handle(TOOL_REQ){ t.callTool(); return WAITING_TOOL; }\n                       handle(NEED_HUMAN){ return WAITING_APPROVAL; } }\nclass WaitingApprovalState { handle(APPROVED){ return EXECUTING; }\n                             handle(REJECTED){ return FAILED; } }</code></pre></li><li><strong>解决什么</strong>：非法转移集中管控（EXECUTING 状态收到 APPROVED 直接忽略或报错，防状态跳变）；新增状态（如 PAUSED、ESCALATED）加一个类，不碰其他状态逻辑。</li><li><strong>对比 if-else/switch</strong>：状态多、转移复杂后 switch 嵌套爆炸难维护；状态模式每个状态独立可测试，转移表清晰。</li><li><strong>工程配套</strong>：状态持久化（checkpoint 存当前状态，重启恢复）；状态转移发事件（配合观察者模式做日志/通知/审计）；LangGraph 的 StateGraph 节点+条件边就是显式状态机实现。</li></ol>";

  A["designAgent Runtime 为什么需要观察者模式？"] = "<p><strong>核心回答：</strong>Agent Runtime 是<strong>事件驱动的长时运行系统</strong>：任务状态变更、LLM 流式 token、工具开始/结束、错误、心跳——这些事件有多个关注方（UI 实时展示、日志、计费、监控、审批触发），观察者模式让 Runtime 只管发布事件，订阅者各自消费，天然解耦。</p><ol><li><strong>典型事件流</strong>：<pre><code>runtime.emit(Event.TASK_STARTED)\nruntime.emit(Event.LLM_CHUNK, delta)   // 流式输出 → UI 增量渲染\nruntime.emit(Event.TOOL_CALLED, {name,args})\nruntime.emit(Event.WAITING_APPROVAL)    // → 触发审批通知\nruntime.emit(Event.TASK_DONE, result)</code></pre></li><li><strong>订阅者</strong>：SSE/WebSocket 网关（推给用户前端）、Trace 收集器（可观测）、Token 计量（计费/限额熔断）、审计日志、异常告警——新增订阅方零侵入 Runtime。</li><li><strong>异步与背压</strong>：事件走消息队列/disruptor 异步分发，慢订阅者（如日志落盘）不拖慢主流程；订阅方可声明只关心某类事件。</li><li><strong>实现形态</strong>：JDK 的 Listener 接口 + 注册表、EventBus、Reactor Flux、Spring 的 ApplicationEvent；LangGraph 的 stream 模式、Claude Code SDK 的 hooks 都是观察者思想。</li><li><strong>对比轮询</strong>：没有事件机制，UI 只能定时拉任务状态——延迟高、浪费；观察者实现推模式，实时性最好。</li></ol>";

  A["designAgent Runtime 如何使用责任链处理 Middleware？"] = "<p><strong>核心回答：</strong>把 Runtime 的每次核心调用（执行 Agent Loop 一步、调用工具）包成<strong>洋葱圈式中间件链</strong>：请求依次穿过 日志 → 限流 → 鉴权 → 脱敏 → 核心逻辑，响应反向穿出，每层可短路（拒绝请求）或增强（注入上下文）。</p><ol><li><strong>结构</strong>：<pre><code>interface Middleware { Ctx invoke(Ctx c, Next next); }\n// 组装：log.limit.auth.sanitize.core\nclass AuthMW implements Middleware {\n  Ctx invoke(Ctx c, Next n){\n    if(!c.user().canRun()) return c.reject(); // 短路\n    c.setPrincipal(...); return n.proceed(c);  // 放行\n  }\n}</code></pre>Next.proceed 触发下一环，核心逻辑在链尾——这就是责任链的“链式传递+可短路”变体。</li><li><strong>Agent 场景的中间件</strong>：输入护栏（prompt 注入检测）、token 预算检查（超限短路）、工具调用黑白名单、结果脱敏（去掉密钥再返回 LLM）、审计埋点。</li><li><strong>收益</strong>：横切关注点与业务分离，顺序可配置（限流要在鉴权前还是后）；单个中间件独立单测；生产/测试装配不同链（测试去掉限流）。</li><li><strong>成熟参照</strong>：Express/Koa 中间件、Spring MVC Filter、OkHttp Interceptor、LangGraph middleware——同一思想的成熟实现，面试可直接类比。</li></ol>";
})(window.OTHERS_AI);
