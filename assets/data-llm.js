window.TAB_DATA = window.TAB_DATA || {};
window.TAB_DATA["llm"] = {"key":"llm","title":"4小时速通大模型面试","url":"https://utxc8uqzfk.feishu.cn/wiki/QmODwjiIjiqtpokZICWcrEpFnWe","chapters":[{"no":"","title":"1.1 大语言模型基础","questions":[{"t":"什么是大语言模型（LLM）？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>大语言模型，也就是 LLM，核心是：<strong>基于海量文本训练出来的、能够理解和生成自然语言的深度学习模型（必须记住）。</strong></p><ol><li><strong>LLM 是什么？</strong></li></ol><p>LLM 全称是 Large Language Model。</p><p>它是一类语言模型，主要能力是根据上下文预测和生成文本。</p><p>比如你输入：</p><pre><code>今天天气很好，我想去</code></pre><p>模型会根据上下文预测后面可能是：</p><pre><code>公园散步。</code></pre><p>更大的模型通过海量语料训练后，不只是能续写文本，还能表现出：</p><ul><li>问答</li><li>总结</li><li>翻译</li><li>推理</li><li>写代码</li><li>信息抽取</li><li>对话</li><li>规划</li></ul><p>这些能力。</p><ol><li><strong>它为什么叫“大”？</strong></li></ol><p>“大”主要体现在几个方面：</p><ul><li>参数规模大</li><li>训练数据量大</li><li>计算资源大</li><li>支持的任务范围大</li><li>上下文处理能力强</li></ul><p>模型参数可以理解成模型内部学到的“知识和模式”。</p><p>参数越多，不一定绝对越聪明，但通常表达能力更强。</p><ol><li><strong>LLM 怎么工作？</strong></li></ol><p>可以简单理解为：</p><pre><code>输入文本 → 分词 Tokenize → 模型计算上下文关系 → 预测下一个 token → 不断生成</code></pre><p>LLM 不是像数据库一样直接查答案，而是根据训练中学到的语言规律、知识关联和上下文模式生成最可能的输出。</p><p>比如：</p><pre><code>中国的首都是</code></pre><p>模型预测下一个 token 大概率是：</p><pre><code>北京</code></pre><ol><li><strong>LLM 的核心技术</strong></li></ol><p>现代 LLM 大多基于 Transformer 架构。</p><p>Transformer 的关键能力是注意力机制，也就是模型可以判断：</p><pre><code>当前词应该重点关注上下文里的哪些词</code></pre><p>这让它能处理长文本关系、理解上下文，并生成连贯内容。</p><ol><li><strong>LLM 能做什么？</strong></li></ol><p>常见能力包括：</p><ul><li>文本生成</li><li>问答</li><li>总结</li><li>翻译</li><li>分类</li><li>信息抽取</li><li>代码生成</li><li>多轮对话</li><li>推理分析</li><li>工具调用</li><li>Agent 决策</li></ul><p>所以它既可以做聊天机器人，也可以作为 Agent 的“大脑”。</p><ol><li>LLM 有什么局限？</li></ol><p>LLM 也有明显局限：</p><ul><li>可能产生幻觉</li><li><strong>可能答错但很自信（最过分的。。。）</strong></li><li><strong>对实时信息不一定知道</strong></li><li>对数学和复杂逻辑不一定稳定</li><li>对输入很敏感</li><li>可能受 Prompt Injection 影响</li><li>本身不能直接操作外部系统</li></ul><p>所以实际应用里通常要结合 RAG、工具调用、权限控制和人工审核。</p><ol><li><strong>面试背诵版：什么是大语言模型（LLM）？</strong></li></ol><blockquote><p>大语言模型，也就是 LLM，是一种基于海量文本数据训练出来的深度学习模型，主要用于理解和生成自然语言。它的基本工作方式是把输入文本切成 token，利用模型参数和上下文关系<strong>预测下一个 token</strong>，并不断生成完整回答。</p><p>它之所以叫“大”，是因为参数规模、训练数据量、计算资源和任务覆盖范围都很大。现代 LLM 通常基于 Transformer 架构，利用注意力机制理解上下文关系，因此可以完成问答、总结、翻译、分类、信息抽取、代码生成、多轮对话和推理等任务。</p><p><strong>但 LLM 不是数据库</strong>，<strong>也不是绝对可靠的知识系统，它可能产生幻觉、答错实时信息或误解上下文（</strong><strong>这个很重要，千万不要太信任大模型了，我在工作过程中已经遇到很多次claude code说已经完成推送代码，结果根本没推送的情况</strong><strong>）</strong>。因此在实际系统中，通常会结合 <strong>RAG、工具调用、权限控制和人工审核</strong>来提升准确性和安全性。</p></blockquote>"},{"t":"大语言模型相比传统NLP模型有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>大语言模型和传统 NLP 模型的核心区别是：<strong>传统 NLP 模型通常是“一个模型解决一个任务”，大语言模型更像“一个通用模型通过提示词解决多种任务”。</strong></p><ol><li><strong>任务方式不同</strong></li></ol><p>传统 NLP 模型一般针对具体任务训练。</p><p>比如：</p><ul><li>情感分类模型</li><li>命名实体识别模型</li><li>机器翻译模型</li><li>文本分类模型</li><li>分词模型</li><li>关键词抽取模型</li></ul><p>一个任务通常训练一个模型。</p><p><strong>LLM 更偏通用模型。</strong></p><p>同一个模型可以通过不同 Prompt 完成：</p><ul><li>问答</li><li>总结</li><li>翻译</li><li>分类</li><li>信息抽取</li><li>写代码</li><li>对话</li><li>推理</li></ul><p>比如你只要换提示词：</p><pre><code>请总结这段话<br/>请翻译这段话<br/>请判断情感倾向</code></pre><p>同一个 LLM 就能完成不同任务。</p><ol><li>训练方式不同</li></ol><p><strong>传统 NLP 模型多是监督学习。</strong></p><p>通常需要：</p><pre><code>大量标注数据 → 针对某个任务训练模型</code></pre><p>比如做情感分类，要准备很多“文本 + 情感标签”。</p><p>LLM 通常先做大规模预训练。</p><p>流程大概是：</p><pre><code>海量文本预训练 → 指令微调 → 人类反馈对齐</code></pre><p>它先学通用语言规律和知识，再通过指令对齐学会按照用户要求回答。</p><ol><li><strong>输入输出形式不同</strong></li></ol><p>传统 NLP 模型输出通常比较固定。</p><p>比如：</p><pre><code>分类：正面 / 负面<br/>NER：人名 / 地名 / 机构名<br/>分词：词列表</code></pre><p>LLM 的输入输出更灵活。</p><p>输入可以是自然语言指令，输出可以是：</p><ul><li>一段解释</li><li>一段代码</li><li>一个 JSON</li><li>一个分析过程</li><li>一个总结</li><li>一个对话回复</li></ul><p>灵活性更强。</p><ol><li><strong>泛化能力不同</strong></li></ol><p>传统 NLP 模型对训练任务效果好，但换任务通常要重新训练。</p><p>LLM 具备更强的泛化能力。</p><p>它可以通过：</p><ul><li>zero-shot</li><li>few-shot</li><li>prompt engineering</li><li>in-context learning</li></ul><p>在没有专门训练的情况下完成新任务。</p><p>比如给它几个示例，它就能模仿格式继续处理。</p><ol><li><strong>上下文理解能力不同</strong></li></ol><p>传统 NLP 模型通常上下文窗口较短，很多模型只处理句子级或段落级任务。</p><p>LLM 可以处理更长上下文，理解多轮对话、长文档、复杂指令和跨段落关系。</p><p>所以它更适合：</p><ul><li>长文总结</li><li>多轮问答</li><li>复杂推理</li><li>文档分析</li><li>Agent 任务规划</li></ul><ol><li>应用方式不同</li></ol><p>传统 NLP 更像一个功能模块。</p><p>比如：</p><pre><code>输入一句话 → 输出情感标签</code></pre><p>LLM 更像一个通用语言接口。</p><p>它可以作为：</p><ul><li>聊天助手</li><li>知识问答系统</li><li>RAG 生成器</li><li>代码助手</li><li>Agent 大脑</li><li>自动化流程决策器</li></ul><p>所以 LLM 更容易接入复杂应用。</p><ol><li><strong>成本和风险不同</strong></li></ol><p>LLM 能力更强，但成本和风险也更高。</p><p>它的问题包括：</p><ul><li>推理成本高</li><li>延迟更高</li><li>输出不稳定</li><li>可能幻觉</li><li>难解释</li><li>难完全可控</li><li>存在安全风险</li></ul><p>传统 NLP 模型虽然能力窄，但在固定任务上：</p><ul><li>成本低</li><li>延迟低</li><li>输出稳定</li><li>更容易评估</li><li>更容易部署</li></ul><ol><li><strong>面试背诵版：大语言模型相比传统 NLP 模型有什么区别？</strong></li></ol><blockquote><p>大语言模型和传统 NLP 模型最大的区别是，<strong>传统 NLP 模型通常是针对某个具体任务训练的，比如情感分类、命名实体识别、机器翻译或文本分类，</strong><strong>一个任务往往对应一个专门模型</strong><strong>；而大语言模型</strong><strong>是通用模型</strong><strong>，可以通过不同 Prompt 完成问答、总结、翻译、分类、抽取、代码生成和多轮对话等多种任务。</strong></p><p>训练方式上，传统 NLP 更多依赖特定任务的标注数据进行监督学习，而大语言模型通常先在<strong>海量文本上</strong>进行预训练，再通过指令微调和人类反馈对齐，让模型学会按自然语言指令完成任务。使用方式上，传统 NLP 输入输出比较固定，LLM 则可以接受自然语言指令，并生成灵活的文本、代码或结构化结果。</p><p>简单说，传统 NLP 更像“专用工具”，在固定任务上稳定、便宜、可控；<strong>大语言模型更像“通用语言智能接口”，泛化能力强、任务覆盖广，但成本更高，也更容易出现幻觉和不稳定输出。</strong></p></blockquote>"},{"t":"GPT类模型和传统语言模型有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>GPT 类模型和传统语言模型的核心区别是：<strong>传统语言模型主要做概率预测，GPT 类模型基于大规模 Transformer 预训练，具备更强的上下文理解、生成和泛化能力。</strong></p><ol><li>传统语言模型是什么？</li></ol><p>传统语言模型主要解决一个问题：</p><pre><code>给定前面的词，预测下一个词出现的概率。</code></pre><p>比如：</p><pre><code>我今天去学校上</code></pre><p>模型预测下一个词可能是：</p><pre><code>课</code></pre><p>早期传统语言模型包括：</p><ul><li>N-gram 语言模型</li><li>HMM</li><li>RNN 语言模型</li><li>LSTM 语言模型</li></ul><p>它们更多依赖统计规律或较小规模神经网络。</p><ol><li>GPT 类模型是什么？</li></ol><p>GPT 全称是 Generative Pre-trained Transformer。</p><p>它的特点是：</p><ul><li>Generative：生成式模型</li><li>Pre-trained：先在海量文本上预训练</li><li>Transformer：基于 Transformer 架构</li></ul><p>GPT 类模型也是预测下一个 token，但它的规模、训练数据、上下文建模能力和泛化能力远强于传统语言模型。</p><ol><li>架构不同</li></ol><p>传统语言模型常见架构：</p><ul><li>N-gram：基于固定窗口统计</li><li>RNN / LSTM：按顺序处理文本</li></ul><p>GPT 类模型基于 Transformer Decoder。</p><p>Transformer 的注意力机制可以让模型在生成时关注上下文中重要的信息。</p><p>相比 RNN / LSTM，它更适合：</p><ul><li>并行训练</li><li>建模长距离依赖</li><li>扩大模型规模</li><li>处理复杂上下文</li></ul><ol><li><strong>上下文能力不同</strong></li></ol><p>传统 N-gram 只能看固定长度的词窗口。</p><p>比如只看前 2 个或 3 个词。</p><p>RNN / LSTM 理论上能看更长，但长距离信息容易衰减。</p><p>GPT 类模型通过 Self-Attention 可以更好建模长上下文关系。</p><p>所以它更擅长：</p><ul><li>多轮对话</li><li>长文理解</li><li>代码生成</li><li>推理分析</li><li>文档总结</li></ul><ol><li><strong>训练规模不同</strong></li></ol><p>传统语言模型通常数据和参数规模较小。</p><p>GPT 类模型则是大规模预训练：</p><ul><li>海量文本数据</li><li>大量参数</li><li>大算力训练</li><li>大上下文窗口</li><li>多阶段对齐训练</li></ul><p>所以 GPT 不只是学语言概率，还学到了大量知识、表达模式和任务模式。</p><ol><li><strong>使用方式不同</strong></li></ol><p>传统语言模型常用于：</p><ul><li>语音识别纠错</li><li>输入法联想</li><li>机器翻译子模块</li><li>文本概率评分</li></ul><p>GPT 类模型可以直接通过 Prompt 完成任务：</p><ul><li>问答</li><li>总结</li><li>翻译</li><li>分类</li><li>写代码</li><li>规划</li><li>对话</li><li>工具调用</li></ul><p>也就是说，GPT 从“语言概率模型”变成了“通用任务模型”。</p><ol><li><strong>泛化能力不同</strong></li></ol><p>传统模型通常需要针对任务单独训练或设计特征。</p><p>GPT 类模型可以通过：</p><ul><li>Prompt</li><li>zero-shot</li><li>few-shot</li><li>in-context learning</li><li>指令微调</li></ul><p>完成很多没有专门训练过的任务。</p><p>这是它和传统语言模型非常重要的区别。</p><ol><li><strong>局限也不同</strong></li></ol><p>GPT 类模型虽然强，但也有问题：</p><ul><li>成本高</li><li>延迟高</li><li>可能幻觉</li><li>输出不稳定</li><li>可解释性差</li><li>对 Prompt 敏感</li><li>需要安全对齐</li></ul><p>传统语言模型虽然能力弱，但更轻量、可控、便宜。</p><ol><li><strong>面试背诵版：GPT 类模型和传统语言模型有什么区别？</strong></li></ol><blockquote><p>GPT 类模型和传统语言模型本质上都在做“<strong>根据上下文预测下一个 token</strong>”，但区别在于模型架构、训练规模和泛化能力。传统语言模型比如 N-gram、HMM、RNN、LSTM，通常依赖固定窗口统计或顺序建模，能处理的上下文有限，主要用于文本概率估计、输入法联想、语音识别纠错等场景。</p><p>GPT 类模型是基于 Transformer Decoder 的生成式预训练模型，通过海量文本、大参数规模和大算力进行预训练，再经过指令微调和对齐，使它具备更强的上下文理解、文本生成和任务泛化能力。它不仅能续写文本，还能通过 Prompt 完成问答、总结、翻译、分类、代码生成、多轮对话和推理等任务。</p><p>简单说，传统语言模型更像“预测下一个词的统计模型”<strong>，而 GPT 类模型是在大规模预训练基础上形成的“通用生成式语言模型”</strong>，能力更强、更灵活，但成本更高，也更容易出现幻觉和不稳定输出。</p></blockquote>"},{"t":"为什么现在的大模型采用Transformer架构？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>现在的大模型采用 Transformer 架构，核心原因是：<strong>Transformer 更适合大规模并行训练、建模长距离依赖，并且在模型规模扩大后效果提升明显。</strong></p><ol><li>传统 RNN / LSTM 有什么问题？</li></ol><p>早期 NLP 常用 RNN、LSTM。</p><p>它们按顺序处理文本：</p><pre><code>第 1 个词 → 第 2 个词 → 第 3 个词 → ...</code></pre><p>问题是：</p><ul><li>不能很好并行训练</li><li>长文本信息容易衰减</li><li>训练速度慢</li><li>扩展到超大模型比较困难</li><li>长距离依赖建模能力有限</li></ul><p>比如一句话很长，前面的关键信息传到后面时可能已经弱化了。</p><ol><li><strong>Transformer 支持并行计算</strong></li></ol><p>Transformer 不需要像 RNN 那样一个词一个词顺序处理。</p><p>它可以一次性处理整个序列。</p><p>这非常适合 GPU / TPU 并行计算。</p><p>对于大模型来说，训练数据和参数规模都很大，如果不能并行，训练成本会非常高。</p><p>所以并行能力是 Transformer 成为大模型主流架构的重要原因。</p><ol><li><strong>Self-Attention 能建模长距离依赖</strong></li></ol><p>Transformer 的核心是 Self-Attention。</p><p>它可以让每个 token 关注上下文中其他 token。</p><p>比如句子：</p><pre><code>小明把书放进书包，因为它太重了。</code></pre><p>模型需要判断“它”指什么。</p><p>Attention 可以帮助模型关注“书”或者“书包”等相关词，建立上下文关系。</p><p>相比 RNN 按顺序传递信息，Self-Attention 更直接。</p><ol><li><strong>扩展性强</strong></li></ol><p>Transformer 有很好的可扩展性。</p><p>当你增加：</p><ul><li>参数规模</li><li>数据规模</li><li>训练算力</li><li>上下文长度</li></ul><p>模型性能通常还能继续提升。</p><p>这就是所谓的 Scaling Law 背后的工程基础之一。</p><p>简单说：</p><pre><code>模型变大、数据变多、算力变强时，Transformer 能比较稳定地吃下这些资源。</code></pre><ol><li><strong>适合预训练范式</strong></li></ol><p>现在的大模型通常采用：</p><pre><code>海量文本预训练 → 指令微调 → 对齐优化</code></pre><p>Transformer 很适合这种预训练方式。</p><p>它可以在大规模语料上学习：</p><ul><li>语言规律</li><li>世界知识</li><li>语义关系</li><li>推理模式</li><li>代码结构</li><li>多任务能力</li></ul><p>然后再通过 Prompt 和微调适配不同任务。</p><ol><li>表达能力强</li></ol><p>Transformer 的多层 Attention 和前馈网络，可以学习复杂模式。</p><p>比如：</p><ul><li>词和词之间关系</li><li>句子结构</li><li>文档上下文</li><li>代码依赖</li><li>多轮对话</li><li>推理链路</li></ul><p>所以它既能做自然语言任务，也能处理代码、多模态等扩展任务。</p><ol><li>工程生态成熟</li></ol><p>Transformer 现在已经形成成熟生态。</p><p>包括：</p><ul><li>训练框架成熟</li><li>分布式训练成熟</li><li>GPU 加速成熟</li><li>推理优化成熟</li><li>量化和蒸馏成熟</li><li>开源模型和工具链丰富</li></ul><p>这也进一步推动了它成为主流架构。</p><ol><li><strong>面试背诵版：为什么现在的大模型采用 Transformer 架构？</strong></li></ol><blockquote><p>现在的大模型普遍采用 Transformer 架构，主要是因为它相比 RNN、LSTM 更适合大规模训练和长上下文建模。RNN 类模型按顺序处理文本，难以并行，训练速度慢，而且长距离依赖容易衰减；而 <strong>Transformer 通过 Self-Attention 机制可以让序列中的 token 直接关注其他位置的 token，更容易捕捉长距离语义关系。</strong></p><p>同时，Transformer 可以并行处理整个序列，非常适合 GPU / TPU 的大规模并行计算；它的扩展性也很好，随着参数规模、训练数据和算力增加，模型能力通常可以持续提升，因此很适合大模型的预训练范式。</p><p>简单说，Transformer 的优势是并行训练效率高、长距离依赖建模能力强、可扩展性好、适合海量数据预训练，并且工程生态成熟，所以成为当前大语言模型的主流架构。</p></blockquote>"},{"t":"大模型为什么具有涌现能力（Emergent Ability）重点？","tag":"","p":"","html":"<p>大模型为什么具有涌现能力？</p><p>大模型的涌现能力，核心是：当模型规模、数据量和计算量达到一定程度后，模型在某些复杂任务上突然表现出小模型不具备的能力。</p><ol><li>什么是涌现能力？</li></ol><p>涌现能力指的是：</p><pre><code>模型变大到一定程度后，突然出现的新能力。</code></pre><p>这些能力在小模型上表现很弱，甚至几乎没有；但模型规模超过某个阈值后，效果明显提升。</p><p>比如：</p><ul><li>多步推理</li><li>复杂问答</li><li>代码生成</li><li>数学题求解</li><li>指令理解</li><li>Few-shot 学习</li><li>工具使用</li><li>多语言迁移</li></ul><p>可以理解为：</p><pre><code>量变积累到一定程度，出现质变。</code></pre><ol><li>为什么模型变大会出现新能力？</li></ol><p>因为模型参数更多，表达能力更强。</p><p>小模型容量有限，只能学到简单模式。</p><p>大模型可以同时学习：</p><ul><li>词法规律</li><li>语法结构</li><li>语义关系</li><li>世界知识</li><li>推理模式</li><li>代码结构</li><li>任务格式</li><li>人类表达习惯</li></ul><p>当这些模式组合起来，就可能表现出复杂能力。</p><ol><li>数据规模带来更多任务模式</li></ol><p>大模型训练数据非常大。</p><p>里面包含大量：</p><ul><li>问答</li><li>代码</li><li>论文</li><li>教程</li><li>对话</li><li>数学推导</li><li>说明文档</li><li>多语言文本</li><li>工具使用痕迹</li></ul><p>模型在预训练中见过很多任务形式。</p><p>当数据规模足够大时，模型不只是学语言，还学到了很多“任务解决模式”。</p><ol><li>In-context Learning 能力增强</li></ol><p>涌现能力里很重要的一类是上下文学习。</p><p>也就是模型看到几个例子后，就能模仿完成任务。</p><p>比如：</p><pre><code>输入：苹果 → fruit<br/>输入：狗 → animal<br/>输入：汽车 →</code></pre><p>模型能推断应该输出：</p><pre><code>vehicle</code></pre><p>小模型可能学不会这种临时规律，大模型更容易从上下文中抽象模式。</p><ol><li><strong>多层表示组合产生复杂能力</strong></li></ol><p>大模型有很多层。</p><p>低层可能学习词语和语法，中间层学习语义关系，高层学习抽象任务和推理模式。</p><p>这些表示组合后，就可能支持更复杂能力。</p><p><strong>比如代码生成不仅需要语言能力，还需要：</strong></p><ul><li><strong>语法理解</strong></li><li><strong>变量关系</strong></li><li><strong>API 知识</strong></li><li><strong>逻辑结构</strong></li><li><strong>错误处理</strong></li><li><strong>上下文一致性</strong></li></ul><p>多个能力叠加，就表现得像“突然会了”。</p><ol><li><strong>指令微调和对齐会放大能力</strong></li></ol><p>有些涌现能力不完全来自预训练，还和后续训练有关。</p><p>比如：</p><ul><li>指令微调</li><li>RLHF / RLAIF</li><li>人类偏好对齐</li><li>高质量问答数据</li></ul><p>这些训练让模型更会：</p><ul><li>理解用户意图</li><li>按步骤回答</li><li>遵守格式</li><li>解释推理</li><li>拒绝危险请求</li></ul><p>所以涌现能力往往是预训练规模 + 指令对齐共同作用的结果。</p><ol><li>注意：涌现能力也有争议</li></ol><p>面试里可以补一句。</p><p>有研究认为，有些“涌现”看起来突然，是因为评估指标是非线性的。</p><p>比如准确率低于某个阈值时看起来像不会，一旦超过阈值就像突然会了。</p><p>所以涌现能力不一定都是神秘现象，也可能和评估方式有关。</p><p>但从工程角度看，确实能观察到：</p><pre><code>模型规模变大后，复杂任务能力明显增强。</code></pre><ol><li><strong>面试背诵版：大模型为什么具有涌现能力？</strong></li></ol><blockquote><p>大模型的涌现能力指的是，<strong>当模型参数规模、训练数据和计算量达到一定程度后，在某些复杂任务上突然表现出小模型不具备或表现很弱的能力，比如多步推理、Few-shot 学习、代码生成、复杂问答和指令理解。</strong></p><p>原因主要有几个方面：</p><ul><li>第一，<strong>模型参数更多</strong>，表达能力更强，可以学习更复杂的语言、语义、知识和任务模式；</li><li>第二，<strong>训练数据规模更大</strong>，包含大量问答、代码、推理、教程和多语言内容，使模型学到丰富的问题解决模式；</li><li>第三，<strong>Transformer 的多层表示可以把词法、语法、语义和高层任务模式组合起来</strong>，形成复杂能力；</li><li>第四，<strong>指令微调和人类反馈对齐</strong>会进一步激发和放大这些能力，让模型更会按照用户意图完成任务。</li></ul><p>不过涌现能力也不是完全神秘的，有些突然提升可能和评估指标、任务阈值有关。简单说，涌现能力可以理解为模型规模、数据规模和训练方式共同作用下，量变积累到一定程度后表现出的复杂任务泛化能力。</p></blockquote>"},{"t":"大模型为什么会产生幻觉（Hallucination）重点？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>大模型产生幻觉的核心原因是<strong>：它的目标是根据上下文生成“概率上合理”的内容，而不是像数据库一样保证每句话都真实。</strong></p><ol><li>训练目标决定了它会“生成最像答案的答案”</li></ol><p>大模型预训练的核心目标通常是预测下一个 token。</p><p>也就是：</p><pre><code>给定前文 → 预测最可能的下一个 token</code></pre><p>它学到的是语言模式、知识关联和表达规律。</p><p>但这个目标并不等价于：</p><pre><code>验证事实是否真实</code></pre><p>所以当模型不知道答案时，它也可能生成一个看起来很合理、语气很自信的回答。</p><p>这是幻觉最根本的原因。</p><ol><li>模型不是数据库</li></ol><p><strong>LLM 的知识存在参数里，是一种压缩后的统计表示。</strong></p><p><strong>它不像数据库：</strong></p><pre><code>精确存储 → 精确查询 → 返回原始事实</code></pre><p>而更像：</p><pre><code>根据训练中学到的模式 → 生成可能的答案</code></pre><p>所以它可能把相似信息混在一起。</p><p>比如：</p><ul><li>人名混淆</li><li>论文标题编造</li><li>API 参数写错</li><li>时间地点混乱</li><li>引用不存在的资料</li></ul><ol><li>训练数据本身不完美</li></ol><p><strong>模型训练数据来自大量文本，其中可能有：</strong></p><ul><li><strong>错误信息</strong></li><li><strong>过时信息</strong></li><li><strong>谣言</strong></li><li><strong>重复内容</strong></li><li><strong>低质量内容</strong></li><li><strong>互相矛盾的信息</strong></li><li><strong>缺少权威来源的数据</strong></li></ul><p><strong>模型学到的知识不一定都是正确的。</strong></p><p>如果训练数据里本来就有噪声，模型就可能继承或放大这些错误。</p><ol><li>缺少实时信息</li></ol><p>模型训练完成后，参数里的知识基本固定。</p><p>如果问题涉及：</p><ul><li>最新新闻</li><li>最新价格</li><li>最新政策</li><li>最新版本 API</li><li>当前公司信息</li><li>实时库存</li><li>实时天气</li></ul><p>模型可能不知道最新情况。</p><p>但它又会尝试生成答案，于是容易幻觉。</p><ol><li>上下文不足或问题模糊</li></ol><p>如果用户问题缺少关键信息，模型可能会自动补全。</p><p>比如用户问：</p><pre><code>那个接口为什么报错？</code></pre><p>如果没有接口名、日志、代码和环境信息，模型只能猜。</p><p>它猜出来的解释可能听起来合理，但未必真实。</p><ol><li><strong>推理链路可能出错</strong></li></ol><p>即使模型知道部分事实，也可能在组合和推理时出错。</p><p>比如：</p><ul><li>数学计算错</li><li>多步逻辑中间一步错</li><li>条件理解错</li><li>因果关系判断错</li><li>把相关性当因果</li><li>引用工具结果时误读</li></ul><p>所以幻觉不只是“知识不知道”，也可能是“推理过程错”。</p><ol><li><strong>对齐训练会让回答更像人，但不一定更真实</strong></li></ol><p>指令微调和 RLHF 会让模型更会回答用户问题。</p><p>但副作用是：</p><ul><li>回答更流畅</li><li>语气更确定</li><li>更像专家</li><li>更愿意给完整答案</li></ul><p>如果缺少“不知道就说不知道”的约束，模型可能会把不确定内容包装得很自信。</p><ol><li><strong>如何减少幻觉？（重点！必考你～）</strong></li></ol><blockquote><p>常见方法：</p><ul><li>RAG 检索权威资料</li><li>工具调用查实时数据</li><li>引用来源</li><li>让模型先说明不确定性</li><li>对关键事实做校验</li><li>使用结构化输出</li><li>降低不必要的自由生成</li><li>高风险场景人工审核</li><li>限制模型只基于给定上下文回答</li></ul></blockquote><p>核心是：</p><pre><code>不要只依赖模型参数里的记忆，要让它基于可验证信息回答。</code></pre><ol><li><strong>面试背诵版：大模型为什么会产生幻觉？</strong></li></ol><blockquote><p>大模型产生幻觉的根本原因是，它的训练目标主要是根据上下文预测下一个 token，<strong>生成概率上最合理的文本</strong>，而不是像数据库一样检索并验证真实事实。模型学到的是语言模式、知识关联和统计规律，所以当它不知道答案、上下文不足或问题模糊时，也可能生成一个看起来合理但实际错误的回答。</p><p>另外，模型参数中的知识是对训练数据的压缩表示，训练数据本身可能包含错误、过时、矛盾或低质量信息；模型训练完成后也缺少实时信息，所以面对最新政策、版本、价格、新闻等问题时容易编造。即使事实部分正确，模型在多步推理、数学计算、因果判断或工具结果理解上也可能出错。</p><p>指令微调和对齐训练会让模型回答更自然、更像专家，但不一定保证事实正确。减少幻觉通常要结合 RAG、工具调用、事实校验、引用来源、结构化输出和人工审核。简单说，幻觉来自“生成式预测目标”和“事实验证能力不足”之间的矛盾。</p></blockquote>"},{"t":"大模型的Scaling Law是什么？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>Scaling Law 核心是：<strong>在一定范围内，模型参数、训练数据和计算量越大，模型性能通常会按照可预测的规律提升。</strong></p><ol><li>Scaling Law 是什么？</li></ol><p>Scaling Law 可以理解为大模型里的“规模规律”。</p><p>它描述的是：</p><pre><code>模型规模变大<br/>训练数据变多<br/>计算量增加<br/>→ 模型损失下降，能力提升</code></pre><p>也就是说，大模型能力提升不是完全随机的，而是和规模之间存在比较稳定的关系。</p><ol><li>Scale 指哪些东西？</li></ol><p>这里的 Scale 主要包括三类：</p><ul><li>模型参数量</li><li>训练数据量</li><li>训练计算量</li></ul><p>也可以理解成：</p><pre><code>更大的模型 + 更多的数据 + 更多的算力</code></pre><p>这三者共同影响模型效果。</p><ol><li>它说明了什么？</li></ol><p>Scaling Law 说明，在没有达到瓶颈之前，继续扩大规模通常能带来收益。</p><p>比如：</p><ul><li>参数更多，表达能力更强</li><li>数据更多，覆盖知识和模式更多</li><li>算力更多，训练更充分</li></ul><p>所以早期大模型发展中，很多能力提升来自“把模型做大、数据做多、训练更久”。</p><ol><li>不是只增大参数就行</li></ol><p>Scaling Law 不是简单说：</p><pre><code>参数越大越好</code></pre><p>模型参数、数据量和计算量要匹配。</p><p>如果模型很大，但数据不够，会过拟合或训练不充分。</p><p>如果数据很多，但模型太小，模型容量不够。</p><p>如果参数和数据都有，但算力不够，也训练不好。</p><p>所以要平衡：</p><pre><code>模型规模、数据规模、计算预算</code></pre><ol><li>Chinchilla Scaling Law</li></ol><p>面试可以提一下 Chinchilla。</p><p>它的核心观点是：</p><pre><code>在相同计算预算下，不一定要盲目加大模型参数，<br/>更合理的做法是让模型参数量和训练 token 数匹配。</code></pre><p>也就是说，很多早期大模型可能“参数很大，但训练 token 不够”。</p><p>Chinchilla 认为，在固定算力下，较小但训练数据更多的模型，可能比更大但训练不充分的模型效果更好。</p><ol><li>Scaling Law 的意义</li></ol><p>它对大模型发展很重要。</p><p>主要意义是：</p><ul><li>指导模型规模设计</li><li>预测训练效果</li><li>估算训练成本</li><li>决定参数量和数据量配比</li><li>指导算力投入</li><li>解释为什么大模型能力会随规模提升</li></ul><p>企业训练模型时，可以根据 Scaling Law 预估：</p><pre><code>投入多少算力，大概能得到什么效果</code></pre><ol><li>Scaling Law 的局限</li></ol><p>Scaling Law 不是万能的。</p><p>它也有局限：</p><ul><li>到一定规模后收益递减</li><li>数据质量比数据数量更重要</li><li>架构、训练方法、对齐也很关键</li><li>不同任务提升速度不同</li><li>推理、数学、安全能力不一定单靠规模解决</li><li>成本会急剧上升</li></ul><p>所以现在不只是拼规模，也强调：</p><ul><li>高质量数据</li><li>更好架构</li><li>工具调用</li><li>RAG</li><li>推理增强</li><li>对齐优化</li><li>推理时计算</li></ul><ol><li><strong>面试背诵版：大模型的 Scaling Law 是什么？</strong></li></ol><blockquote><p>大模型的 Scaling Law 指的是，在一定范围内，模型性能会随着模型参数量、训练数据量和计算量的增加而按照相对可预测的规律提升。简单说，就是模型更大、数据更多、训练计算量更高时，训练损失通常会下降，模型能力会增强。</p><p><strong>但 Scaling Law 不是简单地说参数越大越好，而是强调参数规模、训练 token 数和计算预算要匹配。</strong>比如 Chinchilla Scaling Law 就指出，在固定计算预算下，模型不应该只盲目增大参数，还要保证有足够多的训练 token，否则大模型可能训练不充分；有时候较小但训练数据更多的模型，反而比更大但数据不足的模型效果更好。</p><p>Scaling Law 的意义在于帮助预测模型效果、规划模型规模、分配数据和算力预算。但它也有局限，比如收益递减、数据质量影响很大，推理、安全、数学等能力不一定只靠扩大规模解决。所以现在大模型发展不仅靠 scale，也依赖高质量数据、架构优化、对齐、RAG、工具调用和推理增强。</p></blockquote>"},{"t":"模型参数量、训练数据量、计算量之间有什么关系？","tag":"重点","p":"core","html":"<p></p><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>核心关系是：<strong>模型参数量、训练数据量和计算量要匹配；参数太大但数据不够会训练不充分，数据很多但模型太小会容量不足，计算量决定能不能把两者训练好。</strong></p><ol><li>三者分别是什么？</li></ol><p>模型参数量：</p><pre><code>模型内部可学习的权重数量</code></pre><p>它决定模型的表达能力和容量。</p><p>训练数据量：</p><pre><code>训练时喂给模型的 token 数量</code></pre><p>它决定模型能看到多少知识、语言模式和任务模式。</p><p>计算量：</p><pre><code>训练模型消耗的算力</code></pre><p>通常和参数量、数据量共同相关。</p><p>可以粗略理解为：</p><pre><code>计算量 ≈ 参数量 × 训练 token 数</code></pre><p>当然实际还受架构、序列长度、训练效率等影响。</p><ol><li>参数量和数据量要平衡</li></ol><p>如果参数很多，但训练数据不够：</p><pre><code>大模型 + 少数据</code></pre><p>问题是：</p><ul><li>训练不充分</li><li>容易浪费参数</li><li>泛化能力不足</li><li>成本高但效果不一定好</li></ul><p>如果数据很多，但模型太小：</p><pre><code>小模型 + 大数据</code></pre><p>问题是：</p><ul><li>模型容量不够</li><li>学不下复杂模式</li><li>性能提升受限</li></ul><p>所以不是单独追求“大参数”或“大数据”，而是要匹配。</p><ol><li>计算量是约束条件</li></ol><p>训练预算通常是有限的。</p><p>在固定计算量下，要决定：</p><pre><code>更多参数，少训练 token？<br/>还是较小模型，更多训练 token？</code></pre><p>早期很多模型偏向参数很大，但训练 token 不够。</p><p>后来 Chinchilla 的结论强调：</p><pre><code>在固定计算预算下，参数量和训练 token 数应该更均衡。</code></pre><p>也就是说，模型不是越大越好，训练得足够充分也很重要。</p><ol><li>三者的简单关系</li></ol><p>可以这样记：</p><pre><code>参数量决定模型容量<br/>数据量决定学习材料<br/>计算量决定训练充分程度</code></pre><p>更形象一点：</p><pre><code>参数量 = 学生脑容量<br/>数据量 = 学习资料<br/>计算量 = 学习时间和精力</code></pre><p>脑容量大但资料少，学不充分。</p><p>资料多但脑容量小，装不下。</p><p>脑容量和资料都有，但学习时间不够，也学不好。</p><ol><li>为什么不能无限增大？</li></ol><p>因为会遇到收益递减和成本问题。</p><p>扩大参数、数据和算力会带来提升，但：</p><ul><li>成本急剧上升</li><li>数据质量变成瓶颈</li><li>训练稳定性变难</li><li>推理成本更高</li><li>部署更复杂</li><li>有些能力不是单靠 scale 解决</li></ul><p>所以现在更强调：</p><ul><li>高质量数据</li><li>合理参数规模</li><li>计算最优训练</li><li>模型压缩</li><li>推理优化</li><li>RAG 和工具增强</li></ul><ol><li>推理阶段也有计算量</li></ol><p>训练计算量之外，还有推理计算量。</p><p>参数越大：</p><ul><li>单次推理成本更高</li><li>延迟更高</li><li>显存占用更大</li><li>部署成本更高</li></ul><p>所以企业应用里，不一定总用最大模型，而是根据任务选择合适模型。</p><ol><li><strong>面试背诵版：模型参数量、训练数据量、计算量之间有什么关系？</strong></li></ol><blockquote><p><strong>模型参数量、训练数据量和计算量是大模型 Scaling Law 里的三个核心因素。</strong>参数量决定模型容量和表达能力，训练数据量决定模型能学习到多少语言模式、知识和任务模式，计算量决定模型能否在给定参数和数据上训练充分。粗略来说，训练计算量和参数量乘以训练 token 数成正相关。</p><p><strong>三者需要匹配。</strong>参数量很大但训练数据不够，会导致模型训练不充分，参数浪费，效果不一定好；数据很多但模型太小，则模型容量不足，无法学习复杂模式；参数和数据都有，但计算预算不够，也无法充分训练。在固定计算预算下，需要在模型大小和训练 token 数之间做平衡。</p><p>Chinchilla Scaling Law 强调，在相同计算预算下，不能只盲目增大参数，还要<strong>给模型足够多的训练 token</strong>，参数规模和数据规模要更均衡。简单说，参数量是“脑容量”，数据量是“学习资料”，计算量是“学习时间和精力”，三者匹配，模型效果才会更好。</p></blockquote>"},{"t":"为什么模型越大效果通常越好？","tag":"重点，虽然可能面试不问，但是亲1你要练习内功","p":"core","html":"<span>[原文档此处有配图]</span><p>模型越大通常效果越好，核心原因是：<strong>参数更多带来更强表达能力，再配合更多数据和计算，模型能学习更复杂的语言、知识和推理模式。</strong></p><ol><li>参数更多，表达能力更强</li></ol><p>模型参数可以理解成模型内部可学习的权重。</p><p>参数越多，模型容量越大，能表示的函数和模式越复杂。</p><p>小模型可能只能学到：</p><ul><li>高频词关系</li><li>简单语法</li><li>常见问答模式</li></ul><p>大模型可以学到：</p><ul><li>更复杂的语义关系</li><li>长距离依赖</li><li>多步推理模式</li><li>代码结构</li><li>抽象概念</li><li>多语言迁移</li><li>任务格式规律</li></ul><p>所以参数变大后，模型能“装下”和表达更多复杂规律。</p><ol><li>能记住和压缩更多知识</li></ol><p>LLM 的知识不是像数据库那样逐条保存，而是被压缩在参数里。</p><p>模型越大，压缩知识和模式的能力越强。</p><p>它更容易学习到：</p><ul><li>世界知识</li><li>专业术语</li><li>语言表达</li><li>代码 API</li><li>常见问题解法</li><li>领域知识关联</li></ul><p>所以大模型在开放问答、总结、代码、推理等任务上通常更强。</p><ol><li>能更好建模长距离关系</li></ol><p>复杂语言任务往往不是看一个词就够了。</p><p>比如：</p><pre><code>如果 A 条件成立，同时 B 不成立，那么输出 C。</code></pre><p>模型需要理解多个条件之间的关系。</p><p>大模型有更多层和更高维表示，能更好捕捉：</p><ul><li>上下文依赖</li><li>指代关系</li><li>因果关系</li><li>条件关系</li><li>跨段落关系</li></ul><p>所以在长文本、多轮对话、复杂指令中表现更好。</p><ol><li>更容易形成泛化能力</li></ol><p>小模型容易只学到表面模式。</p><p>大模型更容易从大量任务中抽象出通用规律。</p><p>比如它不只是记住：</p><pre><code>这道题答案是什么</code></pre><p>而是学会：</p><pre><code>这类问题应该怎么解</code></pre><p>所以大模型 zero-shot、few-shot 和 in-context learning 能力更强。</p><p>也就是没专门训练过的任务，它也可能通过提示词完成。</p><ol><li>复杂能力需要多个子能力组合</li></ol><p>很多高级能力不是单一能力。</p><p>比如代码修复需要：</p><ul><li>理解需求</li><li>读懂代码</li><li>理解调用关系</li><li>生成正确语法</li><li>保持上下文一致</li><li>分析测试失败</li><li>修改 bug</li></ul><p>这些能力叠加后，小模型很难稳定完成。</p><p>大模型因为容量更大，更容易把多个子能力组合起来。</p><p>这也是涌现能力出现的原因之一。</p><ol><li>Scaling Law 支持这个现象</li></ol><p>经验上，大模型训练符合 Scaling Law。</p><p>在一定范围内：</p><pre><code>参数量增加<br/>训练数据增加<br/>计算量增加<br/>→ loss 下降<br/>→ 模型能力提升</code></pre><p>所以模型变大通常不是玄学，而是有经验规律支撑。</p><p>但注意，参数量增加必须配合足够数据和计算。</p><ol><li>不是越大一定越好</li></ol><p>这里要补一句，面试更稳。</p><p>模型变大通常更强，但不是无限成立。</p><p>如果：</p><ul><li>数据质量差</li><li>训练 token 不够</li><li>算力预算不足</li><li>架构不好</li><li>对齐不好</li><li>任务很简单</li><li>推理成本受限</li></ul><p>那么更大的模型不一定更划算。</p><p>有时小模型加 RAG、工具、微调，也能在特定任务上超过大模型。</p><ol><li><strong>面试背诵版：为什么模型越大效果通常越好？</strong></li></ol><blockquote><p>模型越大通常效果越好，主要是因为参数更多带来了更强的表达能力和模型容量。小模型只能学习相对简单、高频的语言模式，而大模型可以表示更复杂的语义关系、长距离依赖、任务模式、代码结构和推理模式，也能在参数中压缩更多世界知识和领域知识。</p><p>同时，很多高级能力不是单一能力，而是多个子能力的组合，比如复杂问答需要语言理解、知识关联和推理，代码修复需要理解需求、阅读代码、定位问题、生成修改和分析测试结果。模型规模变大后，更容易把这些子能力组合起来，因此 zero-shot、few-shot、上下文学习和复杂推理能力通常会增强，这也和大模型的涌现能力有关。</p><p>从经验规律上看，Scaling Law 表明在一定范围内，随着参数量、训练数据量和计算量增加，模型 loss 通常会下降，能力会提升。但不是参数越大一定越好，参数规模需要和训练数据、计算预算匹配，数据质量、架构、对齐和推理成本也很关键。简单说，模型变大通常更强，是因为容量更大、知识压缩能力更强、复杂模式建模能力更好，但必须配合足够高质量的数据和计算。</p></blockquote>"},{"t":"为什么不是无限增加模型参数？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>不能无限增加模型参数，核心原因是：<strong>参数变大虽然通常能提升能力，但成本、数据、训练、推理和收益递减都会成为瓶颈。</strong></p><ol><li>成本太高</li></ol><p>参数越多，训练成本越高。</p><p>需要更多：</p><ul><li>GPU / TPU</li><li>显存</li><li>训练时间</li><li>电力</li><li>存储</li><li>分布式训练资源</li><li>工程维护成本</li></ul><p>大模型训练一次可能就是非常大的投入。</p><p>所以不能无限堆参数。</p><ol><li>推理成本也会变高</li></ol><p>模型不只是训练，还要上线服务。</p><p>参数越大，推理时：</p><ul><li>显存占用更高</li><li>延迟更高</li><li>吞吐更低</li><li>单次请求成本更高</li><li>部署机器更多</li><li>扩容更困难</li></ul><p>企业应用里，很多场景更关注：</p><pre><code>成本、延迟、稳定性</code></pre><p>不一定都适合用最大模型。</p><ol><li>数据不够会训练不充分</li></ol><p>参数变大需要更多高质量数据。</p><p>如果：</p><pre><code>模型很大，但训练 token 不够</code></pre><p>就可能出现：</p><ul><li>参数利用不充分</li><li>泛化能力不足</li><li>浪费算力</li><li>效果不如更小但训练充分的模型</li></ul><p>Chinchilla Scaling Law 就强调，参数量和训练 token 数要匹配。</p><ol><li>数据质量会成为瓶颈</li></ol><p>不是所有数据都值得喂给模型。</p><p>互联网数据里有很多：</p><ul><li>噪声</li><li>重复</li><li>低质量内容</li><li>错误信息</li><li>过时知识</li><li>偏见</li><li>版权风险</li><li>恶意内容</li></ul><p>如果只是无限增加数据配合参数，可能学到更多垃圾信息。</p><p>所以后期更重要的是高质量数据，而不是纯数量。</p><ol><li>收益递减</li></ol><p>模型变大后，能力提升不是线性增加。</p><p>可能从 10B 到 100B 提升明显，但从 500B 到 1000B，提升可能变小，而成本却大幅增加。</p><p>也就是：</p><pre><code>收益增长变慢，成本增长很快</code></pre><p>这就是收益递减。</p><ol><li>训练稳定性更难</li></ol><p>超大模型训练很复杂。</p><p>容易遇到：</p><ul><li>梯度不稳定</li><li>loss spike</li><li>数值溢出</li><li>分布式通信瓶颈</li><li>节点故障</li><li>checkpoint 成本高</li><li>调参困难</li><li>训练中断恢复复杂</li></ul><p>模型越大，工程复杂度越高。</p><ol><li>不一定解决所有问题</li></ol><p>有些能力不是单纯靠参数能解决的。</p><p>比如：</p><ul><li>实时知识</li><li>精确计算</li><li>工具操作</li><li>数据库查询</li><li>业务权限</li><li>事实校验</li><li>长期记忆</li><li>安全对齐</li></ul><p>这些更适合用：</p><ul><li>RAG</li><li>Tool Calling</li><li>Memory</li><li>Agent Runtime</li><li>规则系统</li><li>人工审核</li></ul><p>而不是只靠增加参数。</p><ol><li>小模型在特定场景更划算</li></ol><p>很多业务任务不需要超大模型。</p><p>比如：</p><ul><li>意图分类</li><li>信息抽取</li><li>文本审核</li><li>FAQ</li><li>格式转换</li><li>简单总结</li></ul><p>小模型可能：</p><ul><li>更便宜</li><li>更快</li><li>更容易部署</li><li>更稳定</li><li>更适合私有化</li></ul><p>所以实际系统会做模型路由，而不是所有请求都上最大模型。</p><ol><li><strong>面试背诵版：为什么不是无限增加模型参数？</strong></li></ol><blockquote><p>不能无限增加模型参数，主要是因为成本、数据、训练、推理和收益递减都会成为瓶颈。参数越多，训练需要的算力、显存、时间、电力、存储和分布式工程成本都会大幅上升；上线推理时也会带来更高延迟、更低吞吐、更大显存占用和更高单次请求成本。</p><p>同时，参数规模必须和训练数据量匹配。如果模型很大但训练 token 不够，就会训练不充分，参数利用率低，效果可能不如更小但训练更充分的模型。Chinchilla Scaling Law 就强调，在固定计算预算下，不能只盲目增大参数，还要保证足够的训练数据。数据质量也会成为瓶颈，低质量、重复、过时或错误数据可能反而影响模型效果。</p><p>另外，模型越大收益会递减，训练稳定性和工程复杂度也会显著增加。而且很多问题，比如实时知识、精确计算、数据库查询、权限控制和事实校验，并不是单靠增加参数就能解决，通常需要结合 RAG、工具调用、规则系统和人工审核。简单说，模型参数不是越大越好，而是要在效果、成本、数据、算力和业务场景之间做平衡。</p></blockquote><p></p><span>[原文档此处有配图]</span>"}]},{"no":"","title":"2.1 基础概念（都是重点）","questions":[{"t":"监督学习、无监督学习、强化学习有什么区别？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>三者核心区别是：<strong>监督学习有标准答案，无监督学习没有标准答案，强化学习靠奖励信号学习行为策略。</strong></p><ol><li>监督学习是什么？</li></ol><p>监督学习是用带标签的数据训练模型。</p><p>数据形式一般是：</p><pre><code>输入 X + 标准答案 Y</code></pre><p>模型学习的是：</p><pre><code>给定输入 X，预测输出 Y</code></pre><p>比如：</p><ul><li>图片 → 猫 / 狗</li><li>文本 → 正面 / 负面</li><li>用户信息 → 是否会流失</li><li>房屋信息 → 房价</li><li>语音 → 文字</li></ul><p>监督学习的目标是让模型预测结果尽量接近真实标签。</p><ol><li>无监督学习是什么？</li></ol><p>无监督学习没有人工标注的标准答案。</p><p>数据只有：</p><pre><code>输入 X</code></pre><p>模型自己从数据里发现结构和规律。</p><p>常见任务包括：</p><ul><li>聚类</li><li>降维</li><li>异常检测</li><li>主题发现</li><li>表征学习</li></ul><p>比如把用户按行为自动分群：</p><pre><code>高消费用户<br/>价格敏感用户<br/>沉默用户</code></pre><p>这些类别不是提前标好的，而是模型根据数据相似性发现的。</p><ol><li>强化学习是什么？</li></ol><p>强化学习是让智能体通过和环境交互来学习策略。</p><p>核心是：</p><pre><code>Agent 执行动作 → 环境反馈奖励 → Agent 调整策略</code></pre><p>它不是每一步都有标准答案，而是通过奖励判断行为好坏。</p><p>比如：</p><ul><li>游戏 AI</li><li>机器人控制</li><li>推荐系统策略优化</li><li>自动驾驶决策</li><li>大模型 RLHF 对齐</li></ul><p>举例：</p><pre><code>走这一步棋赢了 → 奖励高<br/>走这一步棋输了 → 奖励低</code></pre><p>模型学习的是在不同状态下选择什么动作，使长期奖励最大。</p><ol><li>三者数据形式不同</li></ol><p>可以这样记：</p><pre><code>监督学习：有答案<br/>无监督学习：没答案，找规律<br/>强化学习：没标准答案，有奖励</code></pre><p>对应数据形式：</p><pre><code>监督学习：X, Y<br/>无监督学习：X<br/>强化学习：State, Action, Reward</code></pre><ol><li>目标不同</li></ol><p>监督学习目标：</p><pre><code>预测准确</code></pre><p>无监督学习目标：</p><pre><code>发现结构</code></pre><p>强化学习目标：</p><pre><code>长期奖励最大</code></pre><p>比如同样是用户系统：</p><ul><li>监督学习：预测用户是否流失</li><li>无监督学习：把用户自动分群</li><li>强化学习：决定给用户发什么券，使长期收益最高</li></ul><ol><li>反馈方式不同</li></ol><p>监督学习反馈明确：</p><pre><code>预测错了，和标准答案对比</code></pre><p>无监督学习没有明确答案：</p><pre><code>看数据内部结构是否合理</code></pre><p>强化学习反馈延迟：</p><pre><code>当前动作可能要过很多步后才知道好坏</code></pre><p>比如下棋，某一步看起来不错，但最后输了，说明策略可能有问题。</p><ol><li>和大模型的关系</li></ol><p>大模型训练里三者都有影子。</p><p>比如：</p><ul><li>预训练：常被看作自监督学习</li><li>指令微调：监督学习</li><li>RLHF：强化学习</li><li>Embedding 聚类：无监督学习</li></ul><p>注意：大模型预训练虽然没有人工标签，但会用文本本身构造标签，比如预测下一个 token，所以更准确叫自监督学习。</p><ol><li><strong>面试背诵版：监督学习、无监督学习、强化学习有什么区别？</strong></li></ol><blockquote><p>监督学习、无监督学习和强化学习的主要区别在于训练信号不同。监督学习使用带标签的数据训练，数据包含输入和标准答案，模型学习从输入到输出的映射，目标是预测准确，比如分类、回归、情感分析和图像识别。</p><p>无监督学习没有标准答案，只有原始数据，模型需要自己发现数据中的结构和规律，常见任务包括聚类、降维、异常检测和主题发现。强化学习则是智能体和环境交互，通过执行动作获得奖励或惩罚，学习在不同状态下选择什么动作，使长期累计奖励最大，常用于游戏 AI、机器人控制、推荐策略和大模型 RLHF。</p><p>简单说，监督学习是“有答案，学预测”；无监督学习是“没答案，找结构”；强化学习是“靠奖励，学策略”。在大模型中，预训练更接近自监督学习，指令微调属于监督学习，RLHF 属于强化学习。</p></blockquote>"},{"t":"分类问题和回归问题有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>分类问题和回归问题的核心区别是：<strong>分类预测离散类别，回归预测连续数值</strong>。</p><ol><li>分类问题是什么？</li></ol><p>分类是预测一个样本属于哪个类别。</p><p>输出是离散的标签。</p><p>比如：</p><ul><li>邮件是不是垃圾邮件</li><li>图片是猫还是狗</li><li>用户是否会流失</li><li>文本情感是正面还是负面</li><li>交易是否欺诈</li><li>疾病类型是哪一种</li></ul><p>输出通常是：</p><pre><code>是 / 否<br/>猫 / 狗<br/>正面 / 负面<br/>A 类 / B 类 / C 类</code></pre><ol><li>回归问题是什么？</li></ol><p>回归是预测一个连续数值。</p><p>输出是数值型结果。</p><p>比如：</p><ul><li>房价是多少</li><li>明天温度多少</li><li>用户消费金额多少</li><li>股票收益率多少</li><li>商品销量多少</li><li>订单配送时长多少</li></ul><p>输出通常是：</p><pre><code>532000 元<br/>26.5℃<br/>3.8 分<br/>1200.75</code></pre><ol><li>输出形式不同</li></ol><p>最核心区别：</p><pre><code>分类：输出类别<br/>回归：输出数值</code></pre><p>比如预测房子：</p><ul><li>预测房子是“高价房 / 中价房 / 低价房” → 分类</li><li>预测房子具体价格是 320 万 → 回归</li></ul><ol><li>模型目标不同</li></ol><p>分类模型关注：</p><pre><code>类别是否判断正确</code></pre><p>通常输出每个类别的概率。</p><p>比如：</p><pre><code>猫：0.8<br/>狗：0.2</code></pre><p>回归模型关注：</p><pre><code>预测值和真实值差多少</code></pre><p>比如真实房价 300 万，预测 310 万，误差是 10 万。</p><ol><li>损失函数不同</li></ol><p>分类常用损失函数：</p><ul><li>交叉熵损失</li><li>Hinge Loss</li><li>Log Loss</li></ul><p>回归常用损失函数：</p><ul><li>均方误差 MSE</li><li>平均绝对误差 MAE</li><li>Huber Loss</li></ul><p>简单理解：</p><pre><code>分类看分错没<br/>回归看差多远</code></pre><ol><li>评估指标不同</li></ol><p>分类常用指标：</p><ul><li>Accuracy</li><li>Precision</li><li>Recall</li><li>F1</li><li>AUC</li><li>混淆矩阵</li></ul><p>回归常用指标：</p><ul><li>MSE</li><li>RMSE</li><li>MAE</li><li>R²</li></ul><p>比如欺诈检测更关注 Recall，房价预测更关注 MAE 或 RMSE。</p><ol><li>举个例子</li></ol><p>用户流失预测：</p><p>如果问：</p><pre><code>这个用户会不会流失？</code></pre><p>这是分类问题。</p><p>如果问：</p><pre><code>这个用户未来 30 天可能消费多少钱？</code></pre><p>这是回归问题。</p><ol><li><strong>面试背诵版：分类问题和回归问题有什么区别？</strong></li></ol><blockquote><p>分类问题和回归问题的<strong>区别主要在输出类型</strong>。分类问题预测的是离散类别，比如垃圾邮件识别、猫狗分类、情感正负面、用户是否流失，输出通常是某个类别或类别概率；回归问题预测的是连续数值，比如房价、温度、销量、消费金额和配送时长，输出是一个具体数值。</p><p>两者的优化目标和评估指标也不同。<strong>（重要!）</strong><strong>分类任务通常使用交叉熵损失，关注 Accuracy、Precision、Recall、F1、AUC 等指标</strong>；<strong>回归任务通常使用 MSE、MAE、Huber Loss 等损失函数，关注预测值和真实值之间的误差，比如 RMSE、MAE 和 R²。</strong></p><p>简单说，分类是“判断属于哪一类”，回归是“预测具体是多少”。如果输出是类别，就是分类；如果输出是连续数值，就是回归。</p></blockquote>"},{"t":"什么是过拟合？如何解决？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>过拟合的核心是：<strong>模型把训练数据学得太死，甚至把噪声也记住了，导致训练集效果很好，但新数据效果差。</strong></p><ol><li>什么是过拟合？</li></ol><p>过拟合指模型在训练集上表现很好，但在测试集或真实数据上表现不好。</p><p>比如：</p><pre><code>训练集准确率 99%<br/>测试集准确率 70%</code></pre><p>说明模型可能不是学到了通用规律，而是记住了训练样本的细节和噪声。</p><ol><li>为什么会过拟合？</li></ol><p>常见原因包括：</p><ul><li>模型太复杂</li><li>参数太多</li><li>训练数据太少</li><li>训练轮数太多</li><li>数据噪声太大</li><li>特征太多但有效信息少</li><li>训练集和测试集分布不一致</li><li>正则化不足</li></ul><p>比如样本很少，但模型很复杂，就容易把每个样本都“背下来”。</p><ol><li>过拟合的表现</li></ol><p>典型表现是：</p><pre><code>训练误差很低<br/>验证误差很高</code></pre><p>或者训练过程中：</p><pre><code>训练集 loss 一直下降<br/>验证集 loss 先下降后上升</code></pre><p>这说明模型继续训练后，对训练集越来越熟，但泛化能力变差。</p><ol><li>如何解决：增加数据</li></ol><p>最直接的方法是增加训练数据。</p><p>可以通过：</p><ul><li>收集更多样本</li><li>数据增强</li><li>清洗脏数据</li><li>覆盖更多场景</li><li>保证训练集和测试集分布一致</li></ul><p>数据越丰富，模型越不容易只记住少数样本。</p><ol><li>如何解决：降低模型复杂度</li></ol><p>如果模型太复杂，可以：</p><ul><li>减少参数量</li><li>减少特征数量</li><li>降低模型深度</li><li>剪枝</li><li>使用更简单模型</li></ul><p>比如简单任务没必要用特别复杂的模型。</p><ol><li>如何解决：正则化</li></ol><p>正则化是常用方法。</p><p>包括：</p><ul><li>L1 正则</li><li>L2 正则</li><li>Dropout</li><li>Weight Decay</li><li>Label Smoothing</li></ul><p>它们的作用是约束模型不要过度依赖某些特征或参数。</p><ol><li>如何解决：早停</li></ol><p>Early Stopping 是训练中常用方法。</p><p>当验证集效果不再提升，甚至开始变差时，就停止训练。</p><p>比如：</p><pre><code>验证集 loss 连续 5 轮不下降 → 停止训练</code></pre><p>避免模型继续记忆训练集噪声。</p><ol><li>如何解决：交叉验证</li></ol><p>用交叉验证可以更稳定评估模型泛化能力。</p><p>比如 K 折交叉验证：</p><pre><code>把数据分成 K 份<br/>每次用 K-1 份训练，1 份验证</code></pre><p>可以减少偶然划分带来的评估偏差。</p><ol><li><strong>面试背诵版：什么是过拟合？如何解决？</strong></li></ol><blockquote><p>过拟合指模型在训练集上表现很好，但在验证集、测试集或真实线上数据上表现较差。它通常说明模型没有学到通用规律，而是把训练样本中的细节、噪声甚至偶然特征也记住了。典型表现是训练误差很低，但验证误差较高，或者训练 loss 持续下降，而验证 loss 先下降后上升。</p><p>过拟合常见原因包括模型过于复杂、参数太多、训练数据太少、训练轮数太多、数据噪声大、特征冗余以及正则化不足。解决方法包括增加训练数据或数据增强，清洗噪声数据，降低模型复杂度，减少无效特征，使用 L1、L2、Dropout、Weight Decay 等正则化方法，使用 Early Stopping 在验证集效果不再提升时停止训练，以及通过交叉验证更稳定地评估泛化能力。</p><p>简单说，过拟合就是“训练集背得太熟，但新题不会做”，解决思路是增加数据、降低复杂度、加正则、早停和做好验证。</p></blockquote>"},{"t":"什么是欠拟合？如何解决？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>欠拟合的核心是：<strong>模型太弱或训练不充分，连训练集里的规律都没学好，导致训练集和测试集效果都差。</strong></p><ol><li>什么是欠拟合？</li></ol><p>欠拟合指模型没有学到数据中的有效规律。</p><p>表现是：</p><pre><code>训练集效果差<br/>测试集效果也差</code></pre><p>比如：</p><pre><code>训练集准确率 60%<br/>测试集准确率 58%</code></pre><p>说明模型本身就没学明白任务。</p><ol><li>和过拟合的区别</li></ol><p>过拟合是：</p><pre><code>训练集好，测试集差</code></pre><p>欠拟合是：</p><pre><code>训练集差，测试集也差</code></pre><p>可以这样记：</p><pre><code>过拟合：学太死，泛化差<br/>欠拟合：没学会，整体差</code></pre><ol><li>欠拟合的原因</li></ol><p>常见原因包括：</p><ul><li>模型太简单</li><li>特征不足</li><li>训练时间太短</li><li>学习率不合适</li><li>正则化太强</li><li>数据质量太差</li><li>任务本身复杂，但模型能力不够</li><li>特征和标签关系没有被表达出来</li></ul><p>比如用线性模型去拟合一个很复杂的非线性关系，就容易欠拟合。</p><ol><li>如何解决：提高模型复杂度</li></ol><p>如果模型太简单，可以换更强模型。</p><p>比如：</p><ul><li>线性模型换成树模型</li><li>浅层模型换成深层模型</li><li>小模型换成大模型</li><li>增加神经网络层数或隐藏单元</li><li>使用更复杂的特征交互</li></ul><p>核心是让模型有能力表达数据规律。</p><ol><li>如何解决：增加有效特征</li></ol><p>欠拟合可能是因为输入信息不够。</p><p>可以：</p><ul><li>增加特征</li><li>做特征交叉</li><li>加入领域特征</li><li>提取更有表达力的特征</li><li>使用 embedding 表示</li><li>引入上下文信息</li></ul><p>比如预测房价，只用面积可能不够，还要加入地段、楼层、房龄等特征。</p><ol><li>如何解决：训练更充分</li></ol><p>如果训练不够，可以：</p><ul><li>增加训练轮数</li><li>调整学习率</li><li>使用更合适的优化器</li><li>改 batch size</li><li>检查 loss 是否正常下降</li><li>检查训练流程是否有 bug</li></ul><p>有时候不是模型不行，而是根本没训练好。</p><ol><li>如何解决：减弱正则化</li></ol><p>正则化太强也会导致欠拟合。</p><p>比如：</p><ul><li>L1 / L2 过大</li><li>Dropout 过高</li><li>模型被约束太死</li><li>剪枝过度</li></ul><p>这时可以适当降低正则化强度。</p><ol><li>如何解决：改善数据质量</li></ol><p>如果数据本身质量差，模型也学不好。</p><p>可以：</p><ul><li>清洗错误标签</li><li>去除脏数据</li><li>处理缺失值</li><li>统一数据分布</li><li>修正异常样本</li><li>增加更有代表性的数据</li></ul><ol><li><strong>面试背诵版：什么是欠拟合？如何解决？</strong></li></ol><blockquote><p>欠拟合指模型没有充分学习到训练数据中的有效规律，导致训练集和测试集表现都不好。它通常说明模型表达能力不足、特征信息不够，或者训练过程不充分。和过拟合不同，过拟合是训练集效果很好但测试集效果差；欠拟合是训练集和测试集效果都差。</p><p>欠拟合常见原因包括模型太简单、特征不足、训练轮数太少、学习率设置不合理、正则化太强、数据质量差，或者任务本身复杂但模型容量不够。解决方法包括提高模型复杂度，比如换更强的模型、增加网络层数或参数量；增加有效特征和特征交叉；延长训练时间，调整学习率、优化器和 batch size；适当减弱 L1、L2、Dropout 等正则化；同时清洗错误标签和低质量数据。</p><p>简单说，欠拟合就是“模型连训练集都没学会”，解决思路是让模型更强、特征更充分、训练更到位、约束别太强、数据更干净。</p></blockquote>"},{"t":"Bias和Variance分别是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>Bias 和 Variance 是用来分析模型泛化误差的两个概念：<strong>Bias 表示模型预测的系统性偏差，Variance 表示模型对训练数据变化的敏感程度。</strong></p><ol><li>Bias 是什么？</li></ol><p>Bias 叫偏差。</p><p>它表示模型预测结果和真实规律之间的差距。</p><p>如果 Bias 很高，说明模型太简单，没学到真实规律。</p><p>比如真实关系是非线性的：</p><pre><code>y = x²</code></pre><p>但你用线性模型去拟合：</p><pre><code>y = ax + b</code></pre><p>模型怎么学都学不好，这就是高 Bias。</p><p>高 Bias 通常对应欠拟合。</p><ol><li>Variance 是什么？</li></ol><p>Variance 叫方差。</p><p>它表示模型对训练数据变化有多敏感。</p><p>如果 Variance 很高，说明模型太复杂，训练数据稍微变一点，模型结果就变化很大。</p><p>比如一个很复杂的模型把训练集里的噪声也记住了。</p><p>换一批训练数据，它学出来的边界完全不一样。</p><p>高 Variance 通常对应过拟合。</p><ol><li>高 Bias 的表现</li></ol><p>高 Bias 通常表现为：</p><ul><li>训练集效果差</li><li>测试集效果也差</li><li>模型太简单</li><li>学不到复杂规律</li><li>误差整体偏大</li></ul><p>比如：</p><pre><code>训练准确率 60%<br/>测试准确率 58%</code></pre><p>这更像欠拟合。</p><ol><li>高 Variance 的表现</li></ol><p>高 Variance 通常表现为：</p><ul><li>训练集效果很好</li><li>测试集效果明显差</li><li>模型太复杂</li><li>对训练数据噪声敏感</li><li>泛化能力差</li></ul><p>比如：</p><pre><code>训练准确率 99%<br/>测试准确率 75%</code></pre><p>这更像过拟合。</p><ol><li>Bias 和 Variance 的关系</li></ol><p>两者通常存在 trade-off。</p><p>模型太简单：</p><pre><code>Bias 高，Variance 低</code></pre><p>模型太复杂：</p><pre><code>Bias 低，Variance 高</code></pre><p>理想情况是：</p><pre><code>Bias 和 Variance 都比较低</code></pre><p>但实际中需要平衡。</p><ol><li>怎么降低 Bias？</li></ol><p>高 Bias 说明模型太弱，可以：</p><ul><li>换更复杂的模型</li><li>增加特征</li><li>增加特征交叉</li><li>减少过强正则化</li><li>训练更久</li><li>使用非线性模型</li></ul><p>目标是让模型更能拟合真实规律。</p><ol><li>怎么降低 Variance？</li></ol><p>高 Variance 说明模型太容易过拟合，可以：</p><ul><li>增加训练数据</li><li>数据增强</li><li>加正则化</li><li>Dropout</li><li>Early Stopping</li><li>简化模型</li><li>集成学习</li><li>交叉验证</li></ul><p>目标是提升泛化能力。</p><ol><li><strong>面试背诵版：Bias 和 Variance 分别是什么？</strong></li></ol><blockquote><p>Bias 表示模型预测结果和真实规律之间的系统性偏差。Bias 高通常说明模型太简单，表达能力不足，没有学到数据中的真实规律，训练集和测试集表现都差，容易出现欠拟合。</p><p>Variance 表示模型对训练数据变化的敏感程度。Variance 高说明模型太复杂，容易把训练集中的噪声和偶然特征也学进去，导致训练集表现很好，但测试集表现较差，容易出现过拟合。</p><p>二者通常存在权衡关系：模型太简单时 Bias 高、Variance 低；模型太复杂时 Bias 低、Variance 高。降低 Bias 可以提高模型复杂度、增加有效特征、减少过强正则化；降低 Variance 可以增加数据、做数据增强、加正则化、Dropout、Early Stopping、简化模型或使用交叉验证。简单说，Bias 是“学得不够”，Variance 是“学得太敏感”。</p></blockquote>"},{"t":"Bias-Variance Tradeoff是什么？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>Bias-Variance Tradeoff 核心是：<strong>模型太简单会欠拟合，偏差高；模型太复杂会过拟合，方差高，所以要在两者之间找平衡。</strong></p><ol><li>先理解 Bias 和 Variance</li></ol><p>Bias 是偏差，表示模型和真实规律之间的系统性差距。</p><p>Bias 高说明模型太简单，学不到真实规律，容易欠拟合。</p><p>Variance 是方差，表示模型对训练数据变化的敏感程度。</p><p>Variance 高说明模型太复杂，容易记住噪声，泛化能力差，容易过拟合。</p><ol><li>什么是 Tradeoff？</li></ol><p>Tradeoff 就是权衡。</p><p>模型复杂度变化时，Bias 和 Variance 往往会反向变化。</p><p>模型太简单：</p><pre><code>Bias 高，Variance 低</code></pre><p>模型太复杂：</p><pre><code>Bias 低，Variance 高</code></pre><p>所以不能只追求降低 Bias，也不能只追求降低 Variance。</p><ol><li>举个例子</li></ol><p>假设真实关系是弯曲曲线。</p><p>如果用直线去拟合：</p><pre><code>模型太简单 → Bias 高 → 欠拟合</code></pre><p>如果用非常复杂的曲线穿过每个训练点：</p><pre><code>模型太复杂 → Variance 高 → 过拟合</code></pre><p>最好的模型应该是：</p><pre><code>既能抓住整体趋势，又不被噪声带偏</code></pre><ol><li>和模型复杂度的关系</li></ol><p>随着模型复杂度增加：</p><pre><code>Bias 通常下降<br/>Variance 通常上升</code></pre><p>总误差可以粗略理解为：</p><pre><code>总误差 = Bias² + Variance + 噪声</code></pre><p>目标是让总误差最低，而不是让某一项最低。</p><ol><li>如何平衡？</li></ol><p>常见方法包括：</p><ul><li>选择合适复杂度的模型</li><li>使用验证集调参</li><li>正则化</li><li>Early Stopping</li><li>交叉验证</li><li>增加数据</li><li>数据增强</li><li>集成学习</li><li>控制特征数量</li><li>防止训练过久</li></ul><p>这些方法本质上都是在控制模型的拟合能力和泛化能力。</p><ol><li>在大模型里的体现</li></ol><p>大模型参数多，表达能力强，Bias 往往较低。</p><p>但如果数据质量差、样本不足或任务微调集太小，也可能过拟合。</p><p>所以大模型微调时常用：</p><ul><li>正则化</li><li>少量训练轮数</li><li>验证集监控</li><li>LoRA / Adapter</li><li>Early Stopping</li><li>数据清洗</li></ul><p>来控制过拟合风险。</p><ol><li><strong>面试背诵版：Bias-Variance Tradeoff 是什么？</strong></li></ol><blockquote><p>Bias-Variance Tradeoff 指的是模型偏差和方差之间的权衡关系。Bias 表示模型预测和真实规律之间的系统性差距，Bias 高通常说明模型太简单，容易欠拟合；Variance 表示模型对训练数据变化的敏感程度，Variance 高通常说明模型太复杂，容易记住训练数据中的噪声，导致过拟合。</p><p>随着模型复杂度增加，Bias 通常会下降，因为模型拟合能力更强；但 Variance 往往会上升，因为模型更容易对训练数据中的噪声敏感。模型总误差可以粗略看成 Bias²、Variance 和不可避免噪声的组合，所以最优模型不是 Bias 最低或 Variance 最低，而是在二者之间取得平衡，使验证集或测试集误差最低。</p><p>解决这个权衡通常依靠选择合适模型复杂度、使用验证集调参、正则化、Early Stopping、交叉验证、增加数据、数据增强和集成学习。简单说，Bias-Variance Tradeoff 就是“模型不能太简单，也不能太复杂，要在欠拟合和过拟合之间找到泛化最好的点”。</p></blockquote>"},{"t":"训练集、验证集、测试集为什么要划分？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>划分训练集、验证集、测试集，核心是：<strong>防止模型只在见过的数据上表现好，同时客观评估模型对新数据的泛化能力。</strong></p><ol><li>训练集是干什么的？</li></ol><p>训练集用于训练模型参数。</p><p>模型会直接学习训练集里的规律。</p><p>比如：</p><pre><code>用训练集更新权重，让 loss 下降</code></pre><p>所以训练集相当于“练习题”。</p><p>模型会反复看这些数据。</p><ol><li>验证集是干什么的？</li></ol><p>验证集用于调参和选择模型。</p><p>比如用来决定：</p><ul><li>学习率</li><li>模型结构</li><li>正则化强度</li><li>训练轮数</li><li>batch size</li><li>是否 early stopping</li><li>选择哪个 checkpoint</li></ul><p>验证集相当于“模拟考试”。</p><p>模型训练时不直接用它更新参数，但开发者会根据验证集表现做选择。</p><ol><li>测试集是干什么的？</li></ol><p>测试集用于最终评估。</p><p>它应该在模型和参数都确定后才使用。</p><p>测试集相当于“正式考试”。</p><p>它用来估计模型上线后遇到新数据时的真实表现。</p><ol><li>为什么不能只分训练集和测试集？</li></ol><p>如果没有验证集，开发者可能会反复看测试集结果来调参。</p><p>这样测试集就被“间接学习”了。</p><p>比如：</p><pre><code>测试集效果不好 → 调参数<br/>再测 → 再调</code></pre><p>久而久之，模型就对测试集过拟合了。</p><p>测试集就不再客观。</p><ol><li>为什么要防止数据泄露？</li></ol><p>如果测试集信息进入训练过程，就会数据泄露。</p><p>比如：</p><ul><li>测试样本出现在训练集</li><li>用测试集调参</li><li>标准化时用了全量数据统计量</li><li>时间序列预测中用未来数据训练</li><li>同一个用户的数据同时出现在训练和测试中</li></ul><p>这会导致评估结果虚高。</p><ol><li>三者关系</li></ol><p>可以这样理解：</p><pre><code>训练集：用来学习<br/>验证集：用来调参<br/>测试集：用来最终评估</code></pre><p>或者：</p><pre><code>训练集 = 平时练习<br/>验证集 = 模拟考试<br/>测试集 = 最终考试</code></pre><ol><li>常见划分比例</li></ol><p>常见比例有：</p><pre><code>70% 训练 / 15% 验证 / 15% 测试<br/>80% 训练 / 10% 验证 / 10% 测试</code></pre><p>如果数据量很小，可以用交叉验证。</p><p>如果是时间序列数据，不能随机打乱，要按时间切分。</p><ol><li><strong>面试背诵版：训练集、验证集、测试集为什么要划分？</strong></li></ol><blockquote><p>划分训练集、验证集和测试集，是为了让模型训练、调参和最终评估相互隔离，从而客观衡量模型的泛化能力。训练集用于训练模型参数，模型会直接从中学习规律；验证集用于调参和模型选择，比如选择学习率、模型结构、正则化强度、训练轮数和 checkpoint；测试集只在最终模型确定后使用，用来评估模型在未见过数据上的真实性能。</p><p>如果没有验证集，开发者可能会反复使用测试集调参，导致模型或选择过程对测试集过拟合，使测试结果虚高，不能代表上线效果。同时还要避免数据泄露，比如测试样本进入训练集、用全量数据做标准化、时间序列中使用未来数据等。</p><p>简单说，<strong>训练集是“练习题”，验证集是“模拟考试”，测试集是“正式考试”。训练集用来学习，验证集用来调参</strong>，测试集用来做最终客观评估。</p></blockquote>"}]},{"no":"","title":"2.2 损失函数","questions":[{"t":"常见Loss有哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>Loss 的作用是衡量模型预测和真实答案之间的差距，训练时模型就是通过最小化 Loss 来学习参数。</strong></p><ol><li>分类任务常见 Loss</li></ol><p>分类任务最常用的是交叉熵损失。</p><p>二分类常用：</p><pre><code>Binary Cross Entropy，BCE</code></pre><p>适合输出是 0 / 1 的任务，比如：</p><ul><li>是否垃圾邮件</li><li>是否欺诈</li><li>是否点击</li><li>是否流失</li></ul><p>多分类常用：</p><pre><code>Cross Entropy Loss</code></pre><p>适合输出多个类别，比如：</p><ul><li>猫 / 狗 / 鸟</li><li>正面 / 中性 / 负面</li><li>意图分类</li></ul><p>交叉熵的核心是：</p><pre><code>真实类别的预测概率越高，loss 越小</code></pre><ol><li>回归任务常见 Loss</li></ol><p>回归任务预测连续值。</p><p>常见 Loss 有：</p><ul><li>MSE</li><li>MAE</li><li>Huber Loss</li></ul><p>MSE 是均方误差：</p><pre><code>预测值和真实值差值的平方</code></pre><p>特点是对大误差更敏感。</p><p>MAE 是平均绝对误差：</p><pre><code>预测值和真实值差值的绝对值</code></pre><p>特点是对异常值不如 MSE 敏感。</p><p>Huber Loss 结合了 MSE 和 MAE：</p><ul><li>小误差时像 MSE</li><li>大误差时像 MAE</li></ul><p>所以对异常值更稳健。</p><ol><li>排序任务常见 Loss</li></ol><p>排序任务常用于搜索、推荐、广告。</p><p>常见有：</p><ul><li>Pairwise Ranking Loss</li><li>Hinge Loss</li><li>Listwise Loss</li><li>BPR Loss</li></ul><p>比如推荐系统里，希望用户喜欢的商品排在不喜欢的商品前面。</p><p>核心不是预测绝对分数，而是优化排序关系。</p><ol><li>对比学习常见 Loss</li></ol><p>对比学习常用于表示学习、Embedding、检索。</p><p>常见有：</p><ul><li>Contrastive Loss</li><li>Triplet Loss</li><li>InfoNCE Loss</li></ul><p>它们的目标是：</p><pre><code>相似样本距离更近<br/>不相似样本距离更远</code></pre><p>比如：</p><pre><code>同一个问题的不同表达 → embedding 更近<br/>不同问题 → embedding 更远</code></pre><ol><li>生成模型常见 Loss</li></ol><p>大语言模型预训练常用：</p><pre><code>Next Token Prediction Loss</code></pre><p>本质上通常也是交叉熵。</p><p>模型根据前文预测下一个 token。</p><p>比如：</p><pre><code>输入：中国的首都是<br/>目标：北京</code></pre><p>模型预测“北京”的概率越高，loss 越低。</p><ol><li>目标检测常见 Loss</li></ol><p>目标检测任务通常有多种 loss 组合。</p><p>比如：</p><ul><li>分类 Loss：判断是什么类别</li><li>回归 Loss：预测边界框位置</li><li>IoU Loss / GIoU / DIoU / CIoU：衡量预测框和真实框重叠程度</li></ul><p>目标检测通常不是单一 Loss，而是多个目标一起优化。</p><ol><li>正则化项</li></ol><p>严格说正则化不是普通预测 Loss，但训练目标里经常会加。</p><p>比如：</p><ul><li>L1 正则</li><li>L2 正则</li><li>Weight Decay</li></ul><p>作用是限制模型复杂度，减少过拟合。</p><p>最终目标可能是：</p><pre><code>总 Loss = 任务 Loss + 正则化项</code></pre><ol><li><strong>面试背诵版：常见 Loss 有哪些？</strong></li></ol><blockquote><p>常见 Loss 要按任务类型来回答。分类任务最常用交叉熵损失，二分类用 Binary Cross Entropy，多分类用 Cross Entropy，本质是让真实类别的预测概率越高，loss 越小。回归任务常用 MSE、MAE 和 Huber Loss，MSE 对大误差更敏感，MAE 对异常值更稳健，Huber Loss 结合了两者特点。</p><p>排序任务常见 Pairwise Ranking Loss、Hinge Loss、BPR Loss，主要用于搜索、推荐、广告，让相关样本排在不相关样本前面。对比学习常用 Contrastive Loss、Triplet Loss、InfoNCE Loss，目标是让相似样本表示更近，不相似样本表示更远。生成模型，比如大语言模型，通常用 next token prediction loss，本质上也是交叉熵。</p><p>另外，训练目标里经常会加入 L1、L2 或 Weight Decay 等正则化项，用来限制模型复杂度、防止过拟合。简单说，分类用交叉熵，回归用 MSE / MAE / Huber，排序用 Ranking Loss，对比学习用 Contrastive / Triplet / InfoNCE，语言模型生成常用 next token 的交叉熵。</p></blockquote>"},{"t":"MSE Loss和Cross Entropy Loss有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>MSE Loss 和 Cross Entropy Loss 的核心区别是：<strong>MSE 主要用于回归，衡量数值误差；Cross Entropy 主要用于分类，衡量预测概率分布和真实类别之间的差距。</strong></p><ol><li>MSE Loss 是什么？</li></ol><p>MSE 是均方误差。</p><p>它计算的是：</p><pre><code>预测值和真实值差值的平方的平均</code></pre><p>比如真实房价是 300 万，模型预测 320 万，误差是 20 万。</p><p>MSE 会把误差平方，所以大误差会被放大。</p><p>它适合：</p><ul><li>房价预测</li><li>温度预测</li><li>销量预测</li><li>分数预测</li><li>连续值回归任务</li></ul><ol><li>Cross Entropy Loss 是什么？</li></ol><p>Cross Entropy 是交叉熵损失。</p><p>它主要用于分类任务。</p><p>模型通常输出每个类别的概率，比如：</p><pre><code>猫：0.8<br/>狗：0.2</code></pre><p>如果真实类别是猫，那么模型给猫的概率越高，loss 越小。</p><p>如果模型给真实类别很低概率，loss 就很大。</p><p>它适合：</p><ul><li>二分类</li><li>多分类</li><li>文本分类</li><li>图像分类</li><li>意图识别</li><li>语言模型 next token 预测</li></ul><ol><li>关注点不同</li></ol><p>MSE 关注：</p><pre><code>预测数值离真实数值有多远</code></pre><p>Cross Entropy 关注：</p><pre><code>真实类别的预测概率有多高</code></pre><p>比如分类任务中：</p><pre><code>真实类别：猫<br/>模型预测猫的概率：0.9 → loss 小<br/>模型预测猫的概率：0.1 → loss 大</code></pre><ol><li>输出形式不同</li></ol><p>MSE 的输出一般是连续数值：</p><pre><code>预测值 = 23.6<br/>真实值 = 25.0</code></pre><p>Cross Entropy 的输出一般是类别概率分布：</p><pre><code>类别 A：0.7<br/>类别 B：0.2<br/>类别 C：0.1</code></pre><p>所以二者适用场景不同。</p><ol><li>对错误的惩罚方式不同</li></ol><p>MSE 对数值误差平方惩罚。</p><p>误差越大，惩罚增长越快。</p><p>Cross Entropy 对“真实类别概率很低”的情况惩罚很重。</p><p>比如真实类别概率接近 0，loss 会非常大。</p><p>这会强烈推动模型提高正确类别的概率。</p><ol><li>为什么分类不用 MSE 更常见？</li></ol><p>分类任务本质上是在学习概率分布。</p><p>Cross Entropy 和 softmax / sigmoid 搭配更自然。</p><p>它通常：</p><ul><li>梯度信号更合适</li><li>收敛更快</li><li>对概率建模更合理</li><li>更适合最大似然训练</li></ul><p>MSE 虽然也能用于某些分类场景，但通常不是首选。</p><ol><li>简单例子</li></ol><p>回归任务：</p><pre><code>预测温度：28℃<br/>真实温度：30℃</code></pre><p>用 MSE 更合适。</p><p>分类任务：</p><pre><code>预测图片类别：猫 / 狗 / 鸟<br/>真实类别：猫</code></pre><p>用 Cross Entropy 更合适。</p><ol><li><strong>面试背诵版：MSE Loss 和 Cross Entropy Loss 有什么区别？</strong></li></ol><blockquote><p>MSE Loss 和 Cross Entropy Loss 的主要区别在于适用任务和优化目标不同。<strong>MSE 是均方误差，主要用于回归任务</strong>，衡量预测连续数值和真实数值之间的差距，比如房价、温度、销量预测。它会对误差做平方，因此对大误差惩罚更重。</p><p><strong>Cross Entropy Loss 主要用于分类任务</strong>，衡量模型预测的类别概率分布和真实类别之间的差距。对于真实类别，模型给出的概率越高，交叉熵越小；如果真实类别概率很低，损失会很大。它通常和 sigmoid 或 softmax 搭配使用，更适合概率建模和最大似然训练。</p><p>简单说，MSE 关心“预测数值差多少”，适合回归；Cross Entropy 关心“正确类别概率有多高”，适合分类。分类任务中通常优先使用交叉熵，因为它的梯度信号和概率解释更自然，收敛效果也更好。</p></blockquote>"},{"t":"为什么分类任务通常使用Cross Entropy？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>分类任务通常使用 Cross Entropy，核心原因是：<strong>分类本质是在预测类别概率分布，而交叉熵正好衡量预测概率分布和真实类别之间的差距。</strong></p><ol><li>分类任务输出的是概率</li></ol><span>[原文档此处有配图]</span><p>分类模型通常输出每个类别的概率。</p><p>比如图片分类：</p><pre><code>猫：0.8<br/>狗：0.15<br/>鸟：0.05</code></pre><p>真实类别如果是猫，我们希望模型给“猫”的概率越高越好。</p><p>Cross Entropy 正好优化这个目标：</p><pre><code>真实类别概率越高，loss 越小</code></pre><ol><li>Cross Entropy 惩罚自信的错误</li></ol><p>如果真实类别是猫，但模型预测：</p><pre><code>猫：0.01<br/>狗：0.99</code></pre><p>模型非常自信但错了。</p><p>Cross Entropy 会给很大的惩罚。</p><p>这很符合分类任务需求：</p><pre><code>错得越自信，惩罚越大</code></pre><ol><li>和最大似然估计一致</li></ol><p>分类任务可以看成让模型最大化真实类别的概率。</p><p>也就是：</p><pre><code>让 P(真实类别 | 输入) 尽可能大</code></pre><p>最小化 Cross Entropy，本质上等价于最大化真实标签的似然概率。</p><p>所以它有很好的概率解释。</p><ol><li>和 Softmax / Sigmoid 搭配自然</li></ol><p>多分类常用：</p><pre><code>Softmax + Cross Entropy</code></pre><p>二分类常用：</p><pre><code>Sigmoid + Binary Cross Entropy</code></pre><p>Softmax 把 logits 转成概率分布，Cross Entropy 衡量这个概率分布和真实 one-hot 标签的差异。</p><p>这个组合梯度形式好，训练稳定，收敛速度通常也更好。</p><ol><li>比 MSE 更适合分类</li></ol><p>如果用 MSE 做分类，模型优化的是：</p><pre><code>预测概率和 one-hot 标签的数值距离</code></pre><p>但分类更关心：</p><pre><code>真实类别概率是否足够大</code></pre><p>MSE 在概率接近 0 或 1 时，梯度可能不如交叉熵有效，训练可能更慢。</p><p>Cross Entropy 对错误类别概率高、正确类别概率低的情况惩罚更直接。</p><ol><li>适合多分类和语言模型</li></ol><p>Cross Entropy 不只用于普通分类。</p><p>也用于：</p><ul><li>文本分类</li><li>图像分类</li><li>意图识别</li><li>next token prediction</li><li>机器翻译</li><li>语言模型训练</li></ul><p>大语言模型预测下一个 token，本质也是在词表上做多分类，所以也常用交叉熵。</p><ol><li><strong>面试背诵版：为什么分类任务通常使用 Cross Entropy？</strong></li></ol><blockquote><p>分类任务通常使用 Cross Entropy，是因为分类模型的目标是预测每个类别的概率分布，而交叉熵正好衡量预测概率分布和真实标签分布之间的差距。对于真实类别，模型给出的概率越高，Cross Entropy 越小；如果模型对错误类别非常自信，而真实类别概率很低，Cross Entropy 会给出很大的惩罚。</p><p>从概率角度看，最小化 Cross Entropy 等价于最大化真实类别的似然概率，因此它和分类任务的目标是一致的。工程上，多分类通常使用 Softmax + Cross Entropy，二分类使用 Sigmoid + Binary Cross Entropy，这种组合梯度性质好、训练稳定、收敛较快。</p><p>相比 MSE，Cross Entropy 更直接优化正确类别的概率，对自信错误的惩罚更强，所以更适合分类任务。大语言模型的 next token prediction 本质上也是在词表上做多分类，因此也常用交叉熵损失。</p></blockquote>"},{"t":"KL散度是什么？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>KL 散度的核心是：衡量两个概率分布之间差异的指标，表示用一个分布去近似另一个分布时损失了多少信息。</p><ol><li>KL 散度衡量什么？</li></ol><p>KL 散度通常写作：</p><pre><code>D_KL(P || Q)</code></pre><p>它表示：</p><pre><code>用 Q 分布去近似真实分布 P，会产生多少信息损失</code></pre><p>其中：</p><ul><li>P：真实分布</li><li>Q：模型预测分布</li></ul><p>如果 P 和 Q 越接近，KL 散度越小。</p><p>如果 P 和 Q 差得越多，KL 散度越大。</p><ol><li>直观理解</li></ol><p>假设真实分布 P 是：</p><pre><code>猫：0.9<br/>狗：0.1</code></pre><p>模型预测 Q 是：</p><pre><code>猫：0.8<br/>狗：0.2</code></pre><p>这两个分布比较接近，KL 散度较小。</p><p>如果模型预测成：</p><pre><code>猫：0.1<br/>狗：0.9</code></pre><p>和真实分布差很多，KL 散度就大。</p><ol><li>KL 散度不是距离</li></ol><p>这一点面试可以说一下。</p><p>虽然 KL 散度衡量分布差异，但它不是严格数学意义上的距离。</p><p>因为它不满足对称性：</p><pre><code>D_KL(P || Q) 不一定等于 D_KL(Q || P)</code></pre><p>也就是说，用 Q 近似 P，和用 P 近似 Q，结果可能不一样。</p><ol><li>和交叉熵的关系</li></ol><p>交叉熵可以拆成：</p><pre><code>Cross Entropy(P, Q) = Entropy(P) + KL(P || Q)</code></pre><p>其中：</p><ul><li>Entropy(P)：真实分布自身的不确定性</li><li>KL(P || Q)：预测分布和真实分布的差异</li></ul><p>训练时 P 是固定标签分布，所以 Entropy(P) 是常数。</p><p>因此最小化交叉熵，等价于最小化 KL 散度。</p><ol><li>在机器学习里有什么用？</li></ol><p>KL 散度常用于：</p><ul><li>分类模型训练</li><li>知识蒸馏</li><li>变分自编码器 VAE</li><li>强化学习策略约束</li><li>大模型对齐</li><li>概率分布拟合</li><li>语言模型输出分布比较</li></ul><p>比如知识蒸馏中，希望学生模型的输出分布接近教师模型：</p><pre><code>D_KL(teacher || student)</code></pre><ol><li>KL 散度越小越好吗？</li></ol><p>一般来说，如果目标是让 Q 逼近 P，那么 KL 越小越好。</p><p>当：</p><pre><code>P = Q</code></pre><p>KL 散度为 0。</p><p>但实际任务中要注意方向：</p><pre><code>D_KL(P || Q)<br/>D_KL(Q || P)</code></pre><p>方向不同，优化行为可能不同。</p><ol><li><strong>面试背诵版：KL 散度是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>KL 散度，也叫 Kullback-Leibler Divergence，是用来衡量两个概率分布差异的指标。通常写作 D_KL(P || Q)，表示用分布 Q 去近似真实分布 P 时产生的信息损失。P 和 Q 越接近，KL 散度越小；当两个分布完全相同时，KL 散度为 0。</p><p>需要注意的是，KL 散度虽然衡量分布差异，但它不是严格意义上的距离，因为它不满足对称性，也就是 D_KL(P || Q) 不一定等于 D_KL(Q || P)。在机器学习中，KL 散度常用于概率分布拟合、知识蒸馏、VAE、强化学习策略约束和大模型对齐等场景。</p><p>它和交叉熵也有关系：Cross Entropy(P, Q) = Entropy(P) + KL(P || Q)。当真实分布 P 固定时，Entropy(P) 是常数，所以最小化交叉熵等价于最小化预测分布 Q 和真实分布 P 之间的 KL 散度。</p></blockquote>"},{"t":"为什么大模型训练使用交叉熵？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>大模型训练使用交叉熵，核心原因是：<strong>大语言模型的训练目标是预测下一个 token，本质是一个超大词表上的分类问题，而交叉熵最适合优化“真实 token 的预测概率”。</strong></p><ol><li>大模型训练本质是预测下一个 token</li></ol><p>大语言模型通常做 next token prediction。</p><p>比如输入：</p><pre><code>中国的首都是</code></pre><p>目标 token 是：</p><pre><code>北京</code></pre><p>模型会在整个词表上输出概率分布：</p><pre><code>北京：0.75<br/>上海：0.05<br/>广州：0.02<br/>其他 token：...</code></pre><p>训练目标就是让真实 token 的概率越来越高。</p><p>这本质上是一个多分类任务。</p><ol><li>交叉熵适合分类概率建模</li></ol><p>交叉熵衡量的是：</p><pre><code>真实分布和模型预测分布之间的差异</code></pre><p>在 next token prediction 里：</p><ul><li>真实分布通常是 one-hot</li><li>模型输出是词表上的概率分布</li></ul><p>真实 token 概率越高，交叉熵越小。</p><p>如果模型给真实 token 很低概率，loss 就很大。</p><p>所以它正好符合语言模型训练目标。</p><ol><li>等价于最大化真实文本概率</li></ol><p>从概率角度看，大模型希望最大化训练语料的似然。</p><p>一段文本可以拆成：</p><pre><code>P(x1, x2, ..., xn) = P(x1) × P(x2|x1) × ... × P(xn|x1...x(n-1))</code></pre><p>也就是每一步预测下一个 token。</p><p>最大化整段文本的概率，等价于最大化每个真实 token 的条件概率。</p><p>最小化交叉熵，本质上就是最大化真实 token 的 log likelihood。</p><ol><li>梯度性质好，训练稳定</li></ol><p>交叉熵和 Softmax 搭配非常自然。</p><p>模型先输出 logits，再经过 Softmax 得到词表概率分布。</p><p>然后用交叉熵计算 loss。</p><p>这种组合：</p><ul><li>概率解释清楚</li><li>梯度信号直接</li><li>对错误预测惩罚明显</li><li>收敛效果好</li><li>工程实现成熟</li></ul><p>所以大规模训练中非常常用。</p><ol><li>对“自信错误”惩罚大</li></ol><p>如果真实 token 是“北京”，但模型预测：</p><pre><code>北京：0.001<br/>上海：0.9</code></pre><p>模型非常自信但错了。</p><p>交叉熵会给很大的 loss。</p><p>这能强迫模型降低错误 token 的概率，提高真实 token 的概率。</p><ol><li>和 KL 散度关系密切</li></ol><p>交叉熵可以写成：</p><pre><code>H(P, Q) = H(P) + KL(P || Q)</code></pre><p>其中：</p><ul><li>P 是真实分布</li><li>Q 是模型预测分布</li></ul><p>训练数据固定时，H(P) 是常数。</p><p>所以最小化交叉熵，等价于最小化真实分布和模型预测分布之间的 KL 散度。</p><p>也就是让模型预测分布尽量接近真实数据分布。</p><ol><li>为什么不用 MSE？</li></ol><p>MSE 更适合连续值回归。</p><p>如果用 MSE 训练 token 分类，本质上是在拟合概率数值差距，不如交叉熵直接优化真实 token 概率。</p><p>交叉熵对于分类任务：</p><ul><li>概率解释更自然</li><li>梯度更合适</li><li>收敛更快</li><li>对低概率真实类别惩罚更强</li></ul><p>所以大语言模型训练通常不用 MSE，而用交叉熵。</p><ol><li><strong>面试背诵版：为什么大模型训练使用交叉熵？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>大模型训练使用交叉熵，主要是因为大语言模型的预训练目标通常是 next token prediction，也就是根据前面的上下文预测下一个 token。模型会在整个词表上输出一个概率分布，而真实答案可以看作 one-hot 分布，所以这个任务本质上是一个超大规模多分类问题。交叉熵正好衡量真实 token 分布和模型预测分布之间的差异，真实 token 的预测概率越高，loss 越小。</p><p>从概率角度看，语言模型希望最大化训练文本的似然概率。一段文本可以分解成多个条件概率的乘积，也就是每一步都最大化真实下一个 token 的概率。最小化交叉熵等价于最大化真实 token 的 log likelihood，也等价于让模型预测分布尽量接近真实数据分布。交叉熵和 Softmax 搭配梯度性质好、训练稳定，并且会对自信但错误的预测给出较大惩罚。</p><p>相比 MSE，交叉熵更适合分类概率建模，能更直接优化真实类别概率，所以大语言模型训练通常使用交叉熵作为核心损失函数。</p></blockquote>"}]},{"no":"","title":"2.3 优化算法（都是重点哈）","questions":[{"t":"梯度下降原理是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>梯度下降的核心是：<strong>沿着损失函数下降最快的方向不断更新参数，让模型的 loss 逐步变小。</strong></p><ol><li>梯度下降解决什么问题？</li></ol><p>模型训练的目标是找到一组参数，让损失函数最小。</p><p>比如：</p><pre><code>loss 越小，预测越接近真实值</code></pre><p>但参数很多，很难直接求最优解。</p><p>梯度下降就是一种迭代优化方法：</p><pre><code>先随机初始化参数<br/>计算 loss<br/>根据梯度更新参数<br/>重复这个过程</code></pre><ol><li>梯度是什么？</li></ol><p>梯度可以理解成：</p><pre><code>函数在当前位置上升最快的方向</code></pre><p>如果我们想让 loss 变小，就要往梯度的反方向走。</p><p>所以更新方向是：</p><pre><code>负梯度方向</code></pre><p>这就是“下降”。</p><ol><li>参数怎么更新？</li></ol><p>核心公式是：</p><pre><code>θ = θ - η * ∇L(θ)</code></pre><p>其中：</p><ul><li>θ：模型参数</li><li>η：学习率</li><li>∇L(θ)：loss 对参数的梯度</li></ul><p>意思是：</p><pre><code>参数往 loss 下降的方向移动一小步</code></pre><ol><li>学习率是什么？</li></ol><p>学习率决定每次走多大步。</p><p>如果学习率太大：</p><ul><li>可能震荡</li><li>可能越过最优点</li><li>甚至 loss 发散</li></ul><p>如果学习率太小：</p><ul><li>收敛太慢</li><li>训练时间长</li><li>可能卡在局部区域</li></ul><p>所以学习率非常关键。</p><ol><li>举个直观例子</li></ol><p>把 loss 看成一座山。</p><p>模型参数所在的位置就是山上的一个点。</p><p>训练目标是走到山谷最低处。</p><p>梯度告诉你：</p><pre><code>哪里是上坡最快的方向</code></pre><p>所以你反着走：</p><pre><code>沿着下坡方向走</code></pre><p>每走一步重新看坡度，再继续走，直到接近谷底。</p><ol><li>梯度下降有哪些类型？</li></ol><p>常见有三种：</p><p>第一种，Batch Gradient Descent：</p><pre><code>每次用全部训练数据计算梯度</code></pre><p>优点是稳定，缺点是慢。</p><p>第二种，SGD：</p><pre><code>每次用一个样本计算梯度</code></pre><p>优点是快，缺点是噪声大。</p><p>第三种，Mini-batch Gradient Descent：</p><pre><code>每次用一小批样本计算梯度</code></pre><p>这是深度学习最常用的方式，兼顾效率和稳定性。</p><ol><li>为什么能训练神经网络？</li></ol><p>神经网络通过前向传播计算预测和 loss。</p><p>然后通过反向传播计算每个参数对 loss 的梯度。</p><p>最后用梯度下降更新参数。</p><p>流程是：</p><pre><code>前向传播 → 计算 loss → 反向传播求梯度 → 参数更新</code></pre><ol><li><strong>面试背诵版：梯度下降原理是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>梯度下降是一种迭代优化算法，用来最小化损失函数。模型训练时，先随机初始化参数，然后通过前向传播计算预测结果和 loss，再通过反向传播计算 loss 对每个参数的梯度。梯度表示 loss 在当前位置上升最快的方向，因此为了让 loss 下降，需要沿着负梯度方向更新参数。</p><p>核心更新公式是 θ = θ - η * ∇L(θ)，其中 θ 是模型参数，η 是学习率，∇L(θ) 是损失函数对参数的梯度。学习率决定每次更新的步长，太大会震荡甚至发散，太小会收敛很慢。</p><p>常见梯度下降方式包括 Batch Gradient Descent、SGD 和 Mini-batch Gradient Descent。深度学习里通常使用 Mini-batch，因为它在计算效率和梯度稳定性之间取得了平衡。简单说，梯度下降就是“计算 loss 变大的方向，然后反方向走一步，不断迭代让 loss 变小”。</p></blockquote>"},{"t":"SGD和Batch Gradient Descent有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>SGD 和 Batch Gradient Descent 的核心区别是：<strong>Batch 每次用全部数据算梯度，SGD 每次用一个样本算梯度。</strong></p><ol><li>Batch Gradient Descent 是什么？</li></ol><p>Batch Gradient Descent 每次参数更新时，会使用整个训练集计算梯度。</p><p>流程是：</p><pre><code>遍历全部训练数据<br/>  ↓<br/>计算整体 loss<br/>  ↓<br/>计算平均梯度<br/>  ↓<br/>更新一次参数</code></pre><p>也就是说，一轮完整数据只更新一次参数。</p><ol><li>SGD 是什么？</li></ol><p>SGD 全称是 Stochastic Gradient Descent，随机梯度下降。</p><p>它每次只随机取一个样本计算梯度并更新参数。</p><p>流程是：</p><pre><code>取一个样本<br/>  ↓<br/>计算这个样本的 loss<br/>  ↓<br/>计算梯度<br/>  ↓<br/>更新一次参数</code></pre><p>所以一轮数据会更新很多次参数。</p><ol><li>计算效率不同</li></ol><p>Batch Gradient Descent：</p><ul><li>梯度计算准确</li><li>但每次更新都要看全量数据</li><li>数据量大时很慢</li><li>内存占用高</li></ul><p>SGD：</p><ul><li>每次只看一个样本</li><li>更新速度快</li><li>适合大数据</li><li>内存占用低</li></ul><p>但单次梯度估计不稳定。</p><ol><li>梯度稳定性不同</li></ol><p>Batch 使用全部数据，梯度方向比较稳定。</p><p>SGD 只用一个样本，梯度方向噪声很大。</p><p>所以 SGD 的 loss 下降曲线可能抖动明显。</p><p>但这种噪声也有好处：</p><pre><code>可能帮助模型跳出局部最优或鞍点</code></pre><ol><li>收敛特点不同</li></ol><p>Batch Gradient Descent：</p><ul><li>每步方向稳定</li><li>收敛路径平滑</li><li>单步计算慢</li><li>大数据场景不现实</li></ul><p>SGD：</p><ul><li>单步计算快</li><li>更新频繁</li><li>收敛过程抖动</li><li>可能难以精确收敛到最优点</li></ul><p>实际深度学习里更常用 Mini-batch。</p><ol><li>Mini-batch 是折中方案</li></ol><p>Mini-batch Gradient Descent 每次用一小批样本。</p><p>比如：</p><pre><code>batch size = 32 / 64 / 128</code></pre><p>它结合了两者优点：</p><ul><li>比 Batch 快</li><li>比 SGD 稳定</li><li>适合 GPU 并行</li><li>深度学习最常用</li></ul><p>所以实际说“SGD 训练”，很多时候指的是 Mini-batch SGD。</p><ol><li>举个例子</li></ol><p>假设训练集有 10000 条数据。</p><p>Batch Gradient Descent：</p><pre><code>用 10000 条数据算一次梯度<br/>更新 1 次</code></pre><p>SGD：</p><pre><code>每次用 1 条数据算梯度<br/>更新 10000 次</code></pre><p>Mini-batch：</p><pre><code>每次用 100 条数据算梯度<br/>更新 100 次</code></pre><ol><li><strong>面试背诵版：SGD 和 Batch Gradient Descent 有什么区别？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Batch Gradient Descent 每次使用全部训练数据计算 loss 和梯度，然后更新一次参数。它的优点是梯度方向稳定、收敛路径平滑，但缺点是每次更新计算量大、内存占用高，在大数据集上训练很慢。</p><p>SGD，也就是随机梯度下降，每次只随机使用一个样本计算梯度并更新参数。它的优点是单次更新快、内存占用低、适合大规模数据，而且梯度噪声有时能帮助跳出局部最优；缺点是梯度方向不稳定，loss 曲线抖动大，收敛不如 Batch 平滑。</p><p>实际深度学习中最常用的是 Mini-batch Gradient Descent，每次用一小批样本计算梯度，比如 32、64 或 128 个样本，它在训练效率、梯度稳定性和 GPU 并行之间取得了平衡。简单说，Batch 是“全量数据更新一次”，SGD 是“一个样本更新一次”，Mini-batch 是“小批量数据更新一次”。</p></blockquote>"},{"t":"Momentum解决什么问题？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><ol><li>Momentum 解决什么问题？</li></ol><p>Momentum 主要解决普通梯度下降收敛慢、震荡大、容易在某些方向来回摆动的问题。</p><ol><li>普通梯度下降的问题</li></ol><p>普通梯度下降每次只看当前梯度方向。</p><p>更新公式大概是：</p><pre><code>参数 = 参数 - 学习率 × 当前梯度</code></pre><p>问题是，如果损失函数地形比较复杂，比如狭长山谷：</p><pre><code>一个方向坡度很陡<br/>另一个方向坡度很缓</code></pre><p>参数可能会在陡峭方向来回震荡，而在真正该前进的方向走得很慢。</p><ol><li>Momentum 是什么？</li></ol><p>Momentum 叫动量。</p><p>它借鉴物理里的惯性思想：</p><pre><code>不仅看当前梯度，还看过去梯度累积的方向。</code></pre><p>也就是说，参数更新时会保留之前的运动趋势。</p><p>如果连续几次梯度方向相似，Momentum 会加速前进。</p><p>如果某个方向梯度来回变化，Momentum 会抵消震荡。</p><ol><li>它解决的第一个问题：加速收敛</li></ol><p>在正确下降方向上，如果多个 batch 的梯度方向比较一致，Momentum 会累积速度。</p><p>这样参数更新会越来越有“惯性”。</p><p>效果是：</p><pre><code>沿着稳定方向加速下降</code></pre><p>所以收敛更快。</p><ol><li>它解决的第二个问题：减少震荡</li></ol><p>在不稳定方向上，如果梯度一会儿向左，一会儿向右，Momentum 会互相抵消。</p><p>比如：</p><pre><code>这一步向左<br/>下一步向右</code></pre><p>累积后这个方向的更新会变小。</p><p>效果是：</p><pre><code>减少来回摆动，让优化路径更平滑</code></pre><ol><li>直观例子</li></ol><p>把梯度下降想象成小球下山。</p><p>普通梯度下降像：</p><pre><code>每一步都只根据当前坡度走</code></pre><p>Momentum 像：</p><pre><code>一个有惯性的小球往下滚</code></pre><p>它不会因为局部小坑小坡马上改变方向，而是沿着整体下降方向继续前进。</p><ol><li>简单公式</li></ol><p>Momentum 会维护一个速度项：</p><pre><code>v = βv + gradient<br/>参数 = 参数 - learning_rate × v</code></pre><p>其中：</p><ul><li>v：历史梯度累积形成的速度</li><li>β：动量系数，常见如 0.9</li><li>gradient：当前梯度</li></ul><p>意思是：</p><pre><code>当前更新方向 = 历史方向 + 当前梯度</code></pre><ol><li>和 SGD 的关系</li></ol><p>SGD 梯度噪声比较大。</p><p>Momentum 可以让 SGD：</p><ul><li>更新方向更稳定</li><li>收敛更快</li><li>震荡更少</li><li>更容易穿过平坦区域</li><li>对局部噪声不那么敏感</li></ul><p>所以 SGD + Momentum 是很经典的优化组合。</p><ol><li><strong>面试背诵版：Momentum 解决什么问题？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Momentum 主要解决普通梯度下降尤其是 SGD 在优化过程中收敛慢、震荡大、容易在狭长峡谷中来回摆动的问题。普通梯度下降每次只根据当前梯度更新参数，如果某些方向梯度变化很大，就会导致参数反复震荡；而在真正需要前进的方向上，更新可能又比较慢。</p><p>Momentum 引入了历史梯度的累积，相当于给参数更新加上“惯性”。如果连续多次梯度方向一致，动量会不断累积，使模型在稳定下降方向上加速；如果某个方向梯度来回变化，历史梯度会相互抵消，从而减少震荡。</p><p>简单说，Momentum 的作用是“沿着长期一致的方向加速，抑制来回摆动的方向”，让优化过程更平滑、更快、更稳定。</p></blockquote>"},{"t":"Adam优化器原理是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Adam 的核心是：<strong>结合 Momentum 和自适应学习率，用一阶矩估计方向，用二阶矩估计梯度尺度，让每个参数都有自己的动态学习率。</strong></p><ol><li>Adam 是什么？</li></ol><p>Adam 全称是 Adaptive Moment Estimation。</p><p>它是一种非常常用的优化器。</p><p>可以简单理解为：</p><pre><code>Adam = Momentum + RMSProp</code></pre><p>它既考虑历史梯度方向，又根据梯度大小自动调整学习率。</p><ol><li>Adam 解决什么问题？</li></ol><p>普通 SGD 的问题是：</p><ul><li>学习率需要手动调</li><li>不同参数适合的学习率可能不同</li><li>梯度噪声大</li><li>收敛慢</li><li>容易震荡</li></ul><p>Adam 通过自适应学习率，让不同参数根据自己的梯度情况动态调整更新步长。</p><ol><li>一阶矩：类似 Momentum</li></ol><p>Adam 会维护梯度的一阶矩估计。</p><p>可以理解成：</p><pre><code>历史梯度的指数移动平均</code></pre><p>它反映的是：</p><pre><code>过去一段时间梯度的大致方向</code></pre><p>这和 Momentum 很像。</p><p>作用是：</p><ul><li>平滑梯度方向</li><li>减少噪声</li><li>加速稳定方向收敛</li></ul><ol><li>二阶矩：控制学习率</li></ol><p>Adam 还会维护梯度平方的二阶矩估计。</p><p>可以理解成：</p><pre><code>历史梯度平方的指数移动平均</code></pre><p>它反映的是：</p><pre><code>这个参数的梯度幅度有多大</code></pre><p>如果某个参数梯度经常很大，Adam 会降低它的更新步长。</p><p>如果某个参数梯度较小，Adam 会相对增大它的更新步长。</p><ol><li>更新直觉</li></ol><p>Adam 的更新可以理解为：</p><pre><code>更新方向看一阶矩<br/>更新步长看二阶矩</code></pre><p>也就是：</p><ul><li>一阶矩决定往哪走</li><li>二阶矩决定走多大</li></ul><p>所以它比普通 SGD 更稳定，也更不依赖手动调学习率。</p><ol><li>偏差修正</li></ol><p>Adam 初始化时，一阶矩和二阶矩都是 0。</p><p>训练早期它们会偏小。</p><p>所以 Adam 会做 bias correction，也就是偏差修正。</p><p>作用是：</p><pre><code>让训练初期的一阶矩和二阶矩估计更准确</code></pre><p>这有助于稳定训练。</p><ol><li>Adam 的优点</li></ol><p>Adam 的优点：</p><ul><li>收敛快</li><li>对学习率不那么敏感</li><li>适合稀疏梯度</li><li>每个参数有自适应学习率</li><li>训练初期表现稳定</li><li>工程上很好用</li></ul><p>所以深度学习里非常常见。</p><ol><li>Adam 的缺点</li></ol><p>Adam 也不是万能的。</p><p>可能的问题：</p><ul><li>泛化能力有时不如 SGD + Momentum</li><li>可能收敛到泛化较差的解</li><li>权重衰减需要正确实现</li><li>大模型训练里常用 AdamW 而不是原始 Adam</li></ul><p>AdamW 把 weight decay 和梯度更新解耦，更适合 Transformer 和大模型训练。</p><ol><li><strong>面试背诵版：Adam 优化器原理是什么？</strong></li></ol><blockquote><p>Adam，全称 Adaptive Moment Estimation，可以理解为结合了 Momentum 和 RMSProp 的优化器。它同时维护梯度的一阶矩和二阶矩：一阶矩是历史梯度的指数移动平均，用来表示稳定的更新方向，类似 Momentum；二阶矩是历史梯度平方的指数移动平均，用来估计每个参数的梯度尺度，从而为不同参数自适应调整学习率。</p><p>直观来说，Adam 是“用一阶矩决定往哪走，用二阶矩决定走多大步”。如果某个参数梯度长期较大，Adam 会减小它的更新步长；如果某个参数梯度较小，则会相对增大更新步长。为了避免训练初期一阶矩和二阶矩因为初始化为 0 而偏小，Adam 还会做偏差修正。</p><p>Adam 的优点是收敛快、训练稳定、对学习率不太敏感，适合稀疏梯度和大规模深度学习任务。但它有时泛化不如 SGD + Momentum，而且在 Transformer 和大模型训练中通常更常用 AdamW，因为 AdamW 对 weight decay 的处理更合理。</p></blockquote>"},{"t":"Adam和AdamW有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Adam 和 AdamW 的核心区别是：<strong>Adam 把权重衰减混在梯度更新里，AdamW 把权重衰减和梯度更新解耦，所以 AdamW 的正则化效果更稳定、更适合大模型。</strong></p><ol><li>Adam 是什么？</li></ol><p>Adam 是自适应优化器。</p><p>它会维护：</p><ul><li>一阶矩：历史梯度平均，类似 Momentum</li><li>二阶矩：历史梯度平方平均，用来调整学习率</li></ul><p>所以 Adam 可以理解为：</p><pre><code>更新方向看一阶矩<br/>更新步长看二阶矩</code></pre><p>它收敛快，对学习率不太敏感。</p><ol><li>Weight Decay 是什么？</li></ol><p>Weight Decay 叫权重衰减。</p><p>作用是让参数不要变得过大，起到正则化效果。</p><p>直观理解：</p><pre><code>每次更新时，让权重稍微变小一点</code></pre><p>这样可以减少过拟合，提升泛化。</p><ol><li>Adam 里的问题</li></ol><p>在传统 Adam 里，如果直接把 L2 正则加到 loss 或梯度里：</p><pre><code>gradient = gradient + λ * weight</code></pre><p>这个正则项会进入 Adam 的一阶矩和二阶矩计算。</p><p>问题是 Adam 会对不同参数做自适应学习率缩放。</p><p>所以权重衰减也被自适应缩放了。</p><p>导致：</p><pre><code>不同参数的衰减强度不一致</code></pre><p>正则化效果不够干净。</p><ol><li>AdamW 怎么做？</li></ol><p>AdamW 的 W 就是 Weight Decay。</p><p>它把 weight decay 从梯度更新里拆出来。</p><p>也就是先按 Adam 的方式用梯度更新参数，再额外做权重衰减：</p><pre><code>参数 = 参数 - Adam梯度更新<br/>参数 = 参数 - 学习率 × λ × 参数</code></pre><p>核心是：</p><pre><code>梯度更新归梯度更新<br/>权重衰减归权重衰减</code></pre><p>这叫 decoupled weight decay，解耦权重衰减。</p><ol><li>为什么 AdamW 更好？</li></ol><p>因为 AdamW 的权重衰减不会被 Adam 的自适应学习率干扰。</p><p>好处是：</p><ul><li>正则化更稳定</li><li>超参数更好调</li><li>泛化效果更好</li><li>更适合 Transformer</li><li>更适合大模型训练</li></ul><p>所以现在训练 Transformer、BERT、GPT 这类模型时，AdamW 非常常用。</p><ol><li>一句话区分</li></ol><p>可以这样记：</p><pre><code>Adam：L2 正则混在梯度里，会被自适应学习率影响<br/>AdamW：Weight Decay 独立作用在参数上，不参与梯度矩估计</code></pre><ol><li>面试容易追问：AdamW 是不是 Adam + L2？</li></ol><p>严格说，不是完全一样。</p><p>Adam + L2 是把正则项加到梯度里。</p><p>AdamW 是直接对参数做衰减。</p><p>在 SGD 里，L2 和 weight decay 很接近。</p><p>但在 Adam 这种自适应优化器里，两者不等价。</p><p>这是重点。</p><ol><li><strong>面试背诵版：Adam 和 AdamW 有什么区别？</strong></li></ol><blockquote><p>Adam 和 AdamW 的主要区别在于 weight decay 的处理方式。Adam 是一种自适应优化器，会根据梯度的一阶矩和二阶矩为不同参数调整更新步长。如果在 Adam 中直接使用 L2 正则，相当于把 λw 加到梯度里，这个正则项会参与 Adam 的一阶矩和二阶矩计算，并被自适应学习率缩放，导致不同参数的权重衰减强度不一致。</p><p>AdamW 则采用 decoupled weight decay，也就是把权重衰减从梯度更新中解耦出来。它先按照 Adam 的方式基于梯度更新参数，再单独对参数做 weight decay。这样权重衰减不会被自适应学习率干扰，正则化效果更稳定，超参数更容易调，泛化通常更好。</p><p>简单说，Adam 是“把 L2 正则混进梯度里”，AdamW 是“梯度更新和权重衰减分开做”。在 SGD 中 L2 和 weight decay 很接近，但在 Adam 这类自适应优化器中二者不等价，所以训练 Transformer 和大模型时通常更推荐 AdamW。</p></blockquote>"},{"t":"学习率为什么影响模型训练？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>学习率影响模型训练，核心是：它<strong>决定每次参数更新走多大步，步子太大容易发散，步子太小收敛太慢。</strong></p><ol><li>学习率是什么？</li></ol><p>在梯度下降里，参数更新公式是：</p><pre><code>θ = θ - η * ∇L(θ)</code></pre><p>其中：</p><ul><li>θ：模型参数</li><li>η：学习率</li><li>∇L(θ)：梯度</li></ul><p>学习率 η 决定：</p><pre><code>每次沿负梯度方向走多远</code></pre><p>所以它直接影响训练速度和稳定性。</p><ol><li>学习率太大有什么问题？</li></ol><p>如果学习率太大，参数每次更新步子过大。</p><p>可能导致：</p><ul><li>loss 震荡</li><li>越过最优点</li><li>无法收敛</li><li>训练发散</li><li>出现 NaN</li><li>模型效果不稳定</li></ul><p>比如你下山时一步迈太大，可能直接跨过山谷，又跑到另一边。</p><ol><li>学习率太小有什么问题？</li></ol><p>如果学习率太小，参数更新太慢。</p><p>可能导致：</p><ul><li>收敛速度很慢</li><li>训练时间很长</li><li>卡在局部区域</li><li>很难走出平坦区</li><li>计算资源浪费</li><li>在有限训练轮数内欠拟合</li></ul><p>比如你下山时每次只挪一点点，虽然稳，但太慢。</p><ol><li>合适学习率的作用</li></ol><p>合适学习率可以让训练：</p><ul><li>loss 稳定下降</li><li>收敛速度较快</li><li>不容易发散</li><li>泛化效果更好</li><li>训练资源利用更高</li></ul><p>所以学习率通常是最重要的超参数之一。</p><ol><li>学习率还影响最终效果</li></ol><p>学习率不只影响速度，也会影响模型收敛到什么解。</p><p>较大学习率可能帮助跳出局部最优或尖锐极小值。</p><p>较小学习率有助于后期精细收敛。</p><p>所以很多训练会用学习率调度：</p><pre><code>前期大学习率探索<br/>后期小学习率收敛</code></pre><ol><li>常见学习率策略</li></ol><p>常见策略包括：</p><ul><li>固定学习率</li><li>Step Decay</li><li>Exponential Decay</li><li>Cosine Decay</li><li>Warmup</li><li>ReduceLROnPlateau</li></ul><p>大模型训练常见：</p><pre><code>Warmup + Cosine Decay</code></pre><p>先慢慢升高学习率，避免训练初期不稳定；再逐渐降低学习率，让模型稳定收敛。</p><ol><li>和优化器有关</li></ol><p>不同优化器对学习率敏感程度不同。</p><p>比如：</p><ul><li>SGD 对学习率比较敏感</li><li>Adam / AdamW 自适应能力强一些</li><li>但 AdamW 仍然需要合适学习率</li></ul><p>学习率不合适，再好的优化器也可能训练不好。</p><ol><li><strong>面试背诵版：学习率为什么影响模型训练？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>学习率是梯度下降中控制参数更新步长的超参数。在参数更新公式 θ = θ - η * ∇L(θ) 中，η 就是学习率，它决定模型每次沿负梯度方向移动多远。因此学习率会直接影响训练速度、收敛稳定性和最终模型效果。</p><p>如果学习率太大，参数更新步子过大，可能越过最优点，导致 loss 震荡、无法收敛，甚至训练发散或出现 NaN；如果学习率太小，参数更新太慢，训练时间变长，可能在有限 epoch 内欠拟合，也可能卡在平坦区域。合适的学习率可以让 loss 稳定下降，并在速度和稳定性之间取得平衡。</p><p>实际训练中通常会使用学习率调度，比如 Warmup、Step Decay、Cosine Decay 等。大模型训练常用 Warmup + Cosine Decay，前期逐步升高学习率避免不稳定，后期逐步降低学习率帮助精细收敛。简单说，学习率决定“每一步走多大”，走太大容易发散，走太小收敛太慢。</p></blockquote>"},{"t":"Warmup为什么有效？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Warmup 有效的核心原因是：<strong>训练初期模型参数还不稳定、梯度噪声大，如果一开始就用大学习率，容易震荡或发散；Warmup 通过先用小学习率、再逐步升高，让训练更稳定。</strong></p><ol><li>Warmup 是什么？</li></ol><p>Warmup 指训练刚开始时，不直接使用目标学习率。</p><p>而是先从一个很小的学习率开始，然后逐步升高。</p><p>比如：</p><pre><code>前 1000 step：<br/>学习率从 0 线性升到 1e-4</code></pre><p>之后再进入正常学习率调度，比如 cosine decay。</p><p>常见策略是：</p><pre><code>Warmup + Cosine Decay</code></pre><ol><li>为什么训练初期不稳定？</li></ol><p>训练刚开始时：</p><ul><li>参数是随机初始化的</li><li>模型输出很不稳定</li><li>loss 可能比较大</li><li>梯度噪声比较大</li><li>Adam 的一阶矩、二阶矩估计还不准</li><li>大 batch / 分布式训练更容易不稳定</li></ul><p>如果此时学习率太大，参数会被大幅更新，可能导致：</p><ul><li>loss 剧烈震荡</li><li>梯度爆炸</li><li>训练发散</li><li>出现 NaN</li><li>模型进入很差的区域</li></ul><ol><li>Warmup 如何稳定训练？</li></ol><p>Warmup 相当于让模型“慢慢起步”。</p><p>初期用小学习率：</p><pre><code>先小步更新参数</code></pre><p>等模型输出、梯度统计和优化器状态逐渐稳定后，再升到目标学习率。</p><p>这样可以避免训练一开始走太大步。</p><ol><li>对 Adam / AdamW 特别有用</li></ol><p>Adam / AdamW 会维护一阶矩和二阶矩：</p><pre><code>一阶矩：梯度平均<br/>二阶矩：梯度平方平均</code></pre><p>训练刚开始时，这些统计量还不稳定。</p><p>如果直接用大学习率，自适应更新可能不可靠。</p><p>Warmup 给优化器一段时间积累更稳定的梯度统计。</p><p>所以 Transformer 和大模型训练经常使用 Warmup。</p><ol><li>对大模型训练更重要</li></ol><p>大模型训练更容易不稳定，因为：</p><ul><li>参数量大</li><li>batch 大</li><li>loss scale 敏感</li><li>梯度分布复杂</li><li>分布式训练误差更多</li><li>混合精度训练可能溢出</li></ul><p>Warmup 能降低训练早期发散风险，让大模型更平稳进入训练状态。</p><ol><li>Warmup 不是让模型效果神奇变好</li></ol><p>Warmup 主要解决的是训练稳定性问题。</p><p>它的价值是：</p><ul><li>防止初期发散</li><li>减少 loss 震荡</li><li>稳定优化器状态</li><li>让后续大学习率更安全</li><li>提高训练成功率</li></ul><p>但 warmup 太长也不好。</p><p>如果 warmup 太长，学习率长期太小，会导致训练效率低。</p><ol><li>怎么设置 Warmup？</li></ol><p>常见设置方式：</p><ul><li>固定 warmup steps，比如 500、1000、4000 step</li><li>按总 step 比例，比如前 1% 到 5%</li><li>大模型、大 batch 通常需要更谨慎的 warmup</li></ul><p>实际要结合：</p><ul><li>模型规模</li><li>batch size</li><li>学习率</li><li>优化器</li><li>是否混合精度</li><li>loss 是否稳定</li></ul><ol><li><strong>面试背诵版：Warmup 为什么有效？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Warmup 有效的原因是训练初期模型参数、梯度分布和优化器状态都不稳定，如果一开始就使用较大的学习率，参数可能被大幅更新，导致 loss 震荡、梯度爆炸、训练发散甚至出现 NaN。Warmup 通过在训练前期使用较小学习率，并逐步升高到目标学习率，让模型先以较小步长稳定进入训练状态。</p><p>对于 Adam / AdamW 这类自适应优化器，训练初期一阶矩和二阶矩估计还不准确，Warmup 可以给优化器时间积累稳定的梯度统计，避免早期更新过激。对于 Transformer 和大模型训练，参数量大、梯度分布复杂、batch 大、混合精度和分布式训练都更容易带来不稳定，因此 Warmup 尤其重要。</p><p>简单说，Warmup 的作用是“训练刚开始先慢一点，等模型和优化器稳定后再加速”。它主要提升训练稳定性，降低早期发散风险，但 warmup 太长也会降低训练效率，所以通常会和 Cosine Decay 等学习率调度一起使用。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"3.1 神经网络","questions":[{"t":"神经网络为什么能够拟合复杂函数？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>神经网络能够拟合复杂函数，核心原因是：<strong>它通过多层线性变换和非线性激活的组合，可以表示非常复杂的输入输出映射。</strong></p><ol><li>神经网络本质是函数逼近器</li></ol><p>神经网络可以看作一个函数：</p><pre><code>y = f(x)</code></pre><p>输入是 x，输出是 y。</p><p>训练过程就是不断调整参数，让这个函数尽量接近真实规律。</p><p>比如：</p><pre><code>图片 → 类别<br/>文本 → 标签<br/>用户特征 → 点击概率</code></pre><p>本质都是在学习输入到输出的映射关系。</p><ol><li>单层线性模型表达能力有限</li></ol><p>如果只有线性变换：</p><pre><code>y = Wx + b</code></pre><p>它只能表示线性关系。</p><p>比如：</p><pre><code>输入变大，输出按固定比例变化</code></pre><p>但现实问题往往是非线性的，比如：</p><ul><li>图像识别</li><li>语音识别</li><li>自然语言理解</li><li>用户行为预测</li><li>复杂决策边界</li></ul><p>单纯线性模型不够。</p><ol><li>非线性激活很关键</li></ol><p>神经网络每层通常是：</p><pre><code>线性变换 + 非线性激活</code></pre><p>比如：</p><pre><code>h = ReLU(Wx + b)</code></pre><p>如果没有非线性激活，多层线性网络叠加后仍然等价于一个线性变换。</p><p>也就是说：</p><pre><code>线性 + 线性 + 线性 = 线性</code></pre><p>有了 ReLU、Sigmoid、Tanh、GELU 这类非线性函数后，模型才能表达复杂非线性关系。</p><ol><li>多层结构逐级抽象</li></ol><p>神经网络的层数越深，可以学习越抽象的特征。</p><p>比如图像任务：</p><pre><code>低层：边缘、颜色<br/>中层：纹理、局部形状<br/>高层：物体部件、整体类别</code></pre><p>语言任务也是类似：</p><pre><code>低层：词法、位置<br/>中层：语法、短语关系<br/>高层：语义、意图、推理模式</code></pre><p>多层组合让模型能表示复杂结构。</p><ol><li>参数多，表达空间大</li></ol><p>神经网络有大量参数。</p><p>这些参数决定了模型可以表示的函数空间。</p><p>参数越多，网络越宽、越深，理论上能表示的函数越复杂。</p><p>这也是为什么大模型通常能力更强。</p><p>但参数多也容易过拟合，所以需要数据、正则化和合理训练。</p><ol><li>万能逼近定理</li></ol><p>理论上，神经网络有万能逼近能力。</p><p>意思是：</p><pre><code>只要网络足够宽，并且有非线性激活，<br/>就可以逼近任意连续函数到任意精度。</code></pre><p>当然这是理论结论。</p><p>实际效果还取决于：</p><ul><li>数据量</li><li>网络结构</li><li>优化算法</li><li>训练是否充分</li><li>泛化能力</li></ul><ol><li>组合性带来强表达能力</li></ol><p>神经网络强大不只是因为参数多，还因为它能组合简单模式形成复杂模式。</p><p>比如：</p><pre><code>线条 → 形状 → 物体<br/>词语 → 短语 → 句子语义<br/>函数片段 → 复杂决策边界</code></pre><p>这种组合表达让深层网络比浅层模型更高效地表示复杂函数。</p><ol><li><strong>面试背诵版：神经网络为什么能够拟合复杂函数？</strong></li></ol><span>[原文档此处有配图]</span>"},{"t":"前向传播过程是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>前向传播的核心是：<strong>输入数据从输入层开始，经过神经网络一层层计算，最后得到模型预测结果和 loss。</strong></p><ol><li>前向传播是做什么的？</li></ol><p>前向传播就是模型“根据当前参数做一次预测”的过程。</p><p>比如输入一张图片，模型输出：</p><pre><code>猫：0.8<br/>狗：0.2</code></pre><p>或者输入一句话，模型输出下一个 token 的概率分布。</p><p>前向传播回答的是：</p><pre><code>当前模型会预测什么？<br/>预测得和真实答案差多少？</code></pre><ol><li>每一层怎么计算？</li></ol><p>神经网络每一层通常包含两步：</p><pre><code>线性变换 + 非线性激活</code></pre><p>公式是：</p><pre><code>z = Wx + b<br/>a = activation(z)</code></pre><p>其中：</p><ul><li>x：上一层输入</li><li>W：权重参数</li><li>b：偏置</li><li>z：线性变换结果</li><li>a：激活后的输出</li></ul><p>然后 a 会作为下一层的输入继续往后传。</p><ol><li>多层网络的流程</li></ol><p>整体流程可以写成：</p><pre><code>输入 x<br/>  ↓<br/>第 1 层计算<br/>  ↓<br/>第 2 层计算<br/>  ↓<br/>第 3 层计算<br/>  ↓<br/>输出层<br/>  ↓<br/>得到预测 y_hat<br/>  ↓<br/>计算 loss</code></pre><p>比如分类任务中：</p><pre><code>输入特征 → 多层网络 → logits → softmax → 类别概率 → cross entropy loss</code></pre><ol><li>在大语言模型里的前向传播</li></ol><p>大模型里的前向传播大概是：</p><pre><code>输入文本<br/>  ↓<br/>Tokenizer 切成 token<br/>  ↓<br/>Embedding 转成向量<br/>  ↓<br/>经过多层 Transformer<br/>  ↓<br/>输出 logits<br/>  ↓<br/>Softmax 得到词表概率<br/>  ↓<br/>和真实 next token 计算交叉熵 loss</code></pre><p>比如输入：</p><pre><code>中国的首都是</code></pre><p>模型会预测下一个 token，可能是：</p><pre><code>北京</code></pre><ol><li>前向传播会保存中间结果</li></ol><p>训练时，前向传播不只是得到预测。</p><p>它还会保存一些中间值，比如：</p><ul><li>每层输入</li><li>每层输出</li><li>激活值</li><li>logits</li><li>attention 结果</li><li>loss</li></ul><p>这些中间结果后面反向传播要用。</p><p>因为反向传播需要根据计算图一层层求梯度。</p><ol><li>推理和训练中的区别</li></ol><p>推理时：</p><pre><code>前向传播 → 得到输出</code></pre><p>一般不需要保存完整计算图。</p><p>训练时：</p><pre><code>前向传播 → 计算 loss → 保存中间结果 → 反向传播</code></pre><p>所以训练比推理更耗显存。</p><ol><li><strong>面试背诵版：前向传播过程是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>前向传播是神经网络从输入到输出的计算过程。输入数据进入模型后，会按照网络结构一层层向后传递，每一层通常先进行线性变换，比如 z = Wx + b，再经过非线性激活函数得到输出，并作为下一层输入，直到最后得到模型预测结果。</p><p>对于分类任务，模型通常会输出 logits，再经过 softmax 得到类别概率，并和真实标签计算交叉熵 loss。对于大语言模型，输入文本会先被 tokenizer 切成 token，再转成 embedding，经过多层 Transformer 后输出每个位置的 logits，用来预测下一个 token，并计算 next token 的交叉熵损失。</p><p>训练阶段的前向传播还会保存中间激活值和计算图，供反向传播计算梯度使用。简单说，前向传播就是“输入经过模型一层层计算，得到预测结果和 loss”的过程。</p></blockquote>"},{"t":"反向传播过程是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>前向传播的核心是：<strong>输入数据从网络输入层开始，经过一层层计算，最终得到预测结果和损失值。</strong></p><ol><li>前向传播做什么？</li></ol><p>前向传播就是模型“算答案”的过程。</p><p>比如输入一张图片，模型经过多层神经网络，最后输出：</p><pre><code>猫：0.8<br/>狗：0.2</code></pre><p>如果是真实标签是猫，再计算预测和真实标签之间的 loss。</p><ol><li>基本流程</li></ol><p>神经网络每一层通常做：</p><pre><code>线性变换 → 非线性激活</code></pre><p>比如：</p><pre><code>z = Wx + b<br/>a = activation(z)</code></pre><p>其中：</p><ul><li>x：上一层输入</li><li>W：权重</li><li>b：偏置</li><li>z：线性输出</li><li>a：激活后的输出</li></ul><p>然后 a 作为下一层输入继续传下去。</p><ol><li>多层网络前向传播</li></ol><p>整体可以理解为：</p><pre><code>输入 x<br/>  ↓<br/>第 1 层：W1x + b1 + 激活<br/>  ↓<br/>第 2 层：W2h1 + b2 + 激活<br/>  ↓<br/>第 3 层：W3h2 + b3<br/>  ↓<br/>输出预测 y_hat<br/>  ↓<br/>计算 loss</code></pre><p>前向传播结束后，模型知道自己预测得怎么样。</p><ol><li>在大模型里的前向传播</li></ol><p>大语言模型里，前向传播大概是：</p><pre><code>输入文本<br/>  ↓<br/>Tokenize<br/>  ↓<br/>Embedding<br/>  ↓<br/>多层 Transformer<br/>  ↓<br/>输出每个位置的 logits<br/>  ↓<br/>Softmax 得到 token 概率<br/>  ↓<br/>计算 next token loss</code></pre><p>比如输入：</p><pre><code>中国的首都是</code></pre><p>模型输出下一个 token 的概率分布。</p><ol><li>前向传播的结果有什么用？</li></ol><p>前向传播会产生：</p><ul><li>模型预测值</li><li>中间激活值</li><li>loss</li><li>logits / probability</li><li>hidden states</li></ul><p>这些不只是为了输出答案。</p><p>中间结果还会在反向传播时用来计算梯度。</p><ol><li><strong>面试背诵版：前向传播过程是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>前向传播是神经网络从输入到输出的计算过程。输入数据进入模型后，会经过一层层网络计算，每一层通常先做线性变换，比如 z = Wx + b，再经过非线性激活函数得到输出，并作为下一层的输入，直到最后得到模型预测结果。</p><p>得到预测结果后，模型会根据真实标签计算 loss，用来衡量预测和真实值之间的差距。前向传播过程中产生的中间激活值、logits、概率分布和 loss，不仅用于输出预测，也会被保留下来，供后续反向传播计算梯度。</p><p>简单说，前向传播就是“输入数据经过网络一层层计算，得到预测结果和 loss”的过程。</p></blockquote><ol><li>反向传播过程是什么？</li></ol><p>反向传播的核心是：从 loss 出发，利用链式法则一层层往回计算每个参数对 loss 的影响，也就是梯度。</p><ol><li>反向传播做什么？</li></ol><p>前向传播得到 loss 后，我们要知道：</p><pre><code>哪些参数导致 loss 变大？<br/>每个参数应该往哪个方向调整？</code></pre><p>反向传播就是用来计算这些梯度的。</p><p>计算完梯度后，优化器再用梯度更新参数。</p><ol><li>基本思想：链式法则</li></ol><p>神经网络是多层复合函数。</p><p>比如：</p><pre><code>x → h1 → h2 → y_hat → loss</code></pre><p>loss 依赖 y_hat，y_hat 依赖 h2，h2 依赖 h1，h1 依赖参数 W1。</p><p>反向传播就是用链式法则计算：</p><pre><code>loss 对每一层参数的梯度</code></pre><ol><li>反向传播流程</li></ol><p>整体流程是：</p><pre><code>从 loss 开始<br/>  ↓<br/>计算输出层梯度<br/>  ↓<br/>传回上一层<br/>  ↓<br/>计算上一层参数梯度<br/>  ↓<br/>继续往前传<br/>  ↓<br/>直到输入层附近</code></pre><p>每层都会计算：</p><ul><li>当前层参数的梯度</li><li>传给前一层的梯度</li></ul><ol><li>参数怎么更新？</li></ol><p>反向传播只负责算梯度。</p><p>真正更新参数由优化器完成。</p><p>比如梯度下降：</p><pre><code>W = W - learning_rate × dW<br/>b = b - learning_rate × db</code></pre><p>如果用 Adam / AdamW，则会结合一阶矩、二阶矩和 weight decay 更新。</p><ol><li>为什么需要前向传播的中间结果？</li></ol><p>反向传播计算梯度时，需要用到前向传播保存的中间值。</p><p>比如：</p><pre><code>z = Wx + b<br/>a = activation(z)</code></pre><p>要计算激活函数梯度，就需要知道 z 或 a。</p><p>所以训练时前向传播会保留计算图，反向传播沿计算图求导。</p><ol><li>在大模型里的反向传播</li></ol><p>大模型训练时也一样：</p><pre><code>前向传播计算 next token loss<br/>  ↓<br/>反向传播计算 Transformer 每层参数梯度<br/>  ↓<br/>优化器更新参数</code></pre><p>只是参数很多，计算量和显存占用都很大。</p><p>所以会用：</p><ul><li>混合精度</li><li>梯度累积</li><li>梯度检查点</li><li>数据并行</li><li>张量并行</li><li>流水线并行</li></ul><p>来降低成本和提高效率。</p><ol><li><strong>面试背诵版：反向传播过程是什么？</strong></li></ol><blockquote><p>反向传播是神经网络训练中计算梯度的过程。前向传播先得到预测结果和 loss，反向传播则从 loss 出发，利用链式法则沿着计算图从后往前逐层计算每个参数对 loss 的梯度。</p><p>具体来说，先计算输出层的梯度，再把梯度传回上一层，每一层都会计算本层参数的梯度，同时计算需要继续传给前一层的梯度。这个过程一直传到网络前面的层。反向传播本身只负责求梯度，参数更新由优化器完成，比如 SGD、Adam 或 AdamW。</p><p>反向传播需要用到前向传播保存的中间激活值和计算图，因为很多梯度计算依赖这些中间结果。简单说，前向传播是“算预测和 loss”，反向传播是“根据 loss 反过来算每个参数该怎么改”。</p></blockquote>"},{"t":"梯度如何计算？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>梯度计算的核心是：通过反向传播和链式法则，计算 loss 对每个参数的偏导数。</p><ol><li>梯度是什么？</li></ol><p>梯度表示 loss 对参数变化的敏感程度。</p><p>比如某个参数 w 的梯度是：</p><pre><code>∂L / ∂w</code></pre><p>意思是：</p><pre><code>w 变化一点，loss 会变化多少</code></pre><p>如果梯度很大，说明这个参数对 loss 影响大。</p><p>如果梯度接近 0，说明这个参数当前影响小。</p><ol><li>为什么要计算梯度？</li></ol><p>训练模型的目标是让 loss 变小。</p><p>有了梯度，就知道参数应该怎么改。</p><p>比如梯度下降：</p><pre><code>w = w - learning_rate × ∂L/∂w</code></pre><p>因为梯度方向是 loss 增大的方向，所以要沿负梯度方向更新参数。</p><ol><li>梯度靠链式法则计算</li></ol><p>神经网络是很多函数嵌套起来的。</p><p>比如：</p><pre><code>x → z = wx + b → y_hat → loss</code></pre><p>loss 不直接只依赖 w，而是通过中间变量间接依赖 w。</p><p>所以要用链式法则：</p><pre><code>∂L/∂w = ∂L/∂y_hat × ∂y_hat/∂z × ∂z/∂w</code></pre><p>这就是反向传播的数学基础。</p><ol><li>简单例子</li></ol><p>假设：</p><pre><code>z = wx + b<br/>L = (z - y)²</code></pre><p>那么：</p><pre><code>∂L/∂z = 2(z - y)<br/>∂z/∂w = x<br/>∂z/∂b = 1</code></pre><p>所以：</p><pre><code>∂L/∂w = 2(z - y) × x<br/>∂L/∂b = 2(z - y)</code></pre><p>这就是参数 w 和 b 的梯度。</p><ol><li>多层网络怎么计算？</li></ol><p>多层网络中，梯度从 loss 开始往前传。</p><p>流程是：</p><pre><code>计算 loss<br/>  ↓<br/>输出层梯度<br/>  ↓<br/>上一层梯度<br/>  ↓<br/>再上一层梯度<br/>  ↓<br/>直到所有参数都有梯度</code></pre><p>每一层都会做两件事：</p><ul><li>计算本层参数的梯度</li><li>计算传给前一层的梯度</li></ul><ol><li>自动求导怎么做？</li></ol><p>实际深度学习框架比如 PyTorch、TensorFlow 会自动求导。</p><p>它们在前向传播时记录计算图。</p><p>反向传播时根据计算图自动应用链式法则。</p><p>比如 PyTorch 中：</p><pre><code>loss.backward()</code></pre><p>就会自动计算所有参数的梯度。</p><ol><li>梯度计算后做什么？</li></ol><p>计算完梯度后，优化器更新参数。</p><p>比如：</p><pre><code>optimizer.step()<br/>optimizer.zero_grad()</code></pre><p>整体流程是：</p><pre><code>前向传播 → 计算 loss → 反向传播算梯度 → 优化器更新参数 → 清空梯度</code></pre><ol><li><strong>面试背诵版：梯度如何计算？</strong></li></ol><blockquote><p>梯度表示损失函数对模型参数的偏导数，也就是参数变化对 loss 的影响程度。比如 ∂L/∂w 表示参数 w 变化一点时，loss 会如何变化。训练时需要计算梯度，是因为梯度方向表示 loss 增大的方向，所以优化器会沿负梯度方向更新参数，让 loss 逐步下降。</p><p>神经网络中的梯度主要通过反向传播和链式法则计算。因为神经网络是多层复合函数，loss 通过很多中间变量间接依赖每个参数，所以需要从 loss 开始，沿计算图从后往前逐层计算梯度。每一层会计算本层参数的梯度，同时把梯度继续传给前一层。</p><p>实际框架比如 PyTorch 和 TensorFlow 会在前向传播时记录计算图，调用 backward 时自动根据链式法则计算所有参数的梯度。简单说，梯度计算就是“从 loss 出发，沿计算图反向传播，算出每个参数该往哪个方向改”。</p></blockquote>"},{"t":"为什么会梯度消失？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>梯度消失的核心原因是：<strong>反向传播时梯度需要一层层相乘，如果很多层的导数都小于 1，乘到前面层时梯度会变得非常小。</strong></p><ol><li>梯度消失是什么？</li></ol><p>梯度消失指的是训练深层神经网络时，越靠前的层收到的梯度越小，甚至接近 0。</p><p>结果是：</p><pre><code>前面层参数几乎不更新</code></pre><p>模型就很难训练深。</p><ol><li>为什么会发生？</li></ol><p>反向传播依赖链式法则。</p><p>多层网络的梯度大概是很多项相乘：</p><pre><code>∂L/∂W1 = ∂L/∂h_n × ∂h_n/∂h_{n-1} × ... × ∂h_1/∂W1</code></pre><p>如果每一层的导数都小于 1，比如 0.5，那么很多层相乘后：</p><pre><code>0.5^10 = 0.000976<br/>0.5^50 ≈ 非常接近 0</code></pre><p>梯度传到前面层时就几乎没了。</p><ol><li>激活函数导致梯度变小</li></ol><p>Sigmoid 和 Tanh 很容易导致梯度消失。</p><p>Sigmoid 在输入很大或很小时会饱和：</p><pre><code>输出接近 0 或 1</code></pre><p>这时导数接近 0。</p><p>反向传播经过这些饱和区时，梯度会被不断缩小。</p><p>Tanh 也类似，在饱和区导数很小。</p><ol><li>网络太深会放大问题</li></ol><p>网络层数越多，反向传播路径越长。</p><p>梯度要经过更多层相乘。</p><p>如果每层都稍微缩小一点，很多层后就会变得极小。</p><p>所以早期深层网络很难训练，一个重要原因就是梯度消失。</p><ol><li>权重初始化不合适</li></ol><p>如果权重初始化太小，前向传播时激活值容易越来越小。</p><p>反向传播时梯度也容易越来越小。</p><p>如果初始化不合理，可能导致：</p><ul><li>激活值衰减</li><li>梯度衰减</li><li>前面层学不到东西</li></ul><p>所以 Xavier、He 初始化就是为了解决这类问题。</p><ol><li>RNN 中更明显</li></ol><p>RNN 处理序列时，同一组权重会在时间步上反复相乘。</p><p>长序列中梯度要跨很多时间步传播。</p><p>如果梯度不断小于 1，就会导致：</p><pre><code>早期时间步学不到长期依赖</code></pre><p>这就是传统 RNN 难以学习长距离依赖的重要原因。</p><ol><li>如何缓解梯度消失？</li></ol><p>常见方法有：</p><ul><li>使用 ReLU / Leaky ReLU / GELU</li><li>使用残差连接</li><li>使用 BatchNorm / LayerNorm</li><li>使用合理初始化，比如 Xavier / He</li><li>使用 LSTM / GRU 处理序列</li><li>使用 Transformer 的 Attention</li><li>减少网络过深带来的长链路</li><li>使用残差结构让梯度直接传播</li></ul><p>其中残差连接非常关键。</p><p>它让梯度可以绕过部分层直接向前传播。</p><ol><li><strong>面试背诵版：为什么会梯度消失？</strong></li></ol><blockquote><p>梯度消失是指深层神经网络反向传播时，越靠前的层接收到的梯度越来越小，甚至接近 0，导致前面层参数几乎无法更新。它的根本原因是反向传播依赖链式法则，梯度需要经过很多层导数连乘。如果这些导数大多小于 1，那么随着层数加深，连乘结果会指数级变小。</p><p>Sigmoid、Tanh 这类激活函数在饱和区导数接近 0，会进一步加剧梯度消失；网络太深、权重初始化不合理，也会让激活值和梯度在传播过程中不断衰减。在 RNN 中，由于梯度要跨很多时间步反复传播，梯度消失问题更加明显，导致模型难以学习长距离依赖。</p><p>缓解方法包括使用 ReLU、Leaky ReLU、GELU 等不容易饱和的激活函数，使用 Xavier / He 初始化，加入 BatchNorm 或 LayerNorm，使用残差连接让梯度更直接地传播；在序列模型中可以使用 LSTM、GRU 或 Transformer。简单说，梯度消失就是“反向传播时很多小于 1 的导数连乘，把梯度越乘越小”。</p></blockquote>"},{"t":"为什么会梯度爆炸？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>梯度爆炸的核心原因是：<strong>反向传播时梯度需要一层层相乘，如果很多层的导数或权重都大于 1，连乘后梯度会指数级变大。</strong></p><ol><li>梯度爆炸是什么？</li></ol><p>梯度爆炸指的是训练过程中梯度变得非常大。</p><p>结果是参数更新幅度过大，导致：</p><ul><li>loss 剧烈震荡</li><li>模型无法收敛</li><li>训练发散</li><li>参数变成极大值</li><li>出现 NaN / Inf</li><li>模型训练崩掉</li></ul><p>简单说：</p><pre><code>梯度太大，参数一步迈飞了。</code></pre><ol><li>为什么会发生？</li></ol><p>反向传播依赖链式法则。</p><p>深层网络中的梯度是很多项连乘：</p><pre><code>∂L/∂W1 = ∂L/∂h_n × ∂h_n/∂h_{n-1} × ... × ∂h_1/∂W1</code></pre><p>如果这些导数或权重很多都大于 1，比如 1.5，那么：</p><pre><code>1.5^10 ≈ 57<br/>1.5^50 非常大</code></pre><p>梯度传到前面层时就可能急剧放大。</p><ol><li>网络太深会加剧问题</li></ol><p>网络越深，反向传播路径越长。</p><p>梯度连乘次数越多。</p><p>如果某些层的梯度放大效应持续存在，就会导致梯度指数级增长。</p><p>所以深层网络更容易出现梯度爆炸。</p><ol><li>权重初始化不合理</li></ol><p>如果权重初始化太大，前向传播时激活值可能越来越大。</p><p>反向传播时梯度也可能越来越大。</p><p>这会导致：</p><pre><code>激活值爆炸<br/>梯度爆炸<br/>loss 发散</code></pre><p>所以合理初始化非常重要。</p><ol><li>学习率过大</li></ol><p>学习率太大也会放大问题。</p><p>即使梯度没有特别夸张，如果学习率很大，参数更新量也会很大：</p><pre><code>参数更新量 = 学习率 × 梯度</code></pre><p>这可能导致参数跳到更差区域，让下一轮梯度更大，形成恶性循环。</p><ol><li>RNN 中更明显</li></ol><p>RNN 在长序列中会反复使用同一组权重。</p><p>反向传播时梯度会跨很多时间步传播。</p><p>如果权重矩阵的特征值较大，梯度会随着时间步不断放大。</p><p>所以传统 RNN 既容易梯度消失，也容易梯度爆炸。</p><ol><li>如何缓解梯度爆炸？</li></ol><p>常见方法包括：</p><ul><li>Gradient Clipping</li><li>降低学习率</li><li>合理权重初始化</li><li>使用 BatchNorm / LayerNorm</li><li>使用残差连接</li><li>使用 Adam / AdamW 等优化器</li><li>使用 LSTM / GRU</li><li>检查 loss scale 和混合精度溢出</li><li>避免过深网络没有稳定结构</li></ul><p>其中最常用、最直接的是 Gradient Clipping。</p><ol><li>Gradient Clipping 是什么？</li></ol><p>Gradient Clipping 就是把梯度限制在一定范围内。</p><p>比如：</p><pre><code>如果梯度范数超过阈值，就按比例缩小</code></pre><p>这样可以防止某一次梯度过大导致参数更新崩掉。</p><p>大模型和 RNN 训练里都很常用。</p><ol><li><strong>面试背诵版：为什么会梯度爆炸？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>梯度爆炸是指训练过程中梯度变得非常大，导致参数更新幅度过大，loss 剧烈震荡、训练发散，甚至出现 NaN 或 Inf。它的根本原因是反向传播依赖链式法则，梯度需要经过多层导数或权重连乘。如果很多项大于 1，随着层数加深或时间步变长，梯度就可能指数级放大。</p><p>网络太深、权重初始化过大、学习率过高、激活值过大，都可能加剧梯度爆炸。在 RNN 中，由于同一权重矩阵在长序列中反复参与计算，如果权重矩阵放大效应明显，梯度会随时间步不断变大，因此更容易出现梯度爆炸。</p><p>常见解决方法包括 Gradient Clipping、降低学习率、合理权重初始化、使用 BatchNorm 或 LayerNorm、残差连接、Adam / AdamW，以及在序列模型中使用 LSTM、GRU 或 Transformer。简单说，梯度爆炸就是“反向传播时很多大于 1 的项连乘，把梯度越乘越大”。</p></blockquote>"},{"t":"Residual Connection为什么有效？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Residual Connection 有效的核心原因是：<strong>它给信息和梯度提供了一条“捷径”，让深层网络更容易训练，缓解梯度消失和退化问题。</strong></p><ol><li>Residual Connection 是什么？</li></ol><p>Residual Connection 也叫残差连接、跳跃连接。</p><p>普通网络一层可能是：</p><pre><code>y = F(x)</code></pre><p>残差连接变成：</p><pre><code>y = F(x) + x</code></pre><p>也就是说，输出不仅包含当前层学到的变换 F(x)，还保留原始输入 x。</p><ol><li>它解决什么问题？</li></ol><p>深层网络理论上表达能力更强。</p><p>但实际训练中，网络越深可能越难训练。</p><p>会遇到：</p><ul><li>梯度消失</li><li>优化困难</li><li>训练误差不降反升</li><li>前面层学不到东西</li><li>信息在多层传播中丢失</li></ul><p>Residual Connection 就是为了解决深层网络难训练的问题。</p><ol><li>提供梯度传播捷径</li></ol><p>反向传播时，残差连接让梯度可以绕过复杂层直接传回前面。</p><p>因为：</p><pre><code>y = F(x) + x</code></pre><p>对 x 求导时有一项来自 x 本身：</p><pre><code>dy/dx = dF(x)/dx + 1</code></pre><p>这里的 <code>+1</code> 就相当于给梯度提供了一条直接通路。</p><p>所以梯度不容易在多层网络中完全消失。</p><ol><li>保留原始信息</li></ol><p>前向传播时，x 可以直接传到后面层。</p><p>这意味着即使 F(x) 暂时学得不好，网络也至少可以保留原始输入信息。</p><p>普通深层网络中，信息每经过一层都会被变换，可能逐渐损失。</p><p>残差连接可以减少信息损失。</p><ol><li>让网络学习“增量”更容易</li></ol><p>残差网络不是直接学习完整映射：</p><pre><code>H(x)</code></pre><p>而是学习残差：</p><pre><code>F(x) = H(x) - x</code></pre><p>最终：</p><pre><code>H(x) = F(x) + x</code></pre><p>很多时候，学习“在原输入基础上改一点”比学习一个完整新映射更容易。</p><p>比如如果某层不需要做复杂变换，只需要让 F(x) 接近 0，就可以近似恒等映射：</p><pre><code>y ≈ x</code></pre><ol><li>缓解网络退化问题</li></ol><p>网络退化指的是：</p><pre><code>网络加深后，训练误差反而变高</code></pre><p>这不是过拟合，而是优化困难。</p><p>残差连接让深层网络至少容易学到恒等映射。</p><p>所以即使加了很多层，也不至于比浅层网络更难优化太多。</p><ol><li>在 Transformer 里也很重要</li></ol><p>Transformer 每个子层通常都有残差连接。</p><p>比如：</p><pre><code>x + Attention(x)<br/>x + FFN(x)</code></pre><p>再配合 LayerNorm。</p><p>这让 Transformer 可以堆很多层。</p><p>没有残差连接，大模型训练会困难很多。</p><ol><li><strong>面试背诵版：Residual Connection 为什么有效？</strong></li></ol><blockquote><p>Residual Connection，也叫残差连接或跳跃连接，核心形式是 y = F(x) + x。它有效的原因是给前向信息传播和反向梯度传播都提供了一条捷径。前向传播时，输入 x 可以直接传到后面层，减少信息在深层网络中逐层变换造成的丢失；反向传播时，由于 y 对 x 的导数包含一个直接的 1，梯度可以更容易地传回前面层，从而缓解梯度消失。</p><p>另外，残差连接让网络学习的是残差 F(x) = H(x) - x，而不是直接学习完整映射 H(x)。很多情况下，学习“在原输入基础上做增量修改”比学习一个全新的映射更容易；如果某些层暂时不需要变换，只要让 F(x) 接近 0，就可以近似恒等映射。这缓解了深层网络加深后训练误差反而升高的退化问题。</p><p>简单说，Residual Connection 的作用是“保留原始信息、提供梯度捷径、让网络更容易学习增量变化”，所以它能让 ResNet 和 Transformer 这类深层模型更稳定地训练。</p></blockquote>"}]},{"no":"","title":"3.2 网络结构","questions":[{"t":"CNN为什么适合图像？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"RNN为什么存在长距离依赖问题？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"LSTM如何解决梯度问题？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"GRU和LSTM有什么区别？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Attention为什么可以解决RNN的问题？","tag":"重点","p":"core","html":"<span>[原文档此处有配图]</span><p>Attention 能解决 RNN 的很多问题，核心原因是：<strong>它不需要按顺序一步步传递信息，而是让每个位置直接关注序列中任意位置的信息。</strong></p><ol><li>RNN 的主要问题</li></ol><p>RNN 处理序列是按时间步递归进行的：</p><pre><code>x1 → x2 → x3 → ... → xn</code></pre><p>它的问题包括：</p><ul><li>难以并行训练</li><li>长距离依赖难学</li><li>容易梯度消失 / 爆炸</li><li>前面信息传到后面会衰减</li><li>长序列计算效率低</li><li>最终表示容易压缩丢信息</li></ul><p>比如句子很长时，开头的重要信息传到结尾可能已经弱化了。</p><ol><li>Attention 可以直接建模任意位置关系</li></ol><p>Attention 的核心是：</p><pre><code>当前 token 可以直接关注序列中所有 token</code></pre><p>比如句子：</p><pre><code>小明把书放进书包，因为它太重了。</code></pre><p>模型可以让“它”直接关注“书”或“书包”，而不是靠 RNN 一步步传递。</p><p>这能更好解决长距离依赖问题。</p><ol><li>信息路径更短</li></ol><p>RNN 中，远距离信息要经过很多时间步。</p><p>比如第 1 个词影响第 100 个词，需要经过 99 次传递。</p><p>Attention 中，第 100 个词可以直接 attend 到第 1 个词。</p><p>路径从：</p><pre><code>O(n)</code></pre><p>变成：</p><pre><code>O(1)</code></pre><p>信息更不容易丢失，梯度也更容易传播。</p><ol><li>更容易并行计算</li></ol><p>RNN 必须按顺序算：</p><pre><code>先算 t1，才能算 t2<br/>先算 t2，才能算 t3</code></pre><p>Attention 不依赖这种递归结构。</p><p>一个序列中的所有 token 可以同时计算 Q、K、V 和 attention score。</p><p>这非常适合 GPU 并行训练。</p><p>所以 Transformer 比 RNN 更适合大规模训练。</p><ol><li>缓解梯度消失问题</li></ol><p>RNN 的梯度要跨很多时间步反向传播。</p><p>长序列中容易梯度消失或爆炸。</p><p>Attention 让不同位置之间建立直接连接，减少了长链路反向传播依赖。</p><p>再加上残差连接、LayerNorm 等结构，训练深层模型更稳定。</p><ol><li>表达能力更灵活</li></ol><p>RNN 的状态通常是一个不断更新的 hidden state。</p><p>它容易把很多历史信息压缩到一个向量里。</p><p>Attention 不需要把全部历史压进一个固定状态，而是可以根据当前 token 动态选择关注哪些位置。</p><p>不同 token 可以关注不同上下文。</p><p>所以表达更灵活。</p><ol><li>但 Attention 也有代价</li></ol><p>Attention 也不是没有缺点。</p><p>Self-Attention 的计算复杂度通常是：</p><pre><code>O(n²)</code></pre><p>序列很长时，计算和显存成本会很高。</p><p>所以长上下文模型会做各种优化，比如稀疏注意力、滑动窗口注意力、KV Cache 等。</p><ol><li><strong>面试背诵版：Attention 为什么可以解决 RNN 的问题？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>RNN 处理序列时必须按时间步递归计算，前一个 hidden state 传给下一个 hidden state，因此难以并行训练，而且长距离信息需要经过很多步传递，容易出现信息衰减、梯度消失或梯度爆炸，导致长距离依赖难以学习。</p><p><strong>Attention 的优势是让序列中任意两个位置可以直接建立联系。</strong>每个 token 都可以根据 Query、Key、Value 计算它应该关注哪些 token，因此远距离信息不需要一步步传递，而是可以被直接访问。这样信息路径从 RNN 中的多步传递变成直接连接，更容易建模长距离依赖，也更有利于梯度传播。</p><p>同时，Attention 不依赖严格的顺序递归，<strong>一个序列中所有 token 的 Q、K、V 和 attention score 可以并行计算</strong>，非常适合 GPU 大规模训练。简单说，RNN 是“信息一步步传”，Attention 是“每个位置直接看全局”，所以 Attention 更适合长文本建模和大模型训练。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"模块四：Transformer（重点）","questions":[{"t":"Transformer整体架构是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Transformer 整体架构的核心是<strong>：用 Attention 替代 RNN 的顺序递归，通过多层 Encoder 和 Decoder 堆叠来完成序列建模。</strong></p><ol><li>原始 Transformer 是 Encoder-Decoder 架构</li></ol><p>最早的 Transformer 用于机器翻译，是典型的 Encoder-Decoder 结构：</p><pre><code>输入序列 → Encoder → 上下文表示 → Decoder → 输出序列</code></pre><p>比如：</p><pre><code>英文句子 → Encoder 编码 → Decoder 生成中文句子</code></pre><p>Encoder 负责理解输入。</p><p>Decoder 负责生成输出。</p><ol><li>Encoder 由什么组成？</li></ol><p>Encoder 通常由 N 个相同的 Encoder Layer 堆叠。</p><p>每个 Encoder Layer 主要包括：</p><pre><code>Multi-Head Self-Attention<br/>Feed Forward Network<br/>Residual Connection<br/>LayerNorm</code></pre><p>大致流程：</p><pre><code>输入<br/>  ↓<br/>Self-Attention<br/>  ↓<br/>Add &amp; Norm<br/>  ↓<br/>Feed Forward<br/>  ↓<br/>Add &amp; Norm</code></pre><p>Self-Attention 让每个 token 看到输入序列中其他 token。</p><p>FFN 对每个 token 做非线性变换。</p><p>Residual 和 LayerNorm 用来稳定训练。</p><ol><li>Decoder 由什么组成？</li></ol><p>Decoder 也由 N 个 Decoder Layer 堆叠。</p><p>每个 Decoder Layer 通常包括：</p><pre><code>Masked Multi-Head Self-Attention<br/>Cross-Attention<br/>Feed Forward Network<br/>Residual Connection<br/>LayerNorm</code></pre><p>流程大致是：</p><pre><code>已生成 token<br/>  ↓<br/>Masked Self-Attention<br/>  ↓<br/>Cross-Attention 关注 Encoder 输出<br/>  ↓<br/>Feed Forward<br/>  ↓<br/>输出下一个 token</code></pre><p>Masked Self-Attention 防止模型看到未来 token。</p><p>Cross-Attention 用来读取 Encoder 的输入信息。</p><ol><li>输入怎么进入 Transformer？</li></ol><p>输入文本会先经过：</p><pre><code>Tokenizer → Token ID → Embedding → 加位置编码</code></pre><p>因为 Attention 本身不天然知道词序，所以要加入 Positional Encoding 或 RoPE 这类位置信息。</p><p>这样模型才能区分：</p><pre><code>我喜欢你<br/>你喜欢我</code></pre><ol><li>输出怎么生成？</li></ol><p>Decoder 最后输出 hidden states。</p><p>再经过线性层映射到词表大小：</p><pre><code>hidden state → logits → softmax → token 概率</code></pre><p>然后选择下一个 token。</p><p>大语言模型就是不断重复：</p><pre><code>预测下一个 token → 拼到上下文后面 → 再预测下一个 token</code></pre><ol><li>三种常见 Transformer 形态</li></ol><p>现在常见 Transformer 不只原始 Encoder-Decoder。</p><p>主要有三类：</p><ul><li>Encoder-only：代表 BERT，适合理解任务</li><li>Decoder-only：代表 GPT，适合文本生成和大语言模型</li><li>Encoder-Decoder：代表 T5、BART，适合翻译、摘要、改写</li></ul><p>GPT 类大模型通常是 Decoder-only。</p><ol><li>为什么 Transformer 适合大模型？</li></ol><p>因为它有这些优点：</p><ul><li>Attention 能建模长距离依赖</li><li>相比 RNN 更容易并行训练</li><li>多头注意力能从不同角度建模关系</li><li>残差连接和归一化让深层网络更稳定</li><li>架构可扩展，适合堆大参数和大数据</li></ul><ol><li><strong>面试背诵版：Transformer 整体架构是什么？</strong></li></ol><blockquote><p>Transformer 最早是 Encoder-Decoder 架构，整体流程是输入序列先经过 Encoder 得到上下文表示，再由 Decoder 根据 Encoder 输出和已生成 token 逐步生成目标序列。Encoder 每层主要由 Multi-Head Self-Attention、Feed Forward Network、Residual Connection 和 LayerNorm 组成；Decoder 每层包括 Masked Self-Attention、Cross-Attention、Feed Forward、Residual Connection 和 LayerNorm。</p><p><strong>输入文本会先经过 tokenizer 转成 token id，再通过 embedding 转成向量，并加入位置编码，因为 Attention 本身不具备顺序信息。</strong>Decoder 最后输出 hidden state，再映射成 logits，通过 softmax 得到词表上每个 token 的概率，用来生成下一个 token。</p><p>现在常见 Transformer 有三种形态：Encoder-only，比如 BERT，适合理解任务；Decoder-only，比如 GPT，适合生成任务和大语言模型；Encoder-Decoder，比如 T5、BART，适合翻译、摘要和改写。简单说，Transformer 的核心是用 Attention 建模 token 关系，用 FFN 做非线性变换，再通过残差连接和 LayerNorm 保证深层训练稳定。</p></blockquote>"},{"t":"Transformer为什么可以并行训练？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Transformer 可以并行训练，核心原因是：<strong>它没有 RNN 那种时间步递归依赖，序列中所有 token 的表示可以同时计算。</strong></p><ol><li>RNN 为什么难并行？</li></ol><p>RNN 是按顺序处理序列的。</p><p>比如：</p><pre><code>h1 = f(x1, h0)<br/>h2 = f(x2, h1)<br/>h3 = f(x3, h2)</code></pre><p>计算 <code>h2</code> 必须等 <code>h1</code> 算完。</p><p>计算 <code>h3</code> 必须等 <code>h2</code> 算完。</p><p>所以 RNN 天然是串行的：</p><pre><code>x1 → x2 → x3 → ... → xn</code></pre><p>这导致训练速度慢，不适合大规模并行计算。</p><ol><li>Transformer 没有递归依赖</li></ol><p>Transformer 的 Self-Attention 不需要按时间步逐个计算。</p><p>它可以一次性拿到整个序列的 token embedding。</p><p>然后同时计算每个 token 的：</p><pre><code>Q、K、V</code></pre><p>也就是：</p><pre><code>所有 token 同时做矩阵运算</code></pre><p>这非常适合 GPU / TPU 并行。</p><ol><li>Attention 是矩阵运算</li></ol><p>Self-Attention 的核心计算是：</p><pre><code>Attention(Q, K, V) = softmax(QK^T / sqrt(d)) V</code></pre><p>其中 Q、K、V 都是整个序列一次性算出来的矩阵。</p><p>这个过程本质上是大矩阵乘法。</p><p>而 GPU 最擅长的就是并行矩阵运算。</p><p>所以 Transformer 训练效率很高。</p><ol><li>每个位置可以同时计算</li></ol><p>在训练阶段，序列中每个位置的 next token 预测都可以同时做。</p><p>比如句子：</p><pre><code>我 喜欢 吃 苹果</code></pre><p>训练时可以同时计算：</p><pre><code>我 → 喜欢<br/>我 喜欢 → 吃<br/>我 喜欢 吃 → 苹果</code></pre><p>虽然用了 causal mask 防止看到未来 token，但这些位置的 loss 可以并行计算。</p><ol><li>Mask 不影响并行</li></ol><p>GPT 类模型使用 causal mask。</p><p>它限制当前位置只能看前面的 token，不能看未来 token。</p><p>但这个 mask 是矩阵里的一个遮罩。</p><p>并不要求模型真的按时间步一个个算。</p><p>所以训练时仍然可以并行处理整个序列。</p><ol><li>Decoder 生成时不能完全并行</li></ol><p>这里要区分训练和推理。</p><p>训练时：</p><pre><code>已知完整输入序列，可以并行计算所有位置</code></pre><p>推理生成时：</p><pre><code>必须一个 token 一个 token 生成</code></pre><p>因为下一个 token 生成出来之前，后面的 token 还不知道。</p><p>所以 Transformer 训练可以高度并行，但自回归生成推理仍然是串行的。</p><ol><li>并行带来的好处</li></ol><p>并行训练带来：</p><ul><li>训练速度更快</li><li>更适合 GPU / TPU</li><li>能使用更大 batch</li><li>能扩展到更大数据</li><li>能训练更大模型</li><li>比 RNN 更适合大规模预训练</li></ul><p>这也是 Transformer 能成为大模型主流架构的重要原因。</p><ol><li><strong>面试背诵版：Transformer 为什么可以并行训练？</strong></li></ol><blockquote><p>Transformer 可以并行训练，主要是因为它不像 RNN 那样存在时间步之间的递归依赖。RNN 中当前 hidden state 依赖前一个 hidden state，所以必须按 x1、x2、x3 的顺序逐步计算，难以并行。Transformer 的 Self-Attention 则可以一次性接收整个序列的 token embedding，同时计算所有位置的 Q、K、V，并通过矩阵乘法计算 attention score。</p><p>Self-Attention 的核心计算是 softmax(QK^T / sqrt(d))V，本质上是大规模矩阵运算，非常适合 GPU / TPU 并行。即使 GPT 这类 Decoder-only 模型使用 causal mask 防止当前位置看到未来 token，这个 mask 也只是矩阵中的遮罩，不影响训练阶段对整个序列进行并行计算。</p><p>需要注意的是，Transformer 的训练可以并行，因为训练时完整序列已知；但自回归推理时仍然要一个 token 一个 token 生成，因为后面的 token 依赖前面生成结果。简单说，Transformer 训练并行的原因是“没有递归依赖，核心计算是矩阵运算”。</p></blockquote>"},{"t":"Transformer相比RNN有什么优势？","tag":"","p":"","html":"<p>Transformer 相比 RNN 的核心优势是：可以并行训练、能更好建模长距离依赖，并且更适合大规模预训练。</p><ol><li>Transformer 可以并行训练</li></ol><p>RNN 是按时间步顺序计算的：</p><pre><code>h1 → h2 → h3 → ... → hn</code></pre><p>后一个状态依赖前一个状态，所以很难并行。</p><p>Transformer 不使用递归结构。</p><p>它可以一次性处理整个序列，通过 Self-Attention 同时计算所有 token 之间的关系。</p><p>所以更适合 GPU / TPU 并行训练。</p><ol><li>长距离依赖更强</li></ol><p>RNN 中，前面 token 的信息要一步步传到后面。</p><p>如果距离很远，信息容易衰减。</p><p>比如第 1 个词影响第 100 个词，中间要经过很多步。</p><p>Transformer 中，每个 token 可以通过 Attention 直接关注任意位置的 token。</p><p>路径更短：</p><pre><code>RNN：需要多步传递<br/>Transformer：可以直接连接</code></pre><p>所以更适合长文本建模。</p><ol><li>缓解梯度消失问题</li></ol><p>RNN 反向传播要跨很多时间步。</p><p>长序列中容易出现：</p><ul><li>梯度消失</li><li>梯度爆炸</li><li>长期依赖难学习</li></ul><p>Transformer 通过 Attention 直接建模位置关系，再配合 Residual Connection 和 LayerNorm，训练深层网络更稳定。</p><ol><li>表达能力更灵活</li></ol><p>RNN 主要依赖一个不断更新的 hidden state 来压缩历史信息。</p><p>这容易造成信息瓶颈。</p><p>Transformer 的每个 token 都可以动态关注不同 token。</p><p>比如：</p><ul><li>主语关注谓语</li><li>代词关注指代对象</li><li>当前词关注关键上下文</li></ul><p>这种动态注意力机制表达能力更强。</p><ol><li>更适合大规模预训练</li></ol><p>Transformer 的矩阵计算非常适合并行硬件。</p><p>因此可以支持：</p><ul><li>更大模型</li><li>更多数据</li><li>更大 batch</li><li>更长训练</li><li>大规模分布式训练</li></ul><p>这也是 GPT、BERT、T5 等大模型都采用 Transformer 的重要原因。</p><ol><li>Multi-Head Attention 更强</li></ol><p>Transformer 里的多头注意力可以从多个角度建模关系。</p><p>不同 head 可能关注：</p><ul><li>局部语法</li><li>长距离依赖</li><li>指代关系</li><li>句法结构</li><li>语义关系</li></ul><p>相比 RNN 单一路径传递状态，Multi-Head Attention 更灵活。</p><ol><li>但 Transformer 也有缺点</li></ol><p>面试可以补一句，显得更完整。</p><p>Transformer 的缺点是：</p><ul><li>Self-Attention 复杂度通常是 O(n²)</li><li>长序列显存和计算成本高</li><li>自回归推理仍然是逐 token 生成</li><li>对位置编码有依赖</li></ul><p>所以长上下文场景还需要稀疏注意力、滑动窗口、KV Cache 等优化。</p><ol><li><strong>面试背诵版：Transformer 相比 RNN 有什么优势？</strong></li></ol><blockquote><p>Transformer 相比 RNN 最大的优势是<strong>并行能力</strong>和<strong>长距离依赖建模能力</strong>。RNN 必须按时间步递归计算，当前 hidden state 依赖前一个 hidden state，所以训练难以并行；而 Transformer 没有这种递归依赖，可以一次性计算整个序列中所有 token 的 Q、K、V，并通过矩阵乘法完成 Self-Attention，非常适合 GPU / TPU 并行训练。</p><p>在长距离依赖方面，RNN 中远距离信息需要经过很多时间步传递，容易出现信息衰减、梯度消失或梯度爆炸；Transformer 中任意两个 token 可以通过 Attention 直接建立联系，信息路径更短，更容易捕捉长距离关系。同时 Multi-Head Attention 可以从多个角度建模语法、语义、指代和上下文关系，表达能力更强。</p><p>此外，Transformer 更适合大规模预训练和分布式训练，因此成为 GPT、BERT、T5 等大模型的主流架构。不过它也有缺点，比如 Self-Attention 的计算和显存复杂度通常是 O(n²)，长序列成本较高。简单说，RNN 是“顺序传递信息”，Transformer 是“全局并行建模关系”。</p></blockquote>"},{"t":"Encoder和Decoder有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>Encoder 和 Decoder 的核心区别是：Encoder 负责理解输入，Decoder 负责生成输出。</strong></p><ol><li>Encoder 是什么？</li></ol><p>Encoder 是编码器。</p><p>它的作用是把输入序列编码成上下文表示。</p><p>比如输入一句话：</p><pre><code>I love you</code></pre><p>Encoder 会把每个 token 转成包含上下文信息的向量。</p><p>也就是说，Encoder 更偏理解。</p><p>适合：</p><ul><li>文本分类</li><li>语义匹配</li><li>情感分析</li><li>NER</li><li>检索向量表示</li><li>文本理解任务</li></ul><ol><li>Decoder 是什么？</li></ol><p>Decoder 是解码器。</p><p>它的作用是根据已有信息逐步生成输出序列。</p><p>比如机器翻译中：</p><pre><code>输入英文 → 生成中文</code></pre><p>Decoder 会一个 token 一个 token 地生成：</p><pre><code>我 → 爱 → 你</code></pre><p>也就是说，Decoder 更偏生成。</p><p>适合：</p><ul><li>文本生成</li><li>对话</li><li>写作</li><li>代码生成</li><li>翻译</li><li>摘要</li></ul><ol><li>Attention 方式不同</li></ol><p>Encoder 用的是 Self-Attention。</p><p>它通常可以看到整个输入序列。</p><p>比如 BERT 里，一个 token 可以同时关注前后文。</p><p>所以它是双向理解。</p><p>Decoder 用的是 Masked Self-Attention。</p><p>生成时当前位置不能看到未来 token。</p><p>比如生成第 3 个 token 时，只能看到前面已经生成的 token。</p><p>这叫 causal mask。</p><ol><li>是否能看未来信息不同</li></ol><p>Encoder 通常可以看完整输入。</p><p>比如句子：</p><pre><code>我 喜欢 吃 苹果</code></pre><p>Encoder 处理“喜欢”时，可以看到“我”，也可以看到“吃 苹果”。</p><p>Decoder 不能看未来。</p><p>生成时：</p><pre><code>我 喜欢</code></pre><p>要预测下一个 token，只能基于前面的内容，不能提前看到答案。</p><ol><li>在原始 Transformer 中的关系</li></ol><p>原始 Transformer 是 Encoder-Decoder 架构。</p><p>流程是：</p><pre><code>输入序列 → Encoder → 上下文表示 → Decoder → 输出序列</code></pre><p>Decoder 里还有 Cross-Attention。</p><p>它用来关注 Encoder 的输出。</p><p>比如翻译时，Decoder 生成中文时，会参考 Encoder 编码出来的英文信息。</p><ol><li>常见模型类型</li></ol><p>Encoder-only 模型：</p><pre><code>BERT</code></pre><p>擅长理解任务。</p><p>Decoder-only 模型：</p><pre><code>GPT</code></pre><p>擅长生成任务。</p><p>Encoder-Decoder 模型：</p><pre><code>T5<br/>BART<br/>原始 Transformer</code></pre><p>擅长输入到输出转换，比如翻译、摘要、改写。</p><ol><li>一句话记忆</li></ol><p>可以这样记：</p><pre><code>Encoder：看完整输入，做理解<br/>Decoder：看已生成内容，做生成</code></pre><p>或者：</p><pre><code>Encoder 负责读懂<br/>Decoder 负责写出来</code></pre><ol><li><strong>面试背诵版：Encoder 和 Decoder 有什么区别？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Encoder 和 Decoder 的区别在于职责和注意力方式不同。Encoder 负责理解输入序列，把输入 token 编码成带上下文信息的表示，通常使用双向 Self-Attention，可以同时看到前后文，因此更适合文本分类、语义匹配、命名实体识别、检索表示等理解类任务。</p><p>Decoder 负责生成输出序列，通常使用 Masked Self-Attention，也就是当前位置只能看到已经生成的 token，不能看到未来 token，因此适合文本生成、对话、代码生成、翻译和摘要等生成类任务。在原始 Transformer 中，Decoder 还会通过 Cross-Attention 关注 Encoder 的输出，从而根据输入内容生成目标序列。</p><p>简单说，Encoder 是“读懂输入”，Decoder 是“生成输出”。BERT 是典型 Encoder-only 模型，GPT 是典型 Decoder-only 模型，T5 和 BART 是 Encoder-Decoder 模型。</p></blockquote>"},{"t":"GPT为什么采用Decoder Only？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>GPT 采用 Decoder-only，核心原因是：<strong>GPT 的目标是自回归生成，也就是根据前文预测下一个 token，而 Decoder-only 结构天然适合这个任务。</strong></p><ol><li>GPT 的训练目标是什么？</li></ol><p>GPT 的训练目标是 next token prediction。</p><p>也就是：</p><pre><code>根据前面的 token，预测下一个 token</code></pre><p>比如：</p><pre><code>输入：中国的首都是<br/>目标：北京</code></pre><p>这种任务是从左到右生成文本。</p><p>Decoder-only 正好适合这种自回归建模。</p><ol><li>Decoder-only 使用 causal mask</li></ol><p>GPT 中每个 token 只能看到自己前面的 token。</p><p>不能看到未来 token。</p><p>比如训练句子：</p><pre><code>我 喜欢 吃 苹果</code></pre><p>预测“吃”时，只能看到：</p><pre><code>我 喜欢</code></pre><p>不能看到：</p><pre><code>苹果</code></pre><p>这通过 causal mask 实现。</p><p>这样训练和生成过程保持一致。</p><ol><li>为什么不用 Encoder？</li></ol><p>Encoder 通常是双向注意力。</p><p>它可以同时看前文和后文。</p><p>比如 BERT 处理“喜欢”时，可以看到：</p><pre><code>我<br/>吃 苹果</code></pre><p>这适合理解任务，但不适合自回归生成。</p><p>因为生成时未来 token 还不存在。</p><p>如果用双向 Encoder 来生成，就会出现训练时能看未来、推理时看不到未来的问题。</p><ol><li>为什么不用 Encoder-Decoder？</li></ol><p>Encoder-Decoder 适合输入到输出的转换任务。</p><p>比如：</p><pre><code>英文 → 中文<br/>文章 → 摘要<br/>问题 → 答案</code></pre><p>但 GPT 的目标是通用文本生成。</p><p>它只需要基于一个上下文不断生成后续 token。</p><p>使用 Encoder-Decoder 会增加结构复杂度和计算成本。</p><p>Decoder-only 更简单、更统一。</p><ol><li>Decoder-only 更适合统一建模</li></ol><p>GPT 把很多任务都转成文本续写。</p><p>比如：</p><pre><code>翻译：把下面英文翻译成中文：...<br/>总结：请总结下面文章：...<br/>问答：问题是...答案是...</code></pre><p>这些都可以看作：</p><pre><code>给定 prompt，继续生成答案</code></pre><p>所以 Decoder-only 可以用同一个 next token prediction 目标统一训练和推理。</p><ol><li>结构简单，扩展性好</li></ol><p>Decoder-only 结构比较统一。</p><p>优势是：</p><ul><li>架构简单</li><li>训练目标简单</li><li>推理方式统一</li><li>适合大规模预训练</li><li>适合扩大参数规模</li><li>适合生成式任务</li></ul><p>这也是 GPT 系列能扩展到大模型的重要原因之一。</p><ol><li>和 BERT 的区别</li></ol><p>BERT 是 Encoder-only。</p><p>它适合理解任务，比如分类、匹配、抽取。</p><p>GPT 是 Decoder-only。</p><p>它适合生成任务，比如写作、对话、代码生成。</p><p>区别可以简单记：</p><pre><code>BERT：双向理解<br/>GPT：单向生成</code></pre><ol><li><strong>面试背诵版：GPT 为什么采用 Decoder-only？</strong></li></ol><blockquote><p>GPT 采用 Decoder-only，主要是因为它的核心训练目标是自回归语言建模，也就是根据前面的上下文预测下一个 token。Decoder-only 结构通过 causal mask 保证每个位置只能看到当前位置之前的 token，不能看到未来 token，这和文本生成时从左到右逐个生成 token 的过程天然一致。</p><p>Encoder 通常使用双向 Self-Attention，可以同时看到前后文，适合理解任务，但不适合自回归生成，因为生成时未来 token 还不存在。Encoder-Decoder 结构更适合翻译、摘要这类输入到输出的转换任务，而 GPT 更强调通用文本续写和生成，把问答、翻译、总结、代码生成等任务都统一成“给定 prompt，继续生成答案”。</p><p>因此 Decoder-only 架构更简单、训练目标更统一、推理方式更一致，也更容易扩展到大规模预训练。简单说，GPT 采用 Decoder-only，是因为它要做的是“基于前文持续生成后文”。</p></blockquote>"},{"t":"BERT为什么采用Encoder Only？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>BERT 采用 Encoder-only，核心原因是：<strong>BERT 的目标是理解文本，而 Encoder 的双向注意力更适合做上下文理解。</strong></p><ol><li>BERT 的定位是什么？</li></ol><p>BERT 主要是理解型模型。</p><p>它擅长：</p><ul><li>文本分类</li><li>情感分析</li><li>语义匹配</li><li>NER</li><li>阅读理解</li><li>句子相似度</li><li>信息抽取</li></ul><p>这些任务更需要“读懂整句话”，而不是从左到右生成文本。</p><ol><li>Encoder 可以双向看上下文</li></ol><p>Encoder 使用的是双向 Self-Attention。</p><p>也就是说，每个 token 可以同时关注前文和后文。</p><p>比如句子：</p><pre><code>我把苹果放进书包，因为它太重了。</code></pre><p>理解“它”指代什么，需要结合前后文。</p><p>Encoder 可以让“它”同时看整个句子，更适合理解语义。</p><ol><li>BERT 的训练目标适合理解任务</li></ol><p>BERT 的经典训练目标是 MLM，也就是 Masked Language Model。</p><p>比如：</p><pre><code>我喜欢吃 [MASK]</code></pre><p>模型要根据左右上下文预测被遮住的词。</p><p>它可以看：</p><pre><code>我喜欢吃</code></pre><p>也可以看后面的上下文。</p><p>这和 Encoder 的双向建模天然匹配。</p><ol><li>为什么不用 Decoder-only？</li></ol><p>Decoder-only 是单向的，只能看前文。</p><p>比如 GPT 预测某个位置时不能看后文。</p><p>这适合生成，但对理解任务来说不够充分。</p><p>因为很多理解任务需要同时利用左右上下文。</p><p>比如：</p><pre><code>银行附近有一条河。<br/>我去银行办理业务。</code></pre><p>判断“银行”含义时，需要结合上下文。</p><p>BERT 的双向 Encoder 更合适。</p><ol><li>为什么不用 Encoder-Decoder？</li></ol><p>Encoder-Decoder 适合序列到序列生成任务。</p><p>比如：</p><ul><li>翻译</li><li>摘要</li><li>改写</li><li>生成式问答</li></ul><p>BERT 主要做的是输入文本表示学习，不需要额外 Decoder 生成长文本。</p><p>所以 Encoder-only 更简单、更高效。</p><ol><li>Encoder-only 输出什么？</li></ol><p>BERT 输出的是每个 token 的上下文表示。</p><p>比如：</p><pre><code>输入 token → contextual embedding</code></pre><p>这些表示可以接不同任务头：</p><ul><li>分类头</li><li>序列标注头</li><li>匹配头</li><li>阅读理解 span 预测头</li></ul><p>所以 BERT 更像一个通用文本理解编码器。</p><ol><li>和 GPT 的区别</li></ol><p>可以这样记：</p><pre><code>BERT：Encoder-only，双向理解<br/>GPT：Decoder-only，单向生成</code></pre><p>BERT 更适合理解任务。</p><p>GPT 更适合生成任务。</p><ol><li><strong>面试背诵版：BERT 为什么采用 Encoder-only？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>BERT 采用 Encoder-only，主要是因为它的定位是文本理解模型，而 Encoder 的双向 Self-Attention 可以让每个 token 同时关注前文和后文，更适合捕捉完整上下文语义。很多理解任务，比如文本分类、语义匹配、命名实体识别、阅读理解和情感分析，都需要综合左右上下文来判断语义。</p><p><strong>BERT 的预训练目标是 Masked Language Model，</strong>也就是随机遮住部分 token，让模型根据左右上下文预测被遮住的词。这个目标天然需要双向上下文，因此和 Encoder-only 架构非常匹配。相比之下，Decoder-only 只能看前文，更适合自回归生成；Encoder-Decoder 适合翻译、摘要等序列到序列任务，但 BERT 主要学习输入文本表示，不需要额外 Decoder。</p><p>简单说，<strong>BERT 采用 Encoder-only</strong>，是因为它要做的是“读懂整段输入”，而不是“从左到右生成输出”。它输出每个 token 的上下文表示，再接分类、匹配、抽取等任务头完成下游理解任务。</p></blockquote>"}]},{"no":"","title":"4.2 Attention机制（重点）","questions":[{"t":"Attention公式是什么（必须记住！）？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Attention 的核心公式是：</p><pre><code>Attention(Q, K, V) = softmax(QKᵀ / √d_k) V</code></pre><ol><li>Q、K、V 分别是什么？</li></ol><p>Q、K、V 分别是：</p><ul><li>Q：Query，查询向量</li><li>K：Key，键向量</li><li>V：Value，值向量</li></ul><p>可以这样理解：</p><pre><code>Q：我想找什么信息<br/>K：我有什么信息可被匹配<br/>V：真正要取走的信息内容</code></pre><p>当前 token 用 Q 去和其他 token 的 K 做匹配，算出应该关注谁；然后根据注意力权重对 V 加权求和。</p><ol><li>公式分几步？</li></ol><p>第一步，计算相似度：</p><pre><code>QKᵀ</code></pre><p>表示 Query 和 Key 的匹配程度。</p><p>分数越大，说明当前 token 越应该关注那个 token。</p><p>第二步，缩放：</p><pre><code>QKᵀ / √d_k</code></pre><p><code>d_k</code> 是 Key 向量维度。</p><p>除以 <code>√d_k</code> 是为了防止点积结果过大，导致 softmax 梯度不稳定。</p><p>第三步，softmax：</p><pre><code>softmax(QKᵀ / √d_k)</code></pre><p>把相似度分数转成注意力权重。</p><p>权重加起来等于 1。</p><p>第四步，加权求和：</p><pre><code>softmax(...) V</code></pre><p>根据注意力权重对 Value 向量加权求和，得到当前 token 融合上下文后的表示。</p><ol><li>直观理解</li></ol><p>比如一句话：</p><pre><code>小明把书放进书包，因为它太重了。</code></pre><p>当模型处理“它”时：</p><ul><li>Q：当前“它”想找指代对象</li><li>K：其他词提供可匹配信息</li><li>V：其他词携带的语义内容</li></ul><p>如果“书”和“它”的匹配分数高，模型就会给“书”更高 attention 权重。</p><ol><li>为什么要除以 √d_k？</li></ol><p>如果向量维度很大，Q 和 K 的点积值可能变得很大。</p><p>点积太大会导致 softmax 输出非常尖锐。</p><p>比如某个位置接近 1，其他位置接近 0。</p><p>这样梯度会变小，训练不稳定。</p><p>除以 <code>√d_k</code> 可以让分数尺度更稳定。</p><p>这叫 scaled dot-product attention。</p><ol><li>Self-Attention 里 QKV 从哪里来？</li></ol><p>在 Self-Attention 中，Q、K、V 都来自同一个输入 X。</p><p>通过不同参数矩阵投影得到：</p><pre><code>Q = XW_Q<br/>K = XW_K<br/>V = XW_V</code></pre><p>也就是说，同一批 token 会分别变成查询、键和值。</p><ol><li>Cross-Attention 里 QKV 从哪里来？</li></ol><p>在 Cross-Attention 中：</p><ul><li>Q 来自 Decoder 当前状态</li><li>K、V 来自 Encoder 输出</li></ul><p>也就是 Decoder 用自己的 Query 去关注 Encoder 的输入信息。</p><p>常见于 Encoder-Decoder 架构。</p><ol><li><strong>面试背诵版：Attention 公式是什么？</strong></li></ol><blockquote><p>Attention 的核心公式是 Attention(Q, K, V) = softmax(QKᵀ / √d_k)V。这里 Q 是 Query，表示当前 token 想查询什么信息；K 是 Key，表示每个 token 可被匹配的特征；V 是 Value，表示真正被聚合的信息内容。</p><p>计算过程是先用 Q 和 K 做点积得到相似度分数，再除以 √d_k 做缩放，避免维度过大导致点积值过大、softmax 过于尖锐和梯度不稳定；然后通过 softmax 把相似度转成注意力权重，最后用这些权重对 V 加权求和，得到融合上下文信息的新表示。</p><p>简单说，Attention 就是“用 Q 和 K 算该关注谁，再按权重从 V 里取信息”。Self-Attention 中 Q、K、V 都来自同一个输入序列；Cross-Attention 中 Q 通常来自 Decoder，K 和 V 来自 Encoder 输出。</p></blockquote>"},{"t":"Q、K、V分别代表什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>Q、K、V 分别代表 Query、Key、Value，是 Attention 机制中用来计算“关注谁”和“取什么信息”的三个向量。</strong></p><ol><li>Q 是什么？</li></ol><p>Q 是 Query，查询向量。</p><p>它表示当前 token 想要找什么信息。</p><p>可以理解成：</p><pre><code>我现在需要什么？</code></pre><p>比如模型处理代词“它”时，Q 可能表示：</p><pre><code>我想找一个可以被“它”指代的对象</code></pre><ol><li>K 是什么？</li></ol><p>K 是 Key，键向量。</p><p>它表示每个 token 能提供什么匹配特征。</p><p>可以理解成：</p><pre><code>我有什么特征可以被别人匹配？</code></pre><p>当前 token 的 Q 会和所有 token 的 K 做相似度计算。</p><p>匹配分数越高，说明越应该关注那个 token。</p><ol><li>V 是什么？</li></ol><p>V 是 Value，值向量。</p><p>它表示每个 token 真正携带的信息内容。</p><p>可以理解成：</p><pre><code>如果你关注我，你能从我这里拿走什么信息？</code></pre><p>Attention 最后不是对 K 加权，而是对 V 加权求和。</p><ol><li>三者怎么配合？</li></ol><p>流程是：</p><pre><code>Q 和 K 计算相似度<br/>  ↓<br/>softmax 得到注意力权重<br/>  ↓<br/>用权重对 V 加权求和</code></pre><p>也就是：</p><pre><code>QK 决定关注谁<br/>V 决定取什么内容</code></pre><ol><li>直观例子</li></ol><p>比如句子：</p><pre><code>小明把书放进书包，因为它太重了。</code></pre><p>处理“它”时：</p><ul><li>Q：我想找“它”指代的对象</li><li>K：每个词提供自己的匹配特征</li><li>V：每个词携带的语义信息</li></ul><p>如果“书”的 K 和“它”的 Q 匹配度高，那么“书”的 V 就会被更多加权进来。</p><ol><li>Q、K、V 从哪里来？</li></ol><p>在 Self-Attention 中，Q、K、V 都来自同一个输入 X。</p><p>但它们通过不同参数矩阵变换得到：</p><pre><code>Q = XW_Q<br/>K = XW_K<br/>V = XW_V</code></pre><p>也就是说，同一个 token 会被投影成三种角色：</p><ul><li>用 Q 去查别人</li><li>用 K 被别人匹配</li><li>用 V 提供信息内容</li></ul><ol><li>为什么要分 Q、K、V？</li></ol><p>如果只有一个向量，表达能力会弱。</p><p>分成 Q、K、V 后，模型可以分别学习：</p><ul><li>如何提出查询</li><li>如何被匹配</li><li>提供什么内容</li></ul><p>这样注意力机制更灵活。</p><ol><li><strong>面试背诵版：Q、K、V 分别代表什么？</strong></li></ol><blockquote><p>Q、K、V 分别是 <strong>Query、Key、Value，是 Attention 机制中的三个向量</strong>。Q 表示当前 token 想查询什么信息，K 表示每个 token 可以被匹配的特征，V 表示每个 token 真正携带、可以被聚合的信息内容。</p><p>Attention 的过程是先用 Q 和所有 K 做相似度计算，得到当前 token 应该关注哪些 token；然后经过 softmax 得到注意力权重；最后用这些权重对 V 做加权求和，得到融合上下文后的表示。简单说，Q 和 K 决定“关注谁”，V 决定“取什么信息”。</p><p>在 Self-Attention 中，<strong>Q、K、V 都来自同一个输入序列 X，</strong>只是通过不同的参数矩阵投影得到，也就是 Q = XW_Q，K = XW_K，V = XW_V。这样模型可以分别学习查询方式、匹配方式和信息内容表达。</p></blockquote>"},{"t":"为什么需要QKV？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>需要 QKV 的核心原因是：Attention 要同时解决两个问题——“我该关注谁”和“我从对方那里取什么信息”，Q、K、V 正好把这两个过程拆开了。</p><ol><li>QKV 分别负责什么？</li></ol><p>Q 是 Query：</p><pre><code>当前 token 想找什么信息</code></pre><p>K 是 Key：</p><pre><code>每个 token 提供什么匹配特征</code></pre><p>V 是 Value：</p><pre><code>每个 token 真正提供的信息内容</code></pre><p>所以：</p><pre><code>Q 和 K 用来算注意力权重<br/>V 用来被加权求和</code></pre><ol><li>为什么不能只用一个向量？</li></ol><p>如果每个 token 只有一个表示，同时承担：</p><ul><li>查询别人</li><li>被别人匹配</li><li>提供内容</li></ul><p>表达能力会受限。</p><p>拆成 Q、K、V 后，模型可以分别学习：</p><ul><li>当前 token 怎么提出查询</li><li>其他 token 怎么被匹配</li><li>被关注后传递什么信息</li></ul><p>这让 Attention 更灵活。</p><ol><li>QK 解决“关注谁”</li></ol><p>Attention 需要判断当前 token 应该关注哪些 token。</p><p>比如处理“它”时，要判断“它”指的是“书”还是“书包”。</p><p>这一步靠：</p><pre><code>QKᵀ</code></pre><p>计算匹配分数。</p><p>匹配分数越高，注意力权重越大。</p><ol><li>V 解决“取什么信息”</li></ol><p>算出注意力权重后，模型要从被关注的 token 中拿信息。</p><p>这一步不是拿 K，而是拿 V。</p><p>因为 K 更像“索引用的特征”，V 更像“真正的内容”。</p><p>可以类比数据库：</p><pre><code>Q：查询条件<br/>K：索引键<br/>V：查到的数据内容</code></pre><ol><li>QKV 让同一个 token 扮演不同角色</li></ol><p>同一个 token 在 Attention 里可能有三种角色：</p><ul><li>用 Q 去关注别人</li><li>用 K 让别人关注自己</li><li>用 V 向别人提供信息</li></ul><p>比如句子中的“书”：</p><ul><li>它可以查询上下文</li><li>它也可以被“它”这个代词关注</li><li>它还提供“书”的语义信息</li></ul><p>QKV 分开后，这些角色可以用不同表示学习。</p><ol><li>对 Self-Attention 和 Cross-Attention 都有用</li></ol><p>Self-Attention 中：</p><pre><code>Q、K、V 都来自同一序列</code></pre><p>让序列内部 token 互相关注。</p><p>Cross-Attention 中：</p><pre><code>Q 来自 Decoder<br/>K、V 来自 Encoder</code></pre><p>让 Decoder 根据自己的生成状态去关注输入序列。</p><p>QKV 机制可以统一这两类 Attention。</p><ol><li><strong>面试背诵版：为什么需要 QKV？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Attention 需要 QKV，是因为它要同时完成“匹配”和“取信息”两个过程。Q，也就是 Query，表示当前 token 想查询什么信息；K，也就是 Key，表示每个 token 可被匹配的特征；V，也就是 Value，表示每个 token 真正携带的内容。Q 和 K 用来计算相似度和注意力权重，V 用来根据权重加权求和，生成新的上下文表示。</p><p>如果只用一个向量，让它同时负责查询、匹配和提供内容，表达能力会受限。拆成 Q、K、V 后，模型可以分别学习“怎么提出查询”“怎么被别人匹配”“被关注后传递什么信息”，因此更加灵活。可以类比数据库：Q 是查询条件，K 是索引，V 是查到的数据内容。</p><p>简单说，<strong>QK 决定“关注谁”，V 决定“拿什么信息”</strong>。这就是为什么 Attention 里要设计 Q、K、V。</p></blockquote>"},{"t":"Attention为什么需要缩放？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Attention 需要缩放，核心原因是：Q 和 K 的点积值在维度较大时会变得很大，导致 softmax 过于尖锐，梯度变小，训练不稳定。</p><ol><li>Attention 里的缩放是什么？</li></ol><p>Scaled Dot-Product Attention 的公式是：</p><pre><code>Attention(Q, K, V) = softmax(QKᵀ / √d_k) V</code></pre><p>这里的缩放就是：</p><pre><code>除以 √d_k</code></pre><p><code>d_k</code> 是 Key 向量的维度。</p><ol><li>为什么点积会变大？</li></ol><p>Q 和 K 是向量。</p><p>点积是多个维度相乘再相加：</p><pre><code>q · k = q1k1 + q2k2 + ... + q_dk_d</code></pre><p>如果维度 <code>d_k</code> 很大，累加项就很多。</p><p>即使每一维数值不大，点积结果的方差也会随着维度增大而变大。</p><p>所以 QKᵀ 的值可能变得很大。</p><ol><li>点积太大会怎么样？</li></ol><p>点积太大会进入 softmax。</p><p>比如：</p><pre><code>softmax([1, 2, 3])</code></pre><p>还比较平滑。</p><p>但如果变成：</p><pre><code>softmax([10, 20, 30])</code></pre><p>结果会非常接近 one-hot。</p><p>也就是最大值接近 1，其他值接近 0。</p><p>这会导致 attention 权重过于尖锐。</p><ol><li>Softmax 太尖锐有什么问题？</li></ol><p>如果 softmax 输出过于接近 0 或 1，会进入饱和区域。</p><p>问题是：</p><ul><li>梯度变小</li><li>训练不稳定</li><li>模型过早只关注少数 token</li><li>其他 token 几乎没有学习信号</li><li>参数更新困难</li></ul><p>所以需要把点积分数缩放到合适范围。</p><ol><li>为什么除以 √d_k？</li></ol><p>如果 Q 和 K 的每个维度均值为 0、方差为 1，那么点积的方差大约和 <code>d_k</code> 成正比。</p><p>也就是：</p><pre><code>Var(q · k) ≈ d_k</code></pre><p>除以 <code>√d_k</code> 后，方差就被拉回到比较稳定的范围。</p><p>这样 softmax 输入不会因为维度变大而过大。</p><ol><li>缩放的作用</li></ol><p>缩放的作用可以总结为：</p><ul><li>控制 attention score 的尺度</li><li>防止 softmax 饱和</li><li>保持梯度稳定</li><li>提升训练稳定性</li><li>避免维度变大导致分数过大</li></ul><p>它不是改变 Attention 的逻辑，而是让数值更稳定。</p><ol><li>不缩放可以吗？</li></ol><p>小维度时可能影响不大。</p><p>但在 Transformer 里，head 维度通常不小。</p><p>不缩放会让训练更不稳定，尤其在深层模型、大模型中更明显。</p><p>所以标准 Attention 都会加这个缩放。</p><ol><li><strong>面试背诵版：Attention 为什么需要缩放？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Attention 需要缩放，是因为 Q 和 K 的点积会随着向量维度增大而变大。点积本质上是多个维度乘积的累加，如果 Key 的维度 d_k 较大，QKᵀ 的方差也会增大，导致输入 softmax 的分数过大。</p><p>如果 softmax 输入过大，输出会变得非常尖锐，接近 one-hot，也就是某个位置权重接近 1，其他位置接近 0。这样会让 softmax 进入饱和区，梯度变小，训练不稳定，并且模型可能过早只关注少数 token。</p><p>所以 <strong>Scaled Dot-Product Attention 会用 QKᵀ / √d_k 做缩放</strong>。除以 √d_k 可以把点积分数的方差控制在稳定范围内，避免 softmax 饱和，保持梯度稳定。简单说，<strong>缩放的作用是“防止 attention 分数过大，让 softmax 和训练更稳定”</strong>。</p></blockquote>"},{"t":"Softmax在Attention中的作用是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Softmax 在 Attention 中的核心作用是：<strong>把 Q 和 K 计算出来的相似度分数，转换成一组可解释的注意力权重。</strong></p><ol><li>Attention 里 Softmax 处理什么？</li></ol><p>Attention 先计算：</p><pre><code>QKᵀ / √d_k</code></pre><p>这个结果是当前 token 对其他 token 的相关性分数。</p><p>但这些分数可能是：</p><ul><li>正数</li><li>负数</li><li>很大</li><li>很小</li><li>没有归一化</li></ul><p>Softmax 会把它们转换成概率分布。</p><ol><li>Softmax 输出什么？</li></ol><p>Softmax 输出的注意力权重满足：</p><pre><code>每个权重 ≥ 0<br/>所有权重加起来 = 1</code></pre><p>比如原始分数是：</p><pre><code>[2.0, 1.0, 0.1]</code></pre><p>Softmax 后可能是：</p><pre><code>[0.66, 0.24, 0.10]</code></pre><p>这表示当前 token 更关注第一个 token。</p><ol><li>为什么要转成权重？</li></ol><p>因为下一步要对 V 加权求和：</p><pre><code>softmax(QKᵀ / √d_k) V</code></pre><p>Softmax 让模型知道：</p><pre><code>哪些 token 重要，多取一点<br/>哪些 token 不重要，少取一点</code></pre><p>如果没有归一化权重，加权求和的尺度可能不稳定。</p><ol><li>Softmax 强调高分 token</li></ol><p>Softmax 有一个特点：</p><pre><code>分数越高，权重会被放大<br/>分数越低，权重会被压小</code></pre><p>所以它能让模型更集中地关注相关 token。</p><p>比如处理“它”时，如果“书”的匹配分数最高，softmax 会给“书”更大权重。</p><ol><li>Softmax 保留可微性</li></ol><p>Attention 需要端到端训练。</p><p>Softmax 是可微的。</p><p>所以模型可以通过反向传播学习：</p><ul><li>Q 怎么生成</li><li>K 怎么匹配</li><li>V 怎么表达</li><li>哪些 token 应该被关注</li></ul><p>如果用硬选择，比如直接选最大值，就不利于梯度传播。</p><ol><li>和 Mask 配合使用</li></ol><p>在 Decoder-only 模型中，Softmax 前通常会加 causal mask。</p><p>未来位置的分数会被设成：</p><pre><code>-∞</code></pre><p>经过 softmax 后，这些位置权重变成 0。</p><p>这样模型就不能关注未来 token。</p><ol><li><strong>面试背诵版：Softmax 在 Attention 中的作用是什么？</strong></li></ol><blockquote><p>Softmax 在 Attention 中的作用，是把 Q 和 K 点积得到的相似度分数转换成注意力权重。QKᵀ / √d_k 得到的是当前 token 对其他 token 的相关性分数，这些分数没有归一化，可能为正、为负或尺度很大。Softmax 会把它们变成非负且总和为 1 的概率分布。</p><p>这些注意力权重会用于对 V 做加权求和，权重越大的 token，对最终表示贡献越大。Softmax 还能放大高相关 token 的影响、压低低相关 token 的影响，使模型更关注重要上下文。同时 Softmax 是可微的，可以通过反向传播端到端学习注意力分布。</p><p>在 Decoder-only 模型中，Softmax 还会和 causal mask 配合使用，把未来 token 的分数设为负无穷，使其 softmax 后权重为 0，从而防止模型看到未来信息。简单说，<strong>Softmax 的作用是“把相关性分数变成可训练、可归一化的注意力权重”</strong>。</p></blockquote>"},{"t":"Self Attention计算过程是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Self-Attention 的核心过程是：<strong>同一个序列中的 token 之间互相计算相关性，然后根据相关性加权聚合上下文信息。</strong></p><ol><li>输入先变成向量</li></ol><p>输入文本先经过 tokenizer 和 embedding。</p><p>比如句子：</p><pre><code>我 喜欢 苹果</code></pre><p>会变成向量矩阵：</p><pre><code>X = [x1, x2, x3]</code></pre><p>每个 token 对应一个向量。</p><ol><li>生成 Q、K、V</li></ol><p>Self-Attention 中，Q、K、V 都来自同一个输入 X。</p><p>通过三个不同的参数矩阵投影得到：</p><pre><code>Q = XW_Q<br/>K = XW_K<br/>V = XW_V</code></pre><p>含义是：</p><ul><li>Q：每个 token 想查询什么</li><li>K：每个 token 能被匹配的特征</li><li>V：每个 token 提供的信息内容</li></ul><ol><li>计算注意力分数</li></ol><p>用 Q 和 K 做点积：</p><pre><code>Score = QKᵀ</code></pre><p>这个分数表示 token 之间的相关性。</p><p>比如当前 token 和某个 token 越相关，分数越高。</p><ol><li>缩放</li></ol><p>为了防止点积结果过大，要除以：</p><pre><code>√d_k</code></pre><p>得到：</p><pre><code>Score = QKᵀ / √d_k</code></pre><p>这样可以避免 softmax 过于尖锐，保持训练稳定。</p><ol><li>加 Mask</li></ol><p>如果是 Decoder-only 模型，需要加 causal mask。</p><p>作用是：</p><pre><code>当前位置不能看到未来 token</code></pre><p>比如预测第 3 个 token 时，只能看第 1、2 个 token。</p><p>未来位置的分数会被设成负无穷，softmax 后权重为 0。</p><p>如果是 Encoder 的双向 Self-Attention，一般不需要 causal mask。</p><ol><li>Softmax 得到权重</li></ol><p>对分数做 softmax：</p><pre><code>A = softmax(QKᵀ / √d_k)</code></pre><p>得到注意力权重矩阵。</p><p>每一行表示：</p><pre><code>某个 token 对所有 token 的关注程度</code></pre><p>权重越大，说明越关注那个 token。</p><ol><li>加权求和 V</li></ol><p>最后用注意力权重对 V 加权求和：</p><pre><code>Output = A V</code></pre><p>也就是：</p><pre><code>每个 token 根据自己关注的 token，聚合对应信息</code></pre><p>输出就是融合了上下文的新表示。</p><ol><li>完整公式</li></ol><p>完整公式是：</p><pre><code>Self-Attention(X) = softmax((XW_Q)(XW_K)ᵀ / √d_k) (XW_V)</code></pre><p>如果带 mask：</p><pre><code>softmax((QKᵀ + mask) / √d_k) V</code></pre><ol><li><strong>面试背诵版：Self-Attention 计算过程是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Self-Attention 的计算过程可以分为几步。首先，输入 token 经过 embedding 得到向量矩阵 X；然后通过三个不同的线性变换得到 Q、K、V，也就是 Q = XW_Q，K = XW_K，V = XW_V。因为是 Self-Attention，所以 Q、K、V 都来自同一个输入序列。</p><p>接着用 Q 和 K 做点积，得到 token 之间的相关性分数 QKᵀ，再除以 √d_k 进行缩放，防止分数过大导致 softmax 饱和。如果是 Decoder-only 模型，还会加 causal mask，避免当前位置看到未来 token。然后对分数做 softmax，得到注意力权重矩阵，最后用这个权重矩阵对 V 做加权求和，得到每个 token 融合上下文后的新表示。</p><p>简单说，<strong>（必会！）</strong><strong>Self-Attention 的流程是“输入生成 QKV → QK 算相关性 → 缩放和 mask → softmax 得到权重 → 加权 V 得到输出</strong>”。它的作用是让同一个序列中的每个 token 都能根据相关性动态聚合其他 token 的信息。</p></blockquote>"},{"t":"Multi Head Attention为什么有效？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Multi-Head Attention 有效的核心原因是：<strong>它让模型从多个子空间、多个角度同时关注不同信息，而不是只用一个注意力模式。</strong></p><ol><li>单头 Attention 的问题</li></ol><p>单头 Attention 只有一组 Q、K、V。</p><p>它只能学一种注意力分布。</p><p>比如在一句话里，它可能主要关注：</p><pre><code>主谓关系</code></pre><p>但语言里的关系很复杂，还包括：</p><ul><li>指代关系</li><li>长距离依赖</li><li>局部短语</li><li>语法结构</li><li>语义相关</li><li>位置关系</li></ul><p>一个 head 很难同时稳定捕捉所有关系。</p><ol><li>Multi-Head 是什么？</li></ol><p>Multi-Head Attention 会把输入映射到多个不同子空间。</p><p>每个 head 都有自己的：</p><pre><code>W_Q, W_K, W_V</code></pre><p>每个 head 独立计算 attention。</p><p>然后把多个 head 的结果拼接起来，再做一次线性变换。</p><p>流程是：</p><pre><code>输入 X<br/>  ↓<br/>多个 head 分别计算 Attention<br/>  ↓<br/>拼接 concat<br/>  ↓<br/>线性变换<br/>  ↓<br/>输出</code></pre><ol><li>多个 head 可以关注不同关系</li></ol><p>不同 head 可以学习不同注意力模式。</p><p>比如：</p><ul><li>有的 head 关注相邻词</li><li>有的 head 关注主语</li><li>有的 head 关注宾语</li><li>有的 head 关注代词指代</li><li>有的 head 关注长距离依赖</li><li>有的 head 关注特殊符号或位置</li></ul><p>这让模型表达能力更强。</p><ol><li>多个子空间提升表达能力</li></ol><p>每个 head 的维度通常比整体 hidden size 小。</p><p>比如 hidden size 是 768，12 个 head，每个 head 维度是 64。</p><p>每个 head 在不同低维子空间里学习关系。</p><p>这相当于让模型从不同“视角”理解同一句话。</p><ol><li>比单头更稳定</li></ol><p>如果只有一个 head，注意力模式可能比较单一。</p><p>多个 head 可以分担不同关系。</p><p>这样模型不会过度依赖某一种注意力模式。</p><p>对于复杂任务，多头机制通常更稳定、更鲁棒。</p><ol><li>举个例子</li></ol><p>句子：</p><pre><code>小明把书放进书包，因为它太重了。</code></pre><p>不同 head 可能分别关注：</p><ul><li>“它”和“书”的指代关系</li><li>“书”和“重”的语义关系</li><li>“放进”和“书包”的动作关系</li><li>相邻词之间的局部关系</li></ul><p>最后这些信息被拼接融合，形成更完整的表示。</p><ol><li>为什么不是 head 越多越好？</li></ol><p>head 太少，表达能力可能不够。</p><p>但 head 太多也有问题：</p><ul><li>每个 head 维度变小</li><li>单个 head 表达能力下降</li><li>计算开销增加</li><li>可能出现冗余 head</li><li>不一定带来效果提升</li></ul><p>所以 head 数需要和 hidden size、模型规模匹配。</p><ol><li><strong>面试背诵版：Multi-Head Attention 为什么有效？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Multi-Head Attention 有效，是因为单个 Attention head 只能学习一种注意力分布，而语言和序列中的关系往往很复杂，包括局部关系、长距离依赖、语法结构、语义关系和指代关系。多头注意力让模型可以在多个不同的表示子空间中并行计算 Attention，<strong>每个 head 都有自己的 Q、K、V 投影，可以关注不同类型的信息。</strong></p><p>比如有的 head 关注相邻词，有的关注主谓宾关系，有的关注代词指代，有的关注长距离依赖。多个 head 的结果拼接后再经过线性变换，<strong>模型就能融合多种视角的信息，表达能力比单头 Attention 更强</strong>，也更稳定。</p><p>但 head 数不是越多越好，因为 hidden size 固定时，head 越多，每个 head 的维度越小，单个 head 表达能力会下降，还可能带来冗余和额外计算成本。所以 Multi-Head 的关键是让模型从多个角度并行关注信息，而不是简单堆数量。</p></blockquote>"},{"t":"多个Attention Head分别学习什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>多个 Attention Head 通常会学习不同类型的关系和信息模式，比如局部依赖、长距离依赖、语法关系、语义关系、位置关系和指代关系。</p><ol><li>有的 Head 学局部关系</li></ol><p>一些 head 会关注当前 token 附近的词。</p><p>比如：</p><pre><code>我 喜欢 吃 苹果</code></pre><p>处理“吃”时，可能重点关注：</p><pre><code>喜欢<br/>苹果</code></pre><p>这种 head 更像捕捉短语和局部上下文。</p><ol><li>有的 Head 学长距离依赖</li></ol><p>一些 head 会关注距离很远但语义相关的 token。</p><p>比如长句里前面的主语和后面的谓语。</p><p>这类 head 帮助模型解决：</p><ul><li>跨句关系</li><li>长文本关联</li><li>前后文一致性</li><li>远距离条件依赖</li></ul><ol><li>有的 Head 学语法结构</li></ol><p>一些 head 可能关注句法关系。</p><p>比如：</p><ul><li>主语和谓语</li><li>动词和宾语</li><li>修饰语和被修饰词</li><li>介词短语关系</li></ul><p>例如：</p><pre><code>小明认真地完成了作业</code></pre><p>某些 head 可能关注“小明”和“完成”之间的主谓关系。</p><ol><li>有的 Head 学语义关系</li></ol><p>有些 head 会关注语义上相关的词。</p><p>比如：</p><pre><code>医生 给 病人 开 药</code></pre><p>可能关注：</p><ul><li>医生 和 开药</li><li>病人 和 药</li><li>动作 和 对象</li></ul><p>这类关系不一定相邻，但语义上有关。</p><ol><li>有的 Head 学指代关系</li></ol><p>一些 head 会关注代词和它指代的对象。</p><p>比如：</p><pre><code>小明把书放进书包，因为它太重了。</code></pre><p>某些 head 可能学习：</p><pre><code>它 → 书</code></pre><p>这对阅读理解、对话和长文本非常重要。</p><ol><li>有的 Head 学位置信息</li></ol><p>有些 head 会关注固定位置模式。</p><p>比如：</p><ul><li>前一个 token</li><li>后一个 token</li><li>句首 token</li><li>分隔符</li><li>特殊标记</li><li>段落边界</li></ul><p>这有助于模型理解序列结构。</p><ol><li>有的 Head 可能是冗余的</li></ol><p>不是每个 head 都一定学到清晰功能。</p><p>有些 head 可能：</p><ul><li>关注模式重复</li><li>信息贡献较小</li><li>可以被剪枝</li><li>在不同任务中作用不同</li></ul><p>所以多头注意力提供的是“学习多种关系的机会”，而不是人工指定每个 head 的职责。</p><ol><li><strong>面试背诵版：多个 Attention Head 分别学习什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>多个 Attention Head 通常会在不同表示子空间中学习不同类型的信息关系。有些 head 关注局部上下文，比如相邻词或短语结构；有些 head 关注长距离依赖，比如跨句信息、前后文一致性；有些 head 学习语法关系，比如主谓、动宾、修饰关系；有些 head 学习语义关系，比如动作和对象、实体之间的关联；还有些 head 可能关注代词指代、特殊 token、位置模式或段落边界。</p><p>多头注意力的意义不是人为规定每个 head 学什么，而是给模型多个并行视角，让不同 head 自动学习不同的注意力模式。最后这些 head 的输出会拼接并线性变换，融合成更丰富的表示。</p><p>但也要注意，不是所有 head 都一定有明确可解释的功能，有些 head 可能是冗余的，甚至可以被剪枝。简单说，多个 Attention Head 让模型可以同时关注“局部、长程、语法、语义、指代和位置”等不同信息。</p></blockquote>"},{"t":"Attention复杂度为什么是O(n²)？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Attention 复杂度是 O(n²)，核心原因是：<strong>序列里每个 token 都要和其他所有 token 计算一次相关性。</strong></p><ol><li>n 是什么？</li></ol><p>这里的 n 指序列长度，也就是 token 数量。</p><p>比如输入有：</p><pre><code>n = 1000 个 token</code></pre><p>Attention 要计算这些 token 之间的两两关系。</p><ol><li>Self-Attention 要两两计算</li></ol><p>在 Self-Attention 中，每个 token 都会生成自己的 Q。</p><p>然后它要和所有 token 的 K 做匹配。</p><p>比如：</p><pre><code>第 1 个 token 看所有 n 个 token<br/>第 2 个 token 看所有 n 个 token<br/>...<br/>第 n 个 token 看所有 n 个 token</code></pre><p>所以总共是：</p><pre><code>n × n = n²</code></pre><p>这就是 O(n²) 的来源。</p><ol><li>从矩阵角度看</li></ol><p>Attention 分数计算是：</p><pre><code>QKᵀ</code></pre><p>如果：</p><pre><code>Q 的形状是 n × d<br/>K 的形状是 n × d</code></pre><p>那么：</p><pre><code>QKᵀ 的结果是 n × n</code></pre><p>这个 n × n 矩阵表示每个 token 对每个 token 的注意力分数。</p><p>计算和存储这个矩阵都和 n² 有关。</p><ol><li>显存也是 O(n²)</li></ol><p>不只是计算复杂度。</p><p>Attention 权重矩阵大小也是：</p><pre><code>n × n</code></pre><p>所以序列越长，显存占用增长很快。</p><p>比如序列长度从 1k 增加到 2k：</p><pre><code>Attention 矩阵大小变成 4 倍</code></pre><p>因为是平方增长。</p><ol><li>为什么长文本成本高？</li></ol><p>因为 O(n²) 会导致长序列非常贵。</p><p>比如：</p><pre><code>1,000 token → 1,000,000 个 attention 分数<br/>10,000 token → 100,000,000 个 attention 分数</code></pre><p>token 数增加 10 倍，attention 分数增加 100 倍。</p><p>这就是长上下文模型难训练、难推理的重要原因。</p><ol><li>FFN 复杂度也是成本，但不是 n²</li></ol><p>Transformer 里 FFN 也很耗计算。</p><p>FFN 通常复杂度和序列长度 n 是线性关系：</p><pre><code>O(n)</code></pre><p>更准确说和 <code>n × d²</code> 有关。</p><p>但 Attention 对序列长度是平方级：</p><pre><code>O(n² × d)</code></pre><p>所以长序列时 Attention 成本特别明显。</p><ol><li>有什么优化方法？</li></ol><p>为了解决 O(n²)，有很多优化：</p><ul><li>Sparse Attention</li><li>Sliding Window Attention</li><li>Linear Attention</li><li>FlashAttention</li><li>Longformer</li><li>Performer</li><li>分块 Attention</li><li>KV Cache</li><li>RoPE 外推和长上下文优化</li></ul><p>它们本质上都是减少计算、减少显存或提升 attention kernel 效率。</p><ol><li><strong>面试背诵版：Attention 复杂度为什么是 O(n²)？</strong></li></ol><blockquote><p>Attention 的复杂度是 O(n²)，主要是因为 Self-Attention 中<strong>每个 token 都要和序列中的所有 token 计算一次相关性。</strong>假设序列长度是 n，那么第 1 个 token 要看 n 个 token，第 2 个 token 也要看 n 个 token，一直到第 n 个 token，总共需要计算 n × n 个注意力分数，所以复杂度随序列长度平方增长。</p><p>从矩阵角度看，Attention 的核心是计算 QKᵀ。如果 Q 和 K 的形状都是 n × d，那么 QKᵀ 的结果就是 n × n 的注意力分数矩阵。这个矩阵不仅计算量是 O(n²)，存储注意力权重的显存开销也是 O(n²)。所以当序列长度翻倍时，Attention 矩阵大小大约变成 4 倍。</p><p>这也是长文本场景下 Transformer 成本高的主要原因之一。为了缓解这个问题，常见优化包括 Sparse Attention、Sliding Window Attention、Linear Attention、FlashAttention、分块 Attention 和 KV Cache 等。简单说，Attention 是 O(n²)，<strong>因为它要建模所有 token 两两之间的关系。</strong></p></blockquote>"}]},{"no":"","title":"4.3 Transformer细节","questions":[{"t":"FFN作用是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p>FFN 的核心作用是：<strong>对每个 token 的表示做非线性变换，提升模型表达能力。</strong></p><ol><li>FFN 是什么？</li></ol><p>FFN 全称是 Feed Forward Network，前馈神经网络。</p><p>在 Transformer 里，每一层通常包括：</p><pre><code>Self-Attention + FFN</code></pre><p>Attention 负责让 token 之间交换信息。</p><p>FFN 负责对每个 token 自己的表示做进一步加工。</p><ol><li>FFN 的基本结构</li></ol><p>Transformer 里的 FFN 通常是两层线性层加一个激活函数：</p><pre><code>FFN(x) = W2 * activation(W1x + b1) + b2</code></pre><p>常见激活函数有：</p><ul><li>ReLU</li><li>GELU</li><li>SwiGLU</li></ul><p>一般会先升维，再降维。</p><p>比如：</p><pre><code>hidden size 4096<br/>FFN 中间维度 11008 或 16384<br/>再降回 4096</code></pre><ol><li>Attention 和 FFN 分工不同</li></ol><p>Attention 负责 token 之间的信息交互。</p><p>比如：</p><pre><code>“它”关注“书”<br/>“动词”关注“宾语”</code></pre><p>FFN 负责每个 token 内部的特征变换。</p><p>可以理解为：</p><pre><code>Attention：混合不同 token 的信息<br/>FFN：加工每个 token 的信息</code></pre><ol><li>为什么需要 FFN？</li></ol><p>如果只有 Attention，模型主要做的是上下文信息加权混合。</p><p>但模型还需要非线性变换来学习复杂特征。</p><p>FFN 提供：</p><ul><li>非线性表达能力</li><li>特征组合</li><li>维度变换</li><li>信息加工</li><li>参数容量</li><li>复杂模式建模</li></ul><p>所以 FFN 是 Transformer 表达能力的重要来源。</p><ol><li>FFN 对每个位置独立作用</li></ol><p>Transformer 中 FFN 是 position-wise 的。</p><p>意思是：</p><pre><code>每个 token 用同一个 FFN 独立处理</code></pre><p>不同 token 之间不在 FFN 里直接交互。</p><p>token 之间的交互主要由 Attention 完成。</p><ol><li>FFN 参数量很大</li></ol><p>在很多 Transformer 模型里，FFN 占了大量参数和计算。</p><p>因为 FFN 通常会把 hidden size 扩大 3 到 4 倍。</p><p>比如：</p><pre><code>d_model → 4d_model → d_model</code></pre><p>所以 FFN 不只是一个小模块，而是模型容量的重要组成部分。</p><ol><li>直观理解</li></ol><p>可以把 Transformer 一层理解成：</p><pre><code>Attention：收集上下文信息<br/>FFN：对收集到的信息进行思考和加工</code></pre><p>比如一个 token 通过 Attention 得到了相关上下文，FFN 再把这些信息转成更有用的高层表示。</p><ol><li><strong>面试背诵版：FFN 作用是什么？</strong></li></ol><blockquote><p>FFN，也就是 Feed Forward Network，是 Transformer 每一层中的前馈网络模块。它通常由两层线性变换和一个非线性激活函数组成，比如 FFN(x) = W2 * activation(W1x + b1) + b2，常见激活函数有 ReLU、GELU 或 SwiGLU。FFN 一般会先把 hidden size 升维，再降回原维度。</p><p>在 Transformer 中，Attention 主要负责不同 token 之间的信息交互，比如让一个 token 关注上下文中的相关 token；而 FFN 主要负责对每个 token 的表示做非线性变换和特征加工。它是 position-wise 的，也就是每个 token 独立经过同一个 FFN，不直接和其他 token 交互。</p><p>简单说，<strong>Attention 负责“从上下文收集信息”</strong>，FFN 负责<strong>“对当前 token 的信息进行非线性加工”</strong>。FFN 提供了模型的重要参数容量和非线性表达能力，所以是 Transformer 能拟合复杂模式的重要组成部分。</p></blockquote>"},{"t":"为什么FFN通常扩大维度？","tag":"","p":"","html":"<ol><li>为什么 FFN 通常扩大维度？</li></ol><span>[原文档此处有配图]</span><ol><li>FFN 的常见结构</li></ol><p>Transformer 里的 FFN 通常是：</p><pre><code>d_model → d_ff → d_model</code></pre><p>其中：</p><pre><code>d_ff 通常比 d_model 大</code></pre><p>比如：</p><pre><code>d_model = 4096<br/>d_ff = 11008 或 16384</code></pre><p>也就是先升维，再降维。</p><ol><li>为什么要先升维？</li></ol><p>升维可以给模型更大的中间表示空间。</p><p>在高维空间里，模型更容易表达复杂特征。</p><p>比如原来一个 token 的表示维度是 4096，升到 16384 后，可以表示更多中间特征组合。</p><p>可以理解成：</p><pre><code>先展开信息，再重新组合。</code></pre><ol><li>提升非线性表达能力</li></ol><p>FFN 中间有激活函数，比如：</p><pre><code>GELU / ReLU / SwiGLU</code></pre><p>如果先升维，再经过非线性激活，模型可以学习更多复杂模式。</p><p>比如：</p><ul><li>特征交互</li><li>条件选择</li><li>语义变换</li><li>稀疏激活</li><li>高层抽象</li></ul><p>这比直接在原维度上做变换表达能力更强。</p><ol><li>类似“特征扩展”</li></ol><p>可以把 FFN 升维理解成特征扩展。</p><p>比如原始表示里有一些基础特征。</p><p>升维后，模型可以组合出更多隐含特征。</p><p>然后降维时再把有用信息压回原来的 hidden size，方便和残差连接相加。</p><ol><li>为什么最后要降回原维度？</li></ol><p>因为 Transformer 每一层需要保持统一 hidden size。</p><p>残差连接要求：</p><pre><code>输出维度 = 输入维度</code></pre><p>否则没法做：</p><pre><code>x + FFN(x)</code></pre><p>所以 FFN 先升维提升表达能力，最后再降回 d_model，保持结构一致。</p><ol><li>FFN 是模型容量的重要来源</li></ol><p>FFN 通常占 Transformer 很多参数。</p><p>因为：</p><pre><code>W1: d_model × d_ff<br/>W2: d_ff × d_model</code></pre><p>d_ff 越大，参数越多，表达能力越强。</p><p>很多大模型的 FFN 参数量非常大。</p><ol><li>为什么不是无限扩大？</li></ol><p>d_ff 太大会带来：</p><ul><li>参数量增加</li><li>计算量增加</li><li>显存增加</li><li>推理延迟增加</li><li>过拟合风险增加</li></ul><p>所以 d_ff 通常按经验比例设置，比如原始 Transformer 常用约 4 倍，也有模型使用 SwiGLU 后采用不同扩展比例。</p><ol><li><strong>面试背诵版：为什么 FFN 通常扩大维度？</strong></li></ol><blockquote><p>Transformer 中的 FFN 通常采用 d_model → d_ff → d_model 的结构，也就是先把 hidden size 升到更高维度，再降回原维度。<strong>这样做的主要原因是升维可以提供更大的中间表示空间，让模型学习更丰富的特征组合和非线性模式。</strong>经过 ReLU、GELU 或 SwiGLU 等激活函数后，高维空间中的特征可以被非线性筛选和重组，从而提升模型表达能力。</p><p>可以把这个过程理解为“先展开信息，再重新组合”。Attention 负责聚合上下文信息，FFN 通过升维和非线性变换进一步加工每个 token 的表示。最后需要降回 d_model，是因为 Transformer 每层要保持统一 hidden size，方便和残差连接相加。</p><p>不过 FFN 维度不是越大越好，因为 d_ff 越大，参数量、计算量、显存和推理延迟都会增加。所以通常会在表达能力和计算成本之间取平衡，比如常见设置是 d_ff 约为 d_model 的 4 倍，或者在使用 SwiGLU 时采用其他比例。</p></blockquote>"},{"t":"SwiGLU是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>SwiGLU 是一种用于 Transformer FFN 里的门控激活结构，核心作用是增强 FFN 的非线性表达能力和信息筛选能力。</p><ol><li>先看普通 FFN</li></ol><p>普通 Transformer FFN 通常是：</p><pre><code>FFN(x) = W2 * activation(W1x)</code></pre><p>比如：</p><pre><code>x → Linear → GELU/ReLU → Linear</code></pre><p>它通过激活函数引入非线性。</p><ol><li>GLU 是什么？</li></ol><p>GLU 全称是 Gated Linear Unit，门控线性单元。</p><p>它的思想是：</p><pre><code>一部分负责生成内容<br/>一部分负责控制门</code></pre><p>类似：</p><pre><code>输出 = 内容分支 × 门控分支</code></pre><p>门控分支决定哪些信息通过，哪些信息被抑制。</p><ol><li>SwiGLU 是什么？</li></ol><p>SwiGLU 可以理解成：</p><pre><code>Swish 激活 + GLU 门控</code></pre><p>常见形式大概是：</p><pre><code>SwiGLU(x) = (xW1) * Swish(xW2)</code></pre><p>然后再接一个输出投影：</p><pre><code>Output = SwiGLU(x) W3</code></pre><p>其中 Swish 是一种平滑激活函数：</p><pre><code>Swish(x) = x * sigmoid(x)</code></pre><ol><li>它和普通 FFN 的区别</li></ol><p>普通 FFN 是：</p><pre><code>Linear → Activation → Linear</code></pre><p>SwiGLU FFN 更像：</p><pre><code>Linear 分支 A<br/>Linear 分支 B → Swish<br/>A × B<br/>再 Linear 输出</code></pre><p>也就是用一个门控分支动态控制另一个内容分支。</p><ol><li>为什么有效？</li></ol><p>SwiGLU 有几个好处：</p><ul><li>增强非线性表达能力</li><li>让模型动态选择信息</li><li>门控机制可以过滤无用特征</li><li>训练效果通常优于 ReLU / GELU FFN</li><li>在大模型中经验表现更好</li></ul><p>可以理解成：</p><pre><code>普通 FFN 是直接加工信息；<br/>SwiGLU 是先判断哪些信息重要，再加工输出。</code></pre><ol><li>为什么大模型常用？</li></ol><p>很多现代大语言模型使用 SwiGLU 或类似 GLU 变体。</p><p>原因是它在相近计算量下通常能带来更好效果。</p><p>尤其在 Transformer FFN 中，门控结构可以提升模型容量利用率。</p><ol><li>直观理解</li></ol><p>比如一个 token 表示里有很多特征。</p><p>内容分支生成候选信息。</p><p>门控分支根据当前输入决定：</p><pre><code>哪些特征放大<br/>哪些特征压制</code></pre><p>最后输出的是被筛选后的特征组合。</p><ol><li><strong>面试背诵版：SwiGLU 是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>SwiGLU 是一种常用于 Transformer FFN 模块中的门控激活结构，可以理解为 Swish 激活函数和 GLU 门控机制的结合。普通 FFN 通常是 Linear → ReLU/GELU → Linear<strong>，而 SwiGLU 会把输入分成两个线性分支：一个分支生成内容，另一个分支经过 Swish 激活作为门控，然后两者逐元素相乘，再经过线性层输出。</strong></p><p>它的优势是引入了动态门控机制，让模型可以根据当前输入选择哪些特征通过、哪些特征被抑制，从而增强非线性表达能力和信息筛选能力。相比普通 ReLU 或 GELU FFN，SwiGLU 在很多大模型中表现更好，能更有效利用 FFN 的参数容量。</p><p>简单说，SwiGLU 不是简单地激活特征，而是“<strong>用一个门控分支控制另一个内容分支</strong>”，让 FFN 具备更强的特征选择和表达能力。</p></blockquote>"},{"t":"LayerNorm作用是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>LayerNorm 的核心作用是：稳定每一层的输入分布，让训练更稳定、更快收敛，尤其适合 Transformer 和大模型。</p><ol><li>LayerNorm 是什么？</li></ol><p>LayerNorm 全称是 Layer Normalization。</p><p>它会对一个样本内部的 hidden 维度做归一化。</p><p>比如一个 token 的 hidden 向量是：</p><pre><code>[x1, x2, x3, ..., xd]</code></pre><p>LayerNorm 会计算这个向量的均值和方差，然后归一化：</p><pre><code>减均值，除以标准差</code></pre><p>再乘上可学习缩放参数，加上可学习偏置。</p><ol><li>它解决什么问题？</li></ol><p>深层网络训练时，每一层输入分布可能不断变化。</p><p>这会导致：</p><ul><li>训练不稳定</li><li>梯度容易异常</li><li>收敛慢</li><li>对学习率敏感</li><li>深层模型难训练</li></ul><p>LayerNorm 通过归一化，让每层输入保持相对稳定。</p><ol><li>为什么 Transformer 常用 LayerNorm？</li></ol><p>Transformer 中每层都有：</p><ul><li>Attention</li><li>FFN</li><li>Residual Connection</li></ul><p>这些模块叠很多层后，激活值尺度可能变化很大。</p><p>LayerNorm 可以控制 hidden state 的尺度。</p><p>让深层 Transformer 更容易训练。</p><ol><li>LayerNorm 怎么归一化？</li></ol><p>对每个样本、每个 token 的 hidden 维度做归一化。</p><p>简单说：</p><pre><code>一个 token 内部归一化</code></pre><p>而不是跨 batch 归一化。</p><p>这点和 BatchNorm 不同。</p><ol><li>LayerNorm 和 BatchNorm 区别</li></ol><p>BatchNorm 通常对 batch 维度做统计。</p><p>它依赖 batch 内样本分布。</p><p>LayerNorm 对单个样本的特征维度做统计。</p><p>它不依赖 batch size。</p><p>所以 LayerNorm 更适合：</p><ul><li>NLP</li><li>变长序列</li><li>小 batch</li><li>自回归生成</li><li>Transformer</li></ul><ol><li>Pre-LN 和 Post-LN</li></ol><p>Transformer 里 LayerNorm 有两种常见位置。</p><p>Post-LN：</p><pre><code>x + Sublayer(x) → LayerNorm</code></pre><p>Pre-LN：</p><pre><code>x + Sublayer(LayerNorm(x))</code></pre><p>现在很多大模型更常用 Pre-LN。</p><p>因为 Pre-LN 梯度传播更稳定，深层模型更容易训练。</p><ol><li>LayerNorm 的参数</li></ol><p>LayerNorm 不是完全固定归一化。</p><p>它有可学习参数：</p><ul><li>gamma：缩放</li><li>beta：平移</li></ul><p>这样模型可以在归一化后重新学习合适的表示尺度。</p><ol><li><strong>面试背诵版：LayerNorm 作用是什么？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>LayerNorm 的作用是对每个样本内部的 hidden 维度进行归一化，使每层输入的均值和方差保持稳定，从而缓解深层网络训练不稳定、梯度异常、收敛慢和对学习率敏感的问题。它会先减去均值、除以标准差，再通过可学习的 gamma 和 beta 做缩放和平移。</p><p>在 Transformer 中，Attention、FFN 和残差连接层层堆叠后，激活值尺度容易发生变化，LayerNorm 可以稳定 hidden state 的分布，让深层 Transformer 更容易训练。和 BatchNorm 不同，LayerNorm 不依赖 batch 维度统计，而是在单个 token 或样本的 hidden 维度上归一化，因此更适合 NLP、变长序列、小 batch 和自回归生成场景。</p><p>另外，Transformer 中常见 Pre-LN 和 Post-LN 两种结构，现代大模型很多采用 Pre-LN，因为它更有利于梯度传播和深层模型稳定训练。简单说，LayerNorm 的作用是“<strong>稳定每层表示的尺度，让大模型更好训练</strong>”。</p></blockquote>"},{"t":"Pre-Norm和Post-Norm区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Pre-Norm 和 Post-Norm 的核心区别是：<strong>LayerNorm 放在子层前还是子层后；Pre-Norm 更稳定，Post-Norm 表达上更接近原始 Transformer，但深层训练更难。</strong></p><ol><li>Post-Norm 是什么？</li></ol><p>Post-Norm 是原始 Transformer 里的结构。</p><p>形式是：</p><pre><code>y = LayerNorm(x + Sublayer(x))</code></pre><p>也就是：</p><pre><code>先经过 Attention / FFN<br/>再做残差相加<br/>最后 LayerNorm</code></pre><p>流程：</p><pre><code>x → Sublayer → + x → LayerNorm</code></pre><ol><li>Pre-Norm 是什么？</li></ol><p>Pre-Norm 是把 LayerNorm 放到子层前面。</p><p>形式是：</p><pre><code>y = x + Sublayer(LayerNorm(x))</code></pre><p>流程：</p><pre><code>x → LayerNorm → Sublayer → + x</code></pre><p>也就是说，输入先归一化，再进入 Attention 或 FFN，最后和残差相加。</p><ol><li>核心区别</li></ol><p>两者区别就是 LayerNorm 的位置。</p><pre><code>Post-Norm：Sublayer 后归一化<br/>Pre-Norm：Sublayer 前归一化</code></pre><p>Post-Norm：</p><pre><code>x + Sublayer(x) → LayerNorm</code></pre><p>Pre-Norm：</p><pre><code>x + Sublayer(LN(x))</code></pre><ol><li>Pre-Norm 为什么更稳定？</li></ol><p>Pre-Norm 的残差路径更“干净”。</p><p>因为输出中有一条直接路径：</p><pre><code>y = x + ...</code></pre><p>梯度可以更顺畅地沿着残差分支传回前面层。</p><p>这对深层 Transformer 很重要。</p><p>所以 Pre-Norm 通常：</p><ul><li>更容易训练深层模型</li><li>梯度更稳定</li><li>对学习率更友好</li><li>不容易训练崩</li></ul><ol><li>Post-Norm 的问题</li></ol><p>Post-Norm 中 LayerNorm 在残差相加之后。</p><p>梯度经过很多层时，都要经过 LayerNorm。</p><p>深层情况下可能导致：</p><ul><li>梯度传播不稳定</li><li>训练更难</li><li>需要更小学习率</li><li>更依赖 warmup</li><li>深层模型容易发散</li></ul><p>所以超深大模型一般更偏向 Pre-Norm。</p><ol><li>Post-Norm 的优点</li></ol><p>Post-Norm 也不是完全没优点。</p><p>它在某些浅层或中等深度模型中表现不错。</p><p>因为每层输出都被规范化，表示尺度更规整。</p><p>原始 Transformer 就是 Post-Norm。</p><p>但当模型层数很深时，训练稳定性不如 Pre-Norm。</p><ol><li>大模型通常用哪个？</li></ol><p>现代 LLM 多数更偏 Pre-Norm 或 Pre-Norm 变体。</p><p>比如：</p><pre><code>LN/RMSNorm → Attention → Residual<br/>LN/RMSNorm → FFN → Residual</code></pre><p>还有很多模型用 RMSNorm 替代 LayerNorm。</p><ol><li><strong>面试背诵版：Pre-Norm 和 Post-Norm 区别？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>Pre-Norm 和 Post-Norm 的区别在于 LayerNorm 放置的位置不同。Post-Norm 是原始 Transformer 的结构，形式是 y = LayerNorm(x + Sublayer(x))，也就是先经过 Attention 或 FFN，再和残差相加，最后做 LayerNorm。Pre-Norm 则是 y = x + Sublayer(LayerNorm(x))，先对输入做 LayerNorm，再进入子层，最后和原输入做残差相加。</p><p>Pre-Norm 的优势是训练更稳定，尤其适合深层 Transformer。因为它保留了一条更直接的残差路径，梯度可以更顺畅地从后层传回前层，不容易在深层网络中变得不稳定。Post-Norm 每层输出都经过归一化，表示尺度比较规整，但深层模型中梯度传播更困难，更依赖 warmup 和学习率设置。</p><p>简单说，Post-Norm 是“子层后归一化”，Pre-Norm 是“子层前归一化”。现代大语言模型通常更偏向使用 Pre-Norm 或 RMSNorm 这类变体，因为深层训练更稳定。</p></blockquote>"},{"t":"为什么现在大模型多采用Pre-Norm？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>现在大模型多采用 Pre-Norm，核心原因是：<strong>Pre-Norm 梯度传播更稳定，更适合训练很深的 Transformer。</strong></p><ol><li>Pre-Norm 是什么？</li></ol><p>Pre-Norm 指 LayerNorm 放在子层前面。</p><p>形式是：</p><pre><code>y = x + Sublayer(LayerNorm(x))</code></pre><p>也就是：</p><pre><code>先归一化 → 再进 Attention / FFN → 最后残差相加</code></pre><p>比如：</p><pre><code>x + Attention(LN(x))<br/>x + FFN(LN(x))</code></pre><ol><li>Post-Norm 的问题</li></ol><p>原始 Transformer 用的是 Post-Norm：</p><pre><code>y = LayerNorm(x + Sublayer(x))</code></pre><p>这种结构在浅层模型中可以用。</p><p>但模型变深后容易出现：</p><ul><li>梯度传播不稳定</li><li>训练更难</li><li>对学习率敏感</li><li>更依赖 warmup</li><li>容易 loss spike</li><li>深层模型难收敛</li></ul><p>所以超深 Transformer 训练时，Post-Norm 不够稳定。</p><ol><li>Pre-Norm 的残差路径更直接</li></ol><p>Pre-Norm 的输出是：</p><pre><code>y = x + ...</code></pre><p>残差分支中有一条近似恒等路径。</p><p>反向传播时，梯度可以比较直接地沿着 x 这条路径传回前面层。</p><p>这缓解了深层网络中的梯度消失或不稳定问题。</p><ol><li>更适合深层堆叠</li></ol><p>大模型通常有很多层。</p><p>比如几十层、上百层 Transformer。</p><p>层数越深，梯度传播越难。</p><p>Pre-Norm 可以让每一层都更像在输入基础上做增量修改。</p><p>这样堆很多层时训练更稳。</p><ol><li>对学习率更友好</li></ol><p>Pre-Norm 训练更稳定，所以通常：</p><ul><li>可以使用相对更大的学习率</li><li>对 warmup 依赖相对小一些</li><li>loss 更平滑</li><li>不容易早期发散</li><li>超参数更容易调</li></ul><p>这对大规模训练非常重要。</p><ol><li>为什么不用 Post-Norm？</li></ol><p>不是 Post-Norm 完全不能用。</p><p>Post-Norm 的输出每层都被归一化，表示尺度更规整。</p><p>但在大模型深层训练中，稳定性更关键。</p><p>所以很多现代 LLM 更偏向 Pre-Norm 或 RMSNorm 结构。</p><ol><li>Pre-Norm 也有不足</li></ol><p>面试可以补一句。</p><p>Pre-Norm 虽然稳定，但有人认为它可能让每层输出的规范化约束弱一些。</p><p>有些任务里 Post-Norm 表达效果可能不错。</p><p>所以也有模型使用改进结构，比如：</p><ul><li>RMSNorm</li><li>DeepNorm</li><li>Sandwich Norm</li><li>NormFormer</li><li>Post-Norm 改进版</li></ul><p>但主流 LLM 仍然偏向 Pre-Norm 系。</p><ol><li><strong>面试背诵版：为什么现在大模型多采用 Pre-Norm？</strong></li></ol><blockquote><p>现在大模型多采用 Pre-Norm，主要是因为它比 Post-Norm 更适合深层 Transformer 的稳定训练。Pre-Norm 的形式是 y = x + Sublayer(LayerNorm(x))，也就是先对输入做归一化，再进入 Attention 或 FFN，最后和原输入做残差相加。这样残差路径中保留了一条比较直接的恒等通路，梯度可以更顺畅地从后层传回前层。</p><p>原始 Transformer 使用 Post-Norm，也就是 y = LayerNorm(x + Sublayer(x))。这种结构在浅层模型中可以工作，但模型层数变深后，梯度传播更容易不稳定，对学习率和 warmup 更敏感，训练大模型时更容易出现 loss spike 或发散。Pre-Norm 能缓解这些问题，使深层 Transformer 更容易收敛。</p><p>简单说，<strong>大模型有很多层，训练稳定性非常重要；Pre-Norm 让梯度沿残差路径更直接地传播</strong>，所以现代 LLM 通常采用 Pre-Norm 或 RMSNorm 这类变体。</p></blockquote>"},{"t":"RoPE位置编码原理？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>RoPE 的核心思想是：<strong>不用直接把位置向量加到 token embedding 上，而是通过旋转 Q、K 向量，把位置信息注入 Attention 计算中。</strong></p><ol><li>RoPE 是什么？</li></ol><p>RoPE 全称是 Rotary Position Embedding，旋转位置编码。</p><p>它是一种相对位置编码方法。</p><p>常用于现代大语言模型，比如很多 GPT 类、LLaMA 类模型。</p><p>它主要作用是让模型知道：</p><pre><code>token 在序列中的位置<br/>token 之间的相对距离</code></pre><ol><li>为什么需要位置编码？</li></ol><p>Attention 本身不关心顺序。</p><p>如果没有位置编码，模型看到：</p><pre><code>我 爱 你<br/>你 爱 我</code></pre><p>可能很难区分顺序差异。</p><p>所以 Transformer 必须加入位置信息。</p><ol><li>RoPE 怎么做？</li></ol><p>普通位置编码可能是：</p><pre><code>token embedding + position embedding</code></pre><p>RoPE 不这样做。</p><p>RoPE 是在 Attention 里对 Q、K 做旋转变换。</p><p>也就是：</p><pre><code>Q_position = rotate(Q, position)<br/>K_position = rotate(K, position)</code></pre><p>每个位置对应一个旋转角度。</p><p>位置越靠后，旋转角度不同。</p><ol><li>旋转是什么意思？</li></ol><p>可以把 Q、K 向量按两两维度分组。</p><p>比如：</p><pre><code>(x1, x2), (x3, x4), ...</code></pre><p>每一组二维向量根据当前位置旋转一个角度。</p><p>二维旋转类似：</p><pre><code>[x'1] = [cosθ  -sinθ] [x1]<br/>[x'2]   [sinθ   cosθ] [x2]</code></pre><p>不同位置用不同 θ。</p><p>这样位置信息就被编码进 Q、K 里了。</p><ol><li>为什么能表达相对位置？</li></ol><p>RoPE 的关键性质是：</p><pre><code>旋转后的 Q 和 K 做点积时，<br/>结果会自然包含两个 token 的相对位置差。</code></pre><p>也就是说，Attention score 不只和 token 内容有关，也和位置距离有关。</p><p>比如位置 m 的 Q 和位置 n 的 K 做点积，里面会体现：</p><pre><code>m - n</code></pre><p>所以 RoPE 很适合 Attention。</p><ol><li>RoPE 和绝对位置编码区别</li></ol><p>绝对位置编码：</p><pre><code>直接告诉模型：这是第 1 个位置、第 2 个位置</code></pre><p>RoPE 更强调：</p><pre><code>两个 token 相隔多远</code></pre><p>对语言建模来说，相对距离通常很重要。</p><p>比如：</p><pre><code>当前词更依赖前几个词<br/>主语和谓语相隔多远<br/>括号是否匹配</code></pre><ol><li>RoPE 的优点</li></ol><p>RoPE 的优点：</p><ul><li>能自然建模相对位置</li><li>和 Attention 点积形式匹配</li><li>不需要额外学习位置 embedding</li><li>对长文本外推相对友好</li><li>实现简单</li><li>现代 LLM 使用广泛</li></ul><ol><li>RoPE 的局限</li></ol><p>RoPE 也不是完美的。</p><p>长上下文外推时，位置超过训练长度太多，效果可能下降。</p><p>所以很多长上下文模型会改进 RoPE，比如：</p><ul><li>RoPE scaling</li><li>NTK scaling</li><li>YaRN</li><li>LongRoPE</li></ul><ol><li><strong>面试背诵版：RoPE 位置编码原理？</strong></li></ol><blockquote><p>RoPE，也就是 Rotary Position Embedding，旋转位置编码。它的核心思想不是把位置向量直接加到 token embedding 上，而是在 Attention 计算前，<strong>根据 token 的位置对 Q 和 K 向量做旋转变换，从而把位置信息注入到注意力分数中。</strong></p><p>具体来说，RoPE 会把 Q、K 向量按两两维度分组，每一组二维向量根据当前位置旋转一个角度。不同位置对应不同旋转角度。这样旋转后的 Q 和 K 在做点积时，结果不仅包含 token 内容相似度，还会自然包含两个 token 的相对位置差，也就是 m - n。因此 RoPE 能很好地表达相对位置信息。</p><p>相比传统绝对位置编码，RoPE 更适合 Attention，因为语言建模中很多关系依赖相对距离，比如相邻词、长距离依赖和括号匹配。它实现简单、无需额外学习位置 embedding，并且在现代大语言模型中广泛使用。但 RoPE 对超出训练长度太多的长上下文外推仍可能下降，所以会有 RoPE scaling、NTK scaling、YaRN 等改进方法。</p></blockquote>"},{"t":"RoPE相比绝对位置编码有什么优势？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>RoPE 相比绝对位置编码的核心优势是：<strong>它把位置信息融入 Q、K 的点积中，能更自然地表达 token 之间的相对位置关系。</strong></p><ol><li>绝对位置编码是什么？</li></ol><p>绝对位置编码通常是给每个位置一个位置向量。</p><p>然后和 token embedding 相加：</p><pre><code>x_i = token_embedding_i + position_embedding_i</code></pre><p>它告诉模型：</p><pre><code>这个 token 在第几个位置</code></pre><p>比如：</p><pre><code>第 1 位、第 2 位、第 3 位</code></pre><p>但它直接表达的是绝对位置。</p><ol><li>RoPE 是什么？</li></ol><p>RoPE 不是直接加位置向量。</p><p>它是在 Attention 计算前，对 Q、K 做旋转：</p><pre><code>Q_i = rotate(Q_i, position=i)<br/>K_j = rotate(K_j, position=j)</code></pre><p>旋转后的 Q 和 K 做点积时，会自然包含：</p><pre><code>i - j</code></pre><p>也就是相对位置关系。</p><ol><li>优势一：更自然建模相对位置</li></ol><p>语言中很多关系更依赖相对位置，而不是绝对位置。</p><p>比如：</p><ul><li>当前词和前一个词</li><li>主语和谓语相隔多远</li><li>括号是否匹配</li><li>代词和指代对象距离</li><li>局部上下文窗口</li></ul><p>RoPE 能直接在 Attention score 中体现相对距离，因此更适合语言建模。</p><ol><li>优势二：和 Attention 更匹配</li></ol><p>Attention 的核心是：</p><pre><code>QKᵀ</code></pre><p>RoPE 直接作用在 Q、K 上。</p><p>所以位置信息会影响 attention score。</p><p>也就是说，模型在决定“关注谁”的时候，就已经考虑了位置关系。</p><p>绝对位置编码通常加在输入 embedding 上，位置信息需要模型后续自己学习如何影响 Attention。</p><ol><li>优势三：参数更少</li></ol><p>RoPE 通常是固定函数形式，不需要为每个位置学习一个 position embedding。</p><p>所以它不依赖一个固定长度的位置参数表。</p><p>相比可学习绝对位置编码，参数更少，长度扩展更灵活。</p><ol><li>优势四：长文本外推相对更好</li></ol><p>绝对位置编码如果训练时只见过最大长度 2048，推理时到 4096，新的位置可能没有学过或效果差。</p><p>RoPE 使用连续的旋转函数生成位置编码。</p><p>所以在一定范围内，对超过训练长度的位置更友好。</p><p>不过它也不是无限外推，超长上下文仍可能退化，需要 RoPE scaling 等方法。</p><ol><li>优势五：保留内容向量范数</li></ol><p>旋转变换不会改变向量长度。</p><p>也就是说：</p><pre><code>rotate 后向量范数不变</code></pre><p>它只改变方向，把位置信息编码进角度。</p><p>这有助于保持数值稳定。</p><ol><li><strong>面试背诵版：RoPE 相比绝对位置编码有什么优势？</strong></li></ol><blockquote><p>RoPE 相比绝对位置编码的主要优势是更自然地建模相对位置信息。传统绝对位置编码通常是把 position embedding 加到 token embedding 上，直接告诉模型每个 token 在第几个位置；而 RoPE 是在 Attention 计算前，根据位置对 Q、K 向量做旋转，使得旋转后的 Q 和 K 在点积时能够自然体现两个 token 的相对位置差。</p><p>这和 Attention 机制更匹配，因为 Attention 本身是通过 QKᵀ 决定 token 之间的关注关系，RoPE 直接作用在 Q、K 上，使模型在计算注意力分数时就能感知相对距离。语言任务中很多关系，比如相邻词、长距离依赖、代词指代和括号匹配，都更依赖相对位置而不是绝对位置。</p><p>另外，RoPE 通常不需要学习固定长度的位置 embedding，参数更少，对超过训练长度的外推相对更友好；旋转操作也不会改变向量范数，数值上比较稳定。但 RoPE 也不是无限支持长上下文，超出训练长度太多仍可能退化，需要 RoPE scaling、NTK scaling、YaRN 等改进。简单说，绝对位置编码告诉模型“我在第几位”，RoPE 更强调“我和你相隔多远”。</p></blockquote>"},{"t":"长上下文如何扩展？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>长上下文扩展的核心是：<strong>解决 Transformer 在长序列下计算成本高、位置编码外推难、模型注意力不稳定和有效利用长文本能力不足的问题。</strong></p><ol><li>为什么长上下文难？</li></ol><p>长上下文主要难在几个方面：</p><ul><li>Attention 复杂度是 O(n²)</li><li>显存随序列长度平方增长</li><li>训练成本很高</li><li>位置编码可能无法外推</li><li>模型可能“看得见但用不好”</li><li>长文本中关键信息容易被稀释</li><li>推理 KV Cache 占用很大</li></ul><p>所以不是把窗口调大就完了。</p><ol><li>方法一：位置编码扩展</li></ol><p>很多模型训练时只支持固定长度。</p><p>要扩展上下文，需要处理位置编码。</p><p>常见方法：</p><ul><li>RoPE Scaling</li><li>NTK Scaling</li><li>YaRN</li><li>LongRoPE</li><li>位置插值</li></ul><p>核心思想是让 RoPE 在更长位置上仍然保持合理频率，避免位置超出训练长度后 attention 失效。</p><ol><li>方法二：继续训练长上下文</li></ol><p>只改位置编码不一定够。</p><p>模型还需要在长文本上继续训练。</p><p>比如：</p><ul><li>长文档预训练</li><li>长上下文微调</li><li>长序列指令数据</li><li>多文档 QA</li><li>Needle-in-a-haystack 数据</li><li>长代码仓库数据</li></ul><p>这样模型才学会真正利用远距离信息。</p><p>否则可能只是“窗口变长”，但不一定会用。</p><ol><li>方法三：优化 Attention 计算</li></ol><p>为了降低 O(n²) 成本，可以用：</p><ul><li>FlashAttention</li><li>Sparse Attention</li><li>Sliding Window Attention</li><li>Block Attention</li><li>Ring Attention</li><li>Linear Attention</li><li>分块计算</li></ul><p>其中 FlashAttention 不是改变数学结果，而是优化显存访问和计算效率。</p><p>Sparse / Sliding Window 则是减少关注范围。</p><ol><li>方法四：KV Cache 优化</li></ol><p>推理时长上下文会带来很大的 KV Cache。</p><p>优化方法包括：</p><ul><li>KV Cache 量化</li><li>PagedAttention</li><li>Multi-Query Attention</li><li>Grouped-Query Attention</li><li>KV Cache 压缩</li><li>丢弃不重要历史 token</li><li>分层缓存</li></ul><p>这些主要解决长上下文推理显存和吞吐问题。</p><ol><li>方法五：检索增强</li></ol><p>很多场景没必要把所有内容都塞进上下文。</p><p>可以使用 RAG：</p><pre><code>先检索相关片段 → 再放入上下文</code></pre><p>这样成本更低，也能减少无关信息干扰。</p><p>适合：</p><ul><li>企业知识库</li><li>长文档问答</li><li>代码库问答</li><li>历史对话检索</li></ul><ol><li>方法六：记忆和摘要</li></ol><p>对超长任务，可以用：</p><ul><li>历史摘要</li><li>分层摘要</li><li>滑动窗口记忆</li><li>Episodic Memory</li><li>外部 Memory</li><li>关键信息提取</li></ul><p>思路是：</p><pre><code>不是保留全部原文，而是保留对任务有用的信息。</code></pre><ol><li>方法七：训练模型使用长上下文能力</li></ol><p>长上下文不只是长度问题，也是能力问题。</p><p>模型要学会：</p><ul><li>找到远处关键信息</li><li>不被无关内容干扰</li><li>跨段落推理</li><li>多文档整合</li><li>保持任务目标一致</li><li>处理长代码依赖</li></ul><p>这需要专门数据和评估。</p><ol><li><strong>面试背诵版：长上下文如何扩展？</strong></li></ol><blockquote><p>长上下文扩展不是简单把最大长度调大，而是要同时解决位置编码、Attention 计算成本、KV Cache、训练数据和实际利用能力几个问题。首先，Transformer 的 Self-Attention 复杂度是 O(n²)，序列变长会带来显存和计算成本急剧上升；同时 RoPE 或绝对位置编码在超过训练长度后可能外推效果变差，所以需要 RoPE Scaling、NTK Scaling、YaRN、LongRoPE 或位置插值等方法扩展位置编码。</p><p>其次，模型还需要在长文本数据上继续训练或微调，比如长文档、多文档 QA、长代码、needle-in-a-haystack 任务，否则可能只是“上下文窗口变长”，但模型不会有效使用远距离信息。计算层面可以用 FlashAttention、Sparse Attention、Sliding Window Attention、Block Attention 等方法降低显存和计算压力；推理层面可以用 PagedAttention、KV Cache 量化、MQA、GQA 和 KV Cache 压缩优化长上下文推理成本。</p><p>另外，实际业务中不一定要把所有内容塞进上下文，也可以用 RAG、摘要、外部 Memory 和分层记忆，只把相关信息放进去。简单说，<strong>长上下文扩展要从“位置编码扩展、长上下文训练、Attention 优化、KV Cache 优化、检索和记忆机制”</strong>几个方面一起做。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"模块五：Tokenizer","questions":[{"t":"为什么LLM需要Tokenizer？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>LLM 需要 Tokenizer，核心原因是：<strong>模型不能直接处理原始文本，必须先把文本切成 token，并转换成数字 ID，才能输入神经网络计算。</strong></p><ol><li>模型不能直接读字符串</li></ol><p>神经网络处理的是数字向量，不是原始字符。</p><p>用户输入：</p><pre><code>我喜欢机器学习</code></pre><p>模型不能直接理解这串字符。</p><p>Tokenizer 会先把文本切成 token，再映射成 token id：</p><pre><code>我 / 喜欢 / 机器 / 学习<br/>→ [101, 2354, 8921, 7652]</code></pre><p>然后 token id 再进入 embedding 层，变成向量。</p><ol><li>Token 是模型的基本处理单位</li></ol><p>LLM 不是按“完整句子”直接处理，也不一定按“单词”处理。</p><p>它按 token 处理。</p><p>token 可能是：</p><ul><li>一个英文单词</li><li>一个子词</li><li>一个汉字</li><li>一个标点</li><li>一个代码符号</li><li>一个常见词片段</li></ul><p>比如英文：</p><pre><code>unbelievable</code></pre><p>可能被切成：</p><pre><code>un / believable</code></pre><p>或者更细的子词。</p><ol><li>Tokenizer 控制词表大小</li></ol><p>如果按字符切，序列会很长。</p><p>如果按完整单词切，词表会非常大，而且遇到新词不好处理。</p><p>Tokenizer 通常使用子词切分，比如：</p><ul><li>BPE</li><li>WordPiece</li><li>SentencePiece</li><li>Unigram</li></ul><p>这样可以在词表大小和序列长度之间折中。</p><ol><li>解决未登录词问题</li></ol><p>传统按词切分会遇到 OOV，也就是未登录词。</p><p>比如训练时没见过某个新词，就无法处理。</p><p>子词 Tokenizer 可以把新词拆成已知片段。</p><p>比如：</p><pre><code>ChatGPT-like<br/>→ Chat / GPT / - / like</code></pre><p>这样模型即使没见过完整词，也能处理。</p><ol><li>降低计算成本</li></ol><p>Transformer 的计算复杂度和 token 数有关。</p><p>如果切得太细，token 数变多，Attention 成本会上升。</p><p>如果切得太粗，词表过大，embedding 和输出层成本会上升。</p><p>Tokenizer 需要平衡：</p><pre><code>词表大小<br/>序列长度<br/>表达能力<br/>计算成本</code></pre><ol><li>对多语言和代码很重要</li></ol><p>不同语言和代码的切分很关键。</p><p>比如：</p><ul><li>中文没有天然空格</li><li>英文有单词和子词</li><li>代码有变量名、符号、缩进</li><li>数字和特殊符号需要合理处理</li></ul><p>好的 Tokenizer 能提高模型对多语言、代码和符号的建模能力。</p><ol><li>Tokenizer 影响模型效果</li></ol><p>Tokenizer 不只是预处理工具。</p><p>它会影响：</p><ul><li>上下文长度利用率</li><li>训练效率</li><li>多语言能力</li><li>代码能力</li><li>数字处理能力</li><li>生成质量</li><li>成本</li></ul><p>同样一段中文，如果 tokenizer 切得很碎，就会占更多 token，成本更高。</p><ol><li><strong>面试背诵版：为什么 LLM 需要 Tokenizer？</strong></li></ol><blockquote><p>LLM 需要 Tokenizer，是因为神经网络不能直接处理原始字符串，必须先把文本切分成 token，再映射成数字 ID，最后通过 embedding 层转成向量，才能输入模型计算。Token 是大语言模型处理文本的基本单位，可以是单词、子词、汉字、标点、代码符号或常见片段。</p><p>Tokenizer 的作用不仅是切词，还要在词表大小和序列长度之间做平衡。如果按字符切，序列会很长，Attention 成本高；如果按完整单词切，词表会很大，还容易遇到未登录词。现代 LLM 通常使用 BPE、WordPiece、SentencePiece 等子词方法，把新词拆成已知片段，既能处理未见过的词，又能控制词表规模。</p><p>Tokenizer 会影响模型的上下文利用率、训练效率、多语言能力、代码能力和推理成本。简单说，<strong>Tokenizer 是把人类文本转成模型可处理 token 序列的入口，它决定了模型“怎么看见文本”。</strong></p></blockquote>"},{"t":"Token是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Token 是大语言模型处理文本的基本单位，可以理解成模型眼里的“文字片段”。</p><ol><li>Token 不一定等于一个字或一个词</li></ol><p>Token 可能是：</p><ul><li>一个汉字</li><li>一个英文单词</li><li>一个子词</li><li>一个标点符号</li><li>一个空格</li><li>一个代码符号</li><li>一个常见字符串片段</li></ul><p>比如英文：</p><pre><code>unbelievable</code></pre><p>可能被拆成：</p><pre><code>un / believable</code></pre><p>也可能拆得更细。</p><p>中文里：</p><pre><code>机器学习</code></pre><p>可能是：</p><pre><code>机器 / 学习</code></pre><p>也可能是：</p><pre><code>机 / 器 / 学 / 习</code></pre><p>取决于 tokenizer。</p><ol><li>为什么需要 Token？</li></ol><p>模型不能直接处理原始字符串。</p><p>它只能处理数字。</p><p>所以文本要先经过 tokenizer：</p><pre><code>文本 → token → token id → embedding 向量</code></pre><p>比如：</p><pre><code>我喜欢AI<br/>→ [\"我\", \"喜欢\", \"AI\"]<br/>→ [123, 456, 789]<br/>→ 向量表示</code></pre><ol><li>Token 是计算成本单位</li></ol><p>LLM 的上下文长度和计费通常都按 token 算。</p><p>比如：</p><pre><code>输入 1000 token<br/>输出 500 token</code></pre><p>模型实际处理的是 1500 个 token 左右。</p><p>token 越多：</p><ul><li>计算越多</li><li>延迟越高</li><li>成本越高</li><li>上下文占用越大</li></ul><p>所以 token 是大模型很重要的单位。</p><ol><li>Token 和词表</li></ol><p>Tokenizer 会有一个词表。</p><p>每个 token 对应一个 id。</p><p>比如：</p><pre><code>\"hello\" → 15339<br/>\"world\" → 14957</code></pre><p>模型的 embedding 层会把 token id 映射成向量。</p><p>输出时，模型也是在词表上预测下一个 token。</p><ol><li>大模型是怎么生成文本的？</li></ol><p>LLM 每次生成一个 token。</p><p>流程是：</p><pre><code>已有上下文 → 预测下一个 token 概率 → 选出一个 token → 拼回上下文 → 继续预测</code></pre><p>比如：</p><pre><code>中国的首都是 → 北京</code></pre><p>这里“北京”可能是一个 token，也可能被拆成多个 token。</p><ol><li>Token 数和字符数不一样</li></ol><p>同一段文本，不同语言 token 数可能差异很大。</p><p>英文、中文、代码、数字、符号的切分方式都不同。</p><p>比如：</p><ul><li>常见词可能是一个 token</li><li>生僻词可能拆成多个 token</li><li>长变量名可能拆成多个 token</li><li>空格和换行也可能算 token</li></ul><p>所以不能简单用字数等同 token 数。</p><ol><li><strong>面试背诵版：Token 是什么？</strong></li></ol><blockquote><p>Token 是大语言模型处理文本的基本单位，可以理解为模型眼中的“文字片段”。它不一定等于一个字或一个词，可能是一个汉字、一个英文单词、一个子词、一个标点、一个空格、一个代码符号，具体取决于 tokenizer 的切分规则。</p><p>模型不能直接处理原始字符串，所以文本会先经过 tokenizer 被切成 token，再映射成 token id，最后通过 embedding 层转成向量输入模型。模型输出时也是在词表中预测下一个 token，然后不断生成 token 组成完整文本。</p><p><strong>Token 也是上下文长度、计算成本和计费的重要单位。t</strong>oken 越多，模型计算量、延迟和成本越高。简单说，Token 是连接人类文本和模型数字计算之间的基本单位。</p></blockquote>"},{"t":"BPE算法原理是什么？","tag":"","p":"","html":"<p>BPE 的核心思想是：从字符级开始，反复把语料中最常见的相邻符号合并，最终得到一套常用子词词表。</p><ol><li>BPE 是什么？</li></ol><p>BPE 全称是 Byte Pair Encoding。</p><p>最早是一种压缩算法，后来被用于 NLP 的子词切分。</p><p>它的目标是：</p><pre><code>把文本切成合适的子词 token</code></pre><p>既不要像字符切分那么碎，也不要像按词切分那样词表太大。</p><ol><li>为什么需要 BPE？</li></ol><p>如果按字符切：</p><pre><code>unbelievable → u n b e l i e v a b l e</code></pre><p>序列太长，效率低。</p><p>如果按完整单词切：</p><pre><code>unbelievable</code></pre><p>词表会很大，而且遇到新词可能无法处理。</p><p>BPE 折中：</p><pre><code>un / believable</code></pre><p>或者：</p><pre><code>un / believe / able</code></pre><p>这样既能压缩长度，又能处理新词。</p><ol><li>BPE 训练过程</li></ol><p>BPE 一开始把文本拆成最小单位。</p><p>可能是字符，也可能是 byte。</p><p>比如：</p><pre><code>low<br/>lower<br/>lowest</code></pre><p>初始可以看成：</p><pre><code>l o w<br/>l o w e r<br/>l o w e s t</code></pre><p>然后统计所有相邻符号对出现频率。</p><p>比如：</p><pre><code>l o<br/>o w<br/>w e<br/>e r</code></pre><p>找到出现最多的一对，合并成新 token。</p><p>比如：</p><pre><code>l + o → lo<br/>lo + w → low</code></pre><p>不断重复这个过程，直到达到设定词表大小。</p><ol><li>BPE 切词过程</li></ol><p>训练好词表后，遇到新文本时，会按照学到的 merge 规则切分。</p><p>比如：</p><pre><code>lowest</code></pre><p>可能切成：</p><pre><code>low / est</code></pre><p>如果遇到没见过的新词，也可以拆成更小的已知子词或字符。</p><p>所以 BPE 能缓解 OOV 问题。</p><ol><li>BPE 的优点</li></ol><p>BPE 的优点：</p><ul><li>词表大小可控</li><li>能处理未登录词</li><li>比字符级序列更短</li><li>比词级方法泛化更好</li><li>适合多语言</li><li>适合大模型训练</li></ul><p>它能把高频词保留成较大 token，把低频词拆成子词。</p><ol><li>BPE 的缺点</li></ol><p>BPE 也有问题：</p><ul><li>切分不一定符合语义</li><li>对数字、代码、特殊符号可能不理想</li><li>不同语言效果不同</li><li>低频词可能被切得很碎</li><li>token 边界不一定符合人类直觉</li></ul><p>所以后来也有 WordPiece、SentencePiece、Unigram 等方法。</p><ol><li>举个直观例子</li></ol><p>语料里经常出现：</p><pre><code>learning<br/>learned<br/>learner</code></pre><p>BPE 可能学到：</p><pre><code>learn<br/>ing<br/>ed<br/>er</code></pre><p>这样遇到：</p><pre><code>learnable</code></pre><p>即使没见过完整词，也可以切成：</p><pre><code>learn / able</code></pre><ol><li><strong>面试背诵版：BPE 算法原理是什么？</strong></li></ol><blockquote><p>BPE，也就是 Byte Pair Encoding，是一种常见的子词切分算法。它的核心思想是从字符或 byte 级别开始，统计语料中最频繁出现的相邻符号对，然后把这对符号合并成一个新的 token，不断重复这个过程，直到达到预设词表大小。</p><p>这样训练出来的词表会把高频词或高频片段合并成较大的 token，把低频词拆成更小的子词或字符。相比字符级切分，BPE 可以减少序列长度；相比按完整单词切分，它可以控制词表大小，并且遇到未登录词时也能拆成已有子词，从而缓解 OOV 问题。</p><p>简单说，BPE 是“<strong>从小单位开始，反复合并最高频的相邻片段</strong>”。它在词表大小、序列长度和泛化能力之间做了折中，因此被很多大语言模型的 tokenizer 使用。</p></blockquote>"},{"t":"WordPiece和BPE有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>WordPiece 和 BPE 的核心区别是：BPE 按“出现频率最高”合并子词，WordPiece 更强调“合并后能最大化语言模型似然”。</p><ol><li>它们共同点是什么？</li></ol><p>WordPiece 和 BPE 都是子词切分算法。</p><p>目的都是解决：</p><ul><li>词表太大</li><li>未登录词 OOV</li><li>字符级序列太长</li><li>低频词难处理</li><li>多语言切分困难</li></ul><p>它们都会把词拆成子词。</p><p>比如：</p><pre><code>unbelievable<br/>→ un / believe / able</code></pre><p>这样模型即使没见过完整词，也能处理。</p><ol><li>BPE 怎么合并？</li></ol><p>BPE 从字符或 byte 开始。</p><p>每一步统计语料中最常见的相邻符号对。</p><p>然后合并频率最高的一对。</p><p>比如：</p><pre><code>l + o 出现最多 → 合并成 lo<br/>lo + w 出现最多 → 合并成 low</code></pre><p>核心是：</p><pre><code>按频率合并</code></pre><p>简单直接。</p><ol><li>WordPiece 怎么合并？</li></ol><p>WordPiece 也从小单位开始合并。</p><p>但它不是单纯看频率最高。</p><p>它更关注：</p><pre><code>合并这个子词后，能不能让训练语料的语言模型似然更高</code></pre><p>也就是说，它选择对语言建模更有帮助的子词。</p><p>可以理解为：</p><pre><code>BPE 看频率<br/>WordPiece 看合并收益</code></pre><ol><li>切词策略不同</li></ol><p>BPE 通常根据 merge 规则做切分。</p><p>训练时学到一系列合并规则，推理时按规则应用。</p><p>WordPiece 常用最长匹配策略。</p><p>也就是从左到右，尽量匹配词表里最长的子词。</p><p>比如：</p><pre><code>unaffable</code></pre><p>可能切成：</p><pre><code>un / ##aff / ##able</code></pre><p><code>##</code> 表示这个子词不是词的开头，而是接在前面后面的片段。</p><ol><li>代表模型不同</li></ol><p>BPE 常见于：</p><ul><li>GPT 系列</li><li>RoBERTa</li><li>一些 Byte-level tokenizer</li></ul><p>WordPiece 典型用于：</p><ul><li>BERT</li><li>DistilBERT</li><li>ALBERT</li></ul><p>SentencePiece 常见于：</p><ul><li>T5</li><li>LLaMA 类模型</li><li>多语言模型</li></ul><ol><li>OOV 处理</li></ol><p>两者都能缓解 OOV。</p><p>BPE 可以把未知词拆成子词或 byte。</p><p>WordPiece 如果某个词不能被词表子词完全覆盖，可能会变成 <code>[UNK]</code>。</p><p>不过实际 BERT 的词表通常覆盖较多常见子词。</p><ol><li>简单对比</li></ol><p>可以这样记：</p><pre><code>BPE：合并最高频相邻片段<br/>WordPiece：选择更提升语言模型概率的片段</code></pre><p>BPE 更像压缩。</p><p>WordPiece 更像为语言模型优化子词词表。</p><ol><li><strong>面试背诵版：WordPiece 和 BPE 有什么区别？</strong></li></ol><blockquote><p>WordPiece 和 BPE 都是子词切分算法，目的都是在词表大小和序列长度之间做平衡，同时缓解未登录词问题。它们都会把低频词拆成子词，把高频词或高频片段保留成较大的 token。</p><p>区别在于合并标准不同。BPE 从字符或 byte 级别开始，每一步统计语料中出现频率最高的相邻符号对，并把它们合并成新的 token，本质上更像一种基于频率的压缩方法。WordPiece 也会构建子词词表，但它更关注合并某个子词后是否能提升语料的语言模型似然，也就是选择对语言建模更有帮助的子词。</p><p>在切分时，BPE 通常按照训练得到的 merge 规则切分；WordPiece 常用最长匹配，并用 <code>##</code> 标识非词首子词。典型上，BPE 常见于 GPT、RoBERTa，WordPiece 常见于 BERT。简单说，BPE 更强调“高频合并”，WordPiece 更强调“提升语言模型概率”。</p></blockquote>"},{"t":"SentencePiece是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>SentencePiece 是一种通用的文本 Tokenizer 工具，核心特点是：不依赖空格分词，可以直接从原始文本训练子词模型，特别适合多语言场景。</p><ol><li>SentencePiece 解决什么问题？</li></ol><p>很多传统 tokenizer 会先按空格切词。</p><p>这对英文比较自然：</p><pre><code>I love machine learning</code></pre><p>可以先按空格切成单词。</p><p>但对中文、日文这类语言：</p><pre><code>我喜欢机器学习</code></pre><p>没有天然空格。</p><p>如果强行依赖空格，就不适合多语言。</p><p>SentencePiece 的特点是：</p><pre><code>直接把整段原始文本当输入，不需要预先分词</code></pre><ol><li>SentencePiece 是什么类型的方法？</li></ol><p>SentencePiece 不是单一算法，而是一个 tokenizer 框架。</p><p>它常支持两类子词模型：</p><ul><li>BPE</li><li>Unigram Language Model</li></ul><p>也就是说，SentencePiece 可以用 BPE，也可以用 Unigram。</p><p>它的重点是：</p><pre><code>把 tokenization 做成语言无关的端到端流程</code></pre><ol><li>它如何处理空格？</li></ol><p>SentencePiece 会把空格也当作普通符号处理。</p><p>常见做法是用特殊符号表示空格，比如：</p><pre><code>▁</code></pre><p>比如：</p><pre><code>I love NLP</code></pre><p>可能变成：</p><pre><code>▁I / ▁love / ▁NLP</code></pre><p>这个 <code>▁</code> 表示词前空格。</p><p>这样解码时可以无损还原原文。</p><ol><li>为什么适合多语言？</li></ol><p>因为它不依赖语言特定规则。</p><p>比如：</p><ul><li>英文有空格</li><li>中文没有空格</li><li>日文混合假名汉字</li><li>代码有符号和缩进</li><li>多语言混合文本</li></ul><p>SentencePiece 都可以统一处理。</p><p>这对大模型很重要，因为训练语料通常是多语言、多格式混合的。</p><ol><li>Unigram 模型是什么？</li></ol><p>SentencePiece 常用的 Unigram 方法，会先准备一个较大的候选子词集合。</p><p>然后通过概率模型选择最合适的子词组合，并不断删掉贡献较小的子词，直到得到目标词表大小。</p><p>和 BPE 的“逐步合并”不同，Unigram 更像：</p><pre><code>从大词表里逐步裁剪</code></pre><ol><li>SentencePiece 的优点</li></ol><p>主要优点：</p><ul><li>不依赖预分词</li><li>语言无关</li><li>适合中文、日文、多语言</li><li>可以无损还原文本</li><li>支持 BPE 和 Unigram</li><li>词表大小可控</li><li>工程实现成熟</li><li>很多大模型使用</li></ul><ol><li>哪些模型用过？</li></ol><p>很多模型使用 SentencePiece 或类似方法。</p><p>比如：</p><ul><li>T5</li><li>ALBERT</li><li>XLNet</li><li>LLaMA 类模型</li><li>一些多语言模型</li></ul><p>它在多语言和大模型 tokenizer 里很常见。</p><ol><li><strong>面试背诵版：SentencePiece 是什么？</strong></li></ol><blockquote><p>SentencePiece 是一种通用的子词 Tokenizer 工具，它的核心特点是不依赖空格或语言特定分词规则，可以直接从原始文本训练 tokenizer，因此特别适合中文、日文、多语言和混合文本场景。传统英文 tokenizer 往往先按空格分词，但很多语言没有天然空格，SentencePiece 把文本看成普通字符序列来处理，实现了语言无关的 tokenization。</p><p>SentencePiece 本身不是单一切分算法，而是一个框架，常支持 BPE 和 Unigram Language Model。它会把空格也作为普通符号处理，常用 <code>▁</code> 表示词前空格，因此可以做到文本的无损还原。比如英文中的空格、中文中的连续字符、代码中的符号，都能用统一方式处理。</p><p>简单说，SentencePiece 的价值是<strong>“不依赖预分词，直接从原始文本学习子词词表”</strong>，在多语言大模型中非常常见。</p></blockquote>"},{"t":"为什么中文Token数量通常更多？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>中文 Token 数量通常更多，核心原因是：很多 tokenizer 对中文压缩效率不如英文，中文词语常被拆成单字或较短片段。</p><ol><li>英文有天然空格</li></ol><p>英文句子天然用空格分隔单词。</p><p>比如：</p><pre><code>I love machine learning</code></pre><p>Tokenizer 很容易把高频单词或子词作为 token：</p><pre><code>I / love / machine / learning</code></pre><p>很多英文常见词可以直接是一个 token。</p><ol><li>中文没有天然空格</li></ol><p>中文句子没有空格分词。</p><p>比如：</p><pre><code>我喜欢机器学习</code></pre><p>Tokenizer 不一定知道应该切成：</p><pre><code>我 / 喜欢 / 机器学习</code></pre><p>还是：</p><pre><code>我 / 喜 / 欢 / 机 / 器 / 学 / 习</code></pre><p>如果词表对中文覆盖不够，就容易拆得更碎。</p><ol><li>中文词表覆盖可能不足</li></ol><p>很多 tokenizer 的训练语料中英文占比更高。</p><p>如果中文语料比例不足，词表里中文常用词、短语覆盖不够。</p><p>结果就是：</p><pre><code>机器学习</code></pre><p>可能被切成：</p><pre><code>机 / 器 / 学 / 习</code></pre><p>而不是：</p><pre><code>机器学习</code></pre><p>token 数自然更多。</p><ol><li>子词算法对中文不一定友好</li></ol><p>BPE、WordPiece 这类算法会基于频率合并子词。</p><p>英文里很多高频词形变化明显，比如：</p><pre><code>learn / learning / learned</code></pre><p>子词合并比较自然。</p><p>中文词边界不明显，如果没有好的语料和训练策略，合并结果可能更碎。</p><ol><li>中文信息密度高，但 token 不一定少</li></ol><p>中文一个字承载的信息通常较多。</p><p>比如：</p><pre><code>数据库</code></pre><p>只有 3 个字，但可能被切成 3 个 token。</p><p>英文：</p><pre><code>database</code></pre><p>可能是 1 个 token。</p><p>所以按字符数看中文很短，但按 token 数不一定少。</p><ol><li>标点、数字、英文混杂也会增加 token</li></ol><p>中文文本里经常混合：</p><ul><li>中文</li><li>英文缩写</li><li>数字</li><li>标点</li><li>URL</li><li>代码</li><li>专有名词</li></ul><p>这些内容如果 tokenizer 没有很好覆盖，也会被拆成更多 token。</p><ol><li>不同 tokenizer 差异很大</li></ol><p>中文 token 数不是绝对更多，取决于 tokenizer。</p><p>如果 tokenizer 专门针对中文训练，中文词表覆盖好，token 数会少很多。</p><p>但很多通用大模型 tokenizer 对英文更友好，所以中文相对更“费 token”。</p><ol><li><strong>面试背诵版：为什么中文 Token 数量通常更多？</strong></li></ol><span>[原文档此处有配图]</span><blockquote><p>中文 Token 数量通常更多，主要是因为中文没有像英文那样天然的空格分词，词边界不明显。英文中很多高频单词可以直接作为一个 token，比如 database 可能就是一个 token；而中文词语如果词表覆盖不足，可能会被拆成单个汉字或更短子词，比如“机器学习”可能被切成“机 / 器 / 学 / 习”。</p><p>另外，很多 tokenizer 的训练语料和词表对英文更友好，中文高频词、专有名词和短语没有充分合并，就会导致中文压缩率较低。虽然中文字符信息密度高，但在 tokenizer 里不一定能合成较长 token，所以按 token 数看可能比英文更贵。</p><p>同时，中文文本中常混合数字、英文缩写、标点、URL 和代码，这些也可能增加 token 数。不过这不是绝对的，具体取决于 tokenizer 的训练语料和词表设计。<strong>简单说，中文 token 更多，是因为“中文没有空格、词边界难、词表覆盖不足、子词合并不够”。</strong></p></blockquote>"},{"t":"Token数量如何影响推理成本？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Token 数量影响推理成本，核心是：<strong>模型每处理一个 token 都要计算，输入和输出 token 越多，计算量、显存、延迟和费用都会增加。</strong></p><ol><li>输入 token 会增加计算量</li></ol><p>模型处理输入时，需要对上下文中的 token 做前向计算。</p><p>输入越长：</p><pre><code>prompt token 越多</code></pre><p>需要处理的 token 越多，计算量越大。</p><p>尤其 Transformer 的 Attention 和序列长度强相关。</p><p>长上下文会显著增加计算和显存压力。</p><ol><li>输出 token 也会增加成本</li></ol><p>大语言模型生成时通常是自回归的。</p><p>也就是：</p><pre><code>一次生成一个 token</code></pre><p>每生成一个 token，都要做一次模型前向计算。</p><p>所以输出越长：</p><ul><li>推理时间越长</li><li>计算成本越高</li><li>用户等待越久</li><li>费用越高</li></ul><ol><li>Attention 成本和上下文长度有关</li></ol><p>Self-Attention 需要建模 token 之间关系。</p><p>训练时复杂度通常是：</p><pre><code>O(n²)</code></pre><p>推理时有 KV Cache，可以避免重复计算历史 token 的 K、V。</p><p>但每生成一个新 token，仍然要关注前面所有 token。</p><p>所以上下文越长，每一步生成也越贵。</p><ol><li>KV Cache 占用显存</li></ol><p>推理时模型会缓存历史 token 的 Key 和 Value。</p><p>这就是 KV Cache。</p><p>上下文越长，缓存越大。</p><p>它会占用：</p><ul><li>GPU 显存</li><li>内存带宽</li><li>推理资源</li></ul><p>长上下文、多并发时，KV Cache 经常成为瓶颈。</p><ol><li>输入和输出成本都要算</li></ol><p>一次请求的成本通常和：</p><pre><code>输入 token 数 + 输出 token 数</code></pre><p>相关。</p><p>但输出 token 往往更贵，因为生成是逐 token 进行的。</p><p>比如：</p><pre><code>输入 1000 token，输出 200 token</code></pre><p>模型不只是处理 1200 token，还要经历 200 次自回归生成步骤。</p><ol><li>Token 多还影响延迟</li></ol><p>token 多会导致：</p><ul><li>首 token 延迟增加</li><li>总生成时间变长</li><li>吞吐下降</li><li>并发能力下降</li><li>GPU 显存压力增加</li></ul><p>所以生产系统会限制：</p><ul><li>最大输入长度</li><li>最大输出长度</li><li>最大上下文窗口</li><li>单用户 token 预算</li></ul><ol><li>如何降低 token 成本？</li></ol><p>常见方法：</p><ul><li>精简 prompt</li><li>删除无关上下文</li><li>使用摘要</li><li>RAG 只放相关片段</li><li>限制输出长度</li><li>避免重复历史对话</li><li>缓存常见回答</li><li>使用更便宜的小模型</li><li>对长文先分块处理</li></ul><p>核心是：</p><pre><code>只把真正需要的信息放进上下文。</code></pre><ol><li><strong>面试背诵版：Token 数量如何影响推理成本？</strong></li></ol><blockquote><p>Token 数量会直接影响大模型推理的计算量、显存、延迟和费用。输入 token 越多，模型需要处理的上下文越长，Attention 计算和中间状态开销越大；输出 token 越多，模型需要自回归地一次生成一个 token，每生成一个 token 都要进行一次前向计算，因此生成越长，总耗时和成本越高。</p><p>推理时虽然可以使用 KV Cache 缓存历史 token 的 Key 和 Value，避免重复计算全部历史，但上下文越长，KV Cache 占用的显存越大；每生成一个新 token 仍然需要关注前面的上下文，所以长上下文和长输出都会降低吞吐、增加延迟，并限制并发。</p><p>简单说，<strong>输入 token 决定模型要“读多少”，输出 token 决定模型要“写多久”。</strong>降低成本的方法包括精简 prompt、裁剪无关上下文、使用摘要、RAG 只放相关片段、限制输出长度、缓存结果和使用更合适的小模型。</p></blockquote>"},{"t":"Tokenizer设计会影响模型效果吗？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>会，Tokenizer 设计会明显影响模型效果、训练效率、上下文利用率、多语言能力和推理成本。</strong></p><ol><li>影响序列长度</li></ol><p>Tokenizer 切得越碎，token 数越多。</p><p>比如中文：</p><pre><code>机器学习</code></pre><p>如果切成：</p><pre><code>机 / 器 / 学 / 习</code></pre><p>就是 4 个 token。</p><p>如果切成：</p><pre><code>机器学习</code></pre><p>就是 1 个 token。</p><p>token 数越多：</p><ul><li>上下文占用越多</li><li>Attention 成本越高</li><li>推理延迟越高</li><li>可放入的有效信息越少</li></ul><p>所以 tokenizer 会影响上下文利用率和成本。</p><ol><li>影响未登录词处理</li></ol><p>好的 tokenizer 能把没见过的新词拆成合理子词。</p><p>比如：</p><pre><code>unbelievable<br/>→ un / believe / able</code></pre><p>这样模型能根据子词理解大概含义。</p><p>如果切分太差，模型可能难以理解新词、专有名词、代码变量名和多语言内容。</p><ol><li>影响多语言能力</li></ol><p>如果 tokenizer 主要基于英文训练，对中文、日文、阿拉伯文等语言可能切得很碎。</p><p>结果是：</p><ul><li>非英文 token 数更多</li><li>训练效率更低</li><li>表示质量变差</li><li>多语言能力受影响</li><li>使用成本更高</li></ul><p>所以多语言模型通常需要更均衡的 tokenizer 词表。</p><ol><li>影响代码能力</li></ol><p>代码里有很多特殊模式：</p><ul><li>变量名</li><li>函数名</li><li>缩进</li><li>括号</li><li>点号</li><li>下划线</li><li>驼峰命名</li><li>运算符</li></ul><p>如果 tokenizer 对代码不友好，可能把代码切得很碎，影响模型理解代码结构和生成代码。</p><ol><li>影响数字和符号处理</li></ol><p>数字切分也很重要。</p><p>比如：</p><pre><code>20260810</code></pre><p>如果切分不合理，模型可能更难学习数值关系、日期格式和编号规律。</p><p>特殊符号、URL、公式、表格、JSON 也类似。</p><p>切分不好会影响结构化数据处理能力。</p><ol><li>影响词表大小和模型参数</li></ol><p>Tokenizer 词表越大：</p><ul><li>embedding 层更大</li><li>输出 softmax 层更大</li><li>参数更多</li><li>计算可能更贵</li></ul><p>词表太小：</p><ul><li>token 切得太碎</li><li>序列变长</li><li>attention 成本增加</li></ul><p>所以 tokenizer 要在词表大小和序列长度之间平衡。</p><ol><li>影响生成质量</li></ol><p>Tokenizer 还会影响生成文本的自然度。</p><p>如果切分不合理，可能出现：</p><ul><li>生僻词生成困难</li><li>中文断裂</li><li>代码符号错误</li><li>多语言混杂异常</li><li>标点和空格处理不好</li><li>复制文本不稳定</li></ul><p>尤其在代码、数学、表格和多语言场景里更明显。</p><ol><li><strong>面试背诵版：Tokenizer 设计会影响模型效果吗？</strong></li></ol><blockquote><p>Tokenizer 设计会影响模型效果，因为它决定了文本如何被切成 token，也就决定了模型看到文本的基本单位。切分太碎会导致 token 数增加，占用更多上下文窗口，提高 Attention 计算成本和推理延迟；切分太粗则会导致词表过大，增加 embedding 和输出层参数，也可能影响泛化能力。</p><p>Tokenizer 还会影响未登录词、多语言、代码、数字和符号的处理能力。如果词表主要偏英文，中文、日文等语言可能被切得更碎，导致训练效率和使用成本变差；如果对代码变量名、符号、缩进、JSON、URL、数字切分不好，也会影响模型理解和生成结构化内容的能力。</p><p>简单说，<strong>Tokenizer 不是简单预处理，而是模型输入表示的一部分。</strong>好的 Tokenizer 要在词表大小、序列长度、多语言覆盖、代码和符号处理、训练效率与推理成本之间做平衡。</p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"6.1 训练目标","questions":[{"t":"GPT预训练任务是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p><strong>GPT 的预训练任务核心是：自回归语言建模，也就是根据前面的 token 预测下一个 token。</strong></p><ol><li>GPT 预训练在做什么？</li></ol><p>GPT 训练时会给模型一段文本。</p><p>比如：</p><pre><code>中国的首都是北京</code></pre><p>模型会学习：</p><pre><code>给定“中国的”，预测“首”<br/>给定“中国的首”，预测“都”<br/>给定“中国的首都”，预测“是”<br/>给定“中国的首都是”，预测“北京”</code></pre><p>也就是不断做 next token prediction。</p><ol><li>为什么叫自回归？</li></ol><p>自回归的意思是：</p><pre><code>当前输出依赖之前已经出现的 token</code></pre><p>公式可以写成：</p><pre><code>P(x1, x2, ..., xn) = Π P(x_t | x_1, ..., x_{t-1})</code></pre><p>也就是说，一段文本的概率被分解成每个 token 在前文条件下出现的概率。</p><ol><li>训练目标是什么？</li></ol><p>训练目标是让模型提高真实下一个 token 的概率。</p><p>模型会对整个词表输出概率分布。</p><p>比如：</p><pre><code>输入：中国的首都是</code></pre><p>模型输出：</p><pre><code>北京：0.7<br/>上海：0.1<br/>广州：0.05<br/>...</code></pre><p>如果真实 token 是“北京”，那就希望“北京”的概率越高越好。</p><ol><li>使用什么 Loss？</li></ol><p>GPT 预训练通常使用交叉熵损失。</p><p>因为 next token prediction 本质是词表上的多分类问题。</p><p>真实 token 可以看成 one-hot 标签。</p><p>模型预测的是整个词表上的概率分布。</p><p>所以用 Cross Entropy 很自然。</p><ol><li>为什么只能看前文？</li></ol><p>GPT 是 Decoder-only 架构，使用 causal mask。</p><p>训练时当前位置只能看到前面的 token，不能看到后面的 token。</p><p>这样保证训练和推理一致。</p><p>因为推理生成时，未来 token 本来就还没有生成出来。</p><ol><li>GPT 预训练学到了什么？</li></ol><p>通过海量文本的 next token prediction，模型会学到：</p><ul><li>词法</li><li>语法</li><li>语义</li><li>世界知识</li><li>文本风格</li><li>代码结构</li><li>常见任务模式</li><li>推理模式</li><li>多轮对话模式</li></ul><p>虽然训练目标只是预测下一个 token，但规模足够大后会形成很多泛化能力。</p><ol><li>和 BERT 预训练有什么区别？</li></ol><p>GPT 是：</p><pre><code>自回归语言建模，单向预测下一个 token</code></pre><p>BERT 是：</p><pre><code>Masked Language Model，双向上下文预测被遮住的 token</code></pre><p>所以：</p><pre><code>GPT 更擅长生成<br/>BERT 更擅长理解</code></pre><ol><li><strong>面试背诵版：GPT 预训练任务是什么？</strong></li></ol><blockquote><p>GPT 的预训练任务是自回归语言建模，也就是 next token prediction。给定一段文本，模型只能看到当前位置之前的 token，然后预测下一个 token。比如输入“中国的首都是”，目标是预测“北京”。整个文本的概率可以分解成多个条件概率的乘积，即 P(x_t | x_1, ..., x_{t-1})。</p><p>训练时，GPT 会在词表上输出下一个 token 的概率分布，真实 token 可以看作 one-hot 标签，因此通常使用交叉熵损失来最大化真实 token 的概率。由于 GPT 是 Decoder-only 架构，会使用 causal mask，保证每个位置只能看到前文，不能看到未来 token，这样训练过程和推理生成过程保持一致。</p><p>简单说<strong>，GPT 预训练就是“看前文，猜下一个 token”。</strong>虽然目标很简单，但在海量文本和大规模模型下，它能学习到语法、语义、知识、代码结构、任务模式和生成能力。</p></blockquote>"},{"t":"为什么使用Next Token Prediction？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>使用 Next Token Prediction 的核心原因是：<strong>它简单、通用、数据易得，并且和文本生成任务天然一致。</strong></p><ol><li>训练数据容易获得</li></ol><p>Next Token Prediction 不需要人工标注。</p><p>任意一段文本都可以变成训练样本。</p><p>比如：</p><pre><code>我喜欢机器学习</code></pre><p>可以自动构造：</p><pre><code>输入：我<br/>目标：喜欢<br/><br/>输入：我喜欢<br/>目标：机器<br/><br/>输入：我喜欢机器<br/>目标：学习</code></pre><p>所以它可以利用海量互联网文本、代码、书籍和文档。</p><ol><li>目标简单统一</li></ol><p>Next Token Prediction 只有一个目标：</p><pre><code>根据前文预测下一个 token</code></pre><p>不管是新闻、代码、对话、论文、网页，训练形式都一样。</p><p>这让模型可以在超大规模语料上统一训练。</p><ol><li>和生成任务一致</li></ol><p>GPT 这类模型推理时就是逐 token 生成。</p><p>流程是：</p><pre><code>给定 prompt → 预测下一个 token → 拼回上下文 → 继续预测</code></pre><p>这和训练时的 next token prediction 完全一致。</p><p>所以训练目标和使用方式天然匹配。</p><ol><li>可以学习语言规律</li></ol><p>为了预测下一个 token，模型必须学习：</p><ul><li>词法</li><li>语法</li><li>语义</li><li>上下文关系</li><li>指代关系</li><li>逻辑结构</li><li>文本风格</li></ul><p>比如要预测句子后半部分，模型需要理解前文。</p><p>所以简单预测任务会逼迫模型学习语言结构。</p><ol><li>可以学习知识和模式</li></ol><p>海量文本里包含大量知识和任务模式。</p><p>模型为了预测文本，会学习：</p><ul><li>世界知识</li><li>常识</li><li>专业知识</li><li>代码模式</li><li>问答格式</li><li>推理样例</li><li>指令响应模式</li></ul><p>所以虽然训练目标只是预测 token，但模型会获得很多泛化能力。</p><ol><li>支持自监督学习</li></ol><p>Next Token Prediction 是自监督任务。</p><p>不需要人工标签，因为文本本身就提供了标签。</p><pre><code>前面的 token 是输入<br/>后面的 token 是标签</code></pre><p>这让大模型可以扩展到海量数据。</p><ol><li>为什么不用别的任务？</li></ol><p>当然也可以用 MLM、去噪自编码等任务。</p><p>但 Next Token Prediction 对生成式模型特别适合。</p><p>因为它：</p><ul><li>简单</li><li>稳定</li><li>可扩展</li><li>不需要标注</li><li>和自回归生成一致</li><li>适合 Decoder-only 架构</li></ul><ol><li><strong>面试背诵版：为什么使用 Next Token Prediction？</strong></li></ol><blockquote><p>使用 Next Token Prediction，主要是因为它简单、通用、可扩展，并且和生成式模型的推理方式一致。这个任务不需要人工标注，任意文本都可以自动构造训练样本：前面的 token 作为输入，下一个 token 作为标签，因此可以利用海量互联网文本、代码、书籍和文档进行自监督训练。</p><p>它的训练目标非常统一，不管是新闻、论文、代码还是对话，形式都是根据前文预测下一个 token。为了做好这个任务，模型必须学习词法、语法、语义、上下文关系、世界知识、代码结构和常见任务模式，所以简单的预测任务可以产生很强的泛化能力。</p><p>另外，GPT 推理时本来就是自回归生成，也就是给定 prompt 后预测下一个 token，再把生成结果拼回上下文继续预测。因此 Next Token Prediction 和模型实际使用方式天然一致。简单说，它的优势是“<strong>数据便宜、目标统一、训练可扩展、和生成过程一致</strong>”。</p></blockquote>"},{"t":"Causal Language Modeling是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Causal Language Modeling，<strong>因果语言建模，核心是：模型只能根据当前位置之前的 token 来预测下一个 token，不能看到未来信息。</strong></p><ol><li>它在做什么？</li></ol><p>Causal Language Modeling 也叫自回归语言建模。</p><p>任务是：</p><pre><code>给定前文，预测下一个 token</code></pre><p>比如句子：</p><pre><code>我 喜欢 吃 苹果</code></pre><p>训练时模型学习：</p><pre><code>我 → 喜欢<br/>我 喜欢 → 吃<br/>我 喜欢 吃 → 苹果</code></pre><p>也就是从左到右建模文本。</p><ol><li>为什么叫 Causal？</li></ol><p>Causal 的意思是“因果的、按时间方向的”。</p><p>也就是说，当前 token 的预测只能依赖过去的 token。</p><p>不能依赖未来 token。</p><p>比如预测“吃”时，只能看到：</p><pre><code>我 喜欢</code></pre><p>不能看到：</p><pre><code>苹果</code></pre><p>否则训练时就偷看答案了。</p><ol><li>如何防止看到未来？</li></ol><span>[原文档此处有配图]</span><p>在 Transformer Decoder 中，会使用 causal mask。</p><p>它会把未来位置遮住。</p><p>比如第 3 个位置只能关注：</p><pre><code>第 1、2、3 个位置</code></pre><p>不能关注第 4、5 个位置。</p><p>未来位置 attention score 会被设成负无穷，softmax 后权重为 0。</p><ol><li>和 GPT 的关系</li></ol><p>GPT 类模型就是典型的 Causal Language Model。</p><p>它使用 Decoder-only Transformer。</p><p>训练目标是：</p><pre><code>Next Token Prediction</code></pre><p>推理时也是：</p><pre><code>输入 prompt → 生成下一个 token → 再生成下一个 token</code></pre><p>训练和推理过程一致。</p><ol><li>和 Masked Language Modeling 区别</li></ol><p>Causal Language Modeling：</p><pre><code>只能看左边上下文，预测下一个 token</code></pre><p>代表模型：</p><pre><code>GPT</code></pre><p>Masked Language Modeling：</p><pre><code>可以看左右上下文，预测被 mask 的 token</code></pre><p>代表模型：</p><pre><code>BERT</code></pre><p>所以：</p><pre><code>CLM 适合生成<br/>MLM 适合理解</code></pre><ol><li>概率分解方式</li></ol><p>CLM 会把一段文本概率分解成：</p><pre><code>P(x1, x2, ..., xn)<br/>= P(x1) × P(x2|x1) × P(x3|x1,x2) × ...</code></pre><p>每一步只依赖前面的 token。</p><p>这就是自回归生成的基础。</p><ol><li>优点和缺点</li></ol><p>优点：</p><ul><li>训练目标简单</li><li>不需要人工标注</li><li>和生成过程一致</li><li>适合文本生成</li><li>可扩展到海量数据</li></ul><p>缺点：</p><ul><li>只能单向建模</li><li>对纯理解任务不如双向模型直接</li><li>生成时必须逐 token 生成，推理较慢</li></ul><ol><li><strong>面试背诵版：Causal Language Modeling 是什么？</strong></li></ol><blockquote><p>Causal Language Modeling，也叫<strong>因果语言建模或自回归语言建模</strong>，指的是模型只能根据当前位置之前的 token 来预测下一个 token，不能看到未来 token。比如给定“我 喜欢”，模型预测下一个 token “吃”；预测时不能提前看到后面的“苹果”。</p><p>在 Transformer Decoder 中，通常通过 causal mask 实现这一点。causal mask 会遮住当前位置之后的 token，使未来位置的 attention 权重为 0，从而保证每个位置只能关注自己和前面的 token。GPT 类 Decoder-only 模型就是典型的 Causal Language Model。</p><p>从概率角度看，CLM 会把一段文本的联合概率分解成多个条件概率的乘积，即每个 token 都依赖它前面的 token。它的优点是训练目标简单、不需要人工标注、和自回归生成过程一致，因此非常适合文本生成。简单说，Causal Language Modeling 就是<strong>“只能看过去，预测未来”</strong>。</p></blockquote>"},{"t":"Mask Language Modeling是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Masked Language Modeling，简称 MLM，<strong>核心是：随机遮住输入中的一部分 token，让模型根据左右上下文预测被遮住的 token。</strong></p><ol><li>MLM 在做什么？</li></ol><p>MLM 会从一句话中随机选择一些 token 替换成 <code>[MASK]</code>。</p><p>比如原句：</p><pre><code>我 喜欢 吃 苹果</code></pre><p>变成：</p><pre><code>我 喜欢 吃 [MASK]</code></pre><p>模型要预测：</p><pre><code>[MASK] = 苹果</code></pre><p>它可以同时利用左边和右边的上下文。</p><ol><li>为什么叫 Masked？</li></ol><p>因为训练时会把部分 token 遮住。</p><p>模型不知道被遮住的内容，只能通过上下文推断。</p><p>例如：</p><pre><code>北京 是 中国 的 [MASK]</code></pre><p>模型应该预测：</p><pre><code>首都</code></pre><ol><li>BERT 使用 MLM</li></ol><p>BERT 的核心预训练任务就是 MLM。</p><p>BERT 是 Encoder-only 模型，使用双向 Self-Attention。</p><p>所以它可以同时看前文和后文。</p><p>这和 MLM 很匹配。</p><ol><li>MLM 和 CLM 的区别</li></ol><p>CLM，也就是 Causal Language Modeling：</p><pre><code>只能看左边，预测下一个 token</code></pre><p>代表模型：</p><pre><code>GPT</code></pre><p>MLM：</p><pre><code>可以看左右上下文，预测被 mask 的 token</code></pre><p>代表模型：</p><pre><code>BERT</code></pre><p>所以：</p><pre><code>CLM 更适合生成<br/>MLM 更适合理解</code></pre><ol><li>MLM 为什么适合理解任务？</li></ol><p>很多理解任务需要完整上下文。</p><p>比如判断一个词的含义，要看前后文。</p><p>例子：</p><pre><code>我去银行存钱<br/>河边有一家银行</code></pre><p>同样是“银行”，含义要靠上下文判断。</p><p>MLM 让模型在预训练时就学习双向上下文表示，所以适合理解任务。</p><ol><li>MLM 的训练方式</li></ol><p>BERT 中常见做法是随机选择一部分 token。</p><p>其中：</p><ul><li>一部分替换成 <code>[MASK]</code></li><li>一部分替换成随机 token</li><li>一部分保持原样</li></ul><p>这样做是为了缓解预训练和下游任务之间的不一致。</p><p>因为真实下游任务里通常没有 <code>[MASK]</code> token。</p><ol><li>MLM 的缺点</li></ol><p>MLM 也有缺点：</p><ul><li>预训练时有 <code>[MASK]</code>，生成时没有，存在不一致</li><li>不是天然生成式任务</li><li>一次只预测被 mask 的位置</li><li>不适合从左到右连续生成长文本</li><li>训练效率相对不如 CLM 直接利用每个位置预测下一个 token</li></ul><p>所以 BERT 更偏理解，GPT 更偏生成。</p><ol><li><strong>面试背诵版：Masked Language Modeling 是什么？</strong></li></ol><blockquote><p>Masked Language Modeling，简称 MLM，是一种语言模型预训练任务。<strong>它会随机遮住输入序列中的一部分 token，然后让模型根据左右上下文预测被遮住的 token</strong>。比如把“我喜欢吃苹果”变成“我喜欢吃 [MASK]”，模型需要预测 <code>[MASK]</code> 是“苹果”。</p><p>MLM 典型代表是 BERT。因为 BERT 使用 Encoder-only 架构和双向 Self-Attention，可以同时看到被遮住位置的前文和后文，所以非常适合学习上下文理解能力。它训练出来的表示常用于文本分类、语义匹配、NER、阅读理解等理解类任务。</p><p>它和 Causal Language Modeling 的区别是：CLM 只能看左边上下文预测下一个 token，代表模型是 GPT，更适合生成；MLM 可以看左右上下文预测被 mask 的 token，代表模型是 BERT，更适合理解。简单说，MLM 是“遮<strong>住一部分词，让模型根据上下文补回来</strong>”。</p></blockquote>"},{"t":"GPT和BERT训练方式有什么区别？","tag":"","p":"","html":"<p>GPT 和 BERT 训练方式的核心区别是：<strong>GPT 用自回归语言建模，只看前文预测下一个 token；BERT 用掩码语言建模，看左右上下文预测被 mask 的 token。</strong></p><ol><li>GPT 的训练方式</li></ol><p>GPT 使用 Causal Language Modeling，也叫自回归语言建模。</p><p>训练目标是：</p><pre><code>根据前面的 token，预测下一个 token</code></pre><p>比如：</p><pre><code>输入：我 喜欢<br/>目标：吃</code></pre><p>模型只能看左边上下文，不能看未来 token。</p><p>它通过 causal mask 实现。</p><ol><li>BERT 的训练方式</li></ol><p>BERT 使用 Masked Language Modeling。</p><p>训练目标是：</p><pre><code>随机遮住一些 token，让模型根据左右上下文预测它</code></pre><p>比如：</p><pre><code>输入：我 喜欢 [MASK] 苹果<br/>目标：吃</code></pre><p>BERT 可以同时看前文和后文，所以是双向建模。</p><ol><li>注意力方向不同</li></ol><p>GPT：</p><pre><code>单向注意力，只能看当前位置之前的 token</code></pre><p>BERT：</p><pre><code>双向注意力，可以看整个输入序列</code></pre><p>所以 GPT 更适合生成，BERT 更适合理解。</p><ol><li>架构不同</li></ol><p>GPT 通常是：</p><pre><code>Decoder-only Transformer</code></pre><p>使用 Masked Self-Attention，也就是 causal mask。</p><p>BERT 是：</p><pre><code>Encoder-only Transformer</code></pre><p>使用双向 Self-Attention。</p><ol><li>训练目标不同</li></ol><p>GPT 训练目标是最大化文本从左到右的生成概率：</p><pre><code>P(x_t | x_1, ..., x_{t-1})</code></pre><p>BERT 训练目标是恢复被遮住的 token：</p><pre><code>P(x_mask | left context, right context)</code></pre><p>所以 GPT 学的是续写能力。</p><p>BERT 学的是上下文理解能力。</p><ol><li>下游任务偏向不同</li></ol><p>GPT 更适合：</p><ul><li>文本生成</li><li>对话</li><li>写作</li><li>代码生成</li><li>指令跟随</li><li>Agent 决策</li></ul><p>BERT 更适合：</p><ul><li>文本分类</li><li>语义匹配</li><li>NER</li><li>阅读理解</li><li>信息抽取</li><li>检索表示</li></ul><p>当然，现在 GPT 类模型能力很强，也能做理解任务。</p><ol><li>训练和推理一致性不同</li></ol><p>GPT 训练时看前文预测下一个 token。</p><p>推理时也是看前文生成下一个 token。</p><p>所以训练和推理方式一致。</p><p>BERT 训练时有 <code>[MASK]</code> token，但下游任务通常没有 <code>[MASK]</code>。</p><p>所以 MLM 存在一定预训练和下游使用不一致的问题。</p><ol><li><strong>面试背诵版：GPT 和 BERT 训练方式有什么区别？</strong></li></ol><blockquote><p>GPT 和 BERT 的训练方式主要区别在于预训练目标和上下文方向。GPT 使用 Causal Language Modeling，也就是自回归语言建模，模型只能看当前位置之前的 token，通过 causal mask 防止看到未来信息，训练目标是根据前文预测下一个 token。因此 GPT 的训练方式和推理生成过程一致，都按从左到右逐 token 生成。</p><p>BERT 使用 Masked Language Modeling，也就是随机遮住输入中的一部分 token，让模型根据左右上下文预测被 mask 的 token。因为 BERT 是 Encoder-only 架构，使用双向 Self-Attention，所以可以同时利用前文和后文，适合学习上下文理解表示。</p><p>简单说<strong>，GPT 是“看左边，猜下一个”，更适合生成</strong>；<strong>BERT 是“看两边，补中间”</strong>，更适合理解。GPT 通常采用 Decoder-only，BERT 采用 Encoder-only。GPT 的训练和推理更一致，而 BERT 的 MLM 中存在 <code>[MASK]</code> token，和很多下游任务场景略有不一致。</p></blockquote>"}]},{"no":"","title":"6.2 数据","questions":[{"t":"大模型训练数据来源有哪些？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>大模型训练数据来源，核心包括：公开互联网文本、书籍论文、代码数据、百科知识、对话指令数据、领域数据和人工/模型生成数据。</p><ol><li>互联网公开文本</li></ol><p>这是大模型预训练中很重要的数据来源。</p><p>包括：</p><ul><li>网页</li><li>新闻</li><li>博客</li><li>论坛</li><li>问答网站</li><li>百科页面</li><li>技术文档</li><li>公开社区内容</li></ul><p>优点是规模大、覆盖面广。</p><p>缺点是噪声多，需要清洗。</p><ol><li>书籍和论文</li></ol><p>高质量长文本也很重要。</p><p>比如：</p><ul><li>电子书</li><li>教材</li><li>学术论文</li><li>技术报告</li><li>专利</li><li>课程资料</li></ul><p>这些数据通常结构更完整、知识密度更高，有助于模型学习长文逻辑和专业知识。</p><ol><li>代码数据</li></ol><p>代码模型或通用大模型都会用代码数据。</p><p>来源包括：</p><ul><li>开源代码仓库</li><li>GitHub</li><li>API 文档</li><li>StackOverflow</li><li>技术博客</li><li>编程教程</li><li>单元测试</li><li>配置文件</li></ul><p>代码数据能提升：</p><ul><li>代码生成</li><li>Bug 修复</li><li>逻辑推理</li><li>工具调用</li><li>结构化输出能力</li></ul><ol><li>百科和知识库</li></ol><p>百科类数据有较强结构性。</p><p>比如：</p><ul><li>Wikipedia</li><li>Wikidata</li><li>百科词条</li><li>开放知识库</li><li>术语库</li></ul><p>这类数据有助于模型学习事实知识、实体关系和概念解释。</p><ol><li>对话和指令数据</li></ol><p>预训练之后，模型还需要学会听懂人的指令。</p><p>常见数据包括：</p><ul><li>问答数据</li><li>多轮对话</li><li>指令-回答对</li><li>人工标注数据</li><li>客服对话</li><li>任务执行样例</li><li>安全拒答样例</li><li>偏好排序数据</li></ul><p>这些主要用于指令微调和对齐。</p><ol><li>领域数据</li></ol><p>如果做行业模型，会加入领域数据。</p><p>比如：</p><ul><li>医疗文献</li><li>法律法规</li><li>金融研报</li><li>电商商品数据</li><li>企业知识库</li><li>内部文档</li><li>工单记录</li><li>日志数据</li><li>业务 SOP</li></ul><p>领域数据能提升模型在特定行业的专业能力。</p><ol><li>合成数据</li></ol><p>现在也常用模型生成数据。</p><p>比如：</p><ul><li>生成问答对</li><li>生成推理过程</li><li>生成代码题</li><li>生成多轮对话</li><li>生成工具调用样例</li><li>生成安全训练数据</li></ul><p>合成数据成本低、覆盖可控，但要注意质量和偏差。</p><ol><li>数据清洗和过滤很重要</li></ol><p>数据不是越多越好。</p><p>需要做：</p><ul><li>去重</li><li>去噪</li><li>去低质量内容</li><li>过滤敏感信息</li><li>过滤违法有害内容</li><li>语言识别</li><li>格式清洗</li><li>质量打分</li><li>去除个人隐私</li><li>控制版权风险</li></ul><p>高质量数据比单纯大规模更重要。</p><ol><li><strong>面试背诵版：大模型训练数据来源有哪些？</strong></li></ol><blockquote><p>大模型训练数据主要<strong>包括公开互联网文本、书籍论文、代码数据、百科知识库、对话指令数据、领域数据和合成数据</strong>。互联网文本包括网页、新闻、博客、论坛、问答网站和技术文档，规模大、覆盖广，但噪声多；书籍、教材、论文和技术报告质量较高，有助于模型学习长文本结构和专业知识；代码数据来自开源仓库、API 文档、编程问答和教程，可以提升代码生成、逻辑推理和结构化能力。</p><p>百科和知识库数据有助于学习事实知识和实体关系；指令数据、多轮对话、问答数据、偏好排序和安全拒答样例主要用于指令微调和对齐；行业模型还会加入医疗、法律、金融、电商、企业文档、工单和业务 SOP 等领域数据。现在也常使用合成数据来补充推理、工具调用和安全场景。</p><p>不过数据不是越多越好，训练前必须做去重、清洗、质量过滤、隐私脱敏、有害内容过滤和版权风险控制。简单说，大模型能力很大程度取决于“数据规模 + 数据质量 + 数据覆盖范围”。</p></blockquote>"},{"t":"数据清洗流程是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>数据清洗的核心是：<strong>把原始数据中的脏数据、重复数据、低质量数据、敏感数据和不合规数据过滤掉，保留高质量、可训练的数据。</strong></p><ol><li>数据收集和格式统一</li></ol><p>第一步是把不同来源的数据收集起来。</p><p>来源可能包括：</p><ul><li>网页</li><li>文档</li><li>代码仓库</li><li>问答数据</li><li>论文</li><li>日志</li><li>企业知识库</li></ul><p>然后统一格式，比如转成：</p><pre><code>{<br/>  \"text\": \"...\",<br/>  \"source\": \"...\",<br/>  \"language\": \"zh\",<br/>  \"metadata\": {}<br/>}</code></pre><p>这样后续处理更方便。</p><ol><li>解析和抽取正文</li></ol><p>原始数据里经常有很多噪声。</p><p>比如网页中有：</p><ul><li>HTML 标签</li><li>导航栏</li><li>广告</li><li>评论区</li><li>页脚</li><li>JS / CSS</li><li>重复模板</li></ul><p>需要抽取正文内容。</p><p>否则模型会学到很多无意义页面结构。</p><ol><li>去重</li></ol><p>去重非常重要。</p><p>包括：</p><ul><li>精确去重</li><li>近似去重</li><li>文档级去重</li><li>段落级去重</li><li>句子级去重</li></ul><p>如果重复数据太多，模型可能过度记忆，影响泛化，还会浪费训练算力。</p><ol><li>质量过滤</li></ol><p>要过滤低质量文本。</p><p>比如：</p><ul><li>乱码</li><li>机器翻译垃圾文本</li><li>重复字符</li><li>关键词堆砌</li><li>采集失败页面</li><li>过短或过长异常文本</li><li>低信息密度内容</li><li>广告营销内容</li><li>无意义列表</li></ul><p>可以用规则、模型打分或分类器判断质量。</p><ol><li>语言识别和分类</li></ol><p>大模型训练通常需要知道数据语言。</p><p>要做：</p><ul><li>语言识别</li><li>多语言比例控制</li><li>代码 / 自然语言分类</li><li>领域分类</li><li>文档类型分类</li></ul><p>比如中文、英文、代码、数学、论文、网页数据可以分开统计和采样。</p><ol><li>敏感和有害内容过滤</li></ol><p>要过滤或脱敏：</p><ul><li>个人隐私 PII</li><li>手机号</li><li>邮箱</li><li>身份证</li><li>地址</li><li>密钥</li><li>token</li><li>密码</li><li>恶意代码</li><li>暴力违法内容</li><li>仇恨歧视内容</li></ul><p>尤其企业数据和代码数据中很容易有密钥、token、内部地址。</p><ol><li>安全和合规处理</li></ol><p>还要关注：</p><ul><li>版权风险</li><li>数据授权</li><li>robots 协议</li><li>用户隐私</li><li>未成年人数据</li><li>医疗法律等敏感领域</li><li>企业内部数据访问权限</li></ul><p>训练数据必须可用、可追溯、可审计。</p><ol><li>分词和长度过滤</li></ol><p>训练前还要 tokenizer 处理。</p><p>比如：</p><ul><li>统计 token 数</li><li>过滤超长样本</li><li>切分长文档</li><li>拼接短文本</li><li>保留结构边界</li><li>控制样本长度分布</li></ul><p>这样能提高训练效率。</p><ol><li>数据采样和配比</li></ol><p>清洗后还要决定不同数据怎么混合。</p><p>比如：</p><ul><li>中文多少</li><li>英文多少</li><li>代码多少</li><li>数学多少</li><li>对话多少</li><li>高质量数据加权多少</li></ul><p>数据配比会明显影响模型能力。</p><ol><li><strong>面试背诵版：数据清洗流程是什么？</strong></li></ol><blockquote><p>数据清洗流程通常包括格式统一、正文抽取、去重、质量过滤、语言识别、安全过滤、合规处理、分词长度处理和数据配比。首先要把不同来源的数据统一成标准格式，并从网页、文档中抽取正文，去除 HTML、广告、导航、页脚、JS、CSS 等噪声。然后做精确去重和近似去重，避免模型过度记忆重复内容，也减少训练算力浪费。</p><p>接着要做质量过滤，去掉乱码、采集失败页面、重复字符、低信息密度文本、广告营销内容和无意义列表；同时进行语言识别、代码/自然语言分类、领域分类，方便后续按比例采样。安全上要过滤或脱敏个人隐私、手机号、邮箱、身份证、地址、密码、API Key、token、恶意代码和违法有害内容。</p><p>最后还要做合规检查，比如版权、授权、隐私和数据来源可追溯；训练前通过 tokenizer 统计长度，切分超长文本、过滤异常样本，并根据目标能力调整中文、英文、代码、数学、对话等数据比例。简单说，数据清洗的目标是“去噪、去重、提质、脱敏、合规和控制数据配比”。</p></blockquote>"},{"t":"为什么需要数据去重？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>数据去重的核心原因是：<strong>重复数据会浪费训练算力、降低数据多样性，还可能导致模型过度记忆和评估结果虚高。</strong></p><ol><li>避免浪费训练算力</li></ol><p>训练大模型成本非常高。</p><p>如果语料里大量重复内容，模型会反复学习同样样本。</p><p>这会浪费：</p><ul><li>GPU 时间</li><li>训练 token</li><li>存储</li><li>数据加载成本</li><li>训练预算</li></ul><p>同样的算力应该尽量用在更多样、更高质量的数据上。</p><ol><li>提升数据多样性</li></ol><p>重复数据会挤占训练配额。</p><p>比如某些网页模板、公告、广告、转载文章重复出现很多次。</p><p>这会导致模型看到的信息变窄。</p><p>去重后可以让训练数据覆盖更多：</p><ul><li>主题</li><li>语言</li><li>风格</li><li>领域</li><li>任务模式</li><li>表达方式</li></ul><p>数据多样性越好，泛化能力通常越强。</p><ol><li>防止模型过度记忆</li></ol><p>如果某些文本重复很多次，模型可能把它“背下来”。</p><p>风险包括：</p><ul><li>生成训练原文</li><li>记忆隐私信息</li><li>记忆版权内容</li><li>对重复内容过拟合</li><li>泛化能力下降</li></ul><p>尤其是包含个人隐私、代码密钥、内部文档的重复数据，风险更高。</p><ol><li>避免训练分布被污染</li></ol><p>重复数据会改变真实数据分布。</p><p>比如某个低质量模板出现 100 万次，模型会认为这种模式很重要。</p><p>结果可能学到：</p><ul><li>广告话术</li><li>垃圾文本</li><li>模板化表达</li><li>重复网页结构</li><li>低质量问答模式</li></ul><p>去重可以减少这类分布偏差。</p><ol><li>防止评估结果虚高</li></ol><p>如果测试集或 benchmark 数据泄露到训练集中，模型可能提前见过答案。</p><p>这会导致评估结果虚高。</p><p>去重时要做：</p><ul><li>训练集内部去重</li><li>训练集和验证集去重</li><li>训练集和测试集去重</li><li>与 benchmark 数据去重</li></ul><p>否则模型看似能力强，其实是在背答案。</p><ol><li>减少版权和隐私风险</li></ol><p>重复内容更容易被模型记住。</p><p>如果重复的是：</p><ul><li>版权文本</li><li>用户隐私</li><li>邮箱电话</li><li>API Key</li><li>内部文档</li><li>代码片段</li></ul><p>模型记忆和泄露风险会更高。</p><p>去重能降低这类风险。</p><ol><li>去重有哪些类型？</li></ol><p>常见包括：</p><ul><li>精确去重：完全一样的文本</li><li>近似去重：内容高度相似</li><li>文档级去重</li><li>段落级去重</li><li>句子级去重</li><li>跨数据集去重</li><li>benchmark 去重</li></ul><p>大模型训练通常会做近似去重，因为网页转载、模板页面很多只是略有差异。</p><ol><li><strong>面试背诵版：为什么需要数据去重？</strong></li></ol><blockquote><p>数据去重很重要，因为重复数据会浪费训练算力，降低数据多样性，并导致模型过度记忆。大模型训练成本很高，如果语料里大量重复网页、转载文章、模板内容或广告文本，模型会反复学习相同模式，既浪费 token 预算，也会挤占更有价值的数据比例，影响泛化能力。</p><p>重复数据还可能让模型记住训练样本，尤其是版权内容、个人隐私、API Key、内部文档或代码片段，增加泄露风险。同时，如果训练集和测试集或 benchmark 之间存在重复，会造成数据泄露，使评估结果虚高，看起来模型能力很强，但其实可能只是见过答案。</p><p>所以大模型训练通常会做精确去重、近似去重、文档级去重、段落级去重，以及训练集和验证集、测试集、benchmark 之间的跨集合去重。简单说，去重的目标是“减少浪费、提升多样性、防止记忆、避免数据泄露和降低合规风险”。</p></blockquote>"},{"t":"数据质量为什么重要？","tag":"很重要！！！","p":"core","html":"<blockquote><p></p></blockquote>"},{"t":"数据污染是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"}]},{"no":"","title":"6.3 训练过程","questions":[{"t":"一个Batch输入模型后发生什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>一个 Batch 输入模型后，核心过程是：<strong>先把文本转成 token 和张量，做前向传播算预测和 loss，再反向传播算梯度，最后优化器更新参数。</strong></p><ol><li>Batch 是什么？</li></ol><p>Batch 是一次送进模型的一组样本。</p><p>比如：</p><pre><code>batch size = 32</code></pre><p>意思是一次训练同时处理 32 条样本。</p><p>使用 batch 的好处是：</p><ul><li>提高 GPU 并行效率</li><li>梯度估计更稳定</li><li>训练速度更快</li><li>比单样本训练更高效</li></ul><ol><li>文本先经过 Tokenizer</li></ol><p>原始文本不能直接进模型。</p><p>要先经过 tokenizer：</p><pre><code>文本 → tokens → token ids</code></pre><p>比如：</p><pre><code>我喜欢机器学习<br/>→ [123, 456, 789, 321]</code></pre><p>如果一个 batch 里样本长度不同，通常要做 padding 或 packing。</p><ol><li>组成输入张量</li></ol><p>Tokenizer 后会得到：</p><ul><li>input_ids</li><li>attention_mask</li><li>labels</li><li>position_ids</li></ul><p>其中：</p><ul><li>input_ids：token id</li><li>attention_mask：哪些位置是真 token，哪些是 padding</li><li>labels：训练目标</li><li>position_ids：位置信息</li></ul><p>这些会组成张量送进 GPU。</p><ol><li>Embedding 层</li></ol><p>模型先把 token id 转成向量。</p><p>流程是：</p><pre><code>input_ids → token embedding</code></pre><p>然后加上或注入位置信息，比如：</p><ul><li>绝对位置编码</li><li>RoPE</li><li>ALiBi</li></ul><p>得到每个 token 的初始表示。</p><ol><li>前向传播</li></ol><p>输入会经过多层 Transformer。</p><p>每层通常包括：</p><pre><code>Attention<br/>FFN<br/>Residual Connection<br/>LayerNorm / RMSNorm</code></pre><p>模型会输出 logits：</p><pre><code>batch_size × seq_len × vocab_size</code></pre><p>logits 表示每个位置预测词表中每个 token 的分数。</p><ol><li>计算 Loss</li></ol><p>训练大语言模型时，通常做 next token prediction。</p><p>也就是：</p><pre><code>用当前位置预测下一个 token</code></pre><p>模型输出 logits 后，和 labels 计算交叉熵 loss。</p><p>如果有 padding token，会通过 mask 忽略掉，不参与 loss。</p><ol><li>反向传播</li></ol><p>得到 loss 后，调用 backward。</p><p>模型会沿计算图反向计算每个参数的梯度。</p><p>流程是：</p><pre><code>loss → 输出层 → Transformer 后层 → Transformer 前层 → embedding</code></pre><p>每个参数都会得到对应梯度。</p><ol><li>优化器更新参数</li></ol><p>梯度算完后，优化器更新参数。</p><p>比如 AdamW：</p><pre><code>根据梯度、一阶矩、二阶矩、weight decay 更新参数</code></pre><p>更新后通常会清空梯度：</p><pre><code>zero_grad</code></pre><p>然后进入下一个 batch。</p><ol><li><strong>面试背诵版：一个 Batch 输入模型后发生什么？</strong></li></ol><blockquote><p>一个 batch 输入模型后，首先原始文本会经过 tokenizer 转成 token ids，并根据长度做 padding、truncation 或 packing，形成 input_ids、attention_mask、labels、position_ids 等张量。然后 token ids 进入 embedding 层转成向量，并加入或注入位置信息。</p><p>接着进行前向传播，输入经过多层 Transformer，每层包含 Attention、FFN、残差连接和 LayerNorm / RMSNorm，最后输出 logits，形状通常是 batch_size × seq_len × vocab_size。对于大语言模型训练，会用当前位置的输出预测下一个 token，并和 labels 计算交叉熵 loss，padding 部分会通过 mask 忽略。</p><p>得到 loss 后，反向传播会沿计算图从后往前计算每个参数的梯度；然后优化器，比如 AdamW，根据梯度、一阶矩、二阶矩和 weight decay 更新参数，并清空梯度进入下一个 batch。简单说，一个 batch 的训练流程是“tokenize → 组张量 → embedding → 前向传播 → 计算 loss → 反向传播 → 优化器更新”。</p></blockquote>"},{"t":"Loss如何计算？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Perplexity是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"如何判断模型训练是否收敛？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"为什么大模型训练需要大量GPU？","tag":"","p":"","html":"<blockquote><p></p></blockquote><span>[原文档此处有配图]</span>"}]},{"no":"","title":"7.1 SFT","questions":[{"t":"为什么需要微调？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>需要微调的核心原因是：<strong>预训练模型虽然通用能力强，但不一定适合具体业务任务、领域知识、输出格式和行为要求。</strong></p><ol><li>预训练模型是通用的</li></ol><p>大模型预训练通常使用海量通用语料。</p><p>它能学到：</p><ul><li>语言规律</li><li>常识知识</li><li>基础推理</li><li>代码模式</li><li>多任务能力</li></ul><p>但它不一定知道某个企业、行业或具体任务的规则。</p><p>比如：</p><pre><code>公司的售后政策<br/>内部接口规范<br/>特定医学术语<br/>金融风控规则<br/>客服话术标准</code></pre><p>这些就需要进一步适配。</p><ol><li>微调让模型适配特定任务</li></ol><p>微调可以让模型在目标任务上表现更好。</p><p>比如：</p><ul><li>情感分类</li><li>意图识别</li><li>客服问答</li><li>法律文书生成</li><li>医疗报告总结</li><li>代码生成规范</li><li>SQL 生成</li><li>工具调用</li><li>结构化抽取</li></ul><p>通过任务数据训练，模型会更熟悉任务输入、输出和判断标准。</p><ol><li>微调让模型学习领域知识</li></ol><p>通用模型可能缺少领域知识，或者理解不够准确。</p><p>比如：</p><ul><li>医疗</li><li>法律</li><li>金融</li><li>电商</li><li>工业</li><li>教育</li><li>企业内部知识</li></ul><p>微调可以让模型更熟悉领域术语、业务流程和专业表达。</p><p>不过如果只是补充经常变化的知识，RAG 有时比微调更合适。</p><ol><li>微调让输出格式更稳定</li></ol><p>很多业务需要固定格式输出。</p><p>比如：</p><pre><code>{<br/>  \"intent\": \"refund\",<br/>  \"confidence\": 0.92,<br/>  \"need_human\": false<br/>}</code></pre><p>微调可以让模型更稳定地遵守：</p><ul><li>JSON 格式</li><li>字段规范</li><li>分类标签</li><li>话术模板</li><li>工具调用参数格式</li><li>代码风格</li></ul><p>这对生产系统很重要。</p><ol><li>微调让模型符合行为偏好</li></ol><p>除了知识和任务，微调还可以调整模型行为。</p><p>比如让模型：</p><ul><li>回答更简洁</li><li>更像客服</li><li>更安全</li><li>更少幻觉</li><li>更符合企业话术</li><li>更会拒答</li><li>更会追问</li><li>更会调用工具</li></ul><p>这类属于对齐和行为适配。</p><ol><li>微调可以降低推理成本</li></ol><p>如果一个小模型经过微调后能完成特定任务，就不一定需要每次调用大模型。</p><p>比如：</p><pre><code>通用大模型 → 成本高<br/>微调小模型 → 成本低、延迟低</code></pre><p>在高频固定任务里，微调小模型很有价值。</p><ol><li>微调不适合所有问题</li></ol><p>面试要补一句。</p><p>微调不是万能的。</p><p>如果需求是：</p><ul><li>补充实时知识</li><li>查询企业文档</li><li>频繁变化的政策</li><li>私有知识库问答</li><li>需要引用来源</li></ul><p>通常 RAG 更适合。</p><p>如果需求是：</p><ul><li>改变模型行为</li><li>学会固定格式</li><li>学会特定任务模式</li><li>学会工具调用模式</li><li>领域语言风格</li></ul><p>微调更适合。</p><ol><li><strong>面试背诵版：为什么需要微调？</strong></li></ol><blockquote><p>需要微调，是因为预训练大模型虽然具备通用语言能力和基础知识，但它不一定适合具体业务场景、领域术语、任务格式和行为规范。微调可以让模型在特定任务上表现更好，比如意图识别、信息抽取、客服问答、代码生成、SQL 生成、工具调用和结构化输出。</p><p>微调还能让模型适应领域知识和企业风格，比如医疗、法律、金融、电商等专业术语和业务流程；也能让输出格式更稳定，比如固定 JSON、分类标签、工具参数或客服话术。同时，微调可以调整模型行为偏好，让模型更简洁、更安全、更会拒答、更符合企业规范。对于高频固定任务，微调小模型还可以降低推理成本和延迟。</p><p>不过微调不是万能的。如果只是补充频繁变化的知识、企业文档问答或需要引用来源，RAG 往往更合适；如果是让模型学习任务模式、输出格式、领域风格和行为边界，微调更合适。简单说，微调的目标是让通用模型适配具体任务和业务场景。</p></blockquote>"},{"t":"Pretrain和SFT有什么区别？","tag":"","p":"","html":"<ol><li>Pretrain 和 SFT 有什么区别？</li></ol><p>Pretrain 和 SFT 的核心区别是：Pretrain 让模型学通用语言和知识能力，SFT 让模型学会按照人类指令完成任务。</p><ol><li>Pretrain 是什么？</li></ol><p>Pretrain 是预训练。</p><p>它通常使用海量通用文本训练模型。</p><p>训练目标多是：</p><pre><code>Next Token Prediction</code></pre><p>也就是根据前文预测下一个 token。</p><p>比如：</p><pre><code>输入：中国的首都是<br/>目标：北京</code></pre><p>预训练让模型学到：</p><ul><li>语言规律</li><li>语法语义</li><li>世界知识</li><li>代码结构</li><li>文本风格</li><li>基础推理模式</li></ul><ol><li>SFT 是什么？</li></ol><p>SFT 全称是 Supervised Fine-Tuning。</p><p>也就是监督微调。</p><p>它通常使用高质量指令数据训练模型。</p><p>数据形式是：</p><pre><code>用户指令 → 理想回答</code></pre><p>比如：</p><pre><code>请解释什么是过拟合<br/>→ 过拟合是指模型在训练集上表现很好...</code></pre><p>SFT 的目标是让模型学会听懂指令、按要求回答。</p><ol><li>数据不同</li></ol><p>Pretrain 数据通常是大规模原始文本。</p><p>比如：</p><ul><li>网页</li><li>书籍</li><li>论文</li><li>代码</li><li>百科</li><li>文档</li></ul><p>SFT 数据通常是人工或高质量构造的指令数据。</p><p>比如：</p><ul><li>问答</li><li>多轮对话</li><li>任务指令</li><li>格式输出</li><li>安全拒答</li><li>工具调用样例</li></ul><ol><li>训练目标不同</li></ol><p>Pretrain 目标：</p><pre><code>学习文本分布，预测下一个 token</code></pre><p>SFT 目标：</p><pre><code>模仿高质量回答，学会遵循指令</code></pre><p>所以 Pretrain 更偏“通用能力学习”。</p><p>SFT 更偏“行为对齐和任务适配”。</p><ol><li>模型能力变化不同</li></ol><p>Pretrain 后的模型可能会续写文本，但不一定听话。</p><p>比如用户问问题，它可能继续补全文本，而不是直接回答。</p><p>SFT 后模型会更像助手：</p><ul><li>知道该回答问题</li><li>知道遵循格式</li><li>知道拒绝危险请求</li><li>知道多轮对话</li><li>知道按用户要求输出</li></ul><ol><li>训练规模不同</li></ol><p>Pretrain 通常：</p><ul><li>数据量巨大</li><li>训练成本极高</li><li>时间长</li><li>算力消耗大</li></ul><p>SFT 通常：</p><ul><li>数据量相对小</li><li>质量要求高</li><li>成本低很多</li><li>更关注指令质量和覆盖面</li></ul><ol><li>关系是什么？</li></ol><p>一般流程是：</p><pre><code>Pretrain → SFT → RLHF / DPO</code></pre><p>也就是：</p><pre><code>先学语言和知识<br/>再学听指令<br/>最后学人类偏好</code></pre><p>没有 Pretrain，模型没有基础能力。</p><p>没有 SFT，模型可能不会很好地按指令完成任务。</p><ol><li><strong>面试背诵版：Pretrain 和 SFT 有什么区别？</strong></li></ol><blockquote><p>Pretrain，也就是预训练，主要是在海量通用语料上训练模型，常见目标是 next token prediction，让模型学习语言规律、语法语义、世界知识、代码结构和基础推理模式。它解决的是模型“有没有通用能力”的问题。</p><p>SFT，也就是 Supervised Fine-Tuning，监督微调，通常使用高质量的“用户指令-理想回答”数据训练模型，让模型学会按照人类指令回答问题、遵守输出格式、进行多轮对话、执行具体任务，并具备基础安全拒答能力。它解决的是模型“会不会按人的要求做事”的问题。</p><p>简单说，Pretrain 是“学知识和语言能力”，SFT 是“学会听指令和按格式回答”。大模型训练流程通常是 Pretrain → SFT → RLHF 或 DPO，先获得通用能力，再进行指令对齐和偏好对齐。</p></blockquote>"},{"t":"Instruction Tuning是什么？","tag":"","p":"","html":"<p>Instruction Tuning 的核心是：用大量“指令-回答”数据训练模型，让模型学会理解人类指令，并按要求完成任务。</p><ol><li>Instruction Tuning 是什么？</li></ol><p>Instruction Tuning 可以理解为指令微调。</p><p>它使用的数据一般是：</p><pre><code>instruction → response</code></pre><p>比如：</p><pre><code>请总结下面这段话<br/>→ 总结结果<br/><br/>请把这句话翻译成英文<br/>→ Translation<br/><br/>请判断这段评论情感<br/>→ 正面 / 负面</code></pre><p>模型通过学习这些样例，学会“用户让我做什么，我应该怎么答”。</p><ol><li>它解决什么问题？</li></ol><p>预训练模型虽然会语言建模，但不一定会听指令。</p><p>比如预训练模型看到：</p><pre><code>请解释什么是梯度下降</code></pre><p>可能只是继续续写类似文本，不一定给出清晰回答。</p><p>Instruction Tuning 后，模型更懂：</p><ul><li>回答问题</li><li>总结</li><li>翻译</li><li>分类</li><li>抽取</li><li>改写</li><li>写代码</li><li>遵守格式</li><li>多轮对话</li></ul><ol><li>和 SFT 的关系</li></ol><p>Instruction Tuning 通常可以看作 SFT 的一种。</p><p>SFT 是监督微调的总称。</p><p>Instruction Tuning 是专门用指令数据做监督微调。</p><p>所以可以说：</p><pre><code>Instruction Tuning ⊂ SFT</code></pre><p>不过日常语境里，很多人会把二者混用。</p><ol><li>数据形式</li></ol><p>常见数据形式包括：</p><pre><code>{<br/>  \"instruction\": \"请翻译成英文\",<br/>  \"input\": \"我喜欢机器学习\",<br/>  \"output\": \"I like machine learning.\"<br/>}</code></pre><p>或者对话格式：</p><pre><code>User: 请解释什么是过拟合<br/>Assistant: 过拟合是指...</code></pre><p>关键是数据要告诉模型：</p><pre><code>面对不同任务指令时，应该如何响应。</code></pre><ol><li>为什么有效？</li></ol><p>因为它把很多任务统一成自然语言指令格式。</p><p>模型看到大量任务后，会学到：</p><ul><li>如何识别用户意图</li><li>如何选择回答方式</li><li>如何遵守格式要求</li><li>如何进行多任务泛化</li><li>如何在没见过的新任务上迁移</li></ul><p>这能显著提升 zero-shot 能力。</p><ol><li>Instruction Tuning 学到什么？</li></ol><p>它不仅学知识，更学行为模式。</p><p>包括：</p><ul><li>听懂任务</li><li>按步骤回答</li><li>输出结构化内容</li><li>遵守角色</li><li>不答非所问</li><li>在信息不足时追问</li><li>对危险请求拒答</li><li>使用礼貌语气</li></ul><ol><li>和 Prompt Engineering 区别</li></ol><p>Prompt Engineering 是推理时写好提示词。</p><p>Instruction Tuning 是训练时用指令数据改变模型行为。</p><p>可以理解为：</p><pre><code>Prompt Engineering：临时教<br/>Instruction Tuning：训练中教</code></pre><ol><li><strong>面试背诵版：Instruction Tuning 是什么？</strong></li></ol><blockquote><p>Instruction Tuning，也叫指令微调，是用大量“指令-回答”形式的数据对预训练模型进行监督微调，让模型学会理解人类自然语言指令，并按要求完成任务。数据通常包括问答、总结、翻译、分类、抽取、改写、代码生成、格式化输出和多轮对话等。</p><p>预训练模型主要学的是文本分布和语言知识，但不一定知道用户发出指令后应该如何回答。Instruction Tuning 通过把不同任务统一成 instruction → response 的形式，让模型学会识别用户意图、遵守输出格式、进行多任务泛化，并提升 zero-shot 指令跟随能力。</p><p>它通常可以看作 SFT 的一种，区别是 SFT 是监督微调的泛称，Instruction Tuning 特指使用指令数据进行微调。简单说，Instruction Tuning 是“训练模型听懂人话并按指令做事”。</p></blockquote>"},{"t":"SFT数据是什么格式？","tag":"","p":"","html":"<ol><li>SFT 数据是什么格式？</li></ol><p>SFT 数据的核心格式是：给模型一个输入指令，让模型学习对应的高质量输出回答。</p><ol><li>最基本格式</li></ol><p>最简单的 SFT 数据是：</p><pre><code>instruction → response</code></pre><p>比如：</p><pre><code>{<br/>  \"instruction\": \"请解释什么是过拟合\",<br/>  \"response\": \"过拟合是指模型在训练集上表现很好，但在测试集上表现较差...\"<br/>}</code></pre><p>模型学习的是：</p><pre><code>用户这样问时，应该这样答。</code></pre><ol><li>Instruction + Input + Output 格式</li></ol><p>很多数据集会拆成三部分：</p><pre><code>{<br/>  \"instruction\": \"请总结下面这段文字\",<br/>  \"input\": \"大语言模型是一类基于海量文本训练的模型...\",<br/>  \"output\": \"这段文字主要介绍了大语言模型的定义和训练方式。\"<br/>}</code></pre><p>含义是：</p><ul><li>instruction：任务要求</li><li>input：任务输入内容</li><li>output：标准答案</li></ul><p>这种适合总结、翻译、分类、抽取等任务。</p><ol><li>对话格式</li></ol><p>聊天模型常用多轮对话格式。</p><p>比如：</p><pre><code>{<br/>  \"messages\": [<br/>    {<br/>      \"role\": \"user\",<br/>      \"content\": \"什么是梯度下降？\"<br/>    },<br/>    {<br/>      \"role\": \"assistant\",<br/>      \"content\": \"梯度下降是一种优化算法...\"<br/>    }<br/>  ]<br/>}</code></pre><p>多轮对话可以是：</p><pre><code>{<br/>  \"messages\": [<br/>    {<br/>      \"role\": \"user\",<br/>      \"content\": \"什么是过拟合？\"<br/>    },<br/>    {<br/>      \"role\": \"assistant\",<br/>      \"content\": \"过拟合是...\"<br/>    },<br/>    {<br/>      \"role\": \"user\",<br/>      \"content\": \"怎么解决？\"<br/>    },<br/>    {<br/>      \"role\": \"assistant\",<br/>      \"content\": \"常见方法包括...\"<br/>    }<br/>  ]<br/>}</code></pre><ol><li>带 System Prompt 的格式</li></ol><p>有些 SFT 数据会包含 system 角色。</p><p>比如：</p><pre><code>{<br/>  \"messages\": [<br/>    {<br/>      \"role\": \"system\",<br/>      \"content\": \"你是一个专业、简洁的机器学习面试助手。\"<br/>    },<br/>    {<br/>      \"role\": \"user\",<br/>      \"content\": \"解释一下 Adam 优化器。\"<br/>    },<br/>    {<br/>      \"role\": \"assistant\",<br/>      \"content\": \"Adam 是一种自适应优化器...\"<br/>    }<br/>  ]<br/>}</code></pre><p>System 用来定义角色、风格、安全边界和输出要求。</p><ol><li>工具调用格式</li></ol><p>如果训练模型学会工具调用，SFT 数据可能包含 tool call。</p><p>比如：</p><pre><code>{<br/>  \"messages\": [<br/>    {<br/>      \"role\": \"user\",<br/>      \"content\": \"帮我查订单 123 的状态\"<br/>    },<br/>    {<br/>      \"role\": \"assistant\",<br/>      \"tool_calls\": [<br/>        {<br/>          \"name\": \"query_order\",<br/>          \"arguments\": {<br/>            \"order_id\": \"123\"<br/>          }<br/>        }<br/>      ]<br/>    },<br/>    {<br/>      \"role\": \"tool\",<br/>      \"content\": \"{\\\"status\\\":\\\"配送中\\\"}\"<br/>    },<br/>    {<br/>      \"role\": \"assistant\",<br/>      \"content\": \"您的订单当前正在配送中。\"<br/>    }<br/>  ]<br/>}</code></pre><p>这种数据用于训练模型什么时候调用工具、怎么生成参数、如何利用工具结果回答。</p><ol><li>训练时 labels 怎么处理？</li></ol><p>SFT 训练通常只对 assistant 的回答计算 loss。</p><p>也就是说：</p><pre><code>user / system 部分作为输入上下文<br/>assistant 部分作为学习目标</code></pre><p>否则模型可能学会模仿 user 提问。</p><p>所以训练时会 mask 掉用户输入，只训练 assistant 输出。</p><ol><li>好的 SFT 数据应该有什么特点？</li></ol><p>高质量 SFT 数据应该：</p><ul><li>指令清晰</li><li>回答准确</li><li>格式统一</li><li>覆盖多任务</li><li>多轮上下文合理</li><li>不含错误知识</li><li>不含隐私和敏感信息</li><li>有安全拒答样例</li><li>有格式约束样例</li><li>风格符合目标产品</li></ul><ol><li><strong>面试背诵版：SFT 数据是什么格式？</strong></li></ol><blockquote><p>SFT 数据的基本格式是“用户输入或指令，对应一个高质量标准回答”。最简单可以是 instruction → response，也可以是 instruction + input + output，其中 instruction 表示任务要求，input 表示具体输入内容，output 表示期望回答。对于聊天模型，通常使用 messages 格式，包含 system、user、assistant 等角色，用来表示多轮对话和角色设定。</p><p>如果模型需要学习工具调用，SFT 数据还可以包含 tool call 格式，比如 assistant 先生成工具名称和参数，tool 返回结果后，assistant 再基于工具结果回答用户。训练时通常只对 assistant 的输出部分计算 loss，system 和 user 内容作为上下文输入，会被 mask 掉，避免模型学习去生成用户问题。</p><p>简单说，SFT 数据就是“教模型在什么输入下应该给出什么回答”。高质量 SFT 数据要保证指令清晰、回答准确、格式稳定、任务覆盖广，并包含必要的安全拒答、多轮对话和工具调用样例。</p></blockquote>"}]},{"no":"","title":"7.2 PEFT","questions":[{"t":"Full Fine-tuning有什么问题？","tag":"","p":"","html":"<p>Full Fine-tuning 的核心问题是：<strong>效果可能好，但成本高、风险大、维护难，不适合很多大模型应用场景。</strong></p><ol><li>什么是 Full Fine-tuning？</li></ol><p>Full Fine-tuning 指的是微调时更新模型的全部参数。</p><p>比如一个 7B、13B、70B 模型，所有权重都参与训练。</p><p>这和 LoRA 这类参数高效微调不同。</p><p>LoRA 通常只训练少量新增参数。</p><ol><li>训练成本高</li></ol><p>Full Fine-tuning 要更新全部参数。</p><p>需要大量：</p><ul><li>GPU 显存</li><li>训练时间</li><li>计算资源</li><li>存储空间</li><li>优化器状态内存</li></ul><p>比如 AdamW 不只要存模型参数，还要存梯度、一阶矩、二阶矩。</p><p>所以显存压力非常大。</p><ol><li>存储成本高</li></ol><p>每微调一个任务，就可能得到一份完整模型权重。</p><p>如果基座模型很大，比如几十 GB 到几百 GB。</p><p>多个业务场景分别微调，就要保存多份完整模型。</p><p>这会带来很高存储和部署成本。</p><ol><li>容易过拟合</li></ol><p>很多业务微调数据量并不大。</p><p>Full Fine-tuning 更新全部参数，模型自由度太高。</p><p>如果数据少或质量一般，容易：</p><ul><li>记住训练样本</li><li>泛化变差</li><li>输出模式僵化</li><li>对训练集过拟合</li></ul><p>所以它对数据规模和质量要求较高。</p><ol><li>容易灾难性遗忘</li></ol><p>Full Fine-tuning 会改变原模型大量参数。</p><p>如果微调数据分布很窄，模型可能为了适应新任务，忘掉原来的一些通用能力。</p><p>这叫灾难性遗忘。</p><p>表现可能是：</p><ul><li>通用问答能力下降</li><li>推理能力下降</li><li>多语言能力下降</li><li>安全对齐变差</li><li>原本会的任务变差</li></ul><ol><li>部署和维护复杂</li></ol><p>Full Fine-tuning 后，每个模型都是一套完整权重。</p><p>如果有很多业务：</p><pre><code>客服模型<br/>代码模型<br/>法律模型<br/>金融模型</code></pre><p>就要分别管理、部署、监控和更新。</p><p>基座模型升级时，还可能要重新全量微调。</p><p>维护成本很高。</p><ol><li>安全和对齐风险</li></ol><p>Full Fine-tuning 可能破坏原模型的安全对齐。</p><p>比如原模型本来学会了拒绝危险请求。</p><p>但微调数据质量不好，可能导致：</p><ul><li>更容易越权回答</li><li>安全拒答能力下降</li><li>输出有害内容</li><li>幻觉增加</li><li>行为风格失控</li></ul><p>所以需要重新评估安全性。</p><ol><li>什么时候适合 Full Fine-tuning？</li></ol><p>它不是不能用。</p><p>适合场景包括：</p><ul><li>数据量足够大</li><li>任务和基座模型差异很大</li><li>对性能要求极高</li><li>有足够算力预算</li><li>需要深度改变模型能力</li><li>能承担部署和安全评估成本</li></ul><p>否则通常优先考虑 LoRA、Prefix Tuning、Adapter、RAG 或 Prompt Engineering。</p><ol><li><strong>面试背诵版：Full Fine-tuning 有什么问题？</strong></li></ol><blockquote><p>Full Fine-tuning 是指微调时更新模型的全部参数。它的优点是适配能力强，但问题也很明显。首先训练成本高，因为要存储和更新所有参数、梯度以及 AdamW 的一阶矩和二阶矩，显存和算力消耗非常大；其次存储和部署成本高，每个任务都会产生一份完整模型权重，多业务场景下维护成本很高。</p><p>另外，Full Fine-tuning 对数据质量和规模要求较高，如果业务数据较少，很容易过拟合；由于它会修改大量原模型参数，还可能导致灾难性遗忘，使模型原有的通用能力、推理能力、多语言能力或安全对齐能力下降。基座模型升级后，多个全量微调模型也可能需要重新训练和评估。</p><p>所以 Full Fine-tuning 更适合数据充足、算力充足、任务差异大且对效果要求极高的场景。一般业务场景中，更常优先考虑 LoRA、Adapter、Prefix Tuning、RAG 或 Prompt Engineering。</p></blockquote>"},{"t":"LoRA是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>LoRA 的核心是：<strong>冻结大模型原始参数，只训练少量低秩矩阵，用很低成本完成模型微调。</strong></p><ol><li>LoRA 解决什么问题？</li></ol><p>全量微调要更新模型全部参数。</p><p>问题是：</p><ul><li>显存占用大</li><li>训练成本高</li><li>存储多份模型很贵</li><li>容易过拟合</li><li>容易破坏原模型能力</li></ul><p>LoRA 就是为了解决大模型微调成本高的问题。</p><ol><li>LoRA 的基本思想</li></ol><p>LoRA 全称是 Low-Rank Adaptation。</p><p>它假设模型微调时，参数变化量不需要是满秩的大矩阵。</p><p>也就是说，不直接更新原始权重 W。</p><p>而是学习一个小的增量：</p><pre><code>W' = W + ΔW</code></pre><p>其中：</p><pre><code>ΔW = BA</code></pre><p>A 和 B 是两个低秩矩阵。</p><ol><li>为什么低秩能省参数？</li></ol><p>假设原始权重矩阵 W 是：</p><pre><code>d × k</code></pre><p>全量微调要更新：</p><pre><code>d × k 个参数</code></pre><p>LoRA 把增量拆成：</p><pre><code>A: r × k<br/>B: d × r</code></pre><p>其中 r 很小，比如 4、8、16。</p><p>训练参数量变成：</p><pre><code>r × k + d × r</code></pre><p>远小于：</p><pre><code>d × k</code></pre><p>所以显存和存储成本大幅降低。</p><ol><li>LoRA 怎么训练？</li></ol><p>训练时：</p><ul><li>原始模型参数 W 冻结</li><li>只训练 LoRA 的 A、B 矩阵</li><li>前向时把 LoRA 增量加到原输出上</li></ul><p>比如对线性层：</p><pre><code>output = Wx + BAx</code></pre><p>其中 W 不变，只更新 A 和 B。</p><ol><li>LoRA 通常加在哪里？</li></ol><p>LoRA 常加在 Transformer 的线性层上，比如：</p><ul><li>Q projection</li><li>K projection</li><li>V projection</li><li>O projection</li><li>FFN 的 up / down / gate projection</li></ul><p>实际最常见的是加在 Attention 的 Q、V 或 QKV 投影上。</p><ol><li>LoRA 的优点</li></ol><p>LoRA 优点：</p><ul><li>训练参数少</li><li>显存占用低</li><li>微调成本低</li><li>每个任务只保存小 adapter</li><li>可以快速切换不同任务</li><li>原模型参数不变</li><li>部署时可合并权重</li><li>降低灾难性遗忘风险</li></ul><ol><li>LoRA 的缺点</li></ol><p>LoRA 也有限制：</p><ul><li>表达能力不如全量微调</li><li>rank 设置不合适会影响效果</li><li>对任务差异特别大的场景可能不够</li><li>不同层加 LoRA 效果不同</li><li>多个 LoRA 合并和管理需要注意</li><li>仍然需要高质量微调数据</li></ul><ol><li><strong>面试背诵版：LoRA 是什么？</strong></li></ol><blockquote><p>LoRA，全称 Low-Rank Adaptation，是一种参数高效微调方法。它的核心思想是冻结大模型原始参数，不直接更新完整权重矩阵 W，而是为某些线性层增加一个可训练的低秩增量 ΔW。这个增量被分解成两个小矩阵 A 和 B，即 ΔW = BA，最终权重相当于 W' = W + ΔW。</p><p>因为低秩矩阵的 rank 很小，比如 4、8、16，所以 LoRA 需要训练的参数量远小于全量微调。训练时只更新 A、B 矩阵，原始模型权重保持不变；推理时可以把 LoRA 增量和原权重合并，也可以按任务动态加载不同 LoRA adapter。</p><p>LoRA 的优点是显存占用低、训练成本低、存储成本低，每个任务只需要保存很小的 adapter，同时降低灾难性遗忘风险。它常用于 Transformer 的 Q、K、V、O 投影层或 FFN 线性层。简单说，LoRA 是“冻结大模型，只训练少量低秩增量参数”的微调方法。</p></blockquote>"},{"t":"LoRA为什么有效？","tag":"","p":"","html":"<ol><li>LoRA 为什么有效？</li></ol><p>LoRA 有效的核心原因是：大模型微调时，并不一定需要更新全部参数，任务适配所需的参数变化往往可以用低秩增量来近似。</p><ol><li>大模型已有很强基础能力</li></ol><p>预训练大模型已经学到了大量：</p><ul><li>语言规律</li><li>世界知识</li><li>推理模式</li><li>代码能力</li><li>对话能力</li><li>通用任务模式</li></ul><p>很多微调任务不是从零学习，而是在已有能力上做适配。</p><p>比如：</p><ul><li>调整回答风格</li><li>学习输出格式</li><li>适配领域术语</li><li>学会某类任务模式</li></ul><p>所以不需要大幅修改所有参数。</p><ol><li>微调变化可能是低秩的</li></ol><p>LoRA 的假设是：</p><pre><code>模型适配新任务时，权重变化 ΔW 具有低秩特性</code></pre><p>也就是说，虽然原始权重矩阵很大，但真正需要改变的方向并不多。</p><p>所以可以用两个小矩阵近似：</p><pre><code>ΔW = BA</code></pre><p>其中 rank 很小。</p><p>这就能用很少参数表达关键变化。</p><ol><li>冻结原模型，避免破坏通用能力</li></ol><p>LoRA 不更新原始权重 W。</p><p>它只学习额外增量：</p><pre><code>W' = W + ΔW</code></pre><p>这样可以保留基座模型原有能力。</p><p>相比全量微调，LoRA 更不容易：</p><ul><li>灾难性遗忘</li><li>破坏通用能力</li><li>破坏安全对齐</li><li>过拟合小数据</li></ul><ol><li>参数少，优化更容易</li></ol><p>LoRA 只训练少量参数。</p><p>好处是：</p><ul><li>显存占用低</li><li>优化空间小</li><li>训练更稳定</li><li>小数据场景更不容易过拟合</li><li>训练成本低</li></ul><p>相比更新几十亿参数，训练几百万或几千万 LoRA 参数更容易。</p><ol><li>加在关键线性层上效果好</li></ol><p>Transformer 中很多能力集中在线性投影中。</p><p>比如：</p><ul><li>Q、K、V 投影</li><li>O 投影</li><li>FFN 的 up/down/gate projection</li></ul><p>LoRA 加在这些层上，可以有效影响：</p><ul><li>Attention 关注方式</li><li>特征变换</li><li>输出风格</li><li>任务行为</li></ul><p>所以少量参数也能产生明显效果。</p><ol><li>保持推理友好</li></ol><p>LoRA 学到的是权重增量。</p><p>推理时可以把它合并到原权重：</p><pre><code>W_merge = W + BA</code></pre><p>这样部署时几乎不增加额外延迟。</p><p>这让 LoRA 不只是训练省，也方便上线。</p><ol><li>多任务适配方便</li></ol><p>同一个基座模型可以挂多个 LoRA。</p><p>比如：</p><pre><code>客服 LoRA<br/>代码 LoRA<br/>法律 LoRA<br/>金融 LoRA</code></pre><p>每个 LoRA 很小，可以按任务动态加载。</p><p>这比保存多份完整模型更省。</p><ol><li><strong>面试背诵版：LoRA 为什么有效？</strong></li></ol><blockquote><p>LoRA 有效，主要是因为预训练大模型本身已经具备很强的通用语言、知识、推理和任务能力，很多下游微调并不需要从零学习，只是在原有能力上调整风格、格式、领域表达或任务行为。因此，任务适配所需的参数变化不一定是一个完整的大矩阵，而可以近似为低秩变化。</p><p>LoRA 的假设是微调时权重增量 ΔW 具有低秩特性，所以不用更新原始权重 W，而是学习两个小矩阵 A 和 B，使 ΔW = BA。这样用很少参数就能表达关键的任务适配方向。由于原模型参数被冻结，LoRA 能保留基座模型的通用能力，减少灾难性遗忘和小数据过拟合风险。</p><p>同时，LoRA 通常加在 Attention 的 Q、K、V、O 投影或 FFN 线性层上，这些位置对模型行为影响很大，所以少量低秩参数也能产生明显效果。推理时 LoRA 还可以合并回原权重，几乎不增加延迟。简单说，LoRA 有效是因为“大模型已有能力足够强，下游任务只需要学习少量低秩增量”。</p></blockquote>"},{"t":"LoRA低秩矩阵是什么意思？","tag":"","p":"","html":"<p>LoRA 低秩矩阵的核心意思是：不用直接训练一个完整的大矩阵，而是用两个小矩阵相乘来近似这个大矩阵的参数变化。</p><ol><li>什么是秩？</li></ol><p>矩阵的秩可以简单理解为：</p><pre><code>这个矩阵里有多少个独立的信息方向</code></pre><p>秩越高，表达能力越强，但参数也更多。</p><p>秩越低，说明矩阵信息可以用更少的独立方向表示。</p><ol><li>LoRA 里的原始权重</li></ol><p>神经网络里有很多线性层。</p><p>比如一个权重矩阵：</p><pre><code>W ∈ R^(d × k)</code></pre><p>全量微调时，会直接更新整个 W。</p><p>参数量是：</p><pre><code>d × k</code></pre><p>如果 d 和 k 都很大，参数量非常多。</p><ol><li>LoRA 不直接更新 W</li></ol><p>LoRA 冻结原始权重 W。</p><p>只学习一个增量：</p><pre><code>W' = W + ΔW</code></pre><p>但 ΔW 也可能很大。</p><p>所以 LoRA 不直接训练完整 ΔW，而是把它分解成：</p><pre><code>ΔW = B A</code></pre><p>其中：</p><pre><code>A ∈ R^(r × k)<br/>B ∈ R^(d × r)</code></pre><p>r 就是 rank，通常很小，比如 4、8、16。</p><ol><li>为什么叫低秩？</li></ol><p>因为：</p><pre><code>ΔW = BA</code></pre><p>这个矩阵的秩最多是 r。</p><p>如果 r 远小于 d 和 k，那么 ΔW 就是低秩矩阵。</p><p>比如：</p><pre><code>d = 4096<br/>k = 4096<br/>r = 8</code></pre><p>完整 ΔW 参数量是：</p><pre><code>4096 × 4096 ≈ 1677 万</code></pre><p>LoRA 参数量是：</p><pre><code>4096 × 8 + 8 × 4096 = 65536</code></pre><p>参数量大幅减少。</p><ol><li>直观理解</li></ol><p>可以把 full fine-tuning 理解成：</p><pre><code>允许模型在所有方向上改参数</code></pre><p>LoRA 低秩适配是：</p><pre><code>只允许模型沿少数几个关键方向改参数</code></pre><p>如果下游任务只需要少量关键变化，这就足够了。</p><ol><li>为什么低秩可行？</li></ol><p>因为大模型已经有很强能力。</p><p>下游任务通常只是做适配，比如：</p><ul><li>输出格式</li><li>领域风格</li><li>特定任务模式</li><li>轻微行为调整</li></ul><p>这些变化不一定需要修改所有参数方向。</p><p>少量低秩方向就能表达主要变化。</p><ol><li>rank 影响什么？</li></ol><p>rank r 越大：</p><ul><li>可训练参数越多</li><li>表达能力越强</li><li>显存和存储成本越高</li><li>过拟合风险可能增加</li></ul><p>rank r 越小：</p><ul><li>参数更少</li><li>成本更低</li><li>但表达能力可能不足</li></ul><p>所以 rank 要根据任务复杂度选择。</p><ol><li><strong>面试背诵版：LoRA 低秩矩阵是什么意思？</strong></li></ol><blockquote><p>LoRA 里的低秩矩阵，指的是不直接训练完整的权重增量 ΔW，而是把这个增量分解成两个小矩阵相乘，即 ΔW = BA。假设原始权重 W 的维度是 d × k，全量微调需要更新 d × k 个参数；而 LoRA 只训练 A 和 B，其中 A 的维度是 r × k，B 的维度是 d × r，r 是一个很小的 rank，比如 4、8、16。</p><p>因为 BA 的秩最多是 r，当 r 远小于 d 和 k 时，ΔW 就是一个低秩矩阵。这样可以用远少于完整矩阵的参数，近似表示模型在下游任务上的关键变化方向。直观理解就是，全量微调允许模型在所有参数方向上变化，而 LoRA 只让模型沿少数几个重要方向做调整。</p><p>低秩可行的原因是大模型已经具备很强通用能力，很多下游任务只需要适配风格、格式、领域表达或任务模式，不一定需要修改全部参数。rank 越大表达能力越强但成本越高，rank 越小成本低但可能不够用。简单说，LoRA 低秩矩阵就是“用两个小矩阵近似一个大的参数更新矩阵”。</p></blockquote>"},{"t":"LoRA训练哪些参数？","tag":"","p":"","html":"<ol><li>LoRA 训练哪些参数？</li></ol><p>LoRA 训练的核心是：冻结原始大模型参数，只训练插入到部分线性层上的低秩矩阵参数。</p><ol><li>原模型参数是否训练？</li></ol><p>通常不训练。</p><p>LoRA 会冻结基座模型的原始参数。</p><p>也就是：</p><pre><code>W 不更新</code></pre><p>训练时只学习一个增量：</p><pre><code>W' = W + ΔW<br/>ΔW = BA</code></pre><p>其中 A、B 才是 LoRA 要训练的参数。</p><ol><li>LoRA 训练 A 和 B 矩阵</li></ol><p>对于一个线性层权重 W，LoRA 会增加两个小矩阵：</p><pre><code>A：降维矩阵<br/>B：升维矩阵</code></pre><p>训练时更新：</p><pre><code>A 和 B</code></pre><p>不更新原来的 W。</p><p>所以 LoRA 的可训练参数量很少。</p><ol><li>LoRA 常加在哪些层？</li></ol><p>LoRA 通常加在 Transformer 的线性投影层。</p><p>最常见的是 Attention 里的：</p><ul><li>q_proj</li><li>k_proj</li><li>v_proj</li><li>o_proj</li></ul><p>也可以加在 FFN 里的：</p><ul><li>gate_proj</li><li>up_proj</li><li>down_proj</li></ul><p>不同框架和模型命名可能略有不同。</p><ol><li>最常见训练哪些？</li></ol><p>很多实践中常训练：</p><pre><code>q_proj 和 v_proj</code></pre><p>或者：</p><pre><code>q_proj、k_proj、v_proj、o_proj</code></pre><p>如果任务更复杂，也可能把 FFN 相关投影也加上 LoRA。</p><p>训练更多层，表达能力更强，但参数和显存成本也更高。</p><ol><li>还可能训练哪些额外参数？</li></ol><p>有些设置里，除了 LoRA A/B，还可能训练：</p><ul><li>lm_head</li><li>embedding</li><li>LayerNorm</li><li>bias</li><li>特殊任务头</li><li>新增 token embedding</li></ul><p>但标准 LoRA 通常只训练 LoRA adapter 参数。</p><p>是否训练这些额外参数，要看任务需求。</p><ol><li>QLoRA 训练哪些？</li></ol><p>QLoRA 中基座模型是 4-bit 量化并冻结的。</p><p>训练时仍然主要更新：</p><pre><code>LoRA A/B 参数</code></pre><p>不是更新量化后的原模型权重。</p><ol><li>为什么只训练这些也有效？</li></ol><p>因为基座模型已经学到通用能力。</p><p>LoRA 只需要学习任务适配增量。</p><p>把 LoRA 加在 QKV、O 或 FFN 投影层，可以影响：</p><ul><li>注意力模式</li><li>信息聚合</li><li>特征变换</li><li>输出风格</li><li>任务行为</li></ul><p>所以少量参数也能起作用。</p><ol><li><strong>面试背诵版：LoRA 训练哪些参数？</strong></li></ol><blockquote><p>LoRA 训练时通常会冻结大模型的原始参数，只训练插入到部分线性层上的低秩矩阵参数。对于原始权重 W，LoRA 不直接更新 W，而是学习一个低秩增量 ΔW = BA，其中 A 和 B 是可训练矩阵，rank 通常很小，所以训练参数量远小于全量微调。</p><p>LoRA 常加在 Transformer 的线性投影层上，最常见的是 Attention 里的 q_proj、k_proj、v_proj、o_proj，也可以加在 FFN 的 gate_proj、up_proj、down_proj 上。实际中常见配置是只训练 q_proj 和 v_proj，或者训练整个 Q、K、V、O 投影；任务更复杂时可以加入 FFN 层。</p><p>标准 LoRA 一般只训练 LoRA adapter 参数，不训练原模型权重。某些任务也可能额外训练 embedding、lm_head、bias、LayerNorm 或任务头，但这不是标准 LoRA 的必须部分。简单说，LoRA 训练的是“新增的低秩 A/B 矩阵”，原模型参数通常保持冻结。</p></blockquote>"},{"t":"LoRA rank如何选择？","tag":"","p":"","html":"<ol><li>LoRA rank 如何选择？</li></ol><p>LoRA rank 的选择核心是：rank 越大表达能力越强，但参数、显存和过拟合风险也越高；rank 越小成本低，但可能适配能力不足。</p><ol><li>rank 是什么？</li></ol><p>LoRA 会把权重增量写成：</p><pre><code>ΔW = BA</code></pre><p>其中：</p><pre><code>A ∈ R^(r × k)<br/>B ∈ R^(d × r)</code></pre><p>这里的 r 就是 rank。</p><p>它决定 LoRA 增量矩阵最多能表达多少个独立方向。</p><ol><li>rank 越大有什么影响？</li></ol><p>rank 越大：</p><ul><li>可训练参数更多</li><li>表达能力更强</li><li>能适配更复杂任务</li><li>显存占用更高</li><li>训练成本更高</li><li>过拟合风险更高</li></ul><p>适合复杂任务、数据量较多、领域差异大的场景。</p><ol><li>rank 越小有什么影响？</li></ol><p>rank 越小：</p><ul><li>参数少</li><li>训练快</li><li>显存低</li><li>存储小</li><li>不容易过拟合</li><li>但表达能力有限</li></ul><p>如果任务比较简单，比如格式微调、风格适配、小分类任务，小 rank 往往够用。</p><ol><li>常见 rank 取值</li></ol><p>常见选择：</p><pre><code>r = 4, 8, 16, 32, 64</code></pre><p>一般经验：</p><ul><li>简单任务：4 或 8</li><li>普通指令微调：8 或 16</li><li>复杂领域任务：16 或 32</li><li>任务差异大、数据多：32 或 64</li></ul><p>但没有绝对答案，要看验证集效果。</p><ol><li>和数据量有关</li></ol><p>数据少时，不建议 rank 太大。</p><p>因为参数多了容易过拟合。</p><p>数据多、任务复杂时，可以适当提高 rank。</p><p>可以简单记：</p><pre><code>数据少 → 小 rank<br/>数据多 → 可尝试大 rank<br/>任务简单 → 小 rank<br/>任务复杂 → 大 rank</code></pre><ol><li>和目标模块有关</li></ol><p>如果 LoRA 只加在少数层，比如只加 q_proj、v_proj，可以适当提高 rank。</p><p>如果 LoRA 加在很多模块，比如 QKV、O、FFN 都加，rank 可以适当小一些。</p><p>因为总可训练参数已经不少。</p><ol><li>如何实际选择？</li></ol><p>实践中常用方法：</p><pre><code>先从 r=8 或 r=16 开始<br/>看验证集效果<br/>如果欠拟合，提高 rank<br/>如果过拟合或成本高，降低 rank</code></pre><p>同时还要调：</p><ul><li>learning rate</li><li>alpha</li><li>dropout</li><li>target modules</li><li>训练轮数</li></ul><p>rank 不是唯一影响因素。</p><ol><li><strong>面试背诵版：LoRA rank 如何选择？</strong></li></ol><blockquote><p>LoRA 的 rank 决定低秩增量 ΔW = BA 的表达能力。rank 越大，可训练参数越多，适配能力越强，适合复杂任务、领域差异大或数据量较多的场景；但同时显存、训练成本和过拟合风险也会增加。rank 越小，训练更快、显存更低、存储更小，但如果任务复杂，可能表达能力不足。</p><p>实践中常见 rank 取值是 4、8、16、32、64。简单任务或数据较少时可以选 4 或 8；普通指令微调常从 8 或 16 开始；复杂领域任务或数据量较大时可以尝试 16、32 甚至 64。是否加在 Q、K、V、O 还是 FFN 层也会影响 rank 选择，加的模块越多，rank 可以相对小一些。</p><p>简单说，LoRA rank 要在效果和成本之间权衡。一般可以先用 r=8 或 r=16 做 baseline，根据验证集表现调整：欠拟合就提高 rank，过拟合或资源压力大就降低 rank。</p></blockquote>"},{"t":"QLoRA是什么？","tag":"","p":"","html":"<p>QLoRA 的核心是：把基座大模型量化到低比特冻结起来，再在上面训练 LoRA，从而进一步降低显存成本。</p><ol><li>先理解 LoRA</li></ol><p>LoRA 是参数高效微调。</p><p>它做的是：</p><pre><code>冻结原始模型参数<br/>只训练少量低秩矩阵</code></pre><p>比如：</p><pre><code>W' = W + ΔW<br/>ΔW = BA</code></pre><p>这样不需要更新全部模型参数，只训练很小一部分 adapter。</p><ol><li>QLoRA 是什么？</li></ol><p>QLoRA 可以理解成：</p><pre><code>Quantization + LoRA</code></pre><p>也就是：</p><ul><li>基座模型用低比特量化存储</li><li>原始模型参数冻结</li><li>在量化模型上插入 LoRA</li><li>训练时只更新 LoRA 参数</li></ul><p>比如把 16-bit 模型量化成 4-bit，大幅减少显存占用。</p><ol><li>为什么需要 QLoRA？</li></ol><p>普通 LoRA 虽然只训练少量参数，但基座模型本身仍然要加载到显存里。</p><p>如果模型很大，比如 33B、65B，显存仍然很高。</p><p>QLoRA 通过量化基座模型，让大模型也能在较少 GPU 资源上微调。</p><ol><li>QLoRA 的关键技术</li></ol><p>QLoRA 常见关键点包括：</p><ul><li>4-bit NormalFloat，NF4</li><li>Double Quantization</li><li>Paged Optimizer</li><li>LoRA Adapter</li></ul><p>NF4 是适合正态分布权重的 4-bit 量化格式。</p><p>Double Quantization 进一步压缩量化常数。</p><p>Paged Optimizer 用来减少显存峰值，避免 OOM。</p><ol><li>QLoRA 训练时怎么计算？</li></ol><p>虽然基座模型以 4-bit 存储，但计算时通常会临时反量化到更高精度参与前向和反向。</p><p>不过基座参数不更新。</p><p>只有 LoRA 参数会训练。</p><p>所以显存主要省在：</p><ul><li>模型权重存储</li><li>优化器状态</li><li>梯度存储</li></ul><ol><li>QLoRA 的优点</li></ol><p>优点包括：</p><ul><li>显存占用更低</li><li>可以微调更大模型</li><li>成本比全量微调低很多</li><li>效果通常接近 LoRA 或全量微调</li><li>每个任务仍然只保存小 LoRA adapter</li><li>适合资源有限场景</li></ul><ol><li>QLoRA 的缺点</li></ol><p>缺点包括：</p><ul><li>量化可能带来精度损失</li><li>训练速度可能受反量化影响</li><li>工程实现更复杂</li><li>对量化格式和算子支持有要求</li><li>极端高精度任务可能不如全精度微调</li><li>需要合理设置 LoRA rank、学习率等参数</li></ul><ol><li><strong>面试背诵版：QLoRA 是什么？</strong></li></ol><blockquote><p>QLoRA 是 Quantization + LoRA 的结合，是一种更省显存的参数高效微调方法。它会先把基座大模型量化到低比特，比如 4-bit，并冻结这些量化后的基座参数，然后在模型的部分线性层上插入 LoRA adapter，训练时只更新 LoRA 的低秩矩阵参数。</p><p>它相比普通 LoRA 的区别是，LoRA 虽然只训练少量 adapter 参数，但仍然需要以较高精度加载完整基座模型；QLoRA 则通过 4-bit 量化显著降低基座模型的显存占用，使得在较少 GPU 资源下微调更大的模型成为可能。QLoRA 中常见技术包括 NF4 量化、Double Quantization 和 Paged Optimizer。</p><p>简单说，LoRA 是“冻结大模型，只训练低秩增量”，QLoRA 是“先把冻结的大模型量化得更小，再训练 LoRA”。它的优点是显存低、成本低、能微调更大模型，但也可能带来量化精度损失和工程复杂度。</p></blockquote>"},{"t":"LoRA和Adapter有什么区别？","tag":"","p":"","html":"<p></p><blockquote><p></p></blockquote><hr/>"}]},{"no":"","title":"8.1 强化学习基础","questions":[{"t":"什么是强化学习？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>强化学习的核心是：<strong>智能体通过和环境交互，根据奖励反馈不断调整策略，让长期累计奖励最大。</strong></p><ol><li>强化学习在做什么？</li></ol><p>强化学习不是直接给标准答案。</p><p>它是让 Agent 在环境里尝试行动。</p><p>流程是：</p><pre><code>Agent 观察状态<br/>  ↓<br/>选择动作<br/>  ↓<br/>环境反馈奖励<br/>  ↓<br/>Agent 更新策略<br/>  ↓<br/>继续尝试</code></pre><p>目标是让 Agent 学会在不同状态下做什么动作最有利。</p><ol><li>强化学习的核心元素</li></ol><p>强化学习通常有几个概念：</p><ul><li>Agent：智能体，负责决策</li><li>Environment：环境，智能体交互的对象</li><li>State：状态，当前环境情况</li><li>Action：动作，智能体做出的选择</li><li>Reward：奖励，环境对动作的反馈</li><li>Policy：策略，状态到动作的映射</li><li>Value：价值，某个状态或动作长期看有多好</li></ul><p>比如下棋：</p><ul><li>Agent：棋手 AI</li><li>State：当前棋盘</li><li>Action：下一步棋</li><li>Reward：赢棋奖励高，输棋奖励低</li><li>Policy：当前局面下该怎么走</li></ul><ol><li>和监督学习区别</li></ol><p>监督学习有标准答案。</p><p>比如：</p><pre><code>这张图是猫</code></pre><p>模型直接学输入到标签的映射。</p><p>强化学习没有每一步的标准答案。</p><p>它只有奖励信号。</p><p>比如游戏里：</p><pre><code>当前这一步不一定马上知道好坏<br/>最后赢了才知道整体策略不错</code></pre><ol><li>强化学习关注长期收益</li></ol><p>强化学习不只看当前奖励。</p><p>它关注长期累计奖励。</p><p>比如游戏中某一步可能短期吃亏，但长期能赢。</p><p>所以 Agent 要学会：</p><pre><code>不是当前最爽，而是长期最优</code></pre><p>这就是强化学习难的地方。</p><ol><li>探索和利用</li></ol><p>强化学习有一个经典问题：</p><pre><code>Exploration vs Exploitation</code></pre><p>探索是尝试新动作，发现更好策略。</p><p>利用是选择当前认为最好的动作。</p><p>如果只利用，可能错过更优策略。</p><p>如果只探索，效率低。</p><ol><li>常见强化学习算法</li></ol><p>常见算法包括：</p><ul><li>Q-Learning</li><li>SARSA</li><li>DQN</li><li>Policy Gradient</li><li>Actor-Critic</li><li>PPO</li><li>DDPG</li><li>A3C</li></ul><p>大模型对齐里常提到 PPO。</p><ol><li>强化学习应用场景</li></ol><p>强化学习常用于：</p><ul><li>游戏 AI</li><li>机器人控制</li><li>自动驾驶</li><li>推荐系统</li><li>广告投放</li><li>资源调度</li><li>策略优化</li><li>大模型 RLHF</li></ul><p>比如 RLHF 中，模型根据人类偏好奖励来优化回答策略。</p><ol><li><strong>面试背诵版：什么是强化学习？</strong></li></ol><blockquote><p>强化学习是一种让智能体通过和环境交互来学习决策策略的机器学习方法。智能体在某个状态下选择动作，环境根据这个动作返回新的状态和奖励，智能体根据奖励不断调整策略，目标是最大化长期累计奖励。</p><p>它的核心概念包括 Agent、Environment、State、Action、Reward、Policy 和 Value。和监督学习不同，强化学习没有每一步的标准答案，而是通过奖励信号判断行为好坏，并且很多时候奖励是延迟的，所以它关注的是长期收益而不是当前一步的收益。</p><p>简单说，监督学习是“给答案让模型学”，强化学习是“让模型试错，根据奖励学策略”。它常用于游戏 AI、机器人控制、推荐系统、自动驾驶和大模型 RLHF 等场景。</p></blockquote>"},{"t":"强化学习和监督学习有什么区别？","tag":"","p":"","html":"<ol><li>强化学习和监督学习有什么区别？</li></ol><p>强化学习和监督学习的核心区别是：监督学习是“有标准答案学预测”，强化学习是“没有标准答案，靠奖励学策略”。</p><ol><li>训练数据不同</li></ol><p>监督学习的数据一般是：</p><pre><code>输入 X + 标签 Y</code></pre><p>比如：</p><pre><code>图片 → 猫<br/>文本 → 正面情感<br/>房屋信息 → 房价</code></pre><p>强化学习的数据来自 Agent 和环境交互：</p><pre><code>状态 State → 动作 Action → 奖励 Reward → 新状态</code></pre><p>比如下棋 AI：</p><pre><code>当前棋盘 → 走一步棋 → 最后赢/输得到奖励</code></pre><ol><li>反馈信号不同</li></ol><p>监督学习有明确标签。</p><p>模型预测错了，可以直接和标准答案比较。</p><p>比如：</p><pre><code>真实标签：猫<br/>预测：狗</code></pre><p>马上知道错了。</p><p>强化学习没有每一步标准答案，只有奖励信号。</p><p>而且奖励可能延迟。</p><p>比如下棋时，某一步是否正确，可能要到最终输赢才知道。</p><ol><li>学习目标不同</li></ol><p>监督学习的目标是：</p><pre><code>预测结果尽量接近标签</code></pre><p>强化学习的目标是：</p><pre><code>长期累计奖励最大</code></pre><p>所以强化学习不只关注当前动作好不好，还要看它对未来的影响。</p><p>有些动作短期奖励低，但长期收益高。</p><ol><li>学习方式不同</li></ol><p>监督学习更像：</p><pre><code>看标准答案，模仿学习</code></pre><p>强化学习更像：</p><pre><code>不断试错，从奖励中总结经验</code></pre><p>监督学习通常训练在固定数据集上。</p><p>强化学习的训练数据会随着策略变化而变化。</p><p>Agent 做出不同动作，就会看到不同环境反馈。</p><ol><li>是否需要探索</li></ol><p>监督学习通常不需要探索。</p><p>因为数据集已经给定。</p><p>强化学习必须处理探索和利用问题：</p><pre><code>探索：尝试新动作<br/>利用：选择当前认为最好的动作</code></pre><p>如果只利用，可能错过更好的策略。</p><p>如果只探索，学习效率低。</p><ol><li>应用场景不同</li></ol><p>监督学习适合：</p><ul><li>分类</li><li>回归</li><li>图像识别</li><li>情感分析</li><li>语音识别</li><li>风险预测</li></ul><p>强化学习适合：</p><ul><li>游戏 AI</li><li>机器人控制</li><li>自动驾驶决策</li><li>推荐策略优化</li><li>资源调度</li><li>大模型 RLHF</li><li>多步决策任务</li></ul><ol><li>和大模型的关系</li></ol><p>大模型训练里两者都有。</p><p>SFT 是监督学习：</p><pre><code>用户指令 → 标准回答</code></pre><p>RLHF 是强化学习：</p><pre><code>模型生成回答 → 奖励模型打分 → 根据奖励优化策略</code></pre><p>所以：</p><pre><code>SFT 教模型模仿好答案<br/>RLHF 教模型更符合人类偏好</code></pre><ol><li><strong>面试背诵版：强化学习和监督学习有什么区别？</strong></li></ol><blockquote><p>强化学习和监督学习最大的区别在于训练信号不同。监督学习使用带标签数据，每个输入都有明确标准答案，模型通过预测结果和标签之间的误差来更新参数，目标是让预测更准确。比如图像分类、文本分类、房价预测都属于监督学习。</p><p>强化学习没有每一步的标准答案，而是让 Agent 和环境交互。Agent 在某个状态下选择动作，环境返回奖励和新状态，Agent 根据奖励调整策略，目标是最大化长期累计奖励。强化学习的奖励可能是延迟的，比如下棋时某一步好不好，可能要到最终输赢才知道。</p><p>简单说，监督学习是“给答案学预测”，强化学习是“靠试错和奖励学策略”。在大模型里，SFT 更接近监督学习，用标准回答教模型模仿；RLHF 更接近强化学习，用人类偏好或奖励模型指导模型生成更符合偏好的回答。</p></blockquote>"},{"t":"MDP是什么？","tag":"","p":"","html":"<ol><li>MDP 是什么？</li></ol><p>MDP 的核心是：用数学方式描述强化学习中的决策过程，也就是智能体在不同状态下选择动作，并根据奖励和状态转移学习最优策略。</p><ol><li>MDP 全称是什么？</li></ol><p>MDP 全称是 Markov Decision Process。</p><p>中文叫马尔可夫决策过程。</p><p>它是强化学习最常用的建模框架。</p><p>用来描述：</p><pre><code>状态 → 动作 → 奖励 → 新状态</code></pre><p>这个循环。</p><ol><li>MDP 包含哪些元素？</li></ol><p>MDP 通常由五个部分组成：</p><pre><code>(S, A, P, R, γ)</code></pre><p>分别是：</p><ul><li>S：State，状态集合</li><li>A：Action，动作集合</li><li>P：Transition Probability，状态转移概率</li><li>R：Reward，奖励函数</li><li>γ：Discount Factor，折扣因子</li></ul><p>这五个元素定义了一个决策问题。</p><ol><li>State 是什么？</li></ol><p>State 表示当前环境情况。</p><p>比如下棋：</p><pre><code>当前棋盘局面</code></pre><p>自动驾驶：</p><pre><code>车速、位置、周围车辆、红绿灯状态</code></pre><p>大模型 RLHF：</p><pre><code>当前 prompt 和已生成内容</code></pre><ol><li>Action 是什么？</li></ol><p>Action 是 Agent 可以做的动作。</p><p>比如：</p><ul><li>下棋：落子</li><li>游戏：向左、向右、攻击</li><li>机器人：移动关节</li><li>推荐系统：推荐哪个商品</li><li>大模型：生成下一个 token 或一段回答</li></ul><p>Agent 根据当前状态选择动作。</p><ol><li>P 是什么？</li></ol><p>P 是状态转移概率。</p><p>表示：</p><pre><code>在状态 s 下执行动作 a，会以多大概率转移到状态 s'</code></pre><p>比如机器人往前走，可能成功到达新位置，也可能因为环境噪声偏移。</p><ol><li>R 是什么？</li></ol><p>R 是奖励函数。</p><p>表示 Agent 做了动作后得到多少奖励。</p><p>比如：</p><ul><li>赢棋：+1</li><li>输棋：-1</li><li>撞车：-100</li><li>回答被人类喜欢：高奖励</li><li>生成有害内容：低奖励</li></ul><p>强化学习的目标就是最大化长期奖励。</p><ol><li>γ 是什么？</li></ol><p>γ 是折扣因子。</p><p>它控制未来奖励的重要程度。</p><p>取值通常在 0 到 1 之间。</p><p>如果 γ 接近 0：</p><pre><code>更重视眼前奖励</code></pre><p>如果 γ 接近 1：</p><pre><code>更重视长期奖励</code></pre><ol><li>马尔可夫性是什么？</li></ol><p>MDP 里的 Markov 指马尔可夫性。</p><p>意思是：</p><pre><code>未来只依赖当前状态，而不依赖更早历史</code></pre><p>公式上是：</p><pre><code>P(s' | s, a, history) = P(s' | s, a)</code></pre><p>也就是说，只要当前状态 s 足够完整，就不需要看过去所有历史。</p><ol><li>MDP 的目标</li></ol><p>MDP 的目标是找到一个最优策略：</p><pre><code>π(a | s)</code></pre><p>也就是在每个状态下选择什么动作。</p><p>目标是最大化期望累计奖励：</p><pre><code>最大化长期收益</code></pre><ol><li><strong>面试背诵版：MDP 是什么？</strong></li></ol><blockquote><p>MDP，全称 Markov Decision Process，马尔可夫决策过程，是强化学习中描述序列决策问题的数学框架。它用来建模 Agent 和环境交互的过程：Agent 在当前状态下选择动作，环境返回奖励，并转移到下一个状态。</p><p>一个 MDP 通常由五个元素组成：S、A、P、R、γ。S 表示状态集合，A 表示动作集合，P 表示状态转移概率，也就是在状态 s 执行动作 a 后转移到新状态 s' 的概率；R 是奖励函数，表示执行动作后获得的奖励；γ 是折扣因子，用来衡量未来奖励的重要程度。</p><p>MDP 的核心假设是马尔可夫性，也就是未来只依赖当前状态和动作，而不依赖更早历史。强化学习的目标是在 MDP 中学习一个最优策略 π(a|s)，让 Agent 在长期交互中获得最大的期望累计奖励。简单说，MDP 就是“用状态、动作、奖励和转移概率描述决策过程”。</p></blockquote>"},{"t":"State、Action、Reward分别是什么？","tag":"","p":"","html":"<ol><li>State、Action、Reward 分别是什么？</li></ol><p>State、Action、Reward 是强化学习里最核心的三个概念：State 表示当前情况，Action 表示智能体做什么，Reward 表示这个动作好不好。</p><ol><li>State 是什么？</li></ol><p>State 是状态。</p><p>它表示 Agent 当前观察到的环境信息。</p><p>比如下棋里：</p><pre><code>当前棋盘局面</code></pre><p>自动驾驶里：</p><pre><code>车速、位置、车道线、周围车辆、红绿灯状态</code></pre><p>推荐系统里：</p><pre><code>用户画像、历史点击、当前页面、上下文信息</code></pre><p>大模型 RLHF 里：</p><pre><code>当前 prompt 和已经生成的文本</code></pre><p>State 是 Agent 做决策的依据。</p><ol><li>Action 是什么？</li></ol><p>Action 是动作。</p><p>它表示 Agent 在某个状态下可以做出的选择。</p><p>比如下棋里：</p><pre><code>下一步落子</code></pre><p>游戏里：</p><pre><code>左移、右移、跳跃、攻击</code></pre><p>推荐系统里：</p><pre><code>推荐哪个商品或内容</code></pre><p>大模型里：</p><pre><code>生成下一个 token</code></pre><p>或者更粗粒度地看：</p><pre><code>生成一个完整回答</code></pre><ol><li>Reward 是什么？</li></ol><p>Reward 是奖励。</p><p>它是环境对 Agent 动作的反馈。</p><p>表示这个动作带来的结果好不好。</p><p>比如：</p><ul><li>赢棋：+1</li><li>输棋：-1</li><li>撞车：-100</li><li>用户点击推荐：+1</li><li>用户投诉：-10</li><li>回答被人类喜欢：高奖励</li><li>有害回答：低奖励</li></ul><p>Reward 是 Agent 学习策略的依据。</p><ol><li>三者如何形成循环？</li></ol><p>强化学习的基本循环是：</p><pre><code>Agent 观察 State<br/>  ↓<br/>选择 Action<br/>  ↓<br/>环境返回 Reward 和新 State<br/>  ↓<br/>Agent 更新策略</code></pre><p>也就是：</p><pre><code>State → Action → Reward → New State</code></pre><ol><li>举个下棋例子</li></ol><p>下棋场景：</p><ul><li>State：当前棋盘</li><li>Action：下一步落子</li><li>Reward：赢棋 +1，输棋 -1，中间可能 0</li></ul><p>Agent 通过很多局对弈，学习什么局面下应该走哪一步。</p><ol><li>举个大模型 RLHF 例子</li></ol><p>RLHF 场景：</p><ul><li>State：用户 prompt + 当前已生成内容</li><li>Action：生成下一个 token 或完整回答</li><li>Reward：奖励模型根据回答质量打分</li></ul><p>如果回答更有帮助、更安全、更符合偏好，reward 就高。</p><p>如果回答有害、胡编、答非所问，reward 就低。</p><ol><li>Reward 不一定立即出现</li></ol><p>很多强化学习任务中，reward 是延迟的。</p><p>比如：</p><pre><code>下棋中某一步的好坏，可能到最后输赢才知道</code></pre><p>所以强化学习关注的是长期累计奖励，而不是只看当前一步。</p><ol><li><strong>面试背诵版：State、Action、Reward 分别是什么？</strong></li></ol><blockquote><p>State、Action、Reward 是强化学习中的三个核心概念。State 表示当前环境状态，也就是 Agent 做决策时能观察到的信息，比如下棋中的棋盘局面、自动驾驶中的车速和周围车辆、大模型 RLHF 中的 prompt 和已生成内容。Action 表示 Agent 在当前状态下可以执行的动作，比如下棋落子、游戏移动、推荐某个商品，或者大模型生成下一个 token 或完整回答。</p><p>Reward 是环境对动作的反馈，用来衡量这个动作带来的结果好不好。比如赢棋奖励高，输棋奖励低；推荐系统中用户点击是正奖励，投诉是负奖励；大模型中高质量、安全、有帮助的回答会获得更高奖励。</p><p>三者构成强化学习循环：Agent 观察 State，选择 Action，环境返回 Reward 和新的 State，Agent 再根据奖励更新策略。简单说，State 是“现在是什么情况”，Action 是“我做什么”，Reward 是“做得好不好”。</p></blockquote>"},{"t":"Policy是什么？","tag":"","p":"","html":"<ol><li>Policy 是什么？</li></ol><p>Policy 的核心是：策略，表示 Agent 在某个状态下应该选择什么动作。</p><ol><li>Policy 是什么？</li></ol><p>Policy 记作：</p><pre><code>π(a | s)</code></pre><p>意思是：</p><pre><code>在状态 s 下，选择动作 a 的概率</code></pre><p>也就是说，Policy 决定 Agent 的行为方式。</p><p>比如下棋：</p><pre><code>当前棋盘局面 → 选择下一步棋</code></pre><p>自动驾驶：</p><pre><code>当前道路状态 → 加速、减速、转向</code></pre><p>大模型：</p><pre><code>当前 prompt 和已生成文本 → 生成下一个 token</code></pre><ol><li>确定性 Policy</li></ol><p>确定性策略是：</p><pre><code>一个状态只对应一个动作</code></pre><p>比如：</p><pre><code>π(s) = a</code></pre><p>意思是给定状态 s，策略直接输出动作 a。</p><p>例如：</p><pre><code>当前红灯 → 刹车</code></pre><ol><li>随机性 Policy</li></ol><p>随机性策略输出动作概率分布。</p><p>比如：</p><pre><code>左转：0.1<br/>直行：0.7<br/>右转：0.2</code></pre><p>Agent 会根据概率选择动作。</p><p>大语言模型生成 token 就更像随机策略：</p><pre><code>北京：0.6<br/>上海：0.1<br/>广州：0.05<br/>...</code></pre><p>然后根据采样策略选择下一个 token。</p><ol><li>Policy 学什么？</li></ol><p>强化学习的目标就是学习一个好 Policy。</p><p>也就是让 Agent 在各种状态下选择动作，使长期累计奖励最大。</p><p>目标不是每一步看起来最好，而是长期收益最大。</p><p>比如下棋中，有些走法短期吃亏，但长期能赢。</p><ol><li>Policy 和 Value 的区别</li></ol><p>Policy 回答：</p><pre><code>当前状态下做什么？</code></pre><p>Value 回答：</p><pre><code>当前状态或动作长期看有多好？</code></pre><p>Policy 是决策规则。</p><p>Value 是评估函数。</p><p>很多算法会同时学习 Policy 和 Value，比如 Actor-Critic。</p><ol><li>大模型里的 Policy</li></ol><p>在大模型 RLHF 中，语言模型本身就是 Policy。</p><p>状态是：</p><pre><code>prompt + 已生成 token</code></pre><p>动作是：</p><pre><code>下一个 token</code></pre><p>策略是：</p><pre><code>模型输出的 token 概率分布</code></pre><p>RLHF 的目标是调整这个策略，让它生成更符合人类偏好的回答。</p><ol><li>好 Policy 的特点</li></ol><p>一个好的 Policy 应该：</p><ul><li>奖励高</li><li>稳定</li><li>泛化好</li><li>不做危险动作</li><li>能处理不同状态</li><li>能平衡短期和长期收益</li><li>能在不确定时合理探索</li></ul><ol><li><strong>面试背诵版：Policy 是什么？</strong></li></ol><blockquote><p>Policy，也就是策略，是强化学习中 Agent 的行为规则，表示在某个状态下应该选择什么动作，通常记作 π(a|s)，意思是在状态 s 下选择动作 a 的概率。Policy 决定了 Agent 如何和环境交互。</p><p>Policy 可以是确定性的，也可以是随机性的。确定性策略在一个状态下直接输出一个动作；随机性策略则输出动作概率分布，然后按照概率采样动作。大语言模型在 RLHF 中就可以看作一个随机策略：状态是 prompt 和已经生成的内容，动作是下一个 token，模型输出的是下一个 token 的概率分布。</p><p>强化学习的目标就是学习一个好的 Policy，使 Agent 在不同状态下选择合适动作，从而最大化长期累计奖励。简单说，Policy 回答的是“现在这种状态下，我应该怎么做”。</p></blockquote>"},{"t":"Value Function是什么？","tag":"","p":"","html":"<ol><li>Value Function 是什么？</li></ol><p>Value Function 的核心是：评估一个状态或动作长期来看有多好，也就是未来能拿到多少累计奖励。</p><ol><li>Value Function 是什么？</li></ol><p>Value Function 叫价值函数。</p><p>它不是直接决定做什么，而是评估：</p><pre><code>当前状态好不好<br/>当前动作值不值得</code></pre><p>强化学习关注长期奖励，所以 Value Function 估计的是未来累计收益。</p><ol><li>State Value Function</li></ol><p>状态价值函数通常写作：</p><pre><code>V(s)</code></pre><p>表示：</p><pre><code>从状态 s 出发，按照某个策略继续执行，未来期望能获得多少累计奖励</code></pre><p>比如下棋：</p><pre><code>当前棋盘局面对我有多有利？</code></pre><p>如果局面很可能赢，V(s) 就高。</p><p>如果局面很可能输，V(s) 就低。</p><ol><li>Action Value Function</li></ol><p>动作价值函数通常写作：</p><pre><code>Q(s, a)</code></pre><p>表示：</p><pre><code>在状态 s 下执行动作 a，然后继续按照策略行动，未来期望获得多少累计奖励</code></pre><p>比如下棋：</p><pre><code>当前局面下走这一步棋，长期看收益是多少？</code></pre><p>这就是 Q 值。</p><ol><li>V 和 Q 的区别</li></ol><p>V(s) 评估的是：</p><pre><code>这个状态整体好不好</code></pre><p>Q(s, a) 评估的是：</p><pre><code>这个状态下做某个动作好不好</code></pre><p>所以：</p><pre><code>V 看状态<br/>Q 看状态 + 动作</code></pre><ol><li>Value Function 有什么用？</li></ol><p>Value Function 可以帮助 Agent：</p><ul><li>判断当前局面好坏</li><li>选择更优动作</li><li>估计长期收益</li><li>降低策略梯度方差</li><li>作为 baseline</li><li>计算 Advantage</li></ul><p>比如：</p><pre><code>Advantage = Q(s,a) - V(s)</code></pre><p>表示某个动作比当前状态平均水平好多少。</p><ol><li>和 Policy 的关系</li></ol><p>Policy 负责决策：</p><pre><code>我要做什么？</code></pre><p>Value Function 负责评估：</p><pre><code>这样做长期看值不值？</code></pre><p>Actor-Critic 中：</p><ul><li>Actor 学 Policy</li><li>Critic 学 Value Function</li></ul><p>两者配合训练。</p><ol><li>大模型 RLHF 中的 Value Function</li></ol><p>在 PPO 类 RLHF 中，通常会训练一个 value model 或 value head。</p><p>它估计当前生成状态后续能获得的奖励。</p><p>作用是：</p><ul><li>估计 baseline</li><li>计算 advantage</li><li>稳定策略更新</li></ul><p>不过像 GRPO 这类方法会尝试不用单独 value model。</p><ol><li><strong>面试背诵版：Value Function 是什么？</strong></li></ol><blockquote><p>Value Function，也就是价值函数，用来评估一个状态或状态-动作对长期来看有多好，本质上是估计从当前状态开始未来能获得的期望累计奖励。它不是直接告诉 Agent 做什么，而是判断当前局面或某个动作的长期价值。</p><p>常见有两类：V(s) 是状态价值函数，表示在状态 s 下按照某个策略继续行动，未来期望获得多少奖励；Q(s,a) 是动作价值函数，表示在状态 s 下执行动作 a 后，未来期望获得多少奖励。简单说，V 评估“这个状态好不好”，Q 评估“这个状态下做这个动作好不好”。</p><p>Value Function 可以用来选择动作、估计长期收益、作为 baseline 降低策略梯度方差，并计算 Advantage。在 Actor-Critic 结构中，Actor 学策略，Critic 学价值函数。在大模型 RLHF 的 PPO 中，也常用 value model 或 value head 来估计价值，帮助稳定训练。</p></blockquote>"}]},{"no":"","title":"8.2 RL算法","questions":[{"t":"Q Learning原理？","tag":"","p":"","html":"<ol><li>Q-Learning 原理？</li></ol><p>Q-Learning 的核心是：学习一个 Q(s,a) 表，估计在某个状态下执行某个动作的长期价值，然后选择 Q 值最大的动作。</p><ol><li>Q-Learning 学什么？</li></ol><p>Q-Learning 学的是动作价值函数：</p><pre><code>Q(s, a)</code></pre><p>表示：</p><pre><code>在状态 s 下执行动作 a，未来能获得的期望累计奖励</code></pre><p>比如下棋：</p><pre><code>当前棋盘下走这一步，长期看收益是多少？</code></pre><ol><li>Q 值有什么用？</li></ol><p>如果知道每个动作的 Q 值，就可以选最好的动作：</p><pre><code>a = argmax Q(s, a)</code></pre><p>也就是在当前状态下选择 Q 值最大的动作。</p><p>所以 Q-Learning 的目标是学出一个准确的 Q 函数。</p><ol><li>Bellman 更新思想</li></ol><p>Q-Learning 基于 Bellman 方程。</p><p>当前动作的价值应该等于：</p><pre><code>当前奖励 + 未来最好动作的折扣价值</code></pre><p>更新目标是：</p><pre><code>target = r + γ max_a' Q(s', a')</code></pre><p>其中：</p><ul><li>r：当前奖励</li><li>γ：折扣因子</li><li>s'：下一个状态</li><li>max Q(s',a')：下个状态能取得的最大未来价值</li></ul><ol><li>Q-Learning 更新公式</li></ol><p>经典更新公式是：</p><pre><code>Q(s,a) = Q(s,a) + α [r + γ max_a' Q(s',a') - Q(s,a)]</code></pre><p>其中：</p><ul><li>α：学习率</li><li>r：奖励</li><li>γ：折扣因子</li><li>s'：下一个状态</li><li>max_a' Q(s',a')：下个状态最优动作价值</li></ul><p>中括号里的部分叫 TD Error。</p><ol><li>TD Error 是什么？</li></ol><p>TD Error 是：</p><pre><code>目标 Q 值 - 当前 Q 值</code></pre><p>也就是：</p><pre><code>r + γ max Q(s',a') - Q(s,a)</code></pre><p>如果目标比当前大，说明当前 Q 值低估了，要调高。</p><p>如果目标比当前小，说明当前 Q 值高估了，要调低。</p><ol><li>Off-policy 是什么？</li></ol><p>Q-Learning 是 off-policy 算法。</p><p>意思是：</p><pre><code>行为策略可以用于探索<br/>但学习目标是最优策略</code></pre><p>比如 Agent 实际行动时可能用 ε-greedy 探索。</p><p>但更新时使用：</p><pre><code>max_a' Q(s',a')</code></pre><p>也就是假设下一步选择最优动作。</p><ol><li>探索和利用</li></ol><p>Q-Learning 常用 ε-greedy。</p><p>意思是：</p><ul><li>以 ε 的概率随机选动作，进行探索</li><li>以 1-ε 的概率选择当前 Q 值最大动作，进行利用</li></ul><p>这样既能尝试新动作，也能利用已有经验。</p><ol><li>Q-Learning 的局限</li></ol><p>经典 Q-Learning 适合状态和动作空间较小的场景。</p><p>如果状态空间很大，比如图像、文本、连续控制，就不能简单用 Q 表。</p><p>这时会用神经网络近似 Q 函数，比如 DQN。</p><ol><li><strong>面试背诵版：Q-Learning 原理？</strong></li></ol><blockquote><p>Q-Learning 是一种基于价值函数的强化学习算法，它学习的是动作价值函数 Q(s,a)，表示在状态 s 下执行动作 a 后，未来可以获得的期望累计奖励。学到 Q 函数后，Agent 可以在每个状态下选择 Q 值最大的动作作为最优动作。</p><p>它的核心是 Bellman 更新：当前 Q 值应该向“当前奖励 + 下一状态最优动作价值”靠近。更新公式是 Q(s,a) = Q(s,a) + α [r + γ max_a' Q(s',a') - Q(s,a)]，其中 α 是学习率，γ 是折扣因子，中括号里的部分叫 TD Error，表示目标 Q 值和当前 Q 值之间的差距。</p><p>Q-Learning 是 off-policy 算法，因为行为时可以用 ε-greedy 做探索，但更新时使用下一状态的最大 Q 值，也就是学习目标是最优策略。简单说，Q-Learning 就是“不断更新每个状态-动作的长期价值，最后选择 Q 值最高的动作”。它适合离散且规模较小的问题，大规模状态空间通常会用 DQN 等方法近似 Q 函数。</p></blockquote>"},{"t":"DQN解决什么问题？","tag":"","p":"","html":"<ol><li>DQN 解决什么问题？</li></ol><p>DQN 的核心是：用神经网络近似 Q 函数，解决传统 Q-Learning 无法处理大规模、高维状态空间的问题。</p><ol><li>Q-Learning 的问题</li></ol><p>传统 Q-Learning 通常用一张 Q 表：</p><pre><code>Q(s, a)</code></pre><p>记录每个状态-动作的价值。</p><p>如果状态和动作很少，可以用表格存。</p><p>但现实任务中状态空间可能非常大。</p><p>比如：</p><ul><li>游戏画面是图像</li><li>机器人状态是连续变量</li><li>自动驾驶环境很复杂</li><li>推荐系统用户状态很多</li></ul><p>这时 Q 表根本存不下，也无法泛化。</p><ol><li>DQN 是什么？</li></ol><p>DQN 全称是 Deep Q-Network。</p><p>它用深度神经网络来近似 Q 函数。</p><p>输入是状态 s。</p><p>输出是每个动作的 Q 值。</p><p>比如：</p><pre><code>输入：游戏画面<br/>输出：<br/>左移 Q=1.2<br/>右移 Q=0.8<br/>跳跃 Q=2.1<br/>攻击 Q=1.5</code></pre><p>然后选择 Q 值最大的动作。</p><ol><li>DQN 解决的核心问题</li></ol><p>DQN 解决的是：</p><pre><code>大规模状态空间下，Q 表无法存储和泛化的问题</code></pre><p>神经网络可以从高维输入中提取特征。</p><p>比如从游戏像素中学到：</p><ul><li>敌人位置</li><li>道具位置</li><li>当前局势</li><li>哪个动作更有利</li></ul><p>所以 DQN 能处理复杂状态。</p><ol><li>DQN 的两个关键技术</li></ol><p>DQN 为了稳定训练，引入了两个重要机制。</p><p>第一个是 Experience Replay，经验回放。</p><p>它把智能体交互产生的数据存到 replay buffer。</p><p>训练时随机采样小批量数据。</p><p>好处是：</p><ul><li>打破样本相关性</li><li>提高数据利用率</li><li>训练更稳定</li></ul><p>第二个是 Target Network，目标网络。</p><p>DQN 会维护一个延迟更新的目标网络，用来计算 target Q 值。</p><p>这样目标不会随着当前网络频繁变化，训练更稳定。</p><ol><li>DQN 的更新目标</li></ol><p>DQN 的目标和 Q-Learning 类似：</p><pre><code>target = r + γ max_a' Q_target(s', a')</code></pre><p>然后让当前网络预测的：</p><pre><code>Q(s,a)</code></pre><p>靠近 target。</p><p>损失通常是：</p><pre><code>MSE(target, Q(s,a))</code></pre><ol><li>为什么不能直接用神经网络替代 Q 表？</li></ol><p>如果直接用神经网络训练 Q-Learning，会不稳定。</p><p>原因是：</p><ul><li>数据样本连续相关</li><li>目标值不断变化</li><li>Q 值容易过估计</li><li>分布随策略变化</li></ul><p>Experience Replay 和 Target Network 就是为了缓解这些不稳定。</p><ol><li>DQN 的局限</li></ol><p>DQN 主要适合离散动作空间。</p><p>如果动作是连续的，比如机器人关节角度，就不太适合直接用 DQN。</p><p>连续动作任务更常用：</p><ul><li>DDPG</li><li>TD3</li><li>SAC</li><li>PPO</li></ul><p>另外 DQN 也可能有 Q 值过估计问题，所以有 Double DQN 等改进。</p><ol><li><strong>面试背诵版：DQN 解决什么问题？</strong></li></ol><blockquote><p>DQN 主要解决传统 Q-Learning 在大规模、高维状态空间下无法使用 Q 表的问题。传统 Q-Learning 需要维护 Q(s,a) 表，如果状态空间很小可以直接存表；但在游戏画面、机器人控制、自动驾驶等场景中，状态可能是图像或连续高维特征，Q 表无法存储，也无法对相似状态进行泛化。</p><p>DQN 用深度神经网络来近似 Q 函数，输入状态，输出每个动作对应的 Q 值，从而可以处理高维状态并具备泛化能力。比如在 Atari 游戏中，输入是游戏画面，输出是每个离散动作的价值。</p><p>为了稳定训练，DQN 引入了两个关键机制：Experience Replay 和 Target Network。经验回放通过 replay buffer 随机采样历史经验，打破样本相关性并提高数据利用率；目标网络用于计算相对稳定的 target Q 值，避免训练目标频繁变化。简单说，DQN 就是“用神经网络代替 Q 表，并用经验回放和目标网络稳定训练”。</p></blockquote>"},{"t":"Policy Gradient是什么？","tag":"","p":"","html":"<ol><li>Policy Gradient 是什么？</li></ol><p>Policy Gradient 的核心是：不去学习 Q 表，而是直接优化策略参数，让高奖励动作出现的概率变大。</p><ol><li>Policy Gradient 学什么？</li></ol><p>Policy Gradient 直接学习策略：</p><pre><code>πθ(a | s)</code></pre><p>意思是：</p><pre><code>在状态 s 下，选择动作 a 的概率</code></pre><p>θ 是策略网络参数。</p><p>目标是调整 θ，让 Agent 获得更高长期奖励。</p><ol><li>和 Q-Learning 区别</li></ol><p>Q-Learning 学的是：</p><pre><code>每个动作有多好，也就是 Q(s,a)</code></pre><p>然后选 Q 值最大的动作。</p><p>Policy Gradient 学的是：</p><pre><code>直接学在某个状态下该选什么动作</code></pre><p>也就是直接输出动作概率分布。</p><ol><li>基本思想</li></ol><p>如果某次动作带来了高奖励，就提高这个动作在该状态下的概率。</p><p>如果某次动作带来了低奖励，就降低这个动作的概率。</p><p>可以简单理解为：</p><pre><code>好动作更常做<br/>坏动作少做</code></pre><ol><li>数学目标</li></ol><p>强化学习目标是最大化期望累计奖励：</p><pre><code>J(θ) = E[R]</code></pre><p>Policy Gradient 就是对策略参数 θ 求梯度：</p><pre><code>∇θ J(θ)</code></pre><p>然后用梯度上升更新参数：</p><pre><code>θ = θ + α ∇θ J(θ)</code></pre><p>因为我们要最大化奖励，而不是最小化 loss。</p><ol><li>REINFORCE 思想</li></ol><p>经典 Policy Gradient 形式是：</p><pre><code>∇J(θ) = E[∇ log πθ(a|s) * R]</code></pre><p>意思是：</p><ul><li>如果回报 R 高，就增加该动作概率</li><li>如果回报 R 低，就减少该动作概率</li></ul><p><code>log πθ(a|s)</code> 表示当前策略选择这个动作的 log 概率。</p><ol><li>为什么用 log probability？</li></ol><p>因为对概率分布求导比较方便。</p><p><code>∇ log πθ(a|s)</code> 可以告诉我们：</p><pre><code>怎么调整参数，让这个动作概率变大或变小</code></pre><p>乘上奖励后，就能按奖励高低调整概率。</p><ol><li>Policy Gradient 的优点</li></ol><p>优点：</p><ul><li>可以处理连续动作空间</li><li>可以学习随机策略</li><li>直接优化目标策略</li><li>适合高维复杂策略</li><li>比 Q 表方法更适合很多控制任务</li></ul><p>比如机器人控制中，动作可能是连续角度，Policy Gradient 更自然。</p><ol><li>Policy Gradient 的缺点</li></ol><p>缺点：</p><ul><li>方差大</li><li>样本效率低</li><li>训练不稳定</li><li>对奖励设计敏感</li><li>容易局部最优</li><li>需要 baseline 或 advantage 降低方差</li></ul><p>所以后来有 Actor-Critic、PPO、A2C、A3C 等改进。</p><ol><li><strong>面试背诵版：Policy Gradient 是什么？</strong></li></ol><blockquote><p>Policy Gradient 是一种直接优化策略的强化学习方法。它不先学习 Q 表或价值函数，而是直接学习一个参数化策略 πθ(a|s)，表示在状态 s 下选择动作 a 的概率。训练目标是调整策略参数 θ，使 Agent 的期望累计奖励最大。</p><p>它的核心思想是：如果某个动作带来了高回报，就提高该动作在对应状态下的概率；如果回报低，就降低这个动作的概率。经典形式可以写成 ∇J(θ) = E[∇ log πθ(a|s) * R]，其中 R 是回报，log πθ(a|s) 表示当前策略选择该动作的 log 概率。</p><p>相比 Q-Learning，Policy Gradient 直接学习“该怎么行动”，更适合连续动作空间和随机策略，但它通常方差较大、样本效率低、训练不稳定，所以实际中常结合 value function 形成 Actor-Critic，或者使用 PPO 等更稳定的策略优化方法。</p></blockquote>"},{"t":"Actor-Critic是什么？","tag":"","p":"","html":"<ol><li>Actor-Critic 是什么？</li></ol><p>Actor-Critic 的核心是：Actor 负责选择动作，Critic 负责评价动作好坏，两者配合让策略训练更稳定。</p><ol><li>Actor 是什么？</li></ol><p>Actor 是策略网络。</p><p>它学习的是：</p><pre><code>π(a | s)</code></pre><p>也就是在状态 s 下选择动作 a 的概率。</p><p>Actor 负责决策：</p><pre><code>当前状态下我该做什么？</code></pre><p>比如：</p><ul><li>游戏中选择移动方向</li><li>机器人选择动作</li><li>大模型选择下一个 token</li></ul><ol><li>Critic 是什么？</li></ol><p>Critic 是价值网络。</p><p>它学习的是：</p><pre><code>V(s) 或 Q(s,a)</code></pre><p>也就是评估当前状态或动作长期看有多好。</p><p>Critic 负责评价：</p><pre><code>这个状态好不好？<br/>这个动作值不值得？</code></pre><ol><li>为什么需要 Critic？</li></ol><p>纯 Policy Gradient 通常方差很大。</p><p>因为它可能只根据最终 reward 更新策略。</p><p>比如一次奖励高，整条路径上的动作都被认为好。</p><p>但实际上不一定每个动作都有贡献。</p><p>Critic 可以估计 value，帮助判断：</p><pre><code>这个动作比预期好多少？</code></pre><p>从而降低方差，让训练更稳定。</p><ol><li>Advantage 是关键</li></ol><p>Actor-Critic 常用 Advantage：</p><pre><code>A(s,a) = Q(s,a) - V(s)</code></pre><p>表示：</p><pre><code>这个动作比当前状态下平均水平好多少</code></pre><p>如果 Advantage &gt; 0：</p><pre><code>增加这个动作概率</code></pre><p>如果 Advantage &lt; 0：</p><pre><code>降低这个动作概率</code></pre><p>这样比直接用 reward 更稳定。</p><ol><li>Actor 和 Critic 怎么配合？</li></ol><p>流程是：</p><pre><code>Actor 根据状态选择动作<br/>  ↓<br/>环境返回奖励和新状态<br/>  ↓<br/>Critic 评估这个动作好不好<br/>  ↓<br/>用评价结果更新 Actor<br/>  ↓<br/>同时更新 Critic 自己的价值估计</code></pre><p>Actor 负责改策略。</p><p>Critic 负责提供学习信号。</p><ol><li>和 Q-Learning、Policy Gradient 的关系</li></ol><p>Q-Learning 是 value-based：</p><pre><code>主要学价值</code></pre><p>Policy Gradient 是 policy-based：</p><pre><code>直接学策略</code></pre><p>Actor-Critic 结合二者：</p><pre><code>Actor 学策略<br/>Critic 学价值</code></pre><p>所以它兼具两类方法的优点。</p><ol><li>常见算法</li></ol><p>很多强化学习算法属于 Actor-Critic 思路，比如：</p><ul><li>A2C</li><li>A3C</li><li>DDPG</li><li>TD3</li><li>SAC</li><li>PPO</li></ul><p>PPO 通常也可以看成 Actor-Critic 框架下的策略优化方法。</p><ol><li><strong>面试背诵版：Actor-Critic 是什么？</strong></li></ol><blockquote><p>Actor-Critic 是强化学习中结合策略学习和值函数学习的一类方法。Actor 是策略网络，负责根据当前状态选择动作，也就是学习 π(a|s)；Critic 是价值网络，负责评估当前状态或动作的长期价值，比如学习 V(s) 或 Q(s,a)。</p><p>它的动机是解决纯 Policy Gradient 方差较大的问题。Actor 直接根据策略采样动作，但需要知道这个动作到底好不好；Critic 通过价值函数估计提供评价信号，常用 Advantage A(s,a)=Q(s,a)-V(s) 表示某个动作比当前状态下的平均水平好多少。如果 Advantage 为正，就提高该动作概率；如果为负，就降低该动作概率。</p><p>简单说，Actor 负责“做决策”，Critic 负责“打分评价”。Actor 根据 Critic 的反馈更新策略，Critic 根据环境奖励更新自己的价值估计。PPO、A2C、A3C、DDPG、SAC 等都可以看作 Actor-Critic 思路的应用或变体。</p></blockquote>"},{"t":"PPO算法原理？","tag":"","p":"","html":"<ol><li>PPO 算法原理？</li></ol><p>PPO 的核心是：在策略梯度优化时限制新旧策略变化幅度，避免一步更新太大导致策略崩掉。</p><ol><li>PPO 是什么？</li></ol><p>PPO 全称是 Proximal Policy Optimization，近端策略优化。</p><p>它是一种强化学习中的策略优化算法。</p><p>常用于：</p><ul><li>游戏智能体</li><li>机器人控制</li><li>大模型 RLHF</li><li>对话模型对齐</li></ul><p>它的目标是：</p><pre><code>提高策略收益，同时保证更新不要太激进</code></pre><ol><li>为什么需要 PPO？</li></ol><p>普通 Policy Gradient 会直接根据奖励更新策略。</p><p>问题是：</p><pre><code>一次更新太大，策略可能突然变坏</code></pre><p>比如模型原来回答还可以，一次训练后输出风格崩掉。</p><p>所以 PPO 想解决的是：</p><pre><code>让策略稳步变好，而不是大幅乱跳</code></pre><ol><li>PPO 的核心思想</li></ol><p>PPO 会比较新策略和旧策略的概率比值：</p><pre><code>r(θ) = π_new(a|s) / π_old(a|s)</code></pre><p>这个比值表示：</p><pre><code>新策略选择某个动作的概率，相比旧策略变了多少</code></pre><p>如果变化太大，就限制它。</p><ol><li>Clip 机制</li></ol><p>PPO 最经典的是 clipped objective。</p><p>它会把概率比值限制在一个范围内，比如：</p><pre><code>[1 - ε, 1 + ε]</code></pre><p>常见 ε 是 0.1 或 0.2。</p><p>意思是：</p><pre><code>新策略相对旧策略不能变化太多</code></pre><p>如果超过范围，就截断优化收益，防止模型为了高奖励过度更新。</p><ol><li>Advantage 是什么？</li></ol><p>PPO 不是只看 reward。</p><p>它通常用 Advantage 来衡量一个动作比平均水平好多少。</p><p>简单理解：</p><pre><code>Advantage &gt; 0：这个动作比预期好，增加概率<br/>Advantage &lt; 0：这个动作比预期差，降低概率</code></pre><p>这样策略会更倾向于产生高优势动作。</p><ol><li>PPO 的训练流程</li></ol><p>大致流程是：</p><pre><code>用旧策略采样数据<br/>  ↓<br/>执行动作，获得 reward<br/>  ↓<br/>估计 advantage<br/>  ↓<br/>用 clipped objective 更新策略<br/>  ↓<br/>更新 value model<br/>  ↓<br/>重复</code></pre><p>在 RLHF 里，reward 可能来自奖励模型。</p><ol><li>PPO 在 RLHF 中怎么用？</li></ol><p>大模型 RLHF 中一般是：</p><pre><code>SFT 模型生成回答<br/>  ↓<br/>Reward Model 给回答打分<br/>  ↓<br/>PPO 根据奖励优化语言模型</code></pre><p>同时还会加 KL penalty，防止模型偏离原始 SFT 模型太远。</p><p>也就是：</p><pre><code>既要奖励高，也不能跑偏太多</code></pre><ol><li>PPO 的优点</li></ol><p>PPO 的优点：</p><ul><li>比普通策略梯度稳定</li><li>实现相对简单</li><li>不需要复杂二阶优化</li><li>更新幅度可控</li><li>训练效果较好</li><li>在 RLHF 中应用广泛</li></ul><ol><li><strong>面试背诵版：PPO 算法原理？</strong></li></ol><blockquote><p>PPO，也就是 Proximal Policy Optimization，是一种强化学习中的策略优化算法。它的核心思想是在提升策略收益的同时，限制新策略相对旧策略的变化幅度，避免一次更新过大导致策略崩溃。</p><p>PPO 会计算新旧策略对同一个动作的概率比值 r(θ) = π_new(a|s) / π_old(a|s)，表示新策略相比旧策略改变了多少。然后通过 clipped objective 把这个比值限制在 [1 - ε, 1 + ε] 范围内。如果新策略变化过大，优化目标会被截断，从而防止模型为了追求高奖励而过度更新。</p><p>PPO 通常结合 Advantage 使用，Advantage 表示某个动作比预期好多少。Advantage 为正时提高该动作概率，为负时降低该动作概率。在大模型 RLHF 中，PPO 会用奖励模型给回答打分，再优化语言模型，同时通常加入 KL penalty，防止模型偏离 SFT 模型太远。简单说，PPO 是“让策略变好，但每一步不要变太猛”。</p></blockquote>"}]},{"no":"","title":"8.3 LLM Alignment","questions":[{"t":"RLHF流程是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>RLHF 的核心流程是：<strong>先训练一个会听指令的模型，再训练奖励模型模拟人类偏好，最后用强化学习让模型输出更符合人类偏好。</strong></p><ol><li>RLHF 是什么？</li></ol><p>RLHF 全称是 Reinforcement Learning from Human Feedback。</p><p>意思是：</p><pre><code>基于人类反馈的强化学习</code></pre><p>它用于让大模型回答更符合人类偏好，比如：</p><ul><li>更有帮助</li><li>更安全</li><li>更真实</li><li>更符合指令</li><li>更少有害内容</li><li>更符合对话风格</li></ul><ol><li>第一步：预训练模型</li></ol><p>先有一个基础大模型。</p><p>它通常通过 next token prediction 在海量语料上预训练。</p><p>这个模型具备基础语言能力和知识。</p><p>但它不一定会很好地遵循人类指令。</p><ol><li>第二步：SFT 指令微调</li></ol><p>SFT 是 Supervised Fine-Tuning。</p><p>用人工标注的高质量指令数据训练模型。</p><p>数据形式是：</p><pre><code>用户指令 → 理想回答</code></pre><p>目标是让模型学会：</p><ul><li>按指令回答</li><li>使用对话格式</li><li>给出有帮助的答案</li><li>基础安全拒答</li></ul><p>得到一个 SFT 模型。</p><ol><li>第三步：收集人类偏好数据</li></ol><p>给同一个 prompt，让模型生成多个回答。</p><p>然后让人工标注者比较哪个更好。</p><p>比如：</p><pre><code>Prompt: 如何学习机器学习？<br/><br/>回答 A：...<br/>回答 B：...<br/>人工选择：A 更好</code></pre><p>这些偏好数据用于训练 Reward Model。</p><ol><li>第四步：训练奖励模型</li></ol><p>Reward Model 学习人类偏好。</p><p>输入是：</p><pre><code>prompt + answer</code></pre><p>输出是一个分数：</p><pre><code>这个回答有多好</code></pre><p>它的目标是让人类更喜欢的回答得分更高。</p><p>奖励模型相当于用来近似人类评价。</p><ol><li>第五步：用 PPO 优化模型</li></ol><p>接下来用强化学习优化 SFT 模型。</p><p>流程是：</p><pre><code>模型生成回答<br/>  ↓<br/>Reward Model 打分<br/>  ↓<br/>PPO 根据奖励更新模型</code></pre><p>同时通常加入 KL penalty。</p><p>防止模型为了刷高奖励，偏离原 SFT 模型太远。</p><ol><li>第六步：评估和迭代</li></ol><p>训练完后要评估：</p><ul><li>有帮助性</li><li>真实性</li><li>安全性</li><li>指令跟随</li><li>拒答边界</li><li>幻觉率</li><li>偏见</li><li>人类偏好胜率</li></ul><p>如果效果不好，会继续补数据、改奖励模型、再训练。</p><ol><li>RLHF 的问题</li></ol><p>RLHF 也有局限：</p><ul><li>人工标注成本高</li><li>奖励模型可能学偏</li><li>可能 reward hacking</li><li>PPO 训练不稳定</li><li>人类偏好不完全一致</li><li>可能过度迎合用户</li><li>安全和有用性存在冲突</li></ul><p>所以现在也有 DPO、RLAIF 等替代或简化方法。</p><ol><li><strong>面试背诵版：RLHF 流程是什么？</strong></li></ol><blockquote><p>RLHF，全称 Reinforcement Learning from Human Feedback，是用人类反馈来对齐大模型行为的方法。它的流程通常分为三大阶段：SFT、Reward Model 和强化学习优化。</p><p>第一步是在预训练模型基础上做 SFT，也就是用人工标注的“指令-理想回答”数据进行监督微调，让模型学会基本的指令跟随和对话格式。第二步是收集人类偏好数据：对同一个 prompt 生成多个回答，让人工标注者比较哪个回答更好，然后用这些偏好数据训练 Reward Model，使奖励模型能给更符合人类偏好的回答打更高分。第三步是用 PPO 等强化学习算法优化 SFT 模型，让模型生成回答后由 Reward Model 打分，再根据奖励更新模型，同时通常加入 KL penalty，防止模型偏离原来的 SFT 模型太远。</p><p>简单说，RLHF 的流程是“先用 SFT 教模型怎么答，再用人类偏好训练奖励模型，最后用强化学习让模型更符合人类偏好”。它能提升有帮助性、安全性和指令跟随能力，但也存在标注成本高、奖励模型偏差、reward hacking 和训练不稳定等问题。</p></blockquote>"},{"t":"Reward Model如何训练？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"PPO为什么用于RLHF？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"DPO是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>DPO 的核心是：<strong>不用训练奖励模型，也不用 PPO，直接用人类偏好数据优化模型，让模型更倾向于生成被偏好的回答。</strong></p><ol><li>DPO 是什么？</li></ol><p>DPO 全称是 Direct Preference Optimization。</p><p>意思是直接偏好优化。</p><p>它用于大模型对齐。</p><p>目标是让模型学会：</p><pre><code>更偏向人类喜欢的回答<br/>更少生成不喜欢的回答</code></pre><ol><li>它解决 RLHF 什么问题？</li></ol><p>传统 RLHF 通常流程是：</p><pre><code>SFT → 训练 Reward Model → PPO 强化学习</code></pre><p>问题是：</p><ul><li>奖励模型训练复杂</li><li>PPO 训练不稳定</li><li>调参困难</li><li>成本高</li><li>容易 reward hacking</li><li>工程链路长</li></ul><p>DPO 简化了这个流程。</p><p>它直接用偏好数据训练模型，不需要显式训练 Reward Model，也不需要跑 PPO。</p><ol><li>DPO 需要什么数据？</li></ol><p>DPO 使用偏好对数据。</p><p>形式是：</p><pre><code>prompt<br/>chosen answer<br/>rejected answer</code></pre><p>比如：</p><pre><code>问题：解释什么是过拟合<br/><br/>chosen：准确、清晰、有帮助的回答<br/>rejected：错误、啰嗦或不安全的回答</code></pre><p>模型训练目标是：</p><pre><code>提高 chosen 的概率<br/>降低 rejected 的概率</code></pre><ol><li>DPO 的核心思想</li></ol><p>DPO 从理论上把 RLHF 的奖励优化目标转化成一个监督学习形式的损失函数。</p><p>简单理解：</p><pre><code>让模型更可能生成 chosen<br/>更不可能生成 rejected</code></pre><p>同时它会参考一个 reference model，避免当前模型偏离原模型太远。</p><ol><li>DPO 和 SFT 的区别</li></ol><p>SFT 只看正样本。</p><p>数据形式是：</p><pre><code>prompt → good answer</code></pre><p>它只告诉模型：</p><pre><code>这个答案应该学</code></pre><p>DPO 同时看好答案和坏答案。</p><p>数据形式是：</p><pre><code>prompt → chosen answer vs rejected answer</code></pre><p>它告诉模型：</p><pre><code>相比这个坏答案，你更应该生成那个好答案</code></pre><p>所以 DPO 利用了偏好比较信息。</p><ol><li>DPO 和 PPO 的区别</li></ol><p>PPO 是强化学习方法。</p><p>它通常需要：</p><ul><li>Reward Model</li><li>采样生成</li><li>奖励打分</li><li>策略更新</li><li>KL 控制</li></ul><p>DPO 是监督式偏好优化。</p><p>它直接在偏好数据上训练。</p><p>所以更简单、更稳定、更容易实现。</p><ol><li>DPO 的优点</li></ol><p>DPO 优点：</p><ul><li>不需要 Reward Model</li><li>不需要 PPO</li><li>训练流程简单</li><li>稳定性更好</li><li>成本更低</li><li>实现更容易</li><li>适合指令对齐</li><li>能有效利用偏好数据</li></ul><ol><li>DPO 的局限</li></ol><p>DPO 也有局限：</p><ul><li>依赖高质量偏好数据</li><li>chosen / rejected 质量很关键</li><li>不能像 RL 那样灵活优化复杂奖励</li><li>对分布外问题可能泛化有限</li><li>reference model 和超参数也会影响效果</li><li>数据偏差会直接影响模型偏好</li></ul><ol><li><strong>面试背诵版：DPO 是什么？</strong></li></ol><blockquote><p>DPO，全称 Direct Preference Optimization，是一种用于大模型对齐的偏好优化方法。它的核心思想是不再显式训练 Reward Model，也不使用 PPO，而是直接利用人类偏好数据训练模型，让模型更倾向于生成被人类选择的回答，降低生成被拒绝回答的概率。</p><p>DPO 使用的数据通常是三元组：prompt、chosen answer 和 rejected answer。相比 SFT 只学习一个好答案，DPO 同时利用“好答案”和“坏答案”的对比信息，让模型学会在两个回答中更偏向 chosen。相比传统 RLHF 的“训练奖励模型再用 PPO 优化”，DPO 把偏好优化转化成类似监督学习的损失函数，因此训练更简单、稳定性更好、工程成本更低。</p><p>简单说，SFT 是“模仿好答案”，RLHF 是“训练奖励模型再强化学习”，DPO 是“直接用偏好对告诉模型哪个更好”。它的优点是简单稳定，但效果依赖高质量偏好数据。</p></blockquote>"},{"t":"DPO相比PPO有什么优势？","tag":"","p":"","html":"<ol><li>DPO 相比 PPO 有什么优势？</li></ol><p>DPO 相比 PPO 的核心优势是：训练流程更简单、更稳定，不需要奖励模型和复杂的强化学习采样循环。</p><ol><li>PPO 的流程更复杂</li></ol><p>传统 RLHF 使用 PPO，一般流程是：</p><pre><code>SFT 模型<br/>  ↓<br/>训练 Reward Model<br/>  ↓<br/>模型采样生成回答<br/>  ↓<br/>Reward Model 打分<br/>  ↓<br/>PPO 更新策略<br/>  ↓<br/>KL 约束防止跑偏</code></pre><p>这个流程里有多个模型和多个训练阶段。</p><p>比如：</p><ul><li>Policy Model</li><li>Reference Model</li><li>Reward Model</li><li>Value Model / Critic</li></ul><p>工程链路比较重。</p><ol><li>DPO 不需要显式 Reward Model</li></ol><p>DPO 直接使用偏好数据：</p><pre><code>prompt + chosen answer + rejected answer</code></pre><p>训练目标是：</p><pre><code>提高 chosen 概率<br/>降低 rejected 概率</code></pre><p>它不需要单独训练一个 Reward Model。</p><p>这减少了：</p><ul><li>奖励模型训练成本</li><li>奖励模型偏差</li><li>reward hacking 风险</li><li>工程复杂度</li></ul><ol><li>DPO 不需要 PPO 的强化学习循环</li></ol><p>PPO 需要模型不断采样、打分、更新策略。</p><p>训练过程不稳定，调参也复杂。</p><p>DPO 更像监督学习。</p><p>直接在偏好数据上优化一个损失函数。</p><p>所以：</p><ul><li>更容易实现</li><li>更容易复现</li><li>训练更稳定</li><li>调参更简单</li><li>成本更低</li></ul><ol><li>DPO 避免 Value Model 复杂度</li></ol><p>PPO 通常还需要 Value Model 或 Critic 来估计 baseline。</p><p>这会增加：</p><ul><li>显存占用</li><li>训练不稳定性</li><li>额外模型管理</li><li>advantage 估计复杂度</li></ul><p>DPO 不需要 value model。</p><p>它直接用偏好对优化策略。</p><ol><li>DPO 更适合离线偏好数据</li></ol><p>如果已经有一批偏好数据：</p><pre><code>chosen / rejected</code></pre><p>DPO 可以直接训练。</p><p>不需要在线采样和奖励打分。</p><p>这对企业应用很友好，因为很多偏好数据来自人工标注、用户反馈或历史日志。</p><ol><li>PPO 更灵活但更重</li></ol><p>这里要补一句。</p><p>DPO 不一定全面替代 PPO。</p><p>PPO 的优势是可以在线利用奖励模型或环境反馈继续探索。</p><p>比如：</p><ul><li>复杂 reward</li><li>多步任务</li><li>在线交互</li><li>需要持续探索的场景</li></ul><p>PPO 更灵活。</p><p>但成本和稳定性更差。</p><ol><li>简单对比</li></ol><sheet></sheet><ol><li><strong>面试背诵版：DPO 相比 PPO 有什么优势？</strong></li></ol><blockquote><p>DPO 相比 PPO 的主要优势是流程更简单、训练更稳定、工程成本更低。传统 RLHF 使用 PPO 时，通常需要先训练 Reward Model，再让模型采样回答，由 Reward Model 打分，然后通过 PPO 更新策略，同时还需要 Reference Model 控制 KL 偏移，并且常常需要 Value Model 或 Critic 来估计 advantage。整个流程模型多、链路长、显存成本高，训练也比较不稳定。</p><p>DPO 则直接使用偏好数据，也就是 prompt、chosen answer 和 rejected answer。它不需要显式训练 Reward Model，也不需要 PPO 的在线采样和强化学习循环，而是直接优化模型，让 chosen 的概率相对 rejected 更高。因此 DPO 更像监督式偏好学习，实现简单、调参容易、训练稳定，也减少了 reward hacking 和 value model 训练带来的问题。</p><p>不过 DPO 也不是完全替代 PPO。PPO 更适合需要在线探索、复杂奖励和多步环境反馈的任务；DPO 更适合已有高质量离线偏好数据的对齐场景。简单说，PPO 是“先训练奖励模型再强化学习”，DPO 是“直接用偏好对优化模型”。</p></blockquote>"},{"t":"GRPO是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>GRPO 的核心是：<strong>一种用于大模型强化学习对齐的算法，它不单独训练 value model，而是用一组回答的相对奖励作为基线来更新模型。</strong></p><ol><li>GRPO 是什么？</li></ol><p>GRPO 全称是 Group Relative Policy Optimization。</p><p>可以理解成：</p><pre><code>基于一组候选回答相对好坏来优化策略</code></pre><p>它常被用于大模型后训练，尤其是推理模型训练里。</p><p>和 PPO 相比，GRPO 的一个重要特点是：</p><pre><code>不需要单独训练 Critic / Value Model</code></pre><p>这能降低训练成本和复杂度。</p><ol><li>它解决 PPO 什么问题？</li></ol><p>PPO 在 RLHF 中通常需要：</p><ul><li>Policy Model：当前要优化的模型</li><li>Reference Model：参考模型，用于 KL 约束</li><li>Reward Model：奖励模型</li><li>Value Model / Critic：估计状态价值</li><li>Advantage Estimation：优势估计</li></ul><p>问题是链路比较重：</p><ul><li>显存成本高</li><li>训练不稳定</li><li>value model 训练复杂</li><li>工程实现复杂</li><li>大模型场景成本很高</li></ul><p>GRPO 简化了这个过程。</p><p>它去掉了 value model，用同一个 prompt 下多个回答的奖励相对关系来估计 advantage。</p><ol><li>GRPO 的核心思想</li></ol><p>对于同一个问题 prompt，让模型生成多个回答。</p><p>比如：</p><pre><code>Prompt: 解一道数学题<br/>回答 A：得分 0.9<br/>回答 B：得分 0.6<br/>回答 C：得分 0.2<br/>回答 D：得分 0.8</code></pre><p>GRPO 不只是看某个回答的绝对分数，而是看它在这一组回答里的相对表现。</p><p>比如：</p><pre><code>A、D 比组内平均更好 → 增加概率<br/>C 比组内平均更差 → 降低概率</code></pre><p>也就是说：</p><pre><code>Advantage = 当前回答奖励 - 组内平均奖励</code></pre><p>或者用组内标准化后的相对奖励。</p><ol><li>为什么叫 Group Relative？</li></ol><p>Group 表示同一个 prompt 下采样出一组回答。</p><p>Relative 表示比较的是组内相对好坏。</p><p>Policy Optimization 表示用这些相对优势更新策略模型。</p><p>所以 GRPO 的重点是：</p><pre><code>同题多答，组内比较，相对优化。</code></pre><ol><li>GRPO 和 PPO 的区别</li></ol><p>PPO 通常用 value model 来估计 baseline。</p><p>GRPO 不训练 value model，而是用组内平均奖励作为 baseline。</p><p>可以这样对比：</p><sheet></sheet><ol><li>GRPO 为什么适合推理模型？</li></ol><p>推理任务经常可以对同一道题采样多个答案。</p><p>然后用规则或奖励模型打分。</p><p>比如：</p><ul><li>数学题是否答对</li><li>代码题是否通过测试</li><li>逻辑题答案是否正确</li><li>格式是否符合要求</li></ul><p>同一个 prompt 下，多条回答之间可以形成明显对比。</p><p>这非常适合 GRPO 的组内相对优化。</p><ol><li>GRPO 的优点</li></ol><p>主要优点：</p><ul><li>不需要训练 value model</li><li>显存和计算成本更低</li><li>训练流程比 PPO 简化</li><li>利用同题多个回答的相对质量</li><li>适合数学、代码、推理等可验证任务</li><li>在大模型 RL 训练中更轻量</li></ul><ol><li>GRPO 的局限</li></ol><p>它也有局限：</p><ul><li>需要同一个 prompt 采样多个回答，采样成本较高</li><li>奖励信号质量很关键</li><li>组内样本太少时，优势估计可能不稳定</li><li>如果一组回答都很差，相对比较也可能不够有效</li><li>仍然需要 KL 约束，避免模型偏离太远</li><li>对开放式偏好任务，奖励设计仍然困难</li></ul><ol><li><strong>面试背诵版：GRPO 是什么？</strong></li></ol><blockquote><p>GRPO，全称 Group Relative Policy Optimization，是一种用于大模型强化学习后训练的策略优化方法。它的核心思想是：对于同一个 prompt，让模型生成多个候选回答，然后根据这些回答的奖励分数做组内比较，用相对奖励来估计 advantage，再更新模型策略。</p><p>它和 PPO 的主要区别是，PPO 通常需要单独的 value model 或 critic 来估计 baseline，而 GRPO 不训练 value model，而是用同一组回答的平均奖励或标准化奖励作为 baseline。比如某个回答得分高于组内平均，就提高它的生成概率；低于组内平均，就降低它的生成概率。</p><p>简单说，PPO 是“用价值模型估计这个动作好不好”，GRPO 是“同一道题生成多个答案，和组内其他答案比谁更好”。GRPO 减少了 value model 带来的显存和训练成本，流程更简单，特别适合数学、代码、推理这类可以对多个答案进行打分比较的任务。</p></blockquote>"},{"t":"为什么DeepSeek使用GRPO？","tag":"","p":"","html":"<ol><li>为什么 DeepSeek 使用 GRPO？</li></ol><p>DeepSeek 使用 GRPO 的核心原因是：GRPO 比 PPO 更省资源、更简单，尤其适合大模型在数学、代码、推理任务上的强化学习训练。</p><ol><li>先说背景</li></ol><p>传统 RLHF / RL 训练里，常用 PPO。</p><p>PPO 通常需要：</p><ul><li>Policy Model：要优化的模型</li><li>Reference Model：防止模型跑偏</li><li>Reward Model：给回答打分</li><li>Value Model / Critic：估计价值函数</li><li>Advantage Estimation：计算优势</li></ul><p>问题是：</p><pre><code>链路重、显存高、训练复杂。</code></pre><p>对于大模型来说，额外训练和维护 Value Model 成本很高。</p><p>DeepSeek 在 DeepSeekMath 里提出 GRPO，用它训练 DeepSeekMath-RL 7B；公开资料里也说明 DeepSeekMath-RL 是基于 DeepSeekMath-Instruct 7B，使用 GRPO 训练得到的。(arxiv.org)</p><ol><li>GRPO 省掉 Value Model</li></ol><p>GRPO 最大特点是：</p><pre><code>不需要单独的 Critic / Value Model</code></pre><p>PPO 需要 Value Model 来估计 baseline。</p><p>GRPO 换了一种方式：</p><pre><code>同一个 prompt 生成多个回答<br/>用这组回答的平均奖励作为 baseline</code></pre><p>也就是：</p><pre><code>Advantage = 当前回答奖励 - 组内平均奖励</code></pre><p>这样就不需要额外训练价值模型。</p><ol><li>为什么这对大模型重要？</li></ol><p>因为大模型训练成本非常高。</p><p>少一个 Value Model，就能减少：</p><ul><li>显存占用</li><li>参数存储</li><li>训练计算量</li><li>工程复杂度</li><li>value model 训练不稳定问题</li></ul><p>尤其在几十亿、几百亿参数模型上，这个收益很明显。</p><p>所以 GRPO 更适合大模型后训练。</p><ol><li>适合数学和推理任务</li></ol><p>DeepSeek 很多强化学习训练重点是数学、代码、推理。</p><p>这些任务有一个特点：</p><pre><code>同一道题可以采样多个答案，并且可以比较好坏。</code></pre><p>比如一道数学题，模型生成 8 个答案：</p><pre><code>A 对了<br/>B 错了<br/>C 格式错<br/>D 推理对但算错<br/>...</code></pre><p>可以通过规则、答案校验或奖励模型打分。</p><p>GRPO 正好利用这种“组内比较”。</p><p>高于组平均的回答增强，低于组平均的回答削弱。</p><ol><li>训练更稳定</li></ol><p>GRPO 用组内相对奖励，而不是只看绝对奖励。</p><p>好处是：</p><pre><code>不同题目的难度差异被部分抵消。</code></pre><p>比如：</p><ul><li>简单题大家都得分高</li><li>难题大家都得分低</li></ul><p>如果只看绝对分数，不同 prompt 之间奖励尺度可能不一致。</p><p>组内相对比较可以让优化信号更稳定。</p><ol><li>更适合激发推理能力</li></ol><p>推理任务不是简单模仿标准答案。</p><p>模型需要通过探索发现更好的推理路径。</p><p>GRPO 通过同题多采样：</p><pre><code>生成多个推理过程<br/>比较哪个结果更好<br/>强化更优路径</code></pre><p>这样能鼓励模型产生更有效的 reasoning 行为。</p><p>这也是它在 DeepSeekMath、DeepSeek-R1 这类推理模型训练中被关注的原因。DeepSeekMath 论文提出 GRPO 是一种高效且有效的强化学习算法，核心就是用 group scores 估计 baseline，而不是 critic model。(arxiv.org)</p><ol><li>和 PPO 对比</li></ol><p>可以这样记：</p><pre><code>PPO：用 Value Model 估计这个回答比预期好多少<br/>GRPO：同题生成多个回答，用组内平均分判断谁更好</code></pre><p>对比一下：</p><sheet></sheet><ol><li>但 GRPO 也不是没有代价</li></ol><p>GRPO 的问题是：</p><ul><li>同一个 prompt 要采样多个回答</li><li>采样成本会增加</li><li>奖励函数质量很关键</li><li>如果一组回答都很差，相对比较效果有限</li><li>仍然需要 KL 约束防止模型跑偏</li><li>对开放式主观任务，奖励设计仍然困难</li></ul><p>所以它不是万能替代 PPO，而是在大模型推理训练里很划算。</p><ol><li><strong>面试背诵版：为什么 DeepSeek 使用 GRPO？</strong></li></ol><blockquote><p>DeepSeek 使用 GRPO，主要是因为它比 PPO 更轻量、更省显存、更适合大模型推理任务的强化学习训练。传统 PPO 通常需要额外的 Value Model 或 Critic 来估计 baseline，再计算 Advantage，这会增加显存、计算和工程复杂度。GRPO 直接取消 Value Model，而是对同一个 prompt 采样多个回答，用这组回答的平均奖励作为 baseline，通过“当前回答奖励减去组内平均奖励”来估计优势。</p><p>这种方式特别适合数学、代码和推理任务，因为同一道题可以生成多个答案，并通过规则、答案校验或奖励模型打分，然后比较组内哪个回答更好。高于组平均的回答提高概率，低于组平均的回答降低概率。这样既能降低 PPO 中 value model 的训练成本，又能利用同题多样本比较带来更稳定的优化信号。</p><p>简单说，PPO 是“靠 Critic 判断这个动作比预期好多少”，GRPO 是“同一道题生成多个答案，和组内平均水平比谁更好”。DeepSeek 使用 GRPO，是为了在大模型强化学习后训练中降低成本、简化流程，并更有效地激发数学和推理能力。</p></blockquote><p></p><hr/>"}]},{"no":"","title":"模块九：LLM推理优化（重点）","questions":[{"t":"LLM推理过程是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Prefill和Decode有什么区别？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Prefill 和 Decode 的核心区别是：<strong>Prefill 是一次性处理输入 prompt，建立 KV Cache；Decode 是在已有 KV Cache 基础上，一个 token 一个 token 生成输出。</strong></p><ol><li>Prefill 是什么？</li></ol><p>Prefill 是推理的第一阶段。</p><p>模型拿到完整输入 prompt 后，一次性处理所有输入 token。</p><p>比如用户输入：</p><pre><code>请解释 Transformer 的原理</code></pre><p>这些 token 会一起进入模型做前向计算。</p><p>Prefill 的目标是：</p><pre><code>理解输入上下文<br/>生成初始 KV Cache<br/>得到第一个输出 token 的概率</code></pre><ol><li>Decode 是什么？</li></ol><p>Decode 是生成阶段。</p><p>在 Prefill 后，模型开始逐 token 生成。</p><p>流程是：</p><pre><code>生成第 1 个 token<br/>  ↓<br/>把它加入上下文<br/>  ↓<br/>生成第 2 个 token<br/>  ↓<br/>继续生成</code></pre><p>每次 Decode 只新处理一个 token，但会利用之前保存的 KV Cache。</p><ol><li>KV Cache 的作用</li></ol><p>Transformer 生成时，每一层 Attention 都需要历史 token 的 Key 和 Value。</p><p>如果没有 KV Cache，每生成一个新 token 都要重新计算所有历史 token。</p><p>这会非常浪费。</p><p>KV Cache 会保存历史 token 的 K、V。</p><p>Decode 时只需要计算当前新 token 的 Q、K、V，然后用当前 Q 去 attend 历史 K、V。</p><ol><li>计算特点不同</li></ol><p>Prefill：</p><ul><li>输入 token 多</li><li>可以并行计算</li><li>主要是大矩阵计算</li><li>吞吐高</li><li>受 prompt 长度影响大</li></ul><p>Decode：</p><ul><li>每次只生成一个 token</li><li>自回归串行</li><li>不能完全并行</li><li>受输出长度影响大</li><li>常被 KV Cache 和内存带宽限制</li></ul><ol><li>延迟指标不同</li></ol><p>Prefill 主要影响：</p><pre><code>TTFT，Time To First Token</code></pre><p>也就是首 token 延迟。</p><p>输入 prompt 越长，Prefill 越慢，用户越晚看到第一个 token。</p><p>Decode 主要影响：</p><pre><code>TPOT，Time Per Output Token</code></pre><p>也就是每个输出 token 的生成速度。</p><p>输出越长，Decode 总耗时越长。</p><ol><li>举个例子</li></ol><p>用户输入 1000 个 token，模型输出 200 个 token。</p><p>Prefill 阶段：</p><pre><code>一次性处理 1000 个输入 token<br/>建立 KV Cache<br/>生成第一个 token</code></pre><p>Decode 阶段：</p><pre><code>循环 200 次左右<br/>每次生成一个 token</code></pre><p>所以输入长主要影响 Prefill，输出长主要影响 Decode。</p><ol><li>优化方向不同</li></ol><p>Prefill 优化：</p><ul><li>Prompt 缓存</li><li>FlashAttention</li><li>Batch prefill</li><li>Chunked prefill</li><li>减少输入 token</li><li>RAG 精简上下文</li></ul><p>Decode 优化：</p><ul><li>KV Cache 优化</li><li>PagedAttention</li><li>Speculative Decoding</li><li>Continuous Batching</li><li>MQA / GQA</li><li>量化</li><li>并行采样优化</li></ul><ol><li><strong>面试背诵版：Prefill 和 Decode 有什么区别？</strong></li></ol><blockquote><p>Prefill 和 Decode 是大模型自回归推理的两个阶段。Prefill 是第一阶段，模型一次性处理完整输入 prompt，计算所有输入 token 的 hidden states，并为每一层 Attention 生成 KV Cache，同时得到第一个输出 token 的概率。它可以并行处理输入序列，主要受输入长度影响，决定首 token 延迟，也就是 TTFT。</p><p>Decode 是第二阶段，模型在已有 KV Cache 的基础上逐 token 生成输出。每生成一个新 token，只需要计算这个 token 的 Q、K、V，并用它的 Q 去关注历史 KV Cache，然后得到下一个 token 概率。由于后一个 token 依赖前一个生成结果，所以 Decode 是自回归串行过程，主要受输出长度影响，决定每个 token 的生成速度，也就是 TPOT。</p><p>简单说，Prefill 是“读 prompt、建缓存、出首 token”，Decode 是“<strong>利用缓存，一个 token 一个 token 往后生成</strong>”。Prefill 更偏并行计算，Decode 更容易受 KV Cache、内存带宽和自回归串行限制。</p></blockquote>"},{"t":"为什么第一个Token慢？","tag":"","p":"","html":"<ol><li>为什么第一个 Token 慢？</li></ol><p>第一个 Token 慢，核心原因是：生成第一个 token 前，模型必须先完整处理输入 prompt，并建立 KV Cache，这个阶段叫 Prefill。</p><ol><li>第一个 Token 前要先读完整 prompt</li></ol><p>用户输入 prompt 后，模型不能马上生成。</p><p>它要先处理全部输入 token。</p><p>比如输入有：</p><pre><code>3000 个 token</code></pre><p>模型要先把这 3000 个 token 都经过多层 Transformer 前向计算。</p><p>只有理解完上下文后，才能预测第一个输出 token。</p><ol><li>Prefill 阶段计算量大</li></ol><p>第一个 token 的延迟主要来自 Prefill。</p><p>Prefill 要做：</p><pre><code>Tokenizer<br/>Embedding<br/>多层 Transformer 前向传播<br/>Attention 计算<br/>FFN 计算<br/>建立 KV Cache<br/>输出第一个 token 概率</code></pre><p>如果 prompt 很长，Prefill 计算就会很重。</p><ol><li>Attention 和输入长度相关</li></ol><p>Prefill 阶段要对输入序列做完整 Attention。</p><p>标准 Attention 复杂度和序列长度关系很强。</p><p>输入越长：</p><ul><li>Attention 计算越多</li><li>显存占用越大</li><li>中间结果越多</li><li>Prefill 越慢</li></ul><p>所以长 prompt 会显著增加首 token 延迟。</p><ol><li>要建立 KV Cache</li></ol><p>生成后续 token 时，可以复用 KV Cache。</p><p>但第一个 token 前，KV Cache 还没有。</p><p>所以模型必须先为 prompt 中所有 token 计算并保存每层的 K、V。</p><p>这个过程会占用时间和显存。</p><ol><li>后续 token 为什么快一些？</li></ol><p>后续 Decode 阶段每次只处理一个新 token。</p><p>而历史 token 的 K、V 已经缓存好了。</p><p>所以后续 token 不需要重新处理整个 prompt。</p><p>流程是：</p><pre><code>只算当前 token → 复用 KV Cache → 生成下一个 token</code></pre><p>因此通常第一个 token 比后续 token 慢。</p><ol><li>还受调度和排队影响</li></ol><p>线上服务中，第一个 token 还可能受：</p><ul><li>请求排队</li><li>batch 调度</li><li>GPU 负载</li><li>tokenizer 耗时</li><li>网络延迟</li><li>冷启动</li><li>权限和安全检查</li><li>RAG 检索时间</li></ul><p>所以用户感受到的首 token 慢，不一定全是模型计算。</p><ol><li>常见优化方法</li></ol><p>优化首 token 延迟可以做：</p><ul><li>减少 prompt token</li><li>Prompt Cache / Prefix Cache</li><li>Chunked Prefill</li><li>FlashAttention</li><li>Continuous Batching</li><li>KV Cache 预填充</li><li>RAG 只放相关片段</li><li>模型量化</li><li>更小模型</li><li>优化调度和排队</li></ul><ol><li><strong>面试背诵版：为什么第一个 Token 慢？</strong></li></ol><blockquote><p>第一个 token 慢，主要是因为模型在生成第一个 token 前必须先完成 Prefill 阶段。Prefill 会一次性处理完整输入 prompt，包括 token embedding、多层 Transformer 前向传播、Attention 和 FFN 计算，并为每一层、每个输入 token 建立 KV Cache。只有这些计算完成后，模型才能得到第一个输出 token 的概率。</p><p>如果输入 prompt 很长，Prefill 的计算量和显存开销都会增加，尤其 Attention 需要处理输入序列中 token 之间的关系，因此首 token 延迟会明显变高。后续 token 生成进入 Decode 阶段时，历史 token 的 K、V 已经在 KV Cache 中，只需要处理当前新 token，所以通常比第一个 token 快。</p><p>另外，线上系统中的首 token 延迟还可能包括请求排队、batch 调度、tokenizer、RAG 检索、网络延迟和安全检查。简单说，第一个 token 慢，是因为模型要先“读完整个 prompt、建好 KV Cache”，然后才能开始生成。</p></blockquote>"},{"t":"KV Cache是什么？","tag":"","p":"","html":"<ol><li>KV Cache 是什么？</li></ol><p>KV Cache 的核心是：在自回归生成时缓存历史 token 的 Key 和 Value，避免每生成一个 token 都重复计算整段上下文。</p><ol><li>为什么需要 KV Cache？</li></ol><p>大模型生成文本是逐 token 生成的。</p><p>比如：</p><pre><code>我 → 喜欢 → 机器 → 学习</code></pre><p>每生成一个新 token，都要基于前面所有 token 计算 Attention。</p><p>如果没有 KV Cache，每一步都要重新计算全部历史 token 的 K、V。</p><p>这会非常浪费计算。</p><ol><li>KV Cache 缓存什么？</li></ol><p>Transformer Attention 中有：</p><ul><li>Q：Query</li><li>K：Key</li><li>V：Value</li></ul><p>对于历史 token 来说，它们的 K 和 V 在生成后续 token 时不会变。</p><p>所以可以把历史 token 在每一层的 K、V 缓存下来。</p><p>这就是 KV Cache。</p><ol><li>Prefill 阶段做什么？</li></ol><p>Prefill 阶段会一次性处理输入 prompt。</p><p>同时为 prompt 中所有 token 建立 KV Cache。</p><p>比如 prompt 是：</p><pre><code>我 喜欢</code></pre><p>模型会缓存：</p><pre><code>我 的 K,V<br/>喜欢 的 K,V</code></pre><p>每一层都会有一份对应缓存。</p><ol><li>Decode 阶段怎么用？</li></ol><p>Decode 阶段每次只处理新生成的一个 token。</p><p>比如生成“机器”时：</p><pre><code>只计算“机器”的 Q,K,V</code></pre><p>然后用“机器”的 Q 去 attention 历史缓存里的 K,V：</p><pre><code>我、喜欢 的 K,V</code></pre><p>生成后，再把“机器”的 K,V 加入缓存。</p><p>下一步生成“学习”时，就可以使用：</p><pre><code>我、喜欢、机器 的 K,V</code></pre><ol><li>KV Cache 节省了什么？</li></ol><p>它节省的是历史 token 的重复计算。</p><p>没有 KV Cache：</p><pre><code>每一步重算整个上下文</code></pre><p>有 KV Cache：</p><pre><code>历史 K,V 复用，只算新 token</code></pre><p>所以 KV Cache 能显著提升自回归生成速度。</p><ol><li>KV Cache 的代价是什么？</li></ol><p>KV Cache 会占用显存。</p><p>显存占用和这些因素有关：</p><ul><li>batch size</li><li>序列长度</li><li>Transformer 层数</li><li>head 数量</li><li>head 维度</li><li>数据精度</li></ul><p>上下文越长、并发越高，KV Cache 越大。</p><p>长上下文推理时，KV Cache 常常是显存瓶颈。</p><ol><li>KV Cache 的常见优化</li></ol><p>常见优化包括：</p><ul><li>KV Cache 量化</li><li>PagedAttention</li><li>Prefix Cache</li><li>Multi-Query Attention</li><li>Grouped-Query Attention</li><li>Sliding Window Attention</li><li>Cache Eviction</li><li>CPU / GPU 分层缓存</li></ul><p>目标是降低显存占用，提高推理吞吐。</p><ol><li><strong>面试背诵版：KV Cache 是什么？</strong></li></ol><blockquote><p>KV Cache 是大语言模型自回归推理中的缓存机制，用来保存历史 token 在每一层 Attention 中计算得到的 Key 和 Value。因为生成过程中历史 token 的 K、V 不会变化，所以可以缓存起来，避免每生成一个新 token 都重新计算整个上下文。</p><p>在 Prefill 阶段，模型一次性处理输入 prompt，并为所有输入 token 建立 KV Cache；在 Decode 阶段，每次只处理一个新 token，计算它的 Q、K、V，然后用当前 token 的 Q 去关注缓存中的历史 K、V，生成下一个 token，并把新 token 的 K、V 加入缓存。</p><p>KV Cache 的优点是显著减少重复计算，提高生成速度；缺点是占用显存，且显存开销会随着 batch size、上下文长度、模型层数、head 数和 head 维度增加。简单说，KV Cache 是“用显存换计算”，让模型生成时不用每一步都重算历史 token。</p></blockquote>"},{"t":"KV Cache缓存什么？","tag":"","p":"","html":"<p>KV Cache 的核心是：在自回归生成时，把历史 token 的 Key 和 Value 缓存起来，避免每生成一个 token 都重复计算整段上下文。</p><ol><li>为什么需要 KV Cache？</li></ol><p>大语言模型生成文本是一个 token 一个 token 生成的。</p><p>比如：</p><pre><code>输入：我喜欢<br/>生成：机器<br/>再生成：学习</code></pre><p>每生成一个新 token，都要基于前面的所有 token。</p><p>如果没有 KV Cache，每一步都要重新计算全部历史 token 的 Key 和 Value。</p><p>这会非常浪费。</p><ol><li>KV Cache 缓存什么？</li></ol><p>Transformer Attention 中有 Q、K、V。</p><p>在生成时，对于历史 token，它们的 K 和 V 不会变。</p><p>所以可以把每一层、每个历史 token 的 K、V 存下来。</p><p>这就是 KV Cache。</p><p>Decode 时只需要计算当前新 token 的 Q、K、V，然后把新的 K、V 加入缓存。</p><ol><li>KV Cache 怎么工作？</li></ol><p>假设 prompt 是：</p><pre><code>我 喜欢</code></pre><p>Prefill 阶段会计算并缓存：</p><pre><code>我 的 K,V<br/>喜欢 的 K,V</code></pre><p>生成“机器”时，只需要计算“机器”的 Q,K,V。</p><p>然后“机器”的 Q 去关注：</p><pre><code>我、喜欢 的 K,V</code></pre><p>生成后再把“机器”的 K,V 加入缓存。</p><p>下一步生成“学习”时，就可以使用：</p><pre><code>我、喜欢、机器 的 K,V</code></pre><ol><li>它节省了什么？</li></ol><p>KV Cache 节省的是历史 token 的重复计算。</p><p>没有 KV Cache：</p><pre><code>每一步都重新算所有历史 token</code></pre><p>有 KV Cache：</p><pre><code>历史 K,V 直接复用，只算新 token</code></pre><p>这能大幅提升自回归生成效率。</p><ol><li>KV Cache 的代价</li></ol><p>KV Cache 虽然省计算，但会占显存。</p><p>缓存规模和这些因素有关：</p><ul><li>batch size</li><li>序列长度</li><li>层数</li><li>attention head 数</li><li>head 维度</li><li>数据精度</li></ul><p>上下文越长、并发越高，KV Cache 越大。</p><p>长上下文推理时，KV Cache 往往是显存瓶颈。</p><ol><li>KV Cache 对 Prefill 和 Decode 的影响</li></ol><p>Prefill 阶段：</p><pre><code>处理完整 prompt，并建立 KV Cache</code></pre><p>Decode 阶段：</p><pre><code>每步复用已有 KV Cache，只计算新 token</code></pre><p>所以 KV Cache 主要优化 Decode 阶段。</p><ol><li>常见优化方式</li></ol><p>KV Cache 优化包括：</p><ul><li>KV Cache 量化</li><li>PagedAttention</li><li>Multi-Query Attention</li><li>Grouped-Query Attention</li><li>Sliding Window</li><li>Cache eviction</li><li>Prefix Cache</li><li>分层缓存</li><li>Offload 到 CPU / 内存</li></ul><p>目标是降低显存占用，提高吞吐。</p><ol><li><strong>面试背诵版：KV Cache 是什么？</strong></li></ol><blockquote><p>KV Cache 是大语言模型自回归推理中的缓存机制，用来保存历史 token 在每一层 Attention 中计算得到的 Key 和 Value。因为生成过程中历史 token 的 K、V 不会变化，所以可以缓存起来，避免每生成一个新 token 都重新计算整段上下文。</p><p>在 Prefill 阶段，模型一次性处理输入 prompt，并为所有输入 token 建立 KV Cache；在 Decode 阶段，每次只处理新生成的一个 token，计算它的 Q、K、V，然后用当前 token 的 Q 去关注缓存中的历史 K、V，生成下一个 token，并把新 token 的 K、V 加入缓存。</p><p>KV Cache 的优点是大幅减少重复计算，提高生成速度；缺点是占用显存，且显存开销会随 batch size、序列长度、模型层数、head 数和 head 维度增加而增长。简单说，<strong>KV Cache 是“用显存换计算”，让自回归生成不用每一步都重算历史上下文。</strong></p></blockquote>"},{"t":"为什么KV Cache能够提升速度？","tag":"","p":"","html":"<ol><li>为什么 KV Cache 能够提升速度？</li></ol><p>KV Cache 能提升速度，核心原因是：它复用了历史 token 的 Key 和 Value，避免每生成一个新 token 都重新计算整段上下文。</p><ol><li>自回归生成是逐 token 的</li></ol><p>大语言模型生成时通常是：</p><pre><code>输入 prompt → 生成第 1 个 token → 生成第 2 个 token → ...</code></pre><p>每一步生成都要参考前面所有 token。</p><p>如果没有缓存，每一步都要把完整上下文重新送进模型计算。</p><p>这会产生大量重复计算。</p><ol><li>历史 token 的 K、V 不会变</li></ol><p>在 Attention 里，每个 token 会产生：</p><pre><code>Q、K、V</code></pre><p>当一个 token 已经生成后，它的 K 和 V 在后续生成中是固定的。</p><p>比如已经有：</p><pre><code>我 喜欢</code></pre><p>生成下一个 token 时，“我”和“喜欢”的 K、V 不会变。</p><p>所以可以缓存下来复用。</p><ol><li>没有 KV Cache 会怎样？</li></ol><p>假设已经生成 1000 个 token。</p><p>现在要生成第 1001 个 token。</p><p>如果没有 KV Cache，需要重新计算：</p><pre><code>前 1000 个 token 的 K、V</code></pre><p>下一步生成第 1002 个 token，又要重新计算前 1001 个 token。</p><p>这非常浪费。</p><ol><li>有 KV Cache 会怎样？</li></ol><p>有 KV Cache 后，历史 K、V 直接拿来用。</p><p>生成新 token 时只需要：</p><pre><code>计算当前 token 的 Q、K、V<br/>当前 Q attend 历史 K、V<br/>把当前 K、V 加入缓存</code></pre><p>这样历史 token 不需要重复计算。</p><ol><li>计算复杂度更低</li></ol><p>没有 KV Cache 时，自回归生成 T 个 token，会反复重算历史。</p><p>成本接近：</p><pre><code>O(T²) 甚至更高的重复前向开销</code></pre><p>有 KV Cache 后，每一步只新增一个 token 的计算。</p><p>虽然当前 token 仍要 attend 到所有历史 K、V，但不用重新跑历史 token 的投影和层计算。</p><p>所以 Decode 速度大幅提升。</p><ol><li>对长输出更明显</li></ol><p>输出越长，KV Cache 的收益越明显。</p><p>因为历史越长，重复计算越多。</p><p>没有缓存时，长文本生成会越来越慢。</p><p>有缓存后，每步只处理新 token，生成速度稳定很多。</p><ol><li>代价是显存占用</li></ol><p>KV Cache 不是免费。</p><p>它用显存保存每层每个 token 的 K、V。</p><p>所以：</p><pre><code>速度提升 = 用显存换计算</code></pre><p>并发高、上下文长时，KV Cache 会成为显存瓶颈。</p><ol><li><strong>面试背诵版：为什么 KV Cache 能够提升速度？</strong></li></ol><blockquote><p>KV Cache 能提升速度，是因为大模型自回归生成时每一步都需要参考历史 token，而历史 token 在每层 Attention 中计算出的 Key 和 Value 是固定不变的。如果没有 KV Cache，每生成一个新 token，都要重新计算整段上下文中所有历史 token 的 K、V 和相关中间结果，造成大量重复计算。</p><p>有了 KV Cache 后，Prefill 阶段会把输入 prompt 的 K、V 缓存起来；Decode 阶段每生成一个新 token，只需要计算当前 token 的 Q、K、V，然后用当前 Q 去关注缓存中的历史 K、V，再把当前 token 的 K、V 加入缓存。这样历史 token 的 K、V 可以直接复用，不需要每一步重算。</p><p>所以 KV Cache 本质上是“用显存换计算”，减少重复前向计算，显著提升自回归生成速度，尤其是长输出场景收益更明显。但代价是显存占用会随着上下文长度、batch size、层数和 head 数增加。</p></blockquote>"},{"t":"KV Cache为什么占用大量显存？","tag":"","p":"","html":"<ol><li>KV Cache 为什么占用大量显存？</li></ol><p>KV Cache 占用大量显存，核心原因是：它要为每一层、每个 token、每个 attention head 都保存 Key 和 Value，而且上下文越长、并发越高，缓存越大。</p><ol><li>KV Cache 保存什么？</li></ol><p>KV Cache 保存的是历史 token 的：</p><pre><code>Key 和 Value</code></pre><p>并且不是只保存一份。</p><p>它要在 Transformer 的每一层都保存。</p><p>如果模型有 32 层，那么每个 token 都要保存 32 层的 K 和 V。</p><ol><li>和序列长度成正比</li></ol><p>上下文越长，需要缓存的 token 越多。</p><p>比如：</p><pre><code>1000 token</code></pre><p>要缓存 1000 个 token 的 K、V。</p><p>如果变成：</p><pre><code>8000 token</code></pre><p>KV Cache 大约增长 8 倍。</p><p>所以长上下文会显著增加显存占用。</p><ol><li>和 batch size 成正比</li></ol><p>一次并发处理多个请求时，每个请求都有自己的 KV Cache。</p><p>比如：</p><pre><code>batch size = 16</code></pre><p>就要同时保存 16 个序列的 K、V。</p><p>batch 越大，并发越高，KV Cache 越大。</p><ol><li>和模型层数有关</li></ol><p>模型越深，Transformer 层数越多。</p><p>每一层都有自己的 Attention。</p><p>每一层都需要缓存 K 和 V。</p><p>所以 KV Cache 和层数成正比：</p><pre><code>层数越多，缓存越大</code></pre><ol><li>和 head 数、head 维度有关</li></ol><p>每个 token 的 K、V 维度和注意力头相关。</p><p>大概包括：</p><ul><li>num_heads</li><li>head_dim</li></ul><p>一般每层每个 token 需要保存：</p><pre><code>K: num_heads × head_dim<br/>V: num_heads × head_dim</code></pre><p>也就是 K 和 V 两份。</p><ol><li>和数据精度有关</li></ol><p>KV Cache 通常用 FP16、BF16、FP8 或 INT8 存储。</p><p>精度越高，每个元素占用字节越多。</p><p>比如：</p><ul><li>FP16 / BF16：2 bytes</li><li>FP8：1 byte</li><li>INT8：1 byte</li></ul><p>用低精度 KV Cache 可以节省显存。</p><ol><li>粗略公式</li></ol><p>KV Cache 显存大致和这些因素成正比：</p><pre><code>batch_size × seq_len × num_layers × 2 × num_kv_heads × head_dim × bytes</code></pre><p>其中：</p><pre><code>2 表示 K 和 V 两份</code></pre><p>所以任何一个维度变大，显存都会涨。</p><ol><li>为什么长上下文特别贵？</li></ol><p>假设：</p><ul><li>batch size 大</li><li>seq_len 长</li><li>层数多</li><li>head 数多</li><li>用 FP16</li></ul><p>KV Cache 会非常大。</p><p>这就是长上下文、多并发推理时，显存瓶颈常常不是模型权重，而是 KV Cache 的原因。</p><ol><li><strong>面试背诵版：KV Cache 为什么占用大量显存？</strong></li></ol><blockquote><p>KV Cache 占用大量显存，是因为它需要保存历史 token 在每一层 Attention 中的 Key 和 Value。对于每个 token，不是只保存一份 K、V，而是每一层都要保存；每层还要保存多个 head 的 K 和 V，所以缓存规模会随着序列长度、batch size、模型层数、KV head 数、head 维度和数据精度一起增长。</p><p>粗略公式可以写成：batch_size × seq_len × num_layers × 2 × num_kv_heads × head_dim × bytes，其中 2 表示 Key 和 Value 两份。上下文越长，seq_len 越大；并发越高，batch size 越大；模型越深，num_layers 越大，KV Cache 显存就越高。</p><p>所以长上下文和高并发推理时，KV Cache 往往会成为显存瓶颈。它本质上是“用显存换计算”，通过保存历史 K、V 来减少重复计算，但代价就是显存占用很大。常见优化包括 KV Cache 量化、PagedAttention、MQA、GQA、滑动窗口和 Cache Eviction。</p></blockquote>"},{"t":"FlashAttention解决什么问题？","tag":"","p":"","html":"<p>FlashAttention 的核心是：减少标准 Attention 在计算时的显存读写和中间矩阵存储，让 Attention 更快、更省显存。</p><ol><li>标准 Attention 的问题</li></ol><p>标准 Attention 公式是：</p><pre><code>softmax(QKᵀ / √d)V</code></pre><p>问题在于它会显式生成一个很大的 attention 矩阵：</p><pre><code>seq_len × seq_len</code></pre><p>如果序列长度是 n，这个矩阵大小就是：</p><pre><code>O(n²)</code></pre><p>长序列时，显存占用非常大。</p><ol><li>显存读写是瓶颈</li></ol><p>GPU 计算很快，但显存读写相对慢。</p><p>标准 Attention 会频繁把中间结果写入显存、再从显存读回来。</p><p>比如：</p><ul><li>QKᵀ 分数矩阵</li><li>softmax 后的 attention 权重矩阵</li><li>dropout 中间结果</li><li>再乘 V 的中间结果</li></ul><p>这些都会造成大量 HBM 读写。</p><p>FlashAttention 主要优化的就是这个瓶颈。</p><ol><li>FlashAttention 怎么做？</li></ol><p>FlashAttention 不把完整 attention 矩阵一次性存到显存。</p><p>它把 Q、K、V 分块。</p><p>在 GPU 的高速 SRAM 里分块计算 attention。</p><p>计算过程中使用 online softmax 技术，边算边更新结果。</p><p>最后只写回最终输出。</p><p>简单说：</p><pre><code>标准 Attention：存完整 n×n 矩阵<br/>FlashAttention：分块计算，不存完整矩阵</code></pre><ol><li>它解决的是精确 Attention，不是近似 Attention</li></ol><p>这一点很重要。</p><p>FlashAttention 不是 Sparse Attention，也不是 Linear Attention。</p><p>它不改变 Attention 的数学结果。</p><p>它算出来的结果和标准 Attention 等价。</p><p>区别只是计算方式更高效。</p><ol><li>优点是什么？</li></ol><p>FlashAttention 的优点：</p><ul><li>显存占用更低</li><li>训练更快</li><li>推理更快</li><li>支持更长上下文</li><li>减少 HBM 读写</li><li>保持 Attention 精确结果</li><li>对大模型训练很有价值</li></ul><p>尤其在长序列场景下收益明显。</p><ol><li>为什么叫 Flash？</li></ol><p>因为它充分利用 GPU 内部更快的 SRAM，减少慢速显存访问。</p><p>计算并不是单纯减少 FLOPs，而是减少 memory I/O。</p><p>所以它属于 IO-aware attention。</p><p>也就是根据硬件内存层级优化 Attention。</p><ol><li>它不能解决什么？</li></ol><p>FlashAttention 不能从根本上改变 Attention 的理论计算复杂度。</p><p>标准 Self-Attention 仍然需要考虑 token 两两关系。</p><p>所以计算复杂度本质上仍和 n² 有关。</p><p>它主要是：</p><pre><code>降低显存占用<br/>减少显存读写<br/>提高实际速度</code></pre><p>而不是把 O(n²) 变成 O(n)。</p><ol><li><strong>面试背诵版：FlashAttention 解决什么问题？</strong></li></ol><blockquote><p>FlashAttention 主要解决标准 Attention 显存占用大和显存读写开销高的问题。标准 Attention 在计算 softmax(QKᵀ / √d)V 时，会显式生成 seq_len × seq_len 的注意力分数矩阵和 softmax 权重矩阵，序列长度变长时显存开销是 O(n²)，并且大量中间结果需要在 GPU 显存 HBM 中反复读写，导致训练和推理变慢。</p><p>FlashAttention 的做法是把<strong> Q、K、V 分块，在 GPU 更快的 SRAM 中分块计算 Attention，并使用 online softmax 边计算边更新结果，避免把完整的 n×n attention 矩阵写入显存。</strong>它减少的是 memory I/O 和中间矩阵存储，而不是通过近似来减少计算。</p><p>需要注意，FlashAttention 计算的是精确 Attention，不是 Sparse Attention 或 Linear Attention，结果和标准 Attention 等价。它的优势是更快、更省显存、支持更长上下文；但它没有从理论上把 Attention 的 O(n²) 复杂度变成 O(n)，只是让实际计算更高效。</p></blockquote>"},{"t":"PagedAttention是什么？","tag":"","p":"","html":"<ol><li>PagedAttention 是什么？</li></ol><p>PagedAttention 的核心是：像操作系统分页管理内存一样管理 KV Cache，减少显存碎片，提高大模型推理吞吐。</p><ol><li>PagedAttention 解决什么问题？</li></ol><p>大模型推理时，KV Cache 很占显存。</p><p>而且不同请求的长度不一样：</p><pre><code>请求 A：100 token<br/>请求 B：2000 token<br/>请求 C：800 token</code></pre><p>如果直接为每个请求分配连续显存，很容易出现：</p><ul><li>显存碎片</li><li>预留空间浪费</li><li>动态扩容困难</li><li>batch 调度效率低</li><li>长短请求混合时利用率差</li></ul><p>PagedAttention 就是为了解决 KV Cache 管理效率问题。</p><ol><li>它的核心思想</li></ol><p>PagedAttention 借鉴操作系统虚拟内存分页思想。</p><p>不是给每个请求分配一整块连续 KV Cache。</p><p>而是把 KV Cache 切成固定大小的 block / page。</p><p>每个请求需要多少，就分配多少页。</p><p>比如：</p><pre><code>请求 A 用 3 页<br/>请求 B 用 40 页<br/>请求 C 用 12 页</code></pre><p>这些页在物理显存里可以不连续。</p><ol><li>为什么能减少浪费？</li></ol><p>传统方式可能要给请求预留最大长度。</p><p>比如一个请求可能最多生成 2048 token，就提前分配很大空间。</p><p>但很多请求实际生成不了那么长。</p><p>PagedAttention 按需分配 page：</p><pre><code>生成多少 token，就逐步分配多少 cache block</code></pre><p>这样减少显存浪费。</p><ol><li>为什么能减少碎片？</li></ol><p>因为所有 KV Cache 都按固定大小 block 管理。</p><p>释放和复用更容易。</p><p>类似操作系统里页表管理内存。</p><p>即使物理显存不连续，也可以通过映射表把它组织成一个逻辑连续的序列。</p><ol><li>对 Attention 计算有什么影响？</li></ol><p>PagedAttention 会维护：</p><pre><code>逻辑 token 位置 → 物理 KV block</code></pre><p>Attention 计算时，模型根据映射去读取对应 KV Cache。</p><p>这样不要求 KV Cache 在显存中连续存储。</p><p>这使得动态 batching 和长短请求混合更高效。</p><ol><li>和 vLLM 的关系</li></ol><p>PagedAttention 是 vLLM 中的关键技术。</p><p>vLLM 用它高效管理 KV Cache，从而提高推理吞吐。</p><p>尤其适合：</p><ul><li>多用户并发</li><li>长上下文</li><li>输出长度不确定</li><li>continuous batching</li><li>在线服务</li></ul><ol><li>优点</li></ol><p>PagedAttention 的优点：</p><ul><li>减少 KV Cache 显存浪费</li><li>减少显存碎片</li><li>支持更高并发</li><li>提高 GPU 利用率</li><li>支持动态 batch</li><li>对长短请求混合更友好</li><li>提高推理吞吐</li></ul><ol><li><strong>面试背诵版：PagedAttention 是什么？</strong></li></ol><blockquote><p>PagedAttention 是一种用于大模型推理的 KV Cache 管理技术，核心思想是借鉴操作系统分页机制，把 KV Cache 切分成固定大小的 block 或 page，而不是为每个请求分配一整块连续显存。每个请求根据实际生成长度按需分配若干 page，并通过映射表维护逻辑 token 位置到物理 KV block 的关系。</p><p>它主要解决 KV Cache 显存浪费和显存碎片问题。传统连续分配方式在请求长度不同、输出长度不确定、多请求并发时容易预留过多显存或产生碎片；PagedAttention 通过固定块分页、按需分配和复用，让物理显存可以不连续，但逻辑上仍能组织成完整上下文。</p><p>PagedAttention 是 vLLM 的关键技术之一，特别适合长上下文、多用户并发和 continuous batching 场景。简单说，它不是改变 Attention 数学公式，而是更高效地管理 KV Cache，从而提高显存利用率和推理吞吐。</p></blockquote>"},{"t":"Continuous Batching是什么？","tag":"","p":"","html":"<ol><li>Continuous Batching 是什么？</li></ol><p>Continuous Batching 的核心是：推理服务运行过程中动态把新请求加入 batch，把已完成请求移出 batch，提高 GPU 利用率和吞吐。</p><ol><li>普通 batching 的问题</li></ol><p>传统 batching 通常是静态的。</p><p>比如收集一批请求：</p><pre><code>请求 A、B、C、D</code></pre><p>组成一个 batch 一起推理。</p><p>问题是每个请求长度不同。</p><p>有的请求很快结束，有的请求生成很长。</p><p>如果必须等整个 batch 都结束，短请求会被长请求拖住。</p><p>这会导致：</p><ul><li>GPU 空转</li><li>延迟增加</li><li>吞吐下降</li><li>batch 利用率低</li></ul><ol><li>Continuous Batching 是什么？</li></ol><p>Continuous Batching 也叫动态批处理、迭代级批处理。</p><p>它不是等一个 batch 全部完成后再处理下一批。</p><p>而是在每个 decode step 动态调度。</p><p>流程是：</p><pre><code>已完成请求移出 batch<br/>新来的请求加入 batch<br/>剩余请求继续 decode</code></pre><p>这样 GPU 一直尽量保持满负载。</p><ol><li>为什么适合 LLM 推理？</li></ol><p>LLM 生成是逐 token 的。</p><p>Decode 阶段是：</p><pre><code>一步生成一个 token</code></pre><p>这给动态调度提供了机会。</p><p>每生成一步，都可以检查：</p><ul><li>哪些请求结束了</li><li>哪些新请求来了</li><li>当前 batch 还能塞多少请求</li></ul><p>然后重新组织 batch。</p><ol><li>举个例子</li></ol><p>假设 batch 里有 3 个请求：</p><pre><code>A 还要生成 2 token<br/>B 还要生成 100 token<br/>C 还要生成 5 token</code></pre><p>普通 batching 可能要等 B 结束。</p><p>Continuous Batching 中：</p><pre><code>A 结束后立刻移出<br/>新请求 D 立刻加入<br/>C 结束后移出<br/>新请求 E 加入<br/>B 继续生成</code></pre><p>这样 GPU 不会因为 A、C 提前结束而浪费位置。</p><ol><li>优点</li></ol><p>Continuous Batching 的优点：</p><ul><li>提高 GPU 利用率</li><li>提高吞吐</li><li>降低平均等待时间</li><li>更适合多用户并发</li><li>更适合输出长度不确定</li><li>长短请求混合时效率更好</li></ul><p>在线推理服务中非常重要。</p><ol><li>和 PagedAttention 的关系</li></ol><p>Continuous Batching 需要高效管理每个请求的 KV Cache。</p><p>因为请求会动态加入和退出。</p><p>PagedAttention 可以像分页一样管理 KV Cache。</p><p>所以两者经常配合使用。</p><p>比如 vLLM 就是：</p><pre><code>PagedAttention + Continuous Batching</code></pre><p>来提升推理吞吐。</p><ol><li>挑战</li></ol><p>Continuous Batching 也有复杂点：</p><ul><li>调度逻辑复杂</li><li>KV Cache 管理复杂</li><li>请求长度差异大</li><li>公平性控制</li><li>优先级调度</li><li>首 token 延迟和吞吐权衡</li><li>显存容量管理</li></ul><p>所以它是推理系统层面的优化。</p><ol><li><strong>面试背诵版：Continuous Batching 是什么？</strong></li></ol><blockquote><p>Continuous Batching，也叫动态批处理或迭代级批处理，是大模型推理服务中的一种调度技术。它的核心思想是在推理过程中动态维护 batch：已经生成完成的请求及时移出 batch，新到来的请求可以在后续 decode step 中加入 batch，而不是等当前整个 batch 全部结束后再处理下一批。</p><p>它特别适合 LLM 推理，因为自回归生成是逐 token 进行的，每个 decode step 都有机会重新调度请求。传统静态 batching 中，如果 batch 里有长短请求混合，短请求完成后可能要等待长请求，导致 GPU 利用率下降；Continuous Batching 可以让短请求完成后立即释放位置，并加入新请求，从而提高吞吐和 GPU 利用率。</p><p>它通常和 PagedAttention 配合使用，因为动态加入和移除请求需要高效管理 KV Cache。简单说，Continuous Batching 是“边生成边动态组 batch”，解决的是在线推理中请求长度不同、到达时间不同导致的 GPU 利用率低问题。</p></blockquote>"},{"t":"vLLM为什么快？","tag":"","p":"","html":"<ol><li>vLLM 为什么快？</li></ol><p>vLLM 快的核心原因是：它通过 PagedAttention 高效管理 KV Cache，并结合 Continuous Batching 提高 GPU 利用率和推理吞吐。</p><ol><li>LLM 推理瓶颈在哪里？</li></ol><p>大模型推理通常瓶颈包括：</p><ul><li>KV Cache 占用大</li><li>请求长度不一致</li><li>输出长度不确定</li><li>batch 利用率低</li><li>显存碎片</li><li>GPU 计算资源利用不满</li><li>decode 阶段逐 token 串行</li></ul><p>vLLM 的优化重点主要在推理系统层。</p><ol><li>PagedAttention 提高 KV Cache 利用率</li></ol><p>vLLM 的核心技术之一是 PagedAttention。</p><p>它像操作系统分页一样管理 KV Cache。</p><p>不是给每个请求分配连续大块显存，而是把 KV Cache 分成固定大小 block。</p><p>按需分配、动态复用。</p><p>这样可以：</p><ul><li>减少显存碎片</li><li>减少 KV Cache 浪费</li><li>支持更高并发</li><li>更好处理长短请求混合</li></ul><ol><li>Continuous Batching 提高吞吐</li></ol><p>vLLM 支持 Continuous Batching。</p><p>也就是：</p><pre><code>请求完成就移出<br/>新请求可以动态加入</code></pre><p>不需要等一个 batch 全部结束。</p><p>这能让 GPU 持续保持更高利用率。</p><p>特别适合在线服务中请求长度不一致的场景。</p><ol><li>更高并发能力</li></ol><p>因为 KV Cache 管理更高效，显存浪费更少。</p><p>同样 GPU 显存下，vLLM 可以容纳更多并发请求。</p><p>并发上来后，GPU 更容易被打满。</p><p>所以整体吞吐更高。</p><ol><li>减少显存碎片</li></ol><p>传统推理系统给请求分配 KV Cache 时容易产生碎片。</p><p>尤其是不同请求长度差异大时。</p><p>PagedAttention 用固定 block 管理缓存。</p><p>释放和复用更灵活。</p><p>这让显存利用率更高。</p><ol><li>对长输出更友好</li></ol><p>LLM 输出长度通常不确定。</p><p>有些请求只生成几十 token，有些生成几千 token。</p><p>vLLM 按需分配 KV Cache，不需要一开始预留最大长度。</p><p>所以对长短混合请求更友好。</p><ol><li>系统层优化</li></ol><p>除了 PagedAttention 和 batching，vLLM 还会有一些推理系统优化，比如：</p><ul><li>高效调度</li><li>CUDA kernel 优化</li><li>prefix caching</li><li>tensor parallel 支持</li><li>speculative decoding 支持</li><li>OpenAI-compatible serving</li><li>流式输出支持</li></ul><p>这些一起提升服务效率。</p><ol><li><strong>面试背诵版：vLLM 为什么快？</strong></li></ol><blockquote><p>vLLM 快，主要是因为它在大模型推理系统层做了优化，核心技术是 PagedAttention 和 Continuous Batching。LLM 推理中很大的瓶颈是 KV Cache 占用显存多、请求长度不一致、输出长度不确定，导致显存浪费、碎片化和 GPU 利用率不足。</p><p>PagedAttention 借鉴操作系统分页思想，把 KV Cache 切成固定大小的 block，按需分配和复用，而不是为每个请求分配连续大块显存。这样可以减少显存碎片和 KV Cache 浪费，在同样显存下支持更高并发。Continuous Batching 则允许推理过程中动态把已完成请求移出 batch，把新请求加入 batch，不需要等整个 batch 全部完成，从而提高 GPU 利用率和吞吐。</p><p>简单说，vLLM 快不是因为改变了模型本身，而是更高效地管理 KV Cache 和 batch 调度，让 GPU 更少空转、显存利用更高、并发吞吐更大。尤其在长上下文、多用户并发、输出长度不确定的在线服务场景中收益明显。</p></blockquote>"},{"t":"Speculative Decoding是什么？","tag":"","p":"","html":"<ol><li>Speculative Decoding 是什么？</li></ol><p>Speculative Decoding 的核心是：用一个小模型先快速草拟多个 token，再让大模型一次性验证，从而减少大模型逐 token 生成次数。</p><ol><li>为什么需要 Speculative Decoding？</li></ol><p>大模型生成是自回归的。</p><p>也就是：</p><pre><code>一次生成一个 token</code></pre><p>每个 token 都要经过一次大模型前向计算。</p><p>这会导致：</p><ul><li>解码慢</li><li>延迟高</li><li>吞吐受限</li><li>大模型计算成本高</li></ul><p>Speculative Decoding 就是为了加速生成。</p><ol><li>基本思想</li></ol><p>使用两个模型：</p><ul><li>Draft Model：小模型，速度快</li><li>Target Model：大模型，质量高</li></ul><p>小模型先根据上下文快速生成多个候选 token。</p><p>然后大模型一次性验证这些 token 是否可以接受。</p><p>如果接受，就一次推进多个 token。</p><p>如果不接受，就从不接受的位置回退或采样大模型结果。</p><ol><li>流程是什么？</li></ol><p>大致流程是：</p><pre><code>当前上下文<br/>  ↓<br/>小模型草拟 k 个 token<br/>  ↓<br/>大模型并行计算这 k 个 token 的概率<br/>  ↓<br/>逐个判断是否接受<br/>  ↓<br/>接受多个 token 或回退<br/>  ↓<br/>继续下一轮</code></pre><p>核心是减少大模型调用次数。</p><ol><li>为什么能加速？</li></ol><p>正常生成 5 个 token：</p><pre><code>大模型跑 5 次</code></pre><p>Speculative Decoding 中，小模型先生成 5 个 token，大模型可以一次验证这 5 个 token。</p><p>如果大部分被接受：</p><pre><code>大模型跑 1 次，就生成多个 token</code></pre><p>小模型便宜，所以整体更快。</p><ol><li>为什么结果仍然正确？</li></ol><p>标准 Speculative Decoding 设计了接受-拒绝采样机制。</p><p>它可以保证最终采样分布和直接用大模型采样一致。</p><p>也就是说，它不是简单让小模型替代大模型。</p><p>小模型只是提议。</p><p>最终由大模型决定接受与否。</p><ol><li>什么时候效果好？</li></ol><p>效果取决于小模型草稿质量。</p><p>如果小模型和大模型输出分布接近，接受率高，加速明显。</p><p>适合：</p><ul><li>小模型预测比较准</li><li>生成内容较常规</li><li>大模型 decode 成本高</li><li>延迟敏感场景</li></ul><p>如果小模型草稿经常被拒绝，加速效果就差。</p><ol><li>变体</li></ol><p>除了小模型草拟，也有其他方式：</p><ul><li>n-gram speculative decoding</li><li>Medusa 多头草拟</li><li>EAGLE</li><li>Lookahead Decoding</li><li>Self-speculative decoding</li></ul><p>核心都是：</p><pre><code>尽量一次验证多个未来 token，减少大模型逐 token 解码。</code></pre><ol><li><strong>面试背诵版：Speculative Decoding 是什么？</strong></li></ol><blockquote><p>Speculative Decoding 是一种加速大模型自回归生成的方法。普通大模型生成时需要一个 token 一个 token 解码，每生成一个 token 都要跑一次大模型前向计算，延迟较高。Speculative Decoding 引入一个更小、更快的 draft model，让小模型先快速生成多个候选 token，然后由大模型一次性并行验证这些候选 token。</p><p>如果大模型认为这些候选 token 符合自己的分布，就可以一次接受多个 token，从而减少大模型 decode 次数；如果某个 token 不被接受，就从该位置回退或按大模型分布重新采样。标准方法通过接受-拒绝机制保证最终输出分布和直接使用大模型采样一致，因此小模型只是“提议”，大模型仍然负责最终质量。</p><p>它的加速效果取决于 draft model 的质量和接受率。如果小模型和大模型分布接近，接受率高，就能显著加速；如果草稿经常被拒绝，收益会下降。简单说，Speculative Decoding 是“让小模型先猜，大模型批量验，验过就一次生成多个 token”。</p></blockquote><p></p>"}]},{"no":"","title":"模块十：模型压缩与量化","questions":[{"t":"为什么需要模型量化？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>需要模型量化的核心原因是：<strong>大模型参数量太大，直接用高精度部署成本高；量化可以降低显存占用、提升推理速度、降低部署成本。</strong></p><ol><li>模型量化是什么？</li></ol><p>模型量化就是把模型参数或计算从高精度表示转成低精度表示。</p><p>比如从：</p><pre><code>FP32 / FP16 / BF16</code></pre><p>变成：</p><pre><code>INT8 / INT4 / FP8</code></pre><p>原来一个参数可能占 16 bit 或 32 bit。</p><p>量化后可能只占 8 bit 或 4 bit。</p><ol><li>降低显存占用</li></ol><p>大模型参数很多。</p><p>比如 70B 模型，如果用 FP16：</p><pre><code>70B × 2 bytes ≈ 140GB</code></pre><p>如果量化到 INT4：</p><pre><code>70B × 0.5 bytes ≈ 35GB</code></pre><p>显存占用大幅下降。</p><p>这让更大的模型可以部署到更少 GPU 上。</p><ol><li>降低推理成本</li></ol><p>显存占用降低后，可以：</p><ul><li>用更少 GPU 部署</li><li>提高单卡可承载模型大小</li><li>降低机器成本</li><li>降低能耗</li><li>提高资源利用率</li></ul><p>对线上服务来说，这非常重要。</p><ol><li>提升推理速度</li></ol><p>量化可能提升速度。</p><p>原因是：</p><ul><li>低精度计算更快</li><li>内存带宽压力更小</li><li>参数读取更少</li><li>cache 命中更好</li><li>吞吐更高</li></ul><p>尤其大模型推理经常受内存带宽限制，减少权重读取会明显有帮助。</p><ol><li>支持边缘部署</li></ol><p>很多设备资源有限。</p><p>比如：</p><ul><li>手机</li><li>笔记本</li><li>嵌入式设备</li><li>边缘服务器</li><li>私有化小机器</li></ul><p>量化后模型更小，更容易在这些设备上运行。</p><ol><li>量化对训练也有帮助</li></ol><p>训练中也会用低精度。</p><p>比如：</p><ul><li>FP16</li><li>BF16</li><li>FP8</li><li>QLoRA 的 4-bit 量化</li></ul><p>这些可以降低显存和计算成本。</p><p>不过训练量化比推理量化更复杂，因为要保证梯度稳定。</p><ol><li>量化的代价</li></ol><p>量化不是没有代价。</p><p>可能带来：</p><ul><li>精度下降</li><li>输出质量下降</li><li>数值误差</li><li>对小模型影响更明显</li><li>对数学、代码、推理任务更敏感</li><li>需要校准或量化感知训练</li><li>硬件和算子支持要求高</li></ul><p>所以要在效果和成本之间平衡。</p><ol><li><strong>面试背诵版：为什么需要模型量化？</strong></li></ol><blockquote><p>需要模型量化，主要是因为大模型参数量巨大，如果使用 FP16、BF16 或 FP32 部署，会占用大量显存和计算资源，推理成本很高。量化通过把权重或计算从高精度表示转换为低精度表示，比如 INT8、INT4 或 FP8，可以显著降低模型大小和显存占用。</p><p>显存降低后，可以用更少 GPU 部署更大的模型，降低机器成本和能耗；同时低精度数据读取更少，内存带宽压力更低，可能提升推理吞吐和速度。量化也让模型更容易部署到边缘设备、笔记本或私有化资源有限的环境中。</p><p>不过量化会引入数值误差，可能导致模型精度和生成质量下降，尤其是数学、代码和复杂推理任务更敏感。因此量化需要在成本、速度、显存和效果之间做权衡。简单说，模型量化就是“用更低精度表示模型，用少量精度损失换更低成本和更高部署效率”。</p></blockquote>"},{"t":"FP32、FP16、BF16有什么区别？","tag":"","p":"","html":"<ol><li>FP32、FP16、BF16 有什么区别？</li></ol><p>FP32、FP16、BF16 的核心区别是：它们都是浮点数格式，但位宽、精度和数值范围不同；FP32 最稳但最贵，FP16 更省但容易溢出，BF16 范围大、更适合大模型训练。</p><ol><li>FP32 是什么？</li></ol><p>FP32 是 32 位浮点数。</p><p>它包括：</p><ul><li>1 位符号位</li><li>8 位指数位</li><li>23 位尾数位</li></ul><p>特点是：</p><ul><li>数值范围大</li><li>精度高</li><li>训练稳定</li><li>显存占用大</li><li>计算成本高</li></ul><p>早期深度学习训练常用 FP32。</p><ol><li>FP16 是什么？</li></ol><p>FP16 是 16 位浮点数。</p><p>它包括：</p><ul><li>1 位符号位</li><li>5 位指数位</li><li>10 位尾数位</li></ul><p>相比 FP32：</p><ul><li>显存占用减半</li><li>计算更快</li><li>吞吐更高</li><li>但数值范围小</li><li>容易溢出或下溢</li></ul><p>所以 FP16 训练通常需要 loss scaling 来避免梯度下溢。</p><ol><li>BF16 是什么？</li></ol><p>BF16 是 Brain Floating Point 16。</p><p>它也是 16 位浮点数。</p><p>但结构是：</p><ul><li>1 位符号位</li><li>8 位指数位</li><li>7 位尾数位</li></ul><p>它和 FP32 一样有 8 位指数位。</p><p>所以数值范围接近 FP32。</p><p>但尾数位更少，所以精度低于 FP16。</p><ol><li>FP16 和 BF16 最大区别</li></ol><p>FP16：</p><pre><code>指数位少，尾数位多<br/>数值范围小，精度相对高</code></pre><p>BF16：</p><pre><code>指数位多，尾数位少<br/>数值范围大，精度相对低</code></pre><p>所以：</p><ul><li>FP16 更容易溢出 / 下溢</li><li>BF16 更稳定</li><li>BF16 通常不太需要 loss scaling</li><li>大模型训练更偏向 BF16</li></ul><ol><li>显存和速度</li></ol><p>FP16 和 BF16 都是 16 位。</p><p>所以相比 FP32：</p><ul><li>参数显存约减半</li><li>激活显存约减半</li><li>带宽压力更小</li><li>硬件支持下计算更快</li></ul><p>但具体速度取决于 GPU / TPU 对格式的支持。</p><ol><li>为什么大模型常用 BF16？</li></ol><p>大模型训练中，梯度和激活值范围可能变化很大。</p><p>FP16 数值范围较小，容易出现：</p><ul><li>overflow</li><li>underflow</li><li>NaN</li><li>loss 不稳定</li></ul><p>BF16 保留了 FP32 的指数位，范围大很多，所以训练更稳定。</p><p>虽然尾数精度低一些，但大模型训练通常更需要范围稳定。</p><ol><li>简单对比</li></ol><sheet></sheet><ol><li><strong>面试背诵版：FP32、FP16、BF16 有什么区别？</strong></li></ol><blockquote><p>FP32、FP16、BF16 都是浮点数格式，区别主要在位宽、指数位和尾数位。FP32 是 32 位浮点数，指数位 8 位、尾数位 23 位，数值范围大、精度高、训练稳定，但显存和计算成本高。FP16 是 16 位浮点数，指数位 5 位、尾数位 10 位，显存占用低、计算快，但数值范围小，容易发生上溢或下溢，训练时通常需要 loss scaling。</p><p>BF16 也是 16 位浮点数，但它保留了和 FP32 一样的 8 位指数位，尾数位只有 7 位。因此 BF16 的数值范围接近 FP32，比 FP16 更不容易溢出，训练更稳定；缺点是尾数精度比 FP16 低一些。</p><p>简单说，FP32 最稳但最贵；FP16 更省显存、更快，但范围小、容易数值不稳定；BF16 和 FP16 一样省显存，但数值范围接近 FP32，所以更适合大模型训练。现在很多大模型训练更倾向使用 BF16。</p></blockquote>"},{"t":"INT8量化原理？","tag":"","p":"","html":"<ol><li>INT8 量化原理？</li></ol><p>INT8 量化的核心是：把原本 FP32、FP16 这类浮点数，用 8 位整数近似表示，从而降低模型大小和计算成本。</p><ol><li>INT8 量化是什么？</li></ol><p>模型权重或激活原本可能是：</p><pre><code>FP32 / FP16 / BF16</code></pre><p>INT8 量化会把它们映射到 8 位整数范围。</p><p>比如：</p><pre><code>-128 到 127</code></pre><p>或者：</p><pre><code>0 到 255</code></pre><p>这样每个数只占 1 byte。</p><p>相比 FP16 的 2 byte、FP32 的 4 byte，显存占用更低。</p><ol><li>基本映射公式</li></ol><p>常见线性量化公式是：</p><pre><code>x_int8 = round(x_fp / scale) + zero_point</code></pre><p>反量化时：</p><pre><code>x_fp ≈ scale × (x_int8 - zero_point)</code></pre><p>其中：</p><ul><li>scale：缩放因子</li><li>zero_point：零点偏移</li><li>x_fp：原始浮点值</li><li>x_int8：量化后的整数值</li></ul><ol><li>scale 是什么？</li></ol><p>scale 用来控制浮点范围和整数范围之间的比例。</p><p>比如原始浮点值范围是：</p><pre><code>[-2.0, 2.0]</code></pre><p>INT8 范围是：</p><pre><code>[-128, 127]</code></pre><p>scale 就决定每个整数单位代表多少浮点值。</p><p>scale 越合适，量化误差越小。</p><ol><li>zero_point 是什么？</li></ol><p>zero_point 用于非对称量化。</p><p>它让浮点数 0 能准确映射到某个整数值。</p><p>对称量化通常不需要复杂 zero_point。</p><p>比如：</p><pre><code>0 → 0</code></pre><p>非对称量化可以更好利用 INT8 表示范围，尤其是数据分布不对称时。</p><ol><li>对称量化和非对称量化</li></ol><p>对称量化：</p><pre><code>范围以 0 为中心<br/>INT8 通常是 -127 到 127<br/>zero_point = 0</code></pre><p>优点是计算简单。</p><p>非对称量化：</p><pre><code>范围可以不以 0 为中心<br/>使用 zero_point</code></pre><p>优点是表示分布偏移的数据更精确。</p><ol><li>Per-tensor 和 Per-channel</li></ol><p>Per-tensor 量化：</p><pre><code>整个张量共用一个 scale</code></pre><p>简单但误差可能较大。</p><p>Per-channel 量化：</p><pre><code>每个通道有自己的 scale</code></pre><p>更精确，常用于权重量化。</p><ol><li>PTQ 和 QAT</li></ol><p>INT8 量化有两类常见方式。</p><p>PTQ，Post-Training Quantization：</p><pre><code>模型训练完后直接量化</code></pre><p>成本低，但可能精度下降。</p><p>QAT，Quantization-Aware Training：</p><pre><code>训练时模拟量化误差</code></pre><p>效果更好，但训练成本高。</p><ol><li>INT8 的收益和代价</li></ol><p>收益：</p><ul><li>模型更小</li><li>显存更低</li><li>推理更快</li><li>带宽压力更小</li><li>部署成本更低</li></ul><p>代价：</p><ul><li>有量化误差</li><li>可能精度下降</li><li>对异常值敏感</li><li>需要校准数据</li><li>硬件需要支持 INT8 算子</li></ul><ol><li><strong>面试背诵版：INT8 量化原理？</strong></li></ol><blockquote><p>INT8 量化是把模型中的浮点权重或激活值，比如 FP32、FP16，映射成 8 位整数表示，从而减少模型显存占用和计算成本。常见的线性量化公式是 x_int8 = round(x_fp / scale) + zero_point，反量化时用 x_fp ≈ scale × (x_int8 - zero_point) 近似恢复浮点值。</p><p>其中 scale 是缩放因子，用来表示浮点范围和整数范围之间的比例；zero_point 是零点偏移，主要用于非对称量化，让浮点 0 能映射到整数范围中的某个值。量化方式可以分为对称量化和非对称量化，也可以分为 per-tensor 和 per-channel，后者通常更精细、误差更小。</p><p>INT8 量化可以通过 PTQ 在训练后直接量化，也可以通过 QAT 在训练中模拟量化误差。它的优点是模型更小、显存更低、推理更快、部署成本更低；缺点是会引入量化误差，可能导致精度下降，尤其对异常值和高精度任务敏感。简单说，INT8 量化就是“用 8 位整数近似浮点数，用少量精度损失换更低成本”。</p></blockquote>"},{"t":"INT4量化原理？","tag":"","p":"","html":"<ol><li>INT4 量化原理？</li></ol><p>INT4 量化的核心是：用 4 位整数近似表示模型权重或激活，用更大的精度损失换更低显存和更低部署成本。</p><ol><li>INT4 是什么？</li></ol><p>INT4 是 4 位整数。</p><p>它能表示的数很少。</p><p>比如有符号 INT4 通常表示：</p><pre><code>-8 到 7</code></pre><p>无符号 INT4 可以表示：</p><pre><code>0 到 15</code></pre><p>每个参数只占 4 bit，也就是 0.5 byte。</p><p>相比 FP16 的 2 byte：</p><pre><code>显存约减少到 1/4</code></pre><ol><li>INT4 量化做什么？</li></ol><p>它把浮点数权重映射到 4-bit 整数。</p><p>常见公式和 INT8 类似：</p><pre><code>x_int4 = round(x_fp / scale) + zero_point</code></pre><p>反量化：</p><pre><code>x_fp ≈ scale × (x_int4 - zero_point)</code></pre><p>因为 INT4 可表示的离散值很少，所以 scale 和分组策略非常关键。</p><ol><li>为什么 INT4 更难？</li></ol><p>INT8 有 256 个取值。</p><p>INT4 只有 16 个取值。</p><p>这意味着：</p><ul><li>表达范围更粗</li><li>量化误差更大</li><li>对异常值更敏感</li><li>精度更容易下降</li><li>激活量化更困难</li></ul><p>所以 INT4 通常更常用于权重量化，而不是全量权重 + 激活都量化。</p><ol><li>Group-wise 量化</li></ol><p>INT4 常用 group-wise quantization。</p><p>也就是把权重按组切分。</p><p>每组有自己的 scale。</p><p>比如：</p><pre><code>每 64 个或 128 个权重一组</code></pre><p>这样比整个张量共用一个 scale 更精确。</p><p>因为不同组的数据分布不同。</p><ol><li>对称和非对称量化</li></ol><p>INT4 也可以分：</p><ul><li>对称量化</li><li>非对称量化</li></ul><p>对称量化简单，zero_point 通常为 0。</p><p>非对称量化可以更好处理偏移分布，但计算和存储更复杂。</p><ol><li>常见 INT4 方法</li></ol><p>常见方法包括：</p><ul><li>GPTQ</li><li>AWQ</li><li>SmoothQuant 相关思路</li><li>NF4</li><li>QLoRA 里的 4-bit 量化</li></ul><p>其中 NF4 不是普通 INT4，而是一种适合神经网络权重分布的 4-bit 格式，常用于 QLoRA。</p><ol><li>INT4 的优点</li></ol><p>优点：</p><ul><li>显存占用极低</li><li>能部署更大模型</li><li>推理成本低</li><li>带宽压力小</li><li>适合资源有限设备</li><li>适合单卡部署较大模型</li></ul><ol><li>INT4 的缺点</li></ol><p>缺点：</p><ul><li>精度损失比 INT8 更大</li><li>对任务更敏感</li><li>数学、代码、推理能力可能下降</li><li>需要更复杂校准</li><li>硬件和推理框架支持要求高</li><li>激活 INT4 更难</li><li>不合适的量化可能明显劣化模型</li></ul><ol><li><strong>面试背诵版：INT4 量化原理？</strong></li></ol><blockquote><p>INT4 量化是把模型中的浮点权重或激活值映射成 4 位整数表示。4-bit 只能表示 16 个离散值，比如有符号 INT4 大约是 -8 到 7，因此它相比 INT8 更省显存，但量化误差也更大。常见线性量化公式和 INT8 类似：x_int4 = round(x_fp / scale) + zero_point，反量化时用 scale × (x_int4 - zero_point) 近似恢复浮点值。</p><p>由于 INT4 表示能力很有限，scale 的选择非常关键。实践中常用 group-wise quantization，也就是把权重按组划分，每组使用自己的 scale，从而减少不同分布权重共用一个 scale 带来的误差。INT4 通常更多用于权重量化，因为激活值动态范围更大，量化到 4-bit 更容易损失精度。</p><p>INT4 的优点是显存占用非常低，可以用更少资源部署更大模型，降低带宽和推理成本；缺点是精度损失比 INT8 更明显，数学、代码和推理任务可能更敏感，也更依赖 GPTQ、AWQ、NF4、QLoRA 等量化方法和硬件支持。简单说，INT4 是“用更少 bit 表示权重，用更大压缩换取更高量化难度和潜在精度损失”。</p></blockquote>"},{"t":"GPTQ是什么？","tag":"","p":"","html":"<ol><li>GPTQ 是什么？</li></ol><p>GPTQ 的核心是：一种训练后权重量化方法，用少量校准数据把大模型权重量化到 INT4 / INT8，同时尽量减少量化误差。</p><ol><li>GPTQ 解决什么问题？</li></ol><p>大模型参数很多，部署成本高。</p><p>比如 FP16 权重非常占显存。</p><p>GPTQ 的目标是：</p><pre><code>把模型权重量化到更低 bit，比如 4-bit<br/>同时尽量保持模型效果</code></pre><p>它通常用于推理部署，而不是训练。</p><ol><li>GPTQ 是什么类型方法？</li></ol><p>GPTQ 属于 PTQ。</p><p>也就是 Post-Training Quantization，训练后量化。</p><p>意思是：</p><pre><code>模型已经训练好<br/>不重新训练或只用少量校准数据<br/>直接做量化</code></pre><p>所以它成本比重新训练低很多。</p><ol><li>GPTQ 的核心思想</li></ol><p>普通量化可能直接把权重四舍五入到低精度。</p><p>但这样可能带来较大误差。</p><p>GPTQ 会利用少量校准数据估计：</p><pre><code>哪些权重量化误差对输出影响更大</code></pre><p>然后用近似二阶信息，也就是 Hessian 相关信息，来优化量化过程。</p><p>目标是让量化后的权重对模型输出影响尽量小。</p><ol><li>为什么用 Hessian？</li></ol><p>不是所有权重误差影响一样大。</p><p>有些权重变一点，输出影响很大。</p><p>有些权重变一点，影响较小。</p><p>Hessian 可以近似衡量参数变化对 loss 或输出的敏感程度。</p><p>GPTQ 利用这个信息更聪明地分配量化误差。</p><ol><li>GPTQ 怎么做？</li></ol><p>简单理解流程：</p><pre><code>准备少量校准数据<br/>  ↓<br/>逐层处理模型权重<br/>  ↓<br/>估计该层输入统计 / Hessian 近似<br/>  ↓<br/>按列或分组量化权重<br/>  ↓<br/>对未量化部分做误差补偿<br/>  ↓<br/>得到低比特权重</code></pre><p>核心不是单纯 round，而是量化后做误差补偿。</p><ol><li>GPTQ 的优点</li></ol><p>优点：</p><ul><li>不需要完整重新训练</li><li>量化成本低</li><li>支持 4-bit 权重量化</li><li>显著降低显存</li><li>推理部署友好</li><li>效果通常比简单 PTQ 好</li><li>适合大模型本地部署</li></ul><ol><li>GPTQ 的缺点</li></ol><p>缺点：</p><ul><li>主要量化权重，激活处理有限</li><li>校准数据质量会影响效果</li><li>对极低 bit 更敏感</li><li>量化过程比简单 round 复杂</li><li>需要推理框架支持</li><li>不同任务可能精度下降</li><li>不一定适合继续训练</li></ul><ol><li>和 QLoRA 区别</li></ol><p>GPTQ 主要用于：</p><pre><code>训练后量化推理部署</code></pre><p>QLoRA 用于：</p><pre><code>量化基座模型后做 LoRA 微调</code></pre><p>所以 GPTQ 更偏部署压缩。</p><p>QLoRA 更偏低成本微调。</p><ol><li><strong>面试背诵版：GPTQ 是什么？</strong></li></ol><blockquote><p>GPTQ 是一种用于大语言模型的训练后权重量化方法，属于 Post-Training Quantization。它通常用少量校准数据，把已经训练好的模型权重量化到 4-bit 或 8-bit，从而显著降低显存占用和推理部署成本，同时尽量减少模型效果损失。</p><p>它和简单四舍五入量化不同。GPTQ 会利用校准数据估计每层输入统计，并使用近似 Hessian 的二阶信息来判断不同权重量化误差对输出的影响，然后在逐层、逐列或分组量化过程中进行误差补偿，使量化后的模型输出尽量接近原模型。</p><p>GPTQ 的优点是不需要全量重新训练，量化成本低，适合大模型本地推理部署；缺点是校准数据质量会影响效果，极低 bit 下可能精度下降，也需要推理框架支持。简单说，GPTQ 是“用二阶信息和误差补偿做更聪明的训练后权重量化”。</p></blockquote>"},{"t":"AWQ是什么？","tag":"","p":"","html":"<ol><li>AWQ 是什么？</li></ol><p>AWQ 的核心是：一种训练后权重量化方法，通过保护对模型输出最重要的激活相关权重，降低低比特量化带来的精度损失。</p><ol><li>AWQ 是什么？</li></ol><p>AWQ 全称是 Activation-aware Weight Quantization。</p><p>中文可以理解为：</p><pre><code>激活感知的权重量化</code></pre><p>它是一种 PTQ 方法，也就是训练后量化。</p><p>主要用于把大模型权重量化到低 bit，比如 INT4。</p><ol><li>它解决什么问题？</li></ol><p>普通权重量化会把权重直接压到低精度。</p><p>比如：</p><pre><code>FP16 → INT4</code></pre><p>但不是所有权重同等重要。</p><p>有些权重对应的激活对模型输出影响很大。</p><p>如果这些关键权重量化误差大，模型效果会明显下降。</p><p>AWQ 就是为了识别并保护这些重要权重。</p><ol><li>为什么叫 Activation-aware？</li></ol><p>因为 AWQ 不只看权重本身大小。</p><p>它还看激活值。</p><p>简单说：</p><pre><code>某些通道的激活越重要，对应权重越需要保护</code></pre><p>它会用少量校准数据观察激活分布，判断哪些通道或权重对输出更敏感。</p><ol><li>AWQ 的核心思想</li></ol><p>AWQ 认为：</p><pre><code>只要保护少量重要权重，就能显著降低量化误差。</code></pre><p>它会通过缩放等方式，让重要通道在量化时保留更多有效信息。</p><p>然后再进行低比特权重量化。</p><ol><li>AWQ 怎么做？</li></ol><p>简单流程是：</p><pre><code>准备少量校准数据<br/>  ↓<br/>跑模型，收集激活统计<br/>  ↓<br/>识别重要通道 / 权重<br/>  ↓<br/>对权重做缩放保护<br/>  ↓<br/>执行低比特量化<br/>  ↓<br/>部署推理</code></pre><p>它不需要全量训练模型。</p><ol><li>AWQ 的优点</li></ol><p>优点包括：</p><ul><li>不需要重新训练</li><li>量化成本低</li><li>适合 4-bit 权重量化</li><li>对大模型效果保持较好</li><li>推理部署友好</li><li>比普通 RTN 量化更准确</li><li>常用于本地推理压缩</li></ul><ol><li>AWQ 和 GPTQ 区别</li></ol><p>两者都是 PTQ。</p><p>GPTQ 更强调：</p><pre><code>用近似二阶信息和误差补偿降低量化误差</code></pre><p>AWQ 更强调：</p><pre><code>根据激活分布识别重要权重，并保护这些权重</code></pre><p>简单说：</p><pre><code>GPTQ：从误差补偿角度做量化<br/>AWQ：从激活重要性角度做量化</code></pre><ol><li>AWQ 的局限</li></ol><p>AWQ 也有局限：</p><ul><li>依赖校准数据</li><li>对极低 bit 仍可能损失明显</li><li>主要是权重量化</li><li>不同模型和任务效果可能不同</li><li>需要推理框架支持</li><li>对数学、代码、复杂推理任务仍需评估</li></ul><ol><li><strong>面试背诵版：AWQ 是什么？</strong></li></ol><blockquote><p>AWQ，全称 Activation-aware Weight Quantization，是一种大模型训练后权重量化方法，常用于把模型权重量化到 4-bit，以降低显存占用和推理成本。它的核心思想是：低比特量化时，不是所有权重都同等重要，少量和高激活通道相关的权重对模型输出影响更大，因此需要优先保护。</p><p>AWQ 会使用少量校准数据运行模型，收集激活统计，识别哪些通道或权重对输出更敏感，然后通过缩放等方式保护这些重要权重，再进行低比特量化。这样可以在不重新训练模型的情况下，减少量化误差，保持模型效果。</p><p>它和 GPTQ 都属于 PTQ。GPTQ 更强调用近似 Hessian 的二阶信息和误差补偿来优化量化误差；AWQ 更强调根据激活分布找到重要权重并保护。简单说，AWQ 是“激活感知地保护关键权重，再做低比特量化”。</p></blockquote>"},{"t":"GPTQ和AWQ区别？","tag":"","p":"","html":"<ol><li>GPTQ 和 AWQ 区别？</li></ol><p>GPTQ 和 AWQ 的核心区别是：GPTQ 更关注用二阶信息做误差补偿，AWQ 更关注根据激活分布保护重要权重。</p><ol><li>它们共同点</li></ol><p>GPTQ 和 AWQ 都是大模型量化方法。</p><p>都属于：</p><pre><code>PTQ，Post-Training Quantization</code></pre><p>也就是训练后量化。</p><p>特点是：</p><ul><li>不需要全量重新训练</li><li>只需要少量校准数据</li><li>主要用于权重量化</li><li>常用于 INT4 部署</li><li>目标是降低显存和推理成本</li><li>尽量保持模型效果</li></ul><ol><li>GPTQ 的核心思想</li></ol><p>GPTQ 更强调量化误差补偿。</p><p>它会用校准数据估计每层输入统计，并利用近似 Hessian 的二阶信息。</p><p>目的就是判断：</p><pre><code>哪些权重量化误差对输出影响更大</code></pre><p>然后在逐层、逐列或分组量化时，对未量化部分做误差补偿。</p><p>简单说：</p><pre><code>GPTQ 是用二阶信息更聪明地量化权重。</code></pre><ol><li>AWQ 的核心思想</li></ol><p>AWQ 更强调激活感知。</p><p>它认为不是所有权重都同等重要。</p><p>少量和高激活通道相关的权重，对模型输出影响很大。</p><p>所以 AWQ 会通过校准数据统计激活，找到重要通道或权重。</p><p>然后通过缩放等方式保护这些重要权重，再量化。</p><p>简单说：</p><pre><code>AWQ 是先找关键权重并保护，再量化。</code></pre><ol><li>处理角度不同</li></ol><p>GPTQ 看的是：</p><pre><code>量化误差如何影响输出</code></pre><p>更偏优化和误差补偿。</p><p>AWQ 看的是：</p><pre><code>哪些激活通道对应的权重更重要</code></pre><p>更偏重要性识别和保护。</p><ol><li>工程特点不同</li></ol><p>GPTQ：</p><ul><li>量化过程相对复杂</li><li>需要 Hessian 近似</li><li>量化时间可能更长</li><li>通常效果好</li><li>推理框架支持较多</li></ul><p>AWQ：</p><ul><li>思路相对直接</li><li>校准和搜索缩放因子</li><li>推理部署友好</li><li>对 4-bit 权重量化效果好</li><li>在很多 LLM 部署中常用</li></ul><ol><li>效果谁更好？</li></ol><p>没有绝对答案。</p><p>效果取决于：</p><ul><li>模型结构</li><li>量化 bit</li><li>校准数据</li><li>推理框架</li><li>任务类型</li><li>group size</li><li>是否量化激活</li><li>硬件支持</li></ul><p>通常都要在目标任务上实测。</p><p>数学、代码、推理任务对量化更敏感，尤其要评估。</p><ol><li>简单类比</li></ol><p>可以这么记：</p><pre><code>GPTQ：我知道量化会造成误差，所以我要补偿误差<br/>AWQ：我知道有些权重更重要，所以我要保护它们</code></pre><ol><li><strong>面试背诵版：GPTQ 和 AWQ 区别？</strong></li></ol><blockquote><p>GPTQ 和 AWQ 都是大模型训练后量化方法，主要用于把模型权重量化到 4-bit 或 8-bit，以降低显存占用和推理成本。它们通常都只需要少量校准数据，不需要全量重新训练模型。</p><p>区别在于量化思路不同。GPTQ 更关注量化误差补偿，它利用校准数据估计输入统计，并使用近似 Hessian 的二阶信息来判断权重量化误差对输出的影响，在逐层或分组量化过程中对误差进行补偿。AWQ 则更关注激活感知的重要权重保护，它通过校准数据统计激活分布，识别少量对输出影响大的通道或权重，并通过缩放等方式保护这些重要权重后再量化。</p><p>简单说，GPTQ 是“用二阶信息和误差补偿做量化”，AWQ 是“根据激活分布找关键权重并保护”。两者没有绝对谁更好，实际效果取决于模型、校准数据、量化配置、推理框架和具体任务，需要实测比较。</p></blockquote>"},{"t":"量化为什么会影响模型效果？","tag":"","p":"","html":"<ol><li>量化为什么会影响模型效果？</li></ol><p>量化会影响模型效果，核心原因是：低精度表示会引入数值误差，导致权重、激活和注意力计算结果偏离原模型。</p><ol><li>量化会损失精度</li></ol><p>原始模型参数通常是：</p><pre><code>FP16 / BF16 / FP32</code></pre><p>量化后可能变成：</p><pre><code>INT8 / INT4</code></pre><p>低 bit 能表示的数值更少。</p><p>比如 INT4 只有 16 个离散值。</p><p>很多浮点数只能被近似成附近的整数值。</p><p>这就产生量化误差。</p><ol><li>权重误差会影响输出</li></ol><p>神经网络每一层都依赖权重计算。</p><p>如果权重量化后发生偏差：</p><pre><code>W → W_quant</code></pre><p>那么层输出也会变化：</p><pre><code>Wx → W_quant x</code></pre><p>模型有很多层，误差会逐层传播和放大。</p><p>最终可能影响生成结果。</p><ol><li>激活量化更敏感</li></ol><p>权重量化已经会带来误差。</p><p>如果激活也量化，影响可能更大。</p><p>因为激活值：</p><ul><li>和输入有关</li><li>分布动态变化</li><li>可能有异常值</li><li>不同 batch 差异大</li><li>范围更难估计</li></ul><p>所以 activation quantization 通常比 weight quantization 更难。</p><ol><li>异常值影响 scale</li></ol><p>量化需要确定 scale。</p><p>如果数据里有少量异常大值，scale 会被拉大。</p><p>结果是大部分普通值被压到很少的离散级别上。</p><p>这会导致细节丢失。</p><p>比如：</p><pre><code>大多数值在 [-1,1]<br/>少数异常值到 20</code></pre><p>为了覆盖 20，scale 变大，[-1,1] 区间的精度就变差。</p><ol><li>低 bit 更容易损失</li></ol><p>INT8 有 256 个取值。</p><p>INT4 只有 16 个取值。</p><p>bit 越低：</p><ul><li>表示更粗糙</li><li>舍入误差更大</li><li>信息损失更多</li><li>对 scale 更敏感</li><li>模型效果更容易下降</li></ul><p>所以 INT4 比 INT8 更难保持效果。</p><ol><li>对不同任务影响不同</li></ol><p>量化对任务的影响不一样。</p><p>通常更敏感的是：</p><ul><li>数学推理</li><li>代码生成</li><li>长链路推理</li><li>工具调用参数生成</li><li>结构化输出</li><li>小模型</li><li>领域专用任务</li></ul><p>普通问答或简单生成可能受影响较小。</p><ol><li>不同层敏感度不同</li></ol><p>模型里不同层、不同通道对量化误差敏感度不同。</p><p>有些关键层一旦量化不好，效果明显下降。</p><p>所以 GPTQ、AWQ 这类方法会尝试：</p><ul><li>估计误差影响</li><li>保护重要权重</li><li>分组量化</li><li>per-channel scale</li><li>误差补偿</li></ul><ol><li>如何降低影响？</li></ol><p>常见方法：</p><ul><li>使用更高 bit，比如 INT8 而不是 INT4</li><li>Per-channel / group-wise 量化</li><li>使用高质量校准数据</li><li>GPTQ / AWQ / SmoothQuant</li><li>保护异常通道</li><li>量化感知训练 QAT</li><li>混合精度量化</li><li>关键层不量化</li><li>使用更好的推理 kernel</li></ul><ol><li><strong>面试背诵版：量化为什么会影响模型效果？</strong></li></ol><blockquote><p>量化会影响模型效果，是因为它把 FP16、BF16 或 FP32 这类高精度浮点数映射成 INT8、INT4 等低精度表示，低 bit 能表示的离散数值有限，很多原始权重或激活只能被近似表示，从而引入量化误差。权重误差会改变每层的线性计算结果，误差经过多层网络传播后，可能影响最终输出。</p><p>激活量化通常更敏感，因为激活值随输入动态变化，分布更难估计，而且容易出现异常值。少量异常值可能拉大量化 scale，导致大部分普通值精度下降。bit 越低，比如 INT4，表示能力越有限，误差越大，对模型效果影响也越明显。</p><p>不同任务和层对量化敏感度不同，数学、代码、复杂推理、结构化输出通常更容易受影响。为了降低损失，可以使用 per-channel 或 group-wise 量化、高质量校准数据、GPTQ、AWQ、SmoothQuant、混合精度、关键层不量化或量化感知训练。简单说，量化的本质是“用低精度近似高精度”，因此不可避免会带来数值误差和潜在效果下降。</p></blockquote><p></p><hr/>"}]},{"no":"","title":"模块十一：分布式训练与大模型系统","questions":[{"t":"为什么大模型需要分布式训练？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Data Parallel是什么？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Data Parallel，数据并行，核心是：<strong>把同一个模型复制到多张 GPU 上，每张 GPU 处理不同 batch 数据，然后同步梯度更新同一份模型参数。</strong></p><ol><li>为什么需要 Data Parallel？</li></ol><p>单张 GPU 训练太慢，或者 batch 太大放不下。</p><p>数据并行的思路是：</p><pre><code>模型复制多份<br/>数据切成多份<br/>多卡同时训练</code></pre><p>这样可以提高吞吐，加快训练。</p><ol><li>Data Parallel 怎么做？</li></ol><p>假设有 4 张 GPU，一个 batch 有 128 条样本。</p><p>数据并行会把 batch 切成 4 份：</p><pre><code>GPU0：32 条<br/>GPU1：32 条<br/>GPU2：32 条<br/>GPU3：32 条<br/>GPU4：无，假设只有4卡就没有</code></pre><p>每张 GPU 上都有一份完整模型。</p><p>每张卡独立做：</p><pre><code>前向传播 → 计算 loss → 反向传播算梯度</code></pre><p>然后把梯度同步。</p><ol><li>梯度如何同步？</li></ol><p>每张 GPU 算出自己那份数据上的梯度。</p><p>然后通过 All-Reduce 汇总平均梯度。</p><p>比如：</p><pre><code>GPU0 梯度<br/>GPU1 梯度<br/>GPU2 梯度<br/>GPU3 梯度<br/>  ↓<br/>All-Reduce 求平均<br/>  ↓<br/>每张 GPU 得到相同平均梯度</code></pre><p>然后每张 GPU 用相同梯度更新参数。</p><p>这样训练后，各卡模型参数保持一致。</p><ol><li>Data Parallel 的特点</li></ol><p>特点是：</p><ul><li>每张卡有完整模型副本</li><li>不同卡处理不同数据</li><li>梯度需要同步</li><li>实现相对简单</li><li>适合模型能放进单卡的情况</li><li>可以扩大 batch size</li><li>提高训练吞吐</li></ul><ol><li>Data Parallel 的瓶颈</li></ol><p>它的主要瓶颈是：</p><ul><li>每张卡都要放完整模型</li><li>模型太大时单卡放不下</li><li>梯度同步通信开销大</li><li>batch size 太大可能影响收敛</li><li>参数、梯度、优化器状态重复存储</li></ul><p>所以超大模型通常不能只靠数据并行。</p><ol><li>和 Model Parallel 区别</li></ol><p>Data Parallel：</p><pre><code>模型复制，数据切分</code></pre><p>Model Parallel：</p><pre><code>模型切分，数据可以相同或流水处理</code></pre><p>如果模型太大，一张卡放不下，就需要模型并行、张量并行、流水线并行或 ZeRO。</p><ol><li>DDP 是什么？</li></ol><p>PyTorch 常用的是 Distributed Data Parallel，简称 DDP。</p><p>它比早期 DataParallel 更高效。</p><p>DDP 每个进程控制一张 GPU，通过分布式通信做梯度同步，是实际训练中更常用的数据并行方式。</p><ol><li><strong>面试背诵版：Data Parallel 是什么？</strong></li></ol><blockquote><p>Data Parallel，也就是数据并行，是分布式训练中最常见的一种并行方式。它的核心思想是把同一个模型复制到多张 GPU 上，每张 GPU 处理不同的一部分 batch 数据，分别完成前向传播、loss 计算和反向传播，然后通过 All-Reduce 同步并平均各卡梯度，最后每张 GPU 用相同的平均梯度更新参数，从而保证模型副本保持一致。</p><p>数据并行的优点是实现简单、吞吐高，适合模型可以完整放入单张 GPU，但希望加速训练或扩大 batch size 的场景。它的缺点是每张卡都要保存完整模型、梯度和优化器状态，显存重复开销大；同时梯度同步会带来通信开销。如果模型太大，单卡放不下，就不能只靠数据并行，需要结合张量并行、流水线并行、ZeRO 等方法。</p><p>简单说，Data Parallel 是“模型复制，数据切分，梯度同步”。PyTorch 中常用的 DDP 就是典型的数据并行实现。</p></blockquote>"},{"t":"Tensor Parallel是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Pipeline Parallel是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"三种并行有什么区别？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"ZeRO是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"DeepSpeed是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Megatron是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"如何训练百亿参数模型？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"如何计算训练显存？","tag":"","p":"","html":"<blockquote><p></p></blockquote><p></p><hr/>"}]},{"no":"","title":"模块十二：大模型评测","questions":[{"t":"如何评价一个大模型？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"MMLU是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"C-Eval是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"GSM8K是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"HumanEval是什么？","tag":"","p":"","html":"<blockquote><p></p></blockquote>"},{"t":"Perplexity如何评价模型？","tag":"","p":"","html":"<span>[原文档此处有配图]</span><p>Perplexity，困惑度，核心是：<strong>衡量语言模型对一段文本有多“不困惑”，也就是模型给真实文本的概率高不高。</strong></p><ol><li>Perplexity 是什么？</li></ol><p>Perplexity 简称 PPL。</p><p>它可以理解为语言模型预测下一个 token 时的平均不确定性。</p><p>如果模型对真实下一个 token 预测得很准，PPL 就低。</p><p>如果模型总是很不确定，PPL 就高。</p><ol><li>和 Cross Entropy 的关系</li></ol><p>语言模型训练常用交叉熵 loss。</p><p>Perplexity 本质上是交叉熵的指数形式：</p><pre><code>PPL = exp(Cross Entropy)</code></pre><p>如果使用 log base 2，也可以写成：</p><pre><code>PPL = 2^(Cross Entropy)</code></pre><p>所以：</p><pre><code>loss 越低，PPL 越低<br/>loss 越高，PPL 越高</code></pre><ol><li>PPL 怎么理解？</li></ol><p>可以把 PPL 理解成：</p><pre><code>模型平均每一步在多少个候选 token 中犹豫</code></pre><p>比如：</p><pre><code>PPL = 10</code></pre><p>可以粗略理解为模型平均每个位置像是在 10 个候选 token 中选择。</p><p>PPL 越小，说明模型越确信真实 token。</p><ol><li>PPL 越低越好吗？</li></ol><p>一般来说，在同一测试集、同一 tokenizer、同一评估设置下：</p><pre><code>PPL 越低，语言建模能力越好</code></pre><p>说明模型对真实文本分布拟合更好。</p><p>但是不能脱离条件比较。</p><ol><li>PPL 的适用场景</li></ol><p>PPL 适合评价：</p><ul><li>语言模型预训练效果</li><li>next token prediction 能力</li><li>模型是否拟合文本分布</li><li>不同 checkpoint 的训练效果</li><li>同模型在不同语料上的表现</li></ul><p>比如预训练时看验证集 PPL 是否下降。</p><ol><li>PPL 的局限</li></ol><p>PPL 不等于用户体验。</p><p>低 PPL 不一定代表：</p><ul><li>回答更有帮助</li><li>推理更强</li><li>更安全</li><li>更符合指令</li><li>更少幻觉</li><li>更会工具调用</li></ul><p>因为 PPL 主要评价语言建模概率，不直接评价指令跟随和任务完成。</p><ol><li>比较 PPL 要注意什么？</li></ol><p>不同模型比较 PPL 时要注意：</p><ul><li>tokenizer 是否相同</li><li>测试集是否相同</li><li>数据预处理是否一致</li><li>是否有数据泄露</li><li>是否包含特殊 token</li><li>中文、代码、多语言 tokenization 差异</li><li>上下文长度是否一致</li></ul><p>否则 PPL 不能公平比较。</p><ol><li><strong>面试背诵版：Perplexity 如何评价模型？</strong></li></ol><blockquote><p>Perplexity，也叫困惑度，是评价语言模型 next token prediction 能力的指标。它衡量模型对真实文本有多不确定，本质上是交叉熵 loss 的指数形式，即 PPL = exp(Cross Entropy)。如果模型给真实下一个 token 的概率高，交叉熵低，PPL 就低；如果模型预测不准、不确定性高，PPL 就高。</p><p>直观上，PPL 可以理解为模型平均每一步在多少个候选 token 中犹豫。一般在同一测试集、同一 tokenizer、同一评估设置下，PPL 越低，说明模型对文本分布拟合越好，语言建模能力越强。它常用于预训练阶段评估模型和选择 checkpoint。</p><p>但 PPL 也有局限，它主要评价概率建模能力，不等于真实用户体验，也不能直接代表推理能力、指令跟随、安全性、事实性或工具调用能力。不同模型比较 PPL 时，还必须保证 tokenizer、测试集、预处理和上下文长度一致。</p></blockquote>"},{"t":"大模型人工评测如何设计？","tag":"","p":"","html":"<blockquote><p></p></blockquote><span>[原文档此处有配图]</span><span>[原文档此处有配图]</span><p></p>"}]}]};
