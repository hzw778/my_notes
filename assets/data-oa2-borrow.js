window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["distributedRPC 是什么？为什么需要 RPC？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 是一种让程序像调用本地函数一样调用远程服务的方法</strong>；<strong>微服务中服务被拆到不同机器或进程里，所以需要 RPC 来完成服务之间的通信。</strong><ol><li><strong>RPC 是什么</strong></li></ol>RPC，全称 Remote Procedure Call。中文叫远程过程调用。它的核心思想是：让一个服务调用另一个远程服务的方法，就像调用本地函数一样。比如本地代码写：<code>userService.getUserById(1001)</code>但实际上：<ul><li><code>userService</code> 可能在另一台机器上。</li><li>调用要经过网络。</li><li>请求要序列化。</li><li>服务端要执行方法。</li><li>结果再返回给调用方。</li></ul>RPC 会把这些网络通信细节封装起来，让开发者感觉像是在调用本地方法。<ol><li><strong>RPC 的基本流程</strong></li></ol>一次 RPC 调用大致是：<pre><code>客户端调用本地代理方法<br/>  ↓<br/>代理把方法名和参数序列化<br/>  ↓<br/>通过网络发送给服务端<br/>  ↓<br/>服务端反序列化请求<br/>  ↓<br/>找到对应方法并执行<br/>  ↓<br/>把结果序列化返回<br/>  ↓<br/>客户端反序列化得到结果</code></pre>开发者看到的是方法调用。底层实际发生的是网络请求。<ol><li><strong>RPC 解决了什么问题</strong></li></ol>RPC 主要解决的是：分布式系统中，服务之间如何方便地调用。如果没有 RPC，服务之间通信要手写很多细节：<ul><li>建立网络连接。</li><li>组织请求格式。</li><li>参数序列化。</li><li>处理超时。</li><li>处理重试。</li><li>处理异常。</li><li>解析响应结果。</li></ul>RPC 框架会帮我们封装这些复杂逻辑。<ol><li><strong>微服务为什么需要 RPC</strong></li></ol>微服务架构中，一个大系统会被拆成多个独立服务。比如：<ul><li>用户服务。</li><li>订单服务。</li><li>库存服务。</li><li>支付服务。</li><li>优惠券服务。</li></ul>这些服务通常部署在不同进程、不同机器甚至不同集群中。但业务流程又需要互相调用。比如下单流程：<pre><code>订单服务<br/>  ↓<br/>调用用户服务校验用户<br/>  ↓<br/>调用库存服务扣库存<br/>  ↓<br/>调用优惠券服务校验优惠券<br/>  ↓<br/>调用支付服务创建支付单</code></pre>这些服务之间不能直接本地调用，只能通过网络通信。RPC 就是用来完成这种服务间调用的。<ol><li><strong>RPC 的优点</strong></li></ol>RPC 的优点：<ul><li>调用方式接近本地方法，开发体验好。</li><li>封装网络通信细节。</li><li>支持服务治理能力。</li><li>性能通常比较高。</li><li>适合服务间高频调用。</li><li>接口定义清晰。</li></ul>常见服务治理能力包括：<ul><li>服务发现。</li><li>负载均衡。</li><li>超时控制。</li><li>重试机制。</li><li>熔断降级。</li><li>限流。</li><li>链路追踪。</li></ul><ol><li><strong>RPC 和 HTTP REST 的区别</strong></li></ol>RPC 更关注：<pre><code>调用远程方法</code></pre>REST 更关注：<pre><code>操作资源</code></pre>比如 RPC 风格：<code>getUserById(1001)</code>REST 风格：<code>GET /users/1001</code>常见区别：<ul><li>RPC 更像方法调用。</li><li>REST 更像资源访问。</li><li>RPC 通常性能更高、协议更紧凑。</li><li>REST 更通用、跨语言和浏览器友好。</li><li>微服务内部调用常用 RPC。</li><li>对外开放 API 常用 REST/HTTP。</li></ul>不过实际中，RPC 也可以基于 HTTP，比如 gRPC 基于 HTTP/2。<ol><li><strong>常见 RPC 框架</strong></li></ol>常见 RPC 框架包括：<ul><li>gRPC。</li><li>Dubbo。</li><li>Thrift。</li><li>Motan。</li><li>Spring Cloud OpenFeign。</li><li>Hessian。</li></ul>其中：<ul><li>gRPC 常用 Protobuf 做序列化，性能好，跨语言能力强。</li><li>Dubbo 在 Java 微服务体系中很常见。</li><li>OpenFeign 更偏声明式 HTTP 调用。</li></ul><ol><li><strong>使用 RPC 要注意什么</strong></li></ol>RPC 虽然像本地调用，但本质上还是远程调用。所以要注意：<ul><li>网络可能失败。</li><li>调用可能超时。</li><li>服务可能不可用。</li><li>延迟比本地调用高。</li><li>重试可能导致重复请求。</li><li>要考虑幂等性。</li><li>要设置超时、熔断和降级。</li><li>要做好链路追踪和监控。</li></ul>不能把 RPC 完全当成本地函数调用。<ol><li><strong>面试背诵版</strong></li></ol>问题：什么是 RPC？为什么微服务架构中需要 RPC？RPC，全称 Remote Procedure Call，中文叫远程过程调用。它的核心思想是让一个服务像调用本地方法一样调用远程服务的方法。调用方只需要调用本地代理接口，底层由 RPC 框架完成参数序列化、网络传输、服务端方法执行、结果返回和反序列化等过程。微服务架构中，一个系统会被拆分成用户服务、订单服务、库存服务、支付服务等多个独立服务，这些服务通常部署在不同进程或机器上，业务流程又需要互相协作，所以必须通过网络进行服务间通信。RPC 就是为了解决微服务之间高效、透明、可治理的远程调用问题。相比手写 HTTP 请求，RPC 通常调用方式更接近本地方法，并且框架会提供服务发现、负载均衡、超时重试、熔断降级、限流和链路追踪等服务治理能力。但需要注意，RPC 本质仍然是远程网络调用，会有超时、失败和延迟问题，不能完全当成本地调用使用。面试中可以这样收口：RPC 就是把远程服务调用封装成本地方法调用；微服务拆分后服务分布在不同机器上，必须通过 RPC 进行高效通信和服务治理。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：HTTP API 更偏资源访问，通用、简单、适合对外接口；<strong>RPC 更偏远程方法调用，性能高、接口强约束、服务治理能力强，所以很多内部微服务更常用 RPC。</strong><ol><li><strong>RPC 和 HTTP API 分别是什么</strong></li></ol>HTTP API 通常指基于 HTTP 协议暴露的接口。常见形式是 REST API：<code>GET /users/1001</code><code>POST /orders</code>它更强调对资源的操作，比如：<ul><li>查询用户。</li><li>创建订单。</li><li>更新商品。</li></ul>RPC 是远程过程调用。它更像直接调用一个远程方法：<code>userService.getUserById(1001)</code><code>orderService.createOrder(request)</code>它更强调调用服务端的某个函数或方法。<ol><li><strong>调用风格不同</strong></li></ol>HTTP API：<pre><code>面向资源<br/>通过 URL + HTTP Method 表达操作</code></pre>比如：<ul><li><code>GET /users/1001</code></li><li><code>POST /orders</code></li><li><code>DELETE /items/10</code></li></ul>RPC：<pre><code>面向方法<br/>通过服务名 + 方法名表达调用</code></pre>比如：<ul><li><code>UserService.GetUser</code></li><li><code>OrderService.CreateOrder</code></li><li><code>InventoryService.DeductStock</code></li></ul>简单说：<pre><code>HTTP API 像访问资源<br/>RPC 像调用函数</code></pre><ol><li><strong>协议和数据格式不同</strong></li></ol>HTTP API 常见是：<ul><li>HTTP/1.1。</li><li>JSON。</li><li>文本格式。</li><li>可读性强。</li><li>调试方便。</li></ul>RPC 常见是：<ul><li>TCP、HTTP/2 或自定义协议。</li><li>Protobuf、Thrift、Hessian 等二进制序列化。</li><li>数据更紧凑。</li><li>编解码效率更高。</li></ul>比如 gRPC 常用：<pre><code>HTTP/2 + Protobuf</code></pre>所以 RPC 在高频内部调用中通常性能更好。<ol><li><strong>接口约束不同</strong></li></ol>HTTP API 很多时候靠文档约定。比如字段名、类型、返回结构。虽然也可以用 OpenAPI 规范，但实际强约束程度不一定高。RPC 通常依赖 IDL，也就是接口定义语言。比如 Protobuf：<pre><code>service UserService {<br/>  rpc GetUser(GetUserRequest) returns (GetUserResponse);<br/>}</code></pre>这种方式接口更明确：<ul><li>方法名明确。</li><li>参数类型明确。</li><li>返回类型明确。</li><li>可以自动生成客户端和服务端代码。</li></ul>所以内部服务协作时更不容易出错。<ol><li><strong>服务治理能力不同</strong></li></ol>内部微服务调用不仅是发请求，还需要服务治理。比如：<ul><li>服务发现。</li><li>负载均衡。</li><li>超时控制。</li><li>重试。</li><li>熔断。</li><li>限流。</li><li>链路追踪。</li><li>灰度发布。</li><li>连接池管理。</li></ul>RPC 框架通常会内置或更好地集成这些能力。HTTP API 也能做这些，但往往需要额外网关、SDK 或中间件配合。<ol><li><strong>为什么内部服务常用 RPC</strong></li></ol>因为内部服务调用通常有这些特点：<ul><li>调用频率高。</li><li>延迟要求高。</li><li>服务数量多。</li><li>接口变化需要强约束。</li><li>需要自动生成客户端。</li><li>需要服务发现和负载均衡。</li><li>需要统一治理能力。</li></ul>RPC 更适合这些需求。所以很多公司内部会用 RPC 做服务间通信。对外开放接口则更常用 HTTP REST，因为：<ul><li>通用性强。</li><li>调试方便。</li><li>浏览器和第三方容易接入。</li><li>跨语言门槛低。</li><li>生态成熟。</li></ul><ol><li><strong>RPC 一定比 HTTP API 好吗</strong></li></ol>不一定。RPC 适合内部服务间高频、强约束、低延迟调用。HTTP API 适合：<ul><li>对外开放接口。</li><li>简单系统。</li><li>跨团队、跨组织调用。</li><li>浏览器友好场景。</li><li>需要更好可读性和调试性的场景。</li></ul>而且现在边界也没那么绝对。比如 gRPC 也是基于 HTTP/2。所以更准确地说：<pre><code>RPC 和 HTTP API 是不同调用风格和工程取舍<br/>不是绝对谁替代谁</code></pre><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 和 HTTP API 有什么区别？为什么很多内部服务不用 HTTP 而使用 RPC？RPC 和 HTTP API 的核心区别在于调用风格和工程能力。HTTP API 通常以 REST 为代表，更偏面向资源，通过 URL 和 HTTP Method 表达操作，比如 <code>GET /users/1</code>；它通用性强、可读性好、调试方便，适合对外开放接口。RPC 更偏面向方法调用，比如 <code>UserService.GetUser</code>，调用方式更像本地函数，通常通过 IDL 定义接口，并使用 Protobuf、Thrift 等序列化协议，接口约束更强，也可以自动生成客户端代码。很多内部服务使用 RPC，是因为微服务之间调用频率高、延迟敏感、服务数量多，并且需要服务发现、负载均衡、超时、重试、熔断、限流、链路追踪等服务治理能力，RPC 框架通常对这些支持更完整，性能也更好。不过 HTTP API 更适合对外接口，因为它简单、通用、易调试、跨语言接入方便。面试中可以这样收口：HTTP API 更像访问资源，适合对外；RPC 更像调用远程函数，适合内部服务高频调用，因为性能更高、接口约束更强、服务治理能力更完整。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 之所以像调用本地方法，是因为框架帮我们屏蔽了网络通信、序列化、连接管理和结果解析，开发者看到的是普通方法调用，实际底层发生的是一次远程网络请求。</strong><ol><li><strong>为什么像调用本地方法</strong></li></ol>在业务代码里，调用 RPC 服务通常长这样：<pre><code>User user = userService.getUserById(1001);</code></pre>看起来就像调用本地对象的方法。但实际上，<code>userService</code> 很可能不是一个真正的本地实现类，而是 RPC 框架生成的代理对象。这个代理对象会把方法调用转换成网络请求，发给远程服务。<ol><li><strong>核心是代理 Proxy</strong></li></ol>RPC 的关键机制是代理。客户端拿到的不是远程服务本身，而是一个本地代理对象。调用代理方法时，代理会拦截这次调用，拿到：<ul><li>接口名。</li><li>方法名。</li><li>参数类型。</li><li>参数值。</li><li>请求 ID。</li><li>超时时间。</li></ul>然后把这些信息封装成 RPC 请求。<ol><li><strong>底层调用流程</strong></li></ol>一次 RPC 调用大致是：<pre><code>调用本地代理方法<br/>  ↓<br/>代理拦截方法调用<br/>  ↓<br/>封装请求信息<br/>  ↓<br/>序列化请求<br/>  ↓<br/>通过网络发送给服务端<br/>  ↓<br/>服务端反序列化<br/>  ↓<br/>根据接口和方法找到实现类<br/>  ↓<br/>反射或直接调用目标方法<br/>  ↓<br/>序列化返回结果<br/>  ↓<br/>客户端收到响应<br/>  ↓<br/>反序列化结果<br/>  ↓<br/>返回给业务代码</code></pre>所以业务代码看到的是方法返回值，实际上中间经历了网络通信。<ol><li><strong>服务端如何知道调用哪个方法</strong></li></ol>请求里通常会包含方法元信息，比如：<pre><code>serviceName: UserService<br/>methodName: getUserById<br/>parameterTypes: [Long]<br/>args: [1001]<br/>requestId: abc123</code></pre>服务端收到后，根据 <code>serviceName</code> 找到对应服务实现。再根据 <code>methodName</code> 和参数类型找到具体方法。然后执行方法，把结果返回。<ol><li><strong>为什么需要序列化</strong></li></ol>因为网络只能传输字节。方法名、参数对象、返回值这些内存里的对象，不能直接通过网络发送。所以客户端要把请求对象序列化成字节流。服务端收到后再反序列化成对象。返回结果也是一样。常见序列化方式有：<ul><li>JSON。</li><li>Protobuf。</li><li>Hessian。</li><li>Kryo。</li><li>Thrift。</li></ul><ol><li><strong>为什么需要注册中心</strong></li></ol>在微服务中，服务地址可能不是固定的。比如一个 <code>UserService</code> 可能有多个实例。RPC 框架通常会通过注册中心发现服务地址。流程是：<pre><code>Provider 注册服务地址<br/>Consumer 从注册中心订阅服务<br/>Consumer 选择一个 Provider 发起调用</code></pre>所以客户端只需要关心调用哪个服务，不需要写死具体 IP 和端口。<ol><li><strong>面试背诵版</strong></li></ol>问题：为什么说 RPC 是“像调用本地方法一样调用远程服务”？底层是如何实现的？RPC 之所以像调用本地方法，是因为客户端拿到的通常不是远程服务本身，而是 RPC 框架生成的本地代理对象。业务代码调用代理方法时，看起来像普通方法调用，但代理会拦截这次调用，把接口名、方法名、参数、请求 ID 等信息封装成 RPC 请求，然后进行序列化，通过网络发送到服务端。服务端收到请求后反序列化，根据接口名和方法名找到真正的服务实现并执行方法，再把结果序列化返回给客户端。客户端反序列化响应后，把结果返回给业务代码。面试中可以这样收口：简单说，RPC 的“像本地调用”是代理层提供的假象，底层本质是序列化后的网络请求和响应。<hr/>";
  A["distributedRPC 调用流程是什么？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 之所以像本地方法调用，是因为框架在客户端生成了代理对象，把网络通信、序列化、服务发现、超时重试等细节隐藏起来</strong>；<strong>开发者调用的是本地代理，真正执行发生在远程服务端。</strong><ol><li><strong>为什么说像调用本地方法</strong></li></ol>在业务代码里，RPC 调用通常长这样：<code>userService.getUserById(1001)</code>看起来就像普通本地方法调用。但实际上：<ul><li><code>userService</code> 不是本地真实对象。</li><li>它通常是 RPC 框架生成的代理对象。</li><li>方法执行不在当前进程。</li><li>请求会被发送到远程服务。</li><li>远程服务执行后再返回结果。</li></ul>所以“像本地方法”指的是：<pre><code>调用形式像本地方法<br/>底层本质是远程网络通信</code></pre><ol><li><strong>RPC 的核心角色</strong></li></ol>一次 RPC 调用通常有几个角色：<ul><li>Client：服务调用方。</li><li>Client Stub / Proxy：客户端代理。</li><li>Network：网络传输层。</li><li>Server Stub / Skeleton：服务端代理。</li><li>Server：真正提供方法实现的服务。</li><li>Registry：注册中心，负责服务发现。</li></ul>开发者主要接触的是接口。底层由代理和框架完成远程调用。<ol><li><strong>底层调用流程</strong></li></ol>一次 RPC 调用大致流程是：<pre><code>业务代码调用本地代理方法<br/>  ↓<br/>客户端代理拦截方法调用<br/>  ↓<br/>把服务名、方法名、参数封装成请求<br/>  ↓<br/>参数序列化成二进制或 JSON<br/>  ↓<br/>通过注册中心找到服务地址<br/>  ↓<br/>负载均衡选择一个服务实例<br/>  ↓<br/>通过网络发送请求<br/>  ↓<br/>服务端接收并反序列化<br/>  ↓<br/>根据服务名和方法名找到真实实现<br/>  ↓<br/>执行目标方法<br/>  ↓<br/>结果序列化<br/>  ↓<br/>网络返回给客户端<br/>  ↓<br/>客户端反序列化<br/>  ↓<br/>业务代码拿到返回值</code></pre>业务代码只看到：<pre><code>result = userService.getUserById(1001)</code></pre>中间复杂过程被 RPC 框架隐藏了。<ol><li><strong>客户端代理是什么</strong></li></ol>客户端代理是 RPC 的关键。它负责把一次本地方法调用转换成远程请求。比如你调用：<code>userService.getUserById(1001)</code>代理会知道：<ul><li>调用哪个服务。</li><li>调用哪个方法。</li><li>参数是什么。</li><li>返回值类型是什么。</li></ul>然后构造请求：<pre><code>service = UserService<br/>method = getUserById<br/>args = [1001]</code></pre>再发送给服务端。在 Java 中，这通常可以通过动态代理、字节码增强等方式实现。<ol><li><strong>序列化和反序列化</strong></li></ol>网络只能传输字节。所以方法参数不能直接传过去。RPC 需要把参数对象转成字节流。这叫序列化。服务端收到后，再把字节流转回对象。这叫反序列化。常见序列化方式：<ul><li>Protobuf。</li><li>JSON。</li><li>Hessian。</li><li>Kryo。</li><li>Thrift。</li></ul>高性能 RPC 通常使用二进制序列化，比如 Protobuf，因为体积更小、解析更快。<ol><li><strong>服务发现和负载均衡</strong></li></ol>在微服务中，一个服务可能有多个实例。比如用户服务有 5 台机器：<pre><code>UserService:<br/>10.0.0.1:9000<br/>10.0.0.2:9000<br/>10.0.0.3:9000<br/>...</code></pre>服务提供方启动时会把自己注册到注册中心。调用方调用时，会从注册中心获取可用实例列表。然后通过负载均衡选择一个实例。常见负载均衡策略：<ul><li>随机。</li><li>轮询。</li><li>一致性哈希。</li><li>最少活跃调用。</li><li>权重负载均衡。</li></ul><ol><li><strong>服务端如何执行方法</strong></li></ol>服务端收到请求后，会根据请求里的：<ul><li>服务名。</li><li>方法名。</li><li>参数类型。</li></ul>找到对应的真实实现。然后通过反射、方法表或框架调度执行目标方法。执行完成后，把返回值或异常封装成响应，再返回客户端。<ol><li><strong>RPC 不是真的本地调用</strong></li></ol>虽然 RPC 看起来像本地调用，但它和本地调用有本质区别。RPC 可能出现：<ul><li>网络延迟。</li><li>超时。</li><li>连接失败。</li><li>服务不可用。</li><li>序列化失败。</li><li>重试导致重复请求。</li><li>部分成功。</li><li>调用链过长。</li></ul>所以使用 RPC 时必须设置：<ul><li>超时时间。</li><li>重试策略。</li><li>熔断降级。</li><li>幂等设计。</li><li>监控告警。</li><li>链路追踪。</li></ul>不能真的把 RPC 当成本地函数调用。<ol><li><strong>面试背诵版</strong></li></ol>问题：为什么说 RPC 是“像调用本地方法一样调用远程服务”？底层是如何实现的？RPC 之所以说像调用本地方法一样调用远程服务，是因为 RPC 框架会在客户端生成一个代理对象，业务代码调用这个代理方法时，形式上和调用本地方法一样，但真正的方法执行发生在远程服务端。底层流程是：客户端代理拦截方法调用，把服务名、方法名和参数封装成请求；然后对参数进行序列化，通过服务发现找到服务实例，并经过负载均衡选择一个节点；接着通过网络把请求发送到服务端。服务端收到请求后反序列化，根据服务名和方法名找到真实实现并执行方法，再把返回值序列化后通过网络返回给客户端，客户端反序列化后得到结果。RPC 框架把序列化、网络通信、服务发现、负载均衡、超时重试等细节都封装起来，所以开发者感觉像是在调用本地方法。但本质上 RPC 仍然是远程网络调用，需要考虑超时、失败、重试、幂等、熔断和链路追踪等问题。面试中可以这样收口：RPC 的“像本地调用”是靠客户端代理实现的；代理把本地方法调用转换成网络请求，服务端执行真实方法后返回结果，框架隐藏了序列化、传输和服务治理细节。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>Client 发起业务调用，Proxy 把本地方法调用转换成 RPC 请求，Serializer 负责对象和字节之间转换，Transport 负责网络传输，Server 负责接收请求并执行真实服务方法。</strong><ol><li><strong>Client 负责什么</strong></li></ol>Client 是服务调用方。它是业务代码所在的一端。比如订单服务要查用户信息：<code>userService.getUserById(1001)</code>这里订单服务就是 Client。Client 主要负责：<ul><li>发起远程服务调用。</li><li>传入业务参数。</li><li>接收返回结果。</li><li>处理异常、超时或降级结果。</li></ul>但 Client 通常不直接处理底层网络细节。这些细节由 RPC 框架封装。<ol><li><strong>Proxy 负责什么</strong></li></ol>Proxy 是客户端代理。它是 RPC 能做到“像调用本地方法一样调用远程服务”的关键。业务代码以为自己调用的是本地接口：<code>userService.getUserById(1001)</code>实际调用的是 Proxy。Proxy 负责：<ul><li>拦截本地方法调用。</li><li>获取服务名、方法名、参数、参数类型。</li><li>封装成 RpcRequest。</li><li>调用序列化器。</li><li>选择服务实例。</li><li>发起网络请求。</li><li>等待响应并返回结果。</li></ul>简单说：<pre><code>Proxy 把本地方法调用翻译成远程请求</code></pre><ol><li><strong>Serializer 负责什么</strong></li></ol>Serializer 是序列化器。网络只能传输字节流，不能直接传 Java 对象、Python 对象或复杂结构。所以 Serializer 负责：<ul><li>把请求对象转换成字节流。</li><li>把响应字节流转换回对象。</li><li>处理参数、返回值、异常等数据结构。</li></ul>常见序列化方式：<ul><li>JSON。</li><li>Protobuf。</li><li>Hessian。</li><li>Thrift。</li><li>Kryo。</li></ul>比如：<pre><code>RpcRequest 对象 → byte[]<br/>byte[] → RpcResponse 对象</code></pre>高性能 RPC 通常使用 Protobuf、Thrift 这类二进制序列化。<ol><li><strong>Transport 负责什么</strong></li></ol>Transport 是网络传输层。它负责把序列化后的字节数据从客户端发送到服务端。也负责把服务端响应返回给客户端。Transport 主要处理：<ul><li>建立连接。</li><li>发送请求。</li><li>接收响应。</li><li>编解码协议。</li><li>处理粘包拆包。</li><li>连接池。</li><li>超时。</li><li>异步回调。</li></ul>底层可以基于：<ul><li>TCP。</li><li>HTTP。</li><li>HTTP/2。</li><li>Netty。</li><li>自定义协议。</li></ul>比如 gRPC 常见是：<pre><code>HTTP/2 + Protobuf</code></pre>Dubbo 可以基于自定义 TCP 协议。<ol><li><strong>Server 负责什么</strong></li></ol>Server 是服务提供方。它真正实现业务方法。比如用户服务提供：<code>getUserById(1001)</code>Server 负责：<ul><li>启动服务监听端口。</li><li>注册服务到注册中心。</li><li>接收 RPC 请求。</li><li>反序列化请求。</li><li>根据服务名和方法名找到实现类。</li><li>执行真实业务方法。</li><li>封装返回结果或异常。</li><li>返回响应给客户端。</li></ul>简单说：<pre><code>Server 负责接收远程请求并执行真实方法</code></pre><ol><li><strong>它们之间如何协作</strong></li></ol>一次调用大概是：<pre><code>Client 业务代码<br/>  ↓<br/>Proxy 拦截方法调用<br/>  ↓<br/>Serializer 把请求转成字节<br/>  ↓<br/>Transport 发送到服务端<br/>  ↓<br/>Server 接收并执行真实方法<br/>  ↓<br/>Serializer 把响应转成字节<br/>  ↓<br/>Transport 返回响应<br/>  ↓<br/>Proxy 解析结果<br/>  ↓<br/>Client 拿到返回值</code></pre>可以用一句话理解：<pre><code>Client 提需求<br/>Proxy 翻译调用<br/>Serializer 打包数据<br/>Transport 负责运输<br/>Server 真正执行</code></pre><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 中 Client、Proxy、Serializer、Transport、Server 分别负责什么？在 RPC 调用中，Client 是服务调用方，负责发起业务调用并接收结果；Proxy 是客户端代理，是 RPC 屏蔽远程调用细节的关键，它会拦截本地方法调用，提取服务名、方法名、参数和参数类型，封装成 RpcRequest；Serializer 负责序列化和反序列化，因为网络只能传输字节流，所以需要把请求对象转成字节，也要把响应字节转回对象；Transport 是网络传输层，负责连接建立、请求发送、响应接收、协议编解码、超时和连接管理，底层可以基于 TCP、HTTP/2、Netty 等；Server 是服务提供方，负责接收请求，反序列化后根据服务名和方法名找到真实实现并执行，再把结果封装成响应返回。它们协作起来，就完成了从本地方法调用到远程方法执行再返回结果的完整 RPC 流程。面试中可以这样收口：Client 负责发起调用，Proxy 负责把方法调用变成 RPC 请求，Serializer 负责对象和字节转换，Transport 负责网络传输，Server 负责执行真实方法并返回结果。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：一个简单 RPC 框架可以按“服务注册与发现、客户端代理、序列化协议、网络通信、服务端反射调用、治理能力”来设计，核心目标是把本地方法调用转换成远程网络调用。<ol><li><strong>整体架构</strong></li></ol>一个简单 RPC 框架可以分成几层：<ul><li>Client Proxy：客户端代理层。</li><li>Serializer：序列化层。</li><li>Protocol：协议编解码层。</li><li>Transport：网络传输层。</li><li>Registry：注册中心。</li><li>Load Balancer：负载均衡。</li><li>Server Handler：服务端请求处理。</li><li>Service Invoker：服务反射调用。</li><li>Fault Tolerance：超时、重试、熔断等治理能力。</li></ul>整体流程是：<pre><code>客户端业务代码<br/>  ↓<br/>客户端动态代理<br/>  ↓<br/>封装 RPC Request<br/>  ↓<br/>序列化 + 编码<br/>  ↓<br/>服务发现 + 负载均衡<br/>  ↓<br/>网络发送<br/>  ↓<br/>服务端解码 + 反序列化<br/>  ↓<br/>查找服务实现<br/>  ↓<br/>反射调用真实方法<br/>  ↓<br/>序列化响应<br/>  ↓<br/>返回客户端</code></pre><ol><li><strong>客户端代理层</strong></li></ol>客户端不能直接调用远程服务。所以需要生成一个代理对象。业务代码调用：<code>userService.getUserById(1001)</code>实际执行的是代理逻辑。代理负责：<ul><li>拦截方法调用。</li><li>获取服务名。</li><li>获取方法名。</li><li>获取参数和参数类型。</li><li>封装成 RpcRequest。</li><li>发送远程请求。</li><li>等待 RpcResponse。</li><li>返回结果。</li></ul>在 Java 中可以用：<ul><li>JDK Dynamic Proxy。</li><li>CGLIB。</li><li>ByteBuddy。</li></ul><ol><li><strong>请求和响应模型</strong></li></ol>可以定义两个核心对象。RpcRequest 包含：<ul><li>requestId。</li><li>serviceName。</li><li>methodName。</li><li>parameterTypes。</li><li>parameters。</li><li>version。</li><li>timeout。</li></ul>RpcResponse 包含：<ul><li>requestId。</li><li>success。</li><li>result。</li><li>errorCode。</li><li>errorMessage。</li><li>exception。</li></ul><code>requestId</code> 很重要，用来匹配请求和响应，尤其是异步调用场景。<ol><li><strong>序列化层</strong></li></ol>网络只能传输字节。所以请求和响应对象必须序列化。可以支持多种序列化方式：<ul><li>JSON：可读性好，调试方便，但性能一般。</li><li>Protobuf：体积小，性能好，跨语言强。</li><li>Hessian：Java 生态常见。</li><li>Kryo：性能较好，但跨语言弱。</li></ul>简单 RPC 框架可以先用 JSON。后续再扩展 Protobuf 或 Hessian。设计时可以抽象一个接口：<pre><code>Serializer<br/>  - serialize(obj)<br/>  - deserialize(bytes, type)</code></pre>这样方便替换实现。<ol><li><strong>协议编解码层</strong></li></ol>除了序列化，还需要定义通信协议。否则服务端不知道一段字节代表什么。一个简单协议可以包含：<ul><li>magic number：识别协议。</li><li>version：协议版本。</li><li>serializer type：序列化类型。</li><li>message type：请求还是响应。</li><li>requestId：请求 ID。</li><li>body length：消息体长度。</li><li>body：消息内容。</li></ul>这样可以解决：<ul><li>粘包拆包。</li><li>协议识别。</li><li>多序列化方式扩展。</li><li>请求响应匹配。</li></ul><ol><li><strong>网络通信层</strong></li></ol>网络层负责客户端和服务端之间的数据传输。简单实现可以用：<ul><li>Java Socket。</li><li>Netty。</li><li>HTTP。</li></ul>如果追求性能和高并发，通常用 Netty。网络层要处理：<ul><li>建立连接。</li><li>发送请求。</li><li>接收响应。</li><li>编解码。</li><li>连接池。</li><li>超时。</li><li>异步回调。</li></ul>简单版本可以先做同步阻塞调用。进阶版本可以支持异步 Future。<ol><li><strong>注册中心和服务发现</strong></li></ol>微服务中服务地址不能写死。所以需要注册中心。服务端启动时注册服务：<pre><code>UserService -&gt; 10.0.0.1:9000<br/>OrderService -&gt; 10.0.0.2:9000</code></pre>客户端调用前从注册中心查询可用实例。注册中心可以用：<ul><li>ZooKeeper。</li><li>Nacos。</li><li>Etcd。</li><li>Consul。</li><li>Redis。</li><li>本地 Map，适合 demo。</li></ul>注册中心负责：<ul><li>服务注册。</li><li>服务发现。</li><li>服务下线。</li><li>健康检查。</li><li>地址变更通知。</li></ul><ol><li><strong>负载均衡</strong></li></ol>如果一个服务有多个实例，需要选择其中一个调用。常见策略：<ul><li>Random：随机。</li><li>Round Robin：轮询。</li><li>Weighted Round Robin：加权轮询。</li><li>Consistent Hash：一致性哈希。</li><li>Least Active：最少活跃调用。</li></ul>简单版本可以先实现随机或轮询。<ol><li><strong>服务端处理层</strong></li></ol>服务端启动后需要：<ul><li>暴露服务接口和实现类。</li><li>监听端口。</li><li>接收请求。</li><li>反序列化 RpcRequest。</li><li>根据 serviceName 找到实现对象。</li><li>根据 methodName 和 parameterTypes 找到方法。</li><li>通过反射调用。</li><li>封装 RpcResponse。</li><li>返回客户端。</li></ul>服务端需要维护一个服务映射表：<pre><code>serviceName -&gt; serviceImpl</code></pre>比如：<pre><code>UserService -&gt; UserServiceImpl</code></pre><ol><li><strong>服务治理能力</strong></li></ol>一个可用的 RPC 框架不能只完成调用，还要考虑稳定性。至少需要：<ul><li>Timeout：超时控制。</li><li>Retry：失败重试。</li><li>Circuit Breaker：熔断。</li><li>Rate Limit：限流。</li><li>Fallback：降级。</li><li>Heartbeat：心跳检测。</li><li>Metrics：指标监控。</li><li>Tracing：链路追踪。</li><li>Logging：调用日志。</li></ul>简单框架可以先实现：<pre><code>超时 + 重试 + 日志</code></pre>后续再扩展熔断和限流。<ol><li><strong>设计重点</strong></li></ol>面试时可以强调几个关键点：<ul><li>用动态代理屏蔽远程调用细节。</li><li>用序列化把对象转成字节流。</li><li>用协议解决消息边界和扩展性。</li><li>用注册中心实现服务发现。</li><li>用负载均衡选择服务实例。</li><li>服务端通过反射调用真实方法。</li><li>用超时、重试、熔断保证稳定性。</li><li>模块抽象要可插拔，比如序列化、负载均衡、注册中心都做成接口。</li></ul><ol><li><strong>面试背诵版</strong></li></ol>问题：如果让你设计一个简单 RPC 框架，整体架构应该如何设计？如果设计一个简单 RPC 框架，我会把它拆成客户端、网络通信、注册中心、服务端和服务治理几个模块。客户端侧通过动态代理生成接口代理对象，业务代码调用本地代理方法时，代理会把服务名、方法名、参数、参数类型和 requestId 封装成 RpcRequest，然后通过序列化和协议编码转成字节流。接着客户端从注册中心获取服务实例列表，通过负载均衡选择一个服务节点，再通过 TCP、HTTP/2 或 Netty 把请求发送到服务端。服务端收到请求后进行解码和反序列化，根据 serviceName 从本地服务映射表中找到对应实现类，再根据 methodName 和参数类型通过反射调用真实方法，最后把结果或异常封装成 RpcResponse 返回给客户端。注册中心负责服务注册、发现和下线，负载均衡负责多实例选择，序列化、注册中心、负载均衡都应该设计成可插拔接口。为了保证可用性，还需要支持超时、重试、熔断、限流、日志、监控和链路追踪等服务治理能力。简单版本可以先实现动态代理、序列化、网络传输、服务注册发现和反射调用，再逐步补充治理能力。面试中可以这样收口：简单 RPC 框架的核心就是：客户端代理把方法调用封装成请求，通过注册中心和负载均衡找到服务端，网络传输后由服务端反射调用真实方法，再把结果返回；外围再补上序列化、协议编解码和超时重试等治理能力。<hr/>";
  A["distributedRPC 和 HTTP 有什么区别？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：HTTP API 更偏资源访问，通用、简单、适合对外接口；<strong>RPC 更偏远程方法调用，性能高、接口强约束、服务治理能力强，所以很多内部微服务更常用 RPC。</strong><ol><li><strong>RPC 和 HTTP API 分别是什么</strong></li></ol>HTTP API 通常指基于 HTTP 协议暴露的接口。常见形式是 REST API：<code>GET /users/1001</code><code>POST /orders</code>它更强调对资源的操作，比如：<ul><li>查询用户。</li><li>创建订单。</li><li>更新商品。</li></ul>RPC 是远程过程调用。它更像直接调用一个远程方法：<code>userService.getUserById(1001)</code><code>orderService.createOrder(request)</code>它更强调调用服务端的某个函数或方法。<ol><li><strong>调用风格不同</strong></li></ol>HTTP API：<pre><code>面向资源<br/>通过 URL + HTTP Method 表达操作</code></pre>比如：<ul><li><code>GET /users/1001</code></li><li><code>POST /orders</code></li><li><code>DELETE /items/10</code></li></ul>RPC：<pre><code>面向方法<br/>通过服务名 + 方法名表达调用</code></pre>比如：<ul><li><code>UserService.GetUser</code></li><li><code>OrderService.CreateOrder</code></li><li><code>InventoryService.DeductStock</code></li></ul>简单说：<pre><code>HTTP API 像访问资源<br/>RPC 像调用函数</code></pre><ol><li><strong>协议和数据格式不同</strong></li></ol>HTTP API 常见是：<ul><li>HTTP/1.1。</li><li>JSON。</li><li>文本格式。</li><li>可读性强。</li><li>调试方便。</li></ul>RPC 常见是：<ul><li>TCP、HTTP/2 或自定义协议。</li><li>Protobuf、Thrift、Hessian 等二进制序列化。</li><li>数据更紧凑。</li><li>编解码效率更高。</li></ul>比如 gRPC 常用：<pre><code>HTTP/2 + Protobuf</code></pre>所以 RPC 在高频内部调用中通常性能更好。<ol><li><strong>接口约束不同</strong></li></ol>HTTP API 很多时候靠文档约定。比如字段名、类型、返回结构。虽然也可以用 OpenAPI 规范，但实际强约束程度不一定高。RPC 通常依赖 IDL，也就是接口定义语言。比如 Protobuf：<pre><code>service UserService {<br/>  rpc GetUser(GetUserRequest) returns (GetUserResponse);<br/>}</code></pre>这种方式接口更明确：<ul><li>方法名明确。</li><li>参数类型明确。</li><li>返回类型明确。</li><li>可以自动生成客户端和服务端代码。</li></ul>所以内部服务协作时更不容易出错。<ol><li><strong>服务治理能力不同</strong></li></ol>内部微服务调用不仅是发请求，还需要服务治理。比如：<ul><li>服务发现。</li><li>负载均衡。</li><li>超时控制。</li><li>重试。</li><li>熔断。</li><li>限流。</li><li>链路追踪。</li><li>灰度发布。</li><li>连接池管理。</li></ul>RPC 框架通常会内置或更好地集成这些能力。HTTP API 也能做这些，但往往需要额外网关、SDK 或中间件配合。<ol><li><strong>为什么内部服务常用 RPC</strong></li></ol>因为内部服务调用通常有这些特点：<ul><li>调用频率高。</li><li>延迟要求高。</li><li>服务数量多。</li><li>接口变化需要强约束。</li><li>需要自动生成客户端。</li><li>需要服务发现和负载均衡。</li><li>需要统一治理能力。</li></ul>RPC 更适合这些需求。所以很多公司内部会用 RPC 做服务间通信。对外开放接口则更常用 HTTP REST，因为：<ul><li>通用性强。</li><li>调试方便。</li><li>浏览器和第三方容易接入。</li><li>跨语言门槛低。</li><li>生态成熟。</li></ul><ol><li><strong>RPC 一定比 HTTP API 好吗</strong></li></ol>不一定。RPC 适合内部服务间高频、强约束、低延迟调用。HTTP API 适合：<ul><li>对外开放接口。</li><li>简单系统。</li><li>跨团队、跨组织调用。</li><li>浏览器友好场景。</li><li>需要更好可读性和调试性的场景。</li></ul>而且现在边界也没那么绝对。比如 gRPC 也是基于 HTTP/2。所以更准确地说：<pre><code>RPC 和 HTTP API 是不同调用风格和工程取舍<br/>不是绝对谁替代谁</code></pre><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 和 HTTP API 有什么区别？为什么很多内部服务不用 HTTP 而使用 RPC？RPC 和 HTTP API 的核心区别在于调用风格和工程能力。HTTP API 通常以 REST 为代表，更偏面向资源，通过 URL 和 HTTP Method 表达操作，比如 <code>GET /users/1</code>；它通用性强、可读性好、调试方便，适合对外开放接口。RPC 更偏面向方法调用，比如 <code>UserService.GetUser</code>，调用方式更像本地函数，通常通过 IDL 定义接口，并使用 Protobuf、Thrift 等序列化协议，接口约束更强，也可以自动生成客户端代码。很多内部服务使用 RPC，是因为微服务之间调用频率高、延迟敏感、服务数量多，并且需要服务发现、负载均衡、超时、重试、熔断、限流、链路追踪等服务治理能力，RPC 框架通常对这些支持更完整，性能也更好。不过 HTTP API 更适合对外接口，因为它简单、通用、易调试、跨语言接入方便。面试中可以这样收口：HTTP API 更像访问资源，适合对外；RPC 更像调用远程函数，适合内部服务高频调用，因为性能更高、接口约束更强、服务治理能力更完整。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 之所以像调用本地方法，是因为框架帮我们屏蔽了网络通信、序列化、连接管理和结果解析，开发者看到的是普通方法调用，实际底层发生的是一次远程网络请求。</strong><ol><li><strong>为什么像调用本地方法</strong></li></ol>在业务代码里，调用 RPC 服务通常长这样：<pre><code>User user = userService.getUserById(1001);</code></pre>看起来就像调用本地对象的方法。但实际上，<code>userService</code> 很可能不是一个真正的本地实现类，而是 RPC 框架生成的代理对象。这个代理对象会把方法调用转换成网络请求，发给远程服务。<ol><li><strong>核心是代理 Proxy</strong></li></ol>RPC 的关键机制是代理。客户端拿到的不是远程服务本身，而是一个本地代理对象。调用代理方法时，代理会拦截这次调用，拿到：<ul><li>接口名。</li><li>方法名。</li><li>参数类型。</li><li>参数值。</li><li>请求 ID。</li><li>超时时间。</li></ul>然后把这些信息封装成 RPC 请求。<ol><li><strong>底层调用流程</strong></li></ol>一次 RPC 调用大致是：<pre><code>调用本地代理方法<br/>  ↓<br/>代理拦截方法调用<br/>  ↓<br/>封装请求信息<br/>  ↓<br/>序列化请求<br/>  ↓<br/>通过网络发送给服务端<br/>  ↓<br/>服务端反序列化<br/>  ↓<br/>根据接口和方法找到实现类<br/>  ↓<br/>反射或直接调用目标方法<br/>  ↓<br/>序列化返回结果<br/>  ↓<br/>客户端收到响应<br/>  ↓<br/>反序列化结果<br/>  ↓<br/>返回给业务代码</code></pre>所以业务代码看到的是方法返回值，实际上中间经历了网络通信。<ol><li><strong>服务端如何知道调用哪个方法</strong></li></ol>请求里通常会包含方法元信息，比如：<pre><code>serviceName: UserService<br/>methodName: getUserById<br/>parameterTypes: [Long]<br/>args: [1001]<br/>requestId: abc123</code></pre>服务端收到后，根据 <code>serviceName</code> 找到对应服务实现。再根据 <code>methodName</code> 和参数类型找到具体方法。然后执行方法，把结果返回。<ol><li><strong>为什么需要序列化</strong></li></ol>因为网络只能传输字节。方法名、参数对象、返回值这些内存里的对象，不能直接通过网络发送。所以客户端要把请求对象序列化成字节流。服务端收到后再反序列化成对象。返回结果也是一样。常见序列化方式有：<ul><li>JSON。</li><li>Protobuf。</li><li>Hessian。</li><li>Kryo。</li><li>Thrift。</li></ul><ol><li><strong>为什么需要注册中心</strong></li></ol>在微服务中，服务地址可能不是固定的。比如一个 <code>UserService</code> 可能有多个实例。RPC 框架通常会通过注册中心发现服务地址。流程是：<pre><code>Provider 注册服务地址<br/>Consumer 从注册中心订阅服务<br/>Consumer 选择一个 Provider 发起调用</code></pre>所以客户端只需要关心调用哪个服务，不需要写死具体 IP 和端口。<ol><li><strong>面试背诵版</strong></li></ol>问题：为什么说 RPC 是“像调用本地方法一样调用远程服务”？底层是如何实现的？RPC 之所以像调用本地方法，是因为客户端拿到的通常不是远程服务本身，而是 RPC 框架生成的本地代理对象。业务代码调用代理方法时，看起来像普通方法调用，但代理会拦截这次调用，把接口名、方法名、参数、请求 ID 等信息封装成 RPC 请求，然后进行序列化，通过网络发送到服务端。服务端收到请求后反序列化，根据接口名和方法名找到真正的服务实现并执行方法，再把结果序列化返回给客户端。客户端反序列化响应后，把结果返回给业务代码。面试中可以这样收口：简单说，RPC 的“像本地调用”是代理层提供的假象，底层本质是序列化后的网络请求和响应。<hr/>";
  A["distributedRPC 框架一般包含哪些组件？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>Client 发起业务调用，Proxy 把本地方法调用转换成 RPC 请求，Serializer 负责对象和字节之间转换，Transport 负责网络传输，Server 负责接收请求并执行真实服务方法。</strong><ol><li><strong>Client 负责什么</strong></li></ol>Client 是服务调用方。它是业务代码所在的一端。比如订单服务要查用户信息：<code>userService.getUserById(1001)</code>这里订单服务就是 Client。Client 主要负责：<ul><li>发起远程服务调用。</li><li>传入业务参数。</li><li>接收返回结果。</li><li>处理异常、超时或降级结果。</li></ul>但 Client 通常不直接处理底层网络细节。这些细节由 RPC 框架封装。<ol><li><strong>Proxy 负责什么</strong></li></ol>Proxy 是客户端代理。它是 RPC 能做到“像调用本地方法一样调用远程服务”的关键。业务代码以为自己调用的是本地接口：<code>userService.getUserById(1001)</code>实际调用的是 Proxy。Proxy 负责：<ul><li>拦截本地方法调用。</li><li>获取服务名、方法名、参数、参数类型。</li><li>封装成 RpcRequest。</li><li>调用序列化器。</li><li>选择服务实例。</li><li>发起网络请求。</li><li>等待响应并返回结果。</li></ul>简单说：<pre><code>Proxy 把本地方法调用翻译成远程请求</code></pre><ol><li><strong>Serializer 负责什么</strong></li></ol>Serializer 是序列化器。网络只能传输字节流，不能直接传 Java 对象、Python 对象或复杂结构。所以 Serializer 负责：<ul><li>把请求对象转换成字节流。</li><li>把响应字节流转换回对象。</li><li>处理参数、返回值、异常等数据结构。</li></ul>常见序列化方式：<ul><li>JSON。</li><li>Protobuf。</li><li>Hessian。</li><li>Thrift。</li><li>Kryo。</li></ul>比如：<pre><code>RpcRequest 对象 → byte[]<br/>byte[] → RpcResponse 对象</code></pre>高性能 RPC 通常使用 Protobuf、Thrift 这类二进制序列化。<ol><li><strong>Transport 负责什么</strong></li></ol>Transport 是网络传输层。它负责把序列化后的字节数据从客户端发送到服务端。也负责把服务端响应返回给客户端。Transport 主要处理：<ul><li>建立连接。</li><li>发送请求。</li><li>接收响应。</li><li>编解码协议。</li><li>处理粘包拆包。</li><li>连接池。</li><li>超时。</li><li>异步回调。</li></ul>底层可以基于：<ul><li>TCP。</li><li>HTTP。</li><li>HTTP/2。</li><li>Netty。</li><li>自定义协议。</li></ul>比如 gRPC 常见是：<pre><code>HTTP/2 + Protobuf</code></pre>Dubbo 可以基于自定义 TCP 协议。<ol><li><strong>Server 负责什么</strong></li></ol>Server 是服务提供方。它真正实现业务方法。比如用户服务提供：<code>getUserById(1001)</code>Server 负责：<ul><li>启动服务监听端口。</li><li>注册服务到注册中心。</li><li>接收 RPC 请求。</li><li>反序列化请求。</li><li>根据服务名和方法名找到实现类。</li><li>执行真实业务方法。</li><li>封装返回结果或异常。</li><li>返回响应给客户端。</li></ul>简单说：<pre><code>Server 负责接收远程请求并执行真实方法</code></pre><ol><li><strong>它们之间如何协作</strong></li></ol>一次调用大概是：<pre><code>Client 业务代码<br/>  ↓<br/>Proxy 拦截方法调用<br/>  ↓<br/>Serializer 把请求转成字节<br/>  ↓<br/>Transport 发送到服务端<br/>  ↓<br/>Server 接收并执行真实方法<br/>  ↓<br/>Serializer 把响应转成字节<br/>  ↓<br/>Transport 返回响应<br/>  ↓<br/>Proxy 解析结果<br/>  ↓<br/>Client 拿到返回值</code></pre>可以用一句话理解：<pre><code>Client 提需求<br/>Proxy 翻译调用<br/>Serializer 打包数据<br/>Transport 负责运输<br/>Server 真正执行</code></pre><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 中 Client、Proxy、Serializer、Transport、Server 分别负责什么？在 RPC 调用中，Client 是服务调用方，负责发起业务调用并接收结果；Proxy 是客户端代理，是 RPC 屏蔽远程调用细节的关键，它会拦截本地方法调用，提取服务名、方法名、参数和参数类型，封装成 RpcRequest；Serializer 负责序列化和反序列化，因为网络只能传输字节流，所以需要把请求对象转成字节，也要把响应字节转回对象；Transport 是网络传输层，负责连接建立、请求发送、响应接收、协议编解码、超时和连接管理，底层可以基于 TCP、HTTP/2、Netty 等；Server 是服务提供方，负责接收请求，反序列化后根据服务名和方法名找到真实实现并执行，再把结果封装成响应返回。它们协作起来，就完成了从本地方法调用到远程方法执行再返回结果的完整 RPC 流程。面试中可以这样收口：Client 负责发起调用，Proxy 负责把方法调用变成 RPC 请求，Serializer 负责对象和字节转换，Transport 负责网络传输，Server 负责执行真实方法并返回结果。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：一个简单 RPC 框架可以按“服务注册与发现、客户端代理、序列化协议、网络通信、服务端反射调用、治理能力”来设计，核心目标是把本地方法调用转换成远程网络调用。<ol><li><strong>整体架构</strong></li></ol>一个简单 RPC 框架可以分成几层：<ul><li>Client Proxy：客户端代理层。</li><li>Serializer：序列化层。</li><li>Protocol：协议编解码层。</li><li>Transport：网络传输层。</li><li>Registry：注册中心。</li><li>Load Balancer：负载均衡。</li><li>Server Handler：服务端请求处理。</li><li>Service Invoker：服务反射调用。</li><li>Fault Tolerance：超时、重试、熔断等治理能力。</li></ul>整体流程是：<pre><code>客户端业务代码<br/>  ↓<br/>客户端动态代理<br/>  ↓<br/>封装 RPC Request<br/>  ↓<br/>序列化 + 编码<br/>  ↓<br/>服务发现 + 负载均衡<br/>  ↓<br/>网络发送<br/>  ↓<br/>服务端解码 + 反序列化<br/>  ↓<br/>查找服务实现<br/>  ↓<br/>反射调用真实方法<br/>  ↓<br/>序列化响应<br/>  ↓<br/>返回客户端</code></pre><ol><li><strong>客户端代理层</strong></li></ol>客户端不能直接调用远程服务。所以需要生成一个代理对象。业务代码调用：<code>userService.getUserById(1001)</code>实际执行的是代理逻辑。代理负责：<ul><li>拦截方法调用。</li><li>获取服务名。</li><li>获取方法名。</li><li>获取参数和参数类型。</li><li>封装成 RpcRequest。</li><li>发送远程请求。</li><li>等待 RpcResponse。</li><li>返回结果。</li></ul>在 Java 中可以用：<ul><li>JDK Dynamic Proxy。</li><li>CGLIB。</li><li>ByteBuddy。</li></ul><ol><li><strong>请求和响应模型</strong></li></ol>可以定义两个核心对象。RpcRequest 包含：<ul><li>requestId。</li><li>serviceName。</li><li>methodName。</li><li>parameterTypes。</li><li>parameters。</li><li>version。</li><li>timeout。</li></ul>RpcResponse 包含：<ul><li>requestId。</li><li>success。</li><li>result。</li><li>errorCode。</li><li>errorMessage。</li><li>exception。</li></ul><code>requestId</code> 很重要，用来匹配请求和响应，尤其是异步调用场景。<ol><li><strong>序列化层</strong></li></ol>网络只能传输字节。所以请求和响应对象必须序列化。可以支持多种序列化方式：<ul><li>JSON：可读性好，调试方便，但性能一般。</li><li>Protobuf：体积小，性能好，跨语言强。</li><li>Hessian：Java 生态常见。</li><li>Kryo：性能较好，但跨语言弱。</li></ul>简单 RPC 框架可以先用 JSON。后续再扩展 Protobuf 或 Hessian。设计时可以抽象一个接口：<pre><code>Serializer<br/>  - serialize(obj)<br/>  - deserialize(bytes, type)</code></pre>这样方便替换实现。<ol><li><strong>协议编解码层</strong></li></ol>除了序列化，还需要定义通信协议。否则服务端不知道一段字节代表什么。一个简单协议可以包含：<ul><li>magic number：识别协议。</li><li>version：协议版本。</li><li>serializer type：序列化类型。</li><li>message type：请求还是响应。</li><li>requestId：请求 ID。</li><li>body length：消息体长度。</li><li>body：消息内容。</li></ul>这样可以解决：<ul><li>粘包拆包。</li><li>协议识别。</li><li>多序列化方式扩展。</li><li>请求响应匹配。</li></ul><ol><li><strong>网络通信层</strong></li></ol>网络层负责客户端和服务端之间的数据传输。简单实现可以用：<ul><li>Java Socket。</li><li>Netty。</li><li>HTTP。</li></ul>如果追求性能和高并发，通常用 Netty。网络层要处理：<ul><li>建立连接。</li><li>发送请求。</li><li>接收响应。</li><li>编解码。</li><li>连接池。</li><li>超时。</li><li>异步回调。</li></ul>简单版本可以先做同步阻塞调用。进阶版本可以支持异步 Future。<ol><li><strong>注册中心和服务发现</strong></li></ol>微服务中服务地址不能写死。所以需要注册中心。服务端启动时注册服务：<pre><code>UserService -&gt; 10.0.0.1:9000<br/>OrderService -&gt; 10.0.0.2:9000</code></pre>客户端调用前从注册中心查询可用实例。注册中心可以用：<ul><li>ZooKeeper。</li><li>Nacos。</li><li>Etcd。</li><li>Consul。</li><li>Redis。</li><li>本地 Map，适合 demo。</li></ul>注册中心负责：<ul><li>服务注册。</li><li>服务发现。</li><li>服务下线。</li><li>健康检查。</li><li>地址变更通知。</li></ul><ol><li><strong>负载均衡</strong></li></ol>如果一个服务有多个实例，需要选择其中一个调用。常见策略：<ul><li>Random：随机。</li><li>Round Robin：轮询。</li><li>Weighted Round Robin：加权轮询。</li><li>Consistent Hash：一致性哈希。</li><li>Least Active：最少活跃调用。</li></ul>简单版本可以先实现随机或轮询。<ol><li><strong>服务端处理层</strong></li></ol>服务端启动后需要：<ul><li>暴露服务接口和实现类。</li><li>监听端口。</li><li>接收请求。</li><li>反序列化 RpcRequest。</li><li>根据 serviceName 找到实现对象。</li><li>根据 methodName 和 parameterTypes 找到方法。</li><li>通过反射调用。</li><li>封装 RpcResponse。</li><li>返回客户端。</li></ul>服务端需要维护一个服务映射表：<pre><code>serviceName -&gt; serviceImpl</code></pre>比如：<pre><code>UserService -&gt; UserServiceImpl</code></pre><ol><li><strong>服务治理能力</strong></li></ol>一个可用的 RPC 框架不能只完成调用，还要考虑稳定性。至少需要：<ul><li>Timeout：超时控制。</li><li>Retry：失败重试。</li><li>Circuit Breaker：熔断。</li><li>Rate Limit：限流。</li><li>Fallback：降级。</li><li>Heartbeat：心跳检测。</li><li>Metrics：指标监控。</li><li>Tracing：链路追踪。</li><li>Logging：调用日志。</li></ul>简单框架可以先实现：<pre><code>超时 + 重试 + 日志</code></pre>后续再扩展熔断和限流。<ol><li><strong>设计重点</strong></li></ol>面试时可以强调几个关键点：<ul><li>用动态代理屏蔽远程调用细节。</li><li>用序列化把对象转成字节流。</li><li>用协议解决消息边界和扩展性。</li><li>用注册中心实现服务发现。</li><li>用负载均衡选择服务实例。</li><li>服务端通过反射调用真实方法。</li><li>用超时、重试、熔断保证稳定性。</li><li>模块抽象要可插拔，比如序列化、负载均衡、注册中心都做成接口。</li></ul><ol><li><strong>面试背诵版</strong></li></ol>问题：如果让你设计一个简单 RPC 框架，整体架构应该如何设计？如果设计一个简单 RPC 框架，我会把它拆成客户端、网络通信、注册中心、服务端和服务治理几个模块。客户端侧通过动态代理生成接口代理对象，业务代码调用本地代理方法时，代理会把服务名、方法名、参数、参数类型和 requestId 封装成 RpcRequest，然后通过序列化和协议编码转成字节流。接着客户端从注册中心获取服务实例列表，通过负载均衡选择一个服务节点，再通过 TCP、HTTP/2 或 Netty 把请求发送到服务端。服务端收到请求后进行解码和反序列化，根据 serviceName 从本地服务映射表中找到对应实现类，再根据 methodName 和参数类型通过反射调用真实方法，最后把结果或异常封装成 RpcResponse 返回给客户端。注册中心负责服务注册、发现和下线，负载均衡负责多实例选择，序列化、注册中心、负载均衡都应该设计成可插拔接口。为了保证可用性，还需要支持超时、重试、熔断、限流、日志、监控和链路追踪等服务治理能力。简单版本可以先实现动态代理、序列化、网络传输、服务注册发现和反射调用，再逐步补充治理能力。面试中可以这样收口：简单 RPC 框架的核心就是：客户端代理把方法调用封装成请求，通过注册中心和负载均衡找到服务端，网络传输后由服务端反射调用真实方法，再把结果返回；外围再补上序列化、协议编解码和超时重试等治理能力。<hr/>";
  A["distributedgRPC 的工作原理是什么？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>Provider 是服务提供者，Consumer 是服务调用者，Registry 是注册中心</strong>；<strong>Provider 把服务地址注册到 Registry，Consumer 从 Registry 发现 Provider 地址，然后直接发起 RPC 调用。</strong><ol><li><strong>Provider 是什么</strong></li></ol>Provider 是服务提供者。它负责真正实现并暴露服务。比如用户服务提供：<code>UserService.getUserById()</code>Provider 启动后会：<ul><li>加载服务实现。</li><li>暴露 RPC 服务。</li><li>监听端口。</li><li>向 Registry 注册服务地址。</li><li>等待 Consumer 调用。</li></ul>它注册的信息通常包括：<ul><li>服务名。</li><li>IP。</li><li>端口。</li><li>协议。</li><li>版本。</li><li>分组。</li><li>权重。</li></ul><ol><li><strong>Consumer 是什么</strong></li></ol>Consumer 是服务消费者，也就是调用方。比如订单服务需要查询用户信息，就会作为 Consumer 调用用户服务。Consumer 启动后会：<ul><li>向 Registry 订阅目标服务。</li><li>获取 Provider 地址列表。</li><li>在本地缓存地址。</li><li>生成远程服务代理。</li><li>调用时通过负载均衡选择一个 Provider。</li><li>发起 RPC 请求。</li></ul>对业务代码来说，调用像本地接口。底层实际是远程调用。<ol><li><strong>Registry 是什么</strong></li></ol>Registry 是注册中心。它是 Provider 和 Consumer 之间的服务目录。它负责：<ul><li>接收 Provider 注册。</li><li>保存服务实例地址。</li><li>接收 Consumer 订阅。</li><li>返回可用 Provider 列表。</li><li>监听服务上下线变化。</li><li>通知 Consumer 更新地址缓存。</li></ul>常见注册中心有：<ul><li>ZooKeeper。</li><li>Nacos。</li><li>Redis。</li><li>Consul。</li><li>Etcd。</li></ul><ol><li><strong>三者关系</strong></li></ol>三者关系可以简单理解为：<pre><code>Provider：我提供服务，先去 Registry 登记<br/>Registry：我保存服务地址，告诉 Consumer 去哪里调用<br/>Consumer：我要调用服务，先问 Registry 有哪些 Provider</code></pre>流程是：<pre><code>Provider → Registry：注册服务<br/>Consumer → Registry：订阅服务<br/>Registry → Consumer：推送 Provider 地址列表<br/>Consumer → Provider：发起 RPC 调用</code></pre>注意：Consumer 调用 Provider 时，通常不是经过 Registry 转发。Registry 只负责服务发现和地址通知。真正的业务请求是：<pre><code>Consumer 直接调用 Provider</code></pre><ol><li><strong>服务变化时怎么处理</strong></li></ol>如果 Provider 新增实例：<pre><code>Provider 注册到 Registry<br/>Registry 通知 Consumer<br/>Consumer 更新本地地址列表</code></pre>如果 Provider 下线或故障：<pre><code>Registry 感知实例不可用<br/>从服务列表中移除<br/>通知 Consumer 更新缓存<br/>Consumer 不再调用该实例</code></pre>Consumer 本地一般会缓存 Provider 列表。所以不会每次调用都访问 Registry。<ol><li><strong>举个例子</strong></li></ol>假设有用户服务和订单服务。用户服务作为 Provider：<pre><code>UserService 注册到 Registry<br/>地址：10.0.0.1:20880</code></pre>订单服务作为 Consumer：<pre><code>订阅 UserService<br/>拿到地址 10.0.0.1:20880</code></pre>订单服务调用：<code>userService.getUserById(1001)</code>底层实际是：<pre><code>OrderService Consumer → UserService Provider</code></pre>Registry 不参与这次业务请求转发。<ol><li><strong>面试背诵版</strong></li></ol>问题：Dubbo Provider、Consumer、Registry 三者之间是什么关系？在 Dubbo 中，Provider 是服务提供者，负责实现并暴露具体的 RPC 服务；Consumer 是服务消费者，负责调用远程服务；Registry 是注册中心，负责服务注册和服务发现。Provider 启动后，会把自己的服务名、IP、端口、协议、版本、分组等信息注册到 Registry。Consumer 启动后，会向 Registry 订阅自己需要调用的服务，并获取可用 Provider 地址列表，缓存在本地。之后 Consumer 发起调用时，会通过 Dubbo 代理和负载均衡从本地地址列表中选择一个 Provider，直接发起 RPC 调用。需要注意的是，Registry 只负责保存服务地址和通知服务变更，不参与实际业务请求转发；真正的调用链路是 Consumer 直接调用 Provider。当 Provider 上线、下线或地址变化时，Registry 会通知 Consumer 更新本地缓存。面试中可以这样收口：Provider 负责注册并提供服务，Consumer 负责发现并调用服务，Registry 负责维护服务地址；Registry 不转发业务流量，Consumer 拿到地址后直接调用 Provider。<hr/>";
  A["distributedRPC 如何实现服务发现？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 服务注册是服务提供者启动后把自己的服务名、IP、端口等信息上报到注册中心</strong>；<strong>服务发现是调用方从注册中心获取可用实例列表，并通过负载均衡选择实例发起调用。</strong><ol><li><strong>服务注册是什么</strong></li></ol>服务注册发生在服务提供方启动时。比如用户服务启动后，会向注册中心写入自己的服务信息。通常包括：<ul><li>服务名。</li><li>IP 地址。</li><li>端口。</li><li>协议。</li><li>版本号。</li><li>分组。</li><li>权重。</li><li>健康状态。</li><li>元数据。</li></ul>例如：<pre><code>UserService -&gt; 10.0.0.1:9000</code></pre>这样注册中心就知道：<pre><code>当前有一个 UserService 实例可用</code></pre><ol><li><strong>服务发现是什么</strong></li></ol>服务发现发生在服务调用方。比如订单服务要调用用户服务。它不会写死用户服务 IP。而是向注册中心查询：<pre><code>UserService 有哪些可用实例？</code></pre>注册中心返回：<pre><code>10.0.0.1:9000<br/>10.0.0.2:9000<br/>10.0.0.3:9000</code></pre>然后客户端从中选择一个实例发起 RPC 调用。<ol><li><strong>完整流程</strong></li></ol>完整流程一般是：<pre><code>服务提供者启动<br/>  ↓<br/>暴露 RPC 服务<br/>  ↓<br/>向注册中心注册服务名、IP、端口、元数据<br/>  ↓<br/>注册中心保存服务实例信息<br/>  ↓<br/>服务消费者启动<br/>  ↓<br/>向注册中心订阅或查询目标服务<br/>  ↓<br/>获取可用实例列表<br/>  ↓<br/>缓存在本地<br/>  ↓<br/>通过负载均衡选择一个实例<br/>  ↓<br/>发起 RPC 调用</code></pre>当服务实例变化时：<pre><code>服务上线 / 下线 / 故障<br/>  ↓<br/>注册中心更新服务列表<br/>  ↓<br/>通知消费者<br/>  ↓<br/>消费者更新本地缓存</code></pre><ol><li><strong>健康检查和心跳</strong></li></ol>注册中心需要知道服务实例是否还活着。常见方式是心跳机制。服务提供者会定期发送心跳：<pre><code>我还活着</code></pre>如果注册中心一段时间收不到心跳，就认为该实例不可用。然后把它从可用列表中移除。这样消费者就不会继续调用故障实例。<ol><li><strong>客户端本地缓存</strong></li></ol>RPC 客户端通常不会每次调用都查注册中心。因为这样会增加延迟，也会给注册中心造成压力。通常做法是：<ul><li>启动时拉取服务列表。</li><li>本地缓存实例列表。</li><li>订阅注册中心变更。</li><li>有变化时更新本地缓存。</li></ul>调用时直接从本地缓存中选实例。这样性能更好。<ol><li><strong>负载均衡</strong></li></ol>服务发现只负责拿到实例列表。真正调用哪个实例，要靠负载均衡。常见策略：<ul><li>随机。</li><li>轮询。</li><li>加权轮询。</li><li>一致性哈希。</li><li>最少活跃调用。</li></ul>比如注册中心返回 3 个用户服务实例，客户端通过负载均衡选择其中一个。<ol><li><strong>服务下线流程</strong></li></ol>服务正常下线时，应该先从注册中心注销。流程是：<pre><code>服务准备下线<br/>  ↓<br/>停止接收新请求<br/>  ↓<br/>向注册中心注销实例<br/>  ↓<br/>等待已有请求处理完成<br/>  ↓<br/>关闭服务</code></pre>如果服务异常宕机，则依赖心跳超时和健康检查把实例剔除。<ol><li><strong>常见注册中心</strong></li></ol>常见注册中心有：<ul><li>ZooKeeper。</li><li>Nacos。</li><li>Etcd。</li><li>Consul。</li><li>Eureka。</li><li>Kubernetes Service / DNS。</li></ul>不同注册中心实现方式不同，但核心都是：<pre><code>服务注册 + 服务发现 + 健康检查 + 变更通知</code></pre><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 服务注册和发现流程是什么？RPC 服务注册和发现主要用于解决服务地址动态变化的问题。服务提供者启动后，会暴露自己的 RPC 服务，并把服务名、IP、端口、协议、版本、分组、权重等元数据注册到注册中心。注册中心保存这些服务实例信息，并通过心跳或健康检查判断实例是否可用。服务消费者启动或调用服务时，会向注册中心查询或订阅目标服务，获取可用实例列表，并缓存在本地。之后每次 RPC 调用时，消费者通常不会再实时查询注册中心，而是从本地缓存的实例列表中通过负载均衡选择一个服务实例发起调用。当服务实例上线、下线或故障时，注册中心会更新服务列表，并通知消费者刷新本地缓存。服务正常下线时会主动注销，异常宕机时则依赖心跳超时或健康检查剔除。面试中可以这样收口：服务注册是提供方把自己的地址和元数据上报到注册中心，服务发现是调用方从注册中心拿到可用实例列表并本地缓存，再通过负载均衡选择实例进行 RPC 调用。<hr/>";
  A["distributedRPC 如何实现负载均衡？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 需要负载均衡，是因为同一个服务通常有多个实例，调用方需要从这些实例中选择一个来调用，避免请求集中到单个节点，提高系统吞吐、稳定性和可用性。</strong><ol><li><strong>为什么会有多个服务实例</strong></li></ol>在微服务架构中，一个服务通常不会只部署一个实例。比如用户服务可能有：<ul><li><code>UserService-1</code></li><li><code>UserService-2</code></li><li><code>UserService-3</code></li></ul>它们提供相同能力，但部署在不同机器或容器上。这样做是为了：<ul><li>提高并发能力。</li><li>避免单点故障。</li><li>支持扩容缩容。</li><li>提高系统可用性。</li></ul>既然有多个实例，调用方就需要决定：<pre><code>这次请求调用哪一个实例？</code></pre>这就是负载均衡要解决的问题。<ol><li><strong>负载均衡解决什么问题</strong></li></ol>负载均衡的作用是：把请求合理分配到多个服务实例上。它可以避免：<pre><code>某个实例压力过大<br/>其他实例很空闲</code></pre>也可以在某个实例故障时，把流量切到其他健康实例。所以它主要解决：<ul><li>请求分摊。</li><li>避免单点过载。</li><li>提高吞吐量。</li><li>提高可用性。</li><li>支持水平扩展。</li><li>配合故障摘除。</li></ul><ol><li><strong>RPC 中负载均衡的位置</strong></li></ol>RPC 调用一般是：<pre><code>客户端从注册中心拿到服务实例列表<br/>  ↓<br/>负载均衡选择一个实例<br/>  ↓<br/>发起 RPC 调用</code></pre>注册中心负责告诉客户端：<pre><code>有哪些可用实例</code></pre>负载均衡负责决定：<pre><code>这次具体调哪个实例</code></pre><ol><li><strong>常见负载均衡策略</strong></li></ol>随机 Random：从实例列表中随机选一个。优点是简单。轮询 Round Robin：按顺序轮流调用实例。比如：<pre><code>A → B → C → A → B → C</code></pre>加权轮询 Weighted Round Robin：给性能更好的机器更高权重。比如：<pre><code>A 权重 5<br/>B 权重 1</code></pre>A 会承担更多请求。一致性哈希 Consistent Hash：根据某个 key 选择固定实例。比如同一个用户 ID 总是路由到同一个节点。适合有状态缓存场景。最少活跃 Least Active：优先选择当前请求数最少、最空闲的实例。适合实例性能不均或请求耗时差异大的场景。<ol><li><strong>客户端负载均衡和服务端负载均衡</strong></li></ol>RPC 中常见的是客户端负载均衡。也就是：客户端从注册中心拿到所有实例列表，然后自己选择一个调用。优点是：<ul><li>少一层中间转发。</li><li>性能更好。</li><li>调用链更短。</li><li>客户端可感知服务状态。</li></ul>服务端负载均衡则是：请求先到负载均衡器，比如 Nginx、网关、SLB，再由它转发到后端服务。它更常见于 HTTP API 或入口流量。<ol><li><strong>负载均衡和容错的关系</strong></li></ol>负载均衡通常会和容错机制配合。比如：<ul><li>某个实例超时，就降低它的权重。</li><li>某个实例连续失败，就临时摘除。</li><li>重试时换另一个实例。</li><li>熔断后不再调用异常实例。</li><li>健康检查恢复后再加回来。</li></ul>这样可以避免请求持续打到异常节点上。<ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 为什么需要负载均衡？RPC 需要负载均衡，是因为在微服务架构中，同一个服务通常会部署多个实例来提高并发能力和可用性。调用方通过注册中心拿到的是一个服务实例列表，但每次调用时还需要决定具体请求哪个实例。负载均衡的作用就是把请求合理分配到多个服务节点上，避免流量集中到某一个实例导致过载，同时提高整体吞吐量、稳定性和容错能力。常见策略包括随机、轮询、加权轮询、一致性哈希和最少活跃调用等。在 RPC 中常见的是客户端负载均衡，也就是客户端从注册中心获取实例列表后，在本地选择一个实例发起调用，这样可以减少中间转发、降低延迟。负载均衡还会和超时、重试、熔断、健康检查配合，把异常节点摘除或降低权重，提升系统可用性。面试中可以这样收口：RPC 负载均衡的核心作用是从多个服务实例中选一个合适的来调用，把流量分摊开，避免单点过载，并配合故障摘除提升系统可用性。<hr/>";
  A["distributedRPC 调用失败如何处理？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 请求失败后不能只简单重试，要根据失败类型选择超时控制、重试、熔断、降级、限流和幂等保护，核心目标是避免故障扩大并保证业务可接受。</strong><ol><li><strong>RPC 请求为什么会失败</strong></li></ol>RPC 本质是远程网络调用，所以失败很常见。常见原因包括：<ul><li>网络抖动。</li><li>连接失败。</li><li>请求超时。</li><li>服务端宕机。</li><li>服务端线程池打满。</li><li>下游数据库慢。</li><li>序列化失败。</li><li>服务发布或重启。</li><li>注册中心地址过期。</li><li>服务端业务异常。</li></ul>所以 RPC 调用一定要按“不可靠调用”来设计。<ol><li><strong>超时控制</strong></li></ol>第一步一定是设置超时时间。不能让调用无限等待。比如：<pre><code>调用用户服务最多等待 500ms</code></pre>超过时间就快速失败。超时控制的作用是：<ul><li>避免线程被长时间占用。</li><li>防止请求堆积。</li><li>避免故障扩散。</li><li>让上游有机会降级处理。</li></ul>没有超时的 RPC 调用很危险。<ol><li><strong>重试机制</strong></li></ol>对于短暂网络抖动或偶发失败，可以重试。比如：<pre><code>最多重试 2 次</code></pre>但重试要谨慎。适合重试的场景：<ul><li>网络抖动。</li><li>连接失败。</li><li>临时超时。</li><li>读请求。</li><li>幂等接口。</li></ul>不适合随便重试的场景：<ul><li>创建订单。</li><li>扣库存。</li><li>扣款。</li><li>发优惠券。</li><li>非幂等写操作。</li></ul>因为重试可能导致重复执行。所以重试必须配合：<pre><code>幂等设计</code></pre><ol><li><strong>幂等保护</strong></li></ol>幂等是指：同一个请求执行多次，结果和执行一次一样。比如可以用：<ul><li>requestId。</li><li>业务唯一单号。</li><li>去重表。</li><li>分布式锁。</li><li>状态机校验。</li><li>数据库唯一索引。</li></ul>比如支付接口，如果重试了两次，也不能扣两次钱。所以对于写操作，RPC 失败处理必须考虑幂等。<ol><li><strong>熔断</strong></li></ol>如果某个下游服务持续失败，不能一直调用它。否则会导致：<ul><li>上游线程被拖住。</li><li>请求大量堆积。</li><li>故障级联扩散。</li><li>整个系统雪崩。</li></ul>熔断机制会在失败率过高时，临时停止调用该服务。流程类似：<pre><code>失败率过高<br/>  ↓<br/>打开熔断器<br/>  ↓<br/>请求快速失败或走降级<br/>  ↓<br/>过一段时间半开探测<br/>  ↓<br/>恢复正常后关闭熔断</code></pre><ol><li><strong>降级</strong></li></ol>降级是指：当远程服务不可用时，返回一个可接受的兜底结果。比如：<ul><li>推荐服务失败，返回热门商品。</li><li>用户画像失败，使用默认画像。</li><li>积分服务失败，先不展示积分。</li><li>评论服务失败，隐藏评论模块。</li><li>非核心服务失败，不影响主流程。</li></ul>降级的核心是：<pre><code>保核心链路，牺牲非核心功能</code></pre><ol><li><strong>限流和隔离</strong></li></ol>如果请求量太大，也要做限流。限流可以保护服务不被打垮。常见方式：<ul><li>QPS 限流。</li><li>并发数限制。</li><li>令牌桶。</li><li>漏桶。</li></ul>隔离也很重要。比如：<ul><li>线程池隔离。</li><li>连接池隔离。</li><li>服务隔离。</li><li>资源隔离。</li></ul>这样某个下游慢，不会拖垮整个系统。<ol><li><strong>切换实例</strong></li></ol>如果某个实例失败，可以配合负载均衡切到其他实例。比如：<pre><code>第一次调用 A 失败<br/>重试时换 B</code></pre>同时可以：<ul><li>降低异常实例权重。</li><li>临时摘除异常实例。</li><li>等健康检查恢复后再加入。</li></ul>这通常和服务发现、负载均衡、健康检查配合使用。<ol><li><strong>监控和告警</strong></li></ol>RPC 失败还要做好观测。需要记录：<ul><li>调用成功率。</li><li>超时率。</li><li>错误率。</li><li>P95 / P99 延迟。</li><li>重试次数。</li><li>熔断次数。</li><li>下游异常分布。</li><li>traceId 调用链。</li></ul>这样才能快速定位是：<ul><li>网络问题。</li><li>某个实例问题。</li><li>下游服务问题。</li><li>数据库慢。</li><li>流量突增。</li></ul><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 请求失败后如何处理？RPC 请求失败后要根据失败类型进行处理，不能简单无脑重试。首先要设置合理的超时时间，避免调用方线程长时间阻塞，导致请求堆积和故障扩散。对于网络抖动、临时超时这类短暂问题，可以做有限次数重试，但重试必须谨慎，尤其是写操作，比如创建订单、扣库存、扣款等，必须保证接口幂等，可以通过 requestId、业务唯一单号、去重表、状态机或数据库唯一索引来避免重复执行。如果某个下游服务持续失败，需要使用熔断机制，让请求快速失败或走降级逻辑，防止故障级联扩散。对于非核心功能，可以降级返回默认值或兜底结果，保证核心链路可用。同时还可以配合限流、线程池隔离、连接池隔离、负载均衡切换实例、健康检查和异常节点摘除。最后，要通过监控和链路追踪观察错误率、超时率、延迟和重试次数，方便定位问题。面试中可以这样收口：RPC 失败处理的核心是超时先止损，重试要有限且保证幂等，持续失败要熔断降级，再配合限流隔离、实例切换和监控，防止局部故障演变成系统雪崩。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 需要超时机制，是为了避免远程调用长时间阻塞调用方线程，导致资源耗尽和故障扩散</strong><strong>；超时时间要根据业务 SLA、下游延迟分布、调用链路和重试策略综合设置。</strong><ol><li><strong>为什么 RPC 需要超时机制</strong></li></ol>RPC 是远程调用，依赖网络和下游服务。它可能因为各种原因变慢：<ul><li>网络抖动。</li><li>下游服务负载高。</li><li>数据库慢查询。</li><li>服务端线程池打满。</li><li>GC 停顿。</li><li>连接异常。</li><li>依赖服务卡住。</li></ul>如果没有超时机制，调用方可能一直等待。结果是：<ul><li>线程被占满。</li><li>连接被占满。</li><li>请求不断堆积。</li><li>上游服务也被拖慢。</li><li>故障向上游扩散。</li><li>最后可能导致系统雪崩。</li></ul>所以超时机制的核心作用是：<pre><code>快速失败，释放资源，避免故障扩散</code></pre><ol><li><strong>超时机制解决什么问题</strong></li></ol>超时可以解决：<ul><li>防止调用无限等待。</li><li>限制单次调用最大耗时。</li><li>保护调用方线程池。</li><li>控制整体请求延迟。</li><li>给重试、降级、熔断留出时间。</li><li>避免慢下游拖垮上游。</li></ul>简单说：<pre><code>远程调用不能无限等</code></pre><ol><li><strong>超时时间不能随便设</strong></li></ol>超时时间太短：<ul><li>正常请求可能被误判超时。</li><li>失败率升高。</li><li>重试增多。</li><li>下游压力反而变大。</li><li>用户体验变差。</li></ul>超时时间太长：<ul><li>请求线程长期阻塞。</li><li>故障发现慢。</li><li>资源被占用。</li><li>调用链整体延迟变高。</li><li>容易引发级联故障。</li></ul>所以超时设置需要平衡：<pre><code>不能太短，也不能太长</code></pre><ol><li><strong>如何设置超时时间</strong></li></ol>第一，看业务 SLA。比如整个接口要求：<pre><code>用户请求 1 秒内返回</code></pre>那内部多次 RPC 调用的超时总和不能超过 1 秒。要给每个下游分配时间预算。第二，看下游延迟分布。不能只看平均耗时。要看：<ul><li>P90。</li><li>P95。</li><li>P99。</li><li>最大值。</li><li>抖动情况。</li></ul>一般可以根据 P95 或 P99 设置超时。比如某接口 P99 是 200ms，可以考虑设置：<pre><code>250ms ~ 300ms</code></pre>第三，看接口重要性。核心链路可以适当更谨慎。非核心服务可以设置更短超时，失败后降级。比如：<ul><li>支付服务：超时更严格但要谨慎处理。</li><li>推荐服务：可以短超时，失败返回默认推荐。</li><li>日志上报：可以异步或更短超时。</li></ul>第四，看调用链路层级。如果一个请求会调用多个下游，不能每个都设很长。否则总延迟会失控。应该做全链路 timeout budget。例如：<pre><code>总超时 1000ms<br/>用户服务 100ms<br/>库存服务 200ms<br/>优惠券服务 150ms<br/>支付服务 300ms<br/>预留降级和网络开销</code></pre>第五，结合重试策略。如果要重试，单次超时要更短。比如总预算 300ms，最多重试 2 次。不能每次都等 300ms。否则最坏情况变成：<pre><code>300ms × 3 = 900ms</code></pre>应该控制总耗时。<ol><li><strong>常见设置经验</strong></li></ol>常见经验是：<ul><li>不使用默认无限超时。</li><li>不只看平均响应时间。</li><li>基于 P95/P99 延迟设置。</li><li>核心接口按 SLA 分配预算。</li><li>非核心接口短超时 + 降级。</li><li>重试要有总超时限制。</li><li>不同接口设置不同超时。</li><li>动态根据监控数据调整。</li></ul>不要所有 RPC 都统一设置成一个固定值，比如全部 3 秒。这通常不合理。<ol><li><strong>和熔断降级的关系</strong></li></ol>超时通常会触发后续机制：<pre><code>超时增加<br/>  ↓<br/>错误率升高<br/>  ↓<br/>达到阈值后熔断<br/>  ↓<br/>请求快速失败或走降级</code></pre>所以超时是服务治理的基础。没有超时，就很难及时发现下游异常。<ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 为什么需要超时机制？如何设置超时时间？RPC 需要超时机制，是因为它本质上是远程网络调用，可能受到网络抖动、下游服务变慢、数据库慢查询、线程池打满等因素影响。如果没有超时，调用方线程会一直阻塞，连接和线程资源被占满，请求不断堆积，最终可能导致故障向上游扩散，甚至引发系统雪崩。超时机制的作用是限制单次调用最大耗时，让请求快速失败并释放资源，同时为重试、熔断和降级提供基础。超时时间不能随便设，太短会误杀正常请求，导致失败率和重试增加；太长又会拖慢调用链并占用资源。实际设置时要结合业务 SLA、下游接口的 P95/P99 延迟、调用链路的总时间预算、接口重要性和重试策略来决定。一般不要只看平均耗时，也不要所有接口统一设置一个固定超时；核心接口按链路预算分配，非核心接口可以短超时并配合降级，重试场景还要控制总超时时间。面试中可以这样收口：RPC 超时机制的核心是避免远程调用无限等待，保护调用方资源；超时时间要基于 SLA、P95/P99 延迟、调用链预算和重试策略来设置，不能太短也不能太长。<span class=\"ph\">[原文档此处有配图：image.png]</span>核心回答：<strong>RPC 重试要限制次数、控制间隔、区分异常类型，并且只对幂等或可去重的请求重试</strong>；<strong>因为第一次请求可能已经在服务端执行成功，只是响应丢失，客户端再重试就可能造成重复写入或重复扣款。</strong><ol><li><strong>为什么需要重试机制</strong></li></ol>RPC 调用可能因为临时问题失败。比如：<ul><li>网络抖动。</li><li>连接短暂失败。</li><li>服务实例瞬时不可用。</li><li>请求超时。</li><li>服务发布重启。</li><li>注册中心地址短暂不一致。</li></ul>这些问题有时候不是业务失败，而是瞬时故障。重试可以提高调用成功率和系统可用性。<ol><li><strong>哪些情况适合重试</strong></li></ol>适合重试的情况：<ul><li>网络连接失败。</li><li>临时超时。</li><li>服务端 503。</li><li>瞬时限流后可等待重试。</li><li>读请求。</li><li>幂等写请求。</li></ul>比如：<pre><code>查询用户信息<br/>查询商品库存<br/>获取订单状态</code></pre>这类请求多次执行通常不会改变数据。比较适合重试。<ol><li><strong>哪些情况不适合重试</strong></li></ol>不适合盲目重试的情况：<ul><li>创建订单。</li><li>扣库存。</li><li>扣款。</li><li>发优惠券。</li><li>创建支付单。</li><li>发送短信。</li><li>发送消息。</li><li>非幂等写操作。</li></ul>因为这些操作执行多次可能产生副作用。比如：<pre><code>扣款请求重试两次<br/>可能扣两次钱</code></pre>所以写操作如果要重试，必须先做幂等设计。<ol><li><strong>为什么重试可能导致数据重复</strong></li></ol>关键原因是：客户端看到失败，不代表服务端没执行。比如一次扣款 RPC：<pre><code>客户端发送扣款请求<br/>  ↓<br/>服务端收到请求<br/>  ↓<br/>服务端扣款成功<br/>  ↓<br/>服务端返回响应<br/>  ↓<br/>响应在网络中丢失 / 客户端超时<br/>  ↓<br/>客户端以为失败<br/>  ↓<br/>发起重试<br/>  ↓<br/>服务端再次扣款</code></pre>这就导致重复执行。所以 RPC 中经常出现：<pre><code>调用失败 ≠ 业务未执行</code></pre>这是重试风险的核心。<ol><li><strong>如何设计重试机制</strong></li></ol>第一，限制重试次数。比如最多重试 1 到 2 次。不能无限重试。否则下游故障时，重试会放大流量，造成雪崩。第二，设置重试间隔。不要立即疯狂重试。可以使用：<ul><li>固定间隔。</li><li>指数退避。</li><li>随机抖动 jitter。</li></ul>比如：<pre><code>100ms → 200ms → 400ms</code></pre>加 jitter 可以避免大量请求同时重试。第三，区分异常类型。不是所有错误都重试。可以重试：<ul><li>网络异常。</li><li>连接失败。</li><li>临时超时。</li><li>部分 5xx。</li></ul>不应该重试：<ul><li>参数错误。</li><li>权限错误。</li><li>业务校验失败。</li><li>明确的 4xx 错误。</li><li>非幂等写操作。</li></ul>第四，重试换实例。如果一个服务实例失败，重试时可以换另一个实例。比如：<pre><code>第一次调用 A 失败<br/>重试调用 B</code></pre>这样可以绕开故障节点。第五，控制总超时时间。不能每次重试都用完整超时时间。比如总预算 300ms，最多重试 2 次。应该控制整体耗时不超过预算。第六，配合熔断限流。当下游已经明显异常时，不应该继续重试。应该熔断或降级。否则重试会把下游压垮。<ol><li><strong>如何避免重复数据</strong></li></ol>核心是幂等设计。常见方法：使用 requestId。客户端每次业务请求生成唯一 requestId。服务端处理前先检查：<pre><code>这个 requestId 是否处理过</code></pre>如果处理过，直接返回之前结果。使用业务唯一键。比如订单号、支付单号、交易流水号。数据库加唯一索引：<pre><code>order_id 唯一<br/>transaction_id 唯一</code></pre>重复请求插入时会失败或返回已有记录。使用去重表。把请求 ID 记录到去重表中。如果重复请求进来，直接识别。使用状态机。比如订单状态只能：<pre><code>待支付 → 已支付</code></pre>不能重复从已支付再扣一次。使用分布式锁。对同一个业务 key 加锁，避免并发重复处理。但锁要注意超时和可靠性。<ol><li><strong>重试会放大流量</strong></li></ol>假设原本 QPS 是 1000。如果每个请求失败后重试 2 次。最坏情况下，下游承受：<pre><code>3000 QPS</code></pre>这叫重试风暴。所以重试必须配合：<ul><li>限流。</li><li>熔断。</li><li>退避。</li><li>最大次数。</li><li>总超时。</li><li>随机抖动。</li></ul><ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 重试机制如何设计？为什么重试可能导致数据重复？RPC 重试机制主要用于处理网络抖动、临时超时、服务实例短暂不可用等瞬时失败，但不能无脑重试。设计时首先要限制最大重试次数，比如 1 到 2 次，并设置合理的重试间隔，常见做法是指数退避加随机抖动，避免大量请求同时重试形成重试风暴。其次要区分异常类型，网络异常、连接失败、临时超时、部分 5xx 可以考虑重试，但参数错误、权限错误、业务校验失败以及非幂等写操作不应该随便重试。重试时还可以配合负载均衡切换服务实例，并且要控制总超时时间，避免一次调用因为多次重试拖得太久。当下游持续异常时，还要配合熔断、限流和降级，防止故障扩大。重试可能导致数据重复，是因为客户端看到请求失败或超时，并不代表服务端没有执行成功；可能服务端已经完成扣款、下单或发券，只是响应丢失，客户端再次重试就会导致重复执行。因此写操作如果要支持重试，必须做幂等设计，比如使用 requestId、业务唯一键、去重表、数据库唯一索引、状态机或分布式锁来保证同一个业务请求只生效一次。面试中可以这样收口：RPC 重试只能用于瞬时失败，并且要限次数、控间隔、分异常、控总超时；重试导致重复的根本原因是“调用失败不等于服务端没执行”，所以写操作必须配合幂等设计。<span class=\"ph\">[原文档此处有配图：image.png]</span>v核心回答：<strong>RPC 接口幂等性的核心是给每个业务请求一个唯一标识，并在服务端做去重或状态校验，保证同一个请求重复调用多次只生效一次。</strong><ol><li><strong>什么是幂等性</strong></li></ol>幂等性指的是：同一个请求执行一次和执行多次，最终结果是一致的。比如查询用户信息：<pre><code>查一次和查十次，数据不会改变</code></pre>它天然是幂等的。但有些写操作不是幂等的，比如：<ul><li>创建订单。</li><li>扣库存。</li><li>扣款。</li><li>发优惠券。</li><li>发短信。</li><li>新增积分。</li></ul>这些操作重复执行可能导致：<ul><li>重复订单。</li><li>库存多扣。</li><li>钱多扣。</li><li>优惠券多发。</li><li>消息重复发送。</li></ul><ol><li><strong>RPC 为什么需要幂等</strong></li></ol>RPC 调用可能失败、超时或重试。客户端看到失败时，不一定代表服务端没执行。可能是：<pre><code>服务端执行成功<br/>但响应丢失<br/>客户端超时后重试</code></pre>如果接口不幂等，重试就可能导致重复数据。所以幂等性是 RPC 重试机制的前提之一。<ol><li><strong>使用 requestId 去重</strong></li></ol>最常见方式是：客户端为每个业务请求生成唯一 <code>requestId</code>。服务端处理前先检查这个 <code>requestId</code> 是否处理过。流程：<pre><code>收到请求<br/>  ↓<br/>检查 requestId 是否存在<br/>  ↓<br/>不存在：执行业务，记录 requestId 和结果<br/>  ↓<br/>已存在：直接返回之前结果</code></pre>这样同一个请求重复调用，也只会真正执行一次。<code>requestId</code> 可以是：<ul><li>UUID。</li><li>雪花 ID。</li><li>业务流水号。</li><li>traceId + 业务 ID。</li></ul>注意：<pre><code>requestId 必须代表同一次业务请求<br/>重试时不能生成新的 requestId</code></pre><ol><li><strong>使用业务唯一键</strong></li></ol>很多业务天然有唯一键。比如：<ul><li>订单号。</li><li>支付流水号。</li><li>交易号。</li><li>用户 ID + 活动 ID。</li><li>消息 ID。</li></ul>可以用业务唯一键做幂等。比如创建订单时：<pre><code>order_no 唯一</code></pre>数据库加唯一索引。如果重复请求再次插入同一个订单号，会被唯一索引拦住。然后返回已有订单即可。<ol><li><strong>去重表</strong></li></ol>可以专门建一张去重表。字段包括：<ul><li>requestId。</li><li>businessType。</li><li>status。</li><li>result。</li><li>createTime。</li></ul>处理请求时先插入去重记录。如果插入成功，说明第一次处理。如果唯一键冲突，说明重复请求。这种方式适合通用幂等组件。<ol><li><strong>状态机校验</strong></li></ol>很多业务有明确状态流转。比如订单：<pre><code>待支付 → 已支付 → 已发货 → 已完成</code></pre>扣款或支付回调时，要检查当前状态。比如：<pre><code>只有待支付订单才能执行支付成功逻辑</code></pre>如果订单已经是已支付，再来一次支付成功请求，就直接返回成功，不重复处理。状态机能很好防止重复推进。<ol><li><strong>数据库唯一约束</strong></li></ol>数据库唯一索引是非常重要的兜底手段。比如：<pre><code>transaction_id 唯一<br/>order_no 唯一<br/>user_id + coupon_id 唯一</code></pre>即使应用层判断出问题，数据库也能防止重复写入。常见做法：<pre><code>先插入唯一记录<br/>再执行业务逻辑</code></pre>或者利用唯一键冲突识别重复请求。<ol><li><strong>分布式锁</strong></li></ol>对于并发重复请求，可以对业务 key 加锁。比如：<pre><code>lock:pay:order123</code></pre>同一时间只允许一个请求处理该订单。但分布式锁要注意：<ul><li>锁超时时间。</li><li>死锁问题。</li><li>锁释放安全。</li><li>性能开销。</li><li>不能只依赖锁，最好配合唯一约束。</li></ul><ol><li><strong>如何返回结果</strong></li></ol>重复请求进来时，最好不要直接报错。更好的做法是：<ul><li>如果第一次已经成功，返回相同成功结果。</li><li>如果第一次处理中，返回处理中或稍后重试。</li><li>如果第一次失败，根据业务决定是否允许重新处理。</li></ul>也就是说，幂等不仅是“不重复执行”，还要让调用方得到稳定结果。<ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 如何保证接口幂等性？RPC 保证接口幂等性的核心是让同一个业务请求重复调用多次只生效一次。因为 RPC 可能出现超时、响应丢失或重试，客户端看到失败并不代表服务端没有执行成功，如果没有幂等保护，创建订单、扣库存、扣款、发券等写操作就可能重复执行。常见做法是客户端为每个业务请求生成唯一 requestId，并且重试时复用同一个 requestId，服务端处理前先检查该 requestId 是否已经处理过，如果处理过就直接返回之前结果，如果没处理过再执行业务并记录 requestId。也可以使用业务唯一键，比如订单号、支付流水号、用户 ID 加活动 ID，并通过数据库唯一索引防止重复写入。对于复杂业务，还可以使用去重表、状态机校验和分布式锁，比如订单只能从待支付流转到已支付，已经支付过的请求再次到来就直接返回成功。实际设计中通常会把 requestId、业务唯一键、数据库唯一约束和状态机结合起来，保证重复请求不会造成重复扣款、重复下单或重复发券。面试中可以这样收口：RPC 幂等的核心是用 requestId 或业务唯一键识别同一次请求，服务端做去重、唯一约束和状态校验，保证重试或重复调用时业务只执行一次。<hr/>";
  A["distributedRPC 如何保证接口幂等？"] = "<span class=\"ph\">[原文档此处有配图：image.png]</span>v核心回答：<strong>RPC 接口幂等性的核心是给每个业务请求一个唯一标识，并在服务端做去重或状态校验，保证同一个请求重复调用多次只生效一次。</strong><ol><li><strong>什么是幂等性</strong></li></ol>幂等性指的是：同一个请求执行一次和执行多次，最终结果是一致的。比如查询用户信息：<pre><code>查一次和查十次，数据不会改变</code></pre>它天然是幂等的。但有些写操作不是幂等的，比如：<ul><li>创建订单。</li><li>扣库存。</li><li>扣款。</li><li>发优惠券。</li><li>发短信。</li><li>新增积分。</li></ul>这些操作重复执行可能导致：<ul><li>重复订单。</li><li>库存多扣。</li><li>钱多扣。</li><li>优惠券多发。</li><li>消息重复发送。</li></ul><ol><li><strong>RPC 为什么需要幂等</strong></li></ol>RPC 调用可能失败、超时或重试。客户端看到失败时，不一定代表服务端没执行。可能是：<pre><code>服务端执行成功<br/>但响应丢失<br/>客户端超时后重试</code></pre>如果接口不幂等，重试就可能导致重复数据。所以幂等性是 RPC 重试机制的前提之一。<ol><li><strong>使用 requestId 去重</strong></li></ol>最常见方式是：客户端为每个业务请求生成唯一 <code>requestId</code>。服务端处理前先检查这个 <code>requestId</code> 是否处理过。流程：<pre><code>收到请求<br/>  ↓<br/>检查 requestId 是否存在<br/>  ↓<br/>不存在：执行业务，记录 requestId 和结果<br/>  ↓<br/>已存在：直接返回之前结果</code></pre>这样同一个请求重复调用，也只会真正执行一次。<code>requestId</code> 可以是：<ul><li>UUID。</li><li>雪花 ID。</li><li>业务流水号。</li><li>traceId + 业务 ID。</li></ul>注意：<pre><code>requestId 必须代表同一次业务请求<br/>重试时不能生成新的 requestId</code></pre><ol><li><strong>使用业务唯一键</strong></li></ol>很多业务天然有唯一键。比如：<ul><li>订单号。</li><li>支付流水号。</li><li>交易号。</li><li>用户 ID + 活动 ID。</li><li>消息 ID。</li></ul>可以用业务唯一键做幂等。比如创建订单时：<pre><code>order_no 唯一</code></pre>数据库加唯一索引。如果重复请求再次插入同一个订单号，会被唯一索引拦住。然后返回已有订单即可。<ol><li><strong>去重表</strong></li></ol>可以专门建一张去重表。字段包括：<ul><li>requestId。</li><li>businessType。</li><li>status。</li><li>result。</li><li>createTime。</li></ul>处理请求时先插入去重记录。如果插入成功，说明第一次处理。如果唯一键冲突，说明重复请求。这种方式适合通用幂等组件。<ol><li><strong>状态机校验</strong></li></ol>很多业务有明确状态流转。比如订单：<pre><code>待支付 → 已支付 → 已发货 → 已完成</code></pre>扣款或支付回调时，要检查当前状态。比如：<pre><code>只有待支付订单才能执行支付成功逻辑</code></pre>如果订单已经是已支付，再来一次支付成功请求，就直接返回成功，不重复处理。状态机能很好防止重复推进。<ol><li><strong>数据库唯一约束</strong></li></ol>数据库唯一索引是非常重要的兜底手段。比如：<pre><code>transaction_id 唯一<br/>order_no 唯一<br/>user_id + coupon_id 唯一</code></pre>即使应用层判断出问题，数据库也能防止重复写入。常见做法：<pre><code>先插入唯一记录<br/>再执行业务逻辑</code></pre>或者利用唯一键冲突识别重复请求。<ol><li><strong>分布式锁</strong></li></ol>对于并发重复请求，可以对业务 key 加锁。比如：<pre><code>lock:pay:order123</code></pre>同一时间只允许一个请求处理该订单。但分布式锁要注意：<ul><li>锁超时时间。</li><li>死锁问题。</li><li>锁释放安全。</li><li>性能开销。</li><li>不能只依赖锁，最好配合唯一约束。</li></ul><ol><li><strong>如何返回结果</strong></li></ol>重复请求进来时，最好不要直接报错。更好的做法是：<ul><li>如果第一次已经成功，返回相同成功结果。</li><li>如果第一次处理中，返回处理中或稍后重试。</li><li>如果第一次失败，根据业务决定是否允许重新处理。</li></ul>也就是说，幂等不仅是“不重复执行”，还要让调用方得到稳定结果。<ol><li><strong>面试背诵版</strong></li></ol>问题：RPC 如何保证接口幂等性？RPC 保证接口幂等性的核心是让同一个业务请求重复调用多次只生效一次。因为 RPC 可能出现超时、响应丢失或重试，客户端看到失败并不代表服务端没有执行成功，如果没有幂等保护，创建订单、扣库存、扣款、发券等写操作就可能重复执行。常见做法是客户端为每个业务请求生成唯一 requestId，并且重试时复用同一个 requestId，服务端处理前先检查该 requestId 是否已经处理过，如果处理过就直接返回之前结果，如果没处理过再执行业务并记录 requestId。也可以使用业务唯一键，比如订单号、支付流水号、用户 ID 加活动 ID，并通过数据库唯一索引防止重复写入。对于复杂业务，还可以使用去重表、状态机校验和分布式锁，比如订单只能从待支付流转到已支付，已经支付过的请求再次到来就直接返回成功。实际设计中通常会把 requestId、业务唯一键、数据库唯一约束和状态机结合起来，保证重复请求不会造成重复扣款、重复下单或重复发券。面试中可以这样收口：RPC 幂等的核心是用 requestId 或业务唯一键识别同一次请求，服务端做去重、唯一约束和状态校验，保证重试或重复调用时业务只执行一次。<hr/>";
})(window.OTHERS_AI);
