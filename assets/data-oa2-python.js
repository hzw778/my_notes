window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["pythonPython dict 的底层实现原理是什么？"] = "<p><strong>核心回答：</strong>Python dict（CPython）底层是<strong>哈希表（开放寻址法）</strong>，用<strong>稀疏索引表 + 紧凑的 entries 数组</strong>分离存储来节省内存（Python 3.6+ 的紧凑 dict），并预留一定空位以降低冲突。</p><ol><li><strong>哈希定位</strong>：key 先 <code>hash(key)</code> 得到哈希值，再用 <code>idx = hash &amp; mask</code>（mask = 表大小-1，表大小是 2 的幂）定位槽位。</li><li><strong>结构</strong>：<dl><dt>索引表 indices</dt><dd>稀疏的 int8/int16/int32 数组，存 entry 的下标或空/墓碑标记</dd><dt>entries</dt><dd>紧凑数组，每个元素是 (hash, key, value)，插入按顺序追加</dd></dl>这样新插入的 value 天然按插入顺序（dict 保序）。</li><li><strong>冲突解决（开放寻址）</strong>：槽位被占就向后线性探测（<code>i = (i + 1) &amp; mask</code> 或扰动函数散开），直到找到空位。</li><li><strong>扩容</strong>：装填因子 &gt; 2/3 时扩容，新建更大的表并 rehash；删除用「墓碑（dummy）」标记而不是立即移走（避免破坏探测链）。</li><li><strong>key 要求</strong>：必须可哈希（实现 __hash__）且不可变（或哈希值稳定），list/dict 不能做 key。</li><li><strong>对比</strong>：Python 2.7 旧实现是「entries 存哈希+key+value，稀疏槽存索引」，插入顺序不保证；3.6 重写为紧凑布局并保序，3.7 正式定型为语言特性。</li></ol>";

  A["pythonPython list 的扩容机制是什么？"] = "<p><strong>核心回答：</strong>Python list（CPython）底层是<strong>指向对象的指针数组（PyObject*）的动态数组</strong>，扩容采用<strong>「按需增长 + 超量分配（over-allocation）」</strong>策略：满了就分配更大的数组并搬移指针，而非每次加一个。</p><ol><li><strong>存储</strong>：list 存的是元素的引用（指针），不是元素本身；所以 <code>[obj1, obj2]</code> 里是 2 个指针。</li><li><strong>扩容公式</strong>（CPython 源码 listobject.c 的 list_resize）：<pre><code>newsize≈size + (size&gt;&gt;3) + (size&lt;9?3:6)</code></pre>即新容量约为旧容量的 1.125 倍（约 1/8 增量），外加小的固定偏移。旧实现里增长倍数更大（约 1/4）。</li><li><strong>摊还复杂度</strong>：因为超量分配，多次 append 均摊是 O(1)；但单次 append 触发扩容时是 O(n) 搬移。</li><li><strong>关键结论</strong>：Python list 扩容不是 2 倍（不像 C++ std::vector 通常 2 倍），而是更温和的约 1.125 倍，在内存与拷贝次数之间折中。</li><li>频繁在头部插入/删除（<code>insert(0, x)</code>、<code>del l[0]</code>）是 O(n) 全迁移，很慢；适合尾部操作。</li><li><strong>预分配</strong>：知道大小时用 <code>[0]*n</code> 或 list comprehension 一次性建好，避免反复扩容拷贝。</li></ol>";

  A["pythonPython 对象的引用计数是怎么实现的？"] = "<p><strong>核心回答：</strong>CPython 的每个对象头部都有一个 <code>ob_refcnt</code> 引用计数字段，<strong>引用增加时 +1，引用解除时 -1，减到 0 立即回收</strong>。这是 Python 最基础的垃圾回收机制。</p><ol><li><strong>字段位置</strong>：在 <code>PyObject</code> 结构体里（所有对象的公共头）：<pre><code>typedef struct _object {\n  Py_ssize_t ob_refcnt;   // 引用计数\n  struct _typeobject *ob_type;\n} PyObject;</code></pre></li><li><strong>增减时机</strong>：赋值（<code>a = b</code>）、传参、放入容器、作用域创建 → <code>Py_INCREF</code>；离开作用域、del、容器移除、重新赋值 → <code>Py_DECREF</code>。</li><li><strong>减到 0</strong>：触发 <code>tp_dealloc</code> 释放内存；若是容器，逐元素 DECREF，可能级联释放。</li><li><strong>缺点/局限</strong>：维护计数有开销（在多线程下需原子增量，历史上还有 GIL 保护）；<strong>循环引用无法回收</strong>——a.b=b、b.a=a 互相引用，计数永远不为 0，需要靠<strong>分代回收（gc 模块，标记-清除）</strong>兜底。</li><li><strong>调试</strong>：<code>sys.getrefcount(x)</code>（注意会临时 +1）、<code>gc.get_referrers(x)</code> 找引用来源。</li></ol>";

  A["pythonPython 的内存分配器是怎么工作的？"] = "<p><strong>核心回答：</strong>CPython 采用<strong>分层内存分配</strong>：小对象（≤512 字节）走 <strong>pymalloc 内存池</strong>（arena → pool → block），避免频繁 malloc；大对象直接走系统 malloc。</p><ol><li><strong>三层结构（pymalloc）</strong>：<dl><dt>arena（256KB 块）</dt><dd>向系统批量申请的大块内存</dd><dt>pool（4KB）</dt><dd>从 arena 切分，每个 pool 固定一种 block 大小（大小类）</dd><dt>block</dt><dd>实际分配给对象的单元，按 8 字节对齐分档（8、16、24…512）</dd></dl></li><li><strong>大小分档</strong>：对象按大小归到最接近的档位，同档对象共用同尺寸 pool，减少内存碎片。</li><li><strong>对象池复用</strong>：已释放的 block 加入空闲链表 <code>free_list</code> 复用；某些不可变小对象（小整数 -5~256、短字符串、空元组）有专门缓存。</li><li><strong>大对象</strong>：&gt;512 字节直接 <code>malloc</code>，不经过 pymalloc。</li><li><strong>观测与调优</strong>：<code>sys.getsizeof(0)</code>、<code>tracemalloc</code> 追踪分配来源；<code>PYTHONMALLOC=malloc|pymalloc|debug</code> 切换分配器；debug 模式可检测越界/释放后再用。</li><li><strong>GIL 相关</strong>：历史上 pymalloc 依赖 GIL 保证线程安全；3.13 无 GIL 版本对分配器做了 per-thread 优化。</li></ol>";

  A["pythonCPython 的字节码是什么？"] = "<p><strong>核心回答：</strong>字节码（bytecode）是<strong>CPython 把源码编译后生成的低级中间指令序列</strong>，由解释器的主循环逐条执行，是「源码 → 机器码」之间的一层可移植中间表示。</p><ol><li><strong>生成</strong>：<code>.py</code> 被编译成 <code>code object</code>（含字节码），常缓存为 <code>__pycache__/*.pyc</code>。</li><li><strong>查看</strong>：<pre><code>def f(a): return a+1\nimport dis\ndis.dis(f)   # 打印字节码指令</code></pre>常见指令：<code>LOAD_FAST</code>、<code>LOAD_CONST</code>、<code>BINARY_ADD</code>、<code>RETURN_VALUE</code>、<code>STORE_FAST</code>、<code>JUMP_IF_FALSE</code>。</li><li><strong>栈式虚拟机的特点</strong>：操作数在求值栈上传递，指令本身不带寄存器/局部变量寻址（靠 fast locals 索引优化）。</li><li><strong>版本文档</strong>：3.11 引入「专用自适应指令」（specializing adaptive interpreter），常见指令会被特化（如 BINARY_ADD → BINARY_ADD_INT），显著提速。</li><li><strong>意义</strong>：跨平台可移植（同一 .pyc 不同平台可跑，前提版本兼容）、支持动态特性；但比编译型语言慢，因为解释执行 + 动态类型检查。</li></ol>";

  A["pythonPython 代码从源码到执行经历了什么？"] = "<p><strong>核心回答：</strong>Python 执行一条代码大致经历：<strong>源码 → 词法分析（tokens）→ 语法分析（AST/解析树）→ 编译（code object / 字节码）→ PVF 解释执行</strong>，运行时还涉及动态类型、内存分配和垃圾回收。</p><ol><li><strong>词法分析</strong>：tokenize 把字符流切成 token（关键字、标识符、运算符、字面量）。</li><li><strong>语法分析</strong>：parser 生成抽象语法树 AST（可用 <code>ast.parse</code> 查看）。</li><li><strong>编译</strong>：编译器把 AST 转为字节码，生成 code object，可能写盘为 <code>.pyc</code>。</li><li><strong>解释执行</strong>：PVF（Python Virtual Machine）主循环 <code>ceval.c</code> 逐条取指令、派发、执行。</li><li><strong>运行时支撑</strong>：动态类型系统（对象带类型指针）、对象模型（引用计数 GC）、内存分配器、GIL（全局解释器锁，控制同一时刻仅一个线程执行字节码，3.13 起可禁用）。</li><li><strong>一句话链路</strong>：<strong>.py 源文件 → tokenizer → AST → 字节码 code object → PVM 解释执行</strong>，这是典型的「编译到字节码 + 解释执行」混合模型，不是纯解释也不是纯编译。</li></ol>";

  A["pythonCPython 解释器的大致执行流程是什么？"] = "<p><strong>核心回答：</strong>CPython 解释器是一个<strong>「取指令 → 解码 → 执行 →（必要时）函数调用/异常处理 → 循环」的主循环</strong>，核心实现在 <code>Python/ceval.c</code> 的求值循环里，配合帧对象（frame）管理调用栈。</p><ol><li><strong>入口</strong>：<code>python</code> 启动 → 初始化解释器/运行时 → 读源码/字节码 → 得到顶层 code object。</li><li><strong>主循环（ceval）</strong>：<pre><code>while (1) {\n  opcode = GET_NEXT_OPCODE();   // 取指令\n  switch(opcode) {              // 派发\n    case BINARY_ADD: x = POP(); y = POP(); PUSH(x+y); break;\n    case CALL: ... 创建新帧 ...\n    case RETURN_VALUE: ... 退出当前帧 ...\n    ...\n  }\n}</code></pre></li><li><strong>调用帧（frame）</strong>：每次函数调用创建 PyFrameObject，含局部变量、求值栈、指令指针，形成调用栈；返回时销毁。</li><li><strong>异常处理</strong>：执行出错时沿着调用栈找 except 处理器（帧里存异常表）。</li><li><strong>GIL</strong>：字节码执行期间持有 GIL，定时（如每 5ms 或特定指令数）释放让其他线程有机会运行（3.13 可选无 GIL）。</li><li><strong>3.11 特化优化</strong>：主循环里对热指令做运行时特化（quickening），把通用指令替换为类型专用版本加速。</li><li><strong>参考</strong>：这是「解释型」的本质——一条条字节码在 C 实现的 switch 循环里执行，比直接跑机器码慢，但换来了移植性和动态性。</li></ol>";
})(window.OTHERS_AI);