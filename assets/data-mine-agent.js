window.TAB_DATA = window.TAB_DATA || {};
(function () {
  var mine = window.TAB_DATA["mine"] || (window.TAB_DATA["mine"] = { key: "mine", title: "我的整理（本地笔记）", url: "", chapters: [] });
  mine.chapters.push({
    "no": "",
    "title": "agent面经收集参考答案（哔哩AI应用 · 联影医疗 · 影石创新 · 小厂agent）",
    "questions": [

      { "t": "哔哩AI应用 · 1、一个完整的Agent系统通常包含哪些核心模块，相比传统LLM Chatbot最大的区别是什么？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>一个完整的 Agent 系统通常包含五大核心模块：<strong>Agent 核心（大脑）</strong>、<strong>规划 Planning</strong>、<strong>记忆 Memory</strong>、<strong>工具 Tools/Function Calling</strong>、<strong>执行与反馈</strong>。相比传统 LLM Chatbot（只做一次性问答），最大区别是 Agent 能<strong>自主决策、多步执行、调用外部工具并闭环迭代</strong>，而不只是生成一段文本。</p><ol><li><strong>五大模块</strong>：a) 核心大脑：负责理解用户意图、推理决策、生成动作指令；b) 规划：把复杂任务拆解成子步骤（Plan），决定先后顺序与依赖；c) 记忆：短期会话记忆 + 长期知识/经验记忆；d) 工具层：通过 Function Calling / MCP 扩展模型能力（检索、计算、调用 API）；e) 执行与反馈：执行动作并观察结果，判断是否需要调整或终止。</li><li><strong>最大的区别</strong>：传统 Chatbot 是<strong>单轮生成</strong>（Prompt → 文本），一次调用即结束；Agent 是<strong>循环执行</strong>（感知 → 思考 → 行动 → 观察 → 再思考…），直到完成目标。Agent 具备<strong>主动性</strong>（可主动规划、提问澄清、调用工具）和<strong>多步闭环</strong>，而 Chatbot 是被动的一次性响应。</li><li><strong>闭环机制</strong>：Agent 每执行一步都会观察环境反馈（工具返回、错误信息），据此修正下一步，这是它比 Chatbot 更"智能"的根本。</li></ol><blockquote>记忆点：<strong>Agent = LLM（大脑）+ 规划 + 记忆 + 工具 + 执行闭环；Chatbot = 单次生成，Agent = 多步循环。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 2、在Agent项目中，规划、记忆、工具调用、执行模块分别承担什么职责？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>四者分工：<strong>规划负责思考"怎么做"、记忆负责"记住相关上下文"、工具负责"调用外部能力"、执行负责"落实动作并收集结果"</strong>，四者由 Agent 核心协调，形成认知闭环。</p><ol><li><strong>规划 Planning</strong>：把目标拆成有序子任务。实现方式多样：思维链 CoT、ReAct 循环（推理→行动）、树搜索（ToT）、任务拆解/子 Agent。职责是<strong>定策略、定顺序</strong>，决定下一步做什么。</li><li><strong>记忆 Memory</strong>：短期记忆（当前对话上下文、临时状态，存内存/LRU），长期记忆（用户画像、历史经验、领域知识，存向量库/数据库）。职责是让 Agent 考试时"带着资料"而非"凭空回答"。</li><li><strong>工具调用 Tool Use</strong>：把模型的自然语言意图映射为结构化工具调用（Function Calling），负责检索、计算、写库、请求外部服务等"模型本身做不了或做不好"的事，是 Agent 从"嘴"到"手"的关键桥梁。</li><li><strong>执行 Execution</strong>：真正触发工具/接口调用，并拿到执行结果回传给模型。常包含错误处理、重试、超时、权限校验，确保护栏（safety）。</li><li><strong>协作关系</strong>：规划产出步骤 → 模型决定每步调哪个工具 → 执行模块调用并反馈结果 → 结果写回记忆 → 模型再规划下一步，循环直至满足终止条件。</li></ol><blockquote>记忆点：<strong>规划"排兵布阵"、记忆"军师资料"、工具"武器库"、执行"动手与检查"。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 3、意图识别模块通常有哪些实现方式？规则匹配、小模型分类和LLM分类分别适用哪些业务场景？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>意图识别有三种主流实现：<strong>规则匹配、小模型分类、LLM 分类</strong>，按<strong>精确可控性 / 泛化能力 / 成本</strong>矩阵取舍，通常从规则起步、叠加模型兜底。</p><ol><li><strong>规则匹配</strong>：关键词 + 正则 + 模板映射。场景：意图集合<strong>封闭固定</strong>、量级小（几十个）、对误判零容忍的强流程场景，如客服菜单跳转、命令类指令（"查天气""下单"）。优点：快、可解释、零成本；缺点：不抗模糊与变形。</li><li><strong>小模型分类</strong>（BERT/Roberta 分类头）：用标注数据训练一个轻量分类器。场景：意图<strong>较固定但量大、需要泛化到新表达</strong>，且对延迟敏感，如多轮导购、工单自动分派。优点：低延迟低成本、比规则泛化好；缺点：需要标注数据，对<strong>晦涩措辞或未见过的意图</strong>效果有限。</li><li><strong>LLM 分类</strong>：把意图列表和样本填入 Prompt，让大模型输出分类（Zero-shot/Few-shot）。场景：意图<strong>开放、多变、数量大</strong>，或要快速冷启动（没标注数据）。优点：泛化强、免训练、易扩展；缺点：延迟高成本大、可能幻觉，需要约束输出格式。</li><li><strong>工程实践</strong>：工业界常用<strong>级联</strong>——先用规则命中高置信意图直接返回，未命中再交给小模型/LLM，兼顾速度与兜底。</li></ol><blockquote>记忆点：<strong>固定→规则；量大固定→小模型；开放多变/冷启动→LLM。别指望单方案通吃，级联最稳。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 4、如果使用大模型完成意图分类，如何选择Zero-shot、Few-shot方案？当标注数据较少时，如何提升分类稳定性？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>选择原则：<strong>Zero-shot 用于快速冷启动、意图少而清晰；当类别歧义大、或发现结果不稳定时切到 Few-shot（给少量示例），Few-shot 是默认更稳的选择。</strong>标注少时靠<strong>示例质量、输出约束、自检与兜底</strong>来稳定。</p><ol><li><strong>Zero-shot vs Few-shot</strong>：Zero-shot 只给类别名和定义，适合意图彼此差异大、好区分；一旦出现<strong>相近类别混淆</strong>（如"查余额"vs"查明细"）或格式不稳定，就加入 Few-shot 示例。Few-shot 每个类别给 2~5 个代表性例句，能显著拉回分布。</li><li><strong>标注少时提升稳定性的做法</strong>：a) <strong>把类名写成判别式定义</strong>（每个类用一句话说清"什么算、什么不算"）；b) 用 <strong>Few-shot 精选高区分度反例</strong>（比如"这不是客服单，是投诉单"），让模型学会边界；c) <strong>严格约束输出</strong>（只输出类别 ID / JSON，限制可选值），用 function calling 或解码约束避免自由文本；d) <strong>温度调低 + 多次采样/自洽性投票</strong>；e) <strong>置信度阈值 + 兜底</strong>：模型拿不准时返回"低置信"，走人工或规则兜底。</li><li><strong>无标注冷启动路径</strong>：先 Zero-shot 跑一批线上请求 → 人工抽样校对 → 把纠错数据沉淀为 Few-shot 示例或小模型训练集，形成迭代。</li></ol><blockquote>记忆点：<strong>Few-shot 靠"反例+判别式定义"最管用；标注少就上置信度阈值+兜底，别让模型硬答。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 5、文档切片有哪些常见策略，RecursiveCharacterTextSplitter的实现逻辑是什么？针对中文文档处理需要注意哪些问题？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>常见切片策略有<strong>固定长度、标点/段落感知（RecursiveCharacterTextSplitter）、语义切片（Semantic Chunking）、结构化切片、Late Chunking</strong>。RecursiveCharacterTextSplitter 的核心逻辑是<strong>按分隔符优先级递归切分，尽量在语义边界断开</strong>。</p><ol><li><strong>常见策略</strong>：a) 固定长度（fixed_size + overlap）：简单但易切断句义；b) Recursive（递归按分隔符）：默认分隔符优先级 段落 → 换行 → 句号/空格 → 字符；c) Semantic Chunking：用 embedding 算相邻句相似度，低于阈值处切；d) Structured：按 Markdown/HTML 标题层级切，保留结构；e) Late Chunking：先对全文做 token 级 embedding，再在向量空间切分池化。</li><li><strong>RecursiveCharacterTextSplitter 实现逻辑</strong>：给定 chunk_size 和 overlap，先用最高优先级分隔符（段落）尝试切；若某段仍超长，就用下一个优先级分隔符（换行→句号→字符）在该段内部继续递归切，直到每块 ≤ chunk_size；相邻块间按 overlap 保留重叠避免丢边界信息。层次化分隔符是它优于"纯固定长度"的原因。</li><li><strong>中文注意点</strong>：a) <strong>没有空格分词</strong>，按字符切会切断词/句义，必须依赖句号、问号等标点；b) <strong>chunk_size 常用"字数"而非 token</strong>（中文约 1 字≈1 个多 token，若模型按 token 计费要换算）；c) 分隔符要补充中文标点（。！？）；d) 中文语义粘连强，<strong>重叠 overlap 要留够</strong>，防止切分把关键知识点拦腰截断；e) <strong>命名实体/专有名词</strong>易被切开，可结合分词器保护。</li></ol><blockquote>记忆点：<strong>Recursive = 多级分隔符递归切到语义边界；中文要点 = 按标点切、按字数设块、重叠要留足、防切词断义。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 6、如果RAG系统出现召回效果差的问题，你会如何定位？会优先检查embedding、chunk策略、Query重写、混合检索还是Rerank？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>定位召回差采用<strong>从外到里、逐层隔离排查</strong>：先确认<strong>是不是"问题本身变了"（query 语义）、再看检索链路（embedding/chunk/mixed/rerank）</strong>。主流优先级：<strong>先查 chunk 与 query，再查 embedding/混合检索，最后查 rerank</strong>。</p><ol><li><strong>第 0 步：先复现与抽样</strong>，把"召回差"的坏例归因分类——是<strong>没召回（recall 问题）</strong>还是<strong>召回了但排错（precision/排序问题）</strong>，两类原因不同。</li><li><strong>优先查 Query 与 Chunk（最便宜、最常见的根因）</strong>：query 是否太口语/指代不清 → 做查询改写；chunk 是否过大污染、过小断义 → 调 chunk 策略。这是命中率差的最常见来源，先修这里。</li><li><strong>再看 Embedding</strong>：领域专有名词多、中文歧义 → 换更适合领域/中文的 embedding 模型；对比不同模型在评测集上的 recall@k 才有说服力。</li><li><strong>再上混合检索（Hybrid Search）</strong>：纯向量对"关键词精准命中"场景弱，加 BM25 关键词路并做分数融合（RRF 或加权），能救回向量漏掉的精确匹配。</li><li><strong>最后调 Rerank</strong>：只在"粗召回数量够、但排序不准"时才值得；加 cross-encoder reranker 提升 top-k 精准度。</li><li><strong>底层保障</strong>：建立<strong>评测集（query→golden chunk）</strong>，每次改动跑 recall@k / MRR，用数字说话，别靠感觉调。</li></ol><blockquote>记忆点：<strong>先分"没召回 vs 排错"；排查顺序 = Query/Chunk → Embedding → Hybrid → Rerank；靠评测集量化，别凭感觉调参。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 7、Embedding模型如何选择，不同Embedding模型会对检索效果产生哪些影响？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>选择 embedding 模型综合看<strong>语义对齐能力、中文/领域适配、向量维度、推理性能与成本</strong>；模型不同直接决定<strong>向量空间质量</strong>，进而在召回精度和泛化上拉开差距。</p><ol><li><strong>选择维度</strong>：a) <strong>语言/领域匹配度</strong>——中文场景优先中文优化模型（如 BGE、通义 text-embedding、m3e）；领域术语多时要看重领域微调；b) <strong>维度</strong>——一般 768~2048 维，维度越高容量大但存储/检索成本高；c) <strong>MTEB/C-MTEB 榜表现</strong>参考但要对齐自己场景；d) <strong>指令型模型</strong>（有的要求加任务前缀）；e) <strong>性价比与吞吐</strong>——批量吞吐、调用延迟影响线上成本。</li><li><strong>对检索的影响</strong>：<strong>不好的模型</strong>：同义改写检索不到（召回差）、专有名词投影乱、语义相近但无关的判定模糊（排错）。<strong>好的模型</strong>：能理解同义表达、把 query 和片段正确对齐，召回质量与排序稳定性都明显提升。</li><li><strong>工程要点</strong>：a) 训练与推理要<strong>用同一个模型和同一套规范</strong>（否则向量空间不一致全乱）；b) 换模型后<strong>必须全量重建索引</strong>，不能混用；c) 用<strong>评测集度量</strong>（recall@k、命中率）对比再切换。</li></ol><blockquote>记忆点：<strong>选准领域/中文匹配 > 维度 > 榜单；换模型必重建索引，好坏都用评测集说话。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 8、LangChain框架主要有哪些核心组件？相比传统Chain模式，LCEL带来了哪些改进？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>LangChain 核心组件有<strong>LLM/ChatModel、Prompt（模板）、Output Parser、Retriever/VectorStore、Tool/Function、Memory、Chain/Agent、LCEL 表达式</strong>。LCEL 相比传统 Chain 的核心改进是<strong>用统一的管道式表达式（Runnable 协议）取代硬编码的 Chain 对象</strong>，带来组合、并发、流式等一等公民能力。</p><ol><li><strong>核心组件职责</strong>：a) ChatModel/LLM：抽模型调用；b) PromptTemplate：模板化组织输入；c) OutputParser：把模型输出结构化（JSON/函数参数）；d) Retriever/VectorStore：检索层；e) Tool/Function：外部能力；f) Memory：状态保存；g) Chain/Graph：编排。</li><li><strong>传统 Chain 的问题</strong>：Chain 是<strong>硬编码的类/对象</strong>，组合靠手工串，难并发、难流式、难复用、难调试，扩展动词如"输出解析+检索+记忆"要写大量胶水代码。</li><li><strong>LCEL 改进</strong>：a) <strong>统一接口</strong>——所有组件实现 Runnable（invoke/batch/stream + 管道 |），组合靠一条链式表达式；b) <strong>原生流式/异步</strong>——流式输出开箱即用；c) <strong>并发批处理</strong>，自动并行；d) <strong>延迟求值</strong>、可追栈可重试；e) <strong>无缝对接 tracing/fallback</strong>。极大的降低编排成本、提升表达能力。</li></ol><blockquote>记忆点：<strong>LCEL 把"组件串"从硬编码 Chain 变成可组合可流式的 Runnable 管道。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 9、Function Calling和Tool Calling的执行流程是什么，模型是如何判断需要调用工具并生成对应参数的？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>流程分四步：<strong>注入工具 Schema → 模型推理决定是否调用并生成 JSON 参数 → 应用侧执行工具 → 把结果回填给模型继续生成</strong>。判断是否调用是模型在推理时"觉得需要外部信息/动作"时的自回归输出，参数由模型按 Schema 约束生成。</p><ol><li><strong>Step1 注入 Schema</strong>：把每个工具的 name、description、parameters（JSON Schema）拼进 system prompt 或专门的 tools 参数，让模型"知道有哪些工具可用、各自能干嘛"。</li><li><strong>Step2 模型决策</strong>：模型根据用户问题+工具描述，推理是否需要调用：需要则输出一个结构化调用（tool_choice / function_call，含 name 与 arguments JSON）；不需要则直接输出普通回答。</li><li><strong>Step3 执行</strong>：应用解析模型输出的工具名与参数，做<strong>参数校验</strong>后真正调用后端方法/API，拿到结果。</li><li><strong>Step4 回填</strong>：把工具返回结果作为一条消息（role=tool/function）回给模型，模型拿到真实数据后产出最终答案，或继续发起下一步调用（多轮工具循环）。</li><li><strong>落地要点</strong>：a) Schema 描述要<strong>精确</strong>（description 决定选对率）；b) 生成的 arguments 可能带 JSON 包裹导致解析失败，要做<strong>容错解析</strong>；c) 设置<strong>最大工具调用轮数</strong>防止死循环。</li></ol><blockquote>记忆点：<strong>Schema 注入 → 模型出 tool_call（name+args）→ 执行 → 结果回填再生成；关键在 Schema 质量与 JSON 容错。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 10、如果Agent接入大量工具，如何避免工具描述过长导致Prompt膨胀，有哪些优化方式？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>工具多导致的 prompt 膨胀本质是<strong>上下文浪费 + 注意力稀释</strong>，优化思路核心是"<strong>不把全部工具塞进上下文，只露出当前最可能用到的子集</strong>"，主要有**路由分组、embedding 检索工具、分层描述**。</p><ol><li><strong>工具路由/分组</strong>：按领域给工具分组，先让模型选组（或规则路由），再在组内选具体工具，减少一次暴露的数量。</li><li><strong>向量检索式选工具</strong>：把每个工具嵌入到向量库，每次把用户 query 也嵌入，用<strong>相似度召回 top-K 工具的描述</strong>注入 prompt；或让端侧分类模型先粗筛。这是目前大工具集的主流做法。</li><li><strong>描述瘦身</strong>：a) description 精炼到"何时用 + 关键参数"，去掉废话；b) 长参数默认值并入 default，不逐条铺开；c) 相似工具合并成一个带 mode 参数的入口聚合工具。</li><li><strong>元级封装（MCP/API 聚合）</strong>：把一批细粒度工具包成一个"能力接口"，模型只看到少量高阶工具，内部再展开调用。</li><li><strong>动态缓存</strong>：常用工具的 Schema 在服务端缓存，命中直接在 API 层带入，不重发；历史工具调用记录复用。</li></ol><blockquote>记忆点：<strong>核心是"只暴露需要的工具子集"→ 分组 / embedding 检索 / 聚合工具；兼做 description 瘦身。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 11、如何降低大模型输出幻觉，除了Prompt约束之外，还有哪些工程优化方案？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>除了 Prompt 约束，抗幻觉工程化手段有<strong>引入可信检索（RAG）、约束输出（受控生成）、验证与事实核对</strong>，以及<strong>降低对"背诵"的依赖、让上限变下限</strong>。根本原则是"<strong>给模型信息来源，限制它自由发挥</strong>"。</p><ol><li><strong>RAG / 检索增强</strong>：把答案所需的事实塞进上下文并提供来源，模型"引用资料作答"而非凭记忆编造，是最有效的抗幻觉手段。</li><li><strong>约束生成（受控解码）</strong>：用 DSL/JSON Schema/function calling 强制输出格式，阻断自编结构；对数字、日期等关键字段做<strong>白名单或外部校验</strong>。</li><li><strong>事实核对/自验证</strong>：a) 让模型<strong>分开输出"结论"和"依据"</strong>，再对依据做检索核验；b) 对高风险输出用<strong>另一模型/裁判模型</strong>做一致性评分；c) 引用可追溯到具体 chunk。</li><li><strong>工程降参</strong>：降温、限制 max_tokens、关掉不必要的自由度；对数值/代码加<strong>确定性校验器后处理</strong>。</li><li><strong>明确"不知道边界"</strong>：Prompt 加"不确定就明说不知道/拒答"，配合置信度阈值兜底到人。</li></ol><blockquote>记忆点：<strong>抗幻觉=给来源（RAG）+限格式（受控生成）+可核对（依据+校验）+敢说不知道。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 12、Agent执行任务时，如果出现重复调用工具、无法结束或者任务循环的问题，应该如何设计保护机制？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>针对循环/卡死，设计四道护栏：<strong>最大轮数与全局超时、调用去重与幂等、陷进检测（观察不变/重复）、强制中断与降级回退</strong>。</p><ol><li><strong>硬性上限</strong>：a) <strong>最大工具调用次数</strong>（如 8 次），达到即强制结束；b) 每个工具调用设 <strong>超时</strong>（如 30s）；c) 整条 Agent 会话设全局 <strong>deadline</strong>。</li><li><strong>循环检测</strong>：记录每一步的"状态指纹"（工具名 + 参数哈希 + 结果摘要），发现<strong>重复出现同一状态</strong>即判定死循环，中止并汇总已做部分。</li><li><strong>去重与幂等</strong>：a) 相同（工具名+参数）<strong>去重命中直接复用上次结果</strong>；b) 对外部有副作用调用做<strong>幂等键</strong>，避免重复下单/重复扣费等副作用。</li><li><strong>降级回退</strong>：超限后不直接报错，而是<strong>切换策略</strong>（改成更保守的单步、强制让模型总结已得结论、或回退到人工确认）。</li><li><strong>观测</strong>：每一步落日志/traces，方便事后定位是"规划问题、工具问题还是模型问题"。</li></ol><blockquote>记忆点：<strong>护栏=最大轮数+超时+状态循环检测+幂等/去重+降级回退；四件套防死循环。</strong></blockquote><hr>` },

      { "t": "哔哩AI应用 · 13、Agent中的Memory通常如何实现？短期记忆和长期记忆分别解决什么问题？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>Agent 记忆分<strong>短期记忆（工作记忆）和长期记忆（持久记忆）</strong>。短期负责"当前任务/这一步的上下文"，长期负责"历史经验与个性化知识"。实现上短期多用<strong>上下文窗口+LRU</strong>，长期用<strong>向量库/数据库预取</strong>。</p><ol><li><strong>短期记忆</strong>：解决<strong>多轮一致性与任务内状态</strong>。实现：把最近的对话历史、规划步骤、中间变量放进模型上下文；常配合<strong>摘要/压缩</strong>控制长度，或用滑动窗口保留最近 N 轮。</li><li><strong>长期记忆</strong>：解决<strong>跨会话的情境遗忘</strong>——记住用户偏好、历史决定、领域知识、复盘经验。实现：a) 向量化写入向量库，靠相关性<strong>检索注入</strong>；b) 结构化存到数据库（用户画像、偏好表）；c) 记忆<strong>写入策略</strong>（什么该记、什么时候记、过期淘汰）。</li><li><strong>关键设计</strong>：a) 记忆<strong>体量控制</strong>——不能全量塞上下文，靠"需要时检索"；b) <strong>记忆的准确性</strong>——长期记忆写错会带偏后续，要有纠错；c) <strong>隐私与权限</strong>——敏感记忆存哪、谁能读。</li></ol><blockquote>记忆点：<strong>短期=当前上下文（靠窗口+摘要压缩）；长期=历史知识（靠向量库检索注入）；关键在"记什么、怎么记、怎么取"。</strong></blockquote><hr>` },

      { "t": "联影医疗二面 · Q1：为什么要把传统RAG改造成Agentic RAG？传统RAG的痛点是什么？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>传统 RAG 是<strong>一次性的"检索→生成"流水线</strong>，痛点在于<strong>信息不足时不迭代</strong>；Agentic RAG 把检索变成<strong>Agent 驱动的可迭代循环（检索-反思-再检索）</strong>，解决了单次检索"查不准就只能错答"的硬伤。</p><ol><li><strong>传统 RAG 痛点</strong>：a) <strong>检索一次定生死</strong>——第一次召回质量差就直接影响回答，无法补救；b) <strong>query 复杂/指代不清</strong>时不改写，直接拿原始问题检索，命中差；c) <strong>多源/多跳问答</strong>（"对比 A 和 B"）需要多轮检索，单次检索做不了；d) 检索不到就<strong>硬答或答错</strong>，不会主动扩大或换个检索策略；e) <strong>缺少反思</strong>——不知道"这次回答的依据是否足够/可信"。</li><li><strong>Agentic RAG 的改进</strong>：用 Agent（+规划+工具+闭环）编排检索：能<strong>改写 query、多轮/多源检索、判断"够不够"、不足就再检索</strong>，最后带来源回答。本质是把 RAG 从"无反馈流水线"升级为"有反馈闭环"。</li></ol><blockquote>记忆点：<strong>传统RAG=一次性检索死线；Agentic RAG=可反思、可迭代、可补检索的闭环。</strong></blockquote><hr>` },

      { "t": "联影医疗二面 · Q2：Agentic RAG的Agent Loop具体怎么设计的？反思/闭环机制是什么？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>Agent Loop 是"<strong>思考→行动→观察→再思考</strong>"的循环，落到 RAG 上就是<strong>规划检索动作→执行检索→评估结果是否足够→不足则调整继续→足够才给最终答案</strong>。反思机制负责判断"要不要再来一轮、往哪个方向再来"。</p><ol><li><strong>Loop 结构（每轮）</strong>：a) <strong>规划</strong>：决定下一步是"改写 query / 用关键词检索 / 用向量检索 / 跨源检索 / 直接回答"；b) <strong>执行</strong>：调用对应检索/工具；c) <strong>观察</strong>：拿到召回片段及其来源；d) <strong>评估/反思</strong>：判断召回对回答是否充分。</li><li><strong>反思的判定信号</strong>：a) 召回为空→换检索方式或改写 query 重试；b) 得分低/相关度不足→扩大 TopK 或换路；c) 已有信息不够覆盖问题多个子点→继续补检；d) 满足终止条件（充分/超时/达到轮数）→进入生成。</li><li><strong>落地终止条件</strong>：设<strong>最大反思/检索轮数 + 全局超时</strong>，防止死循环；终止后把全部累积片段作为上下文生成，并标注来源。</li><li><strong>工程要点</strong>：把"评估是否充分"可以由<strong>独立判别 prompt / rerank 分数 / 覆盖完备性启发式</strong>实现，控制每一步的 token 与延迟。</li></ol><blockquote>记忆点：<strong>Loop=计划→检索→观察→反思→再检/终止；反思回答"够不够、缺什么"，靠轮数+超时兜底。</strong></blockquote><hr>` },

      { "t": "影石创新Agent · 1、工具或API调用超时、报错时，系统如何处理？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>对工具/API 异常做<strong>分级处理</strong>：先<strong>重试与降级</strong>，再<strong>把可读的错误反馈回模型让它调整</strong>，最后没救时<strong>降级到兜底回答并保证幂等</strong>。</p><ol><li><strong>超时处理</strong>：每个调用设超时，短超时先 <strong>重试 1~2 次（带退避）</strong>；仍超时判定该工具当前不可用，从可用工具集中排除并告知模型。</li><li><strong>报错处理</strong>：a) 捕获异常并<strong>规范化错误信息</strong>（去掉堆栈、转成模型能懂的描述，如"DB 连接失败"）；b) 把错误作为<strong>tool 返回结果回填给模型</strong>，模型可据此<strong>换参数重试、换工具、或告诉用户失败原因</strong>；c) 对<strong>可重试类</strong>错误（限流、瞬时抖动）自动重试，对<strong>不可重试类</strong>（参数错误、无权限）直接反馈不给模型瞎试。</li><li><strong>幂等与副作用保护</strong>：对外部有写操作的工具用<strong>幂等键/请求ID</strong>去重，重试不产生重复副作用（如重复下单）。</li><li><strong>降级兜底</strong>：最终失败时给用户<strong>明确的"任务未完成+原因+已做部分"</strong>，或进入人工/异步重试队列，不让 Agent 假装成功。</li></ol><blockquote>记忆点：<strong>异常=超时重试+规范化错误回填模型自调整+幂等防重+失败明确告知不硬答。</strong></blockquote><hr>` },

      { "t": "影石创新Agent · 2、是否考虑过让大模型参与异常处理或自动恢复？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>可以，但要<strong>分层</strong>——<strong>确定性错误走代码策略，LLM 只处理语义层面的决策</strong>（判断该不该重试、换什么方案、如何措辞），不让模型处理高危/不可逆操作。自动恢复的设计核心是"<strong>模型做策略决策，工程保证安全</strong>"。</p><ol><li><strong>LLM 可参与的层面</strong>：a) 分析错误原因并<strong>判断重试方向</strong>（改参数、换工具、切数据源）；b) 生成<strong>面向用户的友好失败说明</strong>；c) 规划<strong>替补方案/降级路径</strong>。</li><li><strong>必须工程化的层面</strong>：a) 重试次数、退避、熔断由代码控制；b) <strong>危险操作（删除、支付、写库）恢复需人工确认</strong>；c) LLM 的"恢复建议"要先过<strong>白名单校验</strong>再执行。</li><li><strong>自愈示例</strong>：工具 A 返回 5xx → 错误信息回填 → LLM 判断"切到备用源 B"→ 工程侧校验 B 可用且操作安全 → 执行。形成"检测-决策-执行-验证"的自愈闭环。</li><li><strong>风险控制</strong>：给 LLM 的恢复动作设<strong>操作权限与水印</strong>，防止模型在恢复时越权；所有自动恢复都要<strong>可审计</strong>。</li></ol><blockquote>记忆点：<strong>LLM管"该不该恢复、怎么恢复"的决策，安全/幂等/权限由工程兜底；危险动作一律人工确认。</strong></blockquote><hr>` },

      { "t": "影石创新Agent · 3、Token消耗过快时，如何排查和优化？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>先<strong>度量再优化</strong>——给每个 Agent 会话加 token 计量与日志归类，找出"哪步最烧钱"（系统提示/上下文膨胀/多轮工具反复），再针对性打补丁。</p><ol><li><strong>第一步：埋点度量</strong>：按<strong>输入/输出、按调用类型（主模型、工具、检索）、按会话</strong>统计 token，用曲线定位热点。</li><li><strong>常见烧钱点与对策</strong>：a) <strong>上下文累积膨胀</strong>→ 引入压缩/摘要/滑动窗口，舍弃冗长历史；b) <strong>系统提示过大</strong>→ 精简，把长静态内容挪到检索/缓存；c) <strong>工具 Schema 全量注入</strong>→ 只注入需要的那组 + 服务端缓存；d) <strong>工具返回结果过大</strong>→ 对返回做截断/摘要/只保留关键字段；e) <strong>无效多轮循环</strong>→ 去掉重复调用、强化终止条件；f) <strong>检索重复注入同一内容</strong>→ 去重先查再注入。</li><li><strong>架构级</strong>：a) 尽量用 <strong>prompt cache 缓存前缀</strong>（稳定 system+few-shot 部分命中缓存打低折扣）；b) 用<strong>小模型做简单任务</strong>（意图、格式）取代大模型；c) 开流式输出，必要时降 max_tokens。</li></ol><blockquote>记忆点：<strong>先埋点看哪步烧钱；优化=压缩上下文+精简System/Prompt+工具子集注入+去重+缓存前缀+小模型分流。</strong></blockquote><hr>` },

      { "t": "影石创新Agent · 4、是否写过Skill？通常如何设计和评估一个Skill？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>Skill 是<strong>可复用的能力封装</strong>（提示词+逻辑=一个"技能"）。我遵循的设计思路是<strong>先定"输入输出契约"、再写"触发条件+执行步骤+兜底"、最后用一组用例评估</strong>；评估关注<strong>成功率、质量、稳定性与成本</strong>。</p><ol><li><strong>设计要点</strong>：a) <strong>契约先行</strong>：明确 Skill 的输入（触发它的信号/参数）与输出（结构化结果）；b) <strong>单一职责</strong>：一个 Skill 只做一件事，方便复用与路由；c) <strong>触发条件清晰</strong>：写清"什么场景该用它"（是模型主动调还是规则触发）；d) <strong>执行步骤可解释</strong>：内部是固定流程+可选工具；e) 内置<strong>失败兜底</strong>。</li><li><strong>评估维度</strong>：a) <strong>成功率/通过率</strong>：跑一套 golden 用例，看正确产出比例；b) <strong>质量</strong>：输出是否符合契约、是否可被下游直接消费；c) <strong>稳定性</strong>：同输入多次跑是否一致（确定性）；d) <strong>成本/延迟</strong>：用了多少 token、多慢；e) <strong>边界用例</strong>：空输入、异常输入、越界输入的表现。</li><li><strong>实践</strong>：维护 Skill 的 <strong>golden 测试集</strong>，每次改动回归；带版本与文档，可被其他 Agent/流程引用。</li></ol><blockquote>记忆点：<strong>先契约后实现、单一职责、触发清晰、带兜底；评估看成功率+质量+稳定+成本，靠golden用例回归。</strong></blockquote><hr>` },

      { "t": "影石创新Agent · 5、上下文为什么需要压缩？压缩后丢失细节如何处理？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>需要压缩是因为<strong>上下文窗口有限、且长度增长会抬高成本、拉低精度</strong>（信息稀释、注意力退化）。压缩后不会"真丢"，而是走<strong>分级保留 + 需要时还原</strong>：重要细节留全文，次要细节摘要化，需要时通过检索/展开找回。</p><ol><li><strong>为什么要压缩</strong>：a) 窗口硬上限；b) <strong>成本</strong>——每轮都重发全部历史，越长越贵；c) <strong>质量</strong>——超长后模型在关键处注意力涣散、敷衍/遗忘；d) 延迟。</li><li><strong>压缩策略</strong>：a) 对历史做<strong>摘要压缩</strong>（旧轮压成摘要保留在上下文，最近 N 轮保留全文）；b) 按<strong>相关性/重要性</strong>评估哪些要保全文、哪些只留要点；c) 用<strong>结构化摘要</strong>记录"事实、决策、待办"。</li><li><strong>防丢细节的机制</strong>：a) 摘要时<strong>显式标注可能隐藏的关键实体/数字/结论</strong>，宁可多留别漏；b) 保留<strong>完整的依赖原文</strong>（如中间数、字段值）并存到长期记忆/外部存储，用户或后续问到时<strong>按需检索/展开</strong>；c) 把"原始长度版本"打到<strong>可追溯快照</strong>，必要时精读。</li><li><strong>本质</strong>：<strong>内存分层</strong>——热数据（当前）全程保留，温数据（最近相关）摘要+可还原，冷数据（长期）进存储按需取。</li></ol><blockquote>记忆点：<strong>压缩=成本/质量/窗口的平衡；防丢=摘要保要点+关键实体复用+原文存外部按需检索还原。</strong></blockquote><hr>` },

      { "t": "影石创新Agent · 6、如何理解Spec Coding和Harness？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>Spec Coding（规格驱动编程）是指<strong>先写清晰的行为规格（Spec，含输入输出/约束/测试），再让模型按规格写代码</strong>，代码是否达标用规格里的测试来判定。Harness 是<strong>承载模型的"运行框架/沙箱环境"</strong>，负责执行、安全隔离、测试运行与结果回收。两者合起来是"规格约束 + 受控执行"的工程化开发范式。</p><ol><li><strong>Spec Coding 要点</strong>：a) 规格先行——定义<strong>函数签名、边界条件、约束、验收用例</strong>；b) 模型负责<strong>把规格翻译成实现</strong>；c) 评价靠<strong>跑测试通过率</strong>而非"看起来像不像"，把主观变客观，是可验证、可回归的。</li><li><strong>Harness 要点</strong>：a) 提供<strong>沙箱/隔离执行环境</strong>（跑代码、跑工具调用，不污染线上）；b) 负责<strong>输入注入、结果回收、超时与资源限制</strong>；c) 内置<strong>测试 runner</strong> 和 <strong>trace/日志</strong>；d) 是 Agent 与真实仓库/API 之间安全的执行面。</li><li><strong>二者关系</strong>：Spec 定义"做什么、怎么算对"，Harness 定义"在哪儿做、怎么安全执行"。没有 Spec 则以凭感觉改代码，没有 Harness 则无法安全地让 Agent 动手。</li></ol><blockquote>记忆点：<strong>Spec=先定义"对错"再写码（测试判定）；Harness=安全受控的"执行/测试/隔离"环境。</strong></blockquote><hr>` },

      { "t": "小厂agent · 2、多Agent方式任务拆分不稳定，你有考虑过相关问题吗？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>任务拆分不稳定是多 Agent 的常见痛点——<strong>拆分粒度漂移、子任务边界不清、结果拼接缝大</strong>。我的对策是给拆分加"约束与检查"：预置任务模板/结构约束 + 对每个子任务的产出做校验 + 汇总层纠偏，而不是让 AI 每次都自由发挥。</p><ol><li><strong>不稳定表现</strong>：a) 同一任务不同次拆分粒度不一；b) 子任务互相重叠或遗漏；c) 各子 Agent 上下文不共享导致信息断层；d) 结果拼接时风格/格式不一致。</li><li><strong>稳定化手段</strong>：a) <strong>结构化拆分</strong>——用固定模板（标题/章节/Schema）约束拆分结果，限制输出格式；b) <strong>Few-shot 拆分示例</strong>给模型稳定的拆分范式；c) <strong>拆分+校验闭环</strong>——拆完用规则/LLM 检查是否覆盖完整、有无重叠，不满足则让模型重拆；d) <strong>关键子任务不拆分</strong>——能单 Agent 干的别为拆分而拆分。</li><li><strong>系统级兜底</strong>：a) 子 Agent 间用<strong>统一信息传递契约</strong>（结构化接口）；b) 汇总 Agent 做<strong>一致性校验与消除矛盾</strong>；c) 对高确定性流程用<strong>固定流水线</strong>而非自由编排。</li></ol><blockquote>记忆点：<strong>拆分稳定=模板约束拆分格式+完成性/重叠校验+子任务结构化接口+没必要不分。</strong></blockquote><hr>` },

      { "t": "小厂agent · 3、Agent问答是否需要先做意图识别、再分发对应Agent？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>通常需要，但要分层——<strong>先做意图/router 识别再分发到对应 Agent/技能</strong>，能显著提准确率、降成本、减串扰。前提是意图边界要清晰，否则路由本身会错。</p><ol><li><strong>为什么需要</strong>：a) 不同 Agent 的 prompt/工具集不同，先路由避免"问计价的跑到文案 Agent"；b) 避免把无关 Agent 的上下文灌给模型（省 token、提精度）；c) 便于分流计费与监控。</li><li><strong>路由实现</strong>：a) 规则优先（关键词/域名/固定词）兜底快路径；b) 意图分类模型（BERT 分类 / LLM+few-shot）粗分类；c) 结合置信度阈值，低的走咨询/兜底。</li><li><strong>风险与对策</strong>：a) <strong>路由误判</strong>后进了错的 Agent，比不路由更糟 → 给每个 Agent 也配<strong>自我检查"这问题不是我该答的"并转交</strong>；b) 路由本身耗 token → 先规则后模型两级；c) 保留"无匹配→通用入口"兜底。</li></ol><blockquote>记忆点：<strong>先意图识别/routing 再分发，提升准确率+降本；但要有低置信兜底和 Agent 间转交，别让路由成为新瓶颈。</strong></blockquote><hr>` },

      { "t": "小厂agent · 4、你项目写了MCP，没有用MCP之前你的原始方案是什么？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>MCP 之前，工具接入是<strong>手写胶水层</strong>：每个工具写一套 <code>工具名 + description + 参数Schema + 调用函数 + 结果解析</code>，再手动注册进 Function Calling 列表。MCP 的价值是把这套<strong>统一成标准协议</strong>（发现工具、调用、鉴权、类型），不用每个服务各写一套。</p><ol><li><strong>原始方案（无 MCP）</strong>：在代码里为每个工具硬编码 Tool 定义（JSON Schema）、封装调用、错误处理；工具多了要写大量样板代码，且<strong>跨 Agent/跨项目不通用</strong>，换框架/换场景得重写一遍。</li><li><strong>MCP 带来的改进</strong>：a) <strong>标准化的工具发现与调用协议</strong>；b) 工具以<strong>可配置的 server</strong> 形式接入，一个 MCP server 暴露一组工具；c) 支持<strong>鉴权、类型、元数据</strong>的统一描述；d) 图形/文档工具也可以跑在我封装的 server 里，让模型无法直接碰磁盘/权限，<strong>更安全可控</strong>。</li></ol><blockquote>记忆点：<strong>MCP前=每个工具手写Schema+胶水+不通用；MCP后=协议标准化、server化、可复用、更安全。</strong></blockquote><hr>` },

      { "t": "小厂agent · 4追问1：工具完整Schema是在什么时候注入的？会不会出现模型需要参数，但Schema还没加载的情况？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>Schema 是<strong>在发起模型推理请求之前就注入</strong>的（作为 system/tools 一部分，每轮调用都带上当前可用工具集）。因为模型是在"看到 Schema 之后"的同一轮推理里决定调哪个工具的，所以<strong>不会出现"要参数时 Schema 没加载"</strong>——除非是动态工具或加载有 bug。</p><ol><li><strong>注入时机</strong>：组装请求时，把<strong>当前可用工具的完整 Schema</strong>（name/description/parameters JSON Schema）放进 tools 参数一并发送，<strong>早于任何 tool_call 生成</strong>。</li><li><strong>为何不会缺</strong>：模型这轮能想到"用它"必然是它已经见过该工具的 Schema。真正的风险点在<strong>动态工具</strong>：工具列表是运行时才发现/命中的，可能在请求组装时还没拿到（比如通过 MCP 异步发现失败）。对策：a) 请求前先<strong>确保工具列表已同步/已发现</strong>；b) 若工具依赖用户输入才能构造 Schema，用<strong>占位/延迟绑定</strong>，在用户给出参数后才注入并触发下一轮；c) 对发现失败做<strong>兜底</strong>（提示模型该操作暂不可用）。</li><li><strong>最佳实践</strong>：用<strong>函数调用式的显式注入</strong>，工具定义集中管理、预加载、可校验，避免散落导致漏加载。</li></ol><blockquote>记忆点：<strong>Schema先于推理注入，通常不会"要参数时没Schema"；真正的坑在动态工具，要"先发现再注入+失败兜底"。</strong></blockquote><hr>` },

      { "t": "小厂agent · 4追问2：Token优化比例，你是怎么测的？大概多少对话、多少工具测出的数据？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>Token 优化比例不是拍脑袋，而是<strong>基于可复现的基准会话集测出来的</strong>。测法：准备一组有代表性的真实/构造会话 → 分别在优化前后跑一遍 → 统计总 token、输入/输出拆分、每步占比 → 算出节省比例。</p><ol><li><strong>测法步骤</strong>：a) <strong>定基准集</strong>：取 N 次真实请求（或按业务构造的代表性场景），保证覆盖典型路径（单轮/多轮/带工具/带检索）；b) <strong>打点</strong>：在每个模型调用记录 prompt 长度、completion、工具返回长度；c) <strong>对照组</strong>：同输入跑优化前/后，控制其他变量；d) 汇总对比算出<strong>token 减少比例与成本节省</strong>。</li><li><strong>规模量级参考</strong>：这类评测常<strong>几十到几百条会话</strong>就足够反映趋势（如 50~200），一次 3~5 轮、单轮含 1~3 个工具；重点是<strong>代表性比绝对数量更重要</strong>。工具数量对 Token 影响，通常在<strong>几十个工具全量 vs 检索后子集</strong>对比时能看到显著差异。</li><li><strong>注意事项</strong>：a) 测的是<strong>相对变化</strong>，要控制语境/模型/参数一致；b) 兼顾<strong>质量不滑坡</strong>——光省 token 但效果变差是负优化，要一并测答案质量。</li></ol><blockquote>记忆点：<strong>用代表性基准会话集+前后对照打点统计；几十百条即可见趋势，重点看相对比例，且要"省token不伤质量"。</strong></blockquote><hr>` },

      { "t": "小厂agent · 5、工具越多，上下文污染越严重，Agent选工具准确率会下降，你怎么调整优化？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>根本思路是<strong>让模型看到的工具越少越准</strong>：通过<strong>先路由/检索再暴露工具子集</strong>，把"80 个工具里选"压成"5 个候选里选"，准确率和成本都改善。</p><ol><li><strong>两层选工具</strong>：a) <strong>粗筛</strong>——用 embedding 把用户 query 和工具描述匹配，召回首部 K 个候选；或用小分类模型先选组；b) <strong>精选</strong>——把候选 K 个工具的 Schema 注入，让大模型做最终选择，上下文小、噪声低。</li><li><strong>被选中方案的稳定化</strong>：a) 工具<strong>描述精炼且有判别性</strong>（写明"何时用/何时不用"）；b) 相似工具<strong>合并聚合成一个高阶工具</strong>，内部展开（减少候选数量）；c) 把"工具打分/历史使用频率"作为先验排序，优先高位候选。</li><li><strong>动态上下文</strong>：不用的工具绝不出现在 prompt；已被淘汰的工具<strong>从可用集剔除</strong>并告知，避免模型反复尝试。</li></ol><blockquote>记忆点：<strong>工具越少越准——先检索工具→子集注入→大模型精选，加描述判别性+聚合减少候选。</strong></blockquote><hr>` },

      { "t": "小厂agent · 6、你项目提到了沙箱命令，是拦截原始字符串，还是解析AST、Token做拦截？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>做安全工作不能只靠拦截原始字符串（易被混淆绕过），更稳的是<strong>先语法解析成 AST/Token，再在结构层做静态分析与拦截</strong>，同时叠加真正的"沙箱隔离"作为底线。</p><ol><li><strong>纯字符串拦截的问题</strong>：黑名单关键词（如 rm、exec）很容易被<strong>混淆/拼接/编码/换参数</strong>绕过，且误杀正常命令，不可作为安全依据。</li><li><strong>AST/Token 拦截（更好）</strong>：把命令/代码<strong>解析成语法树</strong>，做静态分析：a) 白名单化——只允许特定命令集合，其他一律拒绝；b) 检查<strong>危险 API/系统调用</strong>（读文件、网络、删除、执行 shell）；c) 对<strong>参数做白名单/类型校验</strong>；d) 检测注入模式（管道、重定向、动态执行）。在结构层判断准确、不易被文本混淆骗过。</li><li><strong>真正的底线</strong>：无论静态分析多好，也要<strong>放进受限沙箱</strong>（容器/虚拟机/无网络/非 root/资源限额），即使恶意代码注入也被隔离。理想是<strong>静态分析拦截 + 沙箱隔离双保险</strong>。</li></ol><blockquote>记忆点：<strong>别用字符串黑名单（易扰）；做AST/Token白名单静态分析，再叠容器沙箱当底线，双保险。</strong></blockquote><hr>` },

      { "t": "小厂agent · 7、长上下文压缩这块你熟吗？讲一下你的方案；压缩必然丢失信息，怎么识别关键信息、保证语义连贯不丢核心内容？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>我的方案是<strong>分层压缩 + 关键信息显式提取 + 按需还原</strong>：先用"重要性/相关性"把上下文分级，热数据保留全文、温数据压缩成摘要、冷数据外置，并在压缩时<strong>显式抽取并保留关键实体/数字/结论</strong>，漏掉的靠"原文快照 + 检索还原"找回。</p><ol><li><strong>分层压缩方案</strong>：a) 滑动窗口保最近轮次全文；b) 旧内容交给 LLM 做<strong>摘要</strong>并入上下文；c) 摘要也长了再压缩成<strong>层级摘要</strong>（可递归）；d) 原始版本落到外部存储做快照。</li><li><strong>识别关键信息</strong>：用专门的抽取 prompt/规则优先抓：a) <strong>用户明确诉求与约束</strong>（改了什么、别动哪块）；b) <strong>已确认的事实/数字/字段值</strong>（中间结果、金额、ID）；c) <strong>决策与待办</strong>；d) <strong>提过的问题与结论</strong>。这些是高价值密度信息，压缩时<strong>原样保留而非只留"意思"</strong>。</li><li><strong>保证语义连贯不丢</strong>：a) 摘要时保留<strong>逻辑链/前后因果</strong>（不能只留孤立的点）；b) 用<strong>结构化摘要</strong>（事实清单+结论+待办+原文位置锚点）；c) 发现缺失就<strong>按锚点定位到原文快照还原</strong>；d) 建立"压缩质量自检"——压缩后再问一遍关键事实是否齐全。</li><li><strong>本质</strong>：压缩不是"丢信息"，是把信息<strong>降为可检索的层级</strong>，热数据在上下文、冷数据在存储，能力守恒。</li></ol><blockquote>记忆点：<strong>压缩=分层（全文/摘要/外置）；防丢=显式抽关键实体+数字+结论+因果链；丢了的靠原文快照+锚点还原。</strong></blockquote><hr>` },

      { "t": "小厂agent · 8、你有没有搭建评测集，检测Agent回答准确率、合规度、效果？", "tag": "我的整理", "p": "core", "html": `<p><strong>核心回答：</strong>有。评测集面向<strong>功能正确性、格式合规、约束遵守、成本效率</strong>四类，用"静态 golden 用例 + 动态回归 + 指标打分"来做，追求可量化可回归，而不是看一两眼效果就上线。</p><ol><li><strong>评测集结构</strong>：a) <strong>Golden 用例</strong>：输入（问题+上下文）+期望输出/期望行为，覆盖主路径、边界、异常；b) <strong>指标</strong>：准确率/通过率、格式合法率、是否遵守硬约束（如"不要臆造来源"）、越权/违规率、Token 成本与延迟。</li><li><strong>准确率评测</strong>：a) 有标准答案的用<strong>结构化比对/LLM-as-Judge</strong>打分；b) 开放问答用<strong>裁判模型+人工抽查双轨</strong>，避免单一判分偏差。</li><li><strong>合规度评测</strong>：专门造<strong>提示注入、越权、敏感信息、错误来源</strong>用例，验证 Agent 不泄漏、不越权、按约束拒绝。</li><li><strong>落地</strong>：评测集<strong>版本化存库</strong>，每次改 prompt/流程/模型都在固定集上回归，出现回归即阻断上线；线上还要<strong>抽样回流</strong>把新错误补进评测集持续迭代。</li></ol><blockquote>记忆点：<strong>评测集=golden用例+四维指标（正确/合规/约束/成本）；用LLM裁判+人工抽查，改了必回归，防回归才上线。</strong></blockquote><hr>` }
    ]
  });
})();