window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["pgPostgreSQL 有哪些锁？"] = "<p><strong>核心回答：</strong>PG 锁分<strong>表级锁（8 种）和行级锁（4 种）</strong>，外加轻量级的咨询锁（advisory lock）。</p><ol><li><strong>表级锁</strong>（强度从弱到强）：ACCESS SHARE（查询自动加）、ROW SHARE（SELECT FOR UPDATE）、ROW EXCLUSIVE（INSERT/UPDATE/DELETE 自动加）、SHARE UPDATE EXCLUSIVE（VACUUM/建索引）、SHARE（CREATE INDEX 不带 CONCURRENTLY）、SHARE ROW EXCLUSIVE、EXCLUSIVE、ACCESS EXCLUSIVE（ALTER/DROP/TRUNCATE）。</li><li><strong>行级锁</strong>：FOR UPDATE（排他）、FOR NO KEY UPDATE、FOR SHARE、FOR KEY SHARE；行锁不阻塞读（MVCC），只阻塞互相冲突的写。</li><li><strong>死锁检测</strong>：行锁先存在内存标志（不占磁盘），冲突时才建锁记录；deadlock_timeout（默认 1s）后触发检测。</li><li><strong>咨询锁</strong>：应用层自定义的锁（pg_advisory_lock），常用于分布式任务抢占、防止重复调度。</li><li><strong>排查</strong>：pg_locks + pg_stat_activity 联查看谁阻塞谁（blocking_pid）。</li></ol>";

  A["pgPostgreSQL 行锁和表锁有什么区别？"] = "<p><strong>核心回答：</strong><strong>粒度、加锁方式、冲突范围</strong>三个维度不同：行锁精确到元组只影响并发写同行的会话，表锁粗粒度影响整表操作。</p><ol><li><strong>粒度与目的</strong>：行锁保护单行数据一致性（UPDATE/DELETE 自动加，或 SELECT FOR UPDATE 显式加）；表锁保护表结构/全表操作（DDL、全表维护操作）。</li><li><strong>加锁方式</strong>：行锁在修改元组时通过元组头部的 xmax 字段实现（PG 15 后版本还有磁盘上的 multixact 优化），不加锁时几乎零成本；表锁在 SQL 执行计划阶段显式获取，冲突矩阵固定。</li><li><strong>对并发的影响</strong>：行锁之间：不同行互不干扰，同行写互斥；表锁可能大面积阻塞（如 ALTER TABLE 的 ACCESS EXCLUSIVE 阻塞一切，连 SELECT 都不行）。</li><li><strong>读不受阻塞</strong>：得益于 MVCC，普通 SELECT 不加任何锁，读写不冲突——这是 PG 并发能力的基础。</li><li><strong>实务建议</strong>：长事务持有行锁会阻塞后续同行更新，控制事务长度；DDL 用 lock_timeout 避免排队雪崩。</li></ol>";

  A["pgPostgreSQL SELECT FOR UPDATE 原理是什么？"] = "<p><strong>核心回答：</strong>SELECT FOR UPDATE 在<strong>读取行的同时对行加排他锁</strong>：把选中元组的 xmax 设为当前事务 ID，其他事务修改或 FOR UPDATE 同一行时会阻塞等待。</p><ol><li><strong>实现</strong>：被锁行的元组头 xmax = 当前 xid（锁信息记在磁盘前的行头，PG 14 后行锁存储到单独的行锁表减少写放大）；事务提交后锁释放，xmax 变成删除/更新标记。</li><li><strong>用法</strong>：<pre><code>BEGIN;\nSELECT * FROM account WHERE id=1 FOR UPDATE; -- 锁住\n-- 检查余额、扣款、写回\nUPDATE account SET balance=balance-100 WHERE id=1;\nCOMMIT;</code></pre>并发事务执行同样的 FOR UPDATE 会排队，保证扣款不超卖。</li><li><strong>变体</strong>：FOR SHARE（共享锁）、FOR NO KEY UPDATE（不锁外键引用列，冲突面更小）、FOR UPDATE NOWAIT（不等待直接报错）、SKIP LOCKED（跳过被锁行——任务队列神器）。</li><li><strong>和 MVCC 的关系</strong>：FOR UPDATE 读的是最新提交版本（不能读旧快照，否则锁了个旧版本没意义）。</li><li><strong>典型场景</strong>：秒杀扣库存、任务表抢占（FOR UPDATE SKIP LOCKED）。</li></ol>";

  A["pgPostgreSQL 如何解决并发更新问题？"] = "<p><strong>核心回答：</strong>PG 靠<strong>MVCC 避免读写阻塞 + 行级锁串行化同行的并发写 + 失败重试</strong>解决并发更新。</p><ol><li><strong>基础：MVCC</strong>：UPDATE 不是原地改，而是新版本行（新元组 xmin=当前事务，旧版本 xmax=当前事务）；读不阻塞写、写不阻塞读。</li><li><strong>同行并发写</strong>：第二个 UPDATE 事务会等第一个的行锁；第一个提交后，第二个重新检查行版本（EvalPlanQual 机制），若可见则基于新版本重新计算更新——避免丢失更新。</li><li><strong>隔离级别升级</strong>：REPEATABLE READ/SERIALIZABLE 下，并发修改同一行会直接报序列化失败（40001），应用捕获后重试。</li><li><strong>应用层方案</strong>：a) SELECT FOR UPDATE 悲观锁先锁后改；b) 乐观锁（版本号列，UPDATE ... WHERE version=n，影响行数为 0 说明冲突，重试）；c) 原子 UPDATE（UPDATE SET balance=balance-100 WHERE balance&gt;=100）一条 SQL 完成，利用行锁天然原子。</li><li><strong>选型</strong>：简单计数用原子 UPDATE；复杂业务判断用 FOR UPDATE 或乐观锁重试。</li></ol>";

  A["pgPostgreSQL 死锁如何产生？如何排查？"] = "<p><strong>核心回答：</strong>死锁源于<strong>两个事务以相反顺序锁定资源且互不释放</strong>；PG 默认 1 秒后自动检测并终止其中一个；排查靠 pg_stat_activity 和日志。</p><ol><li><strong>产生</strong>：事务 A 锁行 1 想锁行 2，事务 B 锁行 2 想锁行 1——互相等待成环。</li><li><strong>自动检测</strong>：等锁超过 deadlock_timeout（默认 1s）触发死锁检测器，找到等待环后 abort 其中一个事务（报 deadlock detected，SQLSTATE 40P01），另一个继续。</li><li><strong>排查</strong>：a) 日志搜 deadlock detected，看两个事务的 SQL；b) 运行期查阻塞：<pre><code>SELECT pid, state, wait_event_type, query,\n       pg_blocking_pids(pid) AS blocked_by\nFROM pg_stat_activity\nWHERE wait_event_type='Lock';</code></pre>c) pg_locks 联表看锁模式与对象。</li><li><strong>预防</strong>：a) 所有事务按<strong>相同顺序</strong>访问表和行（如按主键排序后再更新）；b) 缩短事务，锁持有时间短；c) 一个事务内避免“先查后改再查他表”的交叉；d) 用 NOWAIT/lock_timeout 快速失败代替无限等待。</li></ol>";

  A["pgPostgreSQL 如何实现主从复制？"] = "<p><strong>核心回答：</strong>PG 原生主从复制基于<strong>WAL 流复制（Streaming Replication）</strong>：主库持续发送 WAL 日志流，从库回放达成一致。</p><ol><li><strong>核心机制</strong>：主库写操作先记 WAL（预写日志）；wal_sender 进程把 WAL 段流式推给从库；从库 wal_receiver 接收并写本地 WAL，startup 进程回放，回放位置上报主库。</li><li><strong>配置步骤</strong>：主库 postgresql.conf：wal_level=replica、max_wal_senders；pg_hba.conf 放行 replication 连接；从库用 pg_basebackup 做基础备份，配 primary_conninfo 后启动，自动进入流复制。</li><li><strong>复制模式</strong>：物理复制（整库块级一致，从库只读）；逻辑复制（按表订阅，可写，用于选择性同步）。</li><li><strong>同步级别</strong>：synchronous_commit = remote_write / on（等从库）/ off——性能与 RPO 的权衡。</li><li><strong>监控</strong>：pg_stat_replication 的 sent_lsn/write_lsn/flush_lsn/replay_lsn 对比主库 current_wal_lsn 算延迟。</li></ol>";

  A["pgPostgreSQL Streaming Replication 是什么？"] = "<p><strong>核心回答：</strong>Streaming Replication 是 PG 的<strong>物理流复制</strong>：主库把 WAL 日志以字节流实时推送给备库，备库持续回放，实现近实时热备。</p><ol><li><strong>工作原理</strong>：主库每个写事务产生 WAL；walsender 与备库 wal_receiver 建立 TCP 长连接，WAL 记录产生即推送（不等整个 WAL 段写满），备库写盘后由 startup 进程按序回放。</li><li><strong>特点</strong>：物理级（备库与主库完全一致的块拷贝）、异步或同步可配、备库只读（可作读副本分流查询）。</li><li><strong>级联复制</strong>：备库可以再向下游备库发 WAL（级联从库），减轻主库发送压力。</li><li><strong>对比逻辑复制</strong>：流复制整库、DDL 自动同步、备库只读；逻辑复制按 publication/subscription 选表、双端可写、DDL 不自动同步。</li><li><strong>与基于文件的日志传送对比</strong>：旧方式要等 WAL 段写满（16MB）才归档复制，延迟大；流式推送延迟毫秒级。</li></ol>";

  A["pgPostgreSQL 同步复制和异步复制有什么区别？"] = "<p><strong>核心回答：</strong>区别在于<strong>事务提交是否等待备库确认</strong>：同步等备库落盘才返回（不丢数据但更慢），异步本地落盘即返回（快但可能丢最近事务）。</p><ol><li><strong>异步（默认）</strong>：主库 commit 只写本地 WAL 即返回；主库崩溃时未传到备库的最近事务丢失（RPO&gt;0），但应用延迟不受备库影响。</li><li><strong>同步</strong>：synchronous_standby_names 指定同步备库；commit 等备库确认收到 WAL（remote_write=已写备库 OS 缓存 / on/remote_flush=已 fsync）才返回，RPO=0；代价是每次提交多一次网络往返，备库故障时主库写入会阻塞。</li><li><strong>quorum 模式</strong>：ANY n (standby1, standby2)：n 个备库确认即可，兼顾可用性与一致性。</li><li><strong>选型</strong>：金融交易同步（至少 2 个同步备库防单点阻塞）；日志、统计类异步。还可以按事务粒度设置（SET LOCAL synchronous_commit）灵活混用。</li><li><strong>面试收口</strong>：一致性、可用性、延迟的三角权衡，按业务 RPO/RTO 要求选档位。</li></ol>";

  A["pgPostgreSQL 如何实现故障切换？"] = "<p><strong>核心回答：</strong>故障切换 = <strong>故障检测 → 提升备库 → 客户端重路由</strong>三步，通常由 Patroni + etcd 这类 HA 方案自动完成。</p><ol><li><strong>故障检测</strong>：监控主库健康（心跳/SQL 探活）；etcd/DCS 中持有 leader 锁的节点失联即判定故障。</li><li><strong>提升备库（promote）</strong>：选 WAL 最新的备库执行 promote，备库退出恢复模式变成可写主库（pg_ctl promote 或 pg_promote()）。</li><li><strong>脑裂防护</strong>：Patroni 模式下旧主必须先释放 DCS 锁才能继续写，防止双主。</li><li><strong>流量切换</strong>：虚拟 IP 漂移、DNS 切换或代理层（HAProxy/PgBouncer）健康检查自动摘除旧主；应用重连即可。</li><li><strong>旧主处理</strong>：故障恢复后作为备库重新加入（pg_rewind 快速回退分歧 WAL），避免全量重建。</li><li><strong>工具生态</strong>：Patroni（最主流）、repmgr；Kubernetes 环境用 CloudNativePGOperator。RTO 通常可到 10-30 秒。</li></ol>";

  A["pgPostgreSQL 高可用架构如何设计？"] = "<p><strong>核心回答：</strong>经典架构是<strong>1 主 + 2 从（同步+异步）+ Patroni/etcd 自动故障转移 + PgBouncer 连接池 + 跨机房容灾</strong>。</p><ol><li><strong>复制拓扑</strong>：主库 → 1 个同步从库（保 RPO=0）+ 1 个异步从库（保可用性，同步从库故障时自动降级避免写入阻塞）；异地再加异步从库容灾。</li><li><strong>自动故障转移</strong>：Patroni 管理每个 PG 实例，etcd 存集群状态与 leader 锁；主故障 10 秒级完成选举提升。</li><li><strong>接入层</strong>：HAProxy/PgBouncer 前置，健康检查自动路由读写流量到主库、读流量到从库；应用配多个地址或用 DNS。</li><li><strong>备份体系</strong>：pg_basebackup 全量 + WAL 归档（pgBackRest/barman），支持 PITR 任意时间点恢复。</li><li><strong>监控</strong>：复制延迟、备库落后告警、主从切换演练常态化；监控指标：replay_lag、连接数、慢查询。</li><li><strong>扩展读写</strong>：读多场景从库水平扩（读副本）；写瓶颈走分区/分库（Citus）。</li></ol>";

  A["pgPostgreSQL 如何处理大规模数据？"] = "<p><strong>核心回答：</strong>单机内用<strong>分区表 + 索引优化 + VACUUM 调优</strong>扛住 TB 级；再往上<strong>分布式扩展（Citus）+ 冷热分离</strong>。</p><ol><li><strong>分区表（Declarative Partitioning）</strong>：按时间/范围/哈希把大表拆成子分区，查询裁剪（partition pruning）只扫相关分区；日志/流水类按月分区是标配。</li><li><strong>索引策略</strong>：BRIN 索引适合时序追加数据（体积小）；部分索引（partial index）只索引热点数据；大批量导入时先删索引后重建。</li><li><strong> Vacuum 治理</strong>：大表常量删除产生死元组，调 autovacuum_vacuum_scale_factor（按表覆写），监控 dead_tuples；冷表降低频率、热表提高频率。</li><li><strong>参数</strong>：max_wal_size 调大减少 checkpoint 频率；shared_buffers/effective_cache_size 匹配内存。</li><li><strong>冷热分离</strong>：历史数据归档到对象存储/数仓（如 ClickHouse 做分析），在线库只留热数据。</li><li><strong>水平扩展</strong>：写入超单机能力 → Citus 分片（下题）；或按业务垂直拆库。</li></ol>";

  A["pgPostgreSQL 分区表是什么？"] = "<p><strong>核心回答：</strong>分区表是把<strong>一张逻辑大表按规则拆成多个物理子表</strong>（分区），对应用透明；PG 10+ 支持声明式分区（范围、列表、哈希）。</p><ol><li><strong>三种分区类型</strong>：<pre><code>RANGE：按范围（时间最常用）\n  PARTITION BY RANGE (created_at)\nLIST：按离散值列表（地区、类型）\nHASH：按哈希取模打散（均匀写入）</code></pre></li><li><strong>用法</strong>：CREATE TABLE measurement (...) PARTITION BY RANGE (log_time)，再 CREATE TABLE measurement_y2026m01 PARTITION OF measurement FOR VALUES FROM ('2026-01-01') TO ('2026-02-01')；插入时自动路由到子分区。</li><li><strong>收益</strong>：a) 分区裁剪：查询条件带分区键只扫相关分区；b) 删除历史数据 = DROP 整个分区（秒级，无死元组）；c) 索引/维护按分区粒度进行。</li><li><strong>限制</strong>：分区键必须是主键/唯一约束的一部分；跨分区查询和唯一约束受限；分区数过多影响规划时间。</li><li><strong>实践</strong>：配合 pg_partman 自动创建未来分区和清理过期分区。</li></ol>";

  A["pgPostgreSQL Partition 和 MySQL Partition 有什么区别？"] = "<p><strong>核心回答：</strong>本质差异：<strong>PG 分区是真子表（继承体系），MySQL 分区是同一表内的数据组织方式</strong>；功能和运维手感都不同。</p><ol><li><strong>实现层次</strong>：PG 每个分区是独立的表（有自己名字、索引、存储、权限），父表只是路由入口；MySQL 分区是同一张表内部的逻辑划分（分区不是独立表对象）。</li><li><strong>索引</strong>：PG 每个分区建独立索引（可对不同分区差异化）；MySQL 是整表索引自动下推到分区，粒度统一。</li><li><strong>分区管理</strong>：PG 可以单独 DETACH/ATTACH 分区（方便归档迁移：detach 后当普通表搬走）；MySQL 分区管理用 ALTER TABLE ... DROP/TRUNCATE PARTITION，灵活性低一些。</li><li><strong>类型支持</strong>：两者都支持 range/list/hash；PG 11+ 支持分区表的主键/唯一约束/外键（有条件），MySQL 分区表限制外键不支持。</li><li><strong>性能</strong>：小规模分区两者相近；PG 分区数上千个时规划开销明显，MySQL 分区数上限 8192 但裁剪效率类似。</li><li><strong>生态</strong>：PG 有 pg_partman 自动化分区管理；MySQL 需要自写脚本或事件调度。</li></ol>";

  A["pgPostgreSQL 如何水平扩展？"] = "<p><strong>核心回答：</strong>PG 原生不提供分布式分片，水平扩展路径是<strong>读扩展（流复制副本）→ 分布式中间件/扩展（Citus）→ 业务层分库分表</strong>。</p><ol><li><strong>读扩展</strong>：多个流复制从库分担只读查询（写仍在主库），适合读多写少。</li><li><strong>Citus 扩展</strong>：把表按分布列（shard key）哈希分片到多个 worker 节点，协调节点改写 SQL 下推执行；分布式表支持并行查询，reference table 广播到所有节点做本地 join。</li><li><strong>业务分库分表</strong>：应用或代理层（如 ShardingSphere）按 tenant_id/order_id 拆分到多个 PG 实例；事务和跨片查询要在应用层处理。</li><li><strong>选型</strong>：读瓶颈 → 复制副本；写瓶颈且模式统一（SaaS 多租户、时序）→ Citus；写瓶颈且业务边界清晰 → 垂直拆库/分表。</li><li><strong>注意事项</strong>：分片键决定路由效率（跨片 join/事务昂贵）；全局唯一 ID 用序列号段或雪花算法。</li></ol>";

  A["pgCitus 是什么？"] = "<p><strong>核心回答：</strong>Citus 是 PG 的<strong>分布式扩展</strong>（微软开源，Azure 也有托管版），把单机 PG 变成可横向扩展的分布式集群：协调节点 + 多个 worker 节点，表按分布列分片。</p><ol><li><strong>架构</strong>：Coordinator（存元数据、解析改写 SQL、聚合结果）+ N 个 Worker（存真实分片数据，每个分片默认有副本）。</li><li><strong>表类型</strong>：a) distributed table：按 distribute_by column 哈希分片（大事实表）；b) reference table：全量复制到所有节点（小维表，本地 join 不跨网络）；c) local table：只存协调节点。</li><li><strong>查询执行</strong>：按分片键过滤的查询直接路由单分片；聚合查询下推各 worker 并行执行后汇总（并行加速明显）。</li><li><strong>典型场景</strong>：多租户 SaaS（tenant_id 分片，租户内查询全部单分片）、实时分析大屏（并行聚合）、时序海量写入。</li><li><strong>限制</strong>：跨分片分布式事务开销大；不按分片键的 join/更新要重分布数据；依赖 Citus 版本与 PG 版本匹配。</li></ol>";
})(window.OTHERS_AI);
