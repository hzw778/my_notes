window.OTHERS_AI = window.OTHERS_AI || {};
(function (A) {
  A["redisstreamRedis Stream 如何解决消息重复消费问题？"] = "<p><strong>核心回答：</strong>Redis Stream 本身是 at-least-once 语义（可能重复投递），解决重复靠<strong>消费端幂等</strong>：以 message ID 或业务键做去重，配合 pending 列表的 ACK 机制把重复窗口压到最小。</p><ol><li><strong>机制层面减重复</strong>：消费者用 XREADGROUP 读到消息后处理成功才 XACK；未 ACK 的消息留在 PEL，宕机后由原消费者 XPENDING+XCLAIM 认领重处理——重复只发生在“处理完但 ACK 前挂掉”的窗口。</li><li><strong>幂等去重（根本解法）</strong>：a) <strong>业务唯一键</strong>：消息体带业务 ID（订单号+事件类型），处理前查 Redis SETNX dedup:key 或数据库唯一约束，已处理直接 ACK 跳过；b) <strong>状态机</strong>：订单状态只能前进，重复事件被状态机拒绝；c) <strong>版本号</strong>：消息带递增版本，旧版本丢弃。</li><li><strong>去重键设计</strong>：dedup:{group}:{msgid} 设过期时间（如 24h），避免 key 堆积。</li><li><strong>和 Kafka 对比</strong>：Kafka 有幂等生产者+事务从生产端防重，Redis Stream 生产端 XADD 天然不重复（ID 自增），重复主要来自消费端重投——所以重点全在消费幂等。</li></ol>";

  A["redisstreamRedis Stream 为什么会出现重复消费？"] = "<p><strong>核心回答：</strong>重复的根源是<strong>“消息处理成功”和“ACK 落账”不是原子操作</strong>——两者之间消费者崩溃/网络分区，消息未 ACK 留在 PEL，之后被重投（重试或认领），就消费了第二次。</p><ol><li><strong>典型时序</strong>：<pre><code>1 XREADGROUP 读到 msg-1\n2 业务处理成功（比如已加积分）\n3 —— 此时消费者进程崩溃 ——\n4 XACK 未执行，msg-1 仍在 PEL\n5 重启/其他消费者 XCLAIM 认领重处理\n6 加积分执行了两次 → 重复消费</code></pre></li><li><strong>其他来源</strong>：a) 消费者处理超时，监控任务 XCLAIM 走了但原消费者其实还在处理（脑裂双处理）；b) 客户端重试逻辑在网络抖动时重读；c) 业务侧重试（外部调用失败重发触发重入）。</li><li><strong>为什么不做 exactly-once</strong>：处理外部副作用（写 DB、调接口）无法和 Redis ACK 绑成一个原子事务，任何 MQ 都做不到真全局精确一次，只能 at-least-once + 幂等。</li><li><strong>减少窗口</strong>：处理逻辑短平快、先处理再 ACK 顺序固定、XCLAIM 设置最小 idle 时间避免过早认领。</li></ol>";

  A["redisstreamRedis Stream 如何保证消费者幂等？"] = "<p><strong>核心回答：</strong>标准做法是<strong>“去重表 + 业务状态机”双保险</strong>：消息先查/写去重标记，标记新鲜才执行业务；业务本身用状态机或唯一约束兜底。</p><ol><li><strong>去重标记（第一层）</strong>：<pre><code>String dk = dedup:order:evt: + orderId + \":\" + eventType;\nif (redis.set(dk, 1, NX, EX, 86400) == null) { xack(...); return; } // 已处理\n// 执行业务...\n// 成功后 xack</code></pre>注意 setnx 和业务执行之间若崩溃，标记已存在业务没做——所以标记应设短 TTL 并作为<strong>快速过滤</strong>而非绝对真理。</li><li><strong>存储层唯一约束（第二层，最可靠）</strong>：业务表用唯一键（order_id + event_type）INSERT，重复直接违反约束失败捕获后 ACK；或 UPDATE ... WHERE status=旧状态 的条件更新，影响行数 0 说明已处理。</li><li><strong>状态机</strong>：订单 待支付→已支付→已发货，重复的支付事件因状态已流转被拒绝。</li><li><strong>原则</strong>：幂等键要在<strong>消息生产时</strong>就带业务语义 ID，不要只用 Stream 消息 ID（重投的消息 ID 不变，可用，但业务重建消息时 ID 会变）。</li><li><strong>测试</strong>：混沌验证——手动对同一消息调两次处理函数，断言副作用只发生一次。</li></ol>";

  A["redisstreamRedis Stream 消费者宕机后消息如何恢复？"] = "<p><strong>核心回答：</strong>未 ACK 的消息留在该消费者的 <strong>PEL（Pending Entries List）</strong> 里不丢失；恢复方式是<strong>原消费者重启续读，或新消费者用 XPENDING+XCLAIM/XAUTOCLAIM 接管</strong>。</p><ol><li><strong>PEL 机制</strong>：XREADGROUP 分配消息时记录 (消息ID, 消费者名, 投递次数, idle 时间)；消费者挂了，PEL 条目还在，消息不会投给别人（除非被认领）。</li><li><strong>原消费者恢复</strong>：重启后用 XREADGROUP ... 0（ID 传 0）重读自己的 pending 消息，重新处理并 ACK，然后继续 &gt; 读新消息。</li><li><strong>转移接管</strong>：消费者永久下线时，监控程序或备用消费者执行：<pre><code>XPENDING mystream mygroup          -- 看 PEL 明细\nXCLAIM mystream mygroup consumerB 600000 msgId\n-- idle 超 600000ms 的消息转移给 consumerB\n-- 或用 XAUTOCLAIM min-idle-time 自动批量认领</code></pre></li><li><strong>投递上限</strong>：XPENDING 里的 delivery count 超过阈值（如 3 次）应转人工/死信处理（Redis 无内置 DLQ，自己建 dead stream XADD）。</li><li><strong>要点</strong>：恢复逻辑要考虑消息可能已处理过（宕机在 ACK 前）——所以恢复消费也要走幂等。</li></ol>";

  A["redisstreamRedis Stream 消息积压如何处理？"] = "<p><strong>核心回答：</strong>分三步：<strong>定位慢点（消费逻辑 or 认领故障）→ 提升消费能力 → 极端时转移或跳过</strong>，并建立监控防复发。</p><ol><li><strong>观测</strong>：XLEN 看流长度、XPENDING 看积压数量和 idle 分布、XINFO GROUPS 看 lag；判断是消费速度跟不上（正常积压）还是消费者挂了没人认领（pending 巨大且 idle 高）。</li><li><strong>消费故障恢复</strong>：消费者宕机导致 pending 积压 → 新消费者 XAUTOCLAIM 认领接管；注意 delivery count 过高的消息转死信。</li><li><strong>提速</strong>：a) 增加消费者（多实例分摊，注意单 key 是单节点，单 stream 的消费并行受节点限制）；b) 消费逻辑批量处理 + 异步化重 IO；c) 拆分热点——生产端按业务分片到多个 stream（mystream:0..15），并行度立刻翻倍。</li><li><strong>紧急转移</strong>：积压巨大时起“搬运工”消费者快速读出并落库/写入新 stream，慢慢离线消化。</li><li><strong>容量治理</strong>：XADD 加 MAXLEN ~ 100000（近似裁剪）防内存打爆；设置 stream 水位告警（XLEN/XPENDING 阈值）。</li></ol>";

  A["redisstreamRedis Stream 底层使用什么结构存储数据？"] = "<p><strong>核心回答：</strong>底层是 <strong>radix tree（基数树）挂 listpack（紧凑列表）节点</strong>：每个 Stream 是一棵 radix tree，key 是消息 ID，value 是存一批消息的 listpack，兼顾按 ID 范围查找和内存紧凑。</p><ol><li><strong>两层结构</strong>：a) <strong>Rax（radix tree）</strong>：前缀压缩的字典树，用于按 ID 前缀快速定位到宏节点，支持范围迭代（XRANGE 从任意 ID 开始扫）；b) <strong>listpack</strong>：连续内存紧凑数组，一个节点存几十上百条完整消息（ID + field-value 对），省内存且顺序写友好。</li><li><strong>消息定位</strong>：XRANGE start end 时 rax 定位到 start 所在 listpack 节点，在节点内线性扫，跨节点沿树迭代——范围读 O(logN)+节点内 O(k)。</li><li><strong>新增消息</strong>：XADD 追加到 rax 最右侧节点的 listpack，满了（默认约 4KB 或条数上限）封新节点——追加写高效。</li><li><strong>删除</strong>：XDEL 标记删除（置 null），节点整体废弃后回收（lazy free）；XTRIM 从头裁剪老节点。</li><li><strong>Group/PEL 存储</strong>：consumer group 元数据和 pending 条目挂在 stream 对象上（各自 rax），所以 XPENDING 也是高效结构。</li><li><strong>面试加分</strong>：对比 List（无分组无 ack）、Pub/Sub（不持久化）——Stream 的数据结构是专门为“持久化 + 按 ID 索引 + 分组消费”设计的。</li></ol>";

  A["redisstreamRedis Stream 为什么能够高性能写入？"] = "<p><strong>核心回答：</strong>高性能写入来自<strong>纯内存 + 追加写（append-only）+ listpack 紧凑节点 + ID 单调生成免冲突</strong>四点，单节点轻松十万级 QPS。</p><ol><li><strong>纯内存操作</strong>：XADD 只是内存里追加，无磁盘 fsync 阻塞（持久化异步 RDB/AOF，权衡可靠性）。</li><li><strong>追加写局部性好</strong>：新消息总是加到 rax 最右侧节点的 listpack 尾部，连续内存顺序写，无随机 IO、无树再平衡成本（对比 B+ 树插入分裂）。</li><li><strong>listpack 紧凑</strong>：一批消息连续存放，一次分配多次使用，内存碎片少、缓存友好。</li><li><strong>ID 生成零协调</strong>：ms-time-seq 格式由 Redis 单线程生成，天然递增免冲突，客户端不用协商（对比分布式 ID 系统的额外开销）。</li><li><strong>单线程无锁</strong>：命令串行执行无锁竞争；读（XREAD）阻塞等待新数据（阻塞 API 由事件循环唤醒），没有轮询开销。</li><li><strong>瓶颈与上限</strong>：单 stream 在单节点，写入上限=该节点内存和 CPU；超高吞吐要业务分片多 stream/多实例，或换 Kafka。</li></ol>";

  A["redisstreamRedis Stream 和 Kafka Partition 的设计思想有什么区别？"] = "<p><strong>核心回答：</strong>一句话：<strong>Kafka Partition 是“磁盘日志 + 消费者拉取 + offset 自管理”的流平台设计；Redis Stream 是“内存日志 + 服务器侧消费组状态”的轻量队列设计</strong>——可靠性与吞吐的取舍方向不同。</p><ol><li><strong>存储介质</strong>：Kafka 消息落磁盘（顺序写+页缓存+零拷贝），容量按 TB 规划，可保留天/周；Redis Stream 在内存（RDB/AOF 只是持久化手段），容量受内存限制，积压能力有限。</li><li><strong>消费位移</strong>：Kafka offset 由<strong>消费者自己管理提交</strong>（可重置回放任意位置）；Redis Stream 的消费进度（last_delivered_id）和 PEL 状态记在<strong>服务端 group</strong>里，重放要靠 XSETID 等特殊操作，不如 Kafka 灵活。</li><li><strong>投递模型</strong>：Kafka 消费者主动 fetch 拉批，吞吐大；Redis Stream 是 XREADGROUP（可阻塞轮询），服务端维护“谁拿了什么没确认”（PEL），认领/转移语义更接近传统 MQ。</li><li><strong>副本与容错</strong>：Kafka 多副本 ISR 机制+acks=all，节点故障数据不丢；Redis 主从异步复制，故障切换可能丢最近写入（replica 无磁盘持久化时）——可靠性档位不同。</li><li><strong>生态与规模</strong>：Kafka 有 Streams/Connect/Flink 生态，百万级 msg/s，分区数水平扩展；Redis Stream 十万级，胜在零额外部署、延迟低、API 简单。</li><li><strong>选型</strong>：轻量任务队列/已有 Redis/日百万内消息 → Stream；大数据管道/流计算/需回放 → Kafka。</li></ol>";
})(window.OTHERS_AI);
